---
milestone: v6.0
name: Report Revision
status: complete
started: 2026-07-21
completed: 2026-07-21
phases: 35, 36, 37
---

# v6.0 — Report Revision

## Scope

Revise the thesis report to close the gaps judges raised at the 2026-07-15 defense (`documents/Transcript defense.md`), without changing the report's existing voice — a sudden tone shift would itself read as confirming AI authorship, the exact suspicion this milestone had to defuse. The dominant, most-repeated transcript gap (~10 phrasings) was that the training-label mechanism could not be located or explained live; that gap, more than tone, triggered the suspicion. Slides are LOCKED and out of scope; only the thesis report was touched.

## What shipped

**Phase 35 — Problem Framing & Architecture Rationale:**

- Chapter I/Introduction: explicit statement that the core learning problem is supervised 4-class text classification, with the label field's role named before any architecture discussion (`FRAME-01`)
- Chapter III/Materials and Methods: new paragraph explaining generative classification via a verbalizer (decoder emits the class label as generated text) versus a classification head, citing the T5 text-to-text framing, PET's verbalizer concept, and WT5's joint label+explanation faithfulness argument — directly answers the judges' "additional layers on QLoRA" question (`ARCH-01`)
- Chapter III: honest Qwen-vs-PhoBERT comparison — PhoBERT's real strengths acknowledged (Vietnamese-specialized, cheaper, well-precedented for single-label output), the actual reason it wasn't chosen stated plainly (no decoder, no native path to evidence-span/recommendation generation), referencing the report's existing comparison table rather than duplicating it (`ARCH-02`)
- 5 new WebSearch-verified BibTeX entries added: `raffel2020t5`, `schick2021pet`, `narang2020wt5`, `yousefiramandi2025finetuning`, `tran2021bartpho`

**Phase 36 — Dataset Construction & Labeling Methodology:**

- New record-schema table (`tables/record_schema.tex`) naming all 7 dataset fields, with the `label` field's ground-truth role stated explicitly — this is the exact thing judges said they couldn't find
- Explicit statement that labels are assigned at generation time via class-conditioned generation (the pipeline is invoked once per target class), not a separate manual pass, with citation to the same label-conditioned-generation precedent already used elsewhere in the report
- A real record walked through field by field (pulled from the actual validation split, not invented)
- Explicit distinction between the label's role during training (part of the target sequence) versus validation/test (withheld from the model, used only to score the generated output)

**Phase 37 — Consistency Audit & Citation Verification:**

- Split/confusion-matrix counts audited across every table, appendix, and chapter — found already fully consistent (56/75/62/61 per class, 9 errors, 254/2,333/413 splits match everywhere); no changes needed
- New error-analysis subsection in Chapter IV/Results: names the actual 9 misclassified validation rows (pulled from `data/eval/eval-results-qlora.json` and the real validation split text) and shows the errors are a defensible class-boundary overlap in the synthetic corpus (bank-notice-style phrasing generated under `task_scam`/`zalo_social_engineering` targets), not random model confusion
- SHA-256/manifest-integrity sentence confirmed already crisp and explicit; no change needed
- All new academic claims confirmed cited with verified BibTeX entries; existing citations in touched sections spot-checked, no corrections needed
- New prose checked against `WRITING_GUARDRAILS.md` tone rules (short declarative sentences, no inflated/AI-cliche phrasing) — clean

## Requirements delivered

FRAME-01, ARCH-01, ARCH-02, LABEL-01 through LABEL-04, AUDIT-01 through AUDIT-03, CITE-01, CITE-02, VOICE-01 — 13/13.

## Verification

Full safe compile sequence (delete aux + 3x XeLaTeX + 1x BibTeX) run after each phase and once at milestone close: zero errors, zero undefined references/citations, 34 pages.
