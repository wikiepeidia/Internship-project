---
phase: 38-corpus-repair-split-governance
plan: 02
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - data/splits/phase38-corpus-repaired-v2/train.jsonl
  - data/splits/phase38-corpus-repaired-v2/val.jsonl
  - data/splits/phase38-corpus-repaired-v2/test.jsonl
  - data/manifests/manifest-phase38-corpus-repaired-v2.json
  - tests/data_pipeline/test_repair_corpus_full_scale.py
  - .planning/phases/38-corpus-repair-split-governance/38-recovery-narrative-task-scam.md
autonomous: true
requirements: [DATA-04, DATA-05, DATA-06, DATA-07, DATA-08]

must_haves:
  truths:
    - "Every seed_id in the final repaired corpus appears in exactly one of train.jsonl/val.jsonl/test.jsonl — zero cross-split leakage (DATA-04)"
    - "No seed_id accounts for more than 8% of the final repaired corpus row count; seed_1a4f7d4d7c53 (24.41% pre-repair) and seed_157ce0adb043 (11.90% pre-repair) are both measurably reduced below 8%, with before/after percentages recorded in the manifest (DATA-05)"
    - "Every suspicious_spans entry in every row of the final corpus is an exact substring of that row's text — zero invalid evidence spans remain (DATA-06)"
    - "The manifest records the locked 80/10/10 split ratio and per-split, per-class row counts for all four labels (DATA-07)"
    - "A task_scam 0.44->0.871 recovery-narrative markdown snippet exists, grounded in real Phase 7a evidence artifacts, ready for Phase 42 to place (DATA-08)"
  artifacts:
    - data/splits/phase38-corpus-repaired-v2/train.jsonl
    - data/splits/phase38-corpus-repaired-v2/val.jsonl
    - data/splits/phase38-corpus-repaired-v2/test.jsonl
    - data/manifests/manifest-phase38-corpus-repaired-v2.json
    - tests/data_pipeline/test_repair_corpus_full_scale.py
    - .planning/phases/38-corpus-repair-split-governance/38-recovery-narrative-task-scam.md
  key_links:
    - "main() in src/data_pipeline/repair_corpus_split_governance.py (built in 38-01) reads the two original real input files and writes the new versioned split+manifest outputs"
    - "data/synthetic/recovered-balanced.jsonl and data/splits/recovered-balanced/{train,val,test}.jsonl remain byte-unchanged after this plan runs, proving the old lineage was kept as backup, not overwritten, per CONTEXT.md"
---

<objective>
Run the Plan 38-01 pipeline against the REAL full pooled corpus (3,000 rows +
413 reserved rows = 3,413 rows), produce the final repaired split files and
manifest, prove every DATA-04 through DATA-07 acceptance gate against the
actual written output (not a subset or a mock), and draft the evidence-grounded
task_scam 0.44->0.871 recovery narrative for Phase 42 to place.

Purpose: this is where the phase's 5 ROADMAP success criteria become
independently verifiable facts about real files on disk, not just passing
unit tests against fixtures.
Output: `data/splits/phase38-corpus-repaired-v2/{train,val,test}.jsonl`,
`data/manifests/manifest-phase38-corpus-repaired-v2.json`, an automated
full-scale acceptance-gate test suite, and the recovery-narrative markdown
snippet.
</objective>

<!-- Artifacts this phase produces (this plan's share) -->
## Artifacts This Phase Produces

- **New data files** `data/splits/phase38-corpus-repaired-v2/train.jsonl`, `val.jsonl`, `test.jsonl` — the final repaired, seed-disjoint, cap-enforced, 80/10/10 split corpus (supersedes `data/splits/recovered-balanced/` as the canonical corpus for Phase 39/40; old files kept as backup per CONTEXT.md, not deleted).
- **New manifest** `data/manifests/manifest-phase38-corpus-repaired-v2.json` — composed via `build_repair_manifest()` (built in 38-01), recording SHA-256 file hashes, the 80/10/10 ratio, per-split per-class counts, and `repair_stats` (rows pooled/repaired/dropped, seed concentration before/after).
- **New file** `tests/data_pipeline/test_repair_corpus_full_scale.py` — automated post-condition test suite proving DATA-04 through DATA-07 against the real written output files.
- **New file** `.planning/phases/38-corpus-repair-split-governance/38-recovery-narrative-task-scam.md` — the drafted, evidence-cited task_scam 0.44->0.871 recovery narrative for Phase 42 (DATA-08).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-corpus-repair-split-governance/38-CONTEXT.md
@.planning/phases/38-corpus-repair-split-governance/38-RESEARCH.md
@.planning/phases/38-corpus-repair-split-governance/38-01-SUMMARY.md
@src/data_pipeline/repair_corpus_split_governance.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the repair pipeline at full 3,413-row scale and prove every DATA-04 through DATA-07 acceptance gate</name>
  <files>data/splits/phase38-corpus-repaired-v2/train.jsonl, data/splits/phase38-corpus-repaired-v2/val.jsonl, data/splits/phase38-corpus-repaired-v2/test.jsonl, data/manifests/manifest-phase38-corpus-repaired-v2.json, tests/data_pipeline/test_repair_corpus_full_scale.py</files>
  <precondition>data/synthetic/recovered-balanced.jsonl exists with 3,000 rows and data/splits/recovered-balanced/test.jsonl exists with 413 rows, matching the 38-RESEARCH.md 2026-08-08 snapshot (verify via `python -c "print(sum(1 for _ in open('data/synthetic/recovered-balanced.jsonl', encoding='utf-8')))"` and the same for the reserved test file before running main()); halt and report if either count differs.</precondition>
  <read_first>
    src/data_pipeline/repair_corpus_split_governance.py (the exact CLI flags and function names built in 38-01 — read the actual `main()`/argparse definition, do not assume flag names);
    .planning/phases/38-corpus-repair-split-governance/38-CONTEXT.md (locked decisions: new versioned output path, old files kept as backup, exact-substring span validation);
    .planning/phases/38-corpus-repair-split-governance/38-RESEARCH.md (the exact real measurements table: pooled total 3,413 rows / 87 unique seed_ids / 10 shared-seed leakage rows, dominant seed seed_1a4f7d4d7c53=833 rows=24.41%, second over-cap seed seed_157ce0adb043=406 rows=11.90%, 131 invalid-span rows in the 3,000-row file + 40 more in the 413-row file = 171 total bad rows to repair)
  </read_first>
  <action>
    Run `python -m src.data_pipeline.repair_corpus_split_governance --input-main data/synthetic/recovered-balanced.jsonl --input-reserved data/splits/recovered-balanced/test.jsonl --output-dir data/splits/phase38-corpus-repaired-v2 --version-tag phase38-corpus-repaired-v2 --cap-pct 0.08 --split-ratios 0.8,0.1,0.1` against the full real pooled corpus (do not slice or sample — the entire 3,413-row pool). Confirm the run completes without error and writes `train.jsonl`, `val.jsonl`, `test.jsonl` under `data/splits/phase38-corpus-repaired-v2/` plus `data/manifests/manifest-phase38-corpus-repaired-v2.json`.
    Then write `tests/data_pipeline/test_repair_corpus_full_scale.py` with automated post-condition checks that read the ACTUAL written output files from disk (not mocks, not the 38-01 fixtures) and assert, one test per gate:
    (1) DATA-04 — collect every `seed_id` present in each of the three split files; assert the three sets are pairwise disjoint (no `seed_id` appears in more than one file).
    (2) DATA-05 — count rows per `seed_id` across all three combined split files, divide by the combined total row count, and assert every resulting percentage is `<= 0.08`; additionally load `data/manifests/manifest-phase38-corpus-repaired-v2.json` and assert its `repair_stats.seed_concentration_before` records `seed_1a4f7d4d7c53` at approximately 24.41% and `seed_157ce0adb043` at approximately 11.90%, and `repair_stats.seed_concentration_after` shows both now `<= 0.08`.
    (3) DATA-06 — for every row in all three split files, assert every string in `suspicious_spans` is an exact substring (`span in row["text"]`) of that row's `text`; assert zero violations across the full combined corpus.
    (4) DATA-07 — load the manifest and assert `split_class_distribution` contains all three split names, each with non-zero counts for all four labels (`bank_impersonation`, `zalo_social_engineering`, `task_scam`, `benign`), and that the manifest records the ratio `(0.8, 0.1, 0.1)`.
    (5) Backup preservation — assert `data/synthetic/recovered-balanced.jsonl` still has exactly 3,000 lines and `data/splits/recovered-balanced/test.jsonl` still has exactly 413 lines (confirming the old lineage was read but never overwritten).
  </action>
  <verify>python -m pytest tests/data_pipeline/test_repair_corpus_full_scale.py -v</verify>
  <acceptance_criteria>
    All 5 acceptance-gate tests in tests/data_pipeline/test_repair_corpus_full_scale.py pass against the real full-scale output;
    a script run confirms zero seed_id appears in more than one of data/splits/phase38-corpus-repaired-v2/{train,val,test}.jsonl;
    data/manifests/manifest-phase38-corpus-repaired-v2.json exists and its repair_stats.seed_concentration_after values for seed_1a4f7d4d7c53 and seed_157ce0adb043 are both <= 0.08, with the corresponding seed_concentration_before values approximately matching 0.2441 and 0.1190;
    a full scan of all three output files finds zero rows where any suspicious_spans entry is not an exact substring of that row's text;
    data/synthetic/recovered-balanced.jsonl (3,000 lines) and data/splits/recovered-balanced/test.jsonl (413 lines) are confirmed unchanged after the run.
  </acceptance_criteria>
  <done>data/splits/phase38-corpus-repaired-v2/{train,val,test}.jsonl and data/manifests/manifest-phase38-corpus-repaired-v2.json exist from a real full-scale run; tests/data_pipeline/test_repair_corpus_full_scale.py passes all 5 gate tests (zero cross-split seed_id leakage, both over-cap seeds reduced below 8% with before/after recorded, zero invalid spans, manifest records 80/10/10 ratio + per-split per-class counts, old backup files unchanged).</done>
</task>

<task type="auto">
  <name>Task 2: Draft the task_scam 0.44->0.871 recovery narrative, grounded in real Phase 7a evidence</name>
  <files>.planning/phases/38-corpus-repair-split-governance/38-recovery-narrative-task-scam.md</files>
  <read_first>
    .planning/phases/07a-task-scam-recall-recovery/07a-CONTEXT.md (original 0.44 recall, root cause, the 750-original-row narrow-coverage diagnosis, the relaxed >=0.80 floor decision, the gate-bug description);
    .planning/phases/07a-task-scam-recall-recovery/07a-01-SUMMARY.md (the 5 named scenario axes used for the 400 new generated rows, the gate-bug fix description);
    documents/reports/latex/tables/dataset_statistics.tex (the compiled 0.871 recall figure and 62 held-out support count);
    documents/reports/latex/chapters/05_evaluation_and_discussion.tex (the compiled narrative context around the 0.871 result);
    .planning/phases/38-corpus-repair-split-governance/38-RESEARCH.md "Recovery narrative source data" table (the exact verified fact/value/source triples to cite — do not use any number not listed there)
  </read_first>
  <action>
    Write a standalone markdown file at `.planning/phases/38-corpus-repair-split-governance/38-recovery-narrative-task-scam.md` containing 3-5 short paragraphs, ready to paste into the report's Data Construction chapter during Phase 42, covering in order: (a) the original task_scam recall of 0.44 pre-recovery and the evaluation-gate bug that let `audit.ready` stay `true` despite it; (b) the root cause — the original 750 task_scam rows were narrow in scenario coverage and linguistically too close to benign messages; (c) the fix — 400 new targeted task_scam rows generated across the 5 named scenario axes from `07a-01-SUMMARY.md` (like/follow/comment farms, Shopee/Lazada review-bombing, crypto referral schemes, fake purchase seeding, Zalo/Telegram livestream engagement), added via a conditional prompt block in `src/data_pipeline/generation/prompts.py`, retrained and registered as adapter `task-scam-recovery-2026-05-28`; (d) the result — recall recovered to 0.871 on 62 held-out validation examples, clearing the relaxed >=0.80 floor. Every factual claim (0.44, 750, 400, the 5 scenario axis names, 0.871, 62, the adapter tag, the gate-bug description) must carry an inline citation note in the form `[source: <file>]` pointing to one of the read_first artifacts — do not invent or round any number beyond what 38-RESEARCH.md's source table lists.
  </action>
  <verify>python -c "content = open('.planning/phases/38-corpus-repair-split-governance/38-recovery-narrative-task-scam.md', encoding='utf-8').read(); required = ['0.44', '0.871', '62', '750', '400', 'task-scam-recovery-2026-05-28']; missing = [r for r in required if r not in content]; print('MISSING:', missing) if missing else print('ALL PRESENT')"</verify>
  <acceptance_criteria>
    .planning/phases/38-corpus-repair-split-governance/38-recovery-narrative-task-scam.md exists;
    the verify command above prints "ALL PRESENT" (all six required numeric/tag anchors — 0.44, 0.871, 62, 750, 400, task-scam-recovery-2026-05-28 — are present in the file);
    every paragraph carries at least one `[source: <file>]` citation note pointing to one of the read_first artifacts;
    no number appears in the narrative that is absent from 38-RESEARCH.md's "Recovery narrative source data" table.
  </acceptance_criteria>
  <done>.planning/phases/38-corpus-repair-split-governance/38-recovery-narrative-task-scam.md exists with all six required anchors present and every claim source-cited, ready for Phase 42 to paste into the report.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/data_pipeline/test_repair_corpus_full_scale.py -v` — all 5 acceptance-gate tests pass against the real full-scale written output.
Run the Task 2 verify one-liner against `38-recovery-narrative-task-scam.md` — prints "ALL PRESENT".
Run `python -m pytest tests/data_pipeline/ -v` — the full data_pipeline test suite (38-01's new tests, 38-02's new full-scale tests, and every pre-existing test) passes with zero regressions.
Manually diff `data/synthetic/recovered-balanced.jsonl` and `data/splits/recovered-balanced/*.jsonl` against their pre-phase-38 git history (`git diff HEAD -- data/synthetic/recovered-balanced.jsonl data/splits/recovered-balanced/`) — expect zero diff, confirming the old lineage was preserved as backup, not overwritten.
</verification>

<success_criteria>
All 5 ROADMAP Phase 38 success criteria are independently provable against real files on disk: zero seed_id crosses a split boundary; the two previously over-cap seeds are both measurably reduced below 8% with before/after recorded in the manifest; zero invalid evidence spans remain anywhere in the final corpus; the manifest records the 80/10/10 ratio and per-split per-class counts for all four labels; and the task_scam 0.44->0.871 recovery narrative is drafted and evidence-cited, ready for Phase 42.
</success_criteria>

<output>
Create `.planning/phases/38-corpus-repair-split-governance/38-02-SUMMARY.md` when done
</output>
