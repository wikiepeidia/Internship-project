---
phase: 40-multi-model-training-evidence
plan: 03
type: execute
wave: 3
depends_on:
  - 40-02
files_modified:
  - src/model_adaptation/phobert_training.py
  - src/model_adaptation/phase40_notebooks.py
  - src/model_adaptation/cli.py
  - notebooks/phase40/qwen_lora_colab.ipynb
  - notebooks/phase40/qwen_qlora_colab.ipynb
  - notebooks/phase40/phobert_colab.ipynb
  - tests/model_adaptation/test_phase40_phobert.py
  - tests/model_adaptation/test_phase40_notebooks.py
autonomous: true
requirements:
  - TRAIN-03
  - TRAIN-04
  - TRAIN-05
  - TRAIN-06
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "PhoBERT is a genuine four-logit classification model with its encoder and head fully trainable, deterministic Vietnamese preprocessing, and no PEFT or quantization path."
    - "PhoBERT consumes the same canonical train/validation identities, locked label order, shared validation evaluator, and evidence lifecycle as the two Qwen runs."
    - "PhoBERT and both Qwen producers consume the exact validation-row IDs carried by one immutable canonical snapshot; no notebook or backend derives replacement IDs from parsed rows."
    - "Every declared PhoBERT evaluation checkpoint and the final step retain ordered raw validation predictions and metrics, then use the same recall-floor, macro-F1, risky-to-benign, and earlier-step selection contract as Qwen."
    - "Three fresh Colab notebooks are thin operator controllers over repository CLI/module APIs; none contains a second trainer, parser, evaluator, or graph implementation."
    - "Each notebook verifies a pinned repository-relative source archive and deterministic inventory hash before extraction/import, while a Git commit identifier remains optional rather than required."
    - "Each notebook mounts/copies one request-pinned train/validation-only archive at fixed Colab/Drive paths, verifies its archive hash, exact three-member inventory, internal manifest, counts, and member hashes before either JSONL member opens, and never refers to a laptop-relative data path."
    - "Static notebook validation blocks stale data paths, implicit modes, unpinned dependencies/models, unverified code artifacts, embedded secrets, and any reserved-partition reader before external execution."
  artifacts:
    - path: "src/model_adaptation/phobert_training.py"
      provides: "Full PhoBERT sequence-classification backend and preprocessing evidence"
      exports: ["PhoBertTrainingConfig", "segment_for_phobert", "run_phobert_training"]
    - path: "src/model_adaptation/phase40_notebooks.py"
      provides: "Static validator for the three canonical external-run controllers"
      exports: ["validate_phase40_notebook", "validate_phase40_notebooks"]
    - path: "notebooks/phase40/qwen_lora_colab.ipynb"
      provides: "Fresh full Qwen LoRA operator workflow"
    - path: "notebooks/phase40/qwen_qlora_colab.ipynb"
      provides: "Fresh full genuine-Qwen-QLoRA operator workflow"
    - path: "notebooks/phase40/phobert_colab.ipynb"
      provides: "Fresh full PhoBERT classification-head operator workflow"
  key_links:
    - from: "src/model_adaptation/phobert_training.py"
      to: "src/model_adaptation/phase40_contract.py"
      via: "the classifier backend receives only already-preflighted canonical train/validation records"
      pattern: "Phase40DataContract"
    - from: "src/model_adaptation/phobert_training.py"
      to: "src/model_adaptation/phase40_metrics.py"
      via: "four-logit argmax rows copy the immutable validation snapshot IDs at every declared checkpoint and final step before feeding the shared evaluator and deterministic selector"
      pattern: "validation_row_id|evaluate_phase40_predictions|select_phase40_checkpoint"
    - from: "notebooks/phase40/*.ipynb"
      to: "src/model_adaptation/cli.py"
      via: "notebooks invoke positive-mode preflight/run/verify/graph/export commands only"
      pattern: "phase40-"
    - from: "data/models/phase40/full-run-request.json"
      to: "notebooks/phase40/*.ipynb"
      via: "each notebook reads the frozen source-archive path and SHA-256 plus inventory-manifest SHA-256, verifies both before extraction/import, and records them in run evidence"
      pattern: "source_bundle|source_archive_sha256|source_inventory_sha256"
    - from: "data/models/phase40/full-run-request.json"
      to: "notebooks/phase40/*.ipynb"
      via: "each notebook uses the fixed Drive archive and verified extraction root, calls the automated input-bundle verifier before any train/validation member open, and carries its manifest/member/snapshot identities into run evidence"
      pattern: "phase40-verify-input-bundle|/content/drive/MyDrive/internship-phase40/phase40-train-validation.zip|/content/phase40-input-v1"
  prohibitions:
    - value: "Applying LoRA or QLoRA to PhoBERT merely to create another quantized run"
      status: unverified
    - value: "Copying training, validation parsing, metric calculation, or graph arrays into notebook cells"
      status: unverified
    - value: "Using historical recovered-balanced or provider-specific corpus paths in any Phase 40 notebook"
      status: unverified
    - value: "Embedding credentials, tokens, personal paths, external inference calls, or the held-out Phase 41 partition in notebook source"
      status: unverified
---

<objective>
Add the independently trained encoder baseline and freeze three safe, canonical external-run controllers.

Purpose: Answer the Qwen-versus-PhoBERT question with a real ordinary classifier baseline while preventing Colab notebooks from becoming unaudited forks of the repository training/evidence logic.
Output: PhoBERT classification backend, deterministic preprocessing/evidence tests, three fresh Colab notebooks, and a static notebook validator.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-multi-model-training-evidence/40-CONTEXT.md
@.planning/phases/40-multi-model-training-evidence/40-AI-SPEC.md
@.planning/phases/40-multi-model-training-evidence/40-RESEARCH.md
@.planning/phases/40-multi-model-training-evidence/40-PATTERNS.md
@.planning/phases/40-multi-model-training-evidence/40-01-SUMMARY.md
@.planning/phases/40-multi-model-training-evidence/40-02-SUMMARY.md

<interfaces>
`PhoBertTrainingConfig` carries model ID `vinai/phobert-base-v2`, immutable revision `e966aac8cb889325e073aa5f28ff70aca4dbc8c3`, the locked four-label mapping, max length 256, seed/data seed 42, full-run identity, and explicit encoder hyperparameters. It cannot represent PEFT, LoRA, QLoRA, or a probe.

`segment_for_phobert(raw_text)` returns raw text, deterministic segmented text, token-count/truncation metadata, and preprocessor version/hash. The training backend receives the Phase 40 preflight contract with immutable train/validation snapshots and uses `AutoModelForSequenceClassification(..., num_labels=4, id2label=..., label2id=...)` with encoder and head trainable. It copies each `validation_row_id` from `contract.validation_snapshot` into raw predictions; it never derives IDs from `DatasetRecord`, raw text, or its own loop counter.

PhoBERT evaluates at every configured evaluation checkpoint and at the final optimizer step. Each checkpoint atomically retains one canonical-order raw prediction row per validation ID with the four raw logits, argmax state, gold label, artifact identity, and optimizer step; it derives shared metrics from those rows, then calls `select_phase40_checkpoint` with the same risky-class recall floors, zero-invalid admission, macro-F1 ranking, risky-to-benign tie-break, and earlier-step tie-break used for Qwen. Run evidence records all checkpoint candidates and the selected-checkpoint rationale, including the visibly failed safety fallback when no candidate passes.

Each notebook calls the same CLI surface: immutable run-request verification, source-archive/inventory hash verification before extraction or import, fixed Drive mount/copy, automated input-archive verification before either data member opens, mode-specific doctor, explicit fresh full run from the verified extraction root, evidence verification, graph generation, and artifact export. The input archive path is exactly `/content/drive/MyDrive/internship-phase40/phase40-train-validation.zip`; its verified extraction root is exactly `/content/phase40-input-v1`; and its only members are `phase40-input-manifest.json`, `train.jsonl`, and `val.jsonl`. The verifier checks the request-pinned archive/manifest/member identities, rejects extra/test-like/traversal/symlink members, and opens no JSONL member until those checks pass. Notebook commands receive verified contract/snapshot output rather than laptop-relative `data/splits/...` paths. The source archive is repository-relative and pinned by SHA-256 plus a deterministic inventory-manifest SHA-256 in `full-run-request.json`; those identities are retained in every run bundle. A Git commit identifier may be recorded as optional context but is never required for execution or evidence validity. Installation/model acquisition cells remain operator-authorized and are not executed by this plan.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Run one fake PhoBERT row through segmentation, four logits, metrics, and evidence</name>
  <read_first>
    - `src/model_adaptation/training.py`
    - `src/model_adaptation/schemas.py`
    - `src/model_adaptation/phase40_contract.py`
    - `src/model_adaptation/phase40_metrics.py`
    - `src/model_adaptation/phase40_evidence.py`
  </read_first>
  <files>src/model_adaptation/phobert_training.py, src/model_adaptation/cli.py, tests/model_adaptation/test_phase40_phobert.py</files>
  <behavior>
    - A fake tokenizer/classifier receives deterministic segmented Vietnamese and emits exactly four logits in the locked label order.
    - All encoder and classification-head parameters are trainable; importing or invoking PEFT/bitsandbytes causes the test to fail.
    - Raw and segmented text, segmenter version/hash, token count, truncation flag/row IDs, model revision, and label mapping enter evidence.
    - Max length is 256 with dynamic padding; an empty dataset, empty segmented output, invalid UTF-8 upstream row, wrong logit width, or label-map drift fails without dropping a row.
    - At every declared evaluation checkpoint and the final optimizer step, exactly one raw prediction row per canonical validation ID persists in order with four unmodified logits, argmax state, gold label, artifact identity, and checkpoint step; missing, duplicate, reordered, or partial rows fail that checkpoint.
    - Every persisted PhoBERT ID is copied from the immutable `Phase40DataContract.validation_snapshot`; locally reconstructed IDs fail even if text and labels otherwise match.
    - Every checkpoint's argmax predictions use the Plan 40-01 shared evaluator, and `select_phase40_checkpoint` applies the same three risky recall floors, zero-invalid admission, macro-F1 ranking, risky-to-benign tie-break, and earlier-step tie-break as Qwen.
    - Run evidence retains every checkpoint metric candidate plus the selected step and machine-readable rationale; when none passes, the highest-macro-F1 candidate remains visible as a safety-gate failure rather than release-ready.
  </behavior>
  <action>Write fake tokenizer/model/Trainer tests first, then implement a dedicated PhoBERT full-training backend behind the established canonical preflight and evidence interfaces. Use the pinned immutable revision, explicit `id2label`/`label2id`, four outputs, deterministic underthesea adapter metadata, max length 256, dynamic padding, and full encoder-plus-head trainables. Retain raw and segmented Vietnamese plus truncation evidence. At each declared evaluation checkpoint and at the final optimizer step, iterate the immutable `Phase40DataContract.validation_snapshot`, copy its exact `validation_row_id`, canonical index, raw message, and source-row identity into the prediction record, atomically persist canonical-order raw prediction JSONL with all four unmodified logits, reject any independently reconstructed/missing/duplicate/reordered ID, derive shared metrics against the same snapshot ID sequence, and collect a checkpoint candidate. Call the model-family-neutral `select_phase40_checkpoint` only after the complete candidate set exists; persist the selected step, every candidate's safety-gate state, and the exact recall-floor/macro-F1/risky-to-benign/earlier-step rationale in `RunEvidence`. Keep Qwen prompting, PEFT, bitsandbytes, and QLoRA out of this module. Wire the positive tuple `phobert`, `classification-head`, `full` through the common CLI without adding a second QLoRA flag. Tests must inject all model/tokenizer/segmenter/Trainer dependencies, exercise immutable snapshot-ID propagation, multiple checkpoints plus final-step de-duplication and the passing/no-passing/tie branches, and perform no download or real training.</action>
  <acceptance_criteria>
    - The only PhoBERT experiment tuple is full classification-head fine-tuning.
    - Segmentation fixtures lock Vietnamese output and version/hash behavior while retaining untouched raw text.
    - Every input row yields one preprocessing record and one prediction record or an explicit blocking error; no empty/truncated row disappears.
    - Four-logit predictions from every declared checkpoint and the final step use the same metric implementation, locked label order, safety floors, ranking, and tie-breaks as Qwen.
    - PhoBERT, Qwen, metrics, and review fixtures share byte-for-byte identical validation snapshot IDs; local ID reconstruction and one-ID mutation fail.
    - Fake cadence tests persist ordered raw predictions for every candidate, reject missing/duplicate/reordered rows, and prove the selected-checkpoint evidence explains both passing selection and no-passing safety fallback.
    - The fake tracer finalizes a schema-valid evidence bundle without GPU, model files, package installation, or external service use.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/model_adaptation/test_phase40_phobert.py -q --basetemp .tmp/pytest-p40-03-phobert</automated>
  </verify>
  <done>A production-shaped PhoBERT classifier path is proven end to end with fakes, retains deterministic checkpoint-level predictions and selection rationale, and cannot be mistaken for a quantized adapter experiment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create and statically gate three fresh canonical Colab notebooks</name>
  <read_first>
    - `src/model_adaptation/cli.py`
    - `src/model_adaptation/phase40_evidence.py`
    - `.planning/phases/40-multi-model-training-evidence/40-RESEARCH.md`
    - `.planning/phases/40-multi-model-training-evidence/40-PATTERNS.md`
  </read_first>
  <files>src/model_adaptation/phase40_notebooks.py, notebooks/phase40/qwen_lora_colab.ipynb, notebooks/phase40/qwen_qlora_colab.ipynb, notebooks/phase40/phobert_colab.ipynb, tests/model_adaptation/test_phase40_notebooks.py</files>
  <behavior>
    - All notebook JSON parses and contains explicit exact dependency/model pins, package-authority stop text, canonical train/validation preflight, positive experiment identity, fresh full-run origin, resumable full-run storage, evidence verification, graph regeneration, and bundle export.
    - Every notebook obtains its repository code only from the run request's repository-relative source archive, verifies the archive and deterministic inventory-manifest SHA-256 values before extraction/import, and records both identities in run evidence; mutation or extraction-before-verification fails static validation.
    - Every notebook mounts/copies the input archive only at the fixed request-bound Drive path, runs `phase40-verify-input-bundle` before any train/validation member open or extraction, then uses only the fixed verified extraction root; archive/member/manifest drift or extra/test-like membership fails.
    - Qwen LoRA requests `lora`, Qwen QLoRA requests `qlora`, and PhoBERT requests `classification-head`; no mode is inferred from omitted flags.
    - Notebook cells call repository CLI/module APIs and contain no local Trainer construction, model factory, output-repair parser, metric formula, plotting arrays, or alternative evaluator.
    - Static validation rejects stale corpus paths, laptop-relative data paths, broad split discovery, reserved-data readers, alternate Drive/extraction locations, extra/test-like input members, input extraction/data open before bundle verification, unpinned training packages/models, missing/alternate/unhashed source archives, extraction/import before source verification, embedded secrets, personal absolute paths, external inference/judge calls, and probe resume/upload.
    - Full Qwen notebooks share one matched-config artifact and start at step zero; a later resume is permitted only through the exact compatibility verifier.
  </behavior>
  <action>Create all three notebooks as new JSON artifacts rather than copying historical cells. Keep them thin: environment display, explicit operator package/model authority boundary, immutable run-request verification, source-bundle acquisition from the one repository-relative archive named by that request, exact source-archive and deterministic inventory-manifest SHA-256 verification before any extraction/import, operator Drive mount/copy, automated `phase40-verify-input-bundle` against exactly `/content/drive/MyDrive/internship-phase40/phase40-train-validation.zip`, verified extraction to exactly `/content/phase40-input-v1`, one repository CLI full-run command consuming the returned immutable contract/snapshots, evidence verification, deterministic graph command, and bundle export. The input verifier must validate the request-bound archive hash, exact sorted three-member set, safe names, internal-manifest hash/schema, counts, whole-file/member hashes, and snapshot-ID version before opening either JSONL member; the notebook cannot extract, import a data loader, call preflight/run, or read train/validation earlier. Record source hashes plus input archive/manifest/member/snapshot identities in each run bundle. Do not place `data/splits`, a Windows/laptop absolute path, generic Drive discovery, or any held-out/test-like name in notebook executable cells. Do not require a Git commit identifier in the run evidence; an optional Git value may be retained as supplemental context, while reproducibility is gated by the model/data/config/source-archive/inventory/input-bundle identities. Implement a normal Python static validator that walks notebook sources and structured command arguments, validates exact mode-specific requirements, the fixed external path mapping, exact permitted input members, and both verify-before-extract/import orderings, and detects duplicate training/evaluation logic by forbidden imports/calls and AST/text rules. Tests mutate clean fixture notebooks one rule at a time, including absent input/source fields, alternate Drive or extraction path, laptop-relative path, extra/test-like member, one-byte archive/manifest/member hash change, manifest/archive mismatch, and any input member open/extraction/repository import before successful verification. This plan writes notebook code only; it does not execute installation, model acquisition, Colab, Drive, or training cells.</action>
  <acceptance_criteria>
    - The three canonical notebook paths are new and pass the static validator.
    - Each notebook has one explicit full-run identity and no probe checkpoint/adaptor input.
    - The LoRA/QLoRA matched config is referenced identically except for approved quantization mode/proof fields.
    - Every notebook requires the same request-pinned source archive/inventory hash pair before code extraction/import and carries both identities into evidence without requiring a Git commit.
    - Every notebook links to the same request-pinned input archive at the exact Drive path, verifies exact membership/manifest/archive/member hashes before any data-member open, uses the fixed extraction root, and contains no laptop-relative data path.
    - Static mutation tests catch every stale-path, secret, implicit-mode, unpinned-dependency/model, missing/mutated/unverified source bundle, verify-order violation, inline-trainer/parser/evaluator/graph, and reserved-reader case.
    - Notebooks contain no external API/inference integration and require human package/Colab authority before expensive cells.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/model_adaptation/test_phase40_notebooks.py -q --basetemp .tmp/pytest-p40-03-notebooks</automated>
  </verify>
  <done>Three fresh canonical notebooks exist as statically verified operator controllers over one repository implementation.</done>
</task>

</tasks>

## Artifacts this phase produces

- `src/model_adaptation/phobert_training.py`: `PhoBertTrainingConfig`, `PhoBertPreprocessingRecord`, `segment_for_phobert`, `run_phobert_training`, checkpoint-cadence raw prediction production, and shared deterministic checkpoint selection.
- `src/model_adaptation/phase40_notebooks.py`: `NotebookValidationIssue`, `validate_phase40_notebook`, `validate_phase40_notebooks`.
- `notebooks/phase40/qwen_lora_colab.ipynb`: canonical fresh Qwen LoRA full-run controller.
- `notebooks/phase40/qwen_qlora_colab.ipynb`: canonical fresh genuine four-bit Qwen QLoRA full-run controller.
- `notebooks/phase40/phobert_colab.ipynb`: canonical fresh PhoBERT full classification-head controller.
- `tests/model_adaptation/test_phase40_phobert.py` and `test_phase40_notebooks.py`: fake backend and static notebook gates.

## Specless edge-coverage assumptions

| Probe row | Status | Concrete predicate |
|---|---|---|
| TRAIN-04 / unclassified | flagged | Missing/empty segmentation, wrong logits, unavailable pinned model, or preprocessing drift produces an explicit failed run/stop; another model or quantization method is never substituted. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Raw Vietnamese -> PhoBERT preprocessing | Text crosses into a version-sensitive word segmenter and tokenizer with a shorter context limit. |
| Notebook source -> Colab runtime | Repository-authored controller code will later run with external package, model, Drive, and GPU authority. |
| Colab output -> local evidence | Externally executed artifacts return across a copy boundary and require complete re-verification. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-40-03-01 | Tampering | PhoBERT preprocessing/label mapping | high | mitigate | Version/hash segmentation, preserve raw text, fixed four-label mapping/logit width, truncation records, and no-silent-drop tests. |
| T-40-03-02 | Spoofing | PhoBERT experiment identity | high | mitigate | Typed tuple permits classification-head/full only; tests fail on PEFT, bitsandbytes, LoRA, QLoRA, or probe use. |
| T-40-03-03 | Information disclosure | Colab notebooks | high | mitigate | Static validator blocks credentials, personal paths, external inference, broad data readers, and unapproved split references. |
| T-40-03-04 | Tampering / Repudiation | notebook-local logic | high | mitigate | Thin CLI-only notebooks; validator rejects duplicate Trainer, parser, evaluator, and graph implementations. |
| T-40-03-05 | Tampering / Spoofing | Colab source code identity | high | mitigate | Run-request-pinned repository-relative source archive and deterministic inventory hashes must verify before extraction/import and are retained in run evidence; Git identity is optional. |
| T-40-03-06 | Tampering / Information disclosure | Colab input bundle and mount | high | mitigate | Fixed request-bound Drive/extraction paths, exact-three-member and hash validation before JSONL open, static ordering checks, and rejection of extra/test-like/laptop-relative paths prevent held-out or substituted data from entering a run. |
| T-40-03-SC | Tampering | future notebook package installs | high | mitigate | This plan executes no install; notebooks contain explicit pins and a blocking operator authority boundary consumed by Plans 40-04/40-05. |
</threat_model>

<verification>
Run `python -m pytest tests/model_adaptation/test_phase40_phobert.py tests/model_adaptation/test_phase40_notebooks.py -q --basetemp .tmp/pytest-p40-03-all`, then run `python -m src.model_adaptation.cli phase40-validate-notebooks --root notebooks/phase40`. Mutation fixtures must prove source archive/inventory and input archive/manifest/member hash fields, exact input member allowlist, fixed Drive/extraction mapping, immutable snapshot-ID propagation, zero-data-open verify ordering, and absence of laptop-relative data paths while Git metadata remains optional. These checks must be static/fake-only: no model download, package installation, notebook execution, GPU, Colab, Drive, real training, or corpus access.
</verification>

<success_criteria>
- PhoBERT is implemented only as full four-class sequence classification with deterministic, auditable Vietnamese preprocessing.
- Its checkpoint and final-step predictions flow through the same locked-label evaluator, safety-floor selector, and evidence schema as Qwen, with an explicit selected-checkpoint rationale.
- Three fresh notebooks invoke only hash-verified source-bundle repository logic and the request-pinned exact-three-member input bundle at fixed Colab/Drive paths, with automated verification before any data open plus explicit identities, pins, preflight, graph, and export steps; no Git commit identifier is required.
- Static validators reject historical data lineage, inline duplicate logic, secrets, implicit modes, and reserved-data readers.
- Plans 40-01 through 40-03 remain fully unattended and perform no download, install, real training, external service action, or held-out access.
</success_criteria>

<output>
Create `.planning/phases/40-multi-model-training-evidence/40-03-SUMMARY.md` when done.
</output>
