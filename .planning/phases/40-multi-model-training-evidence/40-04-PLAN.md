---
phase: 40-multi-model-training-evidence
plan: 04
type: execute
wave: 4
depends_on:
  - 40-03
files_modified:
  - data/models/phase40/probes/rtx5050-qwen-lora/
  - data/models/phase40/probes/rtx5050-qwen-qlora/
  - data/models/phase40/probes/rtx5050-qwen-qlora/package-authority.json
  - .planning/phases/40-multi-model-training-evidence/40-LOCAL-PROBE-REPORT.md
autonomous: false
requirements:
  - TRAIN-01
  - TRAIN-02
  - TRAIN-03
  - TRAIN-06
user_setup:
  - service: "Local Python package authority"
    why: "A genuine QLoRA probe requires the operator to verify and authorize the exact bitsandbytes package before installation."
    dashboard_config:
      - task: "Verify bitsandbytes 0.50.1 provenance on its official PyPI page and explicitly approve or reject local installation."
        location: "https://pypi.org/project/bitsandbytes/0.50.1/"
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The RTX 5050 LoRA capability is measured through one real bounded run or retained as a genuine pre-start/runtime failure, never assumed from static hardware facts."
    - "The RTX 5050 QLoRA capability is attempted only after human package authority and the complete genuine four-bit proof; incompatibility remains an honest QLoRA failure."
    - "Every runnable probe targets exactly 40 post-warm-up optimizer steps, records measured timing/VRAM/throughput/overhead, extrapolates ETA explicitly, and discards its adapter."
    - "Local probe outcomes never publish, resume, seed a Colab run, enter a full-run curve, or become a causal claim about full-training time."
  artifacts:
    - path: "data/models/phase40/probes/rtx5050-qwen-lora/run-evidence.json"
      provides: "Verified genuine local LoRA probe result or failure receipt"
    - path: "data/models/phase40/probes/rtx5050-qwen-lora/discard-receipt.json"
      provides: "Proof that no LoRA probe adapter remains"
    - path: "data/models/phase40/probes/rtx5050-qwen-qlora/run-evidence.json"
      provides: "Verified genuine local four-bit QLoRA probe result or failure receipt"
    - path: "data/models/phase40/probes/rtx5050-qwen-qlora/discard-receipt.json"
      provides: "Proof that no QLoRA probe adapter remains"
    - path: ".planning/phases/40-multi-model-training-evidence/40-LOCAL-PROBE-REPORT.md"
      provides: "Human-readable measured feasibility, ETA, and failure interpretation"
  key_links:
    - from: "phase40-doctor"
      to: "phase40-run"
      via: "mode-specific readiness and proof must pass before the corresponding probe starts"
      pattern: "adaptation-mode"
    - from: "events.jsonl"
      to: "run-evidence.json"
      via: "Plan 40-02 finalizer derives timing, VRAM, throughput, overhead, and ETA"
      pattern: "steady_step_seconds_median"
    - from: "run-evidence.json"
      to: "discard-receipt.json"
      via: "probe completion is valid only after the adapter identity is removed and receipt verifies"
      pattern: "discard"
  prohibitions:
    - value: "Forcing, engineering, predeclaring, or fabricating an OOM"
      status: unverified
    - value: "Calling a run QLoRA after any four-bit capability/module/frozen-weight/gradient proof fails"
      status: unverified
    - value: "Resuming, publishing, retaining, uploading, or using a probe adapter in any full run"
      status: unverified
    - value: "Presenting extrapolated ETA as completed local full-training duration"
      status: unverified
    - value: "Installing a package before a blocking human legitimacy and authority decision"
      status: unverified
    - value: "Accessing the held-out Phase 41 partition during a local probe"
      status: unverified
---

## Execution update — 2026-08-24

The current execution authority is the newer quick plan `.planning/quick/260824-l7f-revise-phase-40-and-implement-a-monitore/260824-l7f-PLAN.md`, which replaces this plan's older separate-root command shape while preserving its evidence and no-fabrication requirements.

- Local LoRA was genuinely run under the original two-hour decision clock. Its single audited retry retained five warm-up plus 26 measured optimizer steps, then sealed literal `error / parent_controller_error`; it did not reach the 40-step target and must not be represented as a completed probe.
- It did not OOM. The verified result is sustained `gpu_pressure`: 7,902/8,151 MiB peak device use, 9 MiB minimum free, finite losses, 61C peak temperature, and a provisional 18.42–18.88-hour compute-only full-schedule extrapolation.
- The disposable runtime was hash-sealed and removed; no adapter/checkpoint remains. The exact report is `40-LOCAL-PROBE-REPORT.md` and the evidence root is `data/models/phase40/probes/rtx5050-local-decision/`.
- The bitsandbytes 0.50.1 dependency authority/setup receipt was already completed before the experiment clock. The remaining Plan 40-04 work is to persist/verify those package gates in the decision ledger and run the genuine QLoRA 5+40 stage. No further LoRA retry is authorized.

## Execution closure — 2026-08-25

Plan 40-04 is now complete through a separately dated, source-bound continuation at `data/models/phase40/probes/rtx5050-qlora-session-20260825/`. This path was necessary because the original local-decision clock had expired and its evidence/code identities could not be rewritten or resumed.

- The exact `bitsandbytes==0.50.1` setup receipt and live Torch/CUDA/NF4 identities reverified before model load.
- The genuine QLoRA proof passed: NF4 and double quantization, 252 `Linear4bit` modules, frozen base weights, 504 adapter-only trainables, and finite/nonzero adapter-gradient evidence.
- The probe retained exactly five warm-up plus 40 measured optimizer steps, one validation pass, and one checkpoint-overhead measurement. Median measured step time was 3.462389 seconds; the locked 1,245-step formula projects 4,369.750238 seconds (72.83 minutes), explicitly as an estimate rather than a completed full-run duration.
- Peak device use was 7,516/8,151 MiB with 395 MiB minimum free; no OOM or declared pressure failure occurred. Peak system-RAM use was 22,136,381,440 bytes.
- The result sealed as `measured / evidence_target_reached`; its runtime and checkpoint paths were removed, the five-stage ledger verified, and the previous LoRA evidence tree remained byte-identical.
- The human-readable facts and hashes are in `40-LOCAL-PROBE-REPORT.md`. No local probe artifact may seed the fresh full Colab experiments.

<objective>
Measure what the RTX 5050 can genuinely do, without turning a desired defense narrative into fabricated hardware evidence.

Purpose: Produce bounded local LoRA/QLoRA feasibility, resource, and ETA evidence while preserving strict package authority, genuine mode identity, and disposable probe isolation.
Output: Two real probe evidence roots or honest failure receipts, verified adapter-discard receipts, package-authority record, and a local-probe report.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-multi-model-training-evidence/40-CONTEXT.md
@.planning/phases/40-multi-model-training-evidence/40-AI-SPEC.md
@.planning/phases/40-multi-model-training-evidence/40-RESEARCH.md
@.planning/phases/40-multi-model-training-evidence/40-01-SUMMARY.md
@.planning/phases/40-multi-model-training-evidence/40-02-SUMMARY.md
@.planning/phases/40-multi-model-training-evidence/40-03-SUMMARY.md
@.planning/phases/39-independent-quality-re-judge/39-DOWNSTREAM-DATA-CONTRACT.json

<interfaces>
Real probe command shape: `python -m src.model_adaptation.cli phase40-run --model-family qwen --adaptation-mode {lora|qlora} --run-kind probe --train data/splits/train.jsonl --val data/splits/val.jsonl --model-revision cdbee75f17c01a7cc42f958dc650907174af0554 --warmup-steps 5 --post-warmup-steps 40 --output-root <fixed-probe-root>`.

Each run root is valid in one of two honest states: a completed 40-step probe with raw events, evidence, graph/provenance, and verified discard receipt; or a pre-start/runtime failed-run receipt naming requested identity, failure stage, exception category, environment/hardware/authority identity, any partial raw events, and a verified no-artifact/discard receipt. A pre-start failure never synthesizes completed-run metrics, events, gradients, or checkpoints. There is no substitute mode.

Directory ownership is complete rather than file-selective. Each probe root owns every file its lifecycle can emit: resolved configuration, sanitized environment/command inventory, append-only events, raw ordered validation predictions and metrics, `trainer_state`, curve files and graph provenance, evidence or failure receipt, temporary adapter/checkpoint hashes, and final discard/absence receipt. The QLoRA root additionally owns `package-authority.json`. No promised probe artifact may be written outside its declared root or omitted from evidence verification.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Execute the real RTX 5050 LoRA bounded probe</name>
  <precondition>The Phase 39 downstream contract exists, Plans 40-01 through 40-03 tests pass, and the pinned Qwen base revision `cdbee75f17c01a7cc42f958dc650907174af0554` is available locally without download.</precondition>
  <read_first>
    - `.planning/phases/39-independent-quality-re-judge/39-DOWNSTREAM-DATA-CONTRACT.json`
    - `.planning/phases/40-multi-model-training-evidence/40-02-SUMMARY.md`
    - `src/model_adaptation/cli.py`
    - `src/model_adaptation/phase40_evidence.py`
    - `src/model_adaptation/phase40_modes.py`
  </read_first>
  <files>data/models/phase40/probes/rtx5050-qwen-lora/, .planning/phases/40-multi-model-training-evidence/40-LOCAL-PROBE-REPORT.md</files>
  <action>Run the full Phase 40 fixture suite and side-effect-free LoRA doctor, then canonical preflight. If readiness passes, execute one real Qwen LoRA probe at the pinned revision with 5 warm-up and exactly 40 post-warm-up optimizer steps, using the frozen matched controls. Keep the complete lifecycle under the owned LoRA probe root: resolved configuration, sanitized environment/command inventory, raw timing/resource/eval/save events, ordered raw validation predictions and metrics, `trainer_state`, curve files and graph provenance, evidence or failure receipt, temporary adapter/checkpoint hashes, and the explicit ETA calculation. Whether the run succeeds, OOMs, or fails genuinely, preserve its requested LoRA identity and exact failure stage. If local-model availability, doctor, preflight, model load, or another prerequisite stops the run before step zero, emit the same strict pre-start-failure contract used by QLoRA: requested experiment identity, explicit failure stage/category, sanitized environment plus authority/model reference, and a verified absence receipt proving no adapter/checkpoint was created; do not synthesize completed metrics or events. Remove every probe adapter/checkpoint immediately after hashing and verify the discard or no-artifact receipt. Append only evidence-backed facts to the local-probe report; do not predict the outcome or deliberately increase load to cause failure.</action>
  <acceptance_criteria>
    - Preflight and doctor complete before model load and show the pinned local base identity.
    - A runnable probe records 40 post-warm-up steps, synchronized timing, peak allocated/reserved VRAM, examples/s, tokens/s, and measured eval/save overhead.
    - ETA is stored as an estimate using the locked formula and raw event references.
    - A genuine failure is retained with hardware/environment/stage evidence and no rewritten identity.
    - A pre-start failure validates only with the requested LoRA identity, exact failure stage, sanitized environment/authority reference, and verified no-artifact receipt; completed-run fields are neither required nor fabricated.
    - No adapter/checkpoint remains, no registry entry is added, and evidence verification succeeds.
  </acceptance_criteria>
  <verify>
    <automated>python -m src.model_adaptation.cli phase40-verify-evidence --root data/models/phase40/probes/rtx5050-qwen-lora --expected-run-kind probe --require-discard-receipt --allow-prestart-failure</automated>
  </verify>
  <done>The laptop has one genuine measured LoRA probe outcome and no retained probe model artifact.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Decide authority for the exact QLoRA package</name>
  <read_first>
    - `.planning/phases/40-multi-model-training-evidence/40-RESEARCH.md`
    - `.planning/phases/40-multi-model-training-evidence/40-AI-SPEC.md`
    - `src/model_adaptation/doctor.py`
  </read_first>
  <action>Pause before any local package installation. Ask the operator to inspect the official PyPI project/release/files for `bitsandbytes==0.50.1`, confirm the package name, maintainer/provenance, Windows x86-64 wheel, and intended CUDA compatibility, then explicitly approve or reject the exact candidate. This checkpoint only returns the human decision and optional reason to the executor; it does not write or mutate an authority record. A rejection is a valid decision and must leave QLoRA NOT READY rather than choosing another package or mode.</action>
  <decision>Authorize or reject installation of the exact `bitsandbytes==0.50.1` candidate for the local QLoRA probe after official-registry inspection.</decision>
  <options>
    <option id="approve">Approve only `bitsandbytes==0.50.1`; Task 3 persists the decision before installing and proving compatibility.</option>
    <option id="reject">Reject the candidate with a reason; Task 3 persists honest pre-start QLoRA failure evidence and performs no installation.</option>
  </options>
  <what-built>The code-only genuine-QLoRA proof gate and side-effect-free doctor from Plans 40-01/02; no package has been installed by this checkpoint.</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/bitsandbytes/0.50.1/ and its file list.
    2. Confirm the exact package/version and Windows wheel provenance are acceptable for this laptop.
    3. Reply `approve bitsandbytes 0.50.1` to authorize the executor to install that exact version, or `reject` with the reason.
  </how-to-verify>
  <resume-signal>Reply `approve bitsandbytes 0.50.1` or `reject: <reason>`.</resume-signal>
  <acceptance_criteria>
    - A human explicitly approves or rejects the exact package/version from the official registry.
    - No installation/download occurs before approval.
    - The resume payload contains only the explicit decision and optional reason; it contains no secret, token, or personal absolute path.
  </acceptance_criteria>
  <verify>
    <automated>python -c "import importlib.util; print('bitsandbytes_present=' + str(importlib.util.find_spec('bitsandbytes') is not None))"</automated>
  </verify>
  <done>The exact local QLoRA dependency decision has been returned to the executor before any installation or evidence mutation.</done>
</task>

<task type="auto">
  <name>Task 3: Prove genuine four-bit capability and execute the RTX 5050 QLoRA probe</name>
  <precondition>Task 2 returned an explicit approve or reject decision for `bitsandbytes==0.50.1`, and the pinned Qwen base revision remains locally available.</precondition>
  <read_first>
    - `.planning/phases/40-multi-model-training-evidence/40-01-SUMMARY.md`
    - `.planning/phases/40-multi-model-training-evidence/40-02-SUMMARY.md`
    - `src/model_adaptation/phase40_modes.py`
    - `src/model_adaptation/cli.py`
  </read_first>
  <files>data/models/phase40/probes/rtx5050-qwen-qlora/, data/models/phase40/probes/rtx5050-qwen-qlora/package-authority.json, .planning/phases/40-multi-model-training-evidence/40-LOCAL-PROBE-REPORT.md</files>
  <action>First sanitize the Task 2 resume payload and atomically write then validate the explicitly owned `data/models/phase40/probes/rtx5050-qwen-qlora/package-authority.json` with the exact package/version, official URL, decision, timestamp, and optional reason; reject secrets, tokens, and personal absolute paths. If the decision is rejected, perform no install or model load: keep an honest QLoRA pre-start failure evidence document with `failure_stage=package-authority`, requested mode unchanged, the authority-record hash, and an absence receipt proving no adapter/checkpoint was ever created inside the owned QLoRA root, then update the report and stop this path incomplete. Only after a validated approval record exists, run `python -m pip install --no-deps "bitsandbytes==0.50.1"` without changing the existing Torch build, capture package/wheel/environment identity, and run the QLoRA doctor. Require CUDA kernel operation and the full model-level NF4/double-quant, positive `Linear4bit`, frozen-base, adapter-only-trainable, forward/backward-gradient proof before step zero. If proof passes, execute the same 5 warm-up plus 40 post-warm-up probe with the matched controls and evidence lifecycle used for LoRA. Keep every resolved config, sanitized environment/command inventory, event, raw ordered prediction, metric, `trainer_state`, curve/graph provenance file, evidence/failure receipt, temporary artifact hash, and discard/absence receipt under that complete root. If installation, kernel, model proof, OOM, or runtime fails, retain the genuine QLoRA failure receipt and stop that path without fallback. Hash and discard any adapter/checkpoint, verify the receipt, and update the report with measured facts only.</action>
  <acceptance_criteria>
    - Installed package identity matches the human-approved exact candidate and does not upgrade/replace Torch.
    - The sanitized authority record is written and validated by this auto task before any install; an explicit rejection creates verifiable pre-start QLoRA failure/absence evidence and performs no package or model action.
    - `4bit-qlora` appears only after every environment and model-level proof passes.
    - A runnable QLoRA probe records the same 40-step resource/timing/overhead schema as LoRA; otherwise a genuine failure receipt is complete.
    - No QLoRA probe artifact survives or enters the registry/full-run lineage.
    - The report compares local feasibility without causal speed claims beyond the measured same-machine probe conditions.
  </acceptance_criteria>
  <verify>
    <automated>python -m src.model_adaptation.cli phase40-verify-evidence --root data/models/phase40/probes/rtx5050-qwen-qlora --expected-run-kind probe --require-discard-receipt --allow-prestart-failure</automated>
  </verify>
  <done>The laptop has a persisted human package decision plus one honest QLoRA capability/probe or pre-start-rejection outcome, genuine four-bit proof when runnable, and no retained probe model artifact.</done>
</task>

</tasks>

## Artifacts this phase produces

- `data/models/phase40/probes/rtx5050-qwen-lora/`: LoRA events, evidence or failure receipt, graph provenance, and adapter-discard receipt.
- `data/models/phase40/probes/rtx5050-qwen-qlora/`: package-authority record, QLoRA events, genuine four-bit evidence or failure receipt, graph provenance, and adapter-discard receipt.
- `.planning/phases/40-multi-model-training-evidence/40-LOCAL-PROBE-REPORT.md`: measured feasibility, resource values, ETA formula/results, and explicit success/failure interpretation.
- No probe adapter, checkpoint lineage, or registry model record is produced.

## Specless edge-coverage assumptions

| Probe row | Status | Concrete predicate |
|---|---|---|
| TRAIN-01 / unclassified | flagged | LoRA probe outcome is classified only after execution as completed, pre-start failure, OOM, interrupted, or other explicit runtime failure; every state retains evidence and no fabricated result. |
| TRAIN-02 / unclassified | flagged | QLoRA probe outcome uses the same explicit state set plus a distinct capability-proof failure; no state is reclassified as LoRA. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Package registry -> local Python environment | Third-party binary code crosses into the RTX/CUDA runtime only after explicit operator authority. |
| Probe controls -> GPU | Bounded configuration crosses into finite laptop memory/thermal resources. |
| Runtime result -> defense evidence | Success, OOM, compatibility failure, timing, and ETA cross into claims that must remain literal and measured. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-40-04-SC | Tampering / Elevation of privilege | bitsandbytes installation | high | mitigate | Blocking-human official-registry legitimacy/authority checkpoint, exact version, no Torch upgrade, and post-install identity capture. |
| T-40-04-01 | Denial of service | RTX probe runner | high | mitigate | Fixed 5+40-step bound, one run per mode, no engineered OOM, captured genuine failures, and no unattended retry loop. |
| T-40-04-02 | Spoofing | QLoRA claim | high | mitigate | Complete four-bit/module/frozen-weight/gradient proof before step zero and evidence finalization; no semantic fallback. |
| T-40-04-03 | Tampering / Repudiation | local timing/ETA report | high | mitigate | Values derive from retained raw events and locked formula; evidence verifier and graph provenance run before report update. |
| T-40-04-04 | Information disclosure | run evidence | medium | mitigate | Sanitized argv/environment allowlists and repo/run-relative paths only. |
</threat_model>

<verification>
For each mode, run the mode-specific doctor, canonical preflight, bounded probe command, evidence verifier, graph regeneration, and discard-receipt verifier. Then run `python -m pytest tests/model_adaptation -q --basetemp .tmp/pytest-p40-04-regression`. A failed real probe is acceptable only when its failure receipt validates and no adapter/registry entry remains; a fabricated success, forced OOM, fallback, or unverifiable evidence blocks the plan.
</verification>

<success_criteria>
- LoRA has one real bounded RTX 5050 result or honest failure receipt.
- QLoRA has a human-authorized exact dependency and one genuine four-bit bounded result or honest failure receipt.
- Each runnable mode records exactly 40 post-warm-up steps and the complete resource/overhead/ETA evidence.
- Every adapter/checkpoint is discarded and independently verified absent.
- The local report distinguishes measurement from extrapolation and never hides or engineers failure.
</success_criteria>

<output>
Create `.planning/phases/40-multi-model-training-evidence/40-04-SUMMARY.md` when done.
</output>
