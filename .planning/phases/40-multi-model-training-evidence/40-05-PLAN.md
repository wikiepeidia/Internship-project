---
phase: 40-multi-model-training-evidence
plan: 05
type: execute
wave: 5
depends_on:
  - 40-04
files_modified:
  - data/models/phase40/source/phase40-source.zip
  - data/models/phase40/source/phase40-source-manifest.json
  - data/models/phase40/input/phase40-train-validation.zip
  - data/models/phase40/full-run-request.json
  - data/models/phase40/matched-qwen-config.json
  - data/models/phase40/phobert-config.json
  - data/models/phase40/full/
  - data/models/phase40/comparison-manifest.json
  - data/models/phase40/review/review-queue.jsonl
  - data/models/phase40/review/review-queue-manifest.json
  - data/models/phase40/review/reviewer-return.template.jsonl
  - .planning/phases/40-multi-model-training-evidence/40-COLAB-RUN-HANDOFF.md
  - .planning/phases/40-multi-model-training-evidence/40-VALIDATION-COMPARISON.md
autonomous: false
requirements:
  - TRAIN-01
  - TRAIN-02
  - TRAIN-03
  - TRAIN-04
  - TRAIN-05
  - TRAIN-06
user_setup:
  - service: "Google Colab and operator-managed Drive"
    why: "The three fresh full runs require an externally allocated GPU session, package/model acquisition authorization, and manual return of large evidence/model bundles."
    dashboard_config:
      - task: "Verify the exact bitsandbytes and Matplotlib package releases before executing notebook install cells."
        location: "Official PyPI project/release pages"
      - task: "Run the three canonical notebooks and return their complete output bundles to the fixed local roots."
        location: "Google Colab notebook UI and operator-managed Drive"
      - task: "Copy the hash-pinned train/validation-only archive to the fixed Drive path and mount it read-only for automated verification before any training-data member is opened."
        location: "/content/drive/MyDrive/internship-phase40/phase40-train-validation.zip"
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Fresh Qwen LoRA and genuine four-bit QLoRA full runs start at step zero from the same pinned base revision and exact matched controls, differing intentionally only in base-weight quantization."
    - "A fresh PhoBERT encoder-plus-classification-head model trains fully on the same canonical train/validation identities and locked four-class task."
    - "All three returned bundles contain complete raw logs, curves, trainer state, model/checkpoint hashes, environment/resource evidence, and exactly 219 ordered validation predictions."
    - "The final validation comparison reports every result regardless of winner, preserves failed safety gates, labels cross-GPU speed as hardware-confounded, and makes no held-out or statistical-significance claim."
    - "Human package and Colab actions return only structured decisions/paths; a following automated finalizer persists sanitized authority, lexically validates the three fixed roots before opening them, and derives every comparison artifact."
    - "The immutable run request pins a deterministic repository source archive and inventory by SHA-256, so every notebook executes a verified code artifact without requiring a Git commit identifier."
    - "The immutable run request also pins one deterministic exact-three-member train/validation input archive, fixed Drive/extraction paths, and archive/manifest/member identities; automated notebook verification passes before either JSONL member opens."
    - "Automated comparison finalization emits a deterministic review queue whose every row carries the canonical full message and exact source-row hash, ready for the dependent Vietnamese-review closure plan."
  artifacts:
    - path: "data/models/phase40/source/phase40-source.zip"
      provides: "Deterministic repository source bundle executed by all three notebooks"
    - path: "data/models/phase40/source/phase40-source-manifest.json"
      provides: "Sorted source inventory plus per-file and archive SHA-256 identities"
    - path: "data/models/phase40/input/phase40-train-validation.zip"
      provides: "Deterministic exact-three-member canonical train/validation transfer archive with internal manifest"
    - path: "data/models/phase40/full-run-request.json"
      provides: "Immutable operator handoff identity, package pins, expected outputs, and run order"
    - path: "data/models/phase40/matched-qwen-config.json"
      provides: "Single source of matched LoRA/QLoRA controls and approved quantization delta"
    - path: "data/models/phase40/full/qwen-lora/run-evidence.json"
      provides: "Verified fresh full Qwen LoRA training/validation bundle"
    - path: "data/models/phase40/full/qwen-qlora/run-evidence.json"
      provides: "Verified fresh full genuine four-bit Qwen QLoRA training/validation bundle"
    - path: "data/models/phase40/full/phobert/run-evidence.json"
      provides: "Verified fresh full PhoBERT training/validation bundle"
    - path: "data/models/phase40/comparison-manifest.json"
      provides: "Three-model admissibility, artifact identities, measured metrics, and hardware-confounding status"
    - path: ".planning/phases/40-multi-model-training-evidence/40-VALIDATION-COMPARISON.md"
      provides: "Honest human-readable LoRA/QLoRA and PhoBERT/Qwen validation comparison"
    - path: "data/models/phase40/review/review-queue.jsonl"
      provides: "Message-bound deterministic mandatory-error and calibration review queue"
    - path: "data/models/phase40/review/review-queue-manifest.json"
      provides: "Canonical validation, selected-checkpoint, queue, and sampling provenance"
  key_links:
    - from: "data/models/phase40/matched-qwen-config.json"
      to: "notebooks/phase40/qwen_lora_colab.ipynb"
      via: "LoRA notebook consumes the frozen shared controls with explicit lora identity"
      pattern: "matched-qwen-config"
    - from: "data/models/phase40/source/phase40-source-manifest.json"
      to: "notebooks/phase40/*.ipynb"
      via: "the run request supplies the repository-relative archive and archive/inventory SHA-256 values that every notebook verifies before extraction/import"
      pattern: "source_archive_sha256|source_inventory_sha256"
    - from: "data/models/phase40/input/phase40-train-validation.zip"
      to: "notebooks/phase40/*.ipynb"
      via: "the operator copies one hash-pinned archive to the fixed Drive path, then repository automation verifies exact membership/manifest/archive/member hashes before opening or extracting train/validation data"
      pattern: "phase40-verify-input-bundle|phase40-input-manifest.json|/content/phase40-input-v1"
    - from: "data/models/phase40/matched-qwen-config.json"
      to: "notebooks/phase40/qwen_qlora_colab.ipynb"
      via: "QLoRA notebook consumes the same controls plus only the approved quantization proof delta"
      pattern: "matched-qwen-config"
    - from: "data/models/phase40/full/*/events.jsonl"
      to: "data/models/phase40/comparison-manifest.json"
      via: "evidence verification and graph regeneration precede comparison admission"
      pattern: "source_sha256"
    - from: "data/models/phase40/full/*/raw-predictions/*.jsonl"
      to: "data/models/phase40/review/review-queue.jsonl"
      via: "the verified comparison finalizer joins selected-checkpoint predictions by stable ID to the already-preflighted canonical validation snapshot, persists full message/source hash, and derives slices plus deterministic samples"
      pattern: "raw_message|source_row_sha256|invalid_output|risky_to_benign|zalo_error|benign_to_risky|cross_scam|correct_calibration_sample"
  prohibitions:
    - value: "Opening, reading, hashing, uploading, evaluating, plotting, or selecting on the held-out Phase 41 partition"
      status: unverified
    - value: "Starting a full run from any local probe adapter, checkpoint, event stream, or nonzero step"
      status: unverified
    - value: "Calling the QLoRA full run complete without genuine NF4 four-bit and adapter-gradient proof"
      status: unverified
    - value: "Hand-entering graph values or using a notebook-local trainer, parser, evaluator, or renderer"
      status: unverified
    - value: "Hiding a losing model, failed recall floor, invalid output, or hardware difference"
      status: unverified
    - value: "Claiming causal speed superiority across unlike accelerators or statistical superiority from one seed"
      status: unverified
    - value: "Requiring a Git commit identifier in the evidence bundle"
      status: unverified
    - value: "Omitting a mandatory review-queue slice, manually cherry-picking correct examples, or emitting a queue row without its canonical full message and exact source-row hash"
      status: unverified
    - value: "Sending laptop-relative split paths to Colab, transferring any held-out/test-like member, or opening a train/validation member before the request-pinned input archive and manifest verify"
      status: unverified
---

## Routing update — 2026-08-24

Do not treat this plan's older Colab-first wording as authorization to start all three external runs. The sealed local LoRA result proves severe sustained VRAM pressure and a provisional 18.42–18.88-hour compute-only schedule, so a full local ordinary-LoRA run is not the preferred use of the current deadline. However, LoRA did not OOM and the result does not decide QLoRA performance.

Plan 40-04 must first complete the genuine local QLoRA 5+40 measurement. If that verified probe has no OOM/pressure failure and its measured ETA is acceptable, the fresh full QLoRA run may stay local to save Colab credit; otherwise it routes to Colab. Any full run starts fresh at step zero and may not resume from either disposable local probe. The fresh full LoRA/PhoBERT obligations remain visible until the project requirements are explicitly changed; this note does not silently waive them.

<objective>
Produce and freeze the three real independently trained models and their honest validation-only comparison.

Purpose: Replace historical or literature-only claims with fresh matched LoRA/QLoRA and PhoBERT measurements while keeping package authority, external execution, returned-artifact trust, and test isolation explicit.
Output: Hash-verified source handoff/configs, three verified full-run bundles, regenerated graphs, comparison manifest/report, and a canonical-message-bound review queue for Plan 40-06.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-multi-model-training-evidence/40-CONTEXT.md
@.planning/phases/40-multi-model-training-evidence/40-AI-SPEC.md
@.planning/phases/40-multi-model-training-evidence/40-RESEARCH.md
@.planning/phases/40-multi-model-training-evidence/40-03-SUMMARY.md
@.planning/phases/40-multi-model-training-evidence/40-04-SUMMARY.md
@.planning/phases/39-independent-quality-re-judge/39-DOWNSTREAM-DATA-CONTRACT.json
@notebooks/phase40/qwen_lora_colab.ipynb
@notebooks/phase40/qwen_qlora_colab.ipynb
@notebooks/phase40/phobert_colab.ipynb

<interfaces>
The shared Qwen control file fixes: canonical train/validation identities and order; Qwen ID/revision `Qwen/Qwen3-4B-Instruct-2507` / `cdbee75f17c01a7cc42f958dc650907174af0554`; seed/data seed 42; max length 1024; per-device batch 1; gradient accumulation 4; effective batch 4; three epochs; AdamW Torch optimizer; linear schedule with warm-up ratio 0.03; learning rate `2e-4`; identical response-only formatter/mask hashes, LoRA rank/alpha/dropout/targets, BF16/FP16 policy, and log/eval/save cadence 10/50/50. The treatment delta is only unquantized base weights versus proved NF4 double-quantized four-bit base weights.

The PhoBERT control file fixes revision `e966aac8cb889325e073aa5f28ff70aca4dbc8c3`, seed/data seed 42, max length 256, dynamic padding, batch 16, three epochs, AdamW Torch, a linear schedule with warm-up ratio 0.03, learning rate `2e-5`, full encoder/head trainables, deterministic segmentation hash/version, and the locked label order. Architecture-specific optimizer/tokenization controls remain visible rather than falsely matched to Qwen.

Returned bundle roots are exactly `data/models/phase40/full/qwen-lora`, `data/models/phase40/full/qwen-qlora`, and `data/models/phase40/full/phobert`. Each begins as a fresh full run at step zero. Only an interrupted run may resume, and only through Plan 40-02 exact compatibility verification.

`full-run-request.json` is immutable before the operator checkpoint and contains package candidates/pins plus two independent transfer authorities. The code authority is one repository-relative deterministic source archive identity: archive path/SHA-256 and source-inventory manifest path/SHA-256. The data authority is one repository-relative `data/models/phase40/input/phase40-train-validation.zip` identity with archive SHA-256, internal `phase40-input-manifest.json` SHA-256, exact sorted member set `phase40-input-manifest.json`, `train.jsonl`, `val.jsonl`, canonical member counts/hashes, Phase 39 manifest identity, snapshot-ID algorithm/version, fixed Drive path `/content/drive/MyDrive/internship-phase40/phase40-train-validation.zip`, and fixed extraction root `/content/phase40-input-v1`. The two JSONL members are byte-for-byte canonical inputs, not JSON reserialization, and no held-out/test-like member is permitted. The archive/inventory source pair and archive/manifest/member input identities, not a mandatory Git commit, are verified before notebook code or data use and copied into every run-evidence bundle. The request never contains approval decisions or returned hardware facts. Task 3 persists the two sanitized package decisions and per-run GPU identities exclusively in `comparison-manifest.json`.

After comparison finalization, Plan 40-05 owns `review-queue.jsonl`, `review-queue-manifest.json`, and `reviewer-return.template.jsonl`. The queue is a union keyed by `(model_run_id, validation_row_id)` and retains canonical sequence, immutable `raw_message`, `source_row_sha256` over the exact UTF-8 validation JSONL source record excluding only its line terminator, gold label, predicted locked label or `invalid_output`, selected-checkpoint/run/artifact identities, and all applicable slice tags. Qwen and PhoBERT prediction bundles must already carry the exact IDs from the same immutable validation snapshot; the finalizer compares all three expected-ID sequences byte-for-byte before metric or queue work. The builder receives that already-preflighted canonical validation snapshot, requires exact stable-ID set/order equality, and joins predictions by ID; it accepts no caller-selected data path and never re-derives an ID. Correct calibration rows are selected separately for each model and gold class by ascending SHA-256 of model artifact identity plus row ID, taking ceiling 10% with at least one when a class has a correct prediction. Plan 40-06 owns reviewer input, canonical notes, review manifest/report, and closure; human notes remain observational evidence only.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Freeze and verify the three-run Colab handoff</name>
  <precondition>Plans 40-01 through 40-04 are complete, all Phase 40 fixture/static tests pass, and the canonical notebooks validate without external execution.</precondition>
  <read_first>
    - `.planning/phases/39-independent-quality-re-judge/39-DOWNSTREAM-DATA-CONTRACT.json`
    - `.planning/phases/40-multi-model-training-evidence/40-CONTEXT.md`
    - `.planning/phases/40-multi-model-training-evidence/40-04-SUMMARY.md`
    - `src/model_adaptation/phase40_handoff.py`
    - `notebooks/phase40/qwen_lora_colab.ipynb`
    - `notebooks/phase40/qwen_qlora_colab.ipynb`
  </read_first>
  <files>data/models/phase40/source/phase40-source.zip, data/models/phase40/source/phase40-source-manifest.json, data/models/phase40/input/phase40-train-validation.zip, data/models/phase40/full-run-request.json, data/models/phase40/matched-qwen-config.json, data/models/phase40/phobert-config.json, .planning/phases/40-multi-model-training-evidence/40-COLAB-RUN-HANDOFF.md</files>
  <action>Run canonical train/validation preflight and retain its immutable snapshots, then run the static notebook validator. Invoke the Plan 40-02 deterministic source-bundle builder over its explicit runtime-source allowlist, producing one repository-relative archive plus a sorted inventory manifest with per-file hashes, archive SHA-256, and inventory-manifest SHA-256; verify the pair before freezing the request. Invoke `build_phase40_input_bundle` only with the authorized `Phase40DataContract`, producing `data/models/phase40/input/phase40-train-validation.zip` as a normalized stored ZIP with exactly `phase40-input-manifest.json`, `train.jsonl`, and `val.jsonl`; copy the canonical file bytes without reserialization and verify archive hash, manifest-member hash/schema, exact safe member set, member counts/hashes, Phase 39 manifest identity, and snapshot-ID algorithm/version. Freeze the local repository-relative archive identity plus exact fixed transfer path `/content/drive/MyDrive/internship-phase40/phase40-train-validation.zip` and fixed extraction root `/content/phase40-input-v1`; include no laptop absolute path. Serialize the exact shared Qwen controls and approved quantization delta into one immutable matched config; serialize the separate transparent PhoBERT controls. Create a run request listing both verified source and input archive identities, three fresh run IDs, required package/model candidates and exact pins, source notebooks, expected bundle roots/files/hashes, step-zero origin, exact resume rules, environment/hardware capture fields, no-test boundary, and export procedure. The request is frozen before the operator checkpoint and contains no package approval/rejection decision or returned GPU fact; those exist only in the later sanitized operator return and `comparison-manifest.json`. Write a concise operator handoff with commands/cells in run order: copy/mount the exact input archive, automatically verify path/archive/inventory/manifest/member hashes before either JSONL member opens, extract only to the fixed root, then verify source before import and execute. A Git identifier may be included only as optional supplemental metadata and is not needed to validate the request. Do not put a secret, personal path, probe adapter, held-out/test-like member, or external inference service into the request.</action>
  <acceptance_criteria>
    - Canonical train/validation counts/hashes and Phase 39 manifest identity are frozen in the request.
    - Qwen controls compare exactly equal after removing only the explicit quantization treatment/proof fields.
    - All run IDs are fresh full-run identities with step origin zero and no probe parent.
    - The request enumerates complete expected evidence, prediction, graph, trainer-state, and model/checkpoint outputs for all three models.
    - The same deterministic source archive/inventory hash pair is frozen in the request, verified before any notebook extraction/import, and required in every returned run-evidence bundle.
    - One deterministic input archive is byte-stable from the authorized snapshots, contains exactly the manifest plus canonical train/validation files, and freezes its local identity, fixed Drive/extraction locations, counts, whole-file/member/manifest hashes, and snapshot-ID version in the request.
    - Request validation passes with no Git identifier and rejects a missing, alternate, mutated, or unhashed source archive/inventory.
    - Request/input validation rejects extra/test-like/traversal/symlink members, alternate Drive/extraction paths, laptop absolute paths, or any archive/manifest/member mutation before either data member opens or any extraction occurs.
    - The immutable request contains package candidates/pins only; package decisions and returned GPU identities are absent and cannot be backfilled into it.
    - Static notebook and run-request verification passes before any operator action.
  </acceptance_criteria>
  <verify>
    <automated>python -m src.model_adaptation.cli phase40-verify-run-request --request data/models/phase40/full-run-request.json --validate-notebooks</automated>
  </verify>
  <done>A single data/model/config/source-hash-verified operator handoff can launch all three fresh experiments without manual config copying, hidden data/mode choices, or a mandatory Git identifier.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Authorize Colab packages, execute three runs, and return bundle roots</name>
  <read_first>
    - `data/models/phase40/full-run-request.json`
    - `.planning/phases/40-multi-model-training-evidence/40-COLAB-RUN-HANDOFF.md`
    - `.planning/phases/40-multi-model-training-evidence/40-RESEARCH.md`
  </read_first>
  <action>Pause local repository automation. The operator first verifies the exact `bitsandbytes==0.50.1` and `matplotlib==3.11.1` release pages/files on official PyPI and explicitly approves or rejects each pin. If either is rejected, run no install or training cell and return the decisions plus reasons. If both are approved, copy the exact hash-pinned `phase40-train-validation.zip` from the run request to `/content/drive/MyDrive/internship-phase40/phase40-train-validation.zip`, mount Drive, and run the notebook's repository-controlled `phase40-verify-input-bundle` step before any data loader, extraction, preflight, training import, or JSONL member open. That automatic step must lexically match the fixed archive/extraction locations, hash the archive, inspect and accept exactly `phase40-input-manifest.json`, `train.jsonl`, and `val.jsonl`, verify manifest hash/schema/counts/member hashes and the snapshot-ID version, reject every extra/test-like/traversal/symlink member, and only then open/hash/extract train and validation to `/content/phase40-input-v1`. A failure stops all three runs and returns the exact stage; it is never worked around by browsing Drive or using a laptop-relative split path. The operator then executes the three validated notebooks in the requested order; every notebook must verify the request-pinned source archive and inventory hashes before extracting/importing repository code, consume only the verified input contract/snapshots, and retain source plus input identities in evidence. The operator copies each complete output directory unchanged to its fixed local root and returns the two package decisions, assigned GPU identity per run, and the three fixed root paths. This checkpoint performs only human provenance decisions, external mount/copy/notebook execution, and bundle-return actions; repository code performs all verification and evidence generation. It does not claim to write the authority record, rehash returned bundles locally, regenerate comparison graphs, create the comparison manifest, or write the report.</action>
  <what-built>Three canonical notebooks, exact configs, one schema-validated run request, and the already-implemented `phase40-finalize-comparison` automation from Plan 40-02; no Colab package/model has been installed by this checkpoint yet.</what-built>
  <instructions>
    1. Inspect https://pypi.org/project/bitsandbytes/0.50.1/ and its distribution files.
    2. Inspect https://pypi.org/project/matplotlib/3.11.1/ and its distribution files.
    3. Confirm both exact pins are acceptable for the assigned Colab runtime, or reject one with a reason and stop before installation.
    4. Copy the exact request-pinned input archive to `/content/drive/MyDrive/internship-phase40/phase40-train-validation.zip`, mount Drive, and run the automated input verifier with extraction root `/content/phase40-input-v1`; stop if its fixed path, exact-three-member, manifest, count, archive, or member-hash check fails.
    5. Verify the source archive and inventory hashes from `full-run-request.json` before any code extraction/import; run `notebooks/phase40/qwen_lora_colab.ipynb` from step zero using only the verified input snapshots and copy its whole bundle to `data/models/phase40/full/qwen-lora`.
    6. Run `notebooks/phase40/qwen_qlora_colab.ipynb`, confirm the genuine four-bit proof before step zero, and copy its whole bundle to `data/models/phase40/full/qwen-qlora`.
    7. Run `notebooks/phase40/phobert_colab.ipynb`, confirm full encoder/head trainables and the same immutable validation IDs, and copy its whole bundle to `data/models/phase40/full/phobert`.
    8. Return both decisions/reasons, each assigned GPU type, and the three local root paths; do not edit bundle files.
  </instructions>
  <verification>Before resuming, confirm the payload states both exact package decisions; an approval includes all three fixed roots and per-run GPU identities, while a rejection includes a reason and no claimed returned bundle.</verification>
  <resume-signal>Return `approve Colab pins; three bundles returned` with all three fixed roots and GPU types, or `reject <package>: <reason>` before running install/training cells.</resume-signal>
  <acceptance_criteria>
    - Human provenance/legitimacy decisions are explicitly returned for both research-flagged packages.
    - No install cell runs before approval and no unpinned replacement is selected implicitly.
    - Approval returns exactly three fixed local bundle roots and per-run GPU identities; rejection returns a reason and no claimed bundles.
    - Every approved run evidence records the exact request-pinned source archive and inventory hashes after verify-before-extract/import execution.
    - Every approved run evidence records the same input archive, manifest/member hashes, fixed transfer mapping, and snapshot-ID version after the automated exact-membership/hash verifier passed before any JSONL member open.
    - No notebook accepts a laptop-relative split path, generic Drive discovery, alternate extraction root, extra/test-like member, or manual replacement data file.
    - No comparison/evidence/report file is attributed to this checkpoint.
  </acceptance_criteria>
  <verify>
    <automated>python -m src.model_adaptation.cli phase40-verify-run-request --request data/models/phase40/full-run-request.json --validate-notebooks</automated>
  </verify>
  <done>The executor has a structured human resume payload containing either explicit package rejection or approved package decisions plus the three returned roots and GPU identities; no automated finalization has yet run.</done>
</task>

<task type="auto">
  <name>Task 3: Persist the operator return and finalize the comparison automatically</name>
  <precondition>Task 2 returned explicit decisions for both package pins and either a rejection reason or the three fixed local bundle roots plus per-run GPU identities.</precondition>
  <read_first>
    - `.planning/phases/40-multi-model-training-evidence/40-COLAB-RUN-HANDOFF.md`
    - `data/models/phase40/full-run-request.json`
    - `data/models/phase40/matched-qwen-config.json`
    - `data/models/phase40/phobert-config.json`
    - `.planning/phases/40-multi-model-training-evidence/40-AI-SPEC.md`
  </read_first>
  <files>data/models/phase40/full/, data/models/phase40/comparison-manifest.json, data/models/phase40/review/review-queue.jsonl, data/models/phase40/review/review-queue-manifest.json, data/models/phase40/review/reviewer-return.template.jsonl, .planning/phases/40-multi-model-training-evidence/40-VALIDATION-COMPARISON.md</files>
  <action>Sanitize the Task 2 resume payload, accepting only the two exact package/version decisions, short non-secret reasons, the three canonical repository-relative bundle roots, and normalized GPU identity fields. Invoke the Plan 40-02 `phase40-finalize-comparison` implementation with repeated typed `--package-decision` values and, only for an approved return, exactly three `--bundle-root` plus three `--gpu-identity` values; the CLI validates these into `ColabOperatorReturn` and persists the authority/hardware facts exclusively in `comparison-manifest.json`, leaving `full-run-request.json` byte-unchanged. If either package was rejected, call the finalizer's rejection branch without root/GPU options: open no bundle root, write explicit pre-start failure evidence and a human-readable blocked report, preserve the requested model identities, and leave Phase 40 incomplete without substituting a package or model. If both were approved, lexically compare all three returned paths to the fixed RunRequest roots before any open; a decoy or alternate root must yield zero opens. Then rehash and schema-verify every complete bundle, including exact source archive/inventory and input archive/internal-manifest/member/snapshot identities. Require each run to attest that the fixed Drive archive and extraction root passed automated verification before data open, reject any extra/test-like input member or laptop-relative path, and require exactly 219 raw validation predictions whose `validation_row_id` sequence equals the immutable canonical snapshot sequence byte-for-byte for Qwen LoRA, Qwen QLoRA, and PhoBERT. Verify generated-output invalid states and selected-checkpoint rationale, enforce fresh step-zero lineage plus exact Qwen controls/decoder fields with only the quantization proof delta, regenerate every graph from raw logs, verify trainer state and complete adapter/model/checkpoint hashes, and atomically write the comparison manifest and report. The report includes every model and safety failure, the predeclared selection result, risky recall floors, invalid outputs, hardware-confounding status, and the single-seed/no-significance limitation. Only after the comparison is frozen, re-run canonical train/validation preflight and pass that exact immutable validation snapshot plus the selected-checkpoint predictions to `build_phase40_review_queue`; the builder accepts no data path and cannot derive replacement IDs. Require exact validation/prediction stable-ID sets and canonical order, then join by ID and copy immutable `raw_message` and `source_row_sha256` directly from the snapshot, with gold label, prediction or invalid state, selected-checkpoint/run/artifact identity, sequence, and all tags. Queue every invalid output, every risky-to-benign error, every misclassification whose gold or prediction is `zalo_social_engineering`, every benign-to-risky error, and every risky-family cross-confusion; add the deterministic SHA-256-ranked ceiling-10% correct sample per model/gold-class, de-duplicate overlapping model-row keys while retaining all tags, and emit a reviewer-return template plus queue manifest. Store the frozen comparison-manifest, canonical validation snapshot-ID algorithm/sequence hash, and input archive/manifest/member identities in queue provenance. Run the full model-adaptation regression suite after finalization. No value is copied manually into metrics, graphs, the manifest, report, or review queue.</action>
  <acceptance_criteria>
    - Three independently trained model artifacts and complete evidence bundles verify after local copy.
    - All three run bundles prove the same request-pinned input archive/member identities and exact immutable validation snapshot-ID sequence after fixed-path automated verification; any alternate, extra, test-like, reordered, or locally re-derived input identity fails.
    - Package decisions, reasons, returned roots, and GPU identities are persisted only by this auto task after sanitization; a rejected decision creates explicit pre-start failure evidence and no bundle root is opened.
    - Qwen full runs share exact controls and differ only by the allowed quantization treatment/proof; QLoRA is genuinely four-bit.
    - All graphs reproduce from raw logs, all model/checkpoint hashes verify, and every bundle has 219 ordered validation rows.
    - A complete comparison produces a deterministic de-duplicated review queue whose slice counts reconcile to the raw predictions and whose correct samples reproduce from the declared hash/ranking rule.
    - Every queue row contains the exact canonical full message and source-row hash copied from the same immutable snapshot and joined by its supplied stable ID; missing/reordered/mutated/locally reconstructed validation or prediction IDs, messages, or hashes fail before queue publication.
    - Comparison shows every model/result, risky-class recall floors, invalid outputs, hardware confounding, and single-seed limitation without unsupported inference.
    - No held-out data, probe artifact, or external model judge is required or used; Git identity is optional supplemental metadata and never an evidence prerequisite.
  </acceptance_criteria>
  <verify>
    <automated>python -m src.model_adaptation.cli phase40-finalize-comparison --request data/models/phase40/full-run-request.json --manifest data/models/phase40/comparison-manifest.json --report .planning/phases/40-multi-model-training-evidence/40-VALIDATION-COMPARISON.md --review-root data/models/phase40/review --verify-only</automated>
  </verify>
  <done>With approvals and three valid roots, three real full models, complete reproducible evidence, an honest validation-only comparison, and its canonical-message-bound deterministic review queue are frozen for Plan 40-06; with a package rejection, sanitized pre-start failure evidence is frozen and Phase 40 remains explicitly incomplete.</done>
</task>

</tasks>

## Artifacts this phase produces

- `data/models/phase40/source/phase40-source.zip` and `phase40-source-manifest.json`: deterministic runtime source bundle, sorted inventory, and archive/inventory SHA-256 authority; Git identity is optional.
- `data/models/phase40/input/phase40-train-validation.zip`: deterministic stored ZIP containing exactly the canonical `train.jsonl`, `val.jsonl`, and `phase40-input-manifest.json`, pinned to fixed Colab/Drive paths and verified before data-member open.
- `data/models/phase40/full-run-request.json`: immutable canonical data/model/source/package-candidate-and-pin/run/output handoff contract; it contains no returned package decision or hardware fact.
- `data/models/phase40/matched-qwen-config.json`: exact LoRA/QLoRA controlled configuration and approved quantization delta.
- `data/models/phase40/phobert-config.json`: transparent full PhoBERT classifier configuration.
- `data/models/phase40/full/qwen-lora/`: fresh model adapter/checkpoints, raw events/predictions, trainer state, graphs, metrics, hashes, and evidence.
- `data/models/phase40/full/qwen-qlora/`: fresh genuine four-bit model adapter/checkpoints, raw events/predictions, trainer state, graphs, metrics, hashes, and proof.
- `data/models/phase40/full/phobert/`: fresh full classifier model/checkpoints, preprocessing/truncation records, raw events/predictions, trainer state, graphs, metrics, hashes, and evidence.
- `data/models/phase40/comparison-manifest.json`: three-model admissibility and frozen validation comparison identities.
- `data/models/phase40/review/review-queue.jsonl`, `review-queue-manifest.json`, and `reviewer-return.template.jsonl`: immutable message/source-row-bound review handoff for Plan 40-06.
- `.planning/phases/40-multi-model-training-evidence/40-COLAB-RUN-HANDOFF.md`: operator instructions.
- `.planning/phases/40-multi-model-training-evidence/40-VALIDATION-COMPARISON.md`: evidence-backed LoRA/QLoRA and PhoBERT/Qwen comparison.

## Specless edge-coverage assumptions

| Probe row | Status | Concrete predicate |
|---|---|---|
| TRAIN-05 / unclassified | flagged | The comparison requires exactly three complete model rows and reports every value/failure regardless of winner; a missing or losing model cannot be omitted. |

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Three real independently trained models with logged curves and measured LoRA/QLoRA plus Qwen/PhoBERT answers | 04-06 | COVERED | Local probes, three fresh full models, comparison, and review closure cover the goal. |
| REQ | TRAIN-01 | RTX LoRA probe, discard, fresh Colab LoRA, raw curve | 02,04,05 | COVERED | Lifecycle, real local run, then fresh full run. |
| REQ | TRAIN-02 | RTX genuine QLoRA probe, discard, fresh fail-closed four-bit Colab run | 01,02,04,05 | COVERED | Mode proof precedes both runs. |
| REQ | TRAIN-03 | Exact matched Qwen controls and honest hardware-aware comparison | 01,02,05 | COVERED | Exact config diff and confounding flag. |
| REQ | TRAIN-04 | Full PhoBERT classification-head baseline | 03,05 | COVERED | No PEFT/QLoRA path. |
| REQ | TRAIN-05 | Honest PhoBERT-versus-Qwen measured comparison | 02,03,05,06 | COVERED | Shared evaluator, all three rows retained, and qualitative review reported without rewriting results. |
| REQ | TRAIN-06 | Raw-log-derived graphs and complete provenance bundle without required Git ID | 01,02,03,04,05,06 | COVERED | Evidence schema, callbacks, source archive, notebooks, real runs, comparison, and review verification. |
| RESEARCH | contract/modes/metrics | Reject-before-open inputs, typed tuples, response-only Qwen loss, invalid-output state | 01 | COVERED | Includes empty/encoding and impossible-combination tests. |
| RESEARCH | evidence/restart | Append-only events, timing/VRAM/throughput, atomic evidence, exact resume, graphs | 02 | COVERED | Includes empty/adjacency/ordering predicates. |
| RESEARCH | PhoBERT/notebooks | Deterministic full classifier and three fresh thin notebooks | 03 | COVERED | Static validators block historical paths/duplicate logic. |
| RESEARCH | immutable notebook source and data transfer | Retrieve verified code plus exactly the canonical train/validation bytes without making Git identity mandatory or exposing held-out data | 02,03,05 | COVERED | Run-request schema, source archive/inventory hashes, deterministic exact-three-member input archive, fixed Colab paths, verify-before-open/extract notebook gate, and returned-evidence identity. |
| RESEARCH | package legitimacy | Human approval before flagged package installs | 04,05 | COVERED | Both local and Colab boundaries are blocking-human. |
| RESEARCH | Vietnamese review | Review every invalid output, risky-to-benign miss, Zalo error, benign-to-risky error, cross-scam error, and deterministic correct sample | 02,05,06 | COVERED | Message-bound queue/persistence tooling is fake-tested in 02, the queue is frozen in 05, and exact-coverage human evidence closes in 06. |
| CONTEXT | final-corpus prerequisite | Consume Phase 39 downstream contract only after Phase 39 closure | 01,04,05 | COVERED | Precondition and hash/count enforcement. |
| CONTEXT | RTX probes | Bounded real outcomes, measured ETA/resources, adapters discarded | 02,04 | COVERED | Genuine failure retained; OOM never engineered. |
| CONTEXT | fresh matched Colab runs | Fresh step-zero LoRA/QLoRA with shared revision/controls | 02,03,05 | COVERED | Probe lineage prohibited. |
| CONTEXT | PhoBERT baseline | Ordinary full classifier, not novelty QLoRA | 03,05 | COVERED | Typed identity enforces it. |
| CONTEXT | evidence and graphs | Complete hash-linked bundles and mechanically generated graphs | 02,04,05,06 | COVERED | Source archive and all run/review evidence are hash-linked; Git identifier intentionally not required. |
| CONTEXT | evaluation boundary | Validation only; Phase 41 owns held-out evaluation and later all-data fit | 01-06 | COVERED | Held-out access is a plan-wide high-severity prohibition. |

Deferred test evaluation and optional all-data deployment fitting remain excluded because CONTEXT assigns them to Phase 41.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Official package/model sources -> Colab | Third-party code/checkpoints cross into an external GPU runtime under operator authority. |
| Source archive -> Colab imports | Repository code crosses into the external runtime only after archive and inventory identities verify. |
| Canonical inputs -> external notebook | Frozen train/validation data cross into Colab; held-out data must not accompany them. |
| Colab/Drive -> local evidence roots | Large externally executed bundles cross a copy/storage boundary and cannot be trusted until rehashed. |
| Verified bundles -> scientific comparison | Measurements cross into selection and defense claims with safety/hardware/uncertainty limits. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-40-05-SC | Tampering / Elevation of privilege | Colab package installs | high | mitigate | Blocking-human official-PyPI legitimacy approval for both flagged exact pins before notebook installation. |
| T-40-05-01 | Information disclosure | notebook input/upload | high | mitigate | Fixed two-input run request, static notebook gate, no secrets/external inference, and no broad split discovery. |
| T-40-05-02 | Tampering / Repudiation | returned bundles | high | mitigate | Local rehash, schema/completeness validation, trainer-state/prediction counts, model artifact identity, and graph regeneration before admission. |
| T-40-05-03 | Spoofing | QLoRA/full-run identity | high | mitigate | Fresh step-zero identity, exact matched config, genuine four-bit proof, and no probe-parent lineage. |
| T-40-05-04 | Repudiation | scientific comparison | high | mitigate | Preserve all model rows/failures, explicit recall floors/invalid outputs, hardware-confounding flag, single-seed limitation, and no significance claim. |
| T-40-05-05 | Denial of service | external training interruption | medium | mitigate | Exact-compatible full-run checkpoint resume, retained failed attempts, and no automated retry loop across incompatible environments. |
| T-40-05-06 | Tampering / Repudiation | Vietnamese review queue | high | mitigate | Stable-ID join to preflighted validation rows, immutable full message/source-row hash, complete slice union, deterministic correct sampling, and queue/comparison hash verification before Plan 40-06. |
| T-40-05-07 | Tampering / Spoofing | notebook source bundle | high | mitigate | Deterministic archive/inventory hashes are frozen in the request, verified before extraction/import, and required in every returned evidence bundle; Git identity is optional. |
| T-40-05-08 | Tampering / Information disclosure | canonical input transfer | high | mitigate | Deterministic exact-three-member archive, fixed Drive/extraction paths, request-pinned archive/manifest/member hashes, and automated verify-before-data-open reject extra/test-like/substituted inputs and laptop-relative paths. |
</threat_model>

<verification>
Before the Colab checkpoint, verify the immutable source archive/inventory, deterministic input archive/internal manifest/exact member set, run request, and notebooks statically, including source verify-before-extract/import and input verify-before-data-member-open ordering plus fixed Drive/extraction paths. After the operator returns bundles, run the Task 3 comparison finalizer, regenerate graphs, generate and verify the deterministic review queue, and run `python -m pytest tests/model_adaptation -q --basetemp .tmp/pytest-p40-05-regression`. Comparison finalization must fail on any missing/empty bundle, wrong model/source/input/snapshot identity, extra/test-like input member, laptop-relative path, missing QLoRA proof, config drift, nonzero full-run origin, probe lineage, prediction count/order/ID error, hash mismatch, missing raw graph source, or secret/personal path. Queue finalization must fail on any omitted mandatory slice, non-reproducible correct sample, missing/duplicate/reordered/locally reconstructed validation or prediction ID, mutated full message/source-row hash, or canonical-order drift. Plan 40-06 performs the blocking human review and review-evidence finalization.
</verification>

<success_criteria>
- One fresh complete Qwen LoRA, genuine four-bit Qwen QLoRA, and full PhoBERT model exists.
- Qwen controls are exactly matched except for quantization; hardware differences are visible and speed claims are appropriately limited.
- Three complete validation bundles and every graph verify from raw evidence after the Colab copy boundary.
- The comparison reports all results, safety-gate failures, invalid outputs, and single-seed limitation honestly.
- Every mandatory error slice and deterministic per-model/per-class correct sample is frozen in a queue containing the canonical full message, exact source-row hash, gold/prediction state, run/artifact identity, and all tags for Plan 40-06.
- Every notebook and returned bundle is tied to the same hash-verified source archive/inventory without requiring a Git commit identifier.
- Every notebook and returned bundle is tied to the same hash-verified exact-three-member train/validation archive and immutable snapshot-ID sequence, with no laptop-relative or held-out transfer path.
- Phase 40 makes no held-out, all-data deployment, external-judge, Git-authorship, t-test, variance, or significance claim.
</success_criteria>

<output>
Create `.planning/phases/40-multi-model-training-evidence/40-05-SUMMARY.md` when done.
</output>
