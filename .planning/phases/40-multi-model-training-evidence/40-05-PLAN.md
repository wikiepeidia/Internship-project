---
phase: 40-multi-model-training-evidence
plan: 05
type: execute
wave: 5
depends_on:
  - 40-04
files_modified:
  - data/models/phase40/source/phase40-source.zip
  - data/models/phase40/source/phase40-source-manifest.json
  - data/models/phase40/input/phase40-train-validation.zip
  - data/models/phase40/full-run-request.json
  - data/models/phase40/two-full-model-scope-amendment.json
  - data/models/phase40/matched-qwen-config.json
  - data/models/phase40/phobert-config.json
  - data/models/phase40/full/
  - data/models/phase40/comparison-manifest.json
  - data/models/phase40/comparison-report.md
  - data/models/phase40/selected-prediction-bundles.json
  - data/models/phase40/review/review-queue.jsonl
  - data/models/phase40/review/review-queue-manifest.json
  - data/models/phase40/review/reviewer-return.template.jsonl
  - .planning/phases/40-multi-model-training-evidence/40-COLAB-RUN-HANDOFF.md
  - .planning/phases/40-multi-model-training-evidence/40-VALIDATION-COMPARISON.md
autonomous: false
requirements:
  - TRAIN-01
  - TRAIN-02
  - TRAIN-03
  - TRAIN-04
  - TRAIN-05
  - TRAIN-06
user_setup:
  - service: "Google Colab and operator-managed Drive (inactive contingency)"
    why: "The primary QLoRA and PhoBERT runs execute locally. Colab may be activated only if development validation is unacceptable before the reserved-test gate; it is never required for full ordinary LoRA or used in response to held-out results."
    dashboard_config:
      - task: "If and only if the pre-test contingency is activated, verify the exact package releases before executing notebook install cells."
        location: "Official PyPI project/release pages"
      - task: "Run only the affected QLoRA or PhoBERT notebook from step zero and return its complete bundle; the ordinary-LoRA notebook remains retired."
        location: "Google Colab notebook UI and operator-managed Drive"
      - task: "Copy the hash-pinned train/validation-only archive to the fixed Drive path and mount it read-only for automated verification before any training-data member is opened."
        location: "/content/drive/MyDrive/internship-phase40/phase40-train-validation.zip"
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The sealed ordinary-LoRA probe remains resource/ETA evidence only; no fresh full ordinary-LoRA run is required or authorized."
    - "A fresh genuine four-bit QLoRA full run starts locally at step zero from the pinned base revision and frozen controls, then produces a verified Q8_0 GGUF before PhoBERT starts."
    - "A fresh PhoBERT encoder-plus-classification-head model trains fully on the same canonical train/validation identities and locked four-class task."
    - "Both full-run bundles contain complete raw logs, curves, trainer state, model/checkpoint hashes, environment/resource evidence, and exactly 219 ordered validation predictions."
    - "The final QLoRA-versus-PhoBERT validation comparison reports both results regardless of winner, preserves failed safety gates, and makes no held-out or statistical-significance claim."
    - "The immutable earlier three-run request remains byte-unchanged historical provenance; the dated amendment makes its Qwen LoRA slot dormant and comparison finalization admits only QLoRA and PhoBERT."
    - "The additive machine-readable two-model scope amendment binds the immutable request SHA-256 `2512dbe6d7c5b8c16141ebdbdc848382e56b3a5737e8aeea51d7fb89447c643a`; it never rewrites the request."
    - "Training, GGUF export, and PhoBERT remain bound to immutable `source-runtime-v3`; amended comparison code launches only after an external PowerShell/OS preflight records PASS for every amendment-listed source path/byte-count/SHA and tree hash, then in-process code rechecks that authority before any model bundle read."
    - "Completed local bundles prove their exact package/runtime identities in run evidence; comparison admission is not gated by nonexistent Colab package-approval decisions."
    - "The immutable run request pins a deterministic repository source archive and inventory by SHA-256, so every notebook executes a verified code artifact without requiring a Git commit identifier."
    - "The immutable run request also pins one deterministic exact-three-member train/validation input archive, fixed Drive/extraction paths, and archive/manifest/member identities; automated notebook verification passes before either JSONL member opens."
    - "Automated comparison finalization emits a deterministic review queue whose every row carries the canonical full message and exact source-row hash, ready for the dependent Vietnamese-review closure plan."
  artifacts:
    - path: "data/models/phase40/source/phase40-source.zip"
      provides: "Deterministic repository source bundle used by the local runtime and retained historical notebooks"
    - path: "data/models/phase40/source/phase40-source-manifest.json"
      provides: "Sorted source inventory plus per-file and archive SHA-256 identities"
    - path: "data/models/phase40/input/phase40-train-validation.zip"
      provides: "Deterministic exact-three-member canonical train/validation transfer archive with internal manifest"
    - path: "data/models/phase40/full-run-request.json"
      provides: "Immutable operator handoff identity, package pins, expected outputs, and run order"
    - path: "data/models/phase40/two-full-model-scope-amendment.json"
      provides: "Additive 2026-08-25 authority binding the immutable request hash while activating only QLoRA and PhoBERT completion roots"
    - path: "data/models/phase40/matched-qwen-config.json"
      provides: "Single source of matched LoRA/QLoRA controls and approved quantization delta"
    - path: "data/models/phase40/full/qwen-qlora/run-evidence.json"
      provides: "Verified fresh full genuine four-bit Qwen QLoRA training/validation bundle"
    - path: "data/models/phase40/full/phobert/run-evidence.json"
      provides: "Verified fresh full PhoBERT training/validation bundle"
    - path: "data/models/phase40/comparison-manifest.json"
      provides: "Two-model QLoRA/PhoBERT admissibility, artifact identities, measured metrics, and execution-origin status"
    - path: ".planning/phases/40-multi-model-training-evidence/40-VALIDATION-COMPARISON.md"
      provides: "Honest human-readable LoRA/QLoRA and PhoBERT/Qwen validation comparison"
    - path: "data/models/phase40/review/review-queue.jsonl"
      provides: "Message-bound deterministic mandatory-error and calibration review queue"
    - path: "data/models/phase40/review/review-queue-manifest.json"
      provides: "Canonical validation, selected-checkpoint, queue, and sampling provenance"
  key_links:
    - from: ".planning/phases/40-multi-model-training-evidence/40-LOCAL-PROBE-REPORT.md"
      to: ".planning/phases/40-multi-model-training-evidence/40-VALIDATION-COMPARISON.md"
      via: "ordinary-LoRA evidence appears only in the resource/feasibility section and never as a completed quality row"
      pattern: "gpu_pressure|18.42|18.88|probe"
    - from: "data/models/phase40/full-run-request.json"
      to: "data/models/phase40/two-full-model-scope-amendment.json"
      via: "the additive scope record binds request SHA-256 2512dbe6d7c5b8c16141ebdbdc848382e56b3a5737e8aeea51d7fb89447c643a and marks only QLoRA/PhoBERT active without mutating the request"
      pattern: "schema_version|original_run_request_sha256|active_full_run_ids|waived_full_run_id|waived_returned_root|full_lora_disposition|waiver_action|waiver_basis|execution_policy|colab_contingency_policy|no_held_out_boundary"
    - from: "data/models/phase40/two-full-model-scope-amendment.json"
      to: "data/models/phase40/comparison-manifest.json"
      via: "an external pre-launch PowerShell/OS preflight records PASS for every listed source path/byte-count/SHA and tree hash before Python launch; in-process code independently rechecks the same authority before any model bundle read"
      pattern: "comparison_finalizer_authority|files|source_tree_sha256"
    - from: "data/models/phase40/source/phase40-source-manifest.json"
      to: "notebooks/phase40/*.ipynb"
      via: "the run request supplies the repository-relative archive and archive/inventory SHA-256 values that every notebook verifies before extraction/import"
      pattern: "source_archive_sha256|source_inventory_sha256"
    - from: "data/models/phase40/input/phase40-train-validation.zip"
      to: "notebooks/phase40/*.ipynb"
      via: "the operator copies one hash-pinned archive to the fixed Drive path, then repository automation verifies exact membership/manifest/archive/member hashes before opening or extracting train/validation data"
      pattern: "phase40-verify-input-bundle|phase40-input-manifest.json|/content/phase40-input-v1"
    - from: "data/models/phase40/matched-qwen-config.json"
      to: "notebooks/phase40/qwen_qlora_colab.ipynb"
      via: "QLoRA notebook consumes the same controls plus only the approved quantization proof delta"
      pattern: "matched-qwen-config"
    - from: "data/models/phase40/full/*/events.jsonl"
      to: "data/models/phase40/comparison-manifest.json"
      via: "evidence verification and graph regeneration precede comparison admission"
      pattern: "source_sha256"
    - from: "data/models/phase40/full/*/raw-predictions/*.jsonl"
      to: "data/models/phase40/review/review-queue.jsonl"
      via: "the verified comparison finalizer joins selected-checkpoint predictions by stable ID to the already-preflighted canonical validation snapshot, persists full message/source hash, and derives slices plus deterministic samples"
      pattern: "raw_message|source_row_sha256|invalid_output|risky_to_benign|zalo_error|benign_to_risky|cross_scam|correct_calibration_sample"
  prohibitions:
    - value: "Opening, reading, hashing, uploading, evaluating, plotting, or selecting on the held-out Phase 41 partition"
      status: unverified
    - value: "Starting a full run from any local probe adapter, checkpoint, event stream, or nonzero step"
      status: unverified
    - value: "Calling the QLoRA full run complete without genuine NF4 four-bit and adapter-gradient proof"
      status: unverified
    - value: "Hand-entering graph values or using a notebook-local trainer, parser, evaluator, or renderer"
      status: unverified
    - value: "Hiding a losing model, failed recall floor, invalid output, or hardware difference"
      status: unverified
    - value: "Claiming causal speed superiority across unlike accelerators or statistical superiority from one seed"
      status: unverified
    - value: "Requiring a Git commit identifier in the evidence bundle"
      status: unverified
    - value: "Omitting a mandatory review-queue slice, manually cherry-picking correct examples, or emitting a queue row without its canonical full message and exact source-row hash"
      status: unverified
    - value: "Sending laptop-relative split paths to Colab, transferring any held-out/test-like member, or opening a train/validation member before the request-pinned input archive and manifest verify"
      status: unverified
---

## Routing update — 2026-08-24

Do not treat this plan's older Colab-first wording as authorization to start all three external runs. The sealed local LoRA result proves severe sustained VRAM pressure and a provisional 18.42–18.88-hour compute-only schedule, so a full local ordinary-LoRA run is not the preferred use of the current deadline. However, LoRA did not OOM and the result does not decide QLoRA performance.

Plan 40-04 must first complete the genuine local QLoRA 5+40 measurement. If that verified probe has no OOM/pressure failure and its measured ETA is acceptable, the fresh full QLoRA run may stay local to save Colab credit; otherwise it routes to Colab. Any full run starts fresh at step zero and may not resume from either disposable local probe. The fresh full LoRA/PhoBERT obligations remain visible until the project requirements are explicitly changed; this note does not silently waive them.

## Routing resolution — 2026-08-25

The genuine laptop QLoRA probe completed successfully: median 3.462389 seconds per measured optimizer step, no OOM, and a formula-based 1,245-step estimate of 4,369.750238 seconds (72.83 minutes) including the single measured validation/save overhead. Local full QLoRA is therefore feasible; the earlier 18.42–18.88-hour provisional estimate applies only to the incomplete ordinary-LoRA window and must never be reused for QLoRA.

The operator nevertheless chose the fresh Colab path for all full evidence runs. The reason is a consistent, downloadable experiment package—complete logs, curves, validation outputs, checkpoints, and post-training GGUF export—not a claim that Colab is required because laptop QLoRA is too slow. All Qwen full runs still begin at step zero from the pinned base and consume the same frozen matched controls; no disposable local adapter/checkpoint is transferred. The QLoRA notebook must finish and verify the full run before it merges the selected adapter into an ordinary base, converts `q8_0`, performs the pinned `llama-cpp-python` load smoke, persists the export to Drive, and exposes any browser download as an explicit human action.

## Local execution supersession — 2026-08-25

The operator subsequently selected the fresh local QLoRA route to preserve Colab credit. This supersedes only the location of the QLoRA full run; it does not weaken its step-zero lineage, matched controls, quantization proof, 219-row validation cadence, evidence bundle, or GGUF gates. The generated `40-COLAB-RUN-HANDOFF.md` was frozen as the all-Colab fallback at this point in the chronology; the later dated scope amendment explicitly reclassifies that document as historical contingency guidance.

The first v2 attempt produced useful intermediate validation/checkpoint evidence, then exact-resume testing exposed an omitted run-ID segment in the mutable output root and a Python-object-versus-canonical-JSON validation mismatch in the operator. The trainer was intentionally interrupted at the step-150 validation boundary and its terminal failure event, checkpoints, event history, and telemetry remain preserved. Those artifacts are failure/debug evidence only and may not seed another run.

After focused regression, the operator sealed a clean v3 request `2512dbe6d7c5b8c16141ebdbdc848382e56b3a5737e8aeea51d7fb89447c643a` with source archive `eae64f17383d749a7759391d766ad59b337d35155ae89744adeaba8631e71a66`, source inventory `5903dd5d68881916424e0b529760c3e8810b89a7c207aa714f13171fccf02a3d`, and unchanged train/validation archive `12136f9a79e7c9852f6b317f284a9a018710aa66af54de4714ec66e8cf92bf84`. The active immutable root is `D:\PROJEct\AI MODELS\phase40-full-local-20260825\transfer-root-v3\data\models\phase40\full\qwen-qlora`; mutable checkpoints stay under `work-v3\phase40-qwen-qlora-full-seed42-v1` until verification and GGUF export finish. Corrected external telemetry is `controller\system-telemetry-v3.csv`, outside the immutable run root.

A hash-bound fail-closed supervisor may resume at most twice and only from the newest exact checkpoint containing both the compatibility manifest and sealed resume history. It invokes run-evidence verification before Q8_0 export and the pinned llama.cpp load smoke. A verified successful local QLoRA bundle must not be rerun on Colab. Full LoRA, PhoBERT, three-model finalization, and Plan 40-06 remain open.

## Local PhoBERT execution amendment — 2026-08-25

The operator additionally selected a fresh local PhoBERT run on the NVIDIA GeForce RTX 5050 Laptop GPU. This supersedes only the `user_setup` and Task 2/Task 3 wording that requires PhoBERT to execute externally: Qwen LoRA remains externally routed, and Colab remains PhoBERT's terminal-failure fallback. The local run must use the same frozen v3 request, source, input, pinned `vinai/phobert-base-v2` revision, and `phobert-config.json`; begin at step zero without a probe, model, or checkpoint parent; and complete the full three-epoch encoder-plus-classification-head lifecycle. No package/model substitution, control drift, or reduction of TRAIN-04, TRAIN-05, or TRAIN-06 evidence is authorized.

The armed controller may start PhoBERT only after the Qwen QLoRA supervisor finishes, Qwen run-evidence and GGUF verification pass, the original Qwen trainer exits, and GPU use stays at or below 2,048 MiB for three consecutive samples; the two runs must never overlap. PhoBERT retains exact-resume verification, separate system/GPU telemetry, exactly 219 ordered validation predictions, raw logs, curves, trainer state, model/checkpoint hashes, graphs, and the canonical `data/models/phase40/full/phobert` bundle. Task 2 records local Windows/RTX 5050 origin, and Task 3 verifies request-pinned source/input identities through local materialization instead of requiring Colab Drive evidence. External package decisions, Qwen LoRA, hardware-confounding disclosure, three-model comparison, and Plan 40-06 remain unchanged.

## User-approved local two-model scope amendment — 2026-08-25

This is the controlling scope and supersedes the final sentence of the local
PhoBERT amendment plus every older active requirement for a full Qwen LoRA run,
two externally returned bundles, or a three-model comparison. The primary path
is local Qwen QLoRA, complete run-evidence verification, verified Q8_0 GGUF,
then local PhoBERT. The sealed ordinary-LoRA probe is retained unchanged as
resource-pressure and ETA evidence; no full ordinary-LoRA run is required or
authorized. The final quality comparison contains exactly Qwen QLoRA and
PhoBERT.

The immutable three-run request and matched config remain byte-preserved
historical provenance. Canonical additive authority is
`data/models/phase40/two-full-model-scope-amendment.json`, which binds request
SHA-256 `2512dbe6d7c5b8c16141ebdbdc848382e56b3a5737e8aeea51d7fb89447c643a`
and activates exactly the QLoRA and PhoBERT run IDs/roots without rewriting the
request. The handoff is now marked historical/contingency. Its Qwen LoRA run
ID/root and notebook do not imply execution and are dormant. The
72.83-minute probe value estimates 1,245 optimizer steps plus one measured
evaluation/save overhead; it is not a complete evidence-cadence duration. Live
full-run observations currently project roughly 12.85 hours end to end because
repeated 219-row checkpoint generation dominates, and that projection remains
interim until final evidence seals.

Colab is not scheduled. It may be activated only before the reserved Phase 41
partition is opened, only if frozen development-validation review finds the
local QLoRA or PhoBERT result unacceptable, and only for the affected branch
from step zero. It is never a full-LoRA obligation, never an automatic rerun,
and never a response to held-out results. After the reserved-test gate opens,
training, dataset repair, and checkpoint reselection are closed for that
evaluation cycle.

<objective>
Produce and freeze two real independently trained local models and their honest validation-only comparison, while retaining the sealed ordinary-LoRA probe as resource evidence.

Purpose: Replace historical or literature-only claims with fresh Qwen QLoRA and PhoBERT measurements while keeping resource attribution, artifact trust, contingency authority, and test isolation explicit.
Output: Hash-verified source/config authority, two verified full-run bundles, a verified Q8_0 GGUF, regenerated graphs, comparison manifest/report, and a canonical-message-bound review queue for Plan 40-06.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-multi-model-training-evidence/40-CONTEXT.md
@.planning/phases/40-multi-model-training-evidence/40-AI-SPEC.md
@.planning/phases/40-multi-model-training-evidence/40-RESEARCH.md
@.planning/phases/40-multi-model-training-evidence/40-03-SUMMARY.md
@.planning/phases/40-multi-model-training-evidence/40-04-SUMMARY.md
@.planning/phases/39-independent-quality-re-judge/39-DOWNSTREAM-DATA-CONTRACT.json
@notebooks/phase40/qwen_lora_colab.ipynb
@notebooks/phase40/qwen_qlora_colab.ipynb
@notebooks/phase40/phobert_colab.ipynb

<interfaces>
The shared Qwen control file fixes: canonical train/validation identities and order; Qwen ID/revision `Qwen/Qwen3-4B-Instruct-2507` / `cdbee75f17c01a7cc42f958dc650907174af0554`; seed/data seed 42; max length 1024; per-device batch 1; gradient accumulation 4; effective batch 4; three epochs; AdamW Torch optimizer; linear schedule with warm-up ratio 0.03; learning rate `2e-4`; identical response-only formatter/mask hashes, LoRA rank/alpha/dropout/targets, BF16/FP16 policy, and log/eval/save cadence 10/50/50. The treatment delta is only unquantized base weights versus proved NF4 double-quantized four-bit base weights.

The PhoBERT control file fixes revision `e966aac8cb889325e073aa5f28ff70aca4dbc8c3`, seed/data seed 42, max length 256, dynamic padding, batch 16, three epochs, AdamW Torch, a linear schedule with warm-up ratio 0.03, learning rate `2e-5`, full encoder/head trainables, deterministic segmentation hash/version, and the locked label order. Architecture-specific optimizer/tokenization controls remain visible rather than falsely matched to Qwen.

The amended completion roots are exactly `data/models/phase40/full/qwen-qlora` and `data/models/phase40/full/phobert`. Each begins as a fresh full run at step zero. Only an interrupted run may resume, and only through Plan 40-02 exact compatibility verification. The immutable older request still enumerates `data/models/phase40/full/qwen-lora`; that dormant historical slot is not opened or required by the amended comparison.

`two-full-model-scope-amendment.json` is additive machine authority with `schema_version=phase40-two-full-model-scope-amendment-v1`. It binds `original_run_request_sha256` to `2512dbe6d7c5b8c16141ebdbdc848382e56b3a5737e8aeea51d7fb89447c643a`; sets `active_full_run_ids`, `quality_model_run_ids`, and `review_model_run_ids` to exactly `phase40-qwen-qlora-full-seed42-v1` and `phase40-phobert-full-seed42-v1`; sets `waived_full_run_id=phase40-qwen-lora-full-seed42-v1`, `waived_returned_root=data/models/phase40/full/qwen-lora`, `full_lora_disposition=cancelled_before_start`, `waiver_action=withdrawn`, and `waiver_basis=bounded_local_probe_established_resource_pressure_and_deadline_mismatch`; preserves the sealed LoRA probe authority; and sets `execution_policy=local_primary`, `colab_contingency_policy=validation_only_before_held_out_open_if_local_quality_unacceptable`, and `no_held_out_boundary=true`. A missing, mismatched, or mutated request binding fails before any comparison bundle opens.

`full-run-request.json` is immutable before the operator checkpoint and contains package candidates/pins plus two independent transfer authorities. The code authority is one repository-relative deterministic source archive identity: archive path/SHA-256 and source-inventory manifest path/SHA-256. The data authority is one repository-relative `data/models/phase40/input/phase40-train-validation.zip` identity with archive SHA-256, internal `phase40-input-manifest.json` SHA-256, exact sorted member set `phase40-input-manifest.json`, `train.jsonl`, `val.jsonl`, canonical member counts/hashes, Phase 39 manifest identity, snapshot-ID algorithm/version, fixed Drive path `/content/drive/MyDrive/internship-phase40/phase40-train-validation.zip`, and fixed extraction root `/content/phase40-input-v1`. The two JSONL members are byte-for-byte canonical inputs, not JSON reserialization, and no held-out/test-like member is permitted. The archive/inventory source pair and archive/manifest/member input identities, not a mandatory Git commit, are verified before notebook code or data use and copied into every run-evidence bundle. The request never contains approval decisions or returned hardware facts. Under the amended local path, Task 3 derives exact package/runtime and GPU identities from the two verified run-evidence bundles and persists them in `comparison-manifest.json`; it requires no separate Colab package-decision payload. A future explicitly activated contingency retains its own blocking approval boundary without gating already completed local evidence.

After comparison finalization, Plan 40-05 owns `review-queue.jsonl`, `review-queue-manifest.json`, and `reviewer-return.template.jsonl`. The queue is a union keyed by `(model_run_id, validation_row_id)` and retains canonical sequence, immutable `raw_message`, `source_row_sha256` over the exact UTF-8 validation JSONL source record excluding only its line terminator, gold label, predicted locked label or `invalid_output`, selected-checkpoint/run/artifact identities, and all applicable slice tags. The QLoRA and PhoBERT prediction bundles must already carry the exact IDs from the same immutable validation snapshot; the finalizer compares both expected-ID sequences byte-for-byte before metric or queue work. The builder receives that already-preflighted canonical validation snapshot, requires exact stable-ID set/order equality, and joins predictions by ID; it accepts no caller-selected data path and never re-derives an ID. Correct calibration rows are selected separately for each model and gold class by ascending SHA-256 of model artifact identity plus row ID, taking ceiling 10% with at least one when a class has a correct prediction. Plan 40-06 owns reviewer input, canonical notes, review manifest/report, and closure; human notes remain observational evidence only.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Preserve the completed historical three-run handoff authority</name>
  <precondition>Plans 40-01 through 40-04 are complete, all Phase 40 fixture/static tests pass, and the canonical notebooks validate without external execution.</precondition>
  <read_first>
    - `.planning/phases/39-independent-quality-re-judge/39-DOWNSTREAM-DATA-CONTRACT.json`
    - `.planning/phases/40-multi-model-training-evidence/40-CONTEXT.md`
    - `.planning/phases/40-multi-model-training-evidence/40-04-SUMMARY.md`
    - `src/model_adaptation/phase40_handoff.py`
    - `notebooks/phase40/qwen_lora_colab.ipynb`
    - `notebooks/phase40/qwen_qlora_colab.ipynb`
  </read_first>
  <files>data/models/phase40/source/phase40-source.zip, data/models/phase40/source/phase40-source-manifest.json, data/models/phase40/input/phase40-train-validation.zip, data/models/phase40/full-run-request.json, data/models/phase40/matched-qwen-config.json, data/models/phase40/phobert-config.json, .planning/phases/40-multi-model-training-evidence/40-COLAB-RUN-HANDOFF.md</files>
  <action>Run canonical train/validation preflight and retain its immutable snapshots, then run the static notebook validator. Invoke the Plan 40-02 deterministic source-bundle builder over its explicit runtime-source allowlist, producing one repository-relative archive plus a sorted inventory manifest with per-file hashes, archive SHA-256, and inventory-manifest SHA-256; verify the pair before freezing the request. Invoke `build_phase40_input_bundle` only with the authorized `Phase40DataContract`, producing `data/models/phase40/input/phase40-train-validation.zip` as a normalized stored ZIP with exactly `phase40-input-manifest.json`, `train.jsonl`, and `val.jsonl`; copy the canonical file bytes without reserialization and verify archive hash, manifest-member hash/schema, exact safe member set, member counts/hashes, Phase 39 manifest identity, and snapshot-ID algorithm/version. Freeze the local repository-relative archive identity plus exact fixed transfer path `/content/drive/MyDrive/internship-phase40/phase40-train-validation.zip` and fixed extraction root `/content/phase40-input-v1`; include no laptop absolute path. Serialize the exact shared Qwen controls and approved quantization delta into one immutable matched config; serialize the separate transparent PhoBERT controls. Create a run request listing both verified source and input archive identities, three fresh run IDs, required package/model candidates and exact pins, source notebooks, expected bundle roots/files/hashes, step-zero origin, exact resume rules, environment/hardware capture fields, no-test boundary, and export procedure. The request is frozen before the operator checkpoint and contains no package approval/rejection decision or returned GPU fact; those exist only in the later sanitized operator return and `comparison-manifest.json`. Write a concise operator handoff with commands/cells in run order: copy/mount the exact input archive, automatically verify path/archive/inventory/manifest/member hashes before either JSONL member opens, extract only to the fixed root, then verify source before import and execute. A Git identifier may be included only as optional supplemental metadata and is not needed to validate the request. Do not put a secret, personal path, probe adapter, held-out/test-like member, or external inference service into the request.</action>
  <acceptance_criteria>
    - Canonical train/validation counts/hashes and Phase 39 manifest identity are frozen in the request.
    - Qwen controls compare exactly equal after removing only the explicit quantization treatment/proof fields.
    - All run IDs are fresh full-run identities with step origin zero and no probe parent.
    - The historical request enumerates complete expected outputs for its original three slots; the dated amendment leaves those bytes unchanged while making only the QLoRA and PhoBERT roots completion requirements.
    - The same deterministic source archive/inventory hash pair is frozen in the request, verified before any notebook extraction/import, and required in every returned run-evidence bundle.
    - One deterministic input archive is byte-stable from the authorized snapshots, contains exactly the manifest plus canonical train/validation files, and freezes its local identity, fixed Drive/extraction locations, counts, whole-file/member/manifest hashes, and snapshot-ID version in the request.
    - Request validation passes with no Git identifier and rejects a missing, alternate, mutated, or unhashed source archive/inventory.
    - Request/input validation rejects extra/test-like/traversal/symlink members, alternate Drive/extraction paths, laptop absolute paths, or any archive/manifest/member mutation before either data member opens or any extraction occurs.
    - The immutable request contains package candidates/pins only; package decisions and returned GPU identities are absent and cannot be backfilled into it.
    - Static notebook and run-request verification passes before any operator action.
  </acceptance_criteria>
  <verify>
    <automated>python -m src.model_adaptation.phase40_colab_prepare verify --repo-root .</automated>
  </verify>
  <done>The byte-verified historical three-run handoff exists as provenance without proving or authorizing execution; the dated amendment controls the two local completion roots.</done>
</task>

<task type="auto">
  <name>Task 2: Complete the verified local QLoRA-to-GGUF-to-PhoBERT chain</name>
  <read_first>
    - `.planning/phases/40-multi-model-training-evidence/40-LOCAL-FULL-QLORA-REPORT.md`
    - `data/models/phase40/full-run-request.json`
    - `data/models/phase40/two-full-model-scope-amendment.json`
    - `.planning/phases/40-multi-model-training-evidence/40-LOCAL-PROBE-REPORT.md`
    - `data/models/phase40/phobert-config.json`
  </read_first>
  <action>Allow the clean local QLoRA run to reach its complete 1,245-step lifecycle under the bounded exact-resume supervisor. Require the immutable QLoRA bundle to pass request/source/input/base/model/checkpoint/prediction/graph verification, including exact local package/runtime identities, then require the selected adapter to merge into the pinned ordinary base, convert to locked Q8_0, pass manifest verification, and pass the pinned CPU load smoke. Only after the Qwen trainer and supervisor exit and the controller observes three consecutive safe GPU-memory samples may it start fresh local PhoBERT from step zero. Require PhoBERT to consume the same request-pinned train/validation identities, pinned base revision, frozen config and label order, then retain full encoder/head training evidence, exact local package/runtime identity, separate telemetry, selected checkpoint, exactly 219 ordered validation predictions, graphs, and immutable model hashes. No probe artifact or Qwen checkpoint may parent PhoBERT. Preserve every terminal failure rather than changing controls in place. Do not launch any Colab notebook during this task, do not request a Colab package-approval payload, and do not create or open the dormant full-LoRA root. After both local bundles verify, evaluate their frozen development-validation results. If either result is unacceptable under the predeclared validation safety gates, stop before Phase 41 and record that the optional contingency needs an explicit pre-test activation decision; never use reserved-test results to trigger retraining.</action>
  <what-built>A fresh local Qwen QLoRA bundle, its verified Q8_0 GGUF, and a fresh local PhoBERT bundle under one hash-bound source/input authority; the ordinary-LoRA branch remains a sealed resource probe.</what-built>
  <instructions>
    1. Wait for `transfer-root-v3\data\models\phase40\full\qwen-qlora` to pass complete run-evidence verification.
    2. Require `exports-v3\qwen-qlora-q8_0.gguf.manifest.json` and the CPU load smoke to verify before releasing the GPU.
    3. Start PhoBERT only through the armed local chain after the Qwen processes are gone and the declared GPU-memory gate passes.
    4. Verify `transfer-root-v3\data\models\phase40\full\phobert` against the same source/input authority and exact 219-row validation sequence.
    5. Preserve the ordinary-LoRA probe and immutable earlier three-run request unchanged; do not create `data/models/phase40/full/qwen-lora`.
    6. Freeze both validation bundles before deciding whether the pre-test Colab contingency is needed; if Phase 41 has opened, the contingency is permanently closed for this evaluation cycle.
  </instructions>
  <verification>Both local canonical roots verify, Qwen GGUF verifies and loads, no Qwen/PhoBERT process overlap occurred, the full-LoRA root remains absent, and no reserved-test or Colab execution is involved.</verification>
  <acceptance_criteria>
    - QLoRA and PhoBERT each begin at step zero and produce one complete immutable local bundle with exactly 219 ordered validation predictions.
    - QLoRA proves genuine NF4 four-bit mode and the Q8_0 GGUF passes manifest plus CPU load verification before PhoBERT starts.
    - Both runs record the exact request-pinned source/input/model/config identities and separate RTX 5050 telemetry; no model processes overlap.
    - Exact local package/runtime identities verify from each completed run bundle; no Colab approval decision is required to admit local evidence.
    - The sealed ordinary-LoRA probe is unchanged and no full ordinary-LoRA artifact exists or is required.
    - The 72.83-minute probe estimate is not relabeled as observed wall time; final reporting uses the sealed full-run duration, while roughly 12.85 hours remains an interim live projection.
    - Colab remains unexecuted unless a separate explicit pre-test validation-contingency decision is recorded; held-out results can never activate it.
  </acceptance_criteria>
  <verify>
    <working_directory>D:\PROJEct\AI MODELS\phase40-full-local-20260825\source-runtime-v3</working_directory>
    <automated>python -m src.model_adaptation.phase40_operator phase40-verify-run-evidence --run-root ..\transfer-root-v3\data\models\phase40\full\qwen-qlora</automated>
    <automated>python -m src.model_adaptation.phase40_gguf verify --manifest-path ..\exports-v3\qwen-qlora-q8_0.gguf.manifest.json</automated>
    <automated>python -m src.model_adaptation.phase40_operator phase40-verify-run-evidence --run-root ..\transfer-root-v3\data\models\phase40\full\phobert</automated>
  </verify>
  <done>Two complete local model bundles and the Qwen Q8_0 export verify; the sealed LoRA probe remains the only ordinary-LoRA evidence and comparison finalization may begin.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize the two-model local validation comparison automatically</name>
  <precondition>Task 2 verified the local QLoRA bundle, Q8_0 GGUF, and local PhoBERT bundle; or it stopped before Phase 41 with an explicit failed development-validation gate.</precondition>
  <read_first>
    - `.planning/phases/40-multi-model-training-evidence/40-COLAB-RUN-HANDOFF.md`
    - `data/models/phase40/full-run-request.json`
    - `data/models/phase40/two-full-model-scope-amendment.json`
    - `data/models/phase40/matched-qwen-config.json`
    - `data/models/phase40/phobert-config.json`
    - `.planning/phases/40-multi-model-training-evidence/40-AI-SPEC.md`
  </read_first>
  <files>data/models/phase40/two-full-model-scope-amendment.json, data/models/phase40/full/, data/models/phase40/comparison-manifest.json, data/models/phase40/comparison-report.md, data/models/phase40/selected-prediction-bundles.json, data/models/phase40/review/review-queue.jsonl, data/models/phase40/review/review-queue-manifest.json, data/models/phase40/review/reviewer-return.template.jsonl, .planning/phases/40-multi-model-training-evidence/40-VALIDATION-COMPARISON.md</files>
  <action>Materialize a separate post-amendment repository root containing the reviewed comparison code, byte-identical frozen request/input/probe authorities, and the two verified local bundles at their canonical relative roots; do not modify `source-runtime-v3`. From that root, freeze `two-full-model-scope-amendment.json` once through the canonical command, or require an existing canonical file to reproduce byte-identically. This initial freeze is the reviewed authority-establishment event: it binds the unchanged request, sealed LoRA probe, and current exact finalizer-source inventory, but it is not self-proof that Python verified its own source before import. End that authority-establishment process. Before any later Python finalizer launch, run an independent PowerShell/OS pre-launch preflight that treats the amendment as data, compares every `comparison_finalizer_authority.files` entry against the repository's exact relative path, regular-file status, byte count, and SHA-256, recomputes `source_tree_sha256`, and records a timestamped PASS bound to the amendment and request hashes. Any missing, extra, size-mismatched, hash-mismatched, unsafe, or tree-mismatched source produces FAIL, launches no Python finalizer, and reads no model bundle. Only after the external PASS may Python start from that repository root. In-process code then loads the amendment and independently rechecks `schema_version`, `original_run_request_sha256`, exact `active_full_run_ids`/`quality_model_run_ids`/`review_model_run_ids`, `waived_full_run_id`, `waived_returned_root`, `full_lora_disposition`, `waiver_action`, `waiver_basis`, `execution_policy`, `colab_contingency_policy`, `no_held_out_boundary`, every finalizer-source inventory entry, and `source_tree_sha256` before opening any model bundle root. This second check is defense in depth and does not replace the external pre-launch proof. Record both authority establishment and external-preflight PASS in final evidence. Do not edit, replace, or execute amended finalizer code from immutable `source-runtime-v3`; v3 remains solely the training/GGUF/PhoBERT authority. From the externally preflighted post-amendment repository root, run `phase40-finalize-comparison` with `--repo-root .`. Leave `full-run-request.json` byte-unchanged and require `two-full-model-scope-amendment.json` to bind request SHA-256 `2512dbe6d7c5b8c16141ebdbdc848382e56b3a5737e8aeea51d7fb89447c643a` before any bundle opens. Admit exactly the canonical relative roots for `phase40-qwen-qlora-full-seed42-v1` and `phase40-phobert-full-seed42-v1`, both with the recorded RTX 5050 identity; reject any third quality bundle, alternate root, absolute D-drive input, probe parent, or held-out/test-like input. Rehash and schema-verify both complete bundles, including exact package/runtime identities from local run evidence, source archive/inventory and input archive/internal-manifest/member/snapshot identities, exactly 219 ordered validation predictions, selected-checkpoint rationale, raw logs, trainer state, graphs, model hashes, and step-zero lineage. Require no Colab package-decision payload for local bundle admission. Require genuine QLoRA proof and independently verified Q8_0 export. Regenerate every graph from raw evidence, then atomically write the two-model `comparison-manifest.json`, canonical `comparison-report.md`, and `selected-prediction-bundles.json`. Report the sealed LoRA probe only in a separate resource/ETA section, including its incomplete status and 18.42–18.88-hour compute-only estimate; do not create a LoRA accuracy row. Replace the interim roughly 12.85-hour QLoRA projection with the final sealed wall time when available, while retaining the 72.83-minute optimizer-plus-one-evaluation estimate with its explicit boundary. Mechanically copy the canonical report to `.planning/phases/40-multi-model-training-evidence/40-VALIDATION-COMPARISON.md` only after byte-equality and SHA-256 verification. Include both models, all safety-gate failures, risky recall floors, invalid outputs, execution hardware, and the single-seed/no-significance limitation. After the comparison freezes, build the deterministic message-bound review queue from the exact canonical validation snapshot and both selected-prediction bundles. Queue every mandatory error slice plus the deterministic per-model/per-class correct sample, preserve all stable IDs/messages/source hashes/tags, and run the complete regression suite. No value is copied manually. If validation is unacceptable, retain the honest comparison and stop before Phase 41 for an explicit contingency decision; never open the reserved partition first and then retrain.</action>
  <acceptance_criteria>
    - Two independently trained local model artifacts and complete evidence bundles verify.
    - Both run bundles prove the same request-pinned input archive/member identities and exact immutable validation snapshot-ID sequence; any alternate, extra, test-like, reordered, or locally re-derived input identity fails.
    - The comparison manifest records the dated two-model amendment, both canonical roots, and both GPU identities while leaving the older request byte-unchanged.
    - Exact package/runtime identities come from verified local run evidence; missing Colab approval decisions do not block the amended local finalizer.
    - A non-Python PowerShell/OS pre-launch preflight records PASS for every amendment-listed source path, regular-file status, byte count, SHA-256, and recomputed tree hash before Python starts; in-process code rechecks the same authority before any model bundle read.
    - QLoRA is genuinely four-bit, its Q8_0 export verifies, and ordinary LoRA appears only as sealed resource/ETA evidence.
    - All graphs reproduce from raw logs, all model/checkpoint hashes verify, and every bundle has 219 ordered validation rows.
    - A complete comparison produces a deterministic de-duplicated review queue whose slice counts reconcile to the raw predictions and whose correct samples reproduce from the declared hash/ranking rule.
    - Every queue row contains the exact canonical full message and source-row hash copied from the same immutable snapshot and joined by its supplied stable ID; missing/reordered/mutated/locally reconstructed validation or prediction IDs, messages, or hashes fail before queue publication.
    - Comparison shows both full-model results, risky-class recall floors, invalid outputs, resource/evidence timing boundaries, and single-seed limitation without unsupported inference.
    - No held-out data, probe artifact, or external model judge is required or used; Git identity is optional supplemental metadata and never an evidence prerequisite.
  </acceptance_criteria>
  <verify>
    <working_directory>&lt;VERIFIED_POST_AMENDMENT_REPO_ROOT&gt;</working_directory>
    <automated>python -m src.model_adaptation.cli phase40-freeze-scope-amendment --request-path data/models/phase40/full-run-request.json --repo-root .</automated>
    <automated>python -m src.model_adaptation.cli phase40-finalize-comparison --request-path data/models/phase40/full-run-request.json --scope-amendment-path data/models/phase40/two-full-model-scope-amendment.json --repo-root . --output-root data/models/phase40 --bundle-root phase40-qwen-qlora-full-seed42-v1=data/models/phase40/full/qwen-qlora --bundle-root phase40-phobert-full-seed42-v1=data/models/phase40/full/phobert --gpu-identity "phase40-qwen-qlora-full-seed42-v1=NVIDIA GeForce RTX 5050 Laptop GPU" --gpu-identity "phase40-phobert-full-seed42-v1=NVIDIA GeForce RTX 5050 Laptop GPU" --verify-only</automated>
  </verify>
  <done>Two valid local roots, complete reproducible evidence, an honest QLoRA-versus-PhoBERT validation comparison, and its canonical-message-bound deterministic review queue are frozen for Plan 40-06; the ordinary-LoRA result remains resource evidence only.</done>
</task>

</tasks>

## Artifacts this phase produces

- `data/models/phase40/source/phase40-source.zip` and `phase40-source-manifest.json`: deterministic runtime source bundle, sorted inventory, and archive/inventory SHA-256 authority; Git identity is optional.
- `data/models/phase40/input/phase40-train-validation.zip`: deterministic stored ZIP containing exactly the canonical `train.jsonl`, `val.jsonl`, and `phase40-input-manifest.json`, pinned to local authority plus historical contingency paths and verified before data-member open.
- `data/models/phase40/full-run-request.json`: immutable canonical data/model/source/package-candidate-and-pin/run/output handoff contract; it contains no returned package decision or hardware fact.
- `data/models/phase40/two-full-model-scope-amendment.json`: additive machine-readable authority binding request SHA-256 `2512dbe6d7c5b8c16141ebdbdc848382e56b3a5737e8aeea51d7fb89447c643a` and activating only QLoRA/PhoBERT.
- `data/models/phase40/matched-qwen-config.json`: exact LoRA/QLoRA controlled configuration and approved quantization delta.
- `data/models/phase40/phobert-config.json`: transparent full PhoBERT classifier configuration.
- `data/models/phase40/full/qwen-qlora/`: fresh genuine four-bit model adapter/checkpoints, raw events/predictions, trainer state, graphs, metrics, hashes, and proof.
- `data/models/phase40/full/phobert/`: fresh full classifier model/checkpoints, preprocessing/truncation records, raw events/predictions, trainer state, graphs, metrics, hashes, and evidence.
- `data/models/phase40/comparison-manifest.json`: two-model QLoRA/PhoBERT admissibility and frozen validation comparison identities, with the dated scope amendment.
- `data/models/phase40/comparison-report.md` and `selected-prediction-bundles.json`: canonical deterministic comparison report and selected-prediction authority written beside the manifest under the consolidated D-drive root.
- `data/models/phase40/review/review-queue.jsonl`, `review-queue-manifest.json`, and `reviewer-return.template.jsonl`: immutable message/source-row-bound review handoff for Plan 40-06.
- `.planning/phases/40-multi-model-training-evidence/40-COLAB-RUN-HANDOFF.md`: immutable historical contingency instructions; not an executed primary-path artifact.
- `.planning/phases/40-multi-model-training-evidence/40-VALIDATION-COMPARISON.md`: byte-equal mechanically copied mirror of the canonical comparison report, with equality/hash checked after finalization.

## Specless edge-coverage assumptions

| Probe row | Status | Concrete predicate |
|---|---|---|
| TRAIN-05 / unclassified | flagged | The amended comparison requires exactly two complete model rows, QLoRA and PhoBERT, and reports every value/failure regardless of winner; a missing or losing model cannot be omitted. |

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Two real independently trained local models with logged curves, measured LoRA/QLoRA resource evidence, and Qwen/PhoBERT validation answers | 04-06 | COVERED | Local probes, two fresh full models, comparison, and review closure cover the amended goal. |
| REQ | TRAIN-01 | RTX LoRA probe, discard, and honest resource/ETA report | 02,04,05 | COVERED | The sealed incomplete probe is the terminal ordinary-LoRA evidence; no full run follows. |
| REQ | TRAIN-02 | RTX genuine QLoRA probe, discard, fresh fail-closed four-bit full run | 01,02,04,05 | COVERED | Mode proof precedes the clean local run; external execution is a terminal-failure fallback only. |
| REQ | TRAIN-03 | Honest same-laptop LoRA/QLoRA resource attribution without a full-LoRA quality claim | 01,02,04,05 | COVERED | Probe controls and timing boundaries remain explicit; full-model quality compares QLoRA with PhoBERT. |
| REQ | TRAIN-04 | Full PhoBERT classification-head baseline | 03,05 | COVERED | No PEFT/QLoRA path. |
| REQ | TRAIN-05 | Honest PhoBERT-versus-Qwen QLoRA measured comparison | 02,03,05,06 | COVERED | Shared evaluator, both full-model rows retained, and qualitative review reported without rewriting results. |
| REQ | TRAIN-06 | Raw-log-derived graphs and complete provenance bundle without required Git ID | 01,02,03,04,05,06 | COVERED | Evidence schema, callbacks, source archive, notebooks, real runs, comparison, and review verification. |
| RESEARCH | contract/modes/metrics | Reject-before-open inputs, typed tuples, response-only Qwen loss, invalid-output state | 01 | COVERED | Includes empty/encoding and impossible-combination tests. |
| RESEARCH | evidence/restart | Append-only events, timing/VRAM/throughput, atomic evidence, exact resume, graphs | 02 | COVERED | Includes empty/adjacency/ordering predicates. |
| RESEARCH | PhoBERT/notebooks | Deterministic full classifier and three fresh thin notebooks | 03 | COVERED | Static validators block historical paths/duplicate logic. |
| RESEARCH | immutable notebook source and data transfer | Retrieve verified code plus exactly the canonical train/validation bytes without making Git identity mandatory or exposing held-out data | 02,03,05 | COVERED | Run-request schema, source archive/inventory hashes, deterministic exact-three-member input archive, fixed Colab paths, verify-before-open/extract notebook gate, and returned-evidence identity. |
| RESEARCH | package legitimacy | Human approval before flagged package installs | 04,05 | COVERED | Local approval receipts are preserved; any future contingency install remains blocking-human. |
| RESEARCH | Vietnamese review | Review every invalid output, risky-to-benign miss, Zalo error, benign-to-risky error, cross-scam error, and deterministic correct sample | 02,05,06 | COVERED | Message-bound queue/persistence tooling is fake-tested in 02, the queue is frozen in 05, and exact-coverage human evidence closes in 06. |
| CONTEXT | final-corpus prerequisite | Consume Phase 39 downstream contract only after Phase 39 closure | 01,04,05 | COVERED | Precondition and hash/count enforcement. |
| CONTEXT | RTX probes | Bounded real outcomes, measured ETA/resources, adapters discarded | 02,04 | COVERED | Genuine failure retained; OOM never engineered. |
| CONTEXT | fresh full runs | Fresh step-zero local QLoRA and PhoBERT with frozen architecture-specific controls | 02,03,05 | COVERED | Probe lineage is prohibited; full ordinary LoRA is retired by the dated amendment. |
| CONTEXT | PhoBERT baseline | Ordinary full classifier, not novelty QLoRA | 03,05 | COVERED | Typed identity enforces it. |
| CONTEXT | evidence and graphs | Complete hash-linked bundles and mechanically generated graphs | 02,04,05,06 | COVERED | Source archive and all run/review evidence are hash-linked; Git identifier intentionally not required. |
| CONTEXT | evaluation boundary | Validation only; Phase 41 owns held-out evaluation and later all-data fit | 01-06 | COVERED | Held-out access is a plan-wide high-severity prohibition. |

Deferred test evaluation and optional all-data deployment fitting remain excluded because CONTEXT assigns them to Phase 41.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Historical Colab handoff -> optional pre-test contingency | The frozen recipe remains dormant unless unacceptable development validation triggers an explicit decision before Phase 41; held-out outcomes can never activate it. |
| Verified v3 source/runtime -> D-drive local QLoRA bundle | Local code, inputs, checkpoints, and telemetry cross into an off-repo immutable evidence tree under hash-bound request/source/input controls and a bounded exact-resume supervisor. |
| Verified QLoRA/GGUF -> local PhoBERT | The sequential controller releases the GPU only after QLoRA and GGUF verify, then starts PhoBERT under separate telemetry and immutable evidence roots. |
| Two local roots -> consolidated `transfer-root-v3` | The QLoRA and PhoBERT bundles occupy exactly the amended canonical sibling roots before comparison; the historical LoRA slot stays dormant. |
| Scope amendment finalizer authority -> post-amendment repo root | Amendment freeze establishes reviewed authority; an external PowerShell/OS pre-launch check proves every listed path/byte-count/SHA and tree hash before Python launch, then in-process code rechecks before any model bundle read. Immutable training runtime v3 is separate and never patched. |
| Verified bundles -> scientific comparison | Measurements cross into selection and defense claims with safety/hardware/uncertainty limits. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-40-05-SC | Tampering / Elevation of privilege | dormant Colab contingency | high | mitigate | No install or run occurs on the primary path; any contingency requires explicit pre-test activation, exact pins, and verify-before-open controls. |
| T-40-05-01 | Information disclosure | notebook input/upload | high | mitigate | Fixed two-input run request, static notebook gate, no secrets/external inference, and no broad split discovery. |
| T-40-05-02 | Tampering / Repudiation | returned bundles | high | mitigate | Local rehash, schema/completeness validation, trainer-state/prediction counts, model artifact identity, and graph regeneration before admission. |
| T-40-05-03 | Spoofing | QLoRA/full-run identity | high | mitigate | Fresh step-zero identity, exact matched config, genuine four-bit proof, and no probe-parent lineage. |
| T-40-05-04 | Repudiation | scientific comparison | high | mitigate | Preserve all model rows/failures, explicit recall floors/invalid outputs, hardware-confounding flag, single-seed limitation, and no significance claim. |
| T-40-05-05 | Denial of service | sequential local training interruption | medium | mitigate | Each local run permits at most two hash-verified exact resumes; QLoRA/GGUF must verify and release the GPU before PhoBERT starts. |
| T-40-05-06 | Tampering / Repudiation | Vietnamese review queue | high | mitigate | Stable-ID join to preflighted validation rows, immutable full message/source-row hash, complete slice union, deterministic correct sampling, and queue/comparison hash verification before Plan 40-06. |
| T-40-05-07 | Tampering / Spoofing | notebook source bundle | high | mitigate | Deterministic archive/inventory hashes are frozen in the request, verified before extraction/import, and required in every returned evidence bundle; Git identity is optional. |
| T-40-05-08 | Tampering / Information disclosure | canonical input transfer | high | mitigate | Deterministic exact-three-member archive, fixed Drive/extraction paths, request-pinned archive/manifest/member hashes, and automated verify-before-data-open reject extra/test-like/substituted inputs and laptop-relative paths. |
</threat_model>

<verification>
Verify the immutable training source archive/inventory, deterministic input archive/internal manifest/exact member set, byte-unchanged historical run request, and additive scope amendment/request-hash binding. Treat initial amendment freeze as reviewed authority establishment, not as self-verification. Require local QLoRA run-evidence and GGUF verification from the bound v3 runtime before admitting the separately verified local PhoBERT bundle. Before Python finalizer launch, require an external PowerShell/OS preflight to compare every amendment-listed source relative path, regular-file status, byte count, and SHA-256, recompute `source_tree_sha256`, record PASS bound to amendment/request hashes, and prove immutable `source-runtime-v3` was not mutated. After launch, require the in-process verifier to repeat the exact inventory/tree check before any bundle root read. Consolidate exactly the two model roots, run the amended Task 3 comparison finalizer only after both layers pass, regenerate graphs, generate and verify the deterministic review queue, and run `python -m pytest tests/model_adaptation -q --basetemp .tmp/pytest-p40-05-regression`. Comparison finalization must fail closed before Python launch on a failed external source preflight, and before bundle access on a failed in-process amendment/source check, missing/mismatched amendment, wrong request hash, missing/extra/mutated finalizer-authority file, wrong source-tree hash, missing/empty two-model bundle, an opened historical LoRA root, wrong model/source/input/snapshot identity, extra/test-like input member, missing QLoRA proof, config drift, nonzero full-run origin, probe lineage, prediction count/order/ID error, hash mismatch, missing raw graph source, or secret/personal path. Queue finalization must fail on any omitted mandatory slice, non-reproducible correct sample, missing/duplicate/reordered/locally reconstructed validation or prediction ID, mutated full message/source-row hash, or canonical-order drift. Colab remains unexecuted unless a separate explicit validation-contingency decision occurs before Phase 41. Plan 40-06 performs the blocking human review and review-evidence finalization.
</verification>

<success_criteria>
- One fresh complete genuine four-bit Qwen QLoRA model and one fresh complete full PhoBERT model exist; the sealed ordinary-LoRA probe remains resource evidence only.
- The LoRA/QLoRA probe resource comparison retains its same-laptop controls and timing limitations; no full-run accuracy attribution is claimed from it.
- Two complete validation bundles and every graph verify from raw evidence after each recorded local execution/copy boundary.
- The comparison reports all results, safety-gate failures, invalid outputs, and single-seed limitation honestly.
- Every mandatory error slice and deterministic per-model/per-class correct sample is frozen in a queue containing the canonical full message, exact source-row hash, gold/prediction state, run/artifact identity, and all tags for Plan 40-06.
- Both local bundles are tied to the same hash-verified source archive/inventory without requiring a Git commit identifier.
- Both local bundles are tied to the same hash-verified exact-three-member train/validation archive and immutable snapshot-ID sequence, with no held-out transfer path.
- Phase 40 makes no held-out, all-data deployment, external-judge, Git-authorship, t-test, variance, or significance claim.
</success_criteria>

<output>
Create `.planning/phases/40-multi-model-training-evidence/40-05-SUMMARY.md` when done.
</output>
