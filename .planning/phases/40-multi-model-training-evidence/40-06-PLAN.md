---
phase: 40-multi-model-training-evidence
plan: 06
type: execute
wave: 6
depends_on:
  - 40-05
files_modified:
  - data/models/phase40/review/reviewer-return.jsonl
  - data/models/phase40/review/human-review-notes.jsonl
  - data/models/phase40/review/human-review-manifest.json
  - data/models/phase40/review/human-review-report.md
  - .planning/phases/40-multi-model-training-evidence/40-VIETNAMESE-ERROR-REVIEW.md
autonomous: false
requirements:
  - TRAIN-05
  - TRAIN-06
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The frozen review queue contains model rows only for the two amended completion runs, Qwen QLoRA and PhoBERT; the ordinary-LoRA probe contributes no prediction row."
    - "A Vietnamese-fluent reviewer judges every mandatory error and deterministic calibration model-row from its immutable full message, not from IDs, metrics, or isolated keywords."
    - "Each review record remains bound to the canonical source-row hash, gold label, prediction or invalid state, selected checkpoint, run/artifact identity, and complete slice-tag set frozen by Plan 40-05."
    - "Human observations are persisted with exact queue coverage and hash provenance but cannot relabel data, repair output, change metrics, or reselect a checkpoint."
    - "Phase 40 closes only after the review report and machine manifest reproduce byte-stably from the frozen comparison, queue, and structured reviewer return."
  artifacts:
    - path: "data/models/phase40/review/reviewer-return.jsonl"
      provides: "One Vietnamese-fluent structured assessment and note for every immutable queue key"
    - path: "data/models/phase40/review/human-review-notes.jsonl"
      provides: "Schema-normalized exact-coverage qualitative observations"
    - path: "data/models/phase40/review/human-review-manifest.json"
      provides: "Hash-linked comparison, canonical validation, queue, reviewer-return, notes, coverage, and report identities"
    - path: ".planning/phases/40-multi-model-training-evidence/40-VIETNAMESE-ERROR-REVIEW.md"
      provides: "Human-readable per-model/per-slice qualitative review and Phase 40 limitations"
  key_links:
    - from: "data/models/phase40/review/review-queue.jsonl"
      to: "data/models/phase40/review/reviewer-return.jsonl"
      via: "the reviewer reads each persisted raw_message and retains its model/row key, source_row_sha256, frozen prediction identities, and slice tags unchanged"
      pattern: "raw_message|source_row_sha256|model_run_id|validation_row_id"
    - from: "data/models/phase40/review/reviewer-return.jsonl"
      to: "data/models/phase40/review/human-review-manifest.json"
      via: "the finalizer enforces exact queue-key coverage, attestation, immutable frozen fields, and content hashes"
      pattern: "queue_sha256|reviewer_return_sha256|notes_sha256|comparison_manifest_sha256"
    - from: "data/models/phase40/review/human-review-manifest.json"
      to: ".planning/phases/40-multi-model-training-evidence/40-VIETNAMESE-ERROR-REVIEW.md"
      via: "machine-derived counts and observations render the report without changing experiment results"
      pattern: "assessment|slice|single_seed|development_validation"
  prohibitions:
    - value: "Reviewing only row IDs, labels, metrics, snippets, or keywords instead of the full immutable Vietnamese message"
      status: unverified
    - value: "Changing a source message/hash, gold label, raw generated output/logit, predicted state, metric, safety gate, or selected checkpoint through human review"
      status: unverified
    - value: "Omitting, duplicating, reordering, or adding a queue key or mandatory slice during review finalization"
      status: unverified
    - value: "Sending review messages, predictions, or notes to an external model, judge, API, or service"
      status: unverified
    - value: "Opening or consulting the held-out Phase 41 partition"
      status: unverified
---

## User-approved scope amendment — 2026-08-25

Plan 40-06 reviews the two complete local model rows produced by the amended
Phase 40 experiment: Qwen QLoRA and PhoBERT. The sealed ordinary-LoRA probe is
resource/ETA evidence and never contributes prediction-review rows. The
historical three-run request and Colab handoff remain immutable provenance,
not a prerequisite. Any Colab contingency must have been decided from frozen
development validation before the reserved-test gate; Phase 41 outcomes can
never reopen training, dataset repair, or checkpoint selection.

<objective>
Complete Vietnamese qualitative review and close Phase 40 without altering its frozen validation experiment.

Purpose: Give every mandatory model error and deterministic correct calibration example a full-message, Vietnamese-fluent interpretation while keeping the review observational, complete, and reproducible.
Output: Exact-coverage reviewer return, canonical review notes, hash-linked review manifest, and final qualitative review report.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-multi-model-training-evidence/40-CONTEXT.md
@.planning/phases/40-multi-model-training-evidence/40-AI-SPEC.md
@.planning/phases/40-multi-model-training-evidence/40-RESEARCH.md
@.planning/phases/40-multi-model-training-evidence/40-05-SUMMARY.md
@data/models/phase40/comparison-manifest.json
@data/models/phase40/review/review-queue-manifest.json

<interfaces>
`ReviewQueueRow` is immutable and extra-forbid. Its key is `(model_run_id, validation_row_id)` and it carries canonical validation sequence, full `raw_message`, `source_row_sha256` over the exact UTF-8 source JSONL record excluding only its line terminator, gold label, predicted locked label or `invalid_output`, selected-checkpoint/run/model-artifact identities, and every applicable slice tag. Plan 40-05 already joined these rows by stable ID to an authorized canonical validation snapshot and froze queue/comparison hashes; the human checkpoint reads the queue rather than reopening a corpus file.

`ReviewerReturnRow` repeats the immutable key/source/prediction/tag fields and adds one assessment from `prediction_supported`, `prediction_unsupported`, `gold_label_concern`, or `ambiguous`, one non-empty mechanism/error note, and an optional shortcut-pattern note. The local finalizer invocation carries the Vietnamese-fluency attestation; the reviewer-return rows themselves contain no spoofable attestation field. `finalize_phase40_human_review` requires exactly one row for every queue key and re-proves queue, canonical source-row, selected-checkpoint, comparison, and exact original returned-review bytes before atomic publication.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Complete the full-message Vietnamese prediction review</name>
  <precondition>Plan 40-05 completed its verified two-bundle QLoRA/PhoBERT branch under the 2026-08-25 amendment, froze `comparison-manifest.json`, and generated a verified non-empty `review-queue.jsonl`, queue manifest, and reviewer-return template.</precondition>
  <read_first>
    - `data/models/phase40/review/review-queue.jsonl`
    - `data/models/phase40/review/review-queue-manifest.json`
    - `data/models/phase40/review/reviewer-return.template.jsonl`
    - `data/models/phase40/comparison-manifest.json`
    - `.planning/phases/40-multi-model-training-evidence/40-VALIDATION-COMPARISON.md`
    - `.planning/phases/40-multi-model-training-evidence/40-AI-SPEC.md`
  </read_first>
  <action>Pause repository automation after automated comparison and queue finalization. First run the queue verifier and require its hashes, stable-ID order, source-row/message binding, selected-checkpoint identities, and slice/sample coverage to pass. A Vietnamese-fluent reviewer then copies the generated template to `data/models/phase40/review/reviewer-return.jsonl` and reviews every queued model-row exactly once. For each row, read the complete persisted `raw_message` and judge the claimed actor, requested action, pressure/mechanism, and benign context; do not decide from a truncated snippet, row ID, metric, or isolated keyword. Cover every row tagged `invalid_output`, `risky_to_benign`, `zalo_involved_misclassification`, `benign_to_risky`, `risky_cross_confusion`, or `correct_calibration_sample`. Choose one supported assessment, add a non-empty mechanism/error note and optional shortcut-pattern note, and retain the model/row key, canonical sequence, `source_row_sha256`, gold label, predicted/invalid state, checkpoint/run/artifact identity, and all slice tags unchanged. Record disagreements only as observations. Do not relabel validation data, repair raw Qwen output, edit logits/predictions, change metrics or checkpoint selection, access held-out data, or send content to an external model/service.</action>
  <what-built>Two hash-verified local model bundles, a frozen QLoRA-versus-PhoBERT validation comparison, and a mechanically complete queue whose every row already contains its immutable full message and source-row hash.</what-built>
  <how-to-verify>
    1. Run the automated queue verifier and confirm the queue/source/comparison hashes and stable order pass.
    2. Confirm every queue row visibly includes the full `raw_message`, `source_row_sha256`, gold label, prediction/invalid state, run/artifact identity, and all slice tags.
    3. Confirm slice summaries include every invalid output, risky-to-benign error, Zalo-involved misclassification, benign-to-risky error, risky-family cross-confusion, and deterministic correct sample.
    4. Fill every generated row, save it as `reviewer-return.jsonl`, and leave all immutable fields untouched.
    5. Invoke the local finalizer with the Vietnamese-fluent reviewer attestation and confirm no frozen evaluation artifact was edited.
  </how-to-verify>
  <resume-signal>Reply `Vietnamese review complete: data/models/phase40/review/reviewer-return.jsonl` only after every generated row has a structured assessment and non-empty full-message note.</resume-signal>
  <acceptance_criteria>
    - One Vietnamese-fluent reviewer has inspected the full immutable message for every queued model-row and returned exactly one structured record per queue key.
    - All mandatory error slices and deterministic correct samples remain represented; overlapping slices are reviewed once with all tags retained.
    - Every review has a supported assessment and non-empty mechanism/error note, and the finalizer invocation carries the Vietnamese-fluency attestation.
    - All message/hash, sequence, key, label, prediction, run/artifact, and tag fields match the queue exactly, and every frozen comparison artifact remains byte-identical.
  </acceptance_criteria>
  <verify>
    <automated>python -m src.model_adaptation.cli phase40-verify-review-queue --request-path data/models/phase40/full-run-request.json --repo-root . --scope-amendment-path data/models/phase40/two-full-model-scope-amendment.json --comparison-manifest-path data/models/phase40/comparison-manifest.json --selected-predictions-path data/models/phase40/selected-prediction-bundles.json --queue-path data/models/phase40/review/review-queue.jsonl</automated>
  </verify>
  <done>The executor has a complete local full-message Vietnamese reviewer return for every mandatory error and calibration row, with no frozen evaluation mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Persist, reverify, and report the qualitative closure evidence</name>
  <precondition>Task 1 returned `data/models/phase40/review/reviewer-return.jsonl` with one note for every immutable queue key, and a Vietnamese-fluent reviewer is available to attest at finalizer invocation.</precondition>
  <read_first>
    - `data/models/phase40/review/review-queue.jsonl`
    - `data/models/phase40/review/review-queue-manifest.json`
    - `data/models/phase40/review/reviewer-return.jsonl`
    - `data/models/phase40/comparison-manifest.json`
    - `.planning/phases/40-multi-model-training-evidence/40-AI-SPEC.md`
    - `src/model_adaptation/phase40_handoff.py`
  </read_first>
  <files>data/models/phase40/review/human-review-notes.jsonl, data/models/phase40/review/human-review-manifest.json, data/models/phase40/review/human-review-report.md, .planning/phases/40-multi-model-training-evidence/40-VIETNAMESE-ERROR-REVIEW.md</files>
  <action>Invoke the Plan 40-02 human-review finalizer on the immutable queue and structured reviewer return with an explicit Vietnamese-fluent reviewer attestation. Reject a missing invocation attestation or any missing, extra, duplicate, reordered-key, blank-note, unsupported-enum, altered-tag, changed canonical sequence, changed `raw_message` or `source_row_sha256`, or changed gold/prediction/run/artifact field. Normalize only schema-defined review fields and preserve the reviewer input unchanged for provenance. Re-run canonical train/validation preflight internally, never through a caller-supplied path, and re-prove every queue message/source-row hash against its authorized validation snapshot by stable ID without consulting the held-out partition. Recompute all mandatory-slice and deterministic-sample coverage from frozen selected-checkpoint predictions, confirm queue/comparison/source hashes, and prove raw outputs/logits, metrics, safety gates, and selected-checkpoint records still match their pre-review hashes. Atomically write canonical `human-review-notes.jsonl`, `human-review-manifest.json`, and `human-review-report.md` with per-model/per-slice counts, assessment summaries, recurring mechanism/shortcut observations, disagreement counts, attestation status, and single-seed/development-validation limitations. After both normal and verify-only finalizer runs reproduce those canonical bytes, mechanically copy `human-review-report.md` to `.planning/phases/40-multi-model-training-evidence/40-VIETNAMESE-ERROR-REVIEW.md` and require byte equality plus equal SHA-256. Human disagreement is reported but never applied as a relabel, prediction repair, metric change, or checkpoint reselection. Run the full model-adaptation regression suite.</action>
  <acceptance_criteria>
    - Queue keys and canonical review-note keys are an exact one-to-one ordered set with no missing, extra, or duplicate rows.
    - Every full message/source-row hash rebinds to the authorized canonical validation snapshot, and every frozen prediction/selection identity remains unchanged.
    - Coverage proves 100% review of invalid outputs, risky-to-benign errors, Zalo-involved errors, benign-to-risky errors, cross-scam errors, and deterministic correct samples.
    - `human-review-manifest.json` hashes the frozen comparison, canonical validation identity, queue, unchanged reviewer return, canonical notes, and canonical report, and records Vietnamese-fluency attestation.
    - The report exposes all assessment/disagreement outcomes and limitations without changing a data label, model output, metric, safety result, or checkpoint choice.
    - A second verify-only run changes no byte and the full model-adaptation regression suite passes.
  </acceptance_criteria>
  <verify>
    <automated>python -m src.model_adaptation.cli phase40-finalize-human-review --request-path data/models/phase40/full-run-request.json --repo-root . --scope-amendment-path data/models/phase40/two-full-model-scope-amendment.json --comparison-manifest-path data/models/phase40/comparison-manifest.json --selected-predictions-path data/models/phase40/selected-prediction-bundles.json --queue-path data/models/phase40/review/review-queue.jsonl --queue-manifest-path data/models/phase40/review/review-queue-manifest.json --reviewer-return-path data/models/phase40/review/reviewer-return.jsonl --output-root data/models/phase40/review --vietnamese-fluent-attestation
python -m src.model_adaptation.cli phase40-finalize-human-review --request-path data/models/phase40/full-run-request.json --repo-root . --scope-amendment-path data/models/phase40/two-full-model-scope-amendment.json --comparison-manifest-path data/models/phase40/comparison-manifest.json --selected-predictions-path data/models/phase40/selected-prediction-bundles.json --queue-path data/models/phase40/review/review-queue.jsonl --queue-manifest-path data/models/phase40/review/review-queue-manifest.json --reviewer-return-path data/models/phase40/review/reviewer-return.jsonl --output-root data/models/phase40/review --vietnamese-fluent-attestation --verify-only</automated>
  </verify>
  <done>The Phase 40 comparison has complete, deterministic, full-message Vietnamese qualitative review evidence linked to every required row without altering the frozen experiment.</done>
</task>

</tasks>

## Artifacts this phase produces

- `data/models/phase40/review/reviewer-return.jsonl`: unchanged structured Vietnamese-fluent human input with exact queue keys and immutable source/prediction fields.
- `data/models/phase40/review/human-review-notes.jsonl`: normalized exact-coverage assessment and mechanism notes.
- `data/models/phase40/review/human-review-manifest.json`: hash-linked comparison/source/queue/input/note/report identities and coverage.
- `data/models/phase40/review/human-review-report.md`: canonical hash-bound qualitative report emitted by the finalizer.
- `.planning/phases/40-multi-model-training-evidence/40-VIETNAMESE-ERROR-REVIEW.md`: complete per-model/per-slice qualitative review, disagreement summary, and limitations.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Immutable review queue -> human judgment | Full Vietnamese messages and frozen model outcomes cross into subjective interpretation without granting mutation authority. |
| Structured reviewer return -> canonical review evidence | Human-authored observations cross into hash-linked machine outputs and must have exact coverage and immutable source bindings. |
| Review evidence -> Phase 40 claims | Qualitative interpretations cross into the final report while metrics, selections, and development-only limitations remain fixed. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-40-06-01 | Tampering | queue and reviewer-return binding | high | mitigate | Exact ordered model-row coverage plus immutable message/source hash, gold/prediction, run/artifact, and slice-tag equality; mutations fail finalization. |
| T-40-06-02 | Repudiation | reviewer observations | high | mitigate | Vietnamese-fluency attestation, non-empty structured notes, unchanged raw input retention, canonical normalized notes, and SHA-256 provenance. |
| T-40-06-03 | Information disclosure | human review | high | mitigate | Local-only queue/review workflow, no external model/API/service, and no credentials or personal paths in notes/evidence. |
| T-40-06-04 | Spoofing | qualitative closure claim | high | mitigate | Recomputed mandatory-slice/sample coverage, canonical validation rebinding, immutable comparison hashes, and byte-stable verify-only reruns. |
</threat_model>

<verification>
Before human review, run the queue verifier and require canonical message/source-row binding, exact selected-checkpoint identities, complete slice union, and deterministic correct samples. After the returned review, run the Task 2 finalizer and verify-only mode twice, then `python -m pytest tests/model_adaptation -q --basetemp .tmp/pytest-p40-06-regression`. Finalization must fail on any missing/extra/duplicate/reordered key, missing or mutated full message/source-row hash, altered frozen field/tag, blank note, unsupported assessment, absent attestation, omitted slice/sample, changed comparison/source hash, held-out access, or non-byte-stable output.
</verification>

<success_criteria>
- Every mandatory error and deterministic calibration model-row has one Vietnamese-fluent full-message assessment and non-empty mechanism note.
- Canonical validation messages/source-row hashes, selected-checkpoint predictions, metrics, safety gates, and model identities remain immutable and hash-linked.
- Review coverage, attestation, notes, and report verify deterministically from the frozen comparison and queue.
- The report exposes disagreements and single-seed/development-validation limitations without changing experiment results or making held-out/significance claims.
- Phase 40 has complete real-run, quantitative comparison, and qualitative review evidence ready for verification and Phase 41 handoff.
</success_criteria>

<output>
Create `.planning/phases/40-multi-model-training-evidence/40-06-SUMMARY.md` when done.
</output>
