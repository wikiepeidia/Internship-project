---
phase: 41-one-shot-two-model-evaluation
plan: 02
type: execute
wave: 2
depends_on:
  - 41-01
files_modified:
  - data/models/phase41/evaluation-request.json
  - data/models/phase41/frozen-inference-protocols.json
  - data/models/phase41/execution-source-manifest.json
  - data/models/phase41/preauthorization-receipt.json
  - data/models/phase41/one-shot-authorization.json
  - data/models/phase41/one-shot-claim.json
  - data/models/phase41/evaluation-access-receipt.json
  - data/models/phase41/qwen-predictions.jsonl
  - data/models/phase41/phobert-predictions.jsonl
  - data/models/phase41/results.json
  - data/models/phase41/results.md
  - data/models/phase41/terminal.json
  - data/models/phase41/evidence-manifest.json
  - data/models/phase41/deployment-fit-disposition.json
autonomous: false
requirements:
  - EVAL-08
  - EVAL-09
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "No authorization or held-out access occurs until Phase 40 has two terminal verified local models, a frozen validation comparison and human review, a closed Colab contingency, and a verified clean comparison-launch chain."
    - "A human sees the exact two model/protocol identities, held-out metadata, prior-exposure disclosure, and irreversible failure policy before explicitly authorizing one execution."
    - "One launcher invocation creates one durable global claim, acquires one held-out handle, evaluates Qwen and PhoBERT on the same in-memory cohort, and is never retried."
    - "Both models' macro/weighted F1, per-class precision/recall/F1/support, fixed-order confusion matrices, invalid outputs, and plain comparisons are frozen regardless of which model scores higher."
    - "Poor performance is terminal evidence: it cannot trigger repair, retraining, threshold/checkpoint/model selection, or another pass on the same partition."
    - "The final evidence discloses prior human/content exposure and never grants the bounded ordinary-LoRA probe a held-out accuracy claim."
    - "After terminal evidence freezes, a canonical disposition either defers deployment fitting or authorizes only a separate all-2,097-row deployment fit under a choice committed before scores were seen; neither path inherits an unbiased test-score claim."
  artifacts:
    - path: "data/models/phase41/preauthorization-receipt.json"
      provides: "Zero-access Phase 40 closure, protocol, source, contingency, registry, and held-out-metadata gate"
    - path: "data/models/phase41/evaluation-access-receipt.json"
      provides: "Claim-before-open chronology and exact one-handle/one-read evidence"
    - path: "data/models/phase41/results.json"
      provides: "Canonical two-model metrics and both fixed-order confusion matrices"
    - path: "data/models/phase41/evidence-manifest.json"
      provides: "Hash-linked terminal evidence and downstream Phase 42/43 authority"
    - path: "data/models/phase41/deployment-fit-disposition.json"
      provides: "Evidence-bound defer/authorize gate for any separately labeled post-evaluation all-data fit"
  key_links:
    - from: "data/models/phase40/comparison-manifest.json"
      to: "data/models/phase41/evaluation-request.json"
      via: "preauthorization re-verifies exactly two selected local full-model identities and closes every validation contingency before authorization"
      pattern: "qwen|phobert|selected_checkpoint_identity|execution_origin"
    - from: "data/models/phase41/one-shot-authorization.json"
      to: "data/models/phase41/evaluation-access-receipt.json"
      via: "one protected global claim is flushed before the request-fixed path is opened once"
      pattern: "authorization_sha256|claim_sha256|handle_acquisitions|read_operations"
    - from: "data/models/phase41/qwen-predictions.jsonl"
      to: "data/models/phase41/results.json"
      via: "verify-only recomputes Qwen and PhoBERT metrics over equal row IDs, source hashes, gold support, and fixed output columns"
      pattern: "row_id|source_record_sha256|gold_label|predicted_state"
    - from: "data/models/phase41/terminal.json"
      to: "data/models/phase41/evidence-manifest.json"
      via: "completed terminal status and every canonical artifact hash freeze the only unbiased two-model result"
      pattern: "completed|rerun_permitted|results_sha256|report_sha256"
    - from: "data/models/phase41/evidence-manifest.json"
      to: "data/models/phase41/deployment-fit-disposition.json"
      via: "the post-result file reproduces the pre-result checkpoint choice and binds the evidence-manifest hash plus both selected checkpoints"
      pattern: "deferred|authorized_post_evaluation_fit|selected_checkpoint_identity|unbiased_test_score_claim"
---

<objective>
Authorize and perform the one final two-model held-out evaluation, then freeze terminal evidence for reporting.

Purpose: Produce the only Phase 41 Qwen-versus-PhoBERT test result without letting it become a tuning or selection signal.
Output: Explicit authorization, one access receipt, two prediction bundles, canonical metrics/report, terminal record, evidence manifest, and deployment-fit disposition.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-one-shot-two-model-evaluation/41-01-SUMMARY.md
@.planning/phases/39-independent-quality-re-judge/39-DOWNSTREAM-DATA-CONTRACT.json
@.planning/phases/40-multi-model-training-evidence/40-05-SUMMARY.md
@.planning/phases/40-multi-model-training-evidence/40-06-SUMMARY.md
@data/models/phase40/two-full-model-scope-amendment.json
@data/models/phase40/comparison-authority-preflight.json
@data/models/phase40/comparison-manifest.json
@data/models/phase40/review/human-review-manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze zero-access preauthorization after verified Phase 40 closure</name>
  <precondition>Phase 40 Plans 05 and 06 are complete, and their canonical comparison, review, run-evidence, GGUF, clean-launch, and closure artifacts exist.</precondition>
  <files>C:/ProgramData/VNPhish/phase41-evaluation-evidence/evaluation-request.json, C:/ProgramData/VNPhish/phase41-evaluation-evidence/frozen-inference-protocols.json, C:/ProgramData/VNPhish/phase41-evaluation-evidence/execution-source-manifest.json, C:/ProgramData/VNPhish/phase41-evaluation-evidence/preauthorization-receipt.json, data/models/phase41/preclaim-audit/41-02-preclaim-failure.json, data/models/phase41/preclaim-audit/41-02-superseded-authorization.json</files>
  <action>Run the production prepare command against the code-fixed Phase 39 downstream contract and Phase 40 authorities. Preserve the sole rejected bootstrap-preflight invocation as a canonical audit: it exited at the OneDrive output-root reparse guard before materialization, claim, or held-out access, and its old request/source/protocol/preauthorization/authorization/launcher hashes and exact absent post-gate artifacts must remain verifiable. The old authorization is superseded after source/output authority changes and cannot authorize the fresh request. Provision the fixed protected non-reparse `C:\ProgramData\VNPhish\phase41-evaluation-evidence` root and its protected parent, stage exactly the manifest-listed source closure plus launcher beneath that root, and bind the operational-root, staged-package, and preclaim-failure-audit hashes into the fresh request and receipt. Reverify the scope-amendment/request/source-tree chain, external comparison preflight and isolated-launch receipt, both complete local run bundles, Qwen QLoRA plus GGUF proof, PhoBERT classification-head proof, selected checkpoints, validation comparison, exact human-review closure, and package/runtime identities. Require exactly Qwen QLoRA then PhoBERT; reject ordinary LoRA, an extra model, a failed/incomplete bundle, pending validation review, or any still-actionable Colab contingency. Freeze complete architecture-specific protocols and the Phase 41 execution-source import closure. Copy only the opaque reserved path string and its already-frozen SHA/count/byte/four-label-support metadata into the request; do not normalize through the filesystem or call an existence, metadata, hash, enumeration, or open operation on that path. Provision and verify the fixed protected ProgramData claim registry before the human checkpoint. Write a preauthorization receipt that records every verified upstream/protocol/source/registry hash, `reserved_split_access_attempted=false`, the known prior-human/content-exposure disclosure, and terminal policies forbidding selection, tuning, repair, retry, and post-test contingency activation. Run both frozen model adapters on their fixed synthetic smoke inputs, then verify the prepared files byte-stably.</action>
  <verify>
    <automated>python -m src.model_adaptation.cli phase41-prepare-evaluation --repo-root . --phase39-contract-path .planning/phases/39-independent-quality-re-judge/39-DOWNSTREAM-DATA-CONTRACT.json --phase40-comparison-manifest-path data/models/phase40/comparison-manifest.json --phase40-review-manifest-path data/models/phase40/review/human-review-manifest.json --preclaim-rejection-audit-path data/models/phase41/preclaim-audit/41-02-preclaim-failure.json
python -m src.model_adaptation.cli phase41-verify-preauthorization</automated>
  </verify>
  <done>All model, protocol, source, launcher, review, contingency, registry, and opaque split identities are frozen for one execution, both models pass synthetic smokes, and no held-out filesystem access has occurred.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Explicitly authorize or decline the irreversible held-out pass</name>
  <decision>Whether to authorize the one final Qwen-QLoRA/PhoBERT held-out execution now, and independently whether a later all-data deployment fit is deferred or authorized under unchanged pre-result controls.</decision>
  <context>The executor must display the prepared/request/protocol/source/Phase 40 comparison and review hashes; the two exact selected model/checkpoint identities; opaque split SHA/count/byte/support metadata; the protected registry path; the disclosure that test content had prior human exposure during corpus-quality review and thesis drafting; and the rule that creating the claim, even if later execution fails, permanently spends this holdout for an unbiased evaluation. Before any score is seen, the operator must also precommit either to defer deployment fitting or to permit only a separately labeled all-2,097-row deployment fit with separate lineage and no inherited unbiased test-score claim. Poor scores cannot alter that choice, tune the fit, or trigger another pass.</context>
  <options>
    <option id="authorize-defer-fit">
      <name>Authorize; defer fit</name>
      <pros>Produces the required terminal two-model evidence and explicitly closes the optional deployment-fit branch.</pros>
      <cons>Claim creation is irreversible; no all-data deployment model is authorized by Phase 41.</cons>
    </option>
    <option id="authorize-separate-fit">
      <name>Authorize separate fit</name>
      <pros>Produces the terminal evidence and permits a later deployment-only fit using all 2,097 rows under frozen pre-result controls.</pros>
      <cons>The later artifact needs separate lineage and cannot inherit or refresh the unbiased held-out score; technical failure after claim still forbids a retry.</cons>
    </option>
    <option id="decline">
      <name>Do not authorize</name>
      <pros>Leaves the held-out partition unspent by model evaluation.</pros>
      <cons>Phase 41 and dependent report/slide evidence remain blocked.</cons>
    </option>
  </options>
  <resume-signal>After checking the displayed identities, reply exactly `AUTHORIZE PHASE 41 ONE-SHOT; DEPLOYMENT FIT DEFERRED`, `AUTHORIZE PHASE 41 ONE-SHOT; SEPARATE DEPLOYMENT FIT AUTHORIZED`, or `DECLINE PHASE 41`.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Execute once, freeze both results, and seal the deployment-fit disposition</name>
  <precondition>Task 2 returned one exact authorization signal, including its deployment-fit choice, against the unchanged preauthorization hashes.</precondition>
  <reversibility rating="one-way">The content-SHA global claim permanently spends this partition; reversal would require a genuinely new untouched holdout, not deletion or replay.</reversibility>
  <files>C:/ProgramData/VNPhish/phase41-evaluation-evidence/one-shot-authorization.json, C:/ProgramData/VNPhish/phase41-evaluation-evidence/one-shot-claim.json, C:/ProgramData/VNPhish/phase41-evaluation-evidence/evaluation-access-receipt.json, C:/ProgramData/VNPhish/phase41-evaluation-evidence/qwen-predictions.jsonl, C:/ProgramData/VNPhish/phase41-evaluation-evidence/phobert-predictions.jsonl, C:/ProgramData/VNPhish/phase41-evaluation-evidence/results.json, C:/ProgramData/VNPhish/phase41-evaluation-evidence/results.md, C:/ProgramData/VNPhish/phase41-evaluation-evidence/terminal.json, C:/ProgramData/VNPhish/phase41-evaluation-evidence/evidence-manifest.json, C:/ProgramData/VNPhish/phase41-evaluation-evidence/deployment-fit-disposition.json, data/models/phase41/verified-export/</files>
  <action>Use the exact fresh checkpoint response to invoke the authorization verb once and freeze the fixed acknowledgement plus the pre-result deployment-fit choice; reverify all prepared bytes afterward. Invoke the staged `C:\ProgramData\VNPhish\phase41-evaluation-evidence\scripts\phase41_one_shot_launcher.ps1` exactly once and capture its exit without shell or supervisor retry. This is the sole claim-capable evaluation invocation; the separately audited earlier bootstrap preflight rejected the repo root with zero held-out access. The staged launcher must require its derived source root to equal the fixed operational root, load/hash/smoke both models before the irreversible claim, create and flush the protected global claim, acquire the request-fixed partition through one OS handle, read one immutable cohort, and run Qwen then PhoBERT without exposing gold labels to either adapter. Persist prediction rows without raw messages or raw model output, retaining only opaque row/source hashes, gold label for later metric proof, prediction/invalid state, output hash/length, and safe parser code. Freeze both models' macro/weighted F1, accuracy, per-class precision/recall/F1/support, risky misses, invalid outputs, and fixed-order confusion matrices in `results.json`; render `results.md` with unhedged higher/tie statements, including every PhoBERT advantage, and no winner/selection claim. Disclose prior human/content exposure and state that poor performance cannot cause dataset repair, retraining, threshold/checkpoint/model selection, Colab activation, or a repeated evaluation; any corrected unbiased experiment needs a new untouched holdout. State that ordinary LoRA was only a resource probe and has no held-out row. Hash all authorities, receipts, predictions, results, report, and terminal status into `evidence-manifest.json`. If the launch exits after claim with `spent_failed`, preserve that terminal evidence, do not invoke it again, and report that EVAL-09 cannot be completed on this partition. On completed status, create `deployment-fit-disposition.json` from the pre-result checkpoint choice: bind the evidence-manifest SHA and both selected-checkpoint identities; set only `deferred` or `authorized_post_evaluation_fit`; require `post_evaluation_deployment_only=true`, `separate_lineage_required=true`, `unbiased_test_score_claim=false`, and `test_outcome_used_for_tuning=false`. Authorization permits a later separate fit but does not execute it in Phase 41; it must keep frozen pre-result architecture/training controls, use a new run ID/root, and never reuse Phase 41 metrics as fit inputs. Run verify-only twice, including disposition verification, compare artifact hashes before/after, and do not load either model or touch the partition during verification. Finally copy the verified allowlist only into a new `data/models/phase41/verified-export/<evidence-manifest-sha>/` directory, reverify source and copied bytes, and write the export receipt last; never move, overwrite, or treat the repo mirror as operational authority.</action>
  <verify>
    <automated>python -m src.model_adaptation.cli phase41-verify-evidence
python -m src.model_adaptation.cli phase41-verify-evidence</automated>
  </verify>
  <done>Exactly one authorized launcher execution is terminal-complete; both honest model results and downstream hashes reproduce without reopening data/models, and one evidence-bound disposition explicitly defers fitting or permits only a separate post-evaluation deployment fit without an unbiased test-score claim.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Exactly one finalized-Qwen/PhoBERT evaluation after validation contingency closes | 01-02 | COVERED | Production guardrails precede one explicitly authorized execution. |
| REQ | EVAL-08 | One identical-cohort pass, prior exposure disclosure, no test-driven action | 01-02 | COVERED | Claim/handle chronology and terminal policy are machine-bound. |
| REQ | EVAL-09 | Freeze and plainly report both full-model results; no LoRA accuracy claim; gate any later all-data fit | 02 | COVERED | Canonical metrics, matrices, report, terminal, manifest, and deployment-fit disposition are mandatory. |
| RESEARCH | spike state machine | Durable claim before sole open; completed or spent-failed terminal state | 01-02 | COVERED | Production replaces injected opener and checkout-local registry. |
| RESEARCH | spike production gaps | OS-owned handle, protected external registry, complete protocol hashes | 01 | COVERED | Windows launcher/registry and both architecture-specific protocols are explicit. |
| CONTEXT | - | No Phase 41 CONTEXT.md exists | - | EXCLUDED | Roadmap, requirements, Phase 40 authority, and spike are the available controlling sources. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Phase 40 closure -> preauthorization | Development-only model selection and contingency decisions must be terminal before any held-out access. |
| Human decision -> irreversible claim | The user must knowingly authorize the one-way access with exact identities visible. |
| One cohort -> two metrics rows | Both architectures receive identical message/order input while preserving their frozen preprocessing differences. |
| Terminal evidence -> report/slides | Poor or PhoBERT-favorable results cross downstream without becoming a tuning signal. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-41-02-01 | Spoofing | Phase 40 closure/model identities | high | mitigate | Verify comparison launcher, two local bundles, selected checkpoints, validation review, exact protocols, and closed contingency before authorization. |
| T-41-02-02 | Repudiation | human authorization/access | high | mitigate | Blocking-human exact acknowledgement plus hash-linked authorization, claim, access, and terminal timestamps. |
| T-41-02-03 | Tampering | results and matrices | high | mitigate | Recompute from both canonical prediction bundles, exclusive writes, final manifest hashes, and repeated byte-stable verify-only. |
| T-41-02-04 | Information disclosure | held-out content | high | mitigate | Local-only one-handle read, no external service, no message persistence, and hash/length-only raw-output evidence. |
| T-41-02-05 | Elevation of privilege | test-driven follow-up | high | mitigate | Terminal manifest forbids retry, repair, retraining, thresholds, checkpoint/model selection, and contingency activation; corrected claims require a new holdout. |
| T-41-02-06 | Tampering / Repudiation | deployment-fit disposition | high | mitigate | Pre-result human choice, evidence-manifest and checkpoint binding, separate-lineage policy, deployment-only label, and explicit denial of test-outcome tuning or unbiased-score inheritance. |
</threat_model>

<verification>
Before the human checkpoint, `phase41-verify-preauthorization` must pass against the fixed ProgramData root without any held-out filesystem operation. The transcript must distinguish the one audited bootstrap-preflight rejection (zero claim/access) from the later sole claim-capable staged evaluation invocation. A completed run must have one global/local claim pair, one access receipt with one handle/read, equal ordered cohorts for two prediction bundles, two recomputable fixed-label metric/matrix rows, explicit prior-exposure and poor-result policies, and a hash-complete manifest. `deployment-fit-disposition.json` must reproduce the pre-result checkpoint choice, bind the manifest and selected checkpoints, and fail verification if it authorizes score inheritance, shared lineage, test-driven tuning, or execution inside Phase 41. The canonical verify-only command is `python -m src.model_adaptation.cli phase41-verify-evidence`; it must pass twice without artifact changes, split access, or model loading.
</verification>

<success_criteria>
- Phase 40 closure and both complete protocols are proven before the blocking human authorization.
- Exactly one two-model held-out execution completes under a durable claim and single shared snapshot.
- Results plainly include every required metric/matrix and every PhoBERT advantage without selecting or suppressing a model.
- Prior human/content exposure and ordinary-LoRA ineligibility are explicit.
- Poor results are frozen as terminal evidence and cannot reopen any data, training, threshold, checkpoint, model, or contingency decision.
- `evidence-manifest.json` and `terminal.json` provide the sole verified Phase 42/43 evidence intake.
- `deployment-fit-disposition.json` either explicitly defers fitting or authorizes only a separately labeled, separately traced post-evaluation all-data fit with no inherited unbiased test-score claim and no test-outcome tuning.
</success_criteria>

<output>
Create `.planning/phases/41-one-shot-two-model-evaluation/41-02-SUMMARY.md` when done.
</output>
