---
phase: 41.1-codebase-architecture-overhaul
plan: 16
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/architecture/test_import_boundaries.py
  - tests/architecture/test_architecture_policy.py
autonomous: true
requirements: [REFACTOR-03, REFACTOR-06]
gap_closure: true

estimate:
  tokens: 6000
  raw_tokens: 6000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The active codebase uses phase-neutral language outside explicit compatibility/historical boundaries, and the naming gate catches whitespace-separated, string, comment, and non-Python-asset chronology (not just bare Python AST identifiers) — Gap 1 of 41.1-VERIFICATION.md."
    - "The machine-readable architecture policy (architecture/module-boundaries.json) is a truly closed inventory of the live active dependency graph, including src.config, src.config.settings, and the src.source_archiving.* family, in both the dedicated config-closure test AND the general relevant-module closure test — Gap 2 of 41.1-VERIFICATION.md."
    - "New active modules and tools satisfy the 250/600/100-line budgets or an exact documented exception; scripts/archive_phase41_source_closure.py is within budget with an empty budget_exceptions array — Gap 3 of 41.1-VERIFICATION.md."
    - "The Phase 41.1 report handoff is factually accurate and internally consistent with the architecture authority: it states four reviewed legacy-only cycles (matching module-boundaries.json's four historical_sccs entries), and a dedicated test derives/checks that count from the policy file rather than a second hand-maintained number — Gap 4 of 41.1-VERIFICATION.md."
  artifacts:
    - "tests/architecture/test_import_boundaries.py (modified: adds _relevant_non_data_modules helper + two module-level constants)"
    - "tests/architecture/test_architecture_policy.py (modified: reuses the shared helper instead of a second hand-typed prefix list)"
  key_links:
    - "tests/architecture/test_import_boundaries.py::_relevant_non_data_modules -> tests/architecture/test_import_boundaries.py::test_policy_closes_every_relevant_module_and_ownership_index (closed-inventory assertion)"
    - "tests/architecture/test_import_boundaries.py::_relevant_non_data_modules -> tests/architecture/test_architecture_policy.py::test_closed_classification_and_cli_owners_have_no_auto_admission (shared predicate import, prevents future silent drift)"
---

<objective>
Close the one remaining structured gap from 41.1-VERIFICATION.md (status `gaps_found`, verified 2026-08-27T07:24:13Z) that is still open in the current worktree, and produce concrete evidence that the other three are already closed.

Investigation before writing this plan (read-only source inspection plus a bounded, isolated pytest run under the existing `PHASE411_DENY_OPEN_SENTINEL=1` guard — no protected paths touched, no training/inference/evaluation run) found that three of the four gaps recorded in 41.1-VERIFICATION.md were already closed by commits made after that verification ran (part of the `41.1-REVIEW`/`41.1-REVIEW-FIX` code-review-fix loop, which incidentally touched overlapping architecture-policy territory):

- Gap 1 (active phase-number chronology / narrow naming gate): closed. `src/runtime/analyzers/accelerated.py` and `gguf.py` no longer mention "Phase 3"; `tests/architecture/test_active_language_policy.py` now exists with a `_PHASE_PATTERN` regex (`phase[\s_\- ‐-―−]*[0-9]+`) that catches whitespace/underscore/hyphen/non-breaking-space/unicode-dash forms across `.py` (docstrings, strings, comments), `.js`, `.html`, `.css`, `.md`, `.bat`, `.ps1`, and `.sh`, with an explicit `frozen_literal_owners`/`literal_markers` allowlist for legitimate immutable compatibility identifiers. All 14 tests in that file pass.
- Gap 3 (886-line archive-script budget violation): closed. `scripts/archive_phase41_source_closure.py` is now 160 lines; the archival implementation was split into `src/source_archiving/{contracts,filesystem,service}.py`, each within the 600-line budget, all listed in `EXPECTED_BUDGETED_MODULES`/`budgeted_modules`, with `budget_exceptions` correctly empty. `tests/architecture/test_architecture_policy.py::test_new_modules_and_functions_fit_exact_static_budgets` and `test_source_archiving_replaces_only_the_compatibility_import_edge` pass.
- Gap 4 (report-handoff cycle-count mismatch): closed. `41.1-REPORT-HANDOFF.md` now states "four reviewed legacy-only cycles"; `tests/architecture/test_report_handoff.py::test_handoff_historical_cycle_count_matches_policy` derives the count from `architecture/module-boundaries.json`'s `historical_sccs` (asserts `len == 4`) and cross-checks `docs/architecture/overview.md`'s rendered table and the handoff text, so this cannot silently drift again. Passes.

Gap 2 (src.config/src.config.settings missing from the closed module inventory) is **only partially closed**. `architecture/module-boundaries.json` already lists `src.config`, `src.config.settings`, and the newer `src.source_archiving.*` family (added by the Gap-3 fix) in `active_modules`, and a dedicated new test (`test_architecture_policy.py::test_policy_closes_config_modules_and_exact_active_edges`) correctly validates this. But a second, older test — `tests/architecture/test_import_boundaries.py::test_policy_closes_every_relevant_module_and_ownership_index` — computes its own "relevant module" predicate independently and was never updated to know about `src.config` or `src.source_archiving`. Confirmed by direct execution: this test currently FAILS with `AssertionError: assert relevant == active | adapters | historical | index_modules`, reporting `src.config`, `src.config.settings`, `src.source_archiving`, `src.source_archiving.contracts`, `src.source_archiving.filesystem`, `src.source_archiving.service` as "Extra items in the right set" (i.e. present in the policy's classified union but absent from this test's narrower `relevant` predicate). This is exactly the failure mode Gap 2's `missing` list warned about: "Make the closed-universe assertion cover every live source dependency ... so this can't silently drift again" — except the drift already recurred once, in a *different* test file, when `src.source_archiving` was introduced by the Gap-3 fix without updating this second predicate too.

Purpose: eliminate the duplicate hand-maintained "relevant active module" predicate so the closed-inventory claim cannot drift out of sync between test files again, and produce a single bounded, honest verification run proving all four gaps closed together.

Output: `test_import_boundaries.py::test_policy_closes_every_relevant_module_and_ownership_index` passes; both closure predicates are derived from one shared helper; a consolidated bounded test run across the Gap-1/2/3/4-relevant files shows zero failures.

**Protected-boundary note:** this plan touches only two files under `tests/architecture/` (assertion/predicate logic, not fixture data). It does not read, enumerate, stat, or hash `data/splits/test.jsonl` or its containing directory; it does not run training, inference, evaluation, or provider calls; it does not touch D-drive model roots, ProgramData evaluation evidence, `historical/phase41-source-closure/`, the sealed export, the erratum, or any frozen model root. All verification commands use the project's existing `PHASE411_DENY_OPEN_SENTINEL=1` isolated-temp-root contract (same pattern used by 41.1-10-PLAN.md), which pre-collection-denies any attempt to open a protected path.

**Known, explicitly out-of-scope, pre-existing regression:** a *separate* subset of `tests/architecture/*.py` (files that legitimately spawn a subprocess to test import isolation with optional packages blocked — e.g. `test_runtime_boundaries.py`, `test_contract_baselines.py`, `test_dependency_groups.py`, `test_data_cli_contract.py`, `test_data_core_contract.py`, `test_data_recovery_contract.py`, `test_modeling_boundaries.py`, one test in `test_model_cli_contract.py`, and one test in `test_import_boundaries.py` itself — `test_runtime_import_does_not_initialize_training_package`) currently fail with `PermissionError: Phase 41.1 process execution denied: subprocess.run`. This is caused by the `CR-02 deny reviewed native process surfaces` security-hardening commit (`3eb5f98`) from the separate `41.1-REVIEW`/`41.1-REVIEW-FIX` code-review track, which now blanket-denies `subprocess.run` even for these tests' own legitimate isolated-subprocess self-checks. This is **not** one of the 4 structured VERIFICATION.md gaps, is unrelated to phase-neutral naming/config-closure/budgets/cycle-count, and per the planning brief must not be fixed here (`gsd-plan-phase` gap-closure scope is VERIFICATION.md gaps only, not `41.1-REVIEW.md` findings). This plan's verify commands explicitly `--deselect` the one affected node inside `test_import_boundaries.py` rather than silently claiming a false full-file pass.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-VERIFICATION.md
@architecture/module-boundaries.json
@tests/architecture/test_architecture_policy.py
@tests/architecture/test_import_boundaries.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Share one closed-inventory predicate so src.config/src.source_archiving cannot drift out of a second test again</name>
  <files>tests/architecture/test_import_boundaries.py, tests/architecture/test_architecture_policy.py</files>
  <read_first>
    - .planning/phases/41.1-codebase-architecture-overhaul/41.1-VERIFICATION.md (Gap 2 entry, lines 28-40 of the frontmatter `gaps:` block: truth "The machine-readable architecture is a closed inventory of the live active dependency graph", reason citing `test_architecture_policy.py`'s relevant-module predicate, missing item "Make the closed-universe assertion cover every live source dependency ... so this can't silently drift again")
    - tests/architecture/test_import_boundaries.py lines 1-32 (module-level constants/imports) and lines 202-227 (`test_policy_closes_every_relevant_module_and_ownership_index`, the currently-failing test and its stale inline `relevant = {...}` set-comprehension at lines 215-221, which lacks `src.config`/`src.config.` and `src.source_archiving` coverage)
    - tests/architecture/test_architecture_policy.py lines 17-30 (existing `from tests.architecture.test_import_boundaries import (...)` block) and lines 318-357 (`test_closed_classification_and_cli_owners_have_no_auto_admission`, whose inline `relevant = {...}` block at lines 332-349 is the ALREADY-CORRECT version that already includes `module == "src.config"`, `module.startswith("src.config.")`, and `"src.source_archiving"` in its prefix tuple, plus `"src.data_pipeline"` which this task's shared helper deliberately excludes since data-pipeline closure is verified separately by `test_import_boundaries.py::test_data_pipeline_classification_is_closed_and_migrations_are_isolated`)
    - architecture/module-boundaries.json (`active_modules` array — already contains `src.config`, `src.config.settings`, `src.source_archiving`, `src.source_archiving.contracts`, `src.source_archiving.filesystem`, `src.source_archiving.service`; this task does not touch this file)
  </read_first>
  <behavior>
    - Before the fix: `python -m pytest tests/architecture/test_import_boundaries.py::test_policy_closes_every_relevant_module_and_ownership_index` (run under the isolated-root contract below) fails with `AssertionError: assert relevant == active | adapters | historical | index_modules`, listing `src.config`, `src.config.settings`, `src.source_archiving`, `src.source_archiving.contracts`, `src.source_archiving.filesystem`, `src.source_archiving.service` as "Extra items in the right set" (present in the policy union, absent from this test's `relevant`).
    - After the fix: the same node passes because `relevant` is computed from a helper that includes those six modules; `test_architecture_policy.py::test_closed_classification_and_cli_owners_have_no_auto_admission` still passes with the exact same resulting set as before (this task changes how the set is computed, not what it contains for that test).
  </behavior>
  <action>
    In tests/architecture/test_import_boundaries.py, immediately after the existing `ACTIVE_TARGET_PREFIXES = ("src.artifacts", "src.modeling")` module-level constant (around line 20), add two new module-level constants and one helper function:
    - `RELEVANT_NON_DATA_SINGLETON_MODULES = ("src.artifacts", "src.config")` — exact single-module names that count as relevant on their own (not via prefix match).
    - `RELEVANT_NON_DATA_MODULE_PREFIXES = ("src.artifacts.", "src.config.", "src.core", "src.model_adaptation", "src.modeling", "src.runtime", "src.source_archiving")` — prefix tuple covering every non-data-pipeline active-tier module family, matching the already-correct prefix list in `test_architecture_policy.py`'s `test_closed_classification_and_cli_owners_have_no_auto_admission`.
    - `def _relevant_non_data_modules(modules: Mapping[str, Path]) -> set[str]:` returning `{module for module in modules if module in RELEVANT_NON_DATA_SINGLETON_MODULES or module.startswith(RELEVANT_NON_DATA_MODULE_PREFIXES)}`.

    In `test_policy_closes_every_relevant_module_and_ownership_index`, replace the inline `relevant = {...}` set-comprehension (currently `module == "src.artifacts" or module.startswith(("src.runtime", "src.core", "src.modeling")) or module.startswith("src.model_adaptation")`) with `relevant = _relevant_non_data_modules(modules)`. Leave the rest of the test (the `assert relevant == active | adapters | historical | index_modules` line and everything below it) unchanged.

    In tests/architecture/test_architecture_policy.py, add `_relevant_non_data_modules` to the existing `from tests.architecture.test_import_boundaries import (...)` import block (alongside `_data_module_classes`, `_import_edges`, `_module_paths`, `_path_to_target`, `_strong_components`, `_string_set`).

    In `test_closed_classification_and_cli_owners_have_no_auto_admission`, replace the inline `relevant = {...}` block (the one spanning `module == "src.artifacts" or module == "src.config" or module.startswith("src.config.") or module.startswith((...))`) with: `relevant = _relevant_non_data_modules(modules) | {module for module in modules if module == "src.data_pipeline" or module.startswith("src.data_pipeline.")}`. This produces the identical resulting set as before (verified by the unchanged assertion `assert set().union(*groups) == relevant` immediately below it still passing), while removing the second hand-typed copy of the non-data prefix list.

    Do not modify architecture/module-boundaries.json, any src/ file, or any doc file in this task — the policy classification is already correct; only the two duplicated test predicates need to converge on one source.
  </action>
  <verify>
    <automated>$env:PYTHONPATH=(Resolve-Path 'tests/architecture/bootstrap').Path; $env:PHASE411_DENY_OPEN_SENTINEL='1'; $env:PHASE411_REQUIRE_HISTORICAL_ARCHIVE='0'; $env:PYTHONNOUSERSITE='1'; $env:PYTEST_DISABLE_PLUGIN_AUTOLOAD='1'; $phase411Temp=Join-Path ([System.IO.Path]::GetTempPath()) ('vnphish-411-16a-'+[guid]::NewGuid().ToString('N')); $phase411Base=Join-Path $phase411Temp 'pytest'; New-Item -ItemType Directory -Path $phase411Base -Force | Out-Null; $env:DATA_DIR=Join-Path $phase411Temp 'data'; $env:MODEL_ARTIFACT_ROOT=Join-Path $phase411Temp 'models'; $env:MODEL_REGISTRY_PATH=Join-Path $phase411Temp 'registry.json'; $env:TEMP=$phase411Temp; $env:TMP=$phase411Temp; $python=(Get-Command python -CommandType Application).Source; & $python -m pytest -q -p no:cacheprovider --basetemp $phase411Base tests/architecture/test_import_boundaries.py --deselect tests/architecture/test_import_boundaries.py::test_runtime_import_does_not_initialize_training_package tests/architecture/test_architecture_policy.py::test_closed_classification_and_cli_owners_have_no_auto_admission</automated>
  </verify>
  <done>tests/architecture/test_import_boundaries.py::test_policy_closes_every_relevant_module_and_ownership_index passes (was failing before this task); the other 5 non-deselected tests in that file plus test_architecture_policy.py::test_closed_classification_and_cli_owners_have_no_auto_admission also pass, with 0 failed and exactly 1 deselected (the pre-existing, out-of-scope subprocess-denial regression).</done>
</task>

<task type="auto">
  <name>Task 2: Prove all four VERIFICATION.md gaps closed together in one bounded, honest run</name>
  <files>(none modified — verification-of-record only)</files>
  <read_first>
    - .planning/phases/41.1-codebase-architecture-overhaul/41.1-VERIFICATION.md (frontmatter `gaps:` block, all 4 entries, and the "Behavioral Spot-Checks" table's exact bounded-run pattern)
    - tests/architecture/test_active_language_policy.py (Gap 1 — 14 tests, full file, no subprocess usage)
    - tests/architecture/test_architecture_docs.py (Gap 1/2/4 doc-consistency — full file, no subprocess usage)
    - tests/architecture/test_report_handoff.py (Gap 4 — `test_handoff_historical_cycle_count_matches_policy`, full file, no subprocess usage)
    - tests/architecture/test_provenance_boundary.py (baseline provenance/boundary sanity, full file, no subprocess usage)
  </read_first>
  <action>
    No source or test edits in this task. Run the exact consolidated bounded node set below — the same `PHASE411_DENY_OPEN_SENTINEL=1` isolated-temp-root contract used throughout Phase 41.1 — covering: `test_architecture_policy.py` (whole file — Gap 1 identifier gate, Gap 2 config/edges test, Gap 3 budget/archive-split tests, text-contract tests), `test_architecture_docs.py` (whole file — overview/CLI-doc consistency with the policy, including the historical-SCC block used by Gap 4), `test_active_language_policy.py` (whole file — Gap 1's whitespace/string/comment/non-Python naming-gate coverage across all active runtime files, README, and docs), `test_report_handoff.py` (whole file — Gap 4's derived cycle-count cross-check plus provenance/storage/handoff fact contracts), `test_provenance_boundary.py` (whole file), and `test_import_boundaries.py` with `--deselect` on the one pre-existing out-of-scope subprocess-denial node. Confirmed pre-Task-1 baseline for this exact node set was `1 failed, 90 passed, 1 deselected` (the 1 failure being the Gap-2 predicate this plan's Task 1 fixes); after Task 1 the same command must report `0 failed`.
  </action>
  <verify>
    <automated>$env:PYTHONPATH=(Resolve-Path 'tests/architecture/bootstrap').Path; $env:PHASE411_DENY_OPEN_SENTINEL='1'; $env:PHASE411_REQUIRE_HISTORICAL_ARCHIVE='0'; $env:PYTHONNOUSERSITE='1'; $env:PYTEST_DISABLE_PLUGIN_AUTOLOAD='1'; $phase411Temp=Join-Path ([System.IO.Path]::GetTempPath()) ('vnphish-411-16b-'+[guid]::NewGuid().ToString('N')); $phase411Base=Join-Path $phase411Temp 'pytest'; New-Item -ItemType Directory -Path $phase411Base -Force | Out-Null; $env:DATA_DIR=Join-Path $phase411Temp 'data'; $env:MODEL_ARTIFACT_ROOT=Join-Path $phase411Temp 'models'; $env:MODEL_REGISTRY_PATH=Join-Path $phase411Temp 'registry.json'; $env:TEMP=$phase411Temp; $env:TMP=$phase411Temp; $python=(Get-Command python -CommandType Application).Source; $nodes=@('tests/architecture/test_architecture_policy.py','tests/architecture/test_architecture_docs.py','tests/architecture/test_active_language_policy.py','tests/architecture/test_report_handoff.py','tests/architecture/test_provenance_boundary.py','tests/architecture/test_import_boundaries.py','--deselect','tests/architecture/test_import_boundaries.py::test_runtime_import_does_not_initialize_training_package'); & $python -m pytest -q -p no:cacheprovider --basetemp $phase411Base $nodes</automated>
  </verify>
  <done>The consolidated run across all six files reports 0 failed and exactly 1 deselected (the pre-existing, out-of-scope subprocess-denial regression from the separate 41.1-REVIEW code-review track). This is the closure evidence for all 4 structured gaps in 41.1-VERIFICATION.md: Gap 1 (test_active_language_policy.py, 14/14), Gap 2 (test_architecture_policy.py::test_policy_closes_config_modules_and_exact_active_edges + test_import_boundaries.py::test_policy_closes_every_relevant_module_and_ownership_index, both pass), Gap 3 (test_architecture_policy.py::test_new_modules_and_functions_fit_exact_static_budgets + test_source_archiving_replaces_only_the_compatibility_import_edge), Gap 4 (test_report_handoff.py::test_handoff_historical_cycle_count_matches_policy).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process -> repository source tree | The architecture test suite reads `architecture/module-boundaries.json`, `src/**/*.py`, and documentation files under the pre-installed `PHASE411_DENY_OPEN_SENTINEL=1` deny-open guard (`tests/architecture/bootstrap/sitecustomize.py`). No untrusted external input crosses this boundary; this plan edits only static literal tuples inside two already-audited test files. |
| Closed-inventory claim -> report/defense narrative | `architecture/module-boundaries.json`'s "closed active graph" claim, and `41.1-REPORT-HANDOFF.md`'s downstream factual claims built on it, depend on the test predicates that enforce closure staying in sync with the policy file as new module families are added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41.1-16-01 | Tampering | tests/architecture/test_import_boundaries.py::_relevant_non_data_modules | low | accept | The new predicate is a pure static tuple of literal string prefixes, copied verbatim from the already-reviewed predicate in test_architecture_policy.py; it introduces no new I/O, imports, subprocess use, or external data source, so it adds no new tamperable surface beyond the existing reviewed test-policy contract. |
| T-41.1-16-02 | Repudiation | architecture/module-boundaries.json closed-inventory claim | medium | mitigate | Before this plan, two independently hand-maintained "relevant active module" predicates (in test_import_boundaries.py and test_architecture_policy.py) could silently drift apart — exactly what happened when src.source_archiving was introduced by the Gap-3 fix and only one of the two predicates was updated. Task 1 collapses both onto one shared `_relevant_non_data_modules` helper so a future new active-module family requires exactly one edit, removing the drift vector that let the "closed and exhaustive" claim go stale without any test catching it for a full review cycle. |
| T-41.1-16-03 | Information Disclosure | pytest verification commands (Task 1 and Task 2) | low | accept | Both `<automated>` commands run only inside the project's existing isolated-temp-root contract (`DATA_DIR`/`MODEL_ARTIFACT_ROOT`/`MODEL_REGISTRY_PATH` redirected to a fresh `%TEMP%` subdirectory, `PHASE411_DENY_OPEN_SENTINEL=1` pre-collection guard active) and select only named architecture-test files/nodes; no protected path (`data/splits/test.jsonl`, `historical/phase41-source-closure/`, ProgramData evaluation evidence, D-drive model roots) is enumerable or openable under this guard. |

No package-manager (npm/pip/cargo) install task exists in this plan, so the Package Legitimacy Gate does not apply and no T-{phase}-SC row is required.
</threat_model>

<verification>
Both tasks together prove: (1) the one residual, in-scope gap-2 test predicate is fixed and no longer diverges from the already-correct predicate in test_architecture_policy.py; (2) a single consolidated bounded pytest run spanning the Gap-1/2/3/4-relevant files reports 0 failed (baseline before Task 1: `1 failed, 90 passed, 1 deselected`; target after Task 1: `0 failed`, deselected count unchanged at 1 for the explicitly out-of-scope subprocess-denial node). No task in this plan opens, hashes, or enumerates `data/splits/test.jsonl` or its containing directory, runs training/inference/evaluation, or touches `historical/phase41-source-closure/`, D-drive model roots, or ProgramData evaluation evidence.
</verification>

<success_criteria>
- tests/architecture/test_import_boundaries.py::test_policy_closes_every_relevant_module_and_ownership_index passes (previously failing).
- tests/architecture/test_architecture_policy.py::test_closed_classification_and_cli_owners_have_no_auto_admission still passes with an unchanged resulting set after switching to the shared helper.
- The consolidated 6-file bounded run (test_architecture_policy.py, test_architecture_docs.py, test_active_language_policy.py, test_report_handoff.py, test_provenance_boundary.py, test_import_boundaries.py minus 1 deselected node) reports 0 failed.
- No protected path was enumerated, stated, hashed, opened, or mutated; no training/inference/evaluation/provider call occurred.
- The pre-existing, out-of-scope subprocess-denial regression (41.1-REVIEW track, CR-02) is explicitly named and excluded via `--deselect`, never silently passed over or falsely claimed as fixed.
</success_criteria>

<output>
Create `.planning/phases/41.1-codebase-architecture-overhaul/41.1-16-SUMMARY.md` when done
</output>
