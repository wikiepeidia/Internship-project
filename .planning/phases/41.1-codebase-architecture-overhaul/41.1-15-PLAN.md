---
phase: 41.1-codebase-architecture-overhaul
plan: 15
type: execute
wave: 10
depends_on: ["41.1-08"]
files_modified:
  - src/runtime/render.py
  - src/runtime/demo.py
  - src/runtime/doctor.py
  - src/runtime/demo_assets/demo.js
  - tests/architecture/test_active_language_policy.py
autonomous: true
gap_closure: true
requirements: [REFACTOR-03, REFACTOR-05]
estimate:
  tokens: 17000
  raw_tokens: 17000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Renderer, local demo, readiness doctor, and request-ordering JavaScript explain domain responsibilities without chronology wording."
    - "The phase5 release-evidence filename remains an exact policy-owned artifact literal rather than being renamed."
    - "Python AST and comment-stripped JavaScript fingerprints prove no executable behavior changed."
  artifacts:
    - path: "src/runtime/render.py"
      provides: "Domain-readable terminal rendering description"
    - path: "src/runtime/demo.py"
      provides: "Domain-readable local browser service description"
    - path: "src/runtime/doctor.py"
      provides: "Domain-readable local readiness descriptions"
    - path: "src/runtime/demo_assets/demo.js"
      provides: "Domain-readable request-ordering comment"
  key_links:
    - from: "runtime_prose_contract.json"
      to: "four runtime shell/demo files"
      via: "unchanged non-prose AST and JavaScript hashes"
  prohibitions:
    - "Do not change runtime behavior, parser output, artifact lookup, JavaScript ordering, HTML, CSS, or translations."
    - "Do not import runtime/model/backend/training/conversion/provider/evaluation code during verification."
    - "Do not access protected authorities, models, D-drive, ProgramData, or data rows."
---

<objective>
Finish the non-analyzer runtime wording slice without expanding Plan 08 beyond its file budget.

Purpose: Keep defense-facing runtime code readable while retaining D-01/D-02 behavior and artifact contracts.
Output: Domain prose in renderer/demo/doctor/demo.js with exact static fingerprints.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-CONTEXT.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-08-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace renderer and demo wording through the immutable characterization</name>
  <files>src/runtime/render.py, src/runtime/demo.py, tests/architecture/test_active_language_policy.py</files>
  <action>
    Replace only the renderer and local-demo module descriptions with concrete domain responsibilities. Extend the existing scanner with `test_active_runtime_render_and_demo_language_is_domain_named` over exactly these two paths. Preserve every import, call, signature, route, output field, and static font literal. Run the unchanged pre-edit fingerprint node and import blocker before and after edits per D-01/D-04.
  </action>
  <verify>
    <automated>$env:PYTHONPATH=(Resolve-Path 'tests/architecture/bootstrap').Path; $phase411Temp=Join-Path ([System.IO.Path]::GetTempPath()) ('vnphish-411-'+[guid]::NewGuid().ToString('N')); $phase411Base=Join-Path $phase411Temp 'pytest'; New-Item -ItemType Directory -Path $phase411Base -Force | Out-Null; $env:PHASE411_DENY_OPEN_SENTINEL='1'; $env:PHASE411_REQUIRE_HISTORICAL_ARCHIVE='0'; $env:PYTHONNOUSERSITE='1'; $env:DATA_DIR=Join-Path $phase411Temp 'data'; $env:MODEL_ARTIFACT_ROOT=Join-Path $phase411Temp 'models'; $env:MODEL_REGISTRY_PATH=Join-Path $phase411Temp 'registry.json'; $env:TEMP=$phase411Temp; $env:TMP=$phase411Temp; $python=(Get-Command python -CommandType Application).Source; &amp; $python -m pytest -q -p no:cacheprovider --basetemp $phase411Base tests/architecture/test_active_language_policy.py::test_static_language_suite_blocks_execution_imports tests/architecture/test_active_language_policy.py::test_runtime_prose_behavior_fingerprints_match_characterization tests/architecture/test_active_language_policy.py::test_active_runtime_render_and_demo_language_is_domain_named</automated>
  </verify>
  <done>Renderer and demo descriptions are domain-readable with exact non-prose structure.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite readiness and browser request-ordering prose</name>
  <files>src/runtime/doctor.py, src/runtime/demo_assets/demo.js, tests/architecture/test_active_language_policy.py</files>
  <action>
    Replace chronology wording in the doctor module/helper descriptions and the JavaScript request-ordering comment. Keep the literal `phase5-release-eval-*.json` artifact glob unchanged under its exact path/literal policy entry. Add `test_active_runtime_doctor_and_browser_language_is_domain_named`; it scans only these two files and also requires the unchanged Python AST and comment-stripped JavaScript hashes. Do not edit clean HTML/CSS/i18n assets or executable JavaScript per D-01/D-02.
  </action>
  <verify>
    <automated>$env:PYTHONPATH=(Resolve-Path 'tests/architecture/bootstrap').Path; $phase411Temp=Join-Path ([System.IO.Path]::GetTempPath()) ('vnphish-411-'+[guid]::NewGuid().ToString('N')); $phase411Base=Join-Path $phase411Temp 'pytest'; New-Item -ItemType Directory -Path $phase411Base -Force | Out-Null; $env:PHASE411_DENY_OPEN_SENTINEL='1'; $env:PHASE411_REQUIRE_HISTORICAL_ARCHIVE='0'; $env:PYTHONNOUSERSITE='1'; $env:DATA_DIR=Join-Path $phase411Temp 'data'; $env:MODEL_ARTIFACT_ROOT=Join-Path $phase411Temp 'models'; $env:MODEL_REGISTRY_PATH=Join-Path $phase411Temp 'registry.json'; $env:TEMP=$phase411Temp; $env:TMP=$phase411Temp; $python=(Get-Command python -CommandType Application).Source; &amp; $python -m pytest -q -p no:cacheprovider --basetemp $phase411Base tests/architecture/test_active_language_policy.py::test_runtime_prose_behavior_fingerprints_match_characterization tests/architecture/test_active_language_policy.py::test_active_runtime_doctor_and_browser_language_is_domain_named</automated>
  </verify>
  <done>Doctor and browser comments are phase-neutral while artifact and executable contracts remain exact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Prose edits -> runtime/browser behavior | Only characterized comments/docstrings may change |
| Artifact identity -> language policy | Exact legacy filename remains narrowly allowed, not renamed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-41.1-15-01 | Tampering | Runtime/browser code | high | mitigate | Immutable AST/comment-stripped fingerprints and exact target-path tests |
| T-41.1-15-02 | Repudiation | Artifact glob | medium | mitigate | Exact policy-owned literal and near-match rejection |
| T-41.1-15-03 | Information Disclosure | Verification | high | mitigate | Plan 11 bootstrap, exact static nodes, no runtime import |
</threat_model>

<verification>
Run only the named import-free nodes with the bootstrap installed before Python. No runtime/backend/model or broad suite is collected.
</verification>

<success_criteria>
- Four owned files use domain prose outside the exact artifact literal.
- Non-prose Python and executable JavaScript fingerprints remain exact.
- Clean demo assets remain unmodified but scan-visible.
</success_criteria>

<output>
Create `.planning/phases/41.1-codebase-architecture-overhaul/41.1-15-SUMMARY.md` when done.
</output>
