---
phase: 41.1-codebase-architecture-overhaul
plan: 09
type: execute
wave: 13
depends_on: ["41.1-11", "41.1-13"]
files_modified:
  - architecture/module-boundaries.json
  - scripts/archive_phase41_source_closure.py
  - src/source_archiving/__init__.py
  - src/source_archiving/contracts.py
  - src/source_archiving/filesystem.py
  - src/source_archiving/service.py
  - tests/architecture/test_architecture_policy.py
  - tests/architecture/fixtures/active_text_contract.json
  - tests/architecture/fixtures/tool_inventory_contract.json
  - tests/architecture/test_source_archiving.py
autonomous: true
gap_closure: true
requirements: [REFACTOR-01, REFACTOR-05]
estimate:
  tokens: 39000
  raw_tokens: 39000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A dedicated source-archiving test file independently freezes receipt/source/launcher bytes and every CLI stream before extraction."
    - "Archive and verify dispatch exclusively to post-import monkeypatched facade globals; missing/invalid parser paths dispatch zero callables."
    - "The compatibility script delegates through only src.source_archiving, and its direct src.core_binding import is removed."
    - "The code migration atomically changes the independent tool fixture from its exact pre-extraction archive import row to its exact post-extraction row, and changes the active-text fixture/policy from exactly 34 pre rows to exactly 31 post rows."
    - "Every frozen schema, hash, source member, worktree-mismatch path, production authority path, archive filename, provenance label, and CLI token has one exact pre-extraction value and one named destination owner."
    - "All 21 exact archive nodes pass after the test-support metadata verifier and the shared 15-node Plan 11 safety manifest."
    - "No Plan 11 provenance/report test file is modified by this plan."
  artifacts:
    - path: "tests/architecture/test_source_archiving.py"
      provides: "Exactly 21 synthetic archive/CLI/import/compatibility nodes"
    - path: "tests/architecture/fixtures/tool_inventory_contract.json"
      provides: "Independent post-extraction 11-tool fixture whose sole import delta is src.core_binding to src.source_archiving"
    - path: "tests/architecture/fixtures/active_text_contract.json"
      provides: "Independent post_extraction_v1 exact 31-row path/literal ownership authority"
    - path: "scripts/archive_phase41_source_closure.py"
      provides: "Thin late-bound compatibility facade with domain help"
    - path: "src/source_archiving/service.py"
      provides: "Strict capture/publication/verification orchestration"
    - path: "src/source_archiving/filesystem.py"
      provides: "Bound path and race defenses"
  key_links:
    - from: "scripts/archive_phase41_source_closure.py"
      to: "src.source_archiving"
      via: "one public-package import and late facade wrapper lookup"
    - from: "phase411_safety_nodes.txt"
      to: "21 source-archiving nodes"
      via: "ordered outer-PowerShell final gate"
    - from: "fixed-clock independent expected mapping"
      to: "receipt bytes/SHA and stdout/stderr"
      via: "stdlib-only test construction that calls no production serializer/hash helper"
    - from: "the exact frozen-literal table in this plan"
      to: "src.source_archiving contracts, filesystem, service, package exports, and thin facade"
      via: "one named destination per literal with no duplicate raw chronology value"
    - from: "Plan 07 pre_extraction_v1 fixtures"
      to: "Plan 09 post_extraction_v1 fixtures and architecture policy"
      via: "one atomic code-plus-independent-fixture transition after the Plan 13 -> 12 -> 15 -> 08 dependency chain"
  prohibitions:
    - "Do not modify tests/architecture/test_provenance_boundary.py or test_report_handoff.py."
    - "Do not invoke fixed production archive/verify entrypoints without zero-argument synthetic doubles."
    - "Do not recapture, verify, open, traverse, or compare the real archive or another protected authority."
    - "Do not select stale child-process, bootstrap-bypass, non-Python-process, former baseline, or live-authority nodes."
    - "Do not access data rows, models, D-drive, ProgramData, evidence, training, conversion, provider, inference, or evaluation code."
---

<objective>
Extract the oversized source-closure utility behind a thin domain facade with independent byte and CLI proof.

Purpose: Satisfy D-01/D-03/D-04/D-05/D-08 without weakening deterministic provenance, race defenses, or compatibility.
Output: `src.source_archiving`, a thin facade, and exactly 21 safe archive nodes in a dedicated test file.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-CONTEXT.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-11-SUMMARY.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-13-SUMMARY.md
</context>

## Exact final archive nodes

`test_source_archiving.py` owns exactly these 21 selected nodes:

1. `test_archive_bytes_and_cli_streams_match_independent_pre_refactor_baseline`
2. `test_archive_facade_help_uses_exact_domain_language`
3. `test_synthetic_archive_is_captured_receipted_and_verified`
4. `test_archive_cli_is_successful_on_legacy_console`
5. `test_archive_rejects_invalid_manifest_or_source_before_publication`
6. `test_archive_rejects_duplicate_json_keys_and_destination_collision`
7. `test_archive_rejects_overlap_and_redirecting_source_ancestry`
8. `test_archive_rejects_redirecting_destination_parent`
9. `test_archive_stage_creation_is_bound_against_ancestor_swap`
10. `test_archive_member_write_is_bound_against_ancestor_swap`
11. `test_archive_final_rename_is_bound_against_ancestor_swap`
12. `test_failed_archive_publication_keeps_final_destination_retryable`
13. `test_archived_extra_member_and_byte_drift_fail_verification`
14. `test_archive_verification_rejects_non_file_and_root_members`
15. `test_receipt_self_hash_cannot_rebind_false_mismatch_facts`
16. `test_production_entry_points_are_fixed_and_accept_no_paths`
17. `test_archive_json_reader_rejects_non_finite_tokens`
18. `test_archive_entrypoint_preserves_compatibility_surface_after_decomposition`
19. `test_archive_facade_monkeypatch_controls_service_write`
20. `test_archive_facade_dispatch_uses_post_import_monkeypatches`
21. `test_source_archive_import_is_path_inert`

## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Bounded domain-named source archiving | Tasks 1-3 |
| REQ | REFACTOR-01 source provenance | All tasks |
| REQ | REFACTOR-05 exact safe regression | Tasks 1 and 3 |
| RESEARCH | Characterize before extraction and preserve exact bytes | Task 1 |
| CONTEXT | D-01 through D-08 | All tasks and final gate |

No source item is missing; final graph/budget/docs integration is owned by Plan 10.

## Exact pre-extraction literal ownership table

Task 1 records these values from `scripts/archive_phase41_source_closure.py` before extraction. Task 2 moves ownership exactly as listed; compatibility aliases may expose the named object but may not duplicate its raw literal.

| literal ID | exact pre-extraction value | destination owner |
|---|---|---|
| `EXPECTED_SCHEMA_VERSION` | `phase41-execution-source-manifest-v1` | `src/source_archiving/contracts.py:EXPECTED_SCHEMA_VERSION` |
| `EXPECTED_TREE_SHA256` | `c3bbc8c8adaf7579fd2eb9c59a0081613be4b2cae05dfdb64472938c7e6d0434` | `src/source_archiving/contracts.py:EXPECTED_TREE_SHA256` |
| `EXPECTED_LAUNCHER_SHA256` | `c5f15a32b2c8d8ee196e3ec484707c27c4c05e5389d958626e775e44f52d49e9` | `src/source_archiving/contracts.py:EXPECTED_LAUNCHER_SHA256` |
| `EXPECTED_MANIFEST_SHA256` | `41a3a7e166dd5077b3b2c689868b862bd5665137e1824094eb5ff1cdce2b0c61` | `src/source_archiving/contracts.py:EXPECTED_MANIFEST_SHA256` |
| `PROVENANCE_LABEL` | `post_evaluation_archival_mirror_not_refactored_metric_producer` | `src/source_archiving/contracts.py:PROVENANCE_LABEL` |
| `RECEIPT_SCHEMA_VERSION` | `phase411-source-closure-archival-receipt-v1` | `src/source_archiving/contracts.py:RECEIPT_SCHEMA_VERSION` |
| `_WINDOWS_REPARSE_POINT` | `0x00000400` | `src/source_archiving/filesystem.py:_WINDOWS_REPARSE_POINT` |
| `_SHA256_RE` | `^[0-9a-f]{64}$` | `src/source_archiving/contracts.py:_SHA256_RE` |
| `_MANIFEST_KEYS` | `alternate_evaluators_permitted`, `closed_import_roots`, `files`, `launcher`, `launcher_host`, `preparation_scope`, `python`, `schema_version`, `source_tree_sha256`, `upstream_declared_source_tree_sha256` | `src/source_archiving/contracts.py:_MANIFEST_KEYS` |
| `SOURCE_MODEL_CLI` | `src/model_adaptation/cli.py` | `src/source_archiving/contracts.py:SOURCE_MODEL_CLI`; both fixed tuples reference this one literal |
| `SOURCE_PHASE41_EVALUATION` | `src/model_adaptation/phase41_evaluation.py` | `src/source_archiving/contracts.py:SOURCE_PHASE41_EVALUATION`; both fixed tuples reference this one literal |
| `_WORKTREE_MISMATCHES` | ordered tuple `SOURCE_MODEL_CLI`, `SOURCE_PHASE41_EVALUATION` | `src/source_archiving/contracts.py:_WORKTREE_MISMATCHES` |
| `PRODUCTION_EVIDENCE_ROOT` | `C:\ProgramData\VNPhish\phase41-evaluation-evidence` | `src/source_archiving/service.py:PRODUCTION_EVIDENCE_ROOT` |
| `PRODUCTION_SOURCE_ROOT` | `PRODUCTION_EVIDENCE_ROOT / "clean-runtime"` | `src/source_archiving/service.py:PRODUCTION_SOURCE_ROOT` |
| `LAUNCHER_RELATIVE_PATH` | `scripts/phase41_one_shot_launcher.ps1` | `src/source_archiving/contracts.py:LAUNCHER_RELATIVE_PATH`; manifest validation and production path reference this one literal |
| `PRODUCTION_LAUNCHER_PATH` | `PRODUCTION_EVIDENCE_ROOT / "scripts" / "phase41_one_shot_launcher.ps1"` | `src/source_archiving/service.py:PRODUCTION_LAUNCHER_PATH` using `LAUNCHER_RELATIVE_PATH` |
| `PRODUCTION_MANIFEST_PATH` | repo `data/models/phase41/verified-export/9ac54d58c273ab0a8c2f2b4b61e472a51ca94231a94b6847637ecad6ceee49f7/execution-source-manifest.json` | `src/source_archiving/service.py:PRODUCTION_MANIFEST_PATH` |
| `PRODUCTION_DESTINATION` | repo `historical/phase41-source-closure/c3bbc8c8adaf7579fd2eb9c59a0081613be4b2cae05dfdb64472938c7e6d0434` | `src/source_archiving/service.py:PRODUCTION_DESTINATION` |
| `MANIFEST_ARCHIVE_NAME` | `execution-source-manifest.json` | `src/source_archiving/contracts.py:MANIFEST_ARCHIVE_NAME` |
| `RECEIPT_ARCHIVE_NAME` | `archival-receipt.json` | `src/source_archiving/contracts.py:RECEIPT_ARCHIVE_NAME` |
| `TREE_ARCHIVE_NAME` | `tree` | `src/source_archiving/contracts.py:TREE_ARCHIVE_NAME` |
| `CLI_COMMANDS` | ordered tuple `archive`, `verify` | `scripts/archive_phase41_source_closure.py:main`; retained facade parser contract only |

`_SOURCE_PATHS` remains an ordered 37-member tuple at `src/source_archiving/contracts.py:_SOURCE_PATHS`, with these exact values and no additional member:

1. `src/__init__.py`
2. `src/config/__init__.py`
3. `src/config/settings.py`
4. `src/data_pipeline/__init__.py`
5. `src/data_pipeline/schemas.py`
6. `src/model_adaptation/__init__.py`
7. `src/model_adaptation/catalog.py`
8. `src/model_adaptation/cli.py` via `SOURCE_MODEL_CLI`
9. `src/model_adaptation/convert.py`
10. `src/model_adaptation/data.py`
11. `src/model_adaptation/doctor.py`
12. `src/model_adaptation/phase40_callbacks.py`
13. `src/model_adaptation/phase40_comparison_launch.py`
14. `src/model_adaptation/phase40_contract.py`
15. `src/model_adaptation/phase40_evidence.py`
16. `src/model_adaptation/phase40_final_authority.py`
17. `src/model_adaptation/phase40_gguf.py`
18. `src/model_adaptation/phase40_graphs.py`
19. `src/model_adaptation/phase40_handoff.py`
20. `src/model_adaptation/phase40_metrics.py`
21. `src/model_adaptation/phase40_modes.py`
22. `src/model_adaptation/phase40_notebooks.py`
23. `src/model_adaptation/phase40_phobert_release.py`
24. `src/model_adaptation/phase40_production_authorities.py`
25. `src/model_adaptation/phase40_release_authorities.py`
26. `src/model_adaptation/phase40_review.py`
27. `src/model_adaptation/phase40_runtime_materialize.py`
28. `src/model_adaptation/phase41_evaluation.py` via `SOURCE_PHASE41_EVALUATION`
29. `src/model_adaptation/phase41_protocols.py`
30. `src/model_adaptation/phobert_training.py`
31. `src/model_adaptation/pilot.py`
32. `src/model_adaptation/prompts.py`
33. `src/model_adaptation/registry.py`
34. `src/model_adaptation/schemas.py`
35. `src/model_adaptation/training.py`
36. `src/runtime/__init__.py`
37. `src/runtime/contracts.py`

The facade keeps the characterized `archive` and `verify` tokens and compatibility symbol aliases, but its module description and argparse help become the exact domain prose already frozen by Task 1. It contains none of the phase-bearing raw values above after extraction.

## Atomic independent fixture transition

The dependency chain is deliberate: Plan 08 (wave 9) and Plan 15 (wave 10) run against Plan 07's `pre_extraction_v1`; Plan 09 runs only after `41.1-13 -> 41.1-12 -> 41.1-15 -> 41.1-08`, then changes production code, both independent fixtures, and `architecture/module-boundaries.json` in one task and one commit. No intermediate commit may contain migrated code with a pre-transition fixture or post-transition fixture with old code.

The six active rows `terminal-result-schema`, `evidence-manifest-schema`, `source-manifest-schema-validator`, `materialization-schema-validator`, `provenance-erratum-schema`, and `legacy-release-artifact-glob` remain byte-for-byte identical. The archive-owned part changes by the exact map below. A `removed` destination means the raw token disappears because the surviving named constant is referenced; it is not silently allowlisted.

| id | exact literal | pre_extraction_v1 path:owner_symbol:lifecycle | post_extraction_v1 path:owner_symbol:lifecycle |
|---|---|---|---|
| `archive-facade-description` | `Archive and verify the exact source closure that produced Phase 41 evidence.` | `scripts/archive_phase41_source_closure.py:<module>.__doc__:compatibility` | `removed; facade description becomes the authorized domain prose` |
| `archive-source-manifest-schema` | `phase41-execution-source-manifest-v1` | `scripts/archive_phase41_source_closure.py:EXPECTED_SCHEMA_VERSION:compatibility` | `src/source_archiving/contracts.py:EXPECTED_SCHEMA_VERSION:active` |
| `archive-receipt-schema` | `phase411-source-closure-archival-receipt-v1` | `scripts/archive_phase41_source_closure.py:RECEIPT_SCHEMA_VERSION:compatibility` | `src/source_archiving/contracts.py:RECEIPT_SCHEMA_VERSION:active` |
| `archive-provenance-label` | `post_evaluation_archival_mirror_not_refactored_metric_producer` | `scripts/archive_phase41_source_closure.py:PROVENANCE_LABEL:compatibility` | `src/source_archiving/contracts.py:PROVENANCE_LABEL:active` |
| `archive-source-phase40-callbacks` | `src/model_adaptation/phase40_callbacks.py` | `scripts/archive_phase41_source_closure.py:_SOURCE_PATHS:compatibility` | `src/source_archiving/contracts.py:_SOURCE_PATHS:active` |
| `archive-source-phase40-comparison-launch` | `src/model_adaptation/phase40_comparison_launch.py` | `scripts/archive_phase41_source_closure.py:_SOURCE_PATHS:compatibility` | `src/source_archiving/contracts.py:_SOURCE_PATHS:active` |
| `archive-source-phase40-contract` | `src/model_adaptation/phase40_contract.py` | `scripts/archive_phase41_source_closure.py:_SOURCE_PATHS:compatibility` | `src/source_archiving/contracts.py:_SOURCE_PATHS:active` |
| `archive-source-phase40-evidence` | `src/model_adaptation/phase40_evidence.py` | `scripts/archive_phase41_source_closure.py:_SOURCE_PATHS:compatibility` | `src/source_archiving/contracts.py:_SOURCE_PATHS:active` |
| `archive-source-phase40-final-authority` | `src/model_adaptation/phase40_final_authority.py` | `scripts/archive_phase41_source_closure.py:_SOURCE_PATHS:compatibility` | `src/source_archiving/contracts.py:_SOURCE_PATHS:active` |
| `archive-source-phase40-gguf` | `src/model_adaptation/phase40_gguf.py` | `scripts/archive_phase41_source_closure.py:_SOURCE_PATHS:compatibility` | `src/source_archiving/contracts.py:_SOURCE_PATHS:active` |
| `archive-source-phase40-graphs` | `src/model_adaptation/phase40_graphs.py` | `scripts/archive_phase41_source_closure.py:_SOURCE_PATHS:compatibility` | `src/source_archiving/contracts.py:_SOURCE_PATHS:active` |
| `archive-source-phase40-handoff` | `src/model_adaptation/phase40_handoff.py` | `scripts/archive_phase41_source_closure.py:_SOURCE_PATHS:compatibility` | `src/source_archiving/contracts.py:_SOURCE_PATHS:active` |
| `archive-source-phase40-metrics` | `src/model_adaptation/phase40_metrics.py` | `scripts/archive_phase41_source_closure.py:_SOURCE_PATHS:compatibility` | `src/source_archiving/contracts.py:_SOURCE_PATHS:active` |
| `archive-source-phase40-modes` | `src/model_adaptation/phase40_modes.py` | `scripts/archive_phase41_source_closure.py:_SOURCE_PATHS:compatibility` | `src/source_archiving/contracts.py:_SOURCE_PATHS:active` |
| `archive-source-phase40-notebooks` | `src/model_adaptation/phase40_notebooks.py` | `scripts/archive_phase41_source_closure.py:_SOURCE_PATHS:compatibility` | `src/source_archiving/contracts.py:_SOURCE_PATHS:active` |
| `archive-source-phase40-phobert-release` | `src/model_adaptation/phase40_phobert_release.py` | `scripts/archive_phase41_source_closure.py:_SOURCE_PATHS:compatibility` | `src/source_archiving/contracts.py:_SOURCE_PATHS:active` |
| `archive-source-phase40-production-authorities` | `src/model_adaptation/phase40_production_authorities.py` | `scripts/archive_phase41_source_closure.py:_SOURCE_PATHS:compatibility` | `src/source_archiving/contracts.py:_SOURCE_PATHS:active` |
| `archive-source-phase40-release-authorities` | `src/model_adaptation/phase40_release_authorities.py` | `scripts/archive_phase41_source_closure.py:_SOURCE_PATHS:compatibility` | `src/source_archiving/contracts.py:_SOURCE_PATHS:active` |
| `archive-source-phase40-review` | `src/model_adaptation/phase40_review.py` | `scripts/archive_phase41_source_closure.py:_SOURCE_PATHS:compatibility` | `src/source_archiving/contracts.py:_SOURCE_PATHS:active` |
| `archive-source-phase40-runtime-materialize` | `src/model_adaptation/phase40_runtime_materialize.py` | `scripts/archive_phase41_source_closure.py:_SOURCE_PATHS:compatibility` | `src/source_archiving/contracts.py:_SOURCE_PATHS:active` |
| `archive-source-phase41-evaluation` | `src/model_adaptation/phase41_evaluation.py` | `scripts/archive_phase41_source_closure.py:_SOURCE_PATHS:compatibility` | `src/source_archiving/contracts.py:SOURCE_PHASE41_EVALUATION:active` |
| `archive-source-phase41-protocols` | `src/model_adaptation/phase41_protocols.py` | `scripts/archive_phase41_source_closure.py:_SOURCE_PATHS:compatibility` | `src/source_archiving/contracts.py:_SOURCE_PATHS:active` |
| `archive-worktree-phase41-evaluation` | `src/model_adaptation/phase41_evaluation.py` | `scripts/archive_phase41_source_closure.py:_WORKTREE_MISMATCHES:compatibility` | `removed; _WORKTREE_MISMATCHES references SOURCE_PHASE41_EVALUATION` |
| `archive-production-evidence-root` | `C:\ProgramData\VNPhish\phase41-evaluation-evidence` | `scripts/archive_phase41_source_closure.py:PRODUCTION_EVIDENCE_ROOT:compatibility` | `src/source_archiving/service.py:PRODUCTION_EVIDENCE_ROOT:active` |
| `archive-production-launcher-filename` | `phase41_one_shot_launcher.ps1` | `scripts/archive_phase41_source_closure.py:PRODUCTION_LAUNCHER_PATH:compatibility` | `removed; PRODUCTION_LAUNCHER_PATH derives from LAUNCHER_RELATIVE_PATH` |
| `archive-production-manifest-root` | `data/models/phase41/verified-export` | `scripts/archive_phase41_source_closure.py:PRODUCTION_MANIFEST_PATH:compatibility` | `src/source_archiving/service.py:PRODUCTION_MANIFEST_PATH:active` |
| `archive-production-destination-root` | `historical/phase41-source-closure` | `scripts/archive_phase41_source_closure.py:PRODUCTION_DESTINATION:compatibility` | `src/source_archiving/service.py:PRODUCTION_DESTINATION:active` |
| `archive-launcher-relative-path` | `scripts/phase41_one_shot_launcher.ps1` | `scripts/archive_phase41_source_closure.py:_manifest_records:compatibility` | `src/source_archiving/contracts.py:LAUNCHER_RELATIVE_PATH:active` |

Therefore `active_text_contract.json.contract_state` changes from `pre_extraction_v1` with exactly 34 rows to `post_extraction_v1` with exactly 31 rows: the same six active rows plus the 25 non-removed destination rows above. The policy object exact-equals that literal fixture before and after the transition. Tests use independent literal PRE and POST tables and fail any missing, extra, duplicate, mixed-state, stale-facade, moved, wrong-literal, wrong-path, wrong-owner, wrong-reason, or wrong-lifecycle row.

The tool fixture transition is equally closed. Its state changes from `pre_extraction_v1` to `post_extraction_v1`; exactly one field changes in exactly one of the 11 rows:

- Pre: `scripts/archive_phase41_source_closure.py.imports = ["__future__","argparse","contextlib","dataclasses","datetime","hashlib","json","os","pathlib","re","secrets","src.core_binding","stat","sys","typing"]`.
- Post: `scripts/archive_phase41_source_closure.py.imports = ["__future__","argparse","contextlib","dataclasses","datetime","hashlib","json","os","pathlib","re","secrets","src.source_archiving","stat","sys","typing"]`.

Every other tool field, all other ten rows, all closed-vocabulary counts, all routes, and the literal 11-path scripts tree remain identical. The post fixture is written literally and independently; it is not generated from policy or source.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Freeze independent bytes, streams, parser dispatch, and help before extraction</name>
  <files>tests/architecture/test_source_archiving.py</files>
  <behavior>
    - Archive and verify each call exactly one selected post-import double and zero unselected doubles.
    - Missing and invalid commands dispatch zero callables, exit 2, keep stdout empty, and match complete argparse stderr.
    - Manifest/source/launcher/receipt bytes, no-hash receipt SHA, full receipt SHA, and accepted stdout are independently exact at a fixed clock.
    - The desired domain help test exists before production edit and starts red only for the authorized help wording.
    - Every row and all 37 ordered source members in the exact pre-extraction literal table are independently frozen before extraction.
    - Literal independent PRE/POST expectations freeze the one-field archive tool-import transition and the exact 34-row to 31-row active-text ownership transition before production or fixture edits.
  </behavior>
  <action>
    Create `test_source_archiving.py`; do not edit Plan 11 tests. First encode the entire exact pre-extraction literal table and ordered 37-member source tuple above as independent test constants, then assert the current facade has each exact value, count, order, command token, and raw-literal ownership before moving code. Also encode, literally and independently of the two JSON fixtures and policy, the complete PRE/POST archive-tool rows and the complete 34-row PRE/31-row POST active-text tables above; assert current source matches PRE and POST differs only by the declared transition map. At literal clock `2030-01-02T03:04:05Z`, construct the full expected manifest/source/launcher/receipt mappings and canonical bytes with test-owned stdlib JSON/hash logic. Do not call a production serializer, hash helper, receipt method, or clock. Assert exact on-disk bytes and SHA values. For `archive` and `verify`, patch both zero-argument facade callables after import; require selected count one, unselected count zero, return zero, exact compact JSON plus newline on stdout, and empty stderr. For missing/invalid argv, require zero dispatch, exit 2, empty stdout, and complete test-owned stderr. Parser construction itself must never dispatch.

    Keep wrapper lookup late: these tests must fail an import-time function-object command map. Add the exact desired help node before extraction and record its expected RED against the current chronology description; all byte/stream/parser assertions must already pass. The authorized help delta is `Archive and verify an immutable source closure for reproducible evidence.` plus positional help `archive a source closure or verify an existing archive`. Do not weaken or regenerate any machine-stream baseline when Task 2 turns this node green.
  </action>
  <verify>
    <automated>$metadata=(Resolve-Path 'tests/architecture/support/verify_archived_source_git_metadata.ps1').Path; &amp; $metadata; if($LASTEXITCODE -ne 0){ exit $LASTEXITCODE }; $env:PYTHONPATH=(Resolve-Path 'tests/architecture/bootstrap').Path; $phase411Temp=Join-Path ([System.IO.Path]::GetTempPath()) ('vnphish-411-'+[guid]::NewGuid().ToString('N')); $safeBase=Join-Path $phase411Temp 'safety'; $archiveBase=Join-Path $phase411Temp 'archive'; New-Item -ItemType Directory -Path $safeBase,$archiveBase -Force | Out-Null; $env:PHASE411_DENY_OPEN_SENTINEL='1'; $env:PHASE411_REQUIRE_HISTORICAL_ARCHIVE='0'; $env:PYTHONNOUSERSITE='1'; $env:DATA_DIR=Join-Path $phase411Temp 'data'; $env:MODEL_ARTIFACT_ROOT=Join-Path $phase411Temp 'models'; $env:MODEL_REGISTRY_PATH=Join-Path $phase411Temp 'registry.json'; $env:TEMP=$phase411Temp; $env:TMP=$phase411Temp; $python=(Get-Command python -CommandType Application).Source; $safe=Get-Content -LiteralPath 'tests/architecture/fixtures/phase411_safety_nodes.txt'; &amp; $python -m pytest -q -p no:cacheprovider --basetemp $safeBase $safe; if($LASTEXITCODE -ne 0){ exit $LASTEXITCODE }; &amp; $python -m pytest -q -p no:cacheprovider --basetemp $archiveBase tests/architecture/test_source_archiving.py::test_archive_bytes_and_cli_streams_match_independent_pre_refactor_baseline</automated>
  </verify>
  <done>The independent byte/machine-stream/parser baseline passes pre-extraction, and the exact domain-help test is recorded RED for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Delegate the compatibility facade through source-archiving modules</name>
  <files>architecture/module-boundaries.json, scripts/archive_phase41_source_closure.py, src/source_archiving/__init__.py, src/source_archiving/contracts.py, src/source_archiving/filesystem.py, src/source_archiving/service.py, tests/architecture/test_architecture_policy.py, tests/architecture/fixtures/active_text_contract.json, tests/architecture/fixtures/tool_inventory_contract.json</files>
  <read_first>
    - `scripts/archive_phase41_source_closure.py`: current constants, contracts, strict JSON/hash, bound publication, receipt, verification, public entrypoints, CLI, and direct src.core_binding import
    - `test_source_archiving.py`: immutable Task 1 baseline and desired help contract
    - `src/core_binding.py`: existing bound-parent interface to consume from filesystem.py without changing it
  </read_first>
  <action>
    Perform one indivisible code-and-contract transition; these coupled files intentionally form one task and one commit because either half alone is an invalid state. Move every exact row in the literal ownership table to its one named destination: value contracts/errors and the exact 37-member tuple into `contracts.py`; bound filesystem/race defenses and `_WINDOWS_REPARSE_POINT` plus the `src.core_binding` dependency into `filesystem.py`; the four production authority locations and deterministic capture/receipt/publication/verification into `service.py`; and intentional path-inert re-exports into `__init__.py`. Consolidate the repeated Phase 41 mismatch literal and launcher identity through `SOURCE_PHASE41_EVALUATION` and `LAUNCHER_RELATIVE_PATH`, so the two explicitly removed fixture rows have no raw source occurrence. Keep each production file at most 600 physical lines and every new/moved function at most 100 AST lines.

    Make the script a thin compatibility facade importing only public `src.source_archiving`; remove its direct `src.core_binding` edge. In the same edit, change `tool_inventory_contract.json.contract_state` and policy to `post_extraction_v1`, replacing only the archive row import `src.core_binding` with `src.source_archiving`. Change `active_text_contract.json.contract_state` and policy to `post_extraction_v1`, preserve the six unrelated active rows exactly, apply every old-to-new row above, and remove exactly `archive-facade-description`, `archive-worktree-phase41-evaluation`, and `archive-production-launcher-filename`. Do not derive either fixture from policy/source. Preserve fixed production constants through aliases, public/private compatibility symbols, zero-path entrypoint signatures, module-level sys, monkeypatch seams, canonical bytes, errors, collision/retry behavior, and all bound path defenses without duplicating raw table values in the facade. Inject facade-visible clock/write seams into services. Archive/verify dispatch must look up current facade globals after monkeypatching. Change only the exact authorized module/argparse help prose; Task 1 machine bytes and parser errors remain unchanged per D-01/D-03/D-08. Add exact PRE-only, POST-only, mixed-state, missing, duplicate-owner, wrong-destination, stale-facade, wrong-value, wrong-reason, wrong-lifecycle, reordered-source, and unregistered-literal failures against independent literal tables.
  </action>
  <verify>
    <automated>$env:PYTHONPATH=(Resolve-Path 'tests/architecture/bootstrap').Path; $phase411Temp=Join-Path ([System.IO.Path]::GetTempPath()) ('vnphish-411-'+[guid]::NewGuid().ToString('N')); $phase411Base=Join-Path $phase411Temp 'pytest'; New-Item -ItemType Directory -Path $phase411Base -Force | Out-Null; $env:PHASE411_DENY_OPEN_SENTINEL='1'; $env:PHASE411_REQUIRE_HISTORICAL_ARCHIVE='0'; $env:PYTHONNOUSERSITE='1'; $env:DATA_DIR=Join-Path $phase411Temp 'data'; $env:MODEL_ARTIFACT_ROOT=Join-Path $phase411Temp 'models'; $env:MODEL_REGISTRY_PATH=Join-Path $phase411Temp 'registry.json'; $env:TEMP=$phase411Temp; $env:TMP=$phase411Temp; $python=(Get-Command python -CommandType Application).Source; $nodes=@('tests/architecture/test_source_archiving.py::test_archive_bytes_and_cli_streams_match_independent_pre_refactor_baseline','tests/architecture/test_source_archiving.py::test_archive_facade_help_uses_exact_domain_language','tests/architecture/test_architecture_policy.py::test_archive_contract_fixtures_transition_atomically','tests/architecture/test_architecture_policy.py::test_tool_inventory_exactly_classifies_scripts_imports_and_routes','tests/architecture/test_architecture_policy.py::test_active_text_contract_exactly_classifies_assets_documents_and_markers'); &amp; $python -m pytest -q -p no:cacheprovider --basetemp $phase411Base $nodes</automated>
  </verify>
  <done>The facade delegates only through the public package, domain help is green, tool and active-text fixtures/policy are atomically post_extraction_v1, every frozen literal has its one exact destination owner, and the independent machine baseline remains exact.</done>
</task>

<task type="auto">
  <name>Task 3: Complete exactly 21 synthetic archive compatibility and security nodes</name>
  <files>tests/architecture/test_source_archiving.py</files>
  <action>
    Port the existing synthetic capture, manifest, collision, ancestry, race, retry, byte-drift, member-type, self-hash, fixed-entrypoint, and non-finite-token coverage into this dedicated file, then add decomposition surface, facade-write monkeypatch, late dispatch, and path-inert import tests. The final selected set is exactly the 21 names above. Use `tmp_path` layouts and synthetic doubles only. Do not select or modify Plan 11 provenance/report tests and do not add any child-process or live-authority check.

    The final gate runs strictly in this order: `tests/architecture/support/verify_archived_source_git_metadata.ps1`; the exact 15-line Plan 11 safety manifest in one guarded Python invocation; then the exact 21 archive nodes in a second guarded Python invocation. Use separate pre-created basetemps. Fail immediately at each nonzero exit.
  </action>
  <verify>
    <automated>$metadata=(Resolve-Path 'tests/architecture/support/verify_archived_source_git_metadata.ps1').Path; &amp; $metadata; if($LASTEXITCODE -ne 0){ exit $LASTEXITCODE }; $env:PYTHONPATH=(Resolve-Path 'tests/architecture/bootstrap').Path; $phase411Temp=Join-Path ([System.IO.Path]::GetTempPath()) ('vnphish-411-'+[guid]::NewGuid().ToString('N')); $safeBase=Join-Path $phase411Temp 'safety'; $archiveBase=Join-Path $phase411Temp 'archive'; New-Item -ItemType Directory -Path $safeBase,$archiveBase -Force | Out-Null; $env:PHASE411_DENY_OPEN_SENTINEL='1'; $env:PHASE411_REQUIRE_HISTORICAL_ARCHIVE='0'; $env:PYTHONNOUSERSITE='1'; $env:PYTHONDONTWRITEBYTECODE='1'; $env:DATA_DIR=Join-Path $phase411Temp 'data'; $env:MODEL_ARTIFACT_ROOT=Join-Path $phase411Temp 'models'; $env:MODEL_REGISTRY_PATH=Join-Path $phase411Temp 'registry.json'; $env:TEMP=$phase411Temp; $env:TMP=$phase411Temp; Remove-Item Env:PYTHONHOME -ErrorAction SilentlyContinue; $python=(Get-Command python -CommandType Application).Source; $safe=Get-Content -LiteralPath 'tests/architecture/fixtures/phase411_safety_nodes.txt'; if(@($safe).Count -ne 15){ throw 'safety manifest drift' }; &amp; $python -m pytest -q -p no:cacheprovider --basetemp $safeBase $safe; if($LASTEXITCODE -ne 0){ exit $LASTEXITCODE }; $archive=@('test_archive_bytes_and_cli_streams_match_independent_pre_refactor_baseline','test_archive_facade_help_uses_exact_domain_language','test_synthetic_archive_is_captured_receipted_and_verified','test_archive_cli_is_successful_on_legacy_console','test_archive_rejects_invalid_manifest_or_source_before_publication','test_archive_rejects_duplicate_json_keys_and_destination_collision','test_archive_rejects_overlap_and_redirecting_source_ancestry','test_archive_rejects_redirecting_destination_parent','test_archive_stage_creation_is_bound_against_ancestor_swap','test_archive_member_write_is_bound_against_ancestor_swap','test_archive_final_rename_is_bound_against_ancestor_swap','test_failed_archive_publication_keeps_final_destination_retryable','test_archived_extra_member_and_byte_drift_fail_verification','test_archive_verification_rejects_non_file_and_root_members','test_receipt_self_hash_cannot_rebind_false_mismatch_facts','test_production_entry_points_are_fixed_and_accept_no_paths','test_archive_json_reader_rejects_non_finite_tokens','test_archive_entrypoint_preserves_compatibility_surface_after_decomposition','test_archive_facade_monkeypatch_controls_service_write','test_archive_facade_dispatch_uses_post_import_monkeypatches','test_source_archive_import_is_path_inert') | ForEach-Object { 'tests/architecture/test_source_archiving.py::'+$_ }; &amp; $python -m pytest -q -p no:cacheprovider --basetemp $archiveBase $archive</automated>
  </verify>
  <done>The ordered external-metadata, 15-safety-node, and 21-archive-node gate passes exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Facade -> source-archiving package | Compatibility symbols and late monkeypatch dispatch must survive delegation |
| Manifest/source -> publication | Strict validation and deterministic receipt bytes must survive extraction |
| Service -> bound filesystem | Reparse, symlink, ancestor-swap, collision, and retry defenses must remain exact |
| Synthetic tests -> protected authorities | Only tmp_path data may be opened |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-41.1-09-01 | Tampering | Receipt/CLI bytes | high | mitigate | Independent fixed-clock bytes and exact stdout/stderr/dispatch counts |
| T-41.1-09-02 | Elevation of Privilege | Filesystem publication | high | mitigate | Exact race/collision/path nodes under tmp_path |
| T-41.1-09-03 | Information Disclosure | Real authorities | critical | mitigate | Plan 11 guard, no production entrypoint, ordered metadata/safety gates |
| T-41.1-09-04 | Tampering | Facade imports | high | mitigate | One public-package import, late wrapper lookup, old core_binding edge removed |
</threat_model>

<verification>
Use only the exact ordered final gate. Every Python command installs the bootstrap and runs in a unique sandbox; no guarded Python child, broad selector, live authority, or production archive command is allowed.
</verification>

<success_criteria>
- Dedicated test_source_archiving owns exactly 21 selected archive nodes.
- Pre-extraction byte/stream/parser baselines are independent and exact.
- The literal independent tool fixture/policy transition changes only the archive import from `src.core_binding` to `src.source_archiving`; final state is `post_extraction_v1`.
- The literal independent active-text fixture/policy transition is exactly 34 PRE rows to 31 POST rows, with three named removals and no mixed or stale-facade state.
- Every listed schema, hash, 37-member source path, mismatch path, production authority path, archive filename, provenance label, and CLI token transfers to its exact one named owner without raw-literal duplication.
- Facade imports only src.source_archiving and no longer imports src.core_binding.
- Test-support Git metadata, 15 safety nodes, then 21 archive nodes pass in order.
- Plan 11 provenance/report tests and protected authorities remain untouched.
</success_criteria>

<output>
Create `.planning/phases/41.1-codebase-architecture-overhaul/41.1-09-SUMMARY.md` when done.
</output>
