# Phase 41.1: Codebase Architecture Overhaul - Context

**Gathered:** 2026-08-26
**Status:** Ready for research and planning

<domain>
## Phase Boundary

Phase 41.1 restructures the active repository so its architecture is coherent,
maintainable, and explainable in the thesis and defense. It separates the
stable user-facing runtime from experimental training, evaluation, evidence,
review, and historical migration tooling. It does not retrain a model, change
the corpus, rerun evaluation, revise a metric, or alter any frozen Phase 40/41
artifact.

This is a compatibility-first refactor, not a clean-slate rewrite. The current
repository contains 71,123 physical lines under `src/`; rewriting all of them
at once would create unacceptable regression and provenance risk. The required
outcome is a clear active architecture plus a byte-preserved historical
boundary, reached through independently verifiable migration slices.

</domain>

<decisions>
## Implementation Decisions

### Decision register
- **D-01:** Restructure the active repository through compatibility-first,
  behavior-preserving slices; do not retrain, alter the corpus, rerun
  evaluation, revise metrics, or perform a clean-slate rewrite.
- **D-02:** Keep `vnphish analyze|doctor|demo` and all 23 model-adaptation
  commands behavior-compatible while phase-neutral names become the active
  domain language and old paths remain explicit shims.
- **D-03:** Preserve the exact evaluated source closure and all frozen
  Phase 40/41 evidence identities as immutable historical authorities; never
  claim refactored code produced the frozen metrics.
- **D-04:** Verify Phase 41.1 only with exact synthetic/temp test selections
  protected before collection and in subprocesses; never access or rerun the
  reserved held-out evaluation.
- **D-05:** Make the installed runtime and new integrity/artifact/modeling/data
  modules an acyclic active graph with one-way lazy adapters into an explicit
  historical allowlist; legacy-only cycles may remain documented debt.
- **D-06:** Preserve the four sealed D-drive model roots and the defense GGUF;
  storage cleanup remains a separately authorized exact-path operation.
- **D-07:** Gate Phase 42 on the report-ready architecture/provenance handoff,
  while Phase 44 remains the final student-authored comment walkthrough and
  defense rehearsal rather than a second structural rewrite.
- **D-08:** Archive from the literal manifest-matching ProgramData authority,
  derive dependency extras after extraction, preserve the existing doctor
  parser quirk, and enforce the agreed active-code size budgets.

### User-visible outcome
- The active code should no longer read like a chronology of emergency phases
  and repair scripts.
- `src/model_adaptation/cli.py` must stop being a 1,329-line multipurpose hub.
- Forward-facing APIs and module names use domain language rather than
  `phase40`/`phase41` names.
- One-off repair tools remain traceable as migrations; they are archived or
  wrapped, not silently erased.
- Architecture documentation must be clear enough to reuse directly as the
  factual basis for the report and defense explanation.

### Compatibility and evidence boundary
- Preserve `vnphish analyze`, `vnphish doctor`, and `vnphish demo` exactly as
  the installed public interface.
- Preserve every supported `python -m src.model_adaptation.cli` command and
  flag through thin compatibility dispatchers until caller searches and tests
  prove removal safe.
- Freeze the Phase 41-producing source closure identified by source-tree SHA
  `c3bbc8c8adaf7579fd2eb9c59a0081613be4b2cae05dfdb64472938c7e6d0434`.
  New source must never be represented as the source that generated the
  already-frozen results.
- Treat the verified Phase 41 export and provenance erratum as one immutable
  reporting authority. Do not modify, reserialize, or regenerate them.
- Preserve schema-version strings, JSON keys, canonical byte encoding,
  filenames, checkpoint identities, and old import/command paths needed by
  historical evidence.
- Do not issue deprecation warnings from hash-sensitive evidence commands.

### Test and data safety
- Characterize existing behavior before moving implementations.
- Use synthetic and temporary fixtures only for Phase 41.1 verification.
- Do not enumerate, inspect, hash, open, copy, or rerun the reserved held-out
  partition or its containing directory.
- Do not use broad test commands whose collection or fixtures may touch the
  reserved partition. Every plan must name a bounded safe test selection.
- Migration happens in small slices with a compatibility test after each
  slice; no mass rename or private-symbol rewrite is allowed.

### Target architecture
- Keep `src/runtime` as the stable public application; it is already small and
  its `vnphish` entry point is the actual installed CLI.
- Make `src/model_adaptation/cli.py` a thin lazy dispatcher backed by focused
  command modules.
- Introduce phase-neutral boundaries for integrity primitives, training,
  inference, evaluation, and read-only evidence consumption.
- Separate reusable data schemas/normalization/deduplication/splitting from
  generation, judge/manual-review workflows, and historical migrations.
- Move phase-specific implementations behind `legacy/phase40` and
  `legacy/phase41` boundaries only when original import and command paths keep
  working through thin shims.
- Dependency extras should distinguish user runtime from generation, training,
  and evaluation dependencies so offline runtime installation does not pull in
  obsolete cloud-generation packages.

### Planner lock-ins after repository research
- Preserve the exact evaluated source bytes as a tracked, non-imported,
  content-addressed mirror under
  `historical/phase41-source-closure/c3bbc8c8adaf7579fd2eb9c59a0081613be4b2cae05dfdb64472938c7e6d0434/`.
  Its only source is the manifest-matching ProgramData `clean-runtime` closure,
  not current repository HEAD, which already differs in two remediated files.
- "Zero active dependency cycles" applies to `src/runtime` and the new
  phase-neutral domain modules. Existing cycles wholly inside the explicit
  historical allowlist may remain during this deadline-bound phase.
- Derive dependency-extra membership from the post-extraction import graph;
  do not guess package ownership before the neutral seams exist.
- Preserve the current `doctor --adaptation-mode` parser behavior even if it
  appears anomalous. Semantic correction is a separate behavior-change task.
- Apply planning budgets of at most 250 lines for the model-adaptation CLI
  facade, at most 600 lines for a new active non-generated module, and at most
  100 lines for a new function. A documented exception may be accepted only
  when compatibility extraction cannot be split safely.

### D-drive storage
- No deletion is part of Phase 41.1 planning or execution unless the user
  separately authorizes exact paths after reviewing the storage inventory.
- Preserve the four exact Phase 41 model roots (10.170 GiB total).
- Preserve the verified Qwen GGUF for the demo/defense unless it has first been
  copied and hash-verified elsewhere.
- Preserve compact source-runtime/controller evidence until the report and
  defense are complete.
- Large checkpoints, superseded releases, and comparison staging are cleanup
  candidates only; deleting a parent directory that contains a sealed model
  root is forbidden.

### Relationship to later phases
- Phase 42 depends on Phase 41.1 because the rewritten report should describe
  the coherent current architecture, while using Phase 41's frozen export for
  all metric claims.
- Phase 44 remains last. It is the student's guided file-by-file walkthrough,
  replacement comments in the student's own words, and defense cheatsheet. It
  does not repeat the structural overhaul.

</decisions>

<code_context>
## Existing Code Insights

### Quantified hotspots
- `src/`: 92 Python files and 71,123 physical lines.
- `src/model_adaptation`: 40 files and 52,542 lines.
- `src/data_pipeline`: 35 files and 16,257 lines.
- Phase-numbered production modules total 40,587 lines, about 57% of `src`.
- `src/model_adaptation/cli.py`: 1,329 lines, including a 512-line parser,
  23 commands, and 16 internal-module dependencies.
- `src/model_adaptation/phase41_evaluation.py`: 6,927 lines.
- `src/model_adaptation/training.py`: 5,279 lines.
- `src/data_pipeline/judge_merge.py`: 3,914 lines.
- Across `src/` and `scripts/`, 112 functions exceed 100 lines and 25 exceed
  200 lines. Tests contain 52 direct private-symbol imports and 159 private
  monkeypatches, so extraction order matters.

### Stable surface
- `pyproject.toml` installs `vnphish = "src.runtime.cli:main"`.
- `src/runtime/cli.py` is 129 lines and exposes the stable `analyze`, `doctor`,
  and `demo` commands. It should remain the public application boundary.
- The large model-adaptation CLI is not installed, but launchers, notebooks,
  tests, planning records, and evidence use its module path and command names;
  it is therefore a compatibility API.

### Structural risks
- Static analysis found cycles in `phase40_evidence`/`phase40_graphs`, a
  six-module Phase 40 release/training knot, and
  `phase41_evaluation`/`phase41_protocols`.
- Canonical JSON, hashing, strict JSON parsing, path safety, atomic writes,
  tree leases, and browser verification helpers are duplicated across modules.
- Frozen historical code may retain duplicate primitives. Shared extraction is
  for active forward-facing code; historical bytes are not rewritten merely
  to eliminate duplication.

### Historical candidates
- Phase 40 operator/probe/Colab/finalization scripts are experiment history.
- Phase 41's one-shot launcher and evaluator are completed evidence machinery.
- `apply_task_scam_risk_tier_repair.py`,
  `repair_zalo_narrator_scaffold.py`,
  `reconstruct_zalo_direct_catalog.py`, `apply_mislabel_triage.py`, and
  `repair_corpus_split_governance.py` are one-off migrations, not active
  application services.
- These are archive candidates, not immediate deletion candidates, because
  their paths and hashes remain in provenance, tests, and planning records.

</code_context>

<deferred>
## Deferred Ideas

- Student-authored comments and the final file-by-file defense walkthrough
  remain Phase 44 work.
- Removing compatibility shims is deferred until after report/slide/defense
  consumers are migrated and a later caller audit proves each removal safe.
- Model retraining, dataset repair, held-out re-evaluation, metric changes, and
  optional all-data deployment fitting are outside this phase.
- Actual D-drive deletion is a separate explicit user-approved operation.

</deferred>
