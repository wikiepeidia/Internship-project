---
phase: 41.1-codebase-architecture-overhaul
plan: 06
subsystem: architecture-documentation
tags: [python, architecture-policy, cli-contracts, provenance, storage-retention, report-handoff]

# Dependency graph
requires:
  - phase: 41.1-05
    provides: closed data-core, workflow, migration, and compatibility boundaries
provides:
  - enforceable static architecture policy for the closed active, adapter, and historical module graph
  - ordered active-architecture and exact CLI-contract documentation
  - provenance, storage-retention, and Phase 42 report handoff grounded in sealed authorities
affects: [42-report-writing, 43-report-review, 44-student-defense-walkthrough]

# Actuals (#2632)
actuals:
  tokens: 19764
  tasks: 3
  commits: 7

# Tech tracking
tech-stack:
  added: []
  patterns: [closed static policy contract, fixture-bound documentation, immutable evidence handoff]

key-files:
  created:
    - tests/architecture/test_architecture_policy.py
    - tests/architecture/test_architecture_docs.py
    - tests/architecture/test_report_handoff.py
    - docs/architecture/overview.md
    - docs/architecture/cli-contracts.md
    - docs/architecture/provenance.md
    - docs/architecture/storage-retention.md
    - .planning/phases/41.1-codebase-architecture-overhaul/41.1-REPORT-HANDOFF.md
  modified:
    - architecture/module-boundaries.json
    - tests/architecture/test_import_boundaries.py

key-decisions:
  - "Treat module-boundaries-v2/static_policy as the sole machine-readable policy for active naming, line budgets, CLI ownership, and forbidden runtime reachability."
  - "Present the repository in a numbered 1-10 domain reading path while retaining phase-numbered names only as compatibility or provenance labels."
  - "Require the sealed Phase 41 export and provenance erratum beside every metric claim; the refactored source is not represented as the producer."
  - "Publish D-root retention facts only from the approved inventory, with cleanup candidates explicitly informational and non-authorized."

patterns-established:
  - "Policy-first architecture prose: documents are parsed and compared with the same closed classification and CLI fixtures enforced by tests."
  - "Evidence boundary: active code, historical producer bytes, verified export, and mandatory erratum remain distinct authorities."

requirements-completed: [REFACTOR-01, REFACTOR-02, REFACTOR-03, REFACTOR-04, REFACTOR-05, REFACTOR-06]

coverage:
  - id: D1
    description: "Closed architecture policy enforces graph direction, historical exceptions, size budgets, stable CLIs, migration paths, and phase-neutral active names."
    requirement: REFACTOR-05
    verification:
      - kind: integration
        ref: "tests/architecture/test_architecture_policy.py plus the Plan 06 Task 1 exact architecture selection"
        status: pass
    human_judgment: false
  - id: D2
    description: "Architecture and CLI documents give an ordered domain path and exactly map the checked modules, edges, three installed commands, and 23 compatibility commands."
    requirement: REFACTOR-06
    verification:
      - kind: integration
        ref: "tests/architecture/test_architecture_docs.py plus the Plan 06 Task 2 exact architecture selection"
        status: pass
    human_judgment: false
  - id: D3
    description: "Provenance, retention, and report-handoff documents preserve exact source/export/erratum/storage facts and reject unsafe claims or deletion guidance."
    requirement: REFACTOR-01
    verification:
      - kind: integration
        ref: "tests/architecture/test_report_handoff.py plus the Plan 06 Task 3 exact architecture selection"
        status: pass
    human_judgment: false

# Metrics
duration: 24min
completed: 2026-08-27
status: complete
---

# Phase 41.1 Plan 06: Architecture Policy and Report Handoff Summary

**A closed static policy, ordered domain map, exact CLI catalog, and immutable provenance/storage handoff now make the active repository navigable without treating historical phase names as the forward architecture.**

## Performance

- **Duration:** 24 min
- **Started:** 2026-08-27T03:01:40Z
- **Completed:** 2026-08-27T03:24:55Z
- **Tasks:** 3
- **Files modified:** 10
- **Realized diff:** 79,055 characters / 19,764 estimate-scale tokens

## Accomplishments

- Added a `module-boundaries-v2` static policy that proves zero active strongly connected components, confines the three historical SCCs, blocks runtime reachability to training/providers, enforces line budgets and phase-neutral active names, and preserves all stable CLI and migration contracts.
- Published a numbered 1-10 reading path from data through modeling and runtime to evaluation/evidence, plus an exact catalog of the three installed commands and all 23 compatibility commands (4/12/7). Human/domain names are forward-facing; historical phase-numbered names are labeled compatibility or provenance only.
- Published a hash-bound source/export/erratum chain, exact inventory-derived model/storage retention tables, and a Phase 42 claim/limitation handoff. Phase 44 retains ownership of student-written commentary and the defense walkthrough.

## Task Commits

Each task used a committed RED test followed by its committed implementation:

1. **Task 1: Trace the closed module classification through the final policy gate**
   - `6a48220` — failing architecture-policy tests
   - `dfd91b0` — enforced final architecture policy
2. **Task 2: Document active architecture and exact CLI contracts**
   - `950a9e3` — failing architecture-document tests
   - `da2e7dc` — active architecture and CLI map
3. **Task 3: Publish provenance, storage, and Phase 42 evidence handoff**
   - `4fc5792` — failing report-handoff tests
   - `91fc08b` — provenance, storage, and report handoff

The separate final metadata commit records this summary and normal planning-state updates.

## Verification Results

- **Task 1 RED:** `tests/architecture/test_architecture_policy.py` produced 4 failures and 1 pass before `static_policy` existed.
- **Task 1 GREEN:** the exact policy/CLI/import/migration/protected-authority selection passed: **35 passed in 8.63s**.
- **Tracer feedback gate:** the same exact selection passed again after commit: **35 passed in 10.17s**.
- **Task 2 RED:** `tests/architecture/test_architecture_docs.py` produced 4 failures before the documents existed.
- **Task 2 GREEN:** the exact document/policy/protected-authority selection passed: **10 passed in 4.70s**.
- **Task 3 RED:** `tests/architecture/test_report_handoff.py` produced 4 failures before the provenance, storage, and handoff documents existed.
- **Task 3 GREEN:** the exact handoff/document/policy/protected-authority selection passed: **14 passed in 4.79s**.
- **Overall named gate:** the union of all Plan 06 named architecture tests passed: **43 passed in 9.71s**.

Every GREEN gate used the architecture bootstrap, deny-open sentinel, historical-archive requirement, temporary data/model/registry roots, and an isolated pytest base directory. No Phase 41 replay, provider, training, inference, evaluator, model-loading, storage-cleanup, external-service, ProgramData, or D-root operation was performed.

## Files Created/Modified

- `architecture/module-boundaries.json` — adds the explicit `static_policy` v2 contract with empty exact-budget exceptions.
- `tests/architecture/test_import_boundaries.py` — accepts the additive v2 policy fields while preserving every prior graph invariant.
- `tests/architecture/test_architecture_policy.py` — statically enforces graph, reachability, naming, size, CLI, migration, and authority contracts.
- `docs/architecture/overview.md` — ordered active-system responsibilities, exact boundaries, and data-to-evidence flow.
- `docs/architecture/cli-contracts.md` — exact installed and compatibility CLI ownership, lazy routes, outputs, exits, and preserved parser quirk.
- `tests/architecture/test_architecture_docs.py` — mutation-sensitive equivalence checks between prose, classification, and CLI fixtures.
- `docs/architecture/provenance.md` — separates current source, historical producer closure, verified export, and mandatory erratum.
- `docs/architecture/storage-retention.md` — exact approved-inventory retention classifications without executable cleanup guidance.
- `.planning/phases/41.1-codebase-architecture-overhaul/41.1-REPORT-HANDOFF.md` — verified Phase 42 facts, citations, limitations, and prohibited claims.
- `tests/architecture/test_report_handoff.py` — rejects missing authorities, invented facts, overclaims, and unsafe storage language.

## Decisions Made

- Kept Python module names human/domain-oriented and used numbering only for the explanatory reading order. No fragile numeric module renames were introduced.
- Made `static_policy` explicit rather than inferring policy from prose or filesystem discovery; the closed classification remains the sole admission mechanism.
- Kept every result statement paired with the verified export and provenance erratum. The exact corrected limitation remains authoritative, including prior automated filesystem access, no retry, and no tuning.
- Represented all sealed and optional model/storage roots only from the already-approved inventory. Candidate space is descriptive, never deletion authorization.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Updated the pre-existing import-boundary schema gate for the explicit policy contract**

- **Found during:** Task 1 (closed module policy gate)
- **Issue:** Adding the plan-required `static_policy` object and `module-boundaries-v2` version caused the existing exact-field/version test to reject the otherwise-valid contract before the new policy gate could run.
- **Fix:** Updated only the accepted schema version and field set in `tests/architecture/test_import_boundaries.py`; all prior group, module, allowed-edge, historical-SCC, adapter, route, and provenance invariants remained unchanged.
- **Files modified:** `architecture/module-boundaries.json`, `tests/architecture/test_import_boundaries.py`
- **Verification:** The prior import-boundary test ran beside the new policy gate in both Task 1 GREEN and the post-commit tracer gate; both exact selections passed with 35 tests.
- **Committed in:** `dfd91b0`
- **Approval:** Parent executor explicitly approved this minimal Rule 3 deviation before implementation.

---

**Total deviations:** 1 auto-fixed (Rule 3 blocking)
**Impact on plan:** Required the smallest additive contract-version adjustment; no prior graph invariant was weakened and no scope was added.

## Issues Encountered

- A document-mutation test initially surfaced a dictionary `KeyError` rather than a direct assertion. The test helper was hardened to fail explicitly, then the exact gate passed.
- The first overclaim validator did not distinguish the erratum's required negative sentence from an affirmative untouchedness claim. The validator was narrowed to preserve the authoritative correction while still rejecting the prohibited affirmative claim.

Both issues were confined to new tests and resolved before their GREEN commits.

The requirements SDK completed the `REFACTOR-06` checkbox but could not match the existing traceability-table row format. The already-verified row was updated from `Pending` to `Complete` during normal final tracking; no requirement text or evidence claim changed.

## Authentication Gates

None.

## Known Stubs

None. Empty collections found by the stub scan are local test/helper accumulators, not placeholder UI or unwired production data.

## Threat Review

No additional threat surface was introduced. Changes are static JSON, documentation, and architecture tests; there are no new endpoints, authentication paths, schema trust boundaries, runtime file-access paths, or executable cleanup operations.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 42 can use `41.1-REPORT-HANDOFF.md` as its verified factual intake while citing the sealed export and erratum beside every result claim.
- The existing Phase 42 reference-report blocker remains; this plan does not invent or substitute a missing reference report.
- Phase 44 remains responsible for student-authored comments and the defense walkthrough; this plan supplies verified technical structure but does not claim student authorship.

## Self-Check: PASSED

- All 11 task, test, document, policy, handoff, and summary paths exist.
- All six task-level RED/GREEN commits resolve to commit objects.
- `git diff --check` passed for every Plan 06 file.

---
*Phase: 41.1-codebase-architecture-overhaul*
*Completed: 2026-08-27*
