---
phase: 41.1-codebase-architecture-overhaul
plan: 11
type: execute
wave: 8
depends_on: ["41.1-07"]
files_modified:
  - tests/architecture/bootstrap/sitecustomize.py
  - tests/architecture/conftest.py
  - tests/architecture/test_provenance_boundary.py
  - tests/architecture/test_report_handoff.py
  - tests/architecture/fixtures/protected_authority_baseline.json
  - tests/architecture/fixtures/report_fact_contract.json
  - tests/architecture/fixtures/phase411_guard_contract.json
  - tests/architecture/fixtures/phase411_safety_nodes.txt
  - tests/architecture/support/verify_archived_source_git_metadata.ps1
autonomous: true
gap_closure: true
requirements: [REFACTOR-01, REFACTOR-05, REFACTOR-06]
estimate:
  tokens: 39000
  raw_tokens: 39000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The authority guard installs lexical path guards and unconditional process denial before any descriptor attestation; pre-install descriptor probes equal zero."
    - "Only post-install safe descriptors become capabilities, and standard handles are accepted only when Windows reports character or pipe types."
    - "An independent test-owned manifest contains exactly seven literal protected roots and 53 fully qualified process symbols; every available member is wrapped and rejects before argument coercion or an underlying call."
    - "Conftest lexically attests the exact sitecustomize origin and rejects protected historical prefixes in sys.path/sys.modules without filesystem discovery."
    - "A non-Python PowerShell verifier binds exactly 40 fixture-derived archive paths to publication commit b0d24820720f53100d9a94174790d70b5354fa27 using Git metadata only."
    - "One reviewed manifest owns exactly 15 safe Python nodes; no guarded Python process launches a child process."
  artifacts:
    - path: "tests/architecture/bootstrap/sitecustomize.py"
      provides: "Startup-ordered lexical/descriptor/process deny policy"
    - path: "tests/architecture/fixtures/protected_authority_baseline.json"
      provides: "Strict 40-member archive metadata anchor and fixed publication commit"
    - path: "tests/architecture/fixtures/phase411_guard_contract.json"
      provides: "Independent exact seven-root and 53-process-symbol guard authority"
    - path: "tests/architecture/support/verify_archived_source_git_metadata.ps1"
      provides: "Test-support literal-path Git index/commit/type/size verifier without blob materialization"
    - path: "tests/architecture/fixtures/phase411_safety_nodes.txt"
      provides: "Exact shared 15-node safety selector manifest"
    - path: "tests/architecture/fixtures/report_fact_contract.json"
      provides: "Strict reviewed report facts with no prediction or authority payload bytes"
  key_links:
    - from: "tests/architecture/bootstrap/sitecustomize.py"
      to: "pytest collection"
      via: "lexically attested PYTHONPATH sitecustomize before conftest"
    - from: "protected_authority_baseline.json"
      to: "b0d24820720f53100d9a94174790d70b5354fa27"
      via: "40 literal index OID == publication-commit OID checks plus blob type/size"
    - from: "phase411_safety_nodes.txt"
      to: "Plan 09 final gate"
      via: "outer PowerShell launches one exact guarded pytest selection, never a Python child"
    - from: "phase411_guard_contract.json"
      to: "sitecustomize exported guard manifests"
      via: "independent fixed equality, fixed counts, platform disposition, wrapper identity, and zero-recorder-call assertions"
  prohibitions:
    - "Do not open, enumerate, stat, hash, resolve, copy, or materialize any protected authority path or byte payload."
    - "Do not permit Python, Git, shell, or any other process exception inside the guarded interpreter."
    - "Do not probe descriptors before path and process guards are installed."
    - "Do not use import-path discovery that may probe a protected tree; use lexical sys.path and sys.modules assertions only."
    - "Do not use Git commands that emit blobs, trees, broad index listings, worktree bytes, status, checkout, archive, show, or diff."
    - "Do not run broad pytest, models, providers, training, conversion, inference, evaluation, or production archive commands."
---

<objective>
Make the verifier boundary fail closed before any later language or archive test can run.

Purpose: Enforce D-03/D-04 with startup ordering, deny-all process coverage, strict reviewed fixtures, and immutable Git metadata rather than live authority reads.
Output: Guard, conftest attestation, exact safety selectors, archive publication metadata verifier, and fixture-driven report tests.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-CONTEXT.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-VERIFICATION.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-SECURITY.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-07-SUMMARY.md
</context>

## Exact shared safety nodes

`phase411_safety_nodes.txt` contains exactly these 15 nonblank newline-terminated selectors, in this order:

1. `tests/architecture/test_provenance_boundary.py::test_guard_origin_and_contract_are_exact_before_collection`
2. `tests/architecture/test_provenance_boundary.py::test_guard_blocks_all_exact_authority_roots_and_alias_spellings`
3. `tests/architecture/test_provenance_boundary.py::test_guard_path_operation_inventory_rejects_before_underlying_calls`
4. `tests/architecture/test_provenance_boundary.py::test_guard_descriptor_operation_inventory_rejects_before_underlying_calls`
5. `tests/architecture/test_provenance_boundary.py::test_guard_process_operation_inventory_rejects_before_underlying_calls`
6. `tests/architecture/test_provenance_boundary.py::test_guard_descriptor_capabilities_are_post_install_only`
7. `tests/architecture/test_provenance_boundary.py::test_protected_authority_fixture_is_strict_and_complete`
8. `tests/architecture/test_provenance_boundary.py::test_historical_import_boundary_is_static_and_unloaded`
9. `tests/architecture/test_report_handoff.py::test_report_fact_fixture_is_strict_and_baseline_bound`
10. `tests/architecture/test_report_handoff.py::test_provenance_binds_source_export_and_erratum`
11. `tests/architecture/test_report_handoff.py::test_storage_retention_matches_approved_inventory_without_deletion`
12. `tests/architecture/test_report_handoff.py::test_report_handoff_carries_facts_limitations_and_later_phase_scope`
13. `tests/architecture/test_report_handoff.py::test_handoff_historical_cycle_count_matches_policy`
14. `tests/architecture/test_report_handoff.py::test_static_validators_reject_missing_authority_invented_facts_and_commands`
15. `tests/architecture/test_report_handoff.py::test_former_live_authority_paths_are_guarded_before_call`

## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Immutable source/model/evidence boundary | Tasks 1-3 |
| REQ | REFACTOR-01 source provenance | Task 2 |
| REQ | REFACTOR-05 exact guarded verification | Tasks 1-2 |
| REQ | REFACTOR-06 truthful report facts | Task 3 |
| RESEARCH | Synthetic/static tests and immutable authorities | All tasks |
| CONTEXT | D-01 through D-08 | All actions, exact selectors, and prohibitions |

No deferred training, evaluation, storage cleanup, shim removal, report authorship, or walkthrough work is added.

## Independent exact guard contract

`tests/architecture/fixtures/phase411_guard_contract.json` is test-owned and literal. It is not generated from `sitecustomize.py`, environment discovery, or another production export.

### Seven protected roots

The manifest contains exactly these seven entries, in this order. `repo:` means the test joins the following literal POSIX-relative value to its already-known repository root using lexical `ntpath` operations only; it never probes the target.

1. `repo:data/splits`
2. `C:\ProgramData\VNPhish\phase41-evaluation-evidence`
3. `repo:historical/phase41-source-closure`
4. `repo:data/models/phase41`
5. `D:\PROJEct\AI MODELS\phase40-full-local-20260825`
6. `D:\PROJEct\AI MODELS\base\qwen2.5-7b-instruct`
7. `D:\PROJEct\AI MODELS\base\qwen3.5-4b`

### Fifty-three process symbols

The manifest contains exactly these 53 fully qualified names, in this order:

1. `os.system`
2. `os.popen`
3. `os.startfile`
4. `os.spawnl`
5. `os.spawnle`
6. `os.spawnv`
7. `os.spawnve`
8. `os.execl`
9. `os.execle`
10. `os.execlp`
11. `os.execlpe`
12. `os.execv`
13. `os.execve`
14. `os.execvp`
15. `os.execvpe`
16. `os.spawnlp`
17. `os.spawnlpe`
18. `os.spawnvp`
19. `os.spawnvpe`
20. `os.fork`
21. `os.forkpty`
22. `os.posix_spawn`
23. `os.posix_spawnp`
24. `subprocess.Popen.__init__`
25. `subprocess.run`
26. `subprocess.call`
27. `subprocess.check_call`
28. `subprocess.check_output`
29. `subprocess.getoutput`
30. `subprocess.getstatusoutput`
31. `asyncio.create_subprocess_exec`
32. `asyncio.create_subprocess_shell`
33. `asyncio.BaseEventLoop.subprocess_exec`
34. `asyncio.BaseEventLoop.subprocess_shell`
35. `multiprocessing.Process`
36. `multiprocessing.Pool`
37. `multiprocessing.process.BaseProcess.start`
38. `multiprocessing.context.BaseContext.Pool`
39. `multiprocessing.context.DefaultContext.Process`
40. `multiprocessing.context.DefaultContext.Pool`
41. `multiprocessing.context.SpawnContext.Process`
42. `multiprocessing.context.SpawnContext.Pool`
43. `multiprocessing.pool.Pool.__init__`
44. `multiprocessing.popen_spawn_win32.Popen.__init__`
45. `multiprocessing.managers.BaseManager.start`
46. `multiprocessing.resource_tracker.ResourceTracker.ensure_running`
47. `multiprocessing.forkserver.ForkServer.ensure_running`
48. `multiprocessing.util.spawnv_passfds`
49. `concurrent.futures.ProcessPoolExecutor.__init__`
50. `_winapi.CreateProcess`
51. `_posixsubprocess.fork_exec`
52. `subprocess._fork_exec`
53. `pty.spawn`

The test independently resolves each fixed name without invoking it. Its only dispositions are `wrapped` and `unavailable_on_platform`. It requires `PHASE411_PROTECTED_ROOTS` to equal the seven lexically normalized roots, `PHASE411_INSTALLED_PROCESS_OPERATIONS` to equal exactly the resolved `wrapped` subset, and a separate exported 53-entry disposition map to equal the independently resolved status for every name. Every resolved original is replaced by the deny wrapper; every wrapper test uses an inert coercion trap plus an underlying recorder and requires both recorder counts to remain zero.

<tasks>

<task type="tracer">
  <name>Task 1: Install path and process denial before safe descriptor capabilities</name>
  <files>tests/architecture/bootstrap/sitecustomize.py, tests/architecture/conftest.py, tests/architecture/test_provenance_boundary.py, tests/architecture/fixtures/phase411_guard_contract.json</files>
  <read_first>
    - `tests/architecture/bootstrap/sitecustomize.py`: current unsafe descriptor scan, child-process allowlist, lexical path wrappers, and recorder hooks
    - `tests/architecture/conftest.py`: current sentinel-only attestation
    - `tests/architecture/test_provenance_boundary.py`: current guard tests; protected targets are strings only and must never be probed
    - The exact seven-root and 53-symbol tables above: independent values to encode literally in `phase411_guard_contract.json`
  </read_first>
  <action>
    Per D-03, D-04, and D-06, encode the exact seven-root and 53-symbol tables above in `phase411_guard_contract.json`, then delete the startup descriptor scan and inherited-FD snapshot entirely. Build the protected roots from `__file__`, the three literal repo-relative manifest entries, the four literal absolute manifest entries, `ntpath.join/normpath/normcase`, and string operations; reject drive-relative spellings before any absolute-path conversion. Install lexical path wrappers first. Install unconditional low-level process denial second. Only then attest standard handles with `_winapi.GetFileType`, accepting `FILE_TYPE_CHAR` or `FILE_TYPE_PIPE` and rejecting disk, unknown, and remote handles. Create an initially empty descriptor-capability registry and register only descriptors returned after installation by guarded safe `open`, `os.open`, `os.pipe`, `dup`, and `dup2` operations. Unknown or pre-existing descriptors remain denied. Install descriptor consumers and the remaining fixed process wrappers, then set `PHASE411_GUARD_INSTALLED=True` last.

    Expose immutable facts `PHASE411_GUARD_POLICY_VERSION`, `PHASE411_PROCESS_POLICY="deny-all"`, `PHASE411_PROTECTED_ROOTS`, `PHASE411_INSTALLED_PATH_OPERATIONS`, `PHASE411_INSTALLED_DESCRIPTOR_OPERATIONS`, `PHASE411_INSTALLED_PROCESS_OPERATIONS`, `PHASE411_PROCESS_OPERATION_DISPOSITIONS`, and `PHASE411_PREINSTALL_DESCRIPTOR_PROBES=0`. The process-operation tuple and disposition-map keys must come from the literal 53-name implementation tuple and must exact-equal the independently fixed test manifest; production output may not define the test expectation. Path wrappers cover current builtins/io/os/Path/glob families, dual-path calls, aliases, namespaces, ADS, descendants, and descriptor-relative variants. Every wrapper records rejection and raises before coercing arguments or calling its original.

    Patch every available member of the literal 53-symbol table above and preserve an `unavailable_on_platform` disposition for each absent portable member. There is no Python-child or Git exception. The implementation tuple, exported dispositions, installed-wrapper tuple, and independent fixture must all use exactly those names and fixed counts; no family prefix, pattern, reflection-discovered extra, or implementation-derived expected set may satisfy acceptance.

    In the test, independently resolve all 53 fixed symbol names and compare platform disposition, implementation export, and installed wrappers against the test-owned manifest. For every available symbol, install a wrapper over an inert argument-coercion trap and underlying recorder; calling it must reject before either trap or recorder is reached, with both counts zero. Test descriptor operations `fdopen`, `fstat`, `fchmod`, `ftruncate`, `fsync`, `read`, `write`, `lseek`, `dup`, `dup2`, plus portable `fchdir`, `pread`, `readv`, and `preadv` when available. Replace all child checks with in-process compile/exec and recorder tests.

    In conftest, compare both `sitecustomize.__file__` and `sitecustomize.__spec__.origin` lexically to the exact bootstrap file without resolving, statting, or import discovery. Reject protected archive prefixes in `sys.path` and reject the exact historical module/prefix in `sys.modules` before collection. Attest all exported policy facts and exact wrapper inventories.
  </action>
  <verify>
    <automated>$env:PYTHONPATH=(Resolve-Path 'tests/architecture/bootstrap').Path; $phase411Temp=Join-Path ([System.IO.Path]::GetTempPath()) ('vnphish-411-'+[guid]::NewGuid().ToString('N')); $phase411Base=Join-Path $phase411Temp 'pytest'; New-Item -ItemType Directory -Path $phase411Base -Force | Out-Null; $env:PHASE411_DENY_OPEN_SENTINEL='1'; $env:PHASE411_REQUIRE_HISTORICAL_ARCHIVE='0'; $env:PYTHONNOUSERSITE='1'; $env:PYTHONDONTWRITEBYTECODE='1'; $env:DATA_DIR=Join-Path $phase411Temp 'data'; $env:MODEL_ARTIFACT_ROOT=Join-Path $phase411Temp 'models'; $env:MODEL_REGISTRY_PATH=Join-Path $phase411Temp 'registry.json'; $env:TEMP=$phase411Temp; $env:TMP=$phase411Temp; Remove-Item Env:PYTHONHOME -ErrorAction SilentlyContinue; $python=(Get-Command python -CommandType Application).Source; $nodes=@('tests/architecture/test_provenance_boundary.py::test_guard_origin_and_contract_are_exact_before_collection','tests/architecture/test_provenance_boundary.py::test_guard_blocks_all_exact_authority_roots_and_alias_spellings','tests/architecture/test_provenance_boundary.py::test_guard_path_operation_inventory_rejects_before_underlying_calls','tests/architecture/test_provenance_boundary.py::test_guard_descriptor_operation_inventory_rejects_before_underlying_calls','tests/architecture/test_provenance_boundary.py::test_guard_process_operation_inventory_rejects_before_underlying_calls','tests/architecture/test_provenance_boundary.py::test_guard_descriptor_capabilities_are_post_install_only'); &amp; $python -m pytest -q -p no:cacheprovider --basetemp $phase411Base $nodes</automated>
  </verify>
  <done>The guard performs zero pre-install descriptor probes, owns exactly seven protected roots and 53 process dispositions, and denies every reviewed path/descriptor/process route before coercion or any underlying call.</done>
</task>

<task type="auto">
  <name>Task 2: Anchor exactly 40 archived members to immutable Git metadata</name>
  <files>tests/architecture/fixtures/protected_authority_baseline.json, tests/architecture/fixtures/phase411_safety_nodes.txt, tests/architecture/support/verify_archived_source_git_metadata.ps1, tests/architecture/test_provenance_boundary.py</files>
  <read_first>
    - `protected_authority_baseline.json`: reviewed destination, 37 source members, launcher, sizes, and hashes; read this fixture only
    - Publication commit `b0d24820720f53100d9a94174790d70b5354fa27`: immutable Git metadata anchor; never materialize a blob
    - Task 1 guard contract: guarded Python cannot spawn Git, so metadata verification is an outer PowerShell step
  </read_first>
  <action>
    Add only `archive_publication_commit`, `expected_member_count`, and `receipt_bytes` to the reviewed fixture, with exact values `b0d24820720f53100d9a94174790d70b5354fa27`, `40`, and `1746`. Keep the existing 37 source entries and launcher facts. Derive exactly 40 literal Git paths from the fixture: manifest, receipt, 37 sources, and launcher. Reject duplicate, absolute, parent-traversing, separator-drifted, missing, or extra paths and any field/schema/type drift.

    Create the exact 15-line safety-node manifest listed above. `test_protected_authority_fixture_is_strict_and_complete` and `test_historical_import_boundary_is_static_and_unloaded` validate strict schemas, exact counts/order, the publication commit literal, and lexical `sys.path`/`sys.modules` isolation only.

    Implement `tests/architecture/support/verify_archived_source_git_metadata.ps1` as a non-Python test-support verifier; it is not a production script and must not appear in the exact 11-item `scripts/` inventory. Resolve one absolute `git.exe`, require the exact repository root as safe cwd, set `GIT_OPTIONAL_LOCKS=0`, use `--no-replace-objects`, `-C <repo>`, `--literal-pathspecs`, and literal `-- <path>` terminators. Reject `GIT_DIR`, `GIT_WORK_TREE`, `GIT_COMMON_DIR`, `GIT_INDEX_FILE`, `GIT_OBJECT_DIRECTORY`, `GIT_ALTERNATE_OBJECT_DIRECTORIES`, `GIT_QUARANTINE_PATH`, `GIT_NAMESPACE`, replacement-object configuration, and an exact `.git/objects/info/alternates` file. For each of the 40 paths, require exactly one `ls-files --stage` record with mode `100644` and stage `0`; require its index OID to equal `rev-parse b0d24820720f53100d9a94174790d70b5354fa27:<path>`; require `cat-file -t <oid>` to return `blob`; and require `cat-file -s <oid>` to equal the reviewed fixture size. Permit no command that materializes a blob or enumerates a tree/index broadly.
  </action>
  <verify>
    <automated>$metadata=(Resolve-Path 'tests/architecture/support/verify_archived_source_git_metadata.ps1').Path; &amp; $metadata; if($LASTEXITCODE -ne 0){ exit $LASTEXITCODE }; $env:PYTHONPATH=(Resolve-Path 'tests/architecture/bootstrap').Path; $phase411Temp=Join-Path ([System.IO.Path]::GetTempPath()) ('vnphish-411-'+[guid]::NewGuid().ToString('N')); $phase411Base=Join-Path $phase411Temp 'pytest'; New-Item -ItemType Directory -Path $phase411Base -Force | Out-Null; $env:PHASE411_DENY_OPEN_SENTINEL='1'; $env:PHASE411_REQUIRE_HISTORICAL_ARCHIVE='0'; $env:PYTHONNOUSERSITE='1'; $env:DATA_DIR=Join-Path $phase411Temp 'data'; $env:MODEL_ARTIFACT_ROOT=Join-Path $phase411Temp 'models'; $env:MODEL_REGISTRY_PATH=Join-Path $phase411Temp 'registry.json'; $env:TEMP=$phase411Temp; $env:TMP=$phase411Temp; $python=(Get-Command python -CommandType Application).Source; &amp; $python -m pytest -q -p no:cacheprovider --basetemp $phase411Base tests/architecture/test_provenance_boundary.py::test_protected_authority_fixture_is_strict_and_complete tests/architecture/test_provenance_boundary.py::test_historical_import_boundary_is_static_and_unloaded</automated>
  </verify>
  <done>Exactly 40 reviewed paths are anchored to the immutable publication commit using index/object metadata only.</done>
</task>

<task type="auto">
  <name>Task 3: Convert report authority checks to strict fixture-only tests</name>
  <files>tests/architecture/fixtures/report_fact_contract.json, tests/architecture/test_report_handoff.py</files>
  <read_first>
    - `test_report_handoff.py`: live receipt/export/erratum reads and current static validators to preserve
    - `protected_authority_baseline.json`: reviewed overlapping identities only; do not open paths named by it
    - `architecture/module-boundaries.json`, architecture docs, and 41.1-REPORT-HANDOFF.md: safe current policy/doc inputs
  </read_first>
  <action>
    Create a strict canonical report-fact fixture containing only reviewed source/export/erratum identities, receipt/manifest/launcher hashes, provenance label, evidence schema/artifact count/terminal policy, corrected claim, and the exact fields needed by current static report validators. Reject duplicate keys, non-finite values, extra/missing fields, wrong scalar types, and overlap drift against the protected-authority fixture. Include no predictions, split rows, model bytes, secrets, or invented claims.

    Rewrite report tests to consume only this fixture plus safe policy/docs/handoff files. Preserve provenance, retention, sealed-root, optional-GGUF, no-deletion, no-overclaim, later-phase ownership, and policy-derived four-cycle assertions. Add pure synthetic mutations for every fact. The former live authority paths are exercised only as lexical strings through the guard, proving denial before any underlying call. The seven report selectors in the shared manifest must be the complete selected report set.
  </action>
  <verify>
    <automated>$env:PYTHONPATH=(Resolve-Path 'tests/architecture/bootstrap').Path; $phase411Temp=Join-Path ([System.IO.Path]::GetTempPath()) ('vnphish-411-'+[guid]::NewGuid().ToString('N')); $phase411Base=Join-Path $phase411Temp 'pytest'; New-Item -ItemType Directory -Path $phase411Base -Force | Out-Null; $env:PHASE411_DENY_OPEN_SENTINEL='1'; $env:PHASE411_REQUIRE_HISTORICAL_ARCHIVE='0'; $env:PYTHONNOUSERSITE='1'; $env:DATA_DIR=Join-Path $phase411Temp 'data'; $env:MODEL_ARTIFACT_ROOT=Join-Path $phase411Temp 'models'; $env:MODEL_REGISTRY_PATH=Join-Path $phase411Temp 'registry.json'; $env:TEMP=$phase411Temp; $env:TMP=$phase411Temp; $nodes=Get-Content -LiteralPath 'tests/architecture/fixtures/phase411_safety_nodes.txt'; if(@($nodes).Count -ne 15 -or @($nodes | Where-Object { [string]::IsNullOrWhiteSpace($_) }).Count -ne 0 -or @($nodes | Sort-Object -Unique).Count -ne 15){ throw 'invalid safety-node manifest' }; $python=(Get-Command python -CommandType Application).Source; &amp; $python -m pytest -q -p no:cacheprovider --basetemp $phase411Base $nodes</automated>
  </verify>
  <done>All 15 exact safety/report nodes pass without a child process or live authority read.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Interpreter startup -> collection | No descriptor or import/filesystem probe may precede guard installation |
| Python API -> process leaves | Every process family must reject before coercion or its original callable |
| Reviewed fixture -> Git index/objects | Exact literal metadata may be inspected; content/tree/worktree materialization may not |
| Reviewed facts -> report validators | Fixture schema and overlapping identities must prevent invented or drifting claims |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-41.1-11-01 | Information Disclosure | Startup order | critical | mitigate | Zero pre-install probes; path/process denial precedes standard-handle and descriptor registration |
| T-41.1-11-02 | Elevation of Privilege | Process APIs | critical | mitigate | Exact cross-platform wrapper inventory, deny-all policy, recorder factories, zero underlying calls |
| T-41.1-11-03 | Information Disclosure | Git metadata verifier | high | mitigate | Fixed commit, 40 literal paths, strict Git env, index/commit/type/size only |
| T-41.1-11-04 | Tampering | Safety selector manifest | high | mitigate | Exact ordered 15-node file and strict count/uniqueness/path assertions |
| T-41.1-11-05 | Repudiation | Report fixture | high | mitigate | Strict schema, cross-binding, synthetic mutation rejection, no payload rows/bytes |
</threat_model>

<verification>
The final gate is: the PowerShell verifier at `tests/architecture/support/verify_archived_source_git_metadata.ps1`, then the exact 15-node manifest in one guarded Python invocation. The outer shell resolves absolute Python/Git executables, uses the exact repo cwd, removes PYTHONHOME, sets PYTHONNOUSERSITE, prepends only the exact bootstrap, and routes TEMP/TMP/data/model roots to one unique sandbox. No guarded Python process starts another process.
</verification>

<success_criteria>
- No preinstall fstat scan or descriptor probe remains.
- Path and unconditional process guards precede safe handle/descriptor capability registration.
- Exactly seven literal protected roots and 53 fully qualified process symbols are independently inventoried; every available symbol is wrapped and rejects before coercion or underlying calls, while unavailable symbols retain an exact disposition.
- Conftest attests exact bootstrap origin and historical import absence lexically.
- The metadata verifier checks exactly 40 paths against publication commit `b0d248...` without materializing content.
- Exactly 15 manifest-owned safety/report nodes pass; Plan 09 consumes the same manifest before archive nodes.
</success_criteria>

<output>
Create `.planning/phases/41.1-codebase-architecture-overhaul/41.1-11-SUMMARY.md` when done.
</output>
