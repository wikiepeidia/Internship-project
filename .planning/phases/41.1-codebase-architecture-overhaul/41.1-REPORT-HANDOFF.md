# Phase 41.1 Report Handoff

This is the verified factual intake for Phase 42. It separates the current active architecture
from the historical producer source and the frozen evaluation
authority. It supplies facts and limitations, not student voice, new metrics, or a
claim that later report work is already complete.

## Architecture facts

| Fact | Report-safe wording | Authority |
| --- | --- | --- |
| Installed application | The application entry point is `vnphish`, with `analyze`, `doctor`, and `demo`. | `pyproject.toml`, `docs/architecture/cli-contracts.md` |
| Active domains | Runtime, integrity/artifacts, data core/workflows/migrations, and modeling training/inference/evaluation/evidence have named owners in an acyclic active graph. | `architecture/module-boundaries.json`, `docs/architecture/overview.md` |
| Compatibility boundary | Twenty-three model-adaptation commands and old data paths remain stable through explicit adapters and fixed lazy routes. | CLI fixtures and architecture tests |
| Historical boundary | Phase-numbered implementations and three legacy-only cycles remain compatibility/provenance debt, not the forward domain model. | `architecture/module-boundaries.json` |

The refactored code did not generate the frozen metrics. Its purpose is to make the
maintained system understandable without rewriting the evidence-producing bytes.

## Metric-authority facts

| Authority | Exact identity | Use |
| --- | --- | --- |
| Historical producer source | `c3bbc8c8adaf7579fd2eb9c59a0081613be4b2cae05dfdb64472938c7e6d0434` | Explains which source closure and launcher correspond to the terminal run |
| Verified export | `9ac54d58c273ab0a8c2f2b4b61e472a51ca94231a94b6847637ecad6ceee49f7` | Sole frozen package for result, selected-model, and terminal-policy facts |
| Mandatory erratum | `c7be74346f0e217c382e556fbf0a730cb33be50356d4155356a5b024871a1672` | Corrects prior-access wording and must accompany the export |

The archival receipt labels the mirror
`post_evaluation_archival_mirror_not_refactored_metric_producer`. Every result claim must cite the verified export and mandatory erratum together.

## Required limitation language

Use the following statements without strengthening them:

- At least two broad default pytest executions before the terminal model evaluation parsed, statted, and hashed the live split files; the exact count may be higher.
- One focused post-evaluation regression rerun also reread the live split files.
- No terminal model-evaluation retry occurred.
- The test outcome was not used for tuning, model selection, thresholding, retraining, or dataset repair.
- The automated test accesses described by the erratum displayed no row content to a human, performed no model inference, used no external service, and did not influence the terminal result.

The erratum's corrected claim is:

> Phase 41 contains exactly one terminal shared-cohort model-evaluation pass over the frozen Qwen QLoRA and PhoBERT models. It does not have zero prior filesystem access to the held-out file.

## Prohibited claims

- Do not attribute the frozen result to the current refactored worktree.
- Do not describe prior access as absent; use the exact limitation language above.
- Do not turn a terminal result into permission for replay, tuning, repair, model
  selection, threshold changes, or a new unbiased claim after deployment fitting.
- Do not infer statistical significance, stable superiority, or any metric not
  present in the verified export.
- Do not present storage candidates as approved actions.
- Do not describe this handoff or the architecture pages as commentary written by
  the student.

## Source citations

- Active architecture: `architecture/module-boundaries.json` and
  `docs/architecture/overview.md`.
- CLI behavior: `tests/architecture/fixtures/runtime_cli_contract.json`,
  `tests/architecture/fixtures/model_cli_contract.json`, and
  `docs/architecture/cli-contracts.md`.
- Historical source:
  `historical/phase41-source-closure/c3bbc8c8adaf7579fd2eb9c59a0081613be4b2cae05dfdb64472938c7e6d0434/archival-receipt.json`.
- Verified export:
  `data/models/phase41/verified-export/9ac54d58c273ab0a8c2f2b4b61e472a51ca94231a94b6847637ecad6ceee49f7/evidence-manifest.json`.
- Mandatory companion: `data/models/phase41/phase41-provenance-erratum.json`.
- Storage classification:
  `.planning/phases/41.1-codebase-architecture-overhaul/41.1-STORAGE-INVENTORY.md`
  and `docs/architecture/storage-retention.md`.

## Later-phase ownership

Phase 42 may use these facts to rewrite the report, but its separate real
passed-student reference-report gate remains unresolved and must not be inferred
from this handoff. Phase 44—not this handoff—owns student-written comments and the guided defense walkthrough.
