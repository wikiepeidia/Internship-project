---
phase: 41.1-codebase-architecture-overhaul
plan: 05
subsystem: data-architecture
tags: [data-core, workflows, migrations, compatibility, import-boundaries, tdd]
requires:
  - phase: 41.1-04
    provides: phase-neutral modeling services and the closed machine-readable module policy
provides:
  - phase-neutral record, text, group-split, and versioning core with explicit old-path shims
  - lazy generation, judging, salvage, and recovery workflows behind the unchanged 16-option CLI
  - fixed five-route migration catalog and closed data-module ownership classification
affects: [41.1-06, phase-42-report, data-pipeline, architecture-policy]
actuals:
  tokens: 32995
  tasks: 3
  commits: 8
tech-stack:
  added: []
  patterns:
    - dependency-light deterministic core with explicit compatibility imports
    - handler-local workflow dependencies behind a lazy CLI boundary
    - immutable literal migration catalog with fail-before-import lookup
    - closed role classification for every data-pipeline module
key-files:
  created:
    - src/data_pipeline/core/__init__.py
    - src/data_pipeline/core/records.py
    - src/data_pipeline/core/splits.py
    - src/data_pipeline/core/text.py
    - src/data_pipeline/workflows.py
    - src/data_pipeline/migrations.py
    - tests/architecture/test_data_core_contract.py
    - tests/architecture/test_data_cli_contract.py
    - tests/architecture/test_data_migration_shims.py
  modified:
    - architecture/module-boundaries.json
    - src/data_pipeline/cli.py
    - src/data_pipeline/schemas.py
    - src/data_pipeline/processing/dedup.py
    - src/data_pipeline/processing/normalizer.py
    - src/data_pipeline/processing/splitter.py
    - src/data_pipeline/versioning/manifest.py
    - tests/architecture/test_import_boundaries.py
key-decisions:
  - "Make build_training_corpus the active domain API while retaining run_phase1 only as an explicit legacy CLI compatibility seam."
  - "Keep records, normalization, lexical deduplication, grouped splitting, and manifest construction dependency-light; old public modules re-export explicit named symbols."
  - "Represent each preserved repair as a literal immutable migration route and reject unknown identifiers before any repair module import."
  - "Classify all data-pipeline modules exactly once as core, compatibility, workflow, or migration ownership."
patterns-established:
  - "Data core: deterministic functions and records do not import providers, scrapers, judges, CLIs, models, or migrations."
  - "Legacy compatibility: old paths remain thin named forwards while canonical active owners use domain language."
  - "Migration selection: fixed ID lookup precedes the sole selected import and validates the exact callable symbol."
requirements-completed: [REFACTOR-04, REFACTOR-05]
coverage:
  - id: D1
    description: "Old and new record/core imports preserve serialized bytes, validators, Unicode normalization, deduplication, grouped splitting, and manifest behavior."
    requirement: REFACTOR-04
    verification:
      - kind: integration
        ref: "tests/architecture/test_data_core_contract.py"
        status: pass
    human_judgment: false
  - id: D2
    description: "The 16-option CLI preserves grammar, precedence, outputs, exits, and monkeypatch seams while help and core imports stay optional-dependency-free."
    requirement: REFACTOR-04
    verification:
      - kind: integration
        ref: "tests/architecture/test_data_cli_contract.py"
        status: pass
    human_judgment: false
  - id: D3
    description: "Five preserved repairs have exact lazy routes, byte-identical old modules, fail-closed unknown lookup, and isolated ownership in the closed import policy."
    requirement: REFACTOR-05
    verification:
      - kind: integration
        ref: "tests/architecture/test_data_migration_shims.py"
        status: pass
      - kind: integration
        ref: "tests/architecture/test_import_boundaries.py"
        status: pass
      - kind: integration
        ref: "tests/architecture/test_provenance_boundary.py#test_protected_authorities_match_fixed_baseline"
        status: pass
    human_judgment: false
duration: 25min
completed: 2026-08-27
status: complete
---

# Phase 41.1 Plan 05: Phase-Neutral Data Core, Workflows, and Migrations Summary

**Reusable data contracts and transforms now have a dependency-light domain core, external generation/review work has a lazy workflow owner, and five frozen repair tools are auditable migrations behind fixed literal routes.**

## Performance

- **Duration:** 25 min
- **Started:** 2026-08-27T02:30:07Z
- **Completed:** 2026-08-27T02:54:53Z
- **Tasks:** 3
- **Files modified:** 17
- **Realized diff:** 131,978 characters / 32,995 estimate tokens

## Accomplishments

- Moved dataset/seed schemas into `core.records` while preserving every observed old `schemas` symbol, seven-field JSON order, validation result, enum, default, and Vietnamese Unicode behavior through explicit imports.
- Separated normalization, lexical deduplication, grouped splitting, and manifest construction from provider/review orchestration; processing and versioning modules are now explicit compatibility surfaces.
- Reduced the CLI to its exact 16-option parser, compatibility seams, lazy dispatch, progress/error boundary, and unchanged JSON/exit behavior without importing providers, judges, scrapers, embeddings, or migrations for help.
- Made `build_training_corpus` the canonical active workflow API and retained the chronological `run_phase1` name only in the legacy CLI compatibility surface.
- Added an immutable five-ID migration catalog whose lookup fails before import, whose selected route imports only its literal old module and `main`, and whose frozen repair files remain byte-identical.
- Classified all 41 `src.data_pipeline` modules exactly once across core, compatibility, workflow, and migration roles, with no active runtime/modeling import into migrations.

## Task Commits

Each task retained separate RED and GREEN gates, followed by one contract-driven neutral-name refactor:

1. **Task 1: Trace one record through the phase-neutral core and old-path shim**
   - `272937e` — RED: pin data record contract
   - `ccd02c9` — GREEN: extract data record core
2. **Task 2: Separate pure transforms from generation and review workflows**
   - `9989a12` — RED: pin data workflow boundaries
   - `286bfc9` — GREEN: separate data workflows
3. **Task 3: Classify one-off repair tools as lazy migrations**
   - `b1ad754` — RED: pin migration catalog
   - `ba50788` — GREEN: catalog data migrations
4. **Final active-name contract audit**
   - `879d870` — REFACTOR: neutralize workflow API while preserving the legacy CLI seam

## Test Results

- Task 1 exact guarded RED/GREEN gate: `8 passed`; the autonomous tracer feedback gate reran the same selection and passed before expansion.
- Task 2 exact guarded gate: `21 passed, 2 warnings`; the post-refactor rerun also passed all 21 checks.
- Task 3 RED: `9 failed, 7 passed` for the intentionally absent catalog/classification; GREEN: `16 passed in 7.36s`.
- Final authorized combined gate: `36 passed, 2 warnings in 17.54s`.
- Exact final selection: `tests/architecture/test_data_core_contract.py`, `tests/architecture/test_data_cli_contract.py`, `tests/architecture/test_data_migration_shims.py`, `tests/architecture/test_import_boundaries.py`, and `tests/architecture/test_provenance_boundary.py::test_protected_authorities_match_fixed_baseline`, with the pre-collection deny-open sentinel and temporary data/model/registry roots.
- The two warnings are third-party SWIG built-in deprecations emitted by the existing optional embedding stack; they do not affect the guarded contract results.
- No repair workflow, provider, crawler, training/inference path, model loader, project corpus, ProgramData evidence, D-drive root, or reserved split path/directory was accessed or executed.

## Files Created/Modified

- `src/data_pipeline/core/` — canonical record, text, and group-safe split/versioning ownership.
- `src/data_pipeline/workflows.py` — lazy generation, judge, salvage, recovery, and corpus-build orchestration.
- `src/data_pipeline/migrations.py` — frozen specifications, lookup, and selected-entrypoint loading for five preserved repairs.
- `src/data_pipeline/cli.py` — thin legacy parser/dispatch and monkeypatch compatibility seam.
- `src/data_pipeline/schemas.py`, `processing/*.py`, and `versioning/manifest.py` — explicit named compatibility forwards.
- `architecture/module-boundaries.json` — closed four-role data-module classification.
- `tests/architecture/test_data_*` and `test_import_boundaries.py` — synthetic/temp-only compatibility, lazy-import, frozen-byte, and graph enforcement.

## Decisions Made

- Domain ownership takes precedence over chronological naming: `build_training_corpus` is active, while `cli.run_phase1` remains only because the old CLI/module contract requires it.
- Core modules may depend on deterministic records/settings-free inputs, but never on providers, scrapers, judges, CLIs, migrations, model code, or default live paths.
- Migration callers choose from five immutable identifiers; arbitrary module or symbol strings are never accepted.
- The narrator-extraction repair remains preserved but is explicitly marked superseded by direct reconstruction rather than presented as current authority.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Contract] Kept chronological naming out of the active workflow API**
- **Found during:** Final plan contract audit after Task 3
- **Issue:** The task-level action named `workflows.run_phase1`, but the phase contract permits phase-number names only in explicit legacy/migration compatibility surfaces.
- **Fix:** Renamed the canonical workflow to `build_training_corpus`, kept `cli.run_phase1` as the exact legacy forward, and added an architecture assertion that the active workflow does not expose the chronological name.
- **Files modified:** `src/data_pipeline/workflows.py`, `src/data_pipeline/cli.py`, `tests/architecture/test_data_cli_contract.py`
- **Commit:** `879d870`

## Authentication Gates

None.

## Known Stubs

None. Empty collections and nullable values found by the required scan are bounded algorithm accumulators, schema defaults, optional dependency sentinels, or test fixtures; none flows to a UI or substitutes for required behavior.

## Threat Review

- The fixed lookup rejects unknown IDs before `importlib` and never accepts caller-supplied module or symbol routes.
- Provider/scraper/judge imports remain handler-local and were exercised only through synthetic doubles.
- The five preserved repair modules retain their exact Git blob identities and were statically inspected, never imported or executed.
- No security-relevant surface outside the plan threat model was introduced, so there are no additional threat flags.

## Next Phase Readiness

- Plan 41.1-06 can consume the closed data/model/runtime ownership policy for report-ready architecture mapping.
- REFACTOR-04 now has exact old/new compatibility and migration provenance evidence; REFACTOR-05 retains the reserved-boundary sentinel in every authorized gate.

## Self-Check: PASSED

All created files and all seven task/refactor commits were found; the final exact guarded gate passed and the summary has no whitespace errors.
