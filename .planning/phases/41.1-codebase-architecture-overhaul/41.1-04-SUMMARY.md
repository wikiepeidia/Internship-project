---
phase: 41.1-codebase-architecture-overhaul
plan: 04
subsystem: modeling-architecture
tags: [modeling, provenance, pydantic, import-graph, legacy-adapters, tdd]
requires:
  - phase: 41.1-03
    provides: phase-neutral integrity/artifact readers, secret-free runtime ownership, and protected-authority baseline
provides:
  - runtime-consumed phase-neutral inference and dependency-injected training services
  - strict immutable two-model evaluation facts and raw-byte reporting authority reader
  - closed active/adapter/historical classification with exact SCC and reachability enforcement
affects: [41.1-05, 41.1-06, phase-42-report, runtime, model-adaptation]
actuals:
  tokens: 21684
  tasks: 3
  commits: 7
tech-stack:
  added: []
  patterns:
    - phase-neutral protocol/service boundary with handler-local legacy imports
    - raw-byte hash-linked immutable evidence consumption
    - closed AST import policy with explicit adapter edges and historical SCCs
key-files:
  created:
    - src/modeling/evaluation.py
    - src/modeling/evidence.py
    - architecture/module-boundaries.json
    - tests/architecture/test_import_boundaries.py
    - tests/architecture/test_modeling_evidence.py
  modified:
    - src/runtime/service.py
    - src/model_adaptation/commands/adaptation.py
    - src/modeling/training.py
    - src/modeling/inference.py
    - src/modeling/legacy_adapters.py
key-decisions:
  - "Keep active public class, function, and module names phase-neutral; retain phase-numbered schema strings and module paths only as historical authority identities."
  - "Bind reporting facts to the exact raw export, result, source-manifest, materialization-receipt, and erratum bytes without opening predictions or exposing an execution/write API."
  - "Admit historical cycles only through three exact legacy-only SCC declarations and permit cross-boundary imports only from named compatibility adapters."
patterns-established:
  - "Active-to-history bridge: services depend on protocols while fixed legacy imports occur only inside named adapters."
  - "Reporting authority: validate five fixed JSON members from captured bytes once, preserve those bytes, and fail on any broken hash or provenance link."
  - "Closed graph: every runtime/core/modeling/model-adaptation module is explicitly classified; new modules cannot enter by glob or reverse-edge exception."
requirements-completed: [REFACTOR-01, REFACTOR-03, REFACTOR-05]
coverage:
  - id: D1
    description: "RuntimeService traverses the phase-neutral inference service while Qwen and PhoBERT training remain lazy and dependency-injected."
    requirement: REFACTOR-03
    verification:
      - kind: integration
        ref: "tests/architecture/test_modeling_boundaries.py"
        status: pass
    human_judgment: false
  - id: D2
    description: "Frozen two-model facts are consumed through a strict raw-byte reporting authority with mandatory erratum and no prediction/model/split execution surface."
    requirement: REFACTOR-01
    verification:
      - kind: integration
        ref: "tests/architecture/test_modeling_evidence.py"
        status: pass
      - kind: integration
        ref: "tests/architecture/test_provenance_boundary.py#test_protected_authorities_match_fixed_baseline"
        status: pass
    human_judgment: false
  - id: D3
    description: "The active graph is acyclic and every historical edge, SCC, ownership index, and forbidden back path is enforced statically."
    requirement: REFACTOR-05
    verification:
      - kind: integration
        ref: "tests/architecture/test_import_boundaries.py"
        status: pass
      - kind: integration
        ref: "tests/architecture/test_model_cli_contract.py"
        status: pass
    human_judgment: false
duration: 34min
completed: 2026-08-27
status: complete
---

# Phase 41.1 Plan 04: Phase-Neutral Modeling and Legacy Boundary Summary

**Runtime-consumed inference, lazy Qwen/PhoBERT training, immutable hash-linked evaluation facts, and a closed acyclic import policy now separate the active modeling domain from frozen historical machinery.**

## Performance

- **Duration:** 34 min
- **Started:** 2026-08-27T01:49:11Z
- **Completed:** 2026-08-27T02:22:43Z
- **Tasks:** 3
- **Files modified:** 14
- **Realized diff:** 86,736 characters / 21,684 estimate tokens

## Accomplishments

- Routed the existing `RuntimeService` contract through `InferenceService`/`InferenceBackend` and added dependency-injected Qwen/PhoBERT training services whose historical imports remain handler-local.
- Added strict frozen evaluation contracts for exact model roles, labels, confusion shape, support/count/rate consistency, checkpoint identity, terminal policy, and comparison statements.
- Added a read-only reporting authority that reads only five fixed JSON files, preserves their captured bytes, binds every required manifest/result/source/materialization/erratum link, and never opens predictions, model roots, or split paths.
- Declared 33 historical implementation modules, 13 named compatibility adapters, 22 active modules, 27 exact adapter-to-history edges, and the three reviewed legacy-only SCCs in a closed machine-readable policy.
- Routed the compatibility training command through the phase-neutral training service without changing the frozen 23-command parser/handler contract.

## Task Commits

Each TDD task retained separate RED and GREEN gates; the evidence task also received one audited correctness repair:

1. **Task 1: Trace one phase-neutral modeling service through a lazy legacy bridge**
   - `32d3e70` — RED: pin modeling service seam
   - `d5e71aa` — GREEN: add modeling service bridge
2. **Task 2: Add evaluation contracts and immutable evidence consumption**
   - `8589418` — RED: pin modeling evidence
   - `61dde7c` — GREEN: add evidence authority
   - `211b8ec` — FIX: close evidence hash chain after independent audit
3. **Task 3: Declare legacy ownership and enforce an acyclic active graph**
   - `333a2f8` — RED: pin import boundaries
   - `a98cb9c` — GREEN: close modeling graph

## Test Results

- Task 1 exact guarded tracer: `10 passed`; its human-verification checkpoint was approved before continuation.
- Task 2 RED: expected collection failure because `src.modeling.evaluation` did not yet exist.
- Task 2 initial GREEN: `14 passed`; strengthened post-audit gate: `18 passed in 4.44s`.
- Task 3 RED: `5 failed, 32 passed`; GREEN: `37 passed in 7.78s`.
- Final authorized combined gate: `37 passed in 6.87s`.
- Exact final selection: `tests/architecture/test_modeling_boundaries.py`, `tests/architecture/test_modeling_evidence.py`, `tests/architecture/test_import_boundaries.py`, `tests/architecture/test_model_cli_contract.py`, and `tests/architecture/test_provenance_boundary.py::test_protected_authorities_match_fixed_baseline`, with the pre-collection sentinel and temporary data/model/registry roots.
- No Phase 41 evaluator, training/inference/provider command, real model loader, ProgramData evidence, D-drive root, prediction file, or reserved split/directory was accessed.

## Files Created/Modified

- `src/modeling/training.py` and `src/modeling/inference.py` — dependency-light protocols/services with single-boundary error translation.
- `src/modeling/legacy_adapters.py` — fixed lazy Qwen/PhoBERT forwards into retained implementations.
- `src/modeling/evaluation.py` — strict immutable two-model result contracts only; no evaluator or runner.
- `src/modeling/evidence.py` — explicit-path reporting reader with raw bytes, hashes, typed facts, and mandatory disclosure.
- `src/runtime/service.py` — preserved public runtime API delegating through the neutral inference port.
- `src/model_adaptation/commands/adaptation.py` — compatibility training wrappers routed through `qwen_training_service()`.
- `src/model_adaptation/legacy/phase40/__init__.py` and `phase41/__init__.py` — import-free exact ownership indexes.
- `architecture/module-boundaries.json` — closed active/adapter/historical classes, allowed edges, SCCs, and forbidden targets.
- `tests/architecture/test_modeling_boundaries.py`, `test_modeling_evidence.py`, and `test_import_boundaries.py` — guarded synthetic runtime, evidence, API, graph, SCC, and reachability proofs.

## Decisions Made

- Used phase-neutral `ReportingAuthority` rather than introducing a new active phase-numbered type; frozen Phase 41 schema/version values remain exact evidence facts.
- Preserved and validated raw bytes directly instead of parsing through a path helper and rereading for hashes, so typed facts and returned identities bind the same captured bytes.
- Classified old phase-neutral model-adaptation implementations that still import historical code as named compatibility adapters rather than pretending they are clean active domain services.
- Kept the three existing SCCs byte-preserved and reviewable; no reverse-edge allowlist exists, and every historical path to active artifacts/modeling is a hard failure.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical Functionality] Closed all frozen evidence cross-links after independent audit**
- **Found during:** Task 2 post-GREEN audit
- **Issue:** The initial reader verified the three opened members and erratum, but did not yet bind the export-directory identity, unopened request/authorization/claim/prediction hashes, or the full source/materialization environment links.
- **Fix:** Added exact manifest-to-result links without opening those members, export-basename binding, launcher/runtime/source consistency, risky-count and checkpoint-identity validation, and typed erratum disclosure facts.
- **Files modified:** `src/modeling/evaluation.py`, `src/modeling/evidence.py`, `tests/architecture/test_modeling_evidence.py`
- **Verification:** Strengthened guarded evidence gate passed `18/18`; final combined gate passed `37/37`.
- **Committed in:** `211b8ec`

**2. [Rule 3 - Blocking] Removed a OneDrive rename race from synthetic authority tests**
- **Found during:** Task 2 post-GREEN audit RED run
- **Issue:** Renaming a freshly written synthetic directory intermittently returned Windows `Access is denied` under the OneDrive workspace.
- **Fix:** Constructed hash-named synthetic authority directories directly and rebuilt exact fixed members when rebinding a manifest, avoiding directory rename without weakening the production reader.
- **Files modified:** `tests/architecture/test_modeling_evidence.py`
- **Verification:** Repeated exact evidence and final combined gates passed.
- **Committed in:** `211b8ec`

---

**Total deviations:** 2 auto-fixed (1 missing critical functionality, 1 blocking test-fixture issue)
**Impact on plan:** Both fixes strengthened provenance and deterministic verification; neither changed frozen authorities or historical implementations.

## Authentication Gates

None.

## Known Stubs

None. The modified-file scan found only intentional optional dependency-injection defaults, local validation accumulators, and synthetic test collections; no unwired UI/data placeholder, TODO, FIXME, skipped test, or execution stub remains.

## Issues Encountered

- The tracer checkpoint was already approved when this continuation began; both checkpoint commits were verified at the exact continuation HEAD and were not redone.
- The evidence audit intentionally reopened the Task 2 gate after the first GREEN result and produced the documented Rule 2/Rule 3 repair before graph work began.
- The state SDK counted 59 completed plans but retained the prior activity text, a phase-based 13% value, and a pending REFACTOR-03 traceability row; the canonical planning metadata was normalized to 59/87 (68%) and all three completed requirements.

## User Setup Required

None - no dependency installation, credential, provider, external service, model operation, or environment change is required.

## Next Phase Readiness

- Phase 41.1-05 can separate data core/workflows/migrations against a proven closed import-policy pattern.
- Phase 41.1-06 and Phase 42 can consume typed frozen metrics plus the mandatory erratum without importing or representing refactored code as the metric producer.
- Historical training/evaluation machinery and all protected authority/model roots remain unchanged.

## Self-Check: PASSED

All 12 declared summary/source/test artifacts exist, all seven RED/GREEN/fix task commits resolve, and the closed policy contains the reported 22 active modules, 13 compatibility adapters, 33 historical modules, 27 allowed edges, and three historical SCCs.

---
*Phase: 41.1-codebase-architecture-overhaul*
*Completed: 2026-08-27*
