---
phase: 41.1-codebase-architecture-overhaul
verified: 2026-08-27T07:24:13Z
status: gaps_found
score: 8/12 must-haves verified
behavior_unverified: 0
overrides_applied: 0
decision_coverage:
  honored: 8
  total: 8
  not_honored: []
gaps:
  - truth: "The active codebase uses phase-neutral language outside explicit compatibility and historical boundaries."
    status: failed
    reason: "Active runtime modules still contain Phase 2/3/4/6/31 chronology, including user-visible setup guidance. The policy regex and AST-only test miss whitespace-separated phase labels, strings, comments, and JavaScript."
    artifacts:
      - path: "src/runtime/analyzers/accelerated.py"
        issue: "ACCELERATED_SETUP_GUIDE tells users to run the Phase 3 training flow."
      - path: "src/runtime/analyzers/gguf.py"
        issue: "GGUF_SETUP_GUIDE tells users to run the Phase 3 conversion flow."
      - path: "src/runtime"
        issue: "Active Python docstrings and demo JavaScript retain Phase 2/3/4/6/31 labels."
      - path: "tests/architecture/test_architecture_policy.py"
        issue: "The naming gate scans only selected Python AST identifiers with pattern phase[0-9]+."
    missing:
      - "Replace active runtime chronology with domain wording while retaining immutable schema, artifact, path, and compatibility identifiers."
      - "Expand the active-name gate to cover whitespace-separated phase labels, strings/comments, and active non-Python assets, with an explicit immutable-identity allowlist."
  - truth: "The machine-readable architecture is a closed inventory of the live active dependency graph."
    status: failed
    reason: "src.config and src.config.settings are live runtime dependencies but are absent from the policy and architecture table; the supposedly closed tests exclude src.config from their universe."
    artifacts:
      - path: "architecture/module-boundaries.json"
        issue: "active_modules omits src.config and src.config.settings."
      - path: "tests/architecture/test_architecture_policy.py"
        issue: "The relevant-module predicate covers artifacts/core/data/modeling/runtime/model_adaptation but not config."
      - path: "docs/architecture/overview.md"
        issue: "The deliberately exhaustive active table omits the live config modules."
    missing:
      - "Classify src.config and src.config.settings, document their ownership, and include their real edges in closed graph tests."
      - "Make the closed-universe assertion cover every live source dependency or explicitly and truthfully declare a complete excluded category."
  - truth: "New active modules and tools satisfy the 250/600/100-line budgets or have an exact documented exception."
    status: failed
    reason: "scripts/archive_phase41_source_closure.py is 886 lines although Plan 01 capped it at 600 lines unless the summary recorded an exact compatibility exception; no exception is recorded and policy budget_exceptions is empty."
    artifacts:
      - path: "scripts/archive_phase41_source_closure.py"
        issue: "886 physical lines; omitted from the enforced budgeted_modules set."
      - path: "architecture/module-boundaries.json"
        issue: "The budget gate does not include the new archival tool and declares no exception."
    missing:
      - "Split the archival tool into cohesive domain-named modules under the existing behavior/provenance tests, or record and enforce an exact justified exception."
  - truth: "The Phase 41.1 report handoff is factually accurate and internally consistent with the architecture authority."
    status: failed
    reason: "41.1-REPORT-HANDOFF.md says there are three legacy-only cycles, while module-boundaries.json contains four historical SCC entries and overview.md correctly documents four. The handoff test does not cross-check this count."
    artifacts:
      - path: ".planning/phases/41.1-codebase-architecture-overhaul/41.1-REPORT-HANDOFF.md"
        issue: "Historical-boundary row states three legacy-only cycles."
      - path: "tests/architecture/test_report_handoff.py"
        issue: "Required-statement checks omit architecture-policy SCC consistency."
    missing:
      - "Correct the handoff to four reviewed legacy-only cycles and make the handoff test derive/check the fact from module-boundaries.json."
---

# Phase 41.1: Codebase Architecture Overhaul Verification Report

**Phase Goal:** The active codebase has a coherent, phase-neutral architecture that is easy to operate and explain: thin stable CLIs, separated data/training/inference/evaluation responsibilities, archived one-off migrations, and an explicit immutable boundary around the exact source and model artifacts that produced Phase 40/41 evidence.

**Verified:** 2026-08-27T07:24:13Z  
**Status:** GAPS FOUND  
**Re-verification:** No — initial verification

## Verdict

The implementation has strong compatibility, provenance, isolation, and synthetic-test foundations, but the phase goal is not yet fully achieved. Four observable defects remain: active phase-number wording, an incomplete supposedly closed module inventory, an unenforced new-tool size budget, and a factually inconsistent report handoff. Green architecture tests do not override these directly observed gaps.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---:|---|---|---|
| 1 | Exact producer closure, verified export, mandatory erratum, schema/artifact identities, and sealed model-root guidance remain provenance-bound | VERIFIED | The guarded authority tests passed; `src/modeling/evidence.py:60-64` pins source/export/erratum identities; the archive verifier reproduced the fixed 37-source/38-payload closure twice. |
| 2 | Installed `vnphish analyze`, `doctor`, and `demo` interface remains stable | VERIFIED | `pyproject.toml:56` still points to `src.runtime.cli:main`; parser/main characterization passed in the bounded suite. |
| 3 | All 23 model-adaptation commands retain compatible parser/exit/output behavior through a thin lazy dispatcher | VERIFIED | `src/model_adaptation/cli.py` is 232 lines; `commands/router.py:19-46` contains a closed 23-route literal table; model CLI contract tests passed. |
| 4 | Machine-readable active dependency classification is exhaustive and closed | FAILED | `src.config.settings` is imported by runtime (`service.py:5`, `doctor.py:11-12`, `accelerated.py:17`, `gguf.py:16`) but omitted from the policy and from the closed-test universe. |
| 5 | The installed runtime delegates through the neutral inference service and does not load training/provider implementation graphs | VERIFIED | `src/runtime/service.py:8,84-92` constructs and uses `InferenceService`; runtime/import-boundary tests passed under blocked optional imports. |
| 6 | Integrity, modeling, evaluation-contract, and read-only evidence boundaries are substantive and one-way | VERIFIED | Neutral services are dependency-injected; historical imports occur only in adapters; `evidence.py:29-48` verifies all evidence members but exposes only four report-safe members. |
| 7 | Reusable data core, external workflows, compatibility shims, and five one-off migrations are separated and wired | VERIFIED | Closed data classification, exact migration catalog, old-path shims, record/split/text behavior, and CLI dispatch passed the bounded suite. |
| 8 | Active code uses phase-neutral domain language outside quarantined compatibility/provenance surfaces | FAILED | Active runtime docstrings, two user-visible setup guides, and `demo.js` retain Phase 2/3/4/6/31 wording; the current gate cannot see these forms. |
| 9 | Compatibility/provenance checks are synthetic or fixed-authority only and do not access/rerun the held-out evaluation | VERIFIED | Exact 17-file architecture run passed 312 tests with the pre-collection deny-open sentinel and isolated temp roots. |
| 10 | New active code/tools obey the declared 250/600/100-line budgets or an exact exception | FAILED | The new archive utility is 886 lines; Plan 01 required at most 600 unless its summary recorded an exact exception, but none exists. |
| 11 | Architecture provenance and storage guidance preserve authority semantics and authorize no deletion | VERIFIED | `docs/architecture/provenance.md` binds export+erratum and denies refactored producer status; storage documentation retains four sealed roots (10.170 GiB), treats the 3.986 GiB GGUF as optional, and contains no deletion command. |
| 12 | The report handoff is accurate and cross-consistent with current architecture | FAILED | Handoff line 15 says three legacy-only cycles; policy lines 175-198 records four and overview lines 212-223 agrees with four. |

**Score:** 8/12 truths verified (0 present-but-behavior-unverified)

## Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `historical/phase41-source-closure/<tree-sha>/` and archive receipt | Exact post-evaluation producer closure mirror | VERIFIED | Independent `verify` mode passed twice with identical 37-source, 38-payload, launcher, manifest, receipt, and provenance-label identities. |
| `scripts/archive_phase41_source_closure.py` | Safe bounded archive/verify utility | PARTIAL | Behavior and provenance checks pass, but the 886-line module violates its explicit Plan 01 budget without an exception. |
| `src/model_adaptation/cli.py` plus `commands/*` | Thin stable compatibility CLI | VERIFIED | 232-line facade, closed literal routes, handler-local lazy imports, exact parser/main fixture parity. |
| `src/core*`, `src/artifacts.py`, runtime settings/readers | Neutral integrity/artifact/runtime boundary | VERIFIED | Strict readers and runtime dependency split are substantive and covered by tamper/strictness tests. |
| `src/modeling/*` | Neutral training/inference/evaluation/evidence services | VERIFIED | Runtime consumes inference; training uses adapters; evaluation exposes contracts, not a runner; evidence is read-only. |
| `src/data_pipeline/core`, `workflows.py`, `migrations.py` and shims | Domain-separated data architecture | VERIFIED | Exact behavioral, shim, migration-catalog, strict-record, split, publication, and recovery tests passed. |
| `architecture/module-boundaries.json` | Closed architecture policy | PARTIAL | Substantive for the enumerated graph but omits live `src.config` modules. |
| `docs/architecture/*` | Defense-readable architecture/provenance/storage guidance | PARTIAL | The 1–10 overview, provenance, CLI, and storage docs are useful; the claimed exhaustive module table omits config. |
| `41.1-REPORT-HANDOFF.md` | Accurate downstream report authority | FAILED | Historical SCC count is stale/inaccurate. |

## Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `pyproject.toml` | `src.runtime.cli:main` | Installed script entry | WIRED | Exact entry point retained. |
| `model_adaptation/cli.py` | command families | Closed router table | WIRED | All 23 fixture commands map to fixed module/symbol pairs. |
| `runtime/service.py` | `modeling/inference.py` | `InferenceService` | WIRED | Synthetic end-to-end runtime test traverses this seam. |
| `modeling/training.py` | `modeling/legacy_adapters.py` | Injected adapter | WIRED | Lazy historical imports remain behind the named bridge. |
| `modeling/evidence.py` | export plus erratum | Pinned hashes and strict read-only validation | WIRED | All 12 export members are verified; prediction bytes are not returned. |
| `data_pipeline/cli.py` | workflows/migrations | Lazy dispatch and compatibility shims | WIRED | Core, workflow, recovery, publication, and migration tests pass. |
| `module-boundaries.json` | full active runtime graph | Closed classification test | NOT FULLY WIRED | Test predicate excludes live config dependency nodes. |
| `41.1-REPORT-HANDOFF.md` | architecture policy | Documentation consistency test | NOT WIRED | SCC count is hand-written and not checked against policy. |

## Data-Flow Trace

| Flow | Source | Consumer | Status |
|---|---|---|---|
| Installed analysis | `vnphish` argv | runtime CLI → `RuntimeService` → `InferenceService` → injected backend | FLOWING (synthetic test) |
| Legacy model command | fixed CLI command | literal router → focused command family → lazy historical implementation | FLOWING (contract tests) |
| Data operation | explicit input path/record | workflow/core/migration service → compatibility result | FLOWING (synthetic tests) |
| Report facts | pinned export manifest + erratum | strict evidence validator → typed facts + four approved raw members | FLOWING (synthetic authority tests) |
| Architecture inventory | Python modules/imports | policy → docs/tests | INCOMPLETE (`src.config` omitted) |

## Behavioral Spot-Checks

| Behavior | Command / evidence | Result | Status |
|---|---|---|---|
| Exact architecture suite | Exact 17 named `tests/architecture/test_*.py` files with `PHASE411_DENY_OPEN_SENTINEL=1`, isolated `DATA_DIR`, `MODEL_ARTIFACT_ROOT`, `MODEL_REGISTRY_PATH`, and unique pre-created `--basetemp` | `312 passed, 2 warnings in 32.20s`; no skips | PASS |
| Historical closure verifier | `python scripts/archive_phase41_source_closure.py verify` under the same guard/temp-root contract | Identical fixed hashes/counts on two runs | PASS |
| Context decision coverage | `gsd-tools query check.decision-coverage-verify ...` | 8/8 trackable decisions honored | PASS (soft heuristic) |
| Active-name coverage | Static scan of `src/runtime` for `phase\s*[0-9]+` | Multiple active/user-visible matches outside legacy boundary | FAIL |
| Closed classification | Policy union compared with live runtime imports | `src.config` and `src.config.settings` absent | FAIL |
| Handoff consistency | Handoff SCC claim compared with policy | 3 claimed vs 4 declared | FAIL |

The first test invocation used a unique `--basetemp` whose parent had not yet been created; the hardened guard rejected recursive parent creation during fixture setup, so no test body ran. After creating only that isolated temporary parent, the same exact bounded selection completed 312/312. This was a verifier-harness setup issue, not product evidence or a protected-path access.

## Probe Execution

No `probe-*.sh` file is declared for this phase. The phase-declared production archive verifier was run independently twice and passed both times.

## Requirements Coverage

| Requirement | Status | Evidence |
|---|---|---|
| REFACTOR-01 | SATISFIED | Exact source closure and launcher identities verify; frozen export/erratum pins and producer denial are intact; storage guidance preserves four model roots. |
| REFACTOR-02 | SATISFIED | Thin 232-line CLI, exact 23-command route set, lazy imports, frozen parser/main behavior. |
| REFACTOR-03 | BLOCKED | Neutral services and enumerated DAG work, but the active architecture is not fully phase-neutral and the closed graph omits live config nodes. |
| REFACTOR-04 | SATISFIED | Core/workflow/migration separation, exact five-ID catalog, and legacy shims are implemented and tested. |
| REFACTOR-05 | SATISFIED | Synthetic-only characterization, CLI, import/cycle, strict artifact, and provenance tests pass under the deny-open sentinel; the naming/inventory blind spots are separately recorded gaps. |
| REFACTOR-06 | BLOCKED | Provenance/storage documents are safe, but the exhaustive architecture inventory is incomplete and the report handoff contains an incorrect SCC count. |

No additional Phase 41.1 requirements are orphaned from the six plans. Later Phases 42–44 do not clearly own these defects; they are therefore not deferred.

## Anti-Patterns and Residual Risks

| Location | Finding | Severity | Impact |
|---|---|---|---|
| `src/runtime/**` | Phase-number chronology in active docstrings, guidance strings, and JavaScript comment | BLOCKER | Contradicts the active phase-neutral goal; two occurrences are user-visible remediation. |
| `test_architecture_policy.py:167-182,281-309` | Closed/naming tests narrow the universe and token forms | BLOCKER | Green suite gives a false-positive architecture verdict. |
| `scripts/archive_phase41_source_closure.py` | 886-line new utility with no exact budget exception | BLOCKER | Violates the plan's bounded-slice contract and evades policy enforcement. |
| `41.1-REPORT-HANDOFF.md:15` | Three-cycle claim conflicts with four policy SCCs | BLOCKER | Unsafe to quote directly in the report/defense. |
| Root Phase 28/30/31/32 verification scripts | Historical phase-numbered scripts remain outside the source policy | WARNING | They appear historically referenced rather than forward-wired; classify/quarantine them explicitly instead of renaming blindly. |
| Exact test run | Two third-party SWIG built-in deprecation warnings | INFO | No Phase 41.1 assertion failed. |

No unreferenced `TBD`, `FIXME`, `XXX`, or `HACK` debt marker was found in the audited active/new surfaces. The `Phase 40 loss curves` title is in quarantined historical producer code (`src/model_adaptation/phase40_graphs.py`); do not rename that protected producer. If a phase-neutral report figure is needed, render a new presentation artifact from approved report-facing facts.

## Test-Quality Audit

The 312 passing cases contain real value/behavior assertions, including strict JSON rejection, tamper failure, route/argument/exit contracts, runtime service delegation, import isolation, graph reachability/SCC checks, path-bounded write behavior, and report/storage text contracts. No tests were skipped in the actual run. Stored contract fixtures are fixed repository baselines and are compared against two independent fresh captures; they are not produced and accepted within one assertion path.

The principal weakness is coverage scope, not assertion emptiness: the architecture tests define away `src.config`, and the naming test examines only selected Python identifiers. Those blind spots explain how the suite passes while goal-level truths fail.

## Protected-Boundary Statement

This verification did **not** enumerate, stat, hash, open, copy, or resolve `data/splits/test.jsonl` or its containing `data/splits` directory. It did not run evaluation, training, inference, providers, or model loading; did not access D-drive model roots; and did not mutate ProgramData, immutable evidence, historical closure, sealed exports, model roots, source files, tests, or documentation. Only exact named architecture tests, isolated temporary roots, the pre-collection deny-open sentinel, tracked synthetic/fixed-authority fixtures, and the archive verifier's read-only `verify` mode were used.

## Human Verification Required

None. The blocking gaps are directly observable in current source, policy, tests, and documentation; they do not require subjective UI or external-service judgment.

## Gaps Summary

Phase 41.1 should not advance as complete until the four structured gaps are closed and the exact bounded architecture suite is rerun. The fixes do not require touching frozen producer bytes, the held-out split, models, evaluation, or D-drive storage.

---

_Verified: 2026-08-27T07:24:13Z_  
_Verifier: Codex goal-backward verifier_
