---
phase: 41.1-codebase-architecture-overhaul
plan: 08
subsystem: architecture-testing
tags: [static-language, ast-fingerprint, import-guard, runtime-prose]
requires:
  - phase: 41.1-codebase-architecture-overhaul
    provides: Plan 07 exact text ownership and Plan 11 fail-closed startup guard
provides:
  - Import-free raw-text chronology scanner for active source, tools, assets, and documents
  - Immutable pre-edit behavior fingerprints for twelve runtime inputs
  - Domain-named installed CLI, typed contracts, and normalized runtime orchestration
affects: [41.1-12, 41.1-13, 41.1-14, 41.1-15, phase-42-report]
actuals:
  tokens: 6866
  tasks: 2
  commits: 3
tech-stack:
  added: []
  patterns: [raw-text-static-gate, explicit-prose-slot-masking, exact-path-exceptions]
key-files:
  created:
    - tests/architecture/test_active_language_policy.py
    - tests/architecture/fixtures/runtime_prose_contract.json
  modified:
    - src/runtime/cli.py
    - src/runtime/contracts.py
    - src/runtime/service.py
key-decisions:
  - "Mask only explicit AST prose slots while hashing every import, call, signature, control-flow node, and non-approved string."
  - "Treat pre_extraction_v1 marker ownership as an exact transition contract: absent future pairs are allowed, but any introduced endpoint must form one complete non-overlapping pair."
patterns-established:
  - "Language checks read UTF-8 text only and install a rejecting meta-path finder before any attempted application, provider, model, or heavy-dependency import."
  - "Chronology exceptions are exact path plus exact literal or exact balanced marker pair; no wildcard matching or whole-file quarantine exists."
requirements-completed: [REFACTOR-02, REFACTOR-03, REFACTOR-05]
coverage:
  - id: D1
    description: Import-free scanner rejects chronology in every supported active text language and scopes all exceptions exactly
    requirement: REFACTOR-03
    verification:
      - kind: unit
        ref: tests/architecture/test_active_language_policy.py::test_active_text_scanner_rejects_every_supported_source_language
        status: pass
      - kind: unit
        ref: tests/architecture/test_active_language_policy.py::test_active_text_allowlist_is_exact_and_marker_scoped
        status: pass
    human_judgment: false
  - id: D2
    description: Twelve runtime inputs retain immutable executable structure while the installed entry path uses domain language
    requirement: REFACTOR-05
    verification:
      - kind: unit
        ref: tests/architecture/test_active_language_policy.py::test_runtime_prose_behavior_fingerprints_match_characterization
        status: pass
      - kind: unit
        ref: tests/architecture/test_active_language_policy.py::test_active_runtime_entrypoint_language_is_domain_named
        status: pass
    human_judgment: false
duration: 9min
completed: 2026-08-27
status: complete
---

# Phase 41.1 Plan 08: Static Language and Runtime Prose Summary

**Import-free chronology enforcement and twelve immutable runtime fingerprints now protect domain-readable installed runtime descriptions.**

## Performance

- **Duration:** 9 min
- **Started:** 2026-08-27T13:08:10Z
- **Completed:** 2026-08-27T13:17:00Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- Added a policy-driven raw UTF-8 scanner for Python, batch, PowerShell, Bash, HTML, CSS, JavaScript, and Markdown without importing application code.
- Froze eleven masked Python AST hashes, one comment-stripped JavaScript hash, all twelve pre-edit source hashes, and the two setup-command literals.
- Replaced chronology-only module descriptions in the installed CLI, contracts, and service with concrete phishing-risk responsibilities while exact behavior fingerprints remained unchanged.
- Passed the combined guarded selection with 15/15 cases under isolated temporary data and model roots.

## Task Commits

1. **Task 1: Trace raw source text through a closed import-free language policy** - `5ef59ea`
2. **Task 2: Rewrite the installed CLI, contracts, and orchestration prose** - `3862ef4`

## Files Created/Modified

- `tests/architecture/test_active_language_policy.py` - Static scanner, exact exception matcher, import blocker, synthetic mutations, and runtime fingerprints.
- `tests/architecture/fixtures/runtime_prose_contract.json` - Independent pre-edit hashes, explicit prose slots, and protected setup commands.
- `src/runtime/cli.py` - Domain-readable installed command-interface description.
- `src/runtime/contracts.py` - Domain-readable typed-contract description.
- `src/runtime/service.py` - Domain-readable normalized-orchestration description.

## Decisions Made

- The runtime fixture masks only named module/function docstrings and two named setup-guidance assignments; all other Python strings and structure remain hash-bound.
- Binary font paths stay exact inventory members and are checked only for classification and presence; the scanner never decodes their bytes.
- Pre-extraction document markers may still be absent until their owning plans run. A partially inserted, duplicate, nested, reversed, or overlapping pair fails immediately.

## Deviations from Plan

The automated tracer was committed, re-run, and passed before Task 2. Execution continued because the parent explicitly requested complete autonomous execution and the tracer required no human judgment.

## Issues Encountered

- The first characterization run intentionally exposed placeholder hash fields, which were replaced with the captured pre-edit values before any runtime prose edit.
- Some future document markers are not installed yet. Validation was aligned with the fixture's explicit `pre_extraction_v1` state while retaining fail-closed behavior for any marker endpoint already present.

## Known Stubs

None. Empty lists in the test module are local recorders and accumulators, not product placeholders.

## Threat Flags

None. The plan adds no endpoint, model loading, provider access, process launch, data access, or new filesystem trust boundary.

## User Setup Required

None - no dependency, credential, external service, model, provider, or protected-data access is required.

## Next Phase Readiness

- Plans 12-15 can extend the shared scanner with their exact document, analyzer, renderer, demo, doctor, and browser nodes.
- The immutable fixture must not be regenerated after wording edits; later owners consume it read-only.
- Current-tree whole-inventory cleanliness remains intentionally deferred until all owning rewrite plans finish.

## Self-Check: PASSED

All five implementation/test artifacts, this summary, and both atomic task commits were verified present.

---
*Phase: 41.1-codebase-architecture-overhaul*
*Completed: 2026-08-27*
