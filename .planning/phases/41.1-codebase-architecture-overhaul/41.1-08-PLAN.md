---
phase: 41.1-codebase-architecture-overhaul
plan: 08
type: execute
wave: 9
depends_on: ["41.1-07", "41.1-11"]
files_modified:
  - tests/architecture/test_active_language_policy.py
  - tests/architecture/fixtures/runtime_prose_contract.json
  - src/runtime/cli.py
  - src/runtime/contracts.py
  - src/runtime/service.py
autonomous: true
gap_closure: true
requirements: [REFACTOR-02, REFACTOR-03, REFACTOR-05]
estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "One import-free raw-text scanner consumes the exact module/tool/document/asset policy and rejects chronology in Python, batch, PowerShell, Bash, HTML, CSS, JavaScript, and Markdown."
    - "The scanner permits only policy-owned literal or balanced marker exceptions and rejects broad, stale, nested, unmatched, or moved exceptions."
    - "A pre-edit fixture freezes non-prose structure for all 12 runtime files named by the verifier before any wording changes."
    - "CLI, contracts, and service descriptions use domain language while imports, calls, signatures, strings, commands, and behavior fingerprints remain exact."
  artifacts:
    - path: "tests/architecture/test_active_language_policy.py"
      provides: "Import-free raw source, document, tool, and demo-asset language enforcement"
    - path: "tests/architecture/fixtures/runtime_prose_contract.json"
      provides: "Independent pre-edit AST/JavaScript fingerprints and protected literal inventory"
    - path: "src/runtime/cli.py"
      provides: "Domain-readable installed command entrypoint"
  key_links:
    - from: "architecture/module-boundaries.json active_text_scan and tools"
      to: "test_active_language_policy.py"
      via: "closed path discovery and exact exception ownership"
    - from: "runtime_prose_contract.json"
      to: "12 verifier-named runtime files"
      via: "AST/JavaScript hashes that mask only explicit prose slots"
  prohibitions:
    - "Do not import src.*, runtime backends, models, providers, training, conversion, inference, evaluation, archive, or historical implementations in this test module."
    - "Do not update expected hashes from post-edit source."
    - "Do not make any current dirty document pass by a wildcard, whole-file quarantine, or omitted inventory entry."
    - "Do not access protected authorities, data rows, D-drive, ProgramData, data/models, or sealed evidence."
---

<objective>
Build the reusable language gate and prove one installed runtime path through it.

Purpose: Make later source/document rewrites exact and behavior-preserving under D-01/D-02/D-04/D-05/D-08.
Output: Static raw-text enforcement, immutable pre-edit fingerprints, and phase-neutral CLI/contracts/service prose.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-CONTEXT.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-07-SUMMARY.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-11-SUMMARY.md
</context>

## Multi-Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Human/domain active code language with exact compatibility boundaries | Tasks 1-2 |
| REQ | REFACTOR-02/03/05 | Tasks 1-2 |
| RESEARCH | Characterize before prose edits; static/import-free enforcement | Task 1 |
| CONTEXT | D-01 through D-08 | Both tasks |

The remaining runtime files are owned by Plans 15 and 14; active documents are owned by Plans 12-13. QLORA typo quality remains explicitly deferred rather than silently certified.

<tasks>

<task type="tracer">
  <name>Task 1: Trace raw source text through a closed import-free language policy</name>
  <files>tests/architecture/test_active_language_policy.py, tests/architecture/fixtures/runtime_prose_contract.json</files>
  <read_first>
    - `architecture/module-boundaries.json`: exact active modules, 11 tools, eight active documents, historical walkthrough, demo assets, and region-marker owners from Plan 07
    - The 12 named runtime inputs: cli, contracts, service, render, demo, doctor, demo.js, accelerated, gguf, heuristic, rules, and local_model
    - `tests/architecture/bootstrap/sitecustomize.py` and conftest: Plan 11 startup contract
  </read_first>
  <action>
    Create a standard-library-plus-pytest static suite that reads raw UTF-8 text only and never imports `src.*`. Consume the exact policy rather than embedding a second inventory. Scan active Python and compatibility tool source, the two active batch launchers, all four demo text assets (including HTML inline CSS/JS and comments), and all eight active Markdown documents. Binary WOFF2 paths remain exact inventory members but are never decoded. Historical/replay scripts and `walkthrough/README.md` remain exact classified inventory entries and are not active rewrite targets.

    Enforce the case-insensitive chronology pattern `phase` followed by optional space/hyphen/underscore and digits. Permit only exact path+literal or path+balanced-marker-pair entries with reason and owner. Reject wildcard/path-wide entries, missing/extra markers, nesting, stale literals, near-matches, and the same literal outside its declared region. Add tmp-path mutations for Python docstring/string/comment, batch/PowerShell/Bash text, HTML prose/inline code, CSS, JavaScript, and Markdown. Exact active-document discovery must equal README, four architecture docs, and three user docs; the historical walkthrough is separately exact.

    Before editing any runtime file, create `runtime_prose_contract.json` from tracked pre-edit bytes. Cover exactly the 12 named runtime inputs. For Python, hash an AST dump that masks only explicit path+AST-location prose slots and retains imports, calls, signatures, control flow, and all non-approved strings. Store accelerated/GGUF setup commands separately. For JavaScript, hash source after comment removal so executable code and strings remain exact. Expected hashes are fixed fixture values, never calculated during assertion. Install a rejecting meta-path finder for model/training/conversion/provider/inference/heavy dependency prefixes and assert they never enter `sys.modules`.
  </action>
  <verify>
    <automated>$env:PYTHONPATH=(Resolve-Path 'tests/architecture/bootstrap').Path; $phase411Temp=Join-Path ([System.IO.Path]::GetTempPath()) ('vnphish-411-'+[guid]::NewGuid().ToString('N')); $phase411Base=Join-Path $phase411Temp 'pytest'; New-Item -ItemType Directory -Path $phase411Base -Force | Out-Null; $env:PHASE411_DENY_OPEN_SENTINEL='1'; $env:PHASE411_REQUIRE_HISTORICAL_ARCHIVE='0'; $env:PYTHONNOUSERSITE='1'; $env:PYTHONDONTWRITEBYTECODE='1'; $env:DATA_DIR=Join-Path $phase411Temp 'data'; $env:MODEL_ARTIFACT_ROOT=Join-Path $phase411Temp 'models'; $env:MODEL_REGISTRY_PATH=Join-Path $phase411Temp 'registry.json'; $env:TEMP=$phase411Temp; $env:TMP=$phase411Temp; $python=(Get-Command python -CommandType Application).Source; $nodes=@('tests/architecture/test_active_language_policy.py::test_static_language_suite_blocks_execution_imports','tests/architecture/test_active_language_policy.py::test_active_text_scanner_rejects_every_supported_source_language','tests/architecture/test_active_language_policy.py::test_active_text_allowlist_is_exact_and_marker_scoped','tests/architecture/test_active_language_policy.py::test_runtime_prose_behavior_fingerprints_match_characterization'); &amp; $python -m pytest -q -p no:cacheprovider --basetemp $phase411Base $nodes</automated>
  </verify>
  <done>The scanner and immutable 12-file pre-edit characterization exist before any prose change.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the installed CLI, contracts, and orchestration prose</name>
  <files>src/runtime/cli.py, src/runtime/contracts.py, src/runtime/service.py, tests/architecture/test_active_language_policy.py</files>
  <read_first>
    - `src/runtime/cli.py`, `contracts.py`, and `service.py`: current module descriptions only
    - `runtime_prose_contract.json`: immutable pre-edit fingerprints
    - `test_active_language_policy.py`: shared scanner; extend it rather than duplicating it
  </read_first>
  <action>
    Per D-01 and D-02, replace chronology-oriented module prose with concrete descriptions of the installed offline phishing-risk command interface, typed runtime contract, and normalized analysis orchestration. Add `test_active_runtime_entrypoint_language_is_domain_named` over exactly these three files. Preserve every import, callable, signature, parser behavior, output, serialization field, command literal, and non-approved string. The immutable fingerprint test must pass unchanged, and the import blocker must remain silent.
  </action>
  <verify>
    <automated>$env:PYTHONPATH=(Resolve-Path 'tests/architecture/bootstrap').Path; $phase411Temp=Join-Path ([System.IO.Path]::GetTempPath()) ('vnphish-411-'+[guid]::NewGuid().ToString('N')); $phase411Base=Join-Path $phase411Temp 'pytest'; New-Item -ItemType Directory -Path $phase411Base -Force | Out-Null; $env:PHASE411_DENY_OPEN_SENTINEL='1'; $env:PHASE411_REQUIRE_HISTORICAL_ARCHIVE='0'; $env:PYTHONNOUSERSITE='1'; $env:DATA_DIR=Join-Path $phase411Temp 'data'; $env:MODEL_ARTIFACT_ROOT=Join-Path $phase411Temp 'models'; $env:MODEL_REGISTRY_PATH=Join-Path $phase411Temp 'registry.json'; $env:TEMP=$phase411Temp; $env:TMP=$phase411Temp; $python=(Get-Command python -CommandType Application).Source; &amp; $python -m pytest -q -p no:cacheprovider --basetemp $phase411Base tests/architecture/test_active_language_policy.py::test_static_language_suite_blocks_execution_imports tests/architecture/test_active_language_policy.py::test_runtime_prose_behavior_fingerprints_match_characterization tests/architecture/test_active_language_policy.py::test_active_runtime_entrypoint_language_is_domain_named</automated>
  </verify>
  <done>The installed runtime entry path uses domain language with exact non-prose compatibility.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Policy inventory -> raw source scanner | Every active source/document/asset must be included exactly once |
| Raw text -> exception matcher | Authority identifiers may pass only at one exact path/region |
| Prose slot -> runtime behavior | Only characterized prose locations may change |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-41.1-08-01 | Tampering | Scanner inventory | high | mitigate | Exact policy-driven paths and synthetic mutation coverage across all supported text languages |
| T-41.1-08-02 | Elevation of Privilege | Exception matcher | high | mitigate | Exact literals/markers/reasons/owners; reject wildcards, stale entries, and moved tokens |
| T-41.1-08-03 | Tampering | Runtime prose | high | mitigate | Pre-edit AST/JS fingerprints and exact protected literals |
| T-41.1-08-04 | Information Disclosure | Static suite | high | mitigate | Plan 11 bootstrap, rejecting importer, exact nodes, no executable src import |
</threat_model>

<verification>
Run only the exact import-free static nodes. Every Python invocation starts with the Plan 11 bootstrap, unique temp roots, and deny-open sentinel. Current-tree whole-inventory cleanliness is intentionally deferred until Plans 12-15 and final Plan 10 complete all owned rewrites.
</verification>

<success_criteria>
- Raw Python, batch, PowerShell, Bash, HTML, CSS, JavaScript, and Markdown chronology is detectable.
- Exact policy markers/literals are the only exceptions.
- All 12 named runtime files have immutable pre-edit behavior fingerprints.
- CLI/contracts/service use domain prose without behavior or command drift.
</success_criteria>

<output>
Create `.planning/phases/41.1-codebase-architecture-overhaul/41.1-08-SUMMARY.md` when done.
</output>
