---
phase: 41.1-codebase-architecture-overhaul
plan: 01
subsystem: architecture-testing
tags: [provenance, archival, argparse, compatibility, synthetic-tests]
requires:
  - phase: 41-production-model-adaptation
    provides: frozen execution-source manifest and immutable clean-runtime evidence
provides:
  - content-addressed historical mirror of the exact Phase 41 producer closure
  - guarded, deterministic model/runtime CLI compatibility snapshots
  - serialization, enum, schema-version, canonical-byte, and protected-authority baselines
affects: [41.1-02, 41.1-03, refactor-verification]
tech-stack:
  added: []
  patterns:
    - fixed-member capture with receipt-last publication
    - pre-collection deny-open bootstrap
    - fresh-process parser capture with closed synthetic stubs
    - Git index-blob verification for byte-exact archival content
key-files:
  created:
    - tests/architecture/bootstrap/sitecustomize.py
    - tests/architecture/conftest.py
    - tests/architecture/test_contract_baselines.py
    - tests/architecture/fixtures/protected_authority_baseline.json
    - tests/architecture/fixtures/model_cli_contract.json
    - tests/architecture/fixtures/runtime_cli_contract.json
    - tests/architecture/fixtures/serialization_contract.json
    - historical/phase41-source-closure/c3bbc8c8adaf7579fd2eb9c59a0081613be4b2cae05dfdb64472938c7e6d0434/
  modified:
    - scripts/archive_phase41_source_closure.py
    - tests/architecture/test_provenance_boundary.py
    - .gitattributes
key-decisions:
  - "Capture real CLI parser construction only after installing a closed synthetic dependency set in a fresh guarded process."
  - "Store the historical mirror with Git text conversion disabled and compare index blobs to the receipt hashes."
  - "Escape unencodable console characters only at the terminal boundary; never rewrite archival or receipt bytes."
patterns-established:
  - "Authority guard: the deny-open sentinel must be installed by sitecustomize before pytest imports conftest."
  - "Compatibility capture: parser graphs, handler outcomes, stdout, stderr, return values, and SystemExit codes are regenerated twice and compared byte-for-byte."
requirements-completed: [REFACTOR-01, REFACTOR-05]
duration: 24min
completed: 2026-08-27
status: complete
actuals:
  tokens: 547550
  tasks: 3
  commits: 5
coverage:
  - id: D1
    description: "The exact Phase 41 producer closure is preserved in a content-addressed historical mirror with truthful post-evaluation provenance."
    requirement: REFACTOR-01
    verification:
      - kind: integration
        ref: "tests/architecture/test_provenance_boundary.py#historical mirror membership and hash checks"
        status: pass
      - kind: other
        ref: "python scripts/archive_phase41_source_closure.py verify"
        status: pass
    human_judgment: false
  - id: D2
    description: "Model/runtime CLI and serialization behavior is frozen in deterministic synthetic-only fixtures."
    requirement: REFACTOR-05
    verification:
      - kind: unit
        ref: "tests/architecture/test_contract_baselines.py"
        status: pass
    human_judgment: false
  - id: D3
    description: "Architecture tests fail closed before collection on reserved split or live model/data-root access."
    requirement: REFACTOR-05
    verification:
      - kind: integration
        ref: "tests/architecture/test_provenance_boundary.py#deny-open and protected-authority checks"
        status: pass
    human_judgment: false
---

# Phase 41.1 Plan 01: Producer Closure and Compatibility Baselines Summary

Content-addressed Phase 41 producer mirror with byte-exact Git storage, guarded CLI snapshots, and inherited deny-open enforcement.

## Performance

- **Duration:** 24 min
- **Started:** 2026-08-27T00:17:38Z
- **Completed:** 2026-08-27T00:41:12Z
- **Tasks:** 3
- **Files changed:** 50

## Accomplishments

- Proved the archive workflow end to end behind a pre-collection filesystem guard that rejects the reserved split tree and live model/data fallbacks before test collection.
- Froze the observable 23-command model CLI and three-command runtime CLI contracts, including parser graphs, error behavior, handler outcomes, safe stderr, Unicode serialization, enums, schemas, artifact names, key order, and newline bytes.
- Published an immutable historical mirror containing exactly 37 manifest-listed sources plus the fixed Phase 41 launcher, the raw manifest, and a receipt that truthfully labels the mirror as post-evaluation archival evidence rather than a refactored metric producer.
- Made Git's stored blobs part of the provenance boundary so checkout-time text conversion cannot silently change preserved producer bytes.

## Task Commits

Each task was committed atomically, with TDD tasks retaining separate RED and GREEN gates:

1. **Task 1: Prove one synthetic source-closure archive end to end**
   - `9f17b2e` — RED: add failing provenance-boundary tests and authority baseline
   - `b200f59` — GREEN: implement archive script and startup guard
2. **Task 2: Freeze observable compatibility and byte baselines**
   - `de290cb` — RED: add failing compatibility baseline tests
   - `9934d3c` — GREEN: freeze deterministic compatibility baselines
3. **Task 3: Materialize and independently verify the historical mirror**
   - `b0d2482` — publish and verify the byte-exact producer closure

## Test Results

- Guarded combined suite: `22 passed in 7.95s`.
- Read-only archive verifier: passed twice consecutively against source tree SHA-256 `c3bbc8c8adaf7579fd2eb9c59a0081613be4b2cae05dfdb64472938c7e6d0434`.
- Receipt reports `37` source files, `38` payload files, launcher SHA-256 `c5f15a32b2c8d8ee196e3ec484707c27c4c05e5389d958626e775e44f52d49e9`, and the fixed ProgramData launcher origin.
- No Phase 41 evaluation, training, inference, model loading, live provider call, or reserved split access was performed.

## Files Created/Modified

- `scripts/archive_phase41_source_closure.py` — validates fixed members and ancestry, captures once, publishes exclusively, emits the receipt last, verifies independently, and writes console output safely.
- `tests/architecture/bootstrap/sitecustomize.py` — installs the deny-open guard before pytest collection.
- `tests/architecture/conftest.py` — requires the sentinel and confines synthetic data/model roots to each test.
- `tests/architecture/test_provenance_boundary.py` — exercises archive attacks, protected authorities, mirror membership, Git index blobs, and console encoding.
- `tests/architecture/test_contract_baselines.py` — regenerates and compares guarded CLI/serialization fixtures deterministically.
- `tests/architecture/fixtures/*.json` — stores fixed protected-authority, model CLI, runtime CLI, and serialization baselines.
- `historical/phase41-source-closure/c3bbc8c8adaf7579fd2eb9c59a0081613be4b2cae05dfdb64472938c7e6d0434/` — contains the immutable producer payload, raw manifest, and archival receipt.
- `.gitattributes` — disables text conversion only beneath the historical Phase 41 closure.

## Decisions Made

- Used the real CLI leaf parser constructors while replacing every eager implementation dependency with a source-controlled synthetic stub set; this captures behavior without importing evaluator, trainer, inference, provider, or model-framework implementations.
- Treated Git index bytes as independently verified archival evidence, not merely the working-tree copies.
- Preserved the archive published by the single authorized capture after its terminal-only encoding failure; repaired presentation and verified the existing destination rather than recapturing immutable evidence.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Made archive success output safe on legacy Windows consoles**

- **Found during:** Task 3
- **Issue:** The archive was fully and correctly published, but printing its JSON receipt raised `UnicodeEncodeError` because the repository path contains Vietnamese characters not representable by CP1252.
- **Fix:** Added a terminal-boundary writer that backslash-escapes only unencodable display characters, plus a strict CP1252 regression test. The archive and receipt bytes remain unchanged.
- **Files modified:** `scripts/archive_phase41_source_closure.py`, `tests/architecture/test_provenance_boundary.py`
- **Commit:** `b0d2482`

**2. [Rule 2 - Missing Critical Functionality] Prevented Git text normalization from corrupting producer bytes**

- **Found during:** Task 3 pre-commit provenance review
- **Issue:** Git's default text conversion could make tracked historical blobs differ from the captured byte hashes even when working-tree files appeared correct.
- **Fix:** Added a narrow `historical/phase41-source-closure/** -text` rule, renormalized only that archive in the index, and added index-blob hash assertions to the provenance gate.
- **Files modified:** `.gitattributes`, `tests/architecture/test_provenance_boundary.py`
- **Commit:** `b0d2482`

## Known Stubs

None introduced. Stub-like names and empty defaults inside the historical mirror are preserved producer bytes, not newly active implementation, and the mirror is never imported by the compatibility harness.

## Issues Encountered

- An ad hoc combined pytest invocation without the required bootstrap correctly failed at collection because the deny-open sentinel was absent. The plan-prescribed guarded invocation then passed all 22 tests; this confirms the startup guard fails closed.

## User Setup Required

None.

## Next Phase Readiness

- Wave 2 can refactor source-closure members against fixed CLI, serialization, authority, and byte baselines.
- The historical mirror and protected Phase 41 authority artifacts are now explicit immutable inputs; later work must continue to avoid evaluation, model loading, reserved splits, and producer relabeling.

## Self-Check: PASSED

All declared artifacts exist, and all five task commits resolve as commits in repository history.
