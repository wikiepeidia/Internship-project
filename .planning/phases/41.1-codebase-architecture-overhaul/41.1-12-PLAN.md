---
phase: 41.1-codebase-architecture-overhaul
plan: 12
type: execute
wave: 11
depends_on: ["41.1-15"]
files_modified:
  - README.md
  - documents/user/USER_GUIDE.md
  - documents/user/LOCAL_MODELS.md
  - tests/architecture/test_active_language_policy.py
  - .planning/phases/41.1-codebase-architecture-overhaul/41.1-REPORT-HANDOFF.md
autonomous: true
gap_closure: true
requirements: [REFACTOR-02, REFACTOR-03, REFACTOR-06]
estimate:
  tokens: 21000
  raw_tokens: 21000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "README and user guides describe runtime, demo, data, and local-model responsibilities in domain language rather than project chronology."
    - "Every retained phase-numbered CLI/version literal is byte-exact inside one policy-owned balanced marker region and fails outside it."
    - "README links to documents/user/LOCAL_MODELS.md correctly."
    - "QLORA.md remains in the active inventory but is explicitly not certified as defense-ready; its typo quality is later documentation work."
  artifacts:
    - path: "README.md"
      provides: "Domain-readable project/operator entry guide with scoped CLI literal appendix"
    - path: "documents/user/USER_GUIDE.md"
      provides: "Phase-neutral local runtime and demo instructions"
    - path: "documents/user/LOCAL_MODELS.md"
      provides: "Phase-neutral local model/operator explanation with exact command literals"
  key_links:
    - from: "policy active document inventory"
      to: "three rewritten documents and unchanged QLORA.md"
      via: "raw UTF-8 scanner and exact marker ownership"
  prohibitions:
    - "Do not rename commands, flags, version-tag values, paths, schemas, artifacts, or models."
    - "Do not exempt an entire document or move active prose inside a marker."
    - "Do not rewrite QLORA.md in this gap or call it defense-ready."
    - "Do not access data rows, providers, models, protected authorities, D-drive, ProgramData, or evidence."
---

<objective>
Make the project entry guide and user documentation readable without hiding compatibility literals.

Purpose: Fulfill D-02/D-07 defense readability while keeping exact commands and visibly retaining the QLORA document-quality limitation.
Output: Domain-language README/USER_GUIDE/LOCAL_MODELS plus exact scoped marker tests.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-CONTEXT.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-08-SUMMARY.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-15-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Rewrite README and local runtime guidance with exact command regions</name>
  <files>README.md, documents/user/USER_GUIDE.md, tests/architecture/test_active_language_policy.py</files>
  <read_first>
    - `README.md`: chronology occurrences at current lines 5, 7, 9, 44, 46, 53, 55, 72, 74, 88, command tags near 91/97/105/113/119/129, target prose near 150, and broken link near 51
    - `documents/user/USER_GUIDE.md`: chronology occurrences at current lines 1, 13, 15, 79, 84, and 96
    - `architecture/module-boundaries.json`: exact active-document and marker ownership contract
    - `tests/architecture/fixtures/active_text_contract.json`: independent exact document inventory and `legacy-readme-data-cli` row produced by Plan 07; consume it read-only
  </read_first>
  <action>
    Exact-equal the policy row used here to the independent Plan 07 fixture before editing. Rewrite README headings/prose as Local Runtime, Local Model Profiles, Local Demo UI, and Dataset Operator Flow. Replace every active narrative reference at the enumerated locations with domain/release wording. Fix `docs/user/LOCAL_MODELS.md` to `documents/user/LOCAL_MODELS.md`. Preserve the six current `phase1-*` version-tag command values byte-for-byte by moving only their command fences into the fixture-owned `legacy-readme-data-cli` marker pair; no explanatory active prose belongs inside that region. Rewrite the retained-dataset target sentence without chronology. Do not introduce another README marker ID.

    Rewrite USER_GUIDE title, runtime boundary, text-only boundary, supported analysis/demo options, and final failure sentence using domain/release language. Its existing commands are phase-neutral and require no chronology marker. Add `test_readme_and_user_guide_use_domain_language_and_scoped_cli_markers`: require exact occurrence elimination in active prose, one balanced nonnested README marker pair, exact retained command bytes, correct local-model link, and rejection when a token moves outside its marker or prose moves inside it.
  </action>
  <verify>
    <automated>$env:PYTHONPATH=(Resolve-Path 'tests/architecture/bootstrap').Path; $phase411Temp=Join-Path ([System.IO.Path]::GetTempPath()) ('vnphish-411-'+[guid]::NewGuid().ToString('N')); $phase411Base=Join-Path $phase411Temp 'pytest'; New-Item -ItemType Directory -Path $phase411Base -Force | Out-Null; $env:PHASE411_DENY_OPEN_SENTINEL='1'; $env:PHASE411_REQUIRE_HISTORICAL_ARCHIVE='0'; $env:PYTHONNOUSERSITE='1'; $env:DATA_DIR=Join-Path $phase411Temp 'data'; $env:MODEL_ARTIFACT_ROOT=Join-Path $phase411Temp 'models'; $env:MODEL_REGISTRY_PATH=Join-Path $phase411Temp 'registry.json'; $env:TEMP=$phase411Temp; $env:TMP=$phase411Temp; $python=(Get-Command python -CommandType Application).Source; &amp; $python -m pytest -q -p no:cacheprovider --basetemp $phase411Base tests/architecture/test_active_language_policy.py::test_readme_and_user_guide_use_domain_language_and_scoped_cli_markers</automated>
  </verify>
  <done>README and USER_GUIDE are phase-neutral, correctly linked, and preserve only exact marker-bound CLI literals.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite local-model narrative and retain visible QLORA debt</name>
  <files>documents/user/LOCAL_MODELS.md, tests/architecture/test_active_language_policy.py, .planning/phases/41.1-codebase-architecture-overhaul/41.1-REPORT-HANDOFF.md</files>
  <read_first>
    - `LOCAL_MODELS.md`: chronology prose at current lines 5, 18, 20, 89-92, 97, 113 and CLI version tags at 101-102
    - `QLORA.md`: current one-line typo-heavy explanation; read only
    - `41.1-REPORT-HANDOFF.md`: Plan 07 limitation statement
    - `tests/architecture/fixtures/active_text_contract.json`: independent exact eight-document inventory and `legacy-local-model-cli` row produced by Plan 07; consume it read-only
  </read_first>
  <action>
    Exact-equal the policy row used here to the independent Plan 07 fixture before editing. Rewrite the local-model scope, LoRA/QLoRA distinction, operator sequence, command-section lead, and Colab-generation sentence in domain language. Preserve `phase3-smoke` and `phase3-main` exactly inside the fixture-owned `legacy-local-model-cli` marker pair containing only the command block. Keep all profile names, commands, flags, artifact expectations, environment variables, and model IDs unchanged. Do not introduce another LOCAL_MODELS marker ID.

    Add `test_local_model_guide_scopes_cli_literals_and_declares_qlora_debt`: exact current chronology prose is gone; the two version-tag literals occur only inside one nonnested marker pair; `QLORA.md` remains in the exact active inventory and chronology scanner; and the handoff still says its typo-quality rewrite is later work and not defense-ready. Do not edit `QLORA.md` or add a quality assertion that would falsely certify it.
  </action>
  <verify>
    <automated>$env:PYTHONPATH=(Resolve-Path 'tests/architecture/bootstrap').Path; $phase411Temp=Join-Path ([System.IO.Path]::GetTempPath()) ('vnphish-411-'+[guid]::NewGuid().ToString('N')); $phase411Base=Join-Path $phase411Temp 'pytest'; New-Item -ItemType Directory -Path $phase411Base -Force | Out-Null; $env:PHASE411_DENY_OPEN_SENTINEL='1'; $env:PHASE411_REQUIRE_HISTORICAL_ARCHIVE='0'; $env:PYTHONNOUSERSITE='1'; $env:DATA_DIR=Join-Path $phase411Temp 'data'; $env:MODEL_ARTIFACT_ROOT=Join-Path $phase411Temp 'models'; $env:MODEL_REGISTRY_PATH=Join-Path $phase411Temp 'registry.json'; $env:TEMP=$phase411Temp; $env:TMP=$phase411Temp; $python=(Get-Command python -CommandType Application).Source; &amp; $python -m pytest -q -p no:cacheprovider --basetemp $phase411Base tests/architecture/test_active_language_policy.py::test_local_model_guide_scopes_cli_literals_and_declares_qlora_debt</automated>
  </verify>
  <done>LOCAL_MODELS is readable and exact, while QLORA quality debt remains explicit and unmisrepresented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Narrative -> command contract | Rewriting prose must not alter frozen command/version bytes |
| Marker -> active prose | A compatibility region must not become a broad bypass |
| Deferred QLORA note -> report claim | Chronology cleanliness must not be misrepresented as editorial readiness |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-41.1-12-01 | Tampering | CLI examples | high | mitigate | Exact literals and balanced marker-only regions |
| T-41.1-12-02 | Repudiation | QLORA quality | medium | mitigate | Explicit handoff limitation and no defense-ready assertion |
| T-41.1-12-03 | Information Disclosure | Documentation tests | high | mitigate | Raw tracked docs only, Plan 11 bootstrap, no data/model access |
</threat_model>

<verification>
Run only the two exact import-free documentation nodes under the startup guard. No command example is executed.
</verification>

<success_criteria>
- All enumerated README, USER_GUIDE, and LOCAL_MODELS narrative occurrences are rewritten.
- Exact retained version-tag commands are marker-bound and byte-stable.
- README's local-model link resolves to the tracked documents/user path.
- QLORA remains inventoried and explicitly not defense-ready.
</success_criteria>

<output>
Create `.planning/phases/41.1-codebase-architecture-overhaul/41.1-12-SUMMARY.md` when done.
</output>
