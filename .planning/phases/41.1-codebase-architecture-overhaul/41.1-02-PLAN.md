---
phase: 41.1-codebase-architecture-overhaul
plan: 02
type: execute
wave: 2
depends_on: ["41.1-01"]
files_modified:
  - src/model_adaptation/cli.py
  - src/model_adaptation/commands/__init__.py
  - src/model_adaptation/commands/router.py
  - src/model_adaptation/commands/adaptation.py
  - src/model_adaptation/commands/legacy_phase40.py
  - src/model_adaptation/commands/legacy_phase41.py
  - tests/architecture/test_model_cli_contract.py
autonomous: true
requirements: [REFACTOR-02, REFACTOR-05]
estimate: {tokens: 42000, raw_tokens: 42000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "All 23 legacy commands retain identical parser actions, help, defaults, requiredness, choices, handler output, stderr, raw argv, and exit behavior."
    - "Importing or asking help from the compatibility CLI does not import training, evaluation, NumPy, sklearn, torch, transformers, or provider implementations."
    - "`src/model_adaptation/cli.py` is at most 250 lines; every new active module is at most 600 lines and every new function at most 100 lines."
    - "The static router maps only literal command names to literal module/symbol pairs and cannot import user-supplied module paths."
  artifacts:
    - {path: "src/model_adaptation/commands/router.py", provides: "Closed static lazy route table"}
    - {path: "src/model_adaptation/commands/adaptation.py", provides: "Pilot/train/convert/doctor command family"}
    - {path: "src/model_adaptation/commands/legacy_phase40.py", provides: "Twelve Phase 40 compatibility commands"}
    - {path: "src/model_adaptation/commands/legacy_phase41.py", provides: "Seven Phase 41 compatibility commands"}
  key_links:
    - {from: "src/model_adaptation/cli.py", to: "src/model_adaptation/commands/router.py", via: "parse first, resolve one closed route after parsing", pattern: "COMMAND_ROUTES|dispatch"}
  prohibitions:
    - "Never enumerate, stat, hash, open, copy, or resolve the reserved held-out split or its containing data/splits directory."
    - "Never import or invoke the Phase 41 evaluator, rerun evaluation/training/inference, or load a real model binary."
    - "Never mutate ProgramData, the verified export, provenance erratum, source manifest, frozen metrics, historical source mirror, or any sealed model root."
    - "Never delete, move, trim, or relink anything on D:."
    - "No wildcard re-export, module __getattr__, user-derived import target, deprecation output, or command/flag semantic correction."
---

<objective>
Replace the 1,329-line model-adaptation hub with a thin lazy compatibility shell without changing one observable CLI contract.

Purpose: Make operator tooling understandable and cheap to import while preserving every historical command used by scripts, notebooks, evidence, and tests.
Output: Static lazy router, three command families, thin facade, and a synthetic contract suite.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-CONTEXT.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-PATTERNS.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-01-SUMMARY.md
@tests/architecture/fixtures/model_cli_contract.json
</context>

## Artifacts this phase produces

- `src/model_adaptation/commands/__init__.py`.
- `router.py`: `CommandRoute`, `COMMAND_ROUTES`, `lazy_handler()`, `dispatch()`.
- `adaptation.py`: `register_commands()` and `handle_pilot`, `handle_train`, `handle_convert`, `handle_doctor`.
- `legacy_phase40.py`: `register_commands()` plus the twelve existing `handle_phase40_*` handlers.
- `legacy_phase41.py`: `register_commands()` plus the seven existing `handle_phase41_*` handlers.
- `tests/architecture/test_model_cli_contract.py`.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace the adaptation command family through the closed lazy router</name>
  <read_first>
    - `src/model_adaptation/cli.py`
    - `tests/model_adaptation/test_cli.py`
    - `tests/architecture/fixtures/model_cli_contract.json`
    - `NEW: src/model_adaptation/commands/__init__.py`
    - `NEW: src/model_adaptation/commands/router.py`
    - `NEW: src/model_adaptation/commands/adaptation.py`
    - `NEW: tests/architecture/test_model_cli_contract.py`
  </read_first>
  <files>src/model_adaptation/cli.py, src/model_adaptation/commands/__init__.py, src/model_adaptation/commands/router.py, src/model_adaptation/commands/adaptation.py, tests/architecture/test_model_cli_contract.py</files>
  <behavior>
    - `pilot`, `train`, `convert`, and `doctor` exactly match the frozen action graph and handler-double outputs.
    - `doctor --adaptation-mode` remains required in its current parser location.
    - Unknown commands exit 2 through argparse and no route accepts a module name from argv.
    - Import/help leaves optional ML/provider modules absent from `sys.modules`.
  </behavior>
  <action>Implement an immutable `MappingProxyType[str, CommandRoute]` containing all 23 literal routes, but migrate only the four adaptation entries in this task. Keep command registration dependency-light; all catalog, training, conversion, pilot, and doctor imports occur inside invoked handlers. Preserve explicit old `src.model_adaptation.cli` handler/helper names needed by caller search as small forwarding functions; migrate private monkeypatch targets only for this family and keep compatibility aliases for any remaining external source/test caller.</action>
  <verify><automated>$env:PHASE411_DENY_OPEN_SENTINEL='1'; $env:DATA_DIR=Join-Path $env:TEMP 'vnphish-411-p02-data'; $env:MODEL_ARTIFACT_ROOT=Join-Path $env:TEMP 'vnphish-411-p02-models'; $env:MODEL_REGISTRY_PATH=Join-Path $env:TEMP 'vnphish-411-p02-registry.json'; python -m pytest tests/architecture/test_model_cli_contract.py -q --basetemp .tmp/pytest-411-p02-adaptation</automated></verify>
  <acceptance_criteria>The four routes are lazy and byte-for-byte contract-equal to the fixture; parser construction invokes no model/provider dependency; remaining commands still use their pre-extraction behavior.</acceptance_criteria>
  <done>The adaptation family is behind the static router with no observable CLI drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract the twelve Phase 40 compatibility commands</name>
  <read_first>
    - `src/model_adaptation/cli.py`
    - `src/model_adaptation/commands/router.py`
    - `tests/architecture/test_model_cli_contract.py`
    - `tests/architecture/fixtures/model_cli_contract.json`
    - `NEW: src/model_adaptation/commands/legacy_phase40.py`
  </read_first>
  <files>src/model_adaptation/cli.py, src/model_adaptation/commands/router.py, src/model_adaptation/commands/legacy_phase40.py, tests/architecture/test_model_cli_contract.py</files>
  <behavior>
    - The exact commands are `phase40-preflight`, `phase40-build-source-bundle`, `phase40-build-input-bundle`, `phase40-verify-input-bundle`, `phase40-verify-run-request`, `phase40-verify-run-evidence`, `phase40-render-graphs`, `phase40-validate-notebooks`, `phase40-finalize-comparison`, `phase40-freeze-scope-amendment`, `phase40-verify-review-queue`, and `phase40-finalize-human-review`.
    - Machine-readable JSON order/content and console-safe printing remain unchanged under handler doubles.
    - Hash-sensitive commands emit no deprecation or relocation warning.
  </behavior>
  <action>Move the twelve parser-registration and handler bodies as one command family. Convert every heavy Phase 40 import to a handler-local import. Preserve `_phase40_raw_argv`, `_print_console_safe`, caught exception types, exit code 1, review authority parsing, and explicit old facade symbols. Perform a caller search for each moved private symbol and change only this family’s test monkeypatch owners; no mass string rewrite.</action>
  <verify><automated>$env:PHASE411_DENY_OPEN_SENTINEL='1'; $env:DATA_DIR=Join-Path $env:TEMP 'vnphish-411-p02-data'; $env:MODEL_ARTIFACT_ROOT=Join-Path $env:TEMP 'vnphish-411-p02-models'; $env:MODEL_REGISTRY_PATH=Join-Path $env:TEMP 'vnphish-411-p02-registry.json'; python -m pytest tests/architecture/test_model_cli_contract.py -q --basetemp .tmp/pytest-411-p02-p40</automated></verify>
  <acceptance_criteria>All twelve commands match the baseline and invoking help/parser paths imports none of their historical implementation graph.</acceptance_criteria>
  <done>The Phase 40 command family is isolated behind explicit lazy compatibility routes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extract Phase 41 routes and enforce the thin-facade budget</name>
  <read_first>
    - `src/model_adaptation/cli.py`
    - `src/model_adaptation/commands/router.py`
    - `tests/model_adaptation/test_cli.py`
    - `tests/architecture/test_model_cli_contract.py`
    - `NEW: src/model_adaptation/commands/legacy_phase41.py`
  </read_first>
  <files>src/model_adaptation/cli.py, src/model_adaptation/commands/router.py, src/model_adaptation/commands/legacy_phase41.py, tests/architecture/test_model_cli_contract.py</files>
  <behavior>
    - The seven routes preserve prepare, preauthorization verify, authorize, run-once, deployment-fit disposition, export, and evidence verify exactly.
    - `phase41-run-once` accepts only help and output-root; authorization/disposition option sets stay fixed.
    - `main()` still captures raw argv and maps RuntimeError, ValueError, and FileNotFoundError to console-safe stderr and return 1.
    - Static AST budgets and the complete 23-command fixture pass.
  </behavior>
  <action>Move all seven Phase 41 handler implementations into the Phase 41 family with local historical imports. Retain explicit compatibility forwarding symbols in `cli.py`, including the current source-observable `handle_phase41_run_once` behavior expected by existing contracts. Finish `build_parser()` as three family registrations plus the shared error boundary. Enforce facade <=250 physical lines, new modules <=600, functions <=100; any exception requires a named symbol, measured line count, and rationale in the summary, never silent overage.</action>
  <verify><automated>$env:PHASE411_DENY_OPEN_SENTINEL='1'; $env:DATA_DIR=Join-Path $env:TEMP 'vnphish-411-p02-data'; $env:MODEL_ARTIFACT_ROOT=Join-Path $env:TEMP 'vnphish-411-p02-models'; $env:MODEL_REGISTRY_PATH=Join-Path $env:TEMP 'vnphish-411-p02-registry.json'; python -m pytest tests/architecture/test_model_cli_contract.py -q --basetemp .tmp/pytest-411-p02-final; git diff --exit-code -- data/models/phase41/verified-export data/models/phase41/phase41-provenance-erratum.json historical/phase41-source-closure</automated></verify>
  <acceptance_criteria>The facade is <=250 lines, all 23 commands/actions/outputs/exits equal the frozen fixture, no optional heavy module is imported by help, and historical archive/authorities are unchanged.</acceptance_criteria>
  <done>The oversized CLI is a thin, static, lazy, compatibility-preserving dispatcher.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| argv -> argparse -> route table | User tokens select only a known command, never a module path. |
| compatibility shell -> historical handlers | Optional/high-risk code loads only after an exact command is parsed. |

## STRIDE Threat Register (ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-411-02-01 | Repudiation | producer provenance | high | mitigate | Plan 01 archive is immutable and every summary states refactored CLI did not produce frozen metrics. |
| T-411-02-02 | Tampering | ProgramData closure | high | mitigate | This plan never accesses ProgramData; archive/receipt are read-only prerequisites. |
| T-411-02-03 | Tampering | reparse/TOCTOU | high | mitigate | No external copy; inherited deny sentinel blocks forbidden filesystem routes. |
| T-411-02-04 | Elevation of privilege | lazy import router | high | mitigate | Closed literal route table, argparse choices, AST assertions, and no argv-derived import string. |
| T-411-02-05 | Tampering | frozen evidence | high | mitigate | No evidence command is executed; Git diff gate covers export, erratum, and archive. |
| T-411-02-06 | Information disclosure | settings secrets | high | mitigate | Parser tests use temp env and never instantiate/log provider settings. |
| T-411-02-07 | Information disclosure | held-out data | high | mitigate | Exact architecture test only, deny-open sentinel, handler doubles, no evaluator invocation. |
| T-411-02-08 | Denial of service | D-drive models | high | mitigate | No model handler invocation and no D-drive write/delete code. |
</threat_model>

<verification>Run only `tests/architecture/test_model_cli_contract.py` with temp roots; compare immutable authorities by Git diff. Do not smoke a real legacy command.</verification>
<success_criteria>
- All 23 commands are contract-identical and lazy.
- The facade satisfies the 250-line budget with no heavy eager imports.
- No historical evidence, data, model, or D-drive artifact is touched.
</success_criteria>
<output>Create `.planning/phases/41.1-codebase-architecture-overhaul/41.1-02-SUMMARY.md` when done.</output>
