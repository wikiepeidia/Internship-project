---
phase: 41.1-codebase-architecture-overhaul
plan: 04
type: execute
wave: 4
depends_on: ["41.1-03"]
files_modified:
  - src/modeling/__init__.py
  - src/modeling/training.py
  - src/modeling/inference.py
  - src/modeling/evaluation.py
  - src/modeling/evidence.py
  - src/modeling/legacy_adapters.py
  - src/model_adaptation/legacy/phase40/__init__.py
  - src/model_adaptation/legacy/phase41/__init__.py
  - src/model_adaptation/commands/adaptation.py
  - architecture/module-boundaries.json
  - tests/architecture/test_modeling_boundaries.py
  - tests/architecture/test_modeling_evidence.py
  - tests/architecture/test_import_boundaries.py
autonomous: true
requirements: [REFACTOR-01, REFACTOR-03, REFACTOR-05]
estimate: {tokens: 42000, raw_tokens: 42000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Training, inference, evaluation contracts, and evidence reading have phase-neutral active module names."
    - "New active modules form a DAG; only explicit lazy adapters may point into historical Phase 40/41 code, never the reverse."
    - "Phase 41 results are consumed read-only as verified export plus provenance erratum; no API can rerun or rewrite them."
    - "Large chronological implementations stay intact and are classified historical rather than cosmetically renamed or falsely claimed as new producer code."
  artifacts:
    - {path: "src/modeling/legacy_adapters.py", provides: "Only permitted active-to-historical bridge"}
    - {path: "src/modeling/evidence.py", provides: "Read-only frozen reporting authority reader"}
    - {path: "architecture/module-boundaries.json", provides: "Closed active/adapter/historical classification and allowed edges"}
  key_links:
    - {from: "src/modeling/evidence.py", to: "verified export plus erratum", via: "strict read-only hash-linked authority", pattern: "load_reporting_authority"}
    - {from: "src/modeling/training.py", to: "src/modeling/legacy_adapters.py", via: "dependency-injected service and handler-local legacy import", pattern: "TrainingService|Legacy"}
  prohibitions:
    - "Never enumerate, stat, hash, open, copy, or resolve the reserved held-out split or its containing data/splits directory."
    - "Never import or invoke the Phase 41 evaluator, rerun evaluation/training/inference, or load a real model binary; only strict read-only frozen evidence metadata may be consumed."
    - "Never mutate ProgramData, the verified export, provenance erratum, source manifest, frozen metrics, historical source mirror, or any sealed model root."
    - "Never delete, move, trim, or relink anything on D:."
    - "No physical rewrite/move of the large Phase 40/41 implementations; no historical-to-active back edge."
---

<objective>
Introduce phase-neutral modeling services and a one-way historical boundary without rewriting frozen experimental machinery.

Purpose: Give the active system a reportable domain architecture while retaining exact legacy behavior and provenance.
Output: Modeling ports/services, read-only evidence reader, lazy legacy adapters, ownership indexes, and graph policy.
</objective>
<execution_context>
@C:/Users/wikiepeidia/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-CONTEXT.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-RESEARCH.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-PATTERNS.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-03-SUMMARY.md
@historical/phase41-source-closure/c3bbc8c8adaf7579fd2eb9c59a0081613be4b2cae05dfdb64472938c7e6d0434/archival-receipt.json
</context>

## Artifacts this phase produces

- `src/modeling/__init__.py`.
- `training.py`: `TrainingBackend`, `TrainingService`, `qwen_training_service()`, `phobert_training_service()`.
- `inference.py`: `InferenceBackend`, `InferenceService`.
- `evaluation.py`: strict `PerClassMetrics`, `ModelMetrics`, `TwoModelEvaluationResult` contracts only; no Phase 41 execution function.
- `evidence.py`: `ReportingAuthorityError`, `Phase41ReportingAuthority`, `load_reporting_authority()`.
- `legacy_adapters.py`: `LegacyQwenTrainingAdapter`, `LegacyPhoBertTrainingAdapter`, and explicit lazy call bridges.
- `src/model_adaptation/legacy/phase40/__init__.py`, `legacy/phase41/__init__.py`: import-free ownership indexes.
- `architecture/module-boundaries.json` and three exact architecture tests.

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Trace one phase-neutral modeling service through a lazy legacy bridge</name>
  <read_first>
    - `src/runtime/analyzers/base.py`
    - `src/runtime/service.py`
    - `src/model_adaptation/training.py`
    - `src/model_adaptation/phobert_training.py`
    - `NEW: src/modeling/__init__.py`
    - `NEW: src/modeling/training.py`
    - `NEW: src/modeling/inference.py`
    - `NEW: src/modeling/legacy_adapters.py`
    - `NEW: tests/architecture/test_modeling_boundaries.py`
  </read_first>
  <files>src/modeling/__init__.py, src/modeling/training.py, src/modeling/inference.py, src/modeling/legacy_adapters.py, tests/architecture/test_modeling_boundaries.py</files>
  <behavior>
    - Importing modeling ports imports no Phase 40/41, torch, transformers, sklearn, bitsandbytes, or provider module.
    - Services delegate once through injected protocols and translate adapter errors at one boundary.
    - Legacy Qwen/PhoBERT implementation imports occur inside adapter methods only and are replaceable by synthetic doubles.
    - New modules/functions meet 600/100-line budgets.
  </behavior>
  <action>Model the small `AnalyzerBackend`/`RuntimeService` seam: dependency-light generic protocols plus services, with no artifact/model path defaults. Implement lazy adapters around existing `build_training_config`/`run_training` and `run_phobert_training`, preserving their real signatures through typed forwarding rather than copying logic. Inference ports describe local analyze/predict operations and can wrap existing runtime backends through injection. No service exposes a Phase 41 evaluator or opens data/models.</action>
  <verify><automated>$env:PHASE411_DENY_OPEN_SENTINEL='1'; $env:DATA_DIR=Join-Path $env:TEMP 'vnphish-411-p04-data'; $env:MODEL_ARTIFACT_ROOT=Join-Path $env:TEMP 'vnphish-411-p04-models'; $env:MODEL_REGISTRY_PATH=Join-Path $env:TEMP 'vnphish-411-p04-registry.json'; python -m pytest tests/architecture/test_modeling_boundaries.py -q --basetemp .tmp/pytest-411-p04-services</automated></verify>
  <acceptance_criteria>Blocked-import subprocesses load all neutral ports; synthetic adapters prove delegation; AST shows no top-level historical edge; budgets pass.</acceptance_criteria>
  <done>New modeling callers can use phase-neutral training/inference services through one lazy bridge.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add evaluation contracts and immutable evidence consumption</name>
  <read_first>
    - `src/artifacts.py`
    - `src/core/integrity.py`
    - `data/models/phase41/verified-export/9ac54d58c273ab0a8c2f2b4b61e472a51ca94231a94b6847637ecad6ceee49f7/evidence-manifest.json`
    - `data/models/phase41/verified-export/9ac54d58c273ab0a8c2f2b4b61e472a51ca94231a94b6847637ecad6ceee49f7/results.json`
    - `data/models/phase41/phase41-provenance-erratum.json`
    - `NEW: src/modeling/evaluation.py`
    - `NEW: src/modeling/evidence.py`
    - `NEW: tests/architecture/test_modeling_evidence.py`
  </read_first>
  <files>src/modeling/evaluation.py, src/modeling/evidence.py, tests/architecture/test_modeling_evidence.py</files>
  <behavior>
    - Strict synthetic result fixtures validate fixed model roles, metrics, four-label order, confusion shape, terminal policy, and comparison statements.
    - The reporting reader requires verified export and erratum together and returns their raw-byte hashes plus typed facts.
    - Reader API has no write, export, predictor, model, split, retry, threshold, or training parameter.
    - Tamper, duplicate keys, absent erratum, wrong manifest link, or attempted reserialization fails closed on synthetic fixtures.
  </behavior>
  <action>Create phase-neutral evaluation result contracts only; explicitly omit any evaluator/run API. Implement `load_reporting_authority(export_root, erratum_path)` as an explicit-path, strict read-only consumer that verifies the manifest/receipt/result/source-manifest chain and mandatory erratum without opening predictions, model roots, or any split. Preserve raw bytes and hashes; never write normalized JSON. Tests build a complete miniature authority under `tmp_path` and use the deny sentinel.</action>
  <verify><automated>$env:PHASE411_DENY_OPEN_SENTINEL='1'; $env:DATA_DIR=Join-Path $env:TEMP 'vnphish-411-p04-data'; $env:MODEL_ARTIFACT_ROOT=Join-Path $env:TEMP 'vnphish-411-p04-models'; $env:MODEL_REGISTRY_PATH=Join-Path $env:TEMP 'vnphish-411-p04-registry.json'; python -m pytest tests/architecture/test_modeling_evidence.py -q --basetemp .tmp/pytest-411-p04-evidence</automated></verify>
  <acceptance_criteria>The synthetic authority validates/tampers as specified; static API inspection finds no mutator or execution hook; frozen real files have no Git diff.</acceptance_criteria>
  <done>Phase 42 can consume frozen evaluation facts through a read-only domain boundary without rerunning anything.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Declare legacy ownership and enforce an acyclic active graph</name>
  <read_first>
    - `src/model_adaptation/commands/adaptation.py`
    - `src/modeling/training.py`
    - `src/modeling/legacy_adapters.py`
    - `.planning/phases/41.1-codebase-architecture-overhaul/41.1-RESEARCH.md`
    - `NEW: src/model_adaptation/legacy/phase40/__init__.py`
    - `NEW: src/model_adaptation/legacy/phase41/__init__.py`
    - `NEW: architecture/module-boundaries.json`
    - `NEW: tests/architecture/test_import_boundaries.py`
  </read_first>
  <files>src/model_adaptation/commands/adaptation.py, src/model_adaptation/legacy/phase40/__init__.py, src/model_adaptation/legacy/phase41/__init__.py, architecture/module-boundaries.json, tests/architecture/test_import_boundaries.py</files>
  <behavior>
    - The closed JSON classifies active modules, compatibility adapters, historical modules, and the exact allowed adapter edges.
    - AST graph has zero SCCs among `src.runtime`, `src.core`, `src.artifacts`, and `src.modeling` active nodes.
    - Existing three historical SCCs pass only when wholly contained in the explicit historical allowlist.
    - No historical module imports a new facade; only named adapter/compatibility modules import historical modules.
  </behavior>
  <action>Create import-free Phase 40/41 ownership indexes listing exact flat historical modules; do not move their implementations. Create a closed machine-readable classification (no glob-based auto-admission) and a stdlib AST graph test for imports/SCCs. Route the four adaptation commands through the neutral service/adapters where their exact behavior permits; retain command-level lazy imports and old compatibility symbols. Record any historical-only SCC explicitly; an active SCC or undeclared edge is a hard failure.</action>
  <verify><automated>$env:PHASE411_DENY_OPEN_SENTINEL='1'; $env:DATA_DIR=Join-Path $env:TEMP 'vnphish-411-p04-data'; $env:MODEL_ARTIFACT_ROOT=Join-Path $env:TEMP 'vnphish-411-p04-models'; $env:MODEL_REGISTRY_PATH=Join-Path $env:TEMP 'vnphish-411-p04-registry.json'; python -m pytest tests/architecture/test_modeling_boundaries.py tests/architecture/test_modeling_evidence.py tests/architecture/test_import_boundaries.py -q --basetemp .tmp/pytest-411-p04-graph; git diff --exit-code -- data/models/phase41/verified-export data/models/phase41/phase41-provenance-erratum.json historical/phase41-source-closure</automated></verify>
  <acceptance_criteria>Active SCC count is zero, historical SCCs equal the reviewed allowlist, adapter direction is one-way, and all modeling/CLI contracts pass without historical execution.</acceptance_criteria>
  <done>The active modeling graph is phase-neutral and acyclic, with historical machinery isolated and traceable.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Active modeling service -> legacy adapter | Only one reviewed, lazy dependency direction may cross. |
| Frozen export + erratum -> reporting reader | Evidence is untrusted input but immutable authority, never execution input. |
## STRIDE Threat Register (ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-411-04-01 | Repudiation | provenance | high | mitigate | Reporting authority includes archive/source/export/erratum hashes and refactored-producer denial. |
| T-411-04-02 | Tampering | ProgramData copy | high | mitigate | No ProgramData access; tracked archive is read-only. |
| T-411-04-03 | Tampering | reparse/TOCTOU | high | mitigate | Explicit temp paths and inherited bounded readers; no external copy. |
| T-411-04-04 | Elevation of privilege | lazy adapters | high | mitigate | Closed classification, handler-local fixed imports, AST allowed-edge gate. |
| T-411-04-05 | Tampering | frozen evidence | high | mitigate | Read-only API, raw-byte hashes, no serializer/writer, Git diff gate. |
| T-411-04-06 | Information disclosure | secrets | high | mitigate | Modeling imports no provider settings; tests use dummy env. |
| T-411-04-07 | Information disclosure | held-out | high | mitigate | Evidence reader excludes predictions/splits; deny sentinel and synthetic fixtures. |
| T-411-04-08 | Denial of service | D-drive | high | mitigate | Adapters are doubled, no model invocation, no deletion/move code. |
</threat_model>
<verification>Use only the three exact architecture tests, synthetic adapters/authorities, and immutable Git diff gate.</verification>
<success_criteria>
- Phase-neutral services exist and are used by new callers.
- Active graph is acyclic with one-way explicit legacy adapters.
- Frozen evidence is readable for reporting but impossible to execute or mutate through the new API.
</success_criteria>
<output>Create `.planning/phases/41.1-codebase-architecture-overhaul/41.1-04-SUMMARY.md` when done.</output>
