---
phase: 41.1-codebase-architecture-overhaul
plan: 11
subsystem: architecture-security-testing
tags: [startup-guard, deny-all, git-metadata, provenance, report-fixture]
requires:
  - phase: 41.1-codebase-architecture-overhaul
    provides: Plan 07 exact architecture inventories and protected authority ownership
provides:
  - Startup-ordered seven-root and 53-symbol fail-closed authority guard
  - Immutable Git metadata verification for exactly 40 archived source members
  - Fixture-only source, export, erratum, retention, and report-handoff truth gates
affects: [41.1-08, 41.1-09, 41.1-10, 41.1-12, 41.1-13, 41.1-15, phase-42-report]
actuals:
  tokens: 19951
  tasks: 3
  commits: 4
tech-stack:
  added: []
  patterns: [startup-deny-before-attestation, literal-platform-disposition, git-metadata-only-authority, strict-reviewed-fact-fixture]
key-files:
  created:
    - tests/architecture/fixtures/phase411_guard_contract.json
    - tests/architecture/fixtures/phase411_safety_nodes.txt
    - tests/architecture/fixtures/report_fact_contract.json
    - tests/architecture/support/verify_archived_source_git_metadata.ps1
  modified:
    - tests/architecture/bootstrap/sitecustomize.py
    - tests/architecture/conftest.py
    - tests/architecture/test_provenance_boundary.py
    - tests/architecture/test_report_handoff.py
    - tests/architecture/fixtures/protected_authority_baseline.json
key-decisions:
  - "Guarded Python has no child-process or Git exception; every available reviewed process symbol is denied unconditionally."
  - "Archived producer bytes are verified only through literal Git index, commit, object type, and size metadata from outer PowerShell."
  - "Report validators consume a strict reviewed fact fixture and never reopen source receipt, sealed export, or erratum paths."
patterns-established:
  - "Guard order: lexical path denial, low-level process denial, safe standard-handle attestation, post-install descriptor capabilities, remaining process denial, installed sentinel last."
  - "Platform variance is explicit: each of 53 fixed symbols is either wrapped or unavailable_on_platform."
requirements-completed: [REFACTOR-01, REFACTOR-05, REFACTOR-06]
coverage:
  - id: D1
    description: Installed a fail-closed startup guard for seven roots, 53 process symbols, and post-install descriptor capabilities
    requirement: REFACTOR-05
    verification:
      - kind: integration
        ref: tests/architecture/fixtures/phase411_safety_nodes.txt#guard nodes 1-6
        status: pass
    human_judgment: false
  - id: D2
    description: Bound exactly 40 archive members to immutable publication-commit Git metadata without materializing content
    requirement: REFACTOR-01
    verification:
      - kind: integration
        ref: tests/architecture/support/verify_archived_source_git_metadata.ps1
        status: pass
      - kind: unit
        ref: tests/architecture/test_provenance_boundary.py#fixture and import-boundary nodes
        status: pass
    human_judgment: false
  - id: D3
    description: Converted report authority checks to strict fixture-only validation with synthetic mutation rejection
    requirement: REFACTOR-06
    verification:
      - kind: integration
        ref: tests/architecture/fixtures/phase411_safety_nodes.txt#report nodes 9-15
        status: pass
    human_judgment: false
duration: 10min
completed: 2026-08-27
status: complete
---

# Phase 41.1 Plan 11: Fail-Closed Authority Verification Summary

**Startup-ordered deny-all guards, 40-member immutable Git metadata proof, and strict fixture-only report truth gates.**

## Performance

- **Duration:** 10 min
- **Started:** 2026-08-27T12:54:38Z
- **Completed:** 2026-08-27T13:04:22Z
- **Tasks:** 3
- **Files modified:** 9

## Accomplishments

- Removed the inherited-descriptor startup scan and child/Git allowlist, replacing them with seven literal protected roots, 53 fixed process dispositions, and capabilities created only after guard installation.
- Added a non-Python verifier that proves exactly 40 archive members match publication commit `b0d24820720f53100d9a94174790d70b5354fa27` by mode, stage, OID, blob type, and reviewed byte size without materializing content.
- Froze one ordered 15-node safety manifest and converted report tests from live source/export/erratum reads to strict reviewed fixtures plus synthetic mutations.

## Task Commits

1. **Task 1: Install path and process denial before safe descriptor capabilities** - `68e1809`
2. **Task 2: Anchor exactly 40 archived members to immutable Git metadata** - `3d5d487`
3. **Task 3: Convert report authority checks to strict fixture-only tests** - `0fdb7bf`

## Files Created/Modified

- `tests/architecture/bootstrap/sitecustomize.py` - Startup-ordered lexical path, process, and descriptor policy.
- `tests/architecture/conftest.py` - Exact lexical bootstrap-origin and historical-import attestation.
- `tests/architecture/fixtures/phase411_guard_contract.json` - Independent seven-root and 53-symbol authority.
- `tests/architecture/fixtures/protected_authority_baseline.json` - Publication commit, exact member count, and receipt-size anchor.
- `tests/architecture/fixtures/phase411_safety_nodes.txt` - Ordered, unique 15-node final safety selection.
- `tests/architecture/support/verify_archived_source_git_metadata.ps1` - Literal-path Git metadata verifier outside the production scripts inventory.
- `tests/architecture/fixtures/report_fact_contract.json` - Strict reviewed source, export, and erratum facts.
- `tests/architecture/test_provenance_boundary.py` - Independent guard, descriptor, process, fixture, and import-boundary checks.
- `tests/architecture/test_report_handoff.py` - Fixture-only provenance, retention, handoff, policy-cycle, and mutation checks.

## Decisions Made

- The guarded interpreter can never start Python, Git, shell, multiprocessing, asyncio subprocess, or platform process primitives; Git metadata checks run only in outer PowerShell.
- Standard descriptors become capabilities only when Windows identifies them as character or pipe handles. All other capabilities originate from guarded post-install opens, pipes, duplication, or duplication-to operations.
- Protected report identities remain lexical strings in tests. Attempts are passed through the startup guard and must fail before the recorder or underlying filesystem operation.

## Deviations from Plan

The automated tracer gate passed twice and execution continued because the parent explicitly requested complete autonomous execution; the tracer required no human judgment.

## Issues Encountered

- The first combined replacement patch was rejected atomically by the patch tool because it targeted the same file twice. No partial edit occurred; the replacement was reapplied as separate atomic patches.

## Known Stubs

None. Empty collections are guard recorders and test accumulators initialized by design, not product placeholders.

## Threat Flags

None. This plan reduced filesystem, process, descriptor, and evidence trust surfaces and introduced no endpoint, authentication, model, data, or network surface.

## User Setup Required

None - no dependency, credential, external service, model, provider, or protected-data access is required.

## Next Phase Readiness

- Plan 08 and later bounded architecture tests can consume the exact 15-node fail-closed bootstrap.
- Plan 09 can run the outer Git metadata verifier before archive selectors without granting guarded Python a child-process exception.
- Report work can consume the reviewed fact fixture while preserving the frozen export and mandatory erratum boundary.

## Self-Check: PASSED

All nine implementation/test artifacts, this summary, and all three atomic task commits were verified present.

---
*Phase: 41.1-codebase-architecture-overhaul*
*Completed: 2026-08-27*
