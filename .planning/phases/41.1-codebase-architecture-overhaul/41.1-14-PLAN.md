---
phase: 41.1-codebase-architecture-overhaul
plan: 14
type: execute
wave: 14
depends_on: ["41.1-09", "41.1-13"]
files_modified:
  - src/runtime/analyzers/accelerated.py
  - src/runtime/analyzers/gguf.py
  - src/runtime/analyzers/heuristic.py
  - src/runtime/analyzers/rules.py
  - src/runtime/analyzers/local_model.py
  - tests/architecture/test_active_language_policy.py
autonomous: true
gap_closure: true
requirements: [REFACTOR-02, REFACTOR-03, REFACTOR-05]
estimate:
  tokens: 19000
  raw_tokens: 19000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "All five active analyzers explain their phishing-risk responsibilities in domain language."
    - "Accelerated/GGUF setup commands and selected-artifact semantics remain byte-exact."
    - "The Plan 08 pre-edit fixture proves imports, signatures, calls, control flow, and non-prose strings did not change."
  artifacts:
    - path: "src/runtime/analyzers"
      provides: "Domain-readable backend and rule analyzer descriptions"
    - path: "tests/architecture/test_active_language_policy.py"
      provides: "Exact backend/rule analyzer static language nodes"
  key_links:
    - from: "runtime_prose_contract.json"
      to: "five analyzer files"
      via: "immutable AST fingerprints and separately recorded setup commands"
  prohibitions:
    - "Do not import analyzers or execute a runtime/model/backend/training/conversion/provider path."
    - "Do not regenerate the Plan 08 fixture or change setup/registration command literals."
    - "Do not decompose existing local_model size debt in this wording slice."
    - "Do not access protected authorities, data rows, D-drive, ProgramData, or models."
---

<objective>
Finish the analyzer wording slice after archive extraction and before final graph closure.

Purpose: Make defense-facing analyzer code readable while preserving D-01/D-02 behavior and the immutable Plan 08 characterization.
Output: Domain prose in five analyzers and exact static regression nodes.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-CONTEXT.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-08-SUMMARY.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-09-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Rewrite accelerated and GGUF guidance under exact command fingerprints</name>
  <files>src/runtime/analyzers/accelerated.py, src/runtime/analyzers/gguf.py, tests/architecture/test_active_language_policy.py</files>
  <action>
    Rewrite only module/help prose as optional GPU-accelerated local analysis and laptop GGUF analysis. Preserve the exact pip/extras/registration commands, selected artifact behavior, errors, imports, signatures, calls, and non-approved strings stored by Plan 08. Add `test_active_backend_analyzer_language_is_domain_named` over exactly these two raw source files; retain the import blocker and unchanged fingerprint node per D-01/D-04.
  </action>
  <verify>
    <automated>$env:PYTHONPATH=(Resolve-Path 'tests/architecture/bootstrap').Path; $phase411Temp=Join-Path ([System.IO.Path]::GetTempPath()) ('vnphish-411-'+[guid]::NewGuid().ToString('N')); $phase411Base=Join-Path $phase411Temp 'pytest'; New-Item -ItemType Directory -Path $phase411Base -Force | Out-Null; $env:PHASE411_DENY_OPEN_SENTINEL='1'; $env:PHASE411_REQUIRE_HISTORICAL_ARCHIVE='0'; $env:PYTHONNOUSERSITE='1'; $env:DATA_DIR=Join-Path $phase411Temp 'data'; $env:MODEL_ARTIFACT_ROOT=Join-Path $phase411Temp 'models'; $env:MODEL_REGISTRY_PATH=Join-Path $phase411Temp 'registry.json'; $env:TEMP=$phase411Temp; $env:TMP=$phase411Temp; $python=(Get-Command python -CommandType Application).Source; &amp; $python -m pytest -q -p no:cacheprovider --basetemp $phase411Base tests/architecture/test_active_language_policy.py::test_static_language_suite_blocks_execution_imports tests/architecture/test_active_language_policy.py::test_runtime_prose_behavior_fingerprints_match_characterization tests/architecture/test_active_language_policy.py::test_active_backend_analyzer_language_is_domain_named</automated>
  </verify>
  <done>Backend analyzer guidance is domain-readable and command/executable structure remains exact.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite heuristic, rules, and selected-model helper prose</name>
  <files>src/runtime/analyzers/heuristic.py, src/runtime/analyzers/rules.py, src/runtime/analyzers/local_model.py, tests/architecture/test_active_language_policy.py</files>
  <action>
    Rewrite only chronology-bearing prose as phishing-risk heuristics, weighted scam cues, selected local-model helpers, and reusable recommendation-safety logic. Add `test_active_rule_analyzer_language_is_domain_named` over exactly these three files. Preserve imports, signatures, selected-artifact semantics, calls, control flow, errors, and all non-approved strings in the immutable fixture. Keep the existing oversized local_model module as declared non-owned debt for Plan 10; do not mask it as compliant or decompose it here.
  </action>
  <verify>
    <automated>$env:PYTHONPATH=(Resolve-Path 'tests/architecture/bootstrap').Path; $phase411Temp=Join-Path ([System.IO.Path]::GetTempPath()) ('vnphish-411-'+[guid]::NewGuid().ToString('N')); $phase411Base=Join-Path $phase411Temp 'pytest'; New-Item -ItemType Directory -Path $phase411Base -Force | Out-Null; $env:PHASE411_DENY_OPEN_SENTINEL='1'; $env:PHASE411_REQUIRE_HISTORICAL_ARCHIVE='0'; $env:PYTHONNOUSERSITE='1'; $env:DATA_DIR=Join-Path $phase411Temp 'data'; $env:MODEL_ARTIFACT_ROOT=Join-Path $phase411Temp 'models'; $env:MODEL_REGISTRY_PATH=Join-Path $phase411Temp 'registry.json'; $env:TEMP=$phase411Temp; $env:TMP=$phase411Temp; $python=(Get-Command python -CommandType Application).Source; &amp; $python -m pytest -q -p no:cacheprovider --basetemp $phase411Base tests/architecture/test_active_language_policy.py::test_runtime_prose_behavior_fingerprints_match_characterization tests/architecture/test_active_language_policy.py::test_active_rule_analyzer_language_is_domain_named</automated>
  </verify>
  <done>Rule/selected-model analyzers use domain prose with exact non-prose compatibility.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Human guidance -> setup commands | Prose rewrites must not alter actionable command bytes |
| Analyzer source -> executable behavior | Static AST fingerprints must prevent logic drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-41.1-14-01 | Tampering | Analyzer commands/logic | high | mitigate | Exact setup literals and immutable AST fingerprints |
| T-41.1-14-02 | Repudiation | Existing size debt | medium | mitigate | Preserve explicit debt for Plan 10 rather than claiming compliance |
| T-41.1-14-03 | Information Disclosure | Static verification | high | mitigate | Bootstrap guard, raw source only, execution-import blocker |
</threat_model>

<verification>
Run only the exact import-free analyzer nodes with the Plan 11 bootstrap installed. No analyzer or backend is imported.
</verification>

<success_criteria>
- Five analyzers use domain language.
- Setup/registration commands and behavior fingerprints remain exact.
- Existing local_model size debt remains separately declared for final policy validation.
</success_criteria>

<output>
Create `.planning/phases/41.1-codebase-architecture-overhaul/41.1-14-SUMMARY.md` when done.
</output>
