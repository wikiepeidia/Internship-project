---
phase: 41.1-codebase-architecture-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/archive_phase41_source_closure.py
  - tests/architecture/conftest.py
  - tests/architecture/test_provenance_boundary.py
  - tests/architecture/test_contract_baselines.py
  - tests/architecture/fixtures/model_cli_contract.json
  - tests/architecture/fixtures/runtime_cli_contract.json
  - tests/architecture/fixtures/serialization_contract.json
  - historical/phase41-source-closure/c3bbc8c8adaf7579fd2eb9c59a0081613be4b2cae05dfdb64472938c7e6d0434/execution-source-manifest.json
  - historical/phase41-source-closure/c3bbc8c8adaf7579fd2eb9c59a0081613be4b2cae05dfdb64472938c7e6d0434/archival-receipt.json
  - historical/phase41-source-closure/c3bbc8c8adaf7579fd2eb9c59a0081613be4b2cae05dfdb64472938c7e6d0434/tree/**
autonomous: true
requirements:
  - REFACTOR-01
  - REFACTOR-05
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The tracked historical mirror contains exactly the 37 manifest-listed source files and launcher bytes that produced Phase 41, sourced from the matching ProgramData clean runtime rather than current HEAD."
    - "The receipt identifies the mirror as post-evaluation archival evidence and records that current HEAD is not the frozen producer."
    - "Synthetic-only architecture tests fail immediately if any code tries to touch the reserved split directory or falls back to live model/data roots."
    - "CLI, enum, schema-version, and canonical-byte baselines exist before any source extraction begins."
  artifacts:
    - path: "scripts/archive_phase41_source_closure.py"
      provides: "Fixed-root, exact-member, capture-once archival and verification command"
    - path: "historical/phase41-source-closure/c3bbc8c8adaf7579fd2eb9c59a0081613be4b2cae05dfdb64472938c7e6d0434/archival-receipt.json"
      provides: "Hash-bound provenance statement for the post-evaluation source mirror"
    - path: "tests/architecture/conftest.py"
      provides: "Autouse temp-root isolation and lexical deny-open sentinel"
  key_links:
    - from: "data/models/phase41/verified-export/9ac54d58c273ab0a8c2f2b4b61e472a51ca94231a94b6847637ecad6ceee49f7/execution-source-manifest.json"
      to: "historical/phase41-source-closure/c3bbc8c8adaf7579fd2eb9c59a0081613be4b2cae05dfdb64472938c7e6d0434/tree/"
      via: "exact manifest membership and SHA-256 verification of captured bytes"
      pattern: "phase41-execution-source-manifest-v1|c3bbc8c8"
  prohibitions:
    - "Do not enumerate, stat, hash, open, copy, or resolve the reserved held-out split or its containing data/splits directory."
    - "Do not import or invoke the Phase 41 evaluator, rerun evaluation/training/inference, or load any real model binary."
    - "Do not write, reserialize, regenerate, or reseal ProgramData evidence, the verified export, provenance erratum, source manifest, model roots, or frozen metrics."
    - "Do not delete, move, trim, or relink anything on D:."
---

<objective>
Preserve the exact Phase 41 producer source before refactoring and establish the synthetic-only compatibility/safety harness.

Purpose: Prevent false provenance and catch contract or forbidden-data drift before later migration slices.
Output: A content-addressed historical mirror, immutable receipt, contract snapshots, and bounded architecture-test harness.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-CONTEXT.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-RESEARCH.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-PATTERNS.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-STORAGE-INVENTORY.md
@data/models/phase41/verified-export/9ac54d58c273ab0a8c2f2b4b61e472a51ca94231a94b6847637ecad6ceee49f7/execution-source-manifest.json
@data/models/phase41/phase41-provenance-erratum.json
@src/model_adaptation/phase40_production_authorities.py
</context>

## Artifacts this phase produces

- `scripts/archive_phase41_source_closure.py`: `ArchiveError`, `ArchiveReceipt`, `archive_bound_source_closure()`, `verify_archived_source_closure()`, `main()`.
- `tests/architecture/conftest.py`: autouse `isolated_phase411_environment` and lexical filesystem-operation sentinel.
- `tests/architecture/test_provenance_boundary.py`, `tests/architecture/test_contract_baselines.py`.
- `tests/architecture/fixtures/model_cli_contract.json`, `runtime_cli_contract.json`, `serialization_contract.json`.
- Archive metadata: `execution-source-manifest.json`, `archival-receipt.json`.
- Archive tree paths (exactly): `tree/src/__init__.py`, `tree/src/config/__init__.py`, `tree/src/config/settings.py`, `tree/src/data_pipeline/__init__.py`, `tree/src/data_pipeline/schemas.py`, `tree/src/model_adaptation/__init__.py`, `tree/src/model_adaptation/catalog.py`, `tree/src/model_adaptation/cli.py`, `tree/src/model_adaptation/convert.py`, `tree/src/model_adaptation/data.py`, `tree/src/model_adaptation/doctor.py`, `tree/src/model_adaptation/phase40_callbacks.py`, `tree/src/model_adaptation/phase40_comparison_launch.py`, `tree/src/model_adaptation/phase40_contract.py`, `tree/src/model_adaptation/phase40_evidence.py`, `tree/src/model_adaptation/phase40_final_authority.py`, `tree/src/model_adaptation/phase40_gguf.py`, `tree/src/model_adaptation/phase40_graphs.py`, `tree/src/model_adaptation/phase40_handoff.py`, `tree/src/model_adaptation/phase40_metrics.py`, `tree/src/model_adaptation/phase40_modes.py`, `tree/src/model_adaptation/phase40_notebooks.py`, `tree/src/model_adaptation/phase40_phobert_release.py`, `tree/src/model_adaptation/phase40_production_authorities.py`, `tree/src/model_adaptation/phase40_release_authorities.py`, `tree/src/model_adaptation/phase40_review.py`, `tree/src/model_adaptation/phase40_runtime_materialize.py`, `tree/src/model_adaptation/phase41_evaluation.py`, `tree/src/model_adaptation/phase41_protocols.py`, `tree/src/model_adaptation/phobert_training.py`, `tree/src/model_adaptation/pilot.py`, `tree/src/model_adaptation/prompts.py`, `tree/src/model_adaptation/registry.py`, `tree/src/model_adaptation/schemas.py`, `tree/src/model_adaptation/training.py`, `tree/src/runtime/__init__.py`, `tree/src/runtime/contracts.py`, and `tree/scripts/phase41_one_shot_launcher.ps1`.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one synthetic source-closure archive end to end</name>
  <read_first>
    - `.planning/phases/41.1-codebase-architecture-overhaul/41.1-CONTEXT.md`
    - `src/model_adaptation/phase40_production_authorities.py`
    - `data/models/phase41/verified-export/9ac54d58c273ab0a8c2f2b4b61e472a51ca94231a94b6847637ecad6ceee49f7/execution-source-manifest.json`
    - `data/models/phase41/phase41-provenance-erratum.json`
    - `NEW: scripts/archive_phase41_source_closure.py`
    - `NEW: tests/architecture/conftest.py`
    - `NEW: tests/architecture/test_provenance_boundary.py`
  </read_first>
  <files>scripts/archive_phase41_source_closure.py, tests/architecture/conftest.py, tests/architecture/test_provenance_boundary.py</files>
  <behavior>
    - A miniature manifest-bound clean-runtime fixture is captured, archived, receipted, and independently verified through the production API under `tmp_path`.
    - Production mode accepts no caller-selected source/destination and reads only manifest-listed members under the fixed ProgramData clean-runtime plus the fixed launcher.
    - Missing/extra manifest entries, duplicate JSON keys, wrong schema/tree hash, source mismatch, redirecting ancestry, path escape, destination collision, or source/destination overlap fails before publication.
    - Each source is captured once, the captured bytes are hashed and exclusively written, and destination bytes are rehashed before the receipt is created last.
    - With `PHASE411_DENY_OPEN_SENTINEL=1`, an autouse lexical sentinel rejects open/read/stat/exists/list/glob/resolve operations beneath the reserved split prefix before the real filesystem sees them; it configures itself without probing that prefix.
  </behavior>
  <action>Start with a failing end-to-end synthetic archive test, then implement the fixed production constants for the frozen manifest, `C:\ProgramData\VNPhish\phase41-evaluation-evidence\clean-runtime`, and the content-addressed historical destination. Parse JSON strictly, require the exact schema string captured in the frozen manifest, tree SHA `c3bbc8c8adaf7579fd2eb9c59a0081613be4b2cae05dfdb64472938c7e6d0434`, 37 files, and launcher SHA `c5f15a32b2c8d8ee196e3ec484707c27c4c05e5389d958626e775e44f52d49e9`. Copy the manifest as raw bytes. The canonical receipt records manifest SHA, closure SHA, launcher SHA, origin, UTC archive time, exact destination, the two current-worktree mismatches, and `post_evaluation_archival_mirror_not_refactored_metric_producer`. Expose temp-root injection only through a private test seam. Keep the new script at most 600 lines and each new function at most 100 lines unless the summary records an exact compatibility exception.</action>
  <verify><automated>$env:PHASE411_DENY_OPEN_SENTINEL='1'; $env:DATA_DIR=Join-Path $env:TEMP 'vnphish-411-p01-data'; $env:MODEL_ARTIFACT_ROOT=Join-Path $env:TEMP 'vnphish-411-p01-models'; $env:MODEL_REGISTRY_PATH=Join-Path $env:TEMP 'vnphish-411-p01-registry.json'; python -m pytest tests/architecture/test_provenance_boundary.py -q --basetemp .tmp/pytest-411-p01-provenance</automated></verify>
  <acceptance_criteria>The synthetic happy path archives and re-verifies one exact closure; adversarial copy tests pass; the sentinel proves blocked calls never reach the filesystem; production roots are immutable literals; no recursive discovery or overwrite route exists; size budgets pass or an exact exception is recorded.</acceptance_criteria>
  <done>A production-quality archive path is proven end to end without touching ProgramData, held-out data, evidence exports, or models.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze observable compatibility and byte baselines</name>
  <read_first>
    - `.planning/phases/41.1-codebase-architecture-overhaul/41.1-CONTEXT.md`
    - `src/model_adaptation/cli.py`
    - `src/runtime/cli.py`
    - `src/runtime/contracts.py`
    - `src/data_pipeline/schemas.py`
    - `tests/model_adaptation/test_cli.py`
    - `tests/architecture/conftest.py`
    - `NEW: tests/architecture/test_contract_baselines.py`
    - `NEW: tests/architecture/fixtures/model_cli_contract.json`
    - `NEW: tests/architecture/fixtures/runtime_cli_contract.json`
    - `NEW: tests/architecture/fixtures/serialization_contract.json`
  </read_first>
  <files>tests/architecture/test_contract_baselines.py, tests/architecture/fixtures/model_cli_contract.json, tests/architecture/fixtures/runtime_cli_contract.json, tests/architecture/fixtures/serialization_contract.json</files>
  <behavior>
    - The model fixture records all 23 commands and complete argparse actions; the runtime fixture records exactly `analyze`, `doctor`, and `demo`.
    - The model baseline includes raw argv, safe stderr, caught exception-to-exit-code behavior, and the current required `doctor --adaptation-mode` quirk.
    - The serialization fixture covers Vietnamese Unicode, exact risk/label enums, schema strings, key order, artifact names, and newline policy without reading corpus files.
    - Two deterministic generations of every fixture are byte-identical while every root remains under `tmp_path`.
  </behavior>
  <action>Build fixtures deterministically from parser objects and code-fixed synthetic values, review them against the current contracts, and commit them as the before-state. Invoke no handler that can train, infer, evaluate, open a model, or read a live artifact. Retain environment names and every machine-readable byte/schema/artifact identity used by later compatibility tests.</action>
  <verify><automated>$env:PHASE411_DENY_OPEN_SENTINEL='1'; $env:DATA_DIR=Join-Path $env:TEMP 'vnphish-411-p01-data'; $env:MODEL_ARTIFACT_ROOT=Join-Path $env:TEMP 'vnphish-411-p01-models'; $env:MODEL_REGISTRY_PATH=Join-Path $env:TEMP 'vnphish-411-p01-registry.json'; python -m pytest tests/architecture/test_contract_baselines.py -q --basetemp .tmp/pytest-411-p01-contracts</automated></verify>
  <acceptance_criteria>The exact contract fixtures are stable on two runs, cover all named compatibility identities, and collect only the named architecture test under the active deny-open sentinel.</acceptance_criteria>
  <done>Compatibility and byte baselines exist before any producer-closure member is refactored.</done>
</task>

<task type="auto">
  <name>Task 3: Materialize and independently verify the historical mirror</name>
  <precondition>The exact ProgramData clean-runtime exists and all 37 source hashes plus the launcher hash match the frozen manifest; otherwise stop without substituting current HEAD.</precondition>
  <read_first>
    - `scripts/archive_phase41_source_closure.py`
    - `data/models/phase41/verified-export/9ac54d58c273ab0a8c2f2b4b61e472a51ca94231a94b6847637ecad6ceee49f7/execution-source-manifest.json`
    - `NEW: historical/phase41-source-closure/c3bbc8c8adaf7579fd2eb9c59a0081613be4b2cae05dfdb64472938c7e6d0434/**`
  </read_first>
  <files>historical/phase41-source-closure/c3bbc8c8adaf7579fd2eb9c59a0081613be4b2cae05dfdb64472938c7e6d0434/execution-source-manifest.json, historical/phase41-source-closure/c3bbc8c8adaf7579fd2eb9c59a0081613be4b2cae05dfdb64472938c7e6d0434/archival-receipt.json, historical/phase41-source-closure/c3bbc8c8adaf7579fd2eb9c59a0081613be4b2cae05dfdb64472938c7e6d0434/tree/**</files>
  <action>Run the fixed `archive` command once, then the read-only `verify` command twice. Confirm the manifest copy is byte-identical, every listed member and launcher matches, no extra tree member exists, and receipt verification does not consult current HEAD. Confirm the verified export and erratum show no Git diff and the script wrote nowhere outside the exact historical destination.</action>
  <verify><automated>$env:PHASE411_DENY_OPEN_SENTINEL='1'; $env:DATA_DIR=Join-Path $env:TEMP 'vnphish-411-p01-data'; $env:MODEL_ARTIFACT_ROOT=Join-Path $env:TEMP 'vnphish-411-p01-models'; $env:MODEL_REGISTRY_PATH=Join-Path $env:TEMP 'vnphish-411-p01-registry.json'; python -m pytest tests/architecture/test_provenance_boundary.py -q --basetemp .tmp/pytest-411-p01-materialize; python scripts/archive_phase41_source_closure.py verify; python scripts/archive_phase41_source_closure.py verify; git diff --exit-code -- data/models/phase41/verified-export data/models/phase41/phase41-provenance-erratum.json</automated></verify>
  <acceptance_criteria>The archive has exactly 38 payload files plus raw manifest and receipt; verification is byte-stable; source tree SHA is exactly `c3bbc8c8...`; frozen authorities remain unchanged.</acceptance_criteria>
  <done>The historical producer bytes are safely preserved and truthfully labeled before Wave 2 edits source-closure members.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Frozen repo manifest -> ProgramData clean runtime | Untrusted filesystem state must match an explicit immutable membership list. |
| Captured source bytes -> tracked historical mirror | Redirects, races, overwrites, and provenance relabeling must fail closed. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-411-01-01 | Repudiation | source provenance | high | mitigate | Exact tree/launcher hashes, current-HEAD mismatch record, and explicit post-evaluation origin label. |
| T-411-01-02 | Tampering | ProgramData copy | high | mitigate | Fixed roots, explicit members, capture-once bytes, exclusive destination, and receipt-last verification. |
| T-411-01-03 | Tampering | reparse/TOCTOU | high | mitigate | Reject redirecting ancestry, validate descendants, capture once, rehash destination, and adversarial tests. |
| T-411-01-04 | Elevation of privilege | later lazy imports | high | mitigate | Baseline the closed 23-command surface now; Wave 2 permits only static route literals. |
| T-411-01-05 | Tampering | frozen evidence | high | mitigate | Separate destination plus no-write tests and Git diff gate for export/erratum. |
| T-411-01-06 | Information disclosure | settings secrets | high | mitigate | Temp environment roots and no Settings serialization/logging in tests. |
| T-411-01-07 | Information disclosure | held-out data | high | mitigate | Autouse lexical deny-open sentinel and exact architecture-only test selection. |
| T-411-01-08 | Denial of service | D-drive models | high | mitigate | No D-drive write/delete code or command; storage inventory is read-only planning input. |
</threat_model>

<verification>
Only the two exact architecture-test files and the archive verifier may run. No model/evaluator command, broad pytest selection, data split access, or storage cleanup is permitted.
</verification>

<success_criteria>
- The exact historical producer closure is tracked and independently verifiable.
- Current HEAD is explicitly not represented as the source of frozen metrics.
- All later plans inherit synthetic-only contract and forbidden-access guards.
</success_criteria>

<output>Create `.planning/phases/41.1-codebase-architecture-overhaul/41.1-01-SUMMARY.md` when done.</output>
