---
phase: 41.1-codebase-architecture-overhaul
plan: 05
type: execute
wave: 5
depends_on: ["41.1-04"]
files_modified:
  - src/data_pipeline/core/__init__.py
  - src/data_pipeline/core/records.py
  - src/data_pipeline/core/text.py
  - src/data_pipeline/core/splits.py
  - src/data_pipeline/workflows.py
  - src/data_pipeline/migrations.py
  - src/data_pipeline/schemas.py
  - src/data_pipeline/cli.py
  - architecture/module-boundaries.json
  - tests/architecture/test_data_core_contract.py
  - tests/architecture/test_data_cli_contract.py
  - tests/architecture/test_data_migration_shims.py
autonomous: true
requirements: [REFACTOR-04, REFACTOR-05]
estimate: {tokens: 43000, raw_tokens: 43000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Reusable record, normalization, deduplication, split, and versioning APIs live under a phase-neutral data core with exact old-path compatibility."
    - "Generation/judging/review orchestration is a workflow boundary and does not enter data-core imports."
    - "The five named one-off repair tools are explicitly catalogued as migrations with phase-neutral names while their original modules and entrypoints remain callable."
    - "The existing flag-based data CLI remains behavior-compatible but help/parser import does not load providers, judge, embeddings, or migrations."
  artifacts:
    - {path: "src/data_pipeline/core/", provides: "Reusable deterministic data contracts and transforms"}
    - {path: "src/data_pipeline/workflows.py", provides: "Generation/judge/review orchestration owner"}
    - {path: "src/data_pipeline/migrations.py", provides: "Closed, lazy catalog of one-off transformations"}
  key_links:
    - {from: "src/data_pipeline/cli.py", to: "src/data_pipeline/workflows.py", via: "thin flag parser and lazy mode dispatch", pattern: "run_phase1|judge_existing|salvage|optimize"}
    - {from: "src/data_pipeline/migrations.py", to: "old repair modules", via: "literal MigrationSpec entries and handler-local imports", pattern: "MIGRATIONS|load_migration"}
  prohibitions:
    - "Never enumerate, stat, hash, open, copy, or resolve the reserved held-out split, its containing data/splits directory, or any live corpus path."
    - "Never import or invoke the Phase 41 evaluator, rerun evaluation/training/inference, call a provider, or load a real model binary."
    - "Never mutate ProgramData, the verified export, provenance erratum, source manifest, frozen metrics, historical source mirror, or any sealed model root."
    - "Never delete, move, trim, or relink anything on D:."
    - "No relabel, repair, split regeneration, metric recomputation, or migration execution on project data."
---

<objective>
Separate reusable data logic, provider/review workflows, and one-off migrations while preserving old records, modules, and CLI behavior.

Purpose: Replace emergency-script chronology in the active architecture with understandable data responsibilities without rewriting historical transformations.
Output: Data core, workflow owner, migration catalog, thin legacy CLI, compatibility shims, and synthetic tests.
</objective>
<execution_context>
@C:/Users/wikiepeidia/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-CONTEXT.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-PATTERNS.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-04-SUMMARY.md
@architecture/module-boundaries.json
@tests/architecture/fixtures/protected_authority_baseline.json
</context>

## Artifacts this phase produces

- `src/data_pipeline/core/__init__.py`.
- `core/records.py`: explicit dataset/seed schema owners and compatibility exports.
- `core/text.py`: `normalize_text()`, deterministic deduplication facade, and related typed results.
- `core/splits.py`: group split and manifest/versioning facade APIs.
- `workflows.py`: `judge_existing_records()`, `salvage_partial_records()`, `optimize_recovered_records()`, `run_phase1()`.
- `migrations.py`: `MigrationSpec`, `MIGRATIONS`, `get_migration()`, `load_migration_entrypoint()`.
- `tests/architecture/test_data_core_contract.py`, `test_data_cli_contract.py`, `test_data_migration_shims.py`.

## Cohesion justification

The file count reflects three complete compatibility slices—record core, workflow dispatch, and migration catalog—each paired with its existing old-path owner and exact test. Separating a new owner from its shim/CLI consumer would create an unverified intermediate import path; the five repair implementations themselves remain byte-unchanged and are referenced only by mechanical literal catalog entries.

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Trace one record through the phase-neutral core and old-path shim</name>
  <read_first>
    - `src/data_pipeline/schemas.py`
    - `src/runtime/contracts.py`
    - `tests/data_pipeline/test_schemas.py`
    - `NEW: src/data_pipeline/core/__init__.py`
    - `NEW: src/data_pipeline/core/records.py`
    - `NEW: tests/architecture/test_data_core_contract.py`
  </read_first>
  <files>src/data_pipeline/core/__init__.py, src/data_pipeline/core/records.py, src/data_pipeline/schemas.py, tests/architecture/test_data_core_contract.py</files>
  <behavior>
    - Old and new imports construct identical records and emit identical seven-field JSON bytes, validation errors, enums, defaults, and field order.
    - Suspicious spans and Vietnamese Unicode behavior remain exact on synthetic fixtures.
    - Old `src.data_pipeline.schemas` contains explicit named compatibility imports only, with no wildcard or dynamic fallback.
  </behavior>
  <action>Move the reusable seed/dataset record definitions and directly required aliases/constants into `core.records`; make the old schema module an explicit compatibility surface for every observed public symbol. Use the frozen serialization fixture and caller search before removing any definition. Do not alter schema strings, source enums, risk/label values, default spans, validators, or canonical encoding.</action>
  <verify><automated>$env:PYTHONPATH=(Resolve-Path 'tests/architecture/bootstrap').Path; $env:PHASE411_DENY_OPEN_SENTINEL='1'; $env:PHASE411_REQUIRE_HISTORICAL_ARCHIVE='1'; $env:DATA_DIR=Join-Path $env:TEMP 'vnphish-411-p05-data'; $env:MODEL_ARTIFACT_ROOT=Join-Path $env:TEMP 'vnphish-411-p05-models'; $env:MODEL_REGISTRY_PATH=Join-Path $env:TEMP 'vnphish-411-p05-registry.json'; python -m pytest tests/architecture/test_data_core_contract.py tests/architecture/test_provenance_boundary.py::test_protected_authorities_match_fixed_baseline -q --basetemp .tmp/pytest-411-p05-records; if ($LASTEXITCODE -ne 0) { throw 'Plan 05 Task 1 verification failed' }</automated></verify>
  <acceptance_criteria>Every old/new symbol pair and golden byte/error fixture matches; old imports still resolve; no live file is opened.</acceptance_criteria>
  <done>Reusable records have a neutral owner with an exact old-path shim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Separate pure transforms from generation and review workflows</name>
  <read_first>
    - `src/data_pipeline/processing/normalizer.py`
    - `src/data_pipeline/processing/dedup.py`
    - `src/data_pipeline/processing/splitter.py`
    - `src/data_pipeline/versioning/manifest.py`
    - `src/data_pipeline/cli.py`
    - `tests/data_pipeline/test_phase1_cli.py`
    - `NEW: src/data_pipeline/core/text.py`
    - `NEW: src/data_pipeline/core/splits.py`
    - `NEW: src/data_pipeline/workflows.py`
    - `NEW: tests/architecture/test_data_cli_contract.py`
  </read_first>
  <files>src/data_pipeline/core/text.py, src/data_pipeline/core/splits.py, src/data_pipeline/workflows.py, src/data_pipeline/cli.py, tests/architecture/test_data_cli_contract.py</files>
  <behavior>
    - Core imports contain no provider, scraper, judge, CLI, migration, model, or live-path dependency.
    - The current 16-option flag grammar, mode precedence, stdout JSON, stderr progress, and exit codes match a captured parser/handler-double baseline.
    - CLI help imports no Anthropic/Google/httpx/playwright/sentence-transformers module.
    - Synthetic normalize/dedup/split/versioning cases equal their old implementation results.
  </behavior>
  <action>Expose deterministic normalizer/dedup/split/versioning functions through the two core modules, retaining existing processing/versioning paths as compatibility owners or explicit shims based on caller search. Move `judge_existing_records`, `salvage_partial_records`, `optimize_recovered_records`, and `run_phase1` into `workflows.py`; all provider/judge/scraper imports must be local to the invoked workflow. Reduce `cli.py` to parser, progress/error boundary, small compatibility forwards, and dispatch while preserving all existing flags and monkeypatch seams one family at a time.</action>
  <verify><automated>$env:PYTHONPATH=(Resolve-Path 'tests/architecture/bootstrap').Path; $env:PHASE411_DENY_OPEN_SENTINEL='1'; $env:PHASE411_REQUIRE_HISTORICAL_ARCHIVE='1'; $env:DATA_DIR=Join-Path $env:TEMP 'vnphish-411-p05-data'; $env:MODEL_ARTIFACT_ROOT=Join-Path $env:TEMP 'vnphish-411-p05-models'; $env:MODEL_REGISTRY_PATH=Join-Path $env:TEMP 'vnphish-411-p05-registry.json'; python -m pytest tests/architecture/test_data_core_contract.py tests/architecture/test_data_cli_contract.py tests/architecture/test_provenance_boundary.py::test_protected_authorities_match_fixed_baseline -q --basetemp .tmp/pytest-411-p05-workflows; if ($LASTEXITCODE -ne 0) { throw 'Plan 05 Task 2 verification failed' }</automated></verify>
  <acceptance_criteria>Core outputs and CLI contract match synthetic baselines; help is optional-dependency-free; active modules/functions meet 600/100 budgets.</acceptance_criteria>
  <done>Reusable transforms and external workflows have separate, tested ownership.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Classify one-off repair tools as lazy migrations</name>
  <read_first>
    - `src/data_pipeline/apply_task_scam_risk_tier_repair.py`
    - `src/data_pipeline/repair_zalo_narrator_scaffold.py`
    - `src/data_pipeline/reconstruct_zalo_direct_catalog.py`
    - `src/data_pipeline/apply_mislabel_triage.py`
    - `src/data_pipeline/repair_corpus_split_governance.py`
    - `architecture/module-boundaries.json`
    - `NEW: src/data_pipeline/migrations.py`
    - `NEW: tests/architecture/test_data_migration_shims.py`
  </read_first>
  <files>src/data_pipeline/migrations.py, architecture/module-boundaries.json, tests/architecture/test_data_migration_shims.py</files>
  <behavior>
    - Five literal phase-neutral migration IDs resolve to the exact old module/main symbols only after explicit selection.
    - Old import paths, functions, `main()` entrypoints, fail-closed validation, and atomic-write behavior remain unchanged under temp synthetic fixtures.
    - Unknown migration IDs fail without importing any repair module.
    - Migration classification is historical/workflow-only and no active runtime/modeling module imports it.
  </behavior>
  <action>Create a frozen catalog for `task-scam-risk-tier`, `zalo-narrator-extraction`, `zalo-direct-reconstruction`, `mislabel-triage`, and `corpus-split-governance`. Each `MigrationSpec` contains the literal old module, entry symbol, purpose, and provenance status; `load_migration_entrypoint()` imports only the selected literal. Do not move or edit the five large historical implementations and do not execute them. Extend the closed module classification so reports distinguish auditable migrations from application services.</action>
  <verify><automated>$env:PYTHONPATH=(Resolve-Path 'tests/architecture/bootstrap').Path; $env:PHASE411_DENY_OPEN_SENTINEL='1'; $env:PHASE411_REQUIRE_HISTORICAL_ARCHIVE='1'; $env:DATA_DIR=Join-Path $env:TEMP 'vnphish-411-p05-data'; $env:MODEL_ARTIFACT_ROOT=Join-Path $env:TEMP 'vnphish-411-p05-models'; $env:MODEL_REGISTRY_PATH=Join-Path $env:TEMP 'vnphish-411-p05-registry.json'; python -m pytest tests/architecture/test_data_migration_shims.py tests/architecture/test_import_boundaries.py tests/architecture/test_provenance_boundary.py::test_protected_authorities_match_fixed_baseline -q --basetemp .tmp/pytest-411-p05-migrations; if ($LASTEXITCODE -ne 0) { throw 'Plan 05 Task 3 verification failed' }</automated></verify>
  <acceptance_criteria>Catalog IDs/routes are exact and lazy, old modules are byte-unchanged, unknown routes import nothing, and graph policy passes.</acceptance_criteria>
  <done>Emergency repairs are traceable migrations, not forward-facing services, with every old path preserved.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Synthetic records -> core transforms | Strict schemas and deterministic functions must reject malformed data without partial writes. |
| CLI/migration ID -> external workflow | Selection may trigger only a literal reviewed route and never during tests. |
## STRIDE Threat Register (ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-411-05-01 | Repudiation | repair provenance | high | mitigate | Explicit migration catalog and preserved original module/source archive. |
| T-411-05-02 | Tampering | ProgramData closure | high | mitigate | No ProgramData access; archive is read-only. |
| T-411-05-03 | Tampering | reparse/TOCTOU | high | mitigate | Tests use temp files and inherited bounded writers; no production migration run. |
| T-411-05-04 | Elevation of privilege | lazy routes | high | mitigate | Frozen literal catalog, unknown-ID failure, import-spy tests. |
| T-411-05-05 | Tampering | frozen evidence | high | mitigate | Repair implementations stay byte-unchanged; every task checks fixed worktree/index/baseline-commit/historical identities. |
| T-411-05-06 | Information disclosure | provider secrets | high | mitigate | Help/parser imports no provider; workflow tests use doubles and dummy env. |
| T-411-05-07 | Information disclosure | held-out/live corpus | high | mitigate | Deny-open sentinel, temp fixtures, no migration/workflow execution against defaults. |
| T-411-05-08 | Denial of service | D-drive | high | mitigate | Data layer contains no D-drive cleanup function or command. |
</threat_model>
<verification>Only exact architecture tests may run; all records/paths/providers are synthetic doubles. No project corpus path or repair CLI is invoked.</verification>
<success_criteria>
- Data core, workflows, and migrations are visibly separate.
- Existing schemas, flags, outputs, and old repair paths remain compatible.
- No live dataset, model, evaluation, evidence, or D-drive artifact is touched.
</success_criteria>
<output>Create `.planning/phases/41.1-codebase-architecture-overhaul/41.1-05-SUMMARY.md` when done.</output>
