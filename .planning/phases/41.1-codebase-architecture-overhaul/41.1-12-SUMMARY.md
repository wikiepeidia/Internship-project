---
phase: 41.1-codebase-architecture-overhaul
plan: 12
subsystem: user-documentation
tags: [domain-language, compatibility-markers, local-runtime, local-models]
requires:
  - phase: 41.1-codebase-architecture-overhaul
    provides: Plan 07 exact active-document ownership and Plan 08 guarded chronology scanner
provides:
  - Domain-readable repository entry path for runtime, model profiles, demo, and dataset operations
  - Phase-neutral runtime and local-model user guidance
  - Exact marker-scoped preservation of legacy data and model command version tags
affects: [41.1-13, phase-42-report, phase-44-defense-walkthrough]
actuals:
  tokens: 5091
  tasks: 2
  commits: 2
tech-stack:
  added: []
  patterns: [domain-first-documentation, exact-command-marker-regions, visible-documentation-debt]
key-files:
  created: []
  modified:
    - readme.md
    - documents/user/USER_GUIDE.md
    - documents/user/LOCAL_MODELS.md
    - tests/architecture/test_active_language_policy.py
key-decisions:
  - "Keep legacy version tags byte-exact inside one command-only marker region per document."
  - "Keep QLORA.md visibly inventoried and explicitly uncertified until later documentation work."
patterns-established:
  - "Active user prose explains domain responsibilities; chronology remains only in exact compatibility command blocks."
  - "Documentation tests compare policy to the independent fixture and reject moved tokens or prose hidden inside compatibility regions."
requirements-completed: [REFACTOR-02, REFACTOR-03, REFACTOR-06]
coverage:
  - id: D1
    description: README and runtime guide use domain language while preserving six exact data-command version tags
    requirement: REFACTOR-02
    verification:
      - kind: unit
        ref: tests/architecture/test_active_language_policy.py::test_readme_and_user_guide_use_domain_language_and_scoped_cli_markers
        status: pass
    human_judgment: false
  - id: D2
    description: Local-model guide explains training and inference responsibilities while preserving two exact legacy tags and visible QLORA debt
    requirement: REFACTOR-06
    verification:
      - kind: unit
        ref: tests/architecture/test_active_language_policy.py::test_local_model_guide_scopes_cli_literals_and_declares_qlora_debt
        status: pass
    human_judgment: false
duration: 15min
completed: 2026-08-27
status: complete
---

# Phase 41.1 Plan 12: Domain-Readable User Documentation Summary

**The repository entry guide and operator documentation now explain runtime, demo, dataset, and local-model responsibilities without hiding frozen compatibility commands.**

## Performance

- **Duration:** 15 min
- **Started:** 2026-08-27T13:15:00Z
- **Completed:** 2026-08-27T13:30:05Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Reorganized the repository entry guide around Local Runtime, Local Model Profiles, Local Demo UI, and Dataset Operator Flow, including the corrected tracked local-model link.
- Rewrote runtime and local-model user guidance in domain/release language while preserving commands, flags, profiles, artifact expectations, environment variables, and model identifiers.
- Preserved all six data version-tag examples and both model-training version tags byte-for-byte inside one exact command-only compatibility region per document.
- Added guarded mutation tests that reject chronology outside its owner marker and reject narrative prose hidden inside command regions.
- Kept `documents/user/QLORA.md` unchanged, active in the scanner inventory, and explicitly described in the report handoff as not defense-ready.

## Task Commits

1. **Task 1: Rewrite README and local runtime guidance with exact command regions** - `c7ea5da`
2. **Task 2: Rewrite local-model narrative and retain visible QLORA debt** - `7eb3866`

The shared documentation test module and both exact test nodes were committed with Task 1; Task 2 then changed only the local-model prose and marker placement exercised by its already-failing-safe node.

## Files Created/Modified

- `readme.md` - Domain-first project and operator entry path with a scoped legacy data-command appendix.
- `documents/user/USER_GUIDE.md` - Phase-neutral local runtime, analysis, demo, and failure guidance.
- `documents/user/LOCAL_MODELS.md` - Phase-neutral profile, training, inference, and operator explanation with an exact legacy command region.
- `tests/architecture/test_active_language_policy.py` - Independent policy-equality, marker-content, moved-token, correct-link, and QLORA-debt checks.

## Decisions Made

- Compatibility markers contain only exact command fences; explanatory prose remains visible to the active chronology scanner.
- The typo-heavy QLORA note remains unchanged and explicitly uncertified rather than receiving a cosmetic rewrite inside the architecture gap.
- The tracked repository path is lowercase `readme.md`; Windows exposes it as `README.md`, which remains the logical path named by the policy fixture.

## Deviations from Plan

The tracer verification was committed and rerun before Task 2. Execution then continued because the plan is autonomous and the parent dispatch explicitly requested complete execution; no human judgment or external system was involved.

The shared test file contains both Plan 12 nodes in the first task commit so its common exact-marker helper and independent expected command tables stayed together. Task 2 remained an atomic documentation-only commit verified by its dedicated node.

## Issues Encountered

- Git tracks the root guide as `readme.md` while the Windows filesystem and policy refer to `README.md`; the logical contract remains valid on this checkout, and commits use the canonical tracked spelling.
- No protected dataset, model, evidence, provider, training, inference, evaluation, or historical-closure access was needed.

## Known Stubs

None. Empty collections in the static test module are local recorders and scan accumulators, not product placeholders.

## Threat Flags

None. Changes are raw documentation and import-free static assertions; no endpoint, process launch, data access, model access, schema, or trust-boundary surface was added.

## User Setup Required

None - no dependency, credential, external service, or model setup is required.

## Next Phase Readiness

- Architecture and report documentation can link directly to the domain-first user reading path.
- Later report/user-document work still owns the QLORA prose-quality rewrite and must not infer that it is defense-ready from this architecture pass.
- The exact two-node guarded selection passes 2/2 under isolated temporary data and model roots.

## Self-Check: PASSED

All four modified artifacts, both task commits, the unchanged QLORA limitation, and this summary were verified present. The exact guarded Plan 12 selection passed 2/2.

---
*Phase: 41.1-codebase-architecture-overhaul*
*Completed: 2026-08-27*
