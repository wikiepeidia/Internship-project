---
phase: 41.1-codebase-architecture-overhaul
plan: 03
type: execute
wave: 3
depends_on: ["41.1-02"]
files_modified:
  - src/core/__init__.py
  - src/core/integrity.py
  - src/artifacts.py
  - src/model_adaptation/schemas.py
  - src/model_adaptation/training.py
  - src/runtime/analyzers/local_model.py
  - src/runtime/doctor.py
  - src/config/settings.py
  - pyproject.toml
  - tests/architecture/test_integrity_artifacts.py
  - tests/architecture/test_runtime_boundaries.py
  - tests/architecture/test_dependency_groups.py
autonomous: true
requirements: [REFACTOR-03, REFACTOR-05]
estimate: {tokens: 44000, raw_tokens: 44000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Runtime artifact lookup no longer imports the 5,279-line trainer or its Phase 40 graph."
    - "Forward-facing SHA, strict JSON, path, and write primitives have one phase-neutral owner without changing frozen historical call sites."
    - "Runtime/provider/modeling settings preserve every environment name and current compatibility `Settings`, while runtime construction does not read or expose provider secrets."
    - "Default/runtime dependency groups exclude generation/training/evaluation packages and `vnphish analyze|doctor|demo` parser behavior stays fixed."
  artifacts:
    - {path: "src/core/integrity.py", provides: "Forward-only canonical bytes, SHA, bounded path, and atomic/exclusive write primitives"}
    - {path: "src/artifacts.py", provides: "Neutral download-manifest, model-registry, and release-evidence contracts/readers"}
    - {path: "tests/architecture/test_runtime_boundaries.py", provides: "Isolated runtime import and CLI compatibility proof"}
  key_links:
    - {from: "src/runtime/analyzers/local_model.py", to: "src/artifacts.py", via: "load_download_manifest instead of private training helper", pattern: "load_download_manifest"}
    - {from: "src/runtime/doctor.py", to: "src/artifacts.py", via: "strict release artifact reader", pattern: "ReleaseEvaluationArtifact|load_release"}
  prohibitions:
    - "Never enumerate, stat, hash, open, copy, or resolve the reserved held-out split or its containing data/splits directory."
    - "Never import or invoke the Phase 41 evaluator, rerun evaluation/training/inference, or load a real model binary."
    - "Never mutate ProgramData, the verified export, provenance erratum, source manifest, frozen metrics, historical source mirror, or any sealed model root."
    - "Never delete, move, trim, or relink anything on D:."
    - "Do not retrofit frozen Phase 40/41 serializers or writers to the new integrity core."
    - "Do not print, hash, copy, or snapshot real secret values or Settings objects."
---

<objective>
Create neutral integrity/artifact/settings boundaries and remove training/cloud ownership from the installed runtime import graph.

Purpose: Make offline runtime dependencies and artifact reading coherent without changing public behavior or frozen bytes.
Output: Phase-neutral primitives/readers, compatibility shims, owner-specific settings, dependency extras, and isolated import tests.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-CONTEXT.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-RESEARCH.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-PATTERNS.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-02-SUMMARY.md
@historical/phase41-source-closure/c3bbc8c8adaf7579fd2eb9c59a0081613be4b2cae05dfdb64472938c7e6d0434/archival-receipt.json
</context>

## Artifacts this phase produces

- `src/core/__init__.py` and `src/core/integrity.py`: `IntegrityError`, `sha256_bytes()`, `sha256_file()`, `canonical_json_bytes()`, `strict_json_object()`, `bounded_descendant()`, `reject_redirecting_ancestry()`, `write_bytes_exclusive()`, `atomic_replace_new_artifact()`.
- `src/artifacts.py`: `ArtifactError`, neutral release-evaluation models/constants, `load_download_manifest()`, `load_model_registry()`, `save_model_registry()`, `load_release_evaluation_artifact()`.
- `tests/architecture/test_integrity_artifacts.py`, `test_runtime_boundaries.py`, `test_dependency_groups.py`.
- New optional dependency groups in `pyproject.toml`: `generation`, `data`, `training`, and `evaluation`; compatibility group `train` remains.

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Trace one runtime manifest lookup through the neutral integrity boundary</name>
  <read_first>
    - `src/model_adaptation/training.py`
    - `src/model_adaptation/phase40_production_authorities.py`
    - `src/model_adaptation/registry.py`
    - `src/runtime/analyzers/local_model.py`
    - `NEW: src/core/__init__.py`
    - `NEW: src/core/integrity.py`
    - `NEW: src/artifacts.py`
    - `NEW: tests/architecture/test_runtime_boundaries.py`
  </read_first>
  <files>src/core/__init__.py, src/core/integrity.py, src/artifacts.py, src/runtime/analyzers/local_model.py, tests/architecture/test_runtime_boundaries.py</files>
  <behavior>
    - A synthetic download manifest passes from runtime model resolution through `src.artifacts.load_download_manifest()` and the neutral strict-JSON reader to the same candidate path as before.
    - Importing `src.runtime.analyzers.local_model` succeeds when `src.model_adaptation.training`, torch, transformers, sklearn, and provider modules are blocked.
    - Invalid UTF-8, duplicate keys, non-object roots, malformed candidates, and missing candidates fail with contract-equivalent results under `tmp_path`.
    - No neutral helper recursively discovers authority members or imports historical model/evaluation code.
  </behavior>
  <action>Start with a failing runtime-boundary test, then extract only the strict JSON and manifest-reading primitives required for the real download-manifest lookup into `src.core.integrity` and `src.artifacts`. Update `resolve_base_model_path()` to use the neutral reader and leave the trainer helper behavior intact until Task 2 converts it to a compatibility forward. Keep each new function at most 100 lines and each new active module at most 600 lines. Use synthetic manifests only; do not import an evaluator, load a model, inspect a live registry, or retrofit frozen historical serializers.</action>
  <verify><automated>$env:PHASE411_DENY_OPEN_SENTINEL='1'; $env:DATA_DIR=Join-Path $env:TEMP 'vnphish-411-p03-data'; $env:MODEL_ARTIFACT_ROOT=Join-Path $env:TEMP 'vnphish-411-p03-models'; $env:MODEL_REGISTRY_PATH=Join-Path $env:TEMP 'vnphish-411-p03-registry.json'; python -m pytest tests/architecture/test_runtime_boundaries.py -q --basetemp .tmp/pytest-411-p03-tracer</automated></verify>
  <acceptance_criteria>The real runtime lookup path returns the frozen synthetic expectation, runtime imports no training/optional graph, malformed fixtures fail closed, the deny-open sentinel remains active, and size budgets pass.</acceptance_criteria>
  <done>One installed-runtime artifact lookup works end to end through a phase-neutral boundary without loading training or models.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move runtime artifact contracts out of training internals</name>
  <read_first>
    - `src/model_adaptation/schemas.py`
    - `src/model_adaptation/training.py`
    - `src/core/integrity.py`
    - `src/artifacts.py`
    - `tests/architecture/fixtures/serialization_contract.json`
    - `NEW: tests/architecture/test_integrity_artifacts.py`
  </read_first>
  <files>src/core/integrity.py, src/artifacts.py, src/model_adaptation/schemas.py, src/model_adaptation/training.py, tests/architecture/test_integrity_artifacts.py</files>
  <behavior>
    - Canonical JSON, SHA-256, bounded paths, and exclusive/atomic writes match the frozen synthetic byte/error fixtures and never overwrite.
    - Download-manifest and model-registry APIs remain contract-equal through old and new imports for absent, valid, and malformed synthetic fixtures.
    - Release schema strings, enum values, field names, validation, and serialized bytes remain contract-equal through old and new imports.
    - The old `_load_download_manifest` and schema names remain explicit compatibility forwards, not independent active implementations.
  </behavior>
  <action>Expand the forward-only integrity owner with SHA-256, canonical bytes, bounded descendant/reparse checks, and exclusive/atomic writers following the production-authority analog. Move the release-evaluation contract cluster (`ReleaseVerdict`, risky label constants, `HeldOutSupportAudit`, `PerLabelMetricRow`, `OverallMetricSummary`, `ExplanationRubricSummary`, `ReleaseEvaluationArtifact`) plus model-registry readers into `src.artifacts`; leave explicit compatibility imports in `src.model_adaptation.schemas` and replace the trainer's private manifest reader with a named forward. Preserve every field/default/validator, environment-facing artifact name, and old import path. Do not read or reserialize a frozen real artifact; use golden synthetic bytes.</action>
  <verify><automated>$env:PHASE411_DENY_OPEN_SENTINEL='1'; $env:DATA_DIR=Join-Path $env:TEMP 'vnphish-411-p03-data'; $env:MODEL_ARTIFACT_ROOT=Join-Path $env:TEMP 'vnphish-411-p03-models'; $env:MODEL_REGISTRY_PATH=Join-Path $env:TEMP 'vnphish-411-p03-registry.json'; python -m pytest tests/architecture/test_integrity_artifacts.py tests/architecture/test_runtime_boundaries.py -q --basetemp .tmp/pytest-411-p03-artifacts</automated></verify>
  <acceptance_criteria>Golden-byte and adversarial path/write tests pass; old/new artifact APIs agree on synthetic bytes and failures; old symbols remain importable; no frozen artifact changes; all new code meets 600/100 budgets or records an exact exception.</acceptance_criteria>
  <done>The full active integrity/artifact surface has one neutral owner and explicit historical compatibility.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Split settings ownership and dependency extras</name>
  <read_first>
    - `src/config/settings.py`
    - `src/runtime/doctor.py`
    - `src/artifacts.py`
    - `pyproject.toml`
    - `tests/runtime/test_cli.py`
    - `NEW: tests/architecture/test_runtime_boundaries.py`
    - `NEW: tests/architecture/test_dependency_groups.py`
  </read_first>
  <files>src/config/settings.py, src/runtime/doctor.py, pyproject.toml, tests/architecture/test_runtime_boundaries.py, tests/architecture/test_dependency_groups.py</files>
  <behavior>
    - `ENV_FILE_CANDIDATES` and every existing environment variable retain name, precedence, default, and compatibility `Settings` access.
    - `get_runtime_settings()` does not load provider credential fields; tests use dummy values and prove secrets never appear in repr/error/output.
    - Runtime doctor reads release artifacts through the neutral contract and its CLI text/exit baseline stays unchanged.
    - Metadata puts cloud generation, data tooling, ML training, and evaluation packages in explicit extras while base/runtime imports work with those packages blocked.
  </behavior>
  <action>Define `RuntimeSettings`, `ProviderSettings`, `DataSettings`, and `ModelingSettings` plus cached owner getters; retain `Settings` and `get_settings()` as the full compatibility composition with the exact env-file contract. Update runtime doctor to use runtime settings and neutral artifact types. In `pyproject.toml`, keep base limited to core/runtime-safe requirements, preserve `runtime` and `train`, add `generation`, `data`, `training`, and `evaluation`, and derive membership from reviewed source imports: provider/scraper packages in generation, corpus/NLP packages in data, torch/transformers/accelerate/peft in training and compatible train, sklearn in evaluation. Add no package and run no install.</action>
  <verify><automated>$env:PHASE411_DENY_OPEN_SENTINEL='1'; $env:DATA_DIR=Join-Path $env:TEMP 'vnphish-411-p03-data'; $env:MODEL_ARTIFACT_ROOT=Join-Path $env:TEMP 'vnphish-411-p03-models'; $env:MODEL_REGISTRY_PATH=Join-Path $env:TEMP 'vnphish-411-p03-registry.json'; python -m pytest tests/architecture/test_runtime_boundaries.py tests/architecture/test_dependency_groups.py -q --basetemp .tmp/pytest-411-p03-runtime; git diff --exit-code -- data/models/phase41/verified-export data/models/phase41/phase41-provenance-erratum.json historical/phase41-source-closure</automated></verify>
  <acceptance_criteria>Runtime CLI contract fixture passes under blocked optional imports; metadata groups are disjoint as declared; compatibility settings pass; no secret/frozen byte is emitted or changed.</acceptance_criteria>
  <done>The installed runtime has explainable dependency/settings ownership without breaking public or legacy contracts.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Filesystem artifacts -> runtime | Untrusted JSON/path state must be validated without trainer imports or writes. |
| Environment/.env -> settings owners | Runtime must not expose or require provider secrets. |
## STRIDE Threat Register (ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-411-03-01 | Repudiation | producer provenance | high | mitigate | Archive receipt remains authoritative; neutral code is labeled post-evaluation. |
| T-411-03-02 | Tampering | ProgramData closure | high | mitigate | No ProgramData access; consume only tracked archive metadata. |
| T-411-03-03 | Tampering | reparse/TOCTOU | high | mitigate | Bounded path/capture/exclusive-write primitives and synthetic race tests. |
| T-411-03-04 | Elevation of privilege | lazy imports | high | mitigate | Runtime import-block tests and closed router from Plan 02. |
| T-411-03-05 | Tampering | frozen evidence | high | mitigate | Forward-only primitives; no retrofit; Git diff immutable-authority gate. |
| T-411-03-06 | Information disclosure | settings secrets | high | mitigate | Ownership-specific settings, dummy temp env tests, no whole-settings logging. |
| T-411-03-07 | Information disclosure | held-out data | high | mitigate | Deny-open sentinel, exact tests, no default live roots. |
| T-411-03-08 | Denial of service | D-drive models | high | mitigate | Import-only tests, no model load, no deletion/move operation. |
</threat_model>
<verification>Run only the three exact architecture tests with temp roots and optional imports blocked. Never install packages or run a runtime model.</verification>
<success_criteria>
- Runtime imports no trainer/provider graph.
- Neutral artifacts and integrity helpers preserve exact contracts.
- Settings/extras make local runtime ownership explicit with no secret leakage.
</success_criteria>
<output>Create `.planning/phases/41.1-codebase-architecture-overhaul/41.1-03-SUMMARY.md` when done.</output>
