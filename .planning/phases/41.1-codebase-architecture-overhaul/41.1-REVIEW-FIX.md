---
phase: 41.1-codebase-architecture-overhaul
fixed_at: 2026-08-27T06:55:32.6126852Z
review_path: .planning/phases/41.1-codebase-architecture-overhaul/41.1-REVIEW.md
iteration: 3
findings_in_scope: 10
fixed: 10
skipped: 0
status: all_fixed
---

# Phase 41.1: Code Review Fix Report

**Fixed at:** 2026-08-27T06:55:32.6126852Z  
**Source review:** `.planning/phases/41.1-codebase-architecture-overhaul/41.1-REVIEW.md`  
**Iteration:** 3

**Summary:**

- Findings in scope: 10
- Fixed: 10
- Skipped: 0
- Verification ran in the shared main checkout because `workflow.use_worktrees=false`.
- Only explicitly named architecture test files ran under the deny-open sentinel and synthetic temporary storage/model roots.
- No held-out split tree, model root, provider, training, inference, Phase 41 evaluation, D-drive root, ProgramData evidence, or immutable evidence/export flow was accessed or mutated.

## Fixed Issues

### CR-01: Dataset JSON ingress silently changes or discards record facts

**Files modified:** `src/data_pipeline/core/records.py`, `src/data_pipeline/workflows.py`, `tests/architecture/test_data_core_contract.py`, `tests/architecture/test_data_cli_contract.py`  
**Commit:** `70a25b6`  
**Status:** Fixed — requires human verification.  
**Applied fix:** Closed every record model with strict, extra-forbidden validation; rejected blank seed facts; and routed both reviewed workflow JSONL readers through one duplicate-key, non-finite-token, non-object, strict-UTF-8 decoder whose failures identify source and line. Added coercion, unknown-field, duplicate-key, array, non-finite, invalid-UTF-8, and source-location regressions.

### CR-02: Nested output-parent creation can mutate outside the trusted root

**Files modified:** `src/core_binding.py`, `src/core/integrity.py`, `tests/architecture/test_integrity_artifacts.py`  
**Commits:** `9aa3dd2`, `a2ad62c`, `140ad3c`  
**Status:** Fixed — requires human verification.  
**Applied fix:** Replaced path-based nested `mkdir` operations with held-parent relative creation, identity checks before and after every level, and exact identity-owned cleanup on POSIX and Win32. Added ancestor-swap and forced post-create-failure regressions, then kept both modules inside the hard 600-line architecture budget.

### CR-03: The preserved validated-record publisher can destroy coherent live state

**Files modified:** `src/data_pipeline/workflows.py`, `tests/architecture/test_data_cli_contract.py`  
**Commit:** `37031c7`  
**Status:** Fixed — requires human verification.  
**Applied fix:** Kept the frozen compatibility callable but made it delegate to the immutable reviewed-dataset generation publisher, returning members of the completed generation and never replacing `processed/validated.jsonl` or `quality-stats.json` independently.

### CR-04: Dataset manifest members are reopened after their containment check

**Files modified:** `src/data_pipeline/core/splits.py`, `tests/architecture/test_data_core_contract.py`  
**Commit:** `beaafd2`  
**Status:** Fixed — requires human verification.  
**Applied fix:** Preserved root and member identities from enumeration, captured each file through a root-bound parent chain and no-follow descriptor/handle, rechecked pathname identity, then reconciled the complete membership and identities before accepting bytes. Added a regular-file replacement race that previously could have changed hashed bytes.

### CR-05: The held-out deny-open guard still permits unguarded child processes

**Files modified:** `tests/architecture/bootstrap/sitecustomize.py`, `tests/architecture/test_provenance_boundary.py`  
**Commit:** `78c9a5e`  
**Status:** Fixed — requires human verification.  
**Applied fix:** Rejected explicit executable overrides, empty or sentinel-free environments, missing bootstrap paths, and `-S`/`-I`/`-E`; restricted Python and Git children to reviewed argument grammars; and guarded metadata, link, and remaining pathname mutation APIs. Added child guard attestations and adversarial bypass probes.

### CR-06: Coordinated unsafe actions still pass the recommendation filter

**Files modified:** `src/runtime/analyzers/local_model.py`, `tests/architecture/test_runtime_boundaries.py`  
**Commit:** `8ed5530`  
**Status:** Fixed — requires human verification.  
**Applied fix:** Required a local explicit negation for every unsafe action occurrence instead of extending one negation across coordinators. Updated the built-in safe fallback and added all three reproduced imperative/coordinated bypasses.

### CR-07: The OTP-notice heuristic can overwrite an explicit high-risk payment result

**Files modified:** `src/runtime/analyzers/local_model.py`, `tests/architecture/test_runtime_boundaries.py`  
**Commit:** `185f92f`  
**Status:** Fixed — requires human verification.  
**Applied fix:** Limited the legitimate-notice correction to suspicious decisions with a bank, OTP, transaction/login context, an explicit nondisclosure clause, and no unsafe action or helper cue. Explicit high-risk decisions can no longer be downgraded. Added mixed bank-language/payment and exact safe-notice regressions.

### CR-08: Untrusted model output can remove the provisional-analysis warning

**Files modified:** `src/runtime/analyzers/local_model.py`, `tests/architecture/test_runtime_boundaries.py`  
**Commit:** `21da695`  
**Status:** Fixed — requires human verification.  
**Applied fix:** Removed `provisional` from the model-controlled decision schema and all internal propagation, ignored generated certification attempts, and pinned the active local-runtime `AnalysisResult` boundary to `provisional=True`.

### WR-01: A release manifest can certify malformed JSONL

**Files modified:** `src/data_pipeline/core/splits.py`, `tests/architecture/test_data_core_contract.py`  
**Commit:** `7422a55`  
**Status:** Fixed.  
**Applied fix:** Parsed every nonblank captured member line with the strict shared decoder and required the closed `DatasetRecord` schema during both manifest build and verification. Added malformed JSON, array, duplicate-key, and schema-invalid regressions.

### WR-02: A valid zero-gap recovery request crashes while building its summary

**Files modified:** `src/data_pipeline/workflows.py`, `tests/architecture/test_data_cli_contract.py`  
**Commit:** `dcc035d`  
**Status:** Fixed.  
**Applied fix:** Allowed a missing generated artifact in the generate-only summary, serialized it as `None`, and returned the zero-gap recovery summary without constructing any generation/provider dependency.

## Verification

- Every modified Python section was re-read and parsed after its fix.
- CR-06 recommendation regressions: 15 passed.
- CR-07 OTP/payment regressions: 2 passed.
- CR-08 provisional-boundary regression: 1 passed.
- WR-02 zero-gap regression: 1 passed.
- CR-03 compatibility/publication contracts: 2 passed.
- CR-01 data-core and data-CLI contracts: 80 passed, 2 third-party SWIG warnings.
- CR-05 deny-open adversarial contracts: 5 passed.
- CR-02 integrity contracts and static-budget gate: 28 passed.
- CR-04 manifest containment/race contracts: 4 passed.
- WR-01 strict manifest contracts: 8 passed.
- Final exact guarded architecture set: **309 passed**, 2 third-party SWIG deprecation warnings, 0 failed in 33.00 seconds.
- The final set named all 17 architecture test files explicitly; no broad pytest or directory selector was used.

---

_Fixed: 2026-08-27T06:55:32.6126852Z_  
_Fixer: the agent (gsd-code-fixer)_  
_Iteration: 3_
