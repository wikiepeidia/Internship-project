---
phase: 41.1-codebase-architecture-overhaul
fixed_at: 2026-08-27T15:27:57Z
review_path: .planning/phases/41.1-codebase-architecture-overhaul/41.1-REVIEW.md
iteration: 1
findings_in_scope: 13
fixed: 13
skipped: 0
status: all_fixed
---

# Phase 41.1: Code Review Fix Report

**Fixed at:** 2026-08-27T15:27:57Z  
**Source review:** `.planning/phases/41.1-codebase-architecture-overhaul/41.1-REVIEW.md`  
**Iteration:** 1

**Summary:**

- Findings in scope: 13
- Fixed: 13
- Skipped: 0
- Verification location: main checkout (`workflow.use_worktrees=false`)

## Fixed Issues

### CR-01: The guard publishes direct references to every protected original

**Files modified:** `tests/architecture/bootstrap/sitecustomize.py`, `tests/architecture/test_provenance_boundary.py`, `tests/architecture/fixtures/phase411_guard_contract.json`, `docs/architecture/provenance.md`  
**Commit:** c6f5f22  
**Applied fix:** Removed original-callable references and wrapper escape metadata, then asserted that protected wrappers expose neither defaults nor unwrap attributes. **Status:** fixed; requires human verification of the defense-in-depth boundary.

### CR-02: Descriptor capability checks ignore keyword arguments

**Files modified:** `tests/architecture/bootstrap/sitecustomize.py`, `tests/architecture/test_provenance_boundary.py`, `tests/architecture/fixtures/phase411_guard_contract.json`  
**Commit:** c6f5f22  
**Applied fix:** Normalized positional and keyword descriptor arguments for every guarded FD consumer and added mutation coverage for both call forms.

### CR-03: The deny-all process policy is only a self-authored wrapper list

**Files modified:** `tests/architecture/bootstrap/sitecustomize.py`, `tests/architecture/test_provenance_boundary.py`, `tests/architecture/fixtures/phase411_guard_contract.json`, `docs/architecture/provenance.md`  
**Commit:** c6f5f22  
**Applied fix:** Added independent native-process surface dispositions and denial probes, removed the false universal-sandbox claim, and made external OS process isolation an explicit required boundary for surfaces Python cannot safely patch. **Status:** fixed; requires human verification of the platform security disposition.

### CR-04: The provenance suite violates and contradicts the guard contract

**Files modified:** `tests/architecture/test_provenance_boundary.py`, `tests/architecture/fixtures/phase411_guard_contract.json`  
**Commit:** c6f5f22  
**Applied fix:** Removed stale child-process exceptions and direct protected-source baseline reads; the suite now uses only synthetic in-process denial probes under the declared guard contract.

### CR-05: Windows bound descriptors make provenance tests fail under the guard

**Files modified:** `tests/architecture/bootstrap/sitecustomize.py`, `tests/architecture/conftest.py`, `tests/architecture/test_provenance_boundary.py`, `tests/architecture/test_source_archiving.py`  
**Commit:** c6f5f22  
**Applied fix:** Added one post-install descriptor-registration seam, registered Windows CRT-bound descriptors through the shared fixture, and verified descriptor removal after close.

### CR-06: The static-language import guard corrupts combined collection

**Files modified:** `tests/architecture/test_active_language_policy.py`  
**Commit:** 1e63bb0  
**Applied fix:** Replaced module-global import-hook installation with a yielding fixture that removes the exact finder by identity in `finally`; combined collection is now a passing required gate.

### CR-07: Tool-route discovery fails open on unrecognized execution forms

**Files modified:** `tests/architecture/test_architecture_policy.py`, `tests/architecture/fixtures/tool_inventory_contract.json`, `architecture/module-boundaries.json`, `docs/architecture/overview.md`  
**Commit:** 1e63bb0  
**Applied fix:** Classified or rejected Python, batch, PowerShell, and shell execution primitives, including dynamic arguments, shell wrappers, and shell-to-shell delegation; mutation tests cover every reviewed bypass.

### CR-08: Archive verification accepts forged actual mismatch facts

**Files modified:** `src/source_archiving/contracts.py`, `src/source_archiving/service.py`, `scripts/archive_phase41_source_closure.py`, `tests/architecture/test_source_archiving.py`  
**Commit:** aa774e3  
**Applied fix:** Bound production receipt bytes to an independent expected digest before publication and verification, with mutations for actual hashes and timestamps.

### WR-01: PRE-to-POST ownership proof is partly synthesized from POST

**Files modified:** `tests/architecture/fixtures/active_text_contract.pre-extraction.json`, `tests/architecture/fixtures/runtime_prose_contract.json`, `tests/architecture/test_architecture_policy.py`, `tests/architecture/test_active_language_policy.py`  
**Commit:** 1e63bb0  
**Applied fix:** Replaced POST-derived PRE construction with an independent frozen PRE manifest and bound PRE source evidence to fixed Git commits, blob OIDs, and raw blob SHA-256 values.

### WR-02: Report scans miss wrapped destructive commands and secret forms

**Files modified:** `tests/architecture/test_report_handoff.py`  
**Commit:** 1e63bb0  
**Applied fix:** Normalized Markdown and prompt prefixes, unwrapped command interpreters, split chained commands, expanded secret patterns, and added every requested wrapped-command mutation.

### WR-03: Exact JSON authority readers accept duplicate keys

**Files modified:** `tests/architecture/json_contract.py`, `tests/architecture/test_architecture_policy.py`, `tests/architecture/test_active_language_policy.py`, `tests/architecture/test_report_handoff.py`  
**Commit:** 1e63bb0  
**Applied fix:** Centralized strict JSON parsing with duplicate-key and non-finite-number rejection and routed all reviewed authority readers through it.

### WR-04: Valid malformed demo requests escape as unhandled exceptions

**Files modified:** `src/runtime/demo.py`, `tests/architecture/test_runtime_demo.py`  
**Commit:** 8ec3652  
**Applied fix:** Added bounded length validation, UTF-8 and JSON error handling, object-payload validation, and one structured JSON 400 response contract.

### WR-05: Arbitrary host binding contradicts the local-only boundary

**Files modified:** `src/runtime/cli.py`, `src/runtime/demo.py`, `tests/architecture/test_runtime_demo.py`, `readme.md`  
**Commit:** 8ec3652  
**Applied fix:** Enforced loopback-only hosts in both CLI parsing and server construction and documented the local-only contract.

## Verification

- `8ec3652`: 16 explicit demo/runtime architecture nodes passed.
- `aa774e3`: 6 explicit source-archiving receipt nodes passed.
- `c6f5f22`: exact guarded provenance plus source-archiving gate passed, 63 tests.
- `1e63bb0`: focused remaining-finding mutation gate passed, 7 tests.
- Final required guarded combined invocation of `test_architecture_policy.py`, `test_active_language_policy.py`, `test_provenance_boundary.py`, `test_report_handoff.py`, and `test_source_archiving.py`: **110 passed in 9.45s**.
- Python AST checks passed for every modified Python file; strict JSON parsing passed for every modified JSON authority; `git diff --check` passed for tracked fix files.
- Safety boundary maintained: no protected split access, model loading, training, inference, provider calls, Phase 41 evaluation, ProgramData evidence access, or sealed historical-closure artifact access occurred.

## Skipped Issues

None.

---

_Fixed: 2026-08-27T15:27:57Z_  
_Fixer: Codex (gsd-code-fixer)_  
_Iteration: 1_
