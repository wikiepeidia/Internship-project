---
phase: 41.1-codebase-architecture-overhaul
fixed_at: 2026-08-27T15:53:43Z
review_path: .planning/phases/41.1-codebase-architecture-overhaul/41.1-REVIEW.md
iteration: 2
findings_in_scope: 5
fixed: 5
skipped: 0
status: all_fixed
---

# Phase 41.1: Code Review Fix Report

**Fixed at:** 2026-08-27T15:53:43Z
**Source review:** `.planning/phases/41.1-codebase-architecture-overhaul/41.1-REVIEW.md`
**Iteration:** 2

**Summary:**

- Findings in scope: 5
- Fixed: 5
- Skipped: 0
- Verification location: main checkout (`workflow.use_worktrees=false`)

## Fixed Issues

### CR-01: Protected filesystem originals remain directly reachable through wrapper closures

**Files modified:** `tests/architecture/bootstrap/sitecustomize.py`, `tests/architecture/test_provenance_boundary.py`
**Commit:** e0a4e5a
**Applied fix:** Installed an append-only CPython audit hook before Python wrappers and added a recursive closure-cell adversarial test. The test recovers the original `_io.open` callable, invokes it against a protected lexical path, and proves the audit boundary rejects it before the filesystem call. **Status:** fixed; requires human verification of the security-boundary reasoning.

### CR-02: Native process isolation is documented but absent from the reviewed execution path

**Files modified:** `tests/architecture/bootstrap/sitecustomize.py`, `tests/architecture/conftest.py`, `tests/architecture/test_provenance_boundary.py`, `tests/architecture/fixtures/phase411_guard_contract.json`, `docs/architecture/provenance.md`
**Commits:** 3eb5f98, 9ec1f52
**Applied fix:** Replaced the unimplemented external-isolation disposition with fail-closed denial for all reviewed native loaders, added audit-level `ctypes.dlopen` denial, cleared arbitrary loader caches, and exposed only six preloaded non-process file-binding capabilities required by synthetic archive tests. Collection now refuses a missing audit hook, the wrong boundary, an incomplete native manifest, or any disposition other than wrapped/unavailable. **Status:** fixed; requires human verification of the security-boundary reasoning.

### CR-03: Tool-route discovery still fails open on valid execution syntax

**Files modified:** `tests/architecture/test_architecture_policy.py`
**Commit:** 268d306
**Applied fix:** Execution APIs are classified before argument-shape filtering; static keyword-only `args=` routes are parsed, while missing, duplicate, dynamic, shell, executable-override, and `**kwargs` forms fail closed. Batch, PowerShell, and shell parsers now reject start/call/operator/direct/unknown executable forms instead of silently returning no route. **Status:** fixed; requires human verification of the closed route grammar.

### WR-01: Static report safety checks still accept wrapped deletion and CLI secret forms

**Files modified:** `tests/architecture/test_report_handoff.py`  
**Commit:** 33b9ec4
**Applied fix:** Command normalization removes PowerShell `&` and dot call operators during fixed-point wrapper unwrapping and again per chained segment. Secret detection now rejects both `--api-key=value` and `--api-key value` while preserving explicit redaction forms.

### WR-02: Accepted IPv6 loopback cannot be served by the selected WSGI server

**Files modified:** `src/runtime/demo.py`, `src/runtime/cli.py`, `tests/architecture/test_runtime_demo.py`, `readme.md`, `tests/architecture/fixtures/runtime_prose_contract.json`
**Commits:** 4c6410e, 8f5041e
**Applied fix:** Restricted the demo contract to `localhost` and IPv4 loopback addresses, rejects `::1` before runtime construction, updated the user-facing contract, and rebound the intentional runtime-behavior fingerprints.

## Verification

- CR-03 focused guarded route-inventory node: **1 passed**.
- WR-01 focused guarded report-mutation node: **1 passed**.
- WR-02 focused guarded parser/server nodes: **9 passed**; fingerprint integration: **10 passed**.
- CR-01 focused guarded origin/recovered-open/root nodes: **3 passed**.
- CR-02 focused guarded native-loader/audit/archive nodes: **3 passed**.
- Final exact guarded six-file integration gate (`test_architecture_policy.py`, `test_active_language_policy.py`, `test_provenance_boundary.py`, `test_report_handoff.py`, `test_source_archiving.py`, `test_runtime_demo.py`): **128 passed in 6.52s**.
- Python AST checks passed for all modified Python files; modified JSON fixtures parsed; `git diff --check` passed before each security commit.
- Safety boundary maintained: no protected split content, D-drive model root, ProgramData evidence, historical closure content, sealed artifact, model/provider, training, inference, or evaluation access occurred.

## Skipped Issues

None.

---

_Fixed: 2026-08-27T15:53:43Z_
_Fixer: Codex (gsd-code-fixer)_
_Iteration: 2_
