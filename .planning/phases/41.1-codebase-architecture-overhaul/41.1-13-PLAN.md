---
phase: 41.1-codebase-architecture-overhaul
plan: 13
type: execute
wave: 12
depends_on: ["41.1-12"]
files_modified:
  - docs/architecture/overview.md
  - docs/architecture/cli-contracts.md
  - docs/architecture/provenance.md
  - docs/architecture/storage-retention.md
  - tests/architecture/test_active_language_policy.py
autonomous: true
gap_closure: true
requirements: [REFACTOR-02, REFACTOR-03, REFACTOR-06]
estimate:
  tokens: 23000
  raw_tokens: 23000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "All four active architecture documents use domain narrative outside exact policy-owned authority/schema/storage/CLI identifier regions."
    - "The overview preserves run_phase1 only as a balanced compatibility identifier and preserves phase-numbered policy rows only inside one exact generated inventory region."
    - "CLI prose no longer says 12 Phase 40 / 7 Phase 41, while every frozen command row remains byte-exact inside its balanced contract region."
    - "Provenance and storage documents retain exact paths, schemas, and correction quote only inside narrow balanced authority regions."
  artifacts:
    - path: "docs/architecture/overview.md"
      provides: "Domain-readable architecture story with exact policy/CLI identifiers"
    - path: "docs/architecture/cli-contracts.md"
      provides: "Phase-neutral command-family prose and frozen command inventory"
    - path: "docs/architecture/provenance.md"
      provides: "Exact authority/schema/correction facts with scoped markers"
    - path: "docs/architecture/storage-retention.md"
      provides: "Exact storage identities with phase-neutral narrative"
  key_links:
    - from: "static_policy.active_text_scan marker registry"
      to: "four architecture documents"
      via: "exact path/marker/reason/owner validation"
  prohibitions:
    - "Do not change any policy-derived module name, command, path, schema, hash, byte count, storage identity, or correction quote."
    - "Do not hide explanatory prose inside authority markers."
    - "Do not inspect any path named by provenance or storage documents."
    - "Do not access protected authorities, models, D-drive, ProgramData, or data rows."
---

<objective>
Clean every active architecture narrative while preserving exact provenance and compatibility identifiers.

Purpose: Make architecture documents report/defense-readable under D-02/D-03/D-06/D-07 without rewriting evidence identities.
Output: Four phase-neutral architecture docs with narrow, mechanically balanced authority markers.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-CONTEXT.md
@.planning/phases/41.1-codebase-architecture-overhaul/41.1-12-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Rewrite overview and CLI narrative around exact compatibility identifiers</name>
  <files>docs/architecture/overview.md, docs/architecture/cli-contracts.md, tests/architecture/test_active_language_policy.py</files>
  <read_first>
    - `overview.md`: run_phase1 prose near current line 71 and phase-numbered policy tables/edges/cycles below the generated inventory sections
    - `cli-contracts.md`: frozen command rows and count prose at current lines 58-59
    - `architecture/module-boundaries.json`: exact marker owners, module rows, edges, and CLI inventory
    - `tests/architecture/fixtures/active_text_contract.json`: independent exact rows for `legacy-data-cli-identifier`, `policy-groups`, `policy-edges`, `historical-sccs`, and `cli-contracts`; consume it read-only
  </read_first>
  <action>
    Exact-equal every policy row consumed by this task to the independent Plan 07 fixture before editing. Rewrite overview's external-workflow paragraph in domain language while retaining literal `run_phase1` only inside the exact `legacy-data-cli-identifier` marker pair. Retain the policy-derived compatibility/historical module, edge, and SCC tables inside the exact `policy-groups`, `policy-edges`, and `historical-sccs` marker pairs; do not create another wrapper region. Keep the generated identifiers and rows byte-exact. Active explanatory prose, headings, diagram labels, and reading-path text remain outside markers.

    In cli-contracts, retain the exact frozen phase-numbered command rows inside the fixture-owned `cli-contracts` marker pair. Rewrite current lines 58-59 to describe four adaptation, twelve retained training/evidence compatibility commands, and seven held-out-evaluation compatibility commands without chronology tokens. Keep all 23 command names, handlers, parser behavior, counts, errors, and output contracts exact. Do not introduce another CLI-contract marker ID.

    Add `test_overview_and_cli_contracts_use_domain_narrative_or_exact_markers`. It requires every enumerated occurrence to be rewritten or located in its one declared marker, rejects nested/duplicate/unbalanced markers, validates exact policy/command inventories, and fails when an identifier moves outside or active prose moves inside.
  </action>
  <verify>
    <automated>$env:PYTHONPATH=(Resolve-Path 'tests/architecture/bootstrap').Path; $phase411Temp=Join-Path ([System.IO.Path]::GetTempPath()) ('vnphish-411-'+[guid]::NewGuid().ToString('N')); $phase411Base=Join-Path $phase411Temp 'pytest'; New-Item -ItemType Directory -Path $phase411Base -Force | Out-Null; $env:PHASE411_DENY_OPEN_SENTINEL='1'; $env:PHASE411_REQUIRE_HISTORICAL_ARCHIVE='0'; $env:PYTHONNOUSERSITE='1'; $env:DATA_DIR=Join-Path $phase411Temp 'data'; $env:MODEL_ARTIFACT_ROOT=Join-Path $phase411Temp 'models'; $env:MODEL_REGISTRY_PATH=Join-Path $phase411Temp 'registry.json'; $env:TEMP=$phase411Temp; $env:TMP=$phase411Temp; $python=(Get-Command python -CommandType Application).Source; &amp; $python -m pytest -q -p no:cacheprovider --basetemp $phase411Base tests/architecture/test_active_language_policy.py::test_overview_and_cli_contracts_use_domain_narrative_or_exact_markers</automated>
  </verify>
  <done>Overview and CLI docs use domain narrative while exact identifiers remain balanced and policy-bound.</done>
</task>

<task type="auto">
  <name>Task 2: Scope provenance and storage chronology to exact authority regions</name>
  <files>docs/architecture/provenance.md, docs/architecture/storage-retention.md, tests/architecture/test_active_language_policy.py</files>
  <read_first>
    - `provenance.md`: authority paths at current lines 13-15, schema literals near 44/60, and exact correction quote near 67
    - `storage-retention.md`: exact storage-path tables, narrative near current lines 23 and 72, and all existing safety/no-deletion wording
    - `architecture/module-boundaries.json`: exact authority-marker registry
    - `tests/architecture/fixtures/active_text_contract.json`: independent exact provenance and storage marker rows produced by Plan 07; consume it read-only
  </read_first>
  <action>
    Exact-equal every policy row consumed by this task to the independent Plan 07 fixture before editing. Preserve provenance path identities inside `provenance-authority-identities`, evidence/erratum schema literals inside `provenance-schema-identities`, and the exact correction quote inside `provenance-correction-quote`. Rewrite all surrounding narrative in domain/evidence language. Markers may contain only the exact table/literal/quote they own; hashes, counts, policies, and limitations remain exact.

    Preserve exact D-drive and model-root identities only inside the fixture-owned `sealed-roots`, `optional-gguf`, `cleanup-candidates`, `nested-candidates`, and `older-bases` marker pairs. Rewrite line-23's recorded-phase-root phrase as recorded experiment root and line-72's final-Phase-40/41 sentence as retained final system wording. Keep every size, hash, path, hardlink warning, and no-deletion/authorization statement exact. Do not introduce another provenance or storage marker ID. Add `test_provenance_and_storage_use_domain_narrative_or_exact_authority_markers`, including synthetic moved-token, near-match, nested-marker, and active-prose-inside-marker failures. Tests read document text only and never probe named paths.
  </action>
  <verify>
    <automated>$env:PYTHONPATH=(Resolve-Path 'tests/architecture/bootstrap').Path; $phase411Temp=Join-Path ([System.IO.Path]::GetTempPath()) ('vnphish-411-'+[guid]::NewGuid().ToString('N')); $phase411Base=Join-Path $phase411Temp 'pytest'; New-Item -ItemType Directory -Path $phase411Base -Force | Out-Null; $env:PHASE411_DENY_OPEN_SENTINEL='1'; $env:PHASE411_REQUIRE_HISTORICAL_ARCHIVE='0'; $env:PYTHONNOUSERSITE='1'; $env:DATA_DIR=Join-Path $phase411Temp 'data'; $env:MODEL_ARTIFACT_ROOT=Join-Path $phase411Temp 'models'; $env:MODEL_REGISTRY_PATH=Join-Path $phase411Temp 'registry.json'; $env:TEMP=$phase411Temp; $env:TMP=$phase411Temp; $python=(Get-Command python -CommandType Application).Source; &amp; $python -m pytest -q -p no:cacheprovider --basetemp $phase411Base tests/architecture/test_active_language_policy.py::test_provenance_and_storage_use_domain_narrative_or_exact_authority_markers tests/architecture/test_active_language_policy.py::test_all_eight_active_documents_are_classified_exactly</automated>
  </verify>
  <done>All active architecture docs are phase-neutral outside exact authority/schema/storage/CLI markers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Human narrative -> exact identifiers | Rewriting must not alter hashes, paths, schemas, commands, or correction text |
| Marker registry -> documents | Only exact policy-owned regions may retain chronology tokens |
| Document path text -> filesystem | Tests must treat paths as text and never inspect targets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-41.1-13-01 | Tampering | Provenance/storage facts | high | mitigate | Exact marker inventories and byte-stable literals |
| T-41.1-13-02 | Elevation of Privilege | Marker bypass | high | mitigate | Balanced/nonnested/owner-scoped markers and moved-prose mutations |
| T-41.1-13-03 | Information Disclosure | Named authorities | high | mitigate | Raw document reads only under Plan 11 guard |
| T-41.1-13-04 | Repudiation | CLI family prose | medium | mitigate | Exact 23-command table plus domain count wording |
</threat_model>

<verification>
Run only the exact import-free document nodes. No command, path, model, archive, or evidence authority named in the documents is invoked or inspected.
</verification>

<success_criteria>
- Every exact explorer-enumerated architecture-document occurrence is either rewritten or inside its specific balanced marker.
- Frozen commands, paths, schemas, hashes, and correction quote remain exact.
- All eight active documents are classified; walkthrough README remains historical.
- No active prose is hidden in a compatibility/authority region.
</success_criteria>

<output>
Create `.planning/phases/41.1-codebase-architecture-overhaul/41.1-13-SUMMARY.md` when done.
</output>
