---
phase: 41.1-codebase-architecture-overhaul
plan: 02
subsystem: cli-architecture
tags: [argparse, lazy-imports, compatibility, static-routing, tdd]
requires:
  - phase: 41.1-01
    provides: frozen 23-command CLI contract, protected-authority baseline, and pre-collection deny-open guard
provides:
  - closed immutable lazy router for all 23 model-adaptation commands
  - dependency-light adaptation, Phase 40, and Phase 41 command families
  - 232-line compatibility facade preserving parser, help, output, raw argv, and error contracts
affects: [41.1-03, 41.1-04, model-adaptation-cli, architecture-verification]
actuals:
  tokens: 35656
  tasks: 3
  commits: 6
tech-stack:
  added: []
  patterns:
    - immutable literal command-to-module route table
    - handler-local imports behind explicit compatibility facades
    - family-owned parser registration with facade-owned handler identities
key-files:
  created:
    - src/model_adaptation/commands/__init__.py
    - src/model_adaptation/commands/router.py
    - src/model_adaptation/commands/adaptation.py
    - src/model_adaptation/commands/legacy_phase40.py
    - src/model_adaptation/commands/legacy_phase41.py
    - tests/architecture/test_model_cli_contract.py
  modified:
    - src/model_adaptation/cli.py
    - tests/model_adaptation/test_cli.py
key-decisions:
  - "Keep parser handler identities on src.model_adaptation.cli while routing invoked commands through an immutable literal table."
  - "Preserve the required doctor --adaptation-mode quirk and the source-observable Phase 41 run-once marker exactly."
  - "Move private test monkeypatch ownership to family modules through explicit lazy wrappers rather than restoring eager implementation imports."
patterns-established:
  - "Compatibility facade: parse with dependency-light family registrars, then dispatch one literal command after parsing."
  - "Lazy seam: implementation dependencies are imported only inside the selected family handler or an explicit lazy dependency wrapper."
requirements-completed: [REFACTOR-02, REFACTOR-05]
coverage:
  - id: D1
    description: "All 23 historical model-adaptation commands retain the frozen parser, help, handler-double, stdout, stderr, raw-argv, and exit contracts."
    requirement: REFACTOR-02
    verification:
      - kind: integration
        ref: "tests/architecture/test_model_cli_contract.py#test_adaptation_family_matches_frozen_contract"
        status: pass
    human_judgment: false
  - id: D2
    description: "The compatibility shell imports no optional model/provider implementation on import or help and stays within the declared static size budgets."
    requirement: REFACTOR-05
    verification:
      - kind: unit
        ref: "tests/architecture/test_model_cli_contract.py#lazy route and static budget tests"
        status: pass
      - kind: integration
        ref: "tests/architecture/test_provenance_boundary.py#test_protected_authorities_match_fixed_baseline"
        status: pass
    human_judgment: false
duration: 24min
completed: 2026-08-27
status: complete
---

# Phase 41.1 Plan 02: Thin Lazy Model-Adaptation CLI Summary

Immutable 23-command lazy router with three dependency-light command families and a 232-line byte-contract-compatible facade.

## Performance

- **Duration:** 24 min
- **Started:** 2026-08-27T00:55:04Z
- **Completed:** 2026-08-27T01:18:51Z
- **Tasks:** 3
- **Files modified:** 8

## Accomplishments

- Preserved the exact frozen parser graph, help text, defaults, requiredness, choices, handler identities, handler-double output, raw argv, stderr, and exit behavior across all 23 commands.
- Replaced eager compatibility paths with an immutable `MappingProxyType` route table whose module and symbol targets are all literal and closed to user input.
- Split the CLI into adaptation, Phase 40, and Phase 41 families whose implementation dependencies load only after an exact command is selected.
- Reduced `src/model_adaptation/cli.py` from 1,329 lines at the phase baseline to 232 lines while keeping every new active module at or below 600 lines and every function at or below 100 lines.

## Task Commits

Each TDD task retained separate RED and GREEN gates:

1. **Task 1: Trace the adaptation command family through the closed lazy router**
   - `44a2f6b` — RED: add the guarded frozen CLI contract
   - `2ac8309` — GREEN: route adaptation commands lazily
2. **Task 2: Extract the twelve Phase 40 compatibility commands**
   - `d5b2115` — RED: require all twelve Phase 40 lazy routes
   - `ab32a20` — GREEN: extract dependency-light Phase 40 registration and handlers
3. **Task 3: Extract Phase 41 routes and enforce the thin-facade budget**
   - `230d649` — RED: require seven Phase 41 routes and static budgets
   - `955baa0` — GREEN: extract Phase 41 and finish the 232-line facade

## Test Results

- Tracer feedback gate: `3 passed` for the exact frozen command contract, lazy import contract, and protected-authority baseline.
- Task 2 guarded verification: `4 passed in 3.57s`.
- Final plan-level guarded verification: `6 passed in 3.82s`.
- Exact final selection: `tests/architecture/test_model_cli_contract.py` plus `tests/architecture/test_provenance_boundary.py::test_protected_authorities_match_fixed_baseline` with the pre-collection sentinel and temporary data/model/registry roots.
- No real Phase 40/41 command, evaluator, trainer, provider, inference path, or model loader was invoked.

## Files Created/Modified

- `src/model_adaptation/commands/router.py` — immutable literal route table and single-route lazy dispatcher.
- `src/model_adaptation/commands/adaptation.py` — pilot, train, convert, and required-mode doctor registration/handlers with lazy dependency wrappers.
- `src/model_adaptation/commands/legacy_phase40.py` — twelve Phase 40 parser surfaces and compatibility handlers in 593 lines.
- `src/model_adaptation/commands/legacy_phase41.py` — seven Phase 41 parser surfaces and compatibility handlers in 272 lines.
- `src/model_adaptation/cli.py` — 232-line parser facade, explicit compatibility symbols, raw-argv capture, and unchanged exception boundary.
- `tests/architecture/test_model_cli_contract.py` — full frozen contract, literal-route checks, import isolation, source-shape guard, and static AST budgets.
- `tests/model_adaptation/test_cli.py` — family-owned monkeypatch targets for moved private implementation seams.

## Decisions Made

- Kept `src.model_adaptation.cli.handle_*` as the parser-visible callable identities so the frozen action graph remains byte-for-byte equal while each function delegates through the closed router.
- Kept `doctor --adaptation-mode` required in its historical parser location; this refactor intentionally makes no semantic correction.
- Preserved the current `handle_phase41_run_once` source-observable marker while its executable behavior now routes lazily to the Phase 41 family.
- Used explicit lazy dependency wrappers for test seams instead of wildcard exports, module `__getattr__`, or eager historical imports.

## Deviations from Plan

None - plan executed exactly as written.

## Known Stubs

None. Empty list/dict and `None` values found by the stub scan are frozen argparse defaults or test accumulator initialization, not UI/data placeholders.

## Issues Encountered

- The requirements/progress SDK updated the requirement checkbox and frontmatter counts but did not match the legacy traceability/current-position rows. Those exact planning rows were normalized to `REFACTOR-02: Complete` and `57/87 plans (66%)`; no source or evidence file was involved.
- The failing RED checks were the expected TDD gates and turned green after their corresponding extraction commits.

## User Setup Required

None - no package, credential, external service, provider, or environment configuration was added.

## Next Phase Readiness

- Plan 41.1-03 can build phase-neutral integrity, artifact, settings, and runtime-import boundaries on top of a dependency-light CLI shell.
- The historical producer mirror and frozen Phase 41 authorities remain unchanged; the refactored code is not represented as their metric producer.

## Self-Check: PASSED

All declared files exist, all six task commits resolve as commits, and the measured facade/module budgets match this summary.

---
*Phase: 41.1-codebase-architecture-overhaul*
*Completed: 2026-08-27*
