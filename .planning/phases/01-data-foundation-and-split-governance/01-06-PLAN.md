---
phase: 01-data-foundation-and-split-governance
plan: 06
type: execute
wave: 6
depends_on:
  - 01-05
files_modified:
  - data/synthetic/generated.jsonl
  - data/processed/validated.jsonl
  - data/processed/quality-stats.json
  - data/splits/train.jsonl
  - data/splits/val.jsonl
  - data/splits/test.jsonl
  - data/manifests/manifest-phase1-uat-gap.json
autonomous: true
requirements:
  - DATA-02
  - DATA-03
gap_closure: true
user_setup:
  - service: anthropic
    why: "Complex synthetic generation per D-06 and D-07"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> API keys"
  - service: gemini
    why: "Bulk generation and judge routing per D-06, D-07, and D-09"
    env_vars:
      - name: GEMINI_API_KEY
        source: "Google AI Studio -> API keys"

must_haves:
  truths:
    - "The workspace retains a synthetic dataset artifact in the 2,000-3,000 JSONL target band for Phase 1 review"
    - "The workspace retains judged validated records, governed split JSONL files, and a manifest produced from the documented Phase 1 command"
    - "Manifest verification passes against the retained split files on this checkout"
  artifacts:
    - path: "data/synthetic/generated.jsonl"
      provides: "Retained synthetic dataset evidence for DATA-02"
      contains: "2000-3000 JSONL records"
    - path: "data/processed/validated.jsonl"
      provides: "Persisted post-judge accepted records"
      contains: "JSONL accepted by QualityJudge.filter_passed"
    - path: "data/processed/quality-stats.json"
      provides: "Judge pass/fail evidence for the retained batch"
      contains: "pass_rate"
    - path: "data/splits/train.jsonl"
      provides: "Governed training split"
      contains: "JSONL train records"
    - path: "data/splits/val.jsonl"
      provides: "Governed validation split"
      contains: "JSONL validation records"
    - path: "data/splits/test.jsonl"
      provides: "Governed test split"
      contains: "JSONL test records"
    - path: "data/manifests/manifest-phase1-uat-gap.json"
      provides: "Versioned lineage and SHA256 integrity evidence"
      contains: "manifest metadata for the retained split set"
  key_links:
    - from: "data/raw/seeds-2026-04-24.jsonl"
      to: "data/synthetic/generated.jsonl"
      via: "Phase 1 CLI `--seed-input` run path from 01-05"
      pattern: "seeds-2026-04-24.jsonl"
    - from: "data/synthetic/generated.jsonl"
      to: "data/processed/validated.jsonl"
      via: "QualityJudge.filter_passed persistence"
      pattern: "validated.jsonl"
    - from: "data/processed/validated.jsonl"
      to: "data/splits/train.jsonl"
      via: "DatasetBuilder.build_splits"
      pattern: "train.jsonl"
    - from: "data/splits/train.jsonl"
      to: "data/manifests/manifest-phase1-uat-gap.json"
      via: "build_manifest/save_manifest"
      pattern: "manifest-phase1-uat-gap.json"
---

<objective>
Materialize and retain the missing Phase 1 synthetic, validated, split, and manifest artifacts using the documented command surface from Plan 01-05 and the retained raw seeds already in the workspace.

Purpose: UAT gaps 3 and 4 are missing retained outputs, not missing business logic. This plan closes them by executing the repo-level Phase 1 flow against the existing raw seed artifact instead of reopening scraping work.

Output: `data/synthetic/generated.jsonl` in the 2,000-3,000 band, `data/processed/validated.jsonl` plus quality stats, governed `train/val/test` split files, and `data/manifests/manifest-phase1-uat-gap.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-foundation-and-split-governance/01-UAT.md
@.planning/phases/01-data-foundation-and-split-governance/01-05-PLAN.md
@readme.md
@src/data_pipeline/cli.py
@src/data_pipeline/generation/generator.py
@src/data_pipeline/generation/quality_judge.py
@src/data_pipeline/versioning/build.py
@data/raw/seeds-2026-04-24.jsonl

<interfaces>
Expected command surface from Plan 01-05:
```bash
python -m src.data_pipeline.cli --seed-input data/raw/seeds-2026-04-24.jsonl --target-count 2500 --version-tag phase1-uat-gap
```

Expected stdout JSON keys from `src.data_pipeline.cli`:

```json
{
  "seed_count": 0,
  "generated_count": 0,
  "validated_count": 0,
  "split_counts": {"train": 0, "val": 0, "test": 0},
  "generated_path": "data/synthetic/generated.jsonl",
  "validated_path": "data/processed/validated.jsonl",
  "manifest_path": "data/manifests/manifest-phase1-uat-gap.json"
}
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the retained-seed Phase 1 flow and keep every UAT artifact in the workspace</name>
  <files>data/synthetic/generated.jsonl, data/processed/validated.jsonl, data/processed/quality-stats.json, data/splits/train.jsonl, data/splits/val.jsonl, data/splits/test.jsonl, data/manifests/manifest-phase1-uat-gap.json</files>
  <action>Use the retained raw seed artifact `data/raw/seeds-2026-04-24.jsonl` as the input source and do not invoke a fresh scrape unless that file is missing or unreadable. First run a reduced-count preflight through the Plan 01-05 command surface to validate the CLI and artifact contract quickly from the retained seed path. That preflight may overwrite the output paths temporarily, so do not retain those interim artifacts in git. Then rerun the same command with `--target-count 2500` so the retained synthetic dataset lands in the required 2,000-3,000 band per DATA-02 and D-13. Keep `data/synthetic/generated.jsonl` as the retained synthetic evidence, `data/processed/validated.jsonl` plus `data/processed/quality-stats.json` as the judged evidence, and `data/splits/{train,val,test}.jsonl` plus `data/manifests/manifest-phase1-uat-gap.json` as the governed lineage outputs per D-14 through D-17 and DATA-03. If the reduced-count preflight fails because the new CLI or artifact persistence is wrong, repair that same slice from Plan 01-05 and rerun the preflight before attempting the full 2,500-record pass; do not reopen the scraper implementation. Preserve the final artifact set in git only after the full-count run and manifest verification pass.</action>
  <verify>
    <automated>python -m src.data_pipeline.cli --seed-input data/raw/seeds-2026-04-24.jsonl --target-count 50 --version-tag phase1-uat-gap && test -s data/synthetic/generated.jsonl && test -s data/processed/validated.jsonl && test -s data/processed/quality-stats.json && test -s data/splits/train.jsonl && test -s data/splits/val.jsonl && test -s data/splits/test.jsonl && python -c "from pathlib import Path; from src.data_pipeline.schemas import ManifestEntry; from src.data_pipeline.versioning.manifest import verify_manifest; manifest = ManifestEntry.model_validate_json(Path('data/manifests/manifest-phase1-uat-gap.json').read_text(encoding='utf-8')); ok, errors = verify_manifest(manifest, Path('data/splits')); assert ok, errors"</automated>
  </verify>
  <done>The workspace contains a retained synthetic dataset in the target band, a persisted judged dataset, governed split JSONL files, and a manifest that verifies cleanly against the retained split artifacts.</done>
</task>

</tasks>

<verification>
- `python -m src.data_pipeline.cli --seed-input data/raw/seeds-2026-04-24.jsonl --target-count 50 --version-tag phase1-uat-gap`
- `python -m src.data_pipeline.cli --seed-input data/raw/seeds-2026-04-24.jsonl --target-count 2500 --version-tag phase1-uat-gap`
- `wc -l data/synthetic/generated.jsonl data/processed/validated.jsonl data/splits/train.jsonl data/splits/val.jsonl data/splits/test.jsonl`
- `python -c "from pathlib import Path; from src.data_pipeline.schemas import ManifestEntry; from src.data_pipeline.versioning.manifest import verify_manifest; manifest = ManifestEntry.model_validate_json(Path('data/manifests/manifest-phase1-uat-gap.json').read_text(encoding='utf-8')); print(verify_manifest(manifest, Path('data/splits')))"`
</verification>

<success_criteria>

- `data/synthetic/generated.jsonl` exists and contains 2,000-3,000 records.
- `data/processed/validated.jsonl` and `data/processed/quality-stats.json` exist and are non-empty.
- `data/splits/train.jsonl`, `val.jsonl`, and `test.jsonl` exist and are non-empty.
- `data/manifests/manifest-phase1-uat-gap.json` verifies against the retained split files.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation-and-split-governance/01-06-SUMMARY.md`
</output>
