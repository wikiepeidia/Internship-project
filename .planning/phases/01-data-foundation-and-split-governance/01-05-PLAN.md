---
phase: 01-data-foundation-and-split-governance
plan: 05
type: execute
wave: 5
depends_on:
  - 01-02
  - 01-03
  - 01-04
files_modified:
  - src/data_pipeline/cli.py
  - tests/data_pipeline/test_phase1_cli.py
  - readme.md
  - START_HERE.md
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
gap_closure: true
user_setup:
  - service: anthropic
    why: "Complex synthetic generation per D-06 and D-07"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> API keys"
  - service: gemini
    why: "Bulk generation and judge routing per D-06, D-07, and D-09"
    env_vars:
      - name: GEMINI_API_KEY
        source: "Google AI Studio -> API keys"

must_haves:
  truths:
    - "A clean operator can run an explicit Phase 1 command path from the repo without reverse-engineering library classes"
    - "The Phase 1 flow persists generated and judged datasets to disk instead of keeping them only in memory"
    - "The documented flow can start from an existing raw seed JSONL so Phase 1 does not reopen scraping unnecessarily"
  artifacts:
    - path: "src/data_pipeline/cli.py"
      provides: "Repo-level Phase 1 command surface"
      exports: ["main", "run_phase1"]
    - path: "tests/data_pipeline/test_phase1_cli.py"
      provides: "Focused regression coverage for the operator command path"
      contains: "test_phase1_cli_uses_seed_input_and_persists_outputs"
    - path: "readme.md"
      provides: "Phase 1 runbook with env vars and exact commands"
      contains: "python -m src.data_pipeline.cli"
    - path: "START_HERE.md"
      provides: "Project-specific quickstart for Phase 1 pipeline execution"
      contains: "data/raw/seeds-2026-04-24.jsonl"
  key_links:
    - from: "src/data_pipeline/cli.py"
      to: "src/data_pipeline/scraper/ncsc_scraper.py"
      via: "optional fresh scrape path when --seed-input is omitted"
      pattern: "NCSCScraper"
    - from: "src/data_pipeline/cli.py"
      to: "src/data_pipeline/generation/generator.py"
      via: "TieredGenerator.generate_dataset and save_generated"
      pattern: "TieredGenerator"
    - from: "src/data_pipeline/cli.py"
      to: "src/data_pipeline/generation/quality_judge.py"
      via: "QualityJudge.filter_passed driving validated artifact persistence"
      pattern: "QualityJudge"
    - from: "src/data_pipeline/cli.py"
      to: "src/data_pipeline/versioning/build.py"
      via: "DatasetBuilder.build_splits for governed split and manifest output"
      pattern: "DatasetBuilder"
---

<objective>
Create the missing operator-facing Phase 1 command path so the existing scraper, generator, judge, and builder can be run from a clean checkout without ad hoc Python snippets.

Purpose: UAT gap 5 is the control failure behind the other unresolved gaps. The repo already contains Phase 1 library code, but nothing currently exposes it as a reproducible repo-level workflow or documents the required environment.

Output: A documented `python -m src.data_pipeline.cli ...` command that can either scrape fresh seeds or start from an existing raw seed JSONL and that reports generated, validated, split, and manifest artifact paths and counts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-foundation-and-split-governance/01-UAT.md
@.planning/phases/01-data-foundation-and-split-governance/01-CONTEXT.md
@.planning/phases/01-data-foundation-and-split-governance/01-RESEARCH.md
@.planning/phases/01-data-foundation-and-split-governance/01-02-SUMMARY.md
@.planning/phases/01-data-foundation-and-split-governance/01-03-SUMMARY.md
@.planning/phases/01-data-foundation-and-split-governance/01-04-SUMMARY.md
@src/data_pipeline/scraper/ncsc_scraper.py
@src/data_pipeline/generation/generator.py
@src/data_pipeline/generation/quality_judge.py
@src/data_pipeline/versioning/build.py
@src/config/settings.py

<interfaces>
From src/data_pipeline/scraper/ncsc_scraper.py:
```python
class NCSCScraper:
    def scrape_advisory_list(
        self,
        max_pages: int = 1,
        max_links_per_page: int = 5,
        max_seeds: int | None = None,
    ) -> list[SeedRecord]
    def save_seeds(self, seeds: list[SeedRecord], output_path: Path | None = None) -> Path
```

From src/data_pipeline/generation/generator.py:

```python
class TieredGenerator:
    def generate_dataset(
        self,
        seeds: list[SeedRecord],
        target_count: int = 2500,
    ) -> list[dict[str, Any]]
    def save_generated(
        self,
        records: list[dict[str, Any]],
        output_path: Path | None = None,
    ) -> Path
```

From src/data_pipeline/generation/quality_judge.py:

```python
class QualityJudge:
    def filter_passed(self, records: list[dict[str, Any]]) -> tuple[list[dict[str, Any]], QualityStats]
```

From src/data_pipeline/versioning/build.py:

```python
class DatasetBuilder:
    def __init__(self, version_tag: str = "v1.0") -> None
    def build_splits(
        self,
        input_path: Path | None = None,
        output_dir: Path | None = None,
        similarity_threshold: float | None = None,
    ) -> dict[str, Any]
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a Phase 1 CLI that bridges scrape, generate, judge, and build</name>
  <files>src/data_pipeline/cli.py, tests/data_pipeline/test_phase1_cli.py</files>
  <behavior>
    - `python -m src.data_pipeline.cli --help` shows the Phase 1 run surface and required flags
    - When `--seed-input` is provided, the command loads that JSONL and does not call the scraper
    - The command writes `data/synthetic/generated.jsonl` and `data/processed/validated.jsonl`
    - The command writes governed split files and a manifest when the full flow succeeds
    - The command prints a machine-readable JSON summary with counts and output paths
    - The command exits non-zero if the seed file is missing, if zero seeds are available, or if judged output is empty
  </behavior>
  <action>Create `src/data_pipeline/cli.py` as the repo-level operator surface for Phase 1. Reuse the existing scraper, generator, judge, and builder implementations instead of rewriting pipeline logic. Load seeds from `--seed-input` when provided so the current `data/raw/seeds-2026-04-24.jsonl` artifact can be used without reopening scraping, per the user constraint and UAT gap. If `--seed-input` is omitted, call `NCSCScraper.scrape_advisory_list()` and `save_seeds()` using the existing scraper defaults per D-01 through D-04; do not modify scraper extraction behavior in this gap-closure slice. Call `TieredGenerator.generate_dataset(..., target_count=...)` and `save_generated()` to persist `data/synthetic/generated.jsonl` per D-06, D-07, D-10, and DATA-02. Call `QualityJudge.filter_passed()` and persist the accepted records to `data/processed/validated.jsonl`; also persist judge stats to `data/processed/quality-stats.json` so the judged batch is retained as evidence per D-09 and the UAT root cause. Call `DatasetBuilder(version_tag=...).build_splits(input_path=Path("data/processed/validated.jsonl"))` to emit governed split files and a manifest per D-14 through D-17 and DATA-03. Return a JSON summary to stdout with `seed_count`, `generated_count`, `validated_count`, `split_counts`, `generated_path`, `validated_path`, and `manifest_path` so later plans can verify outputs without codebase exploration. Fail fast with clear errors when no seed input exists, when target_count is requested inside the 2,000-3,000 band but generated output lands outside it, or when judged output is empty. Cover this with `tests/data_pipeline/test_phase1_cli.py` using mocked scraper, generator, judge, and builder collaborators so the CLI contract is fully executable without live API keys.</action>
  <verify>
    <automated>python -m pytest tests/data_pipeline/test_phase1_cli.py -x --tb=short -v</automated>
  </verify>
  <done>The repo has a single Phase 1 command surface that can start from raw seeds or scrape fresh seeds, persists every downstream artifact path named by the UAT, and is covered by focused CLI tests.</done>
</task>

<task type="auto">
  <name>Task 2: Document the clean Phase 1 operator flow in repo guidance</name>
  <files>readme.md, START_HERE.md</files>
  <action>Replace the empty and template-only repo guidance with a Phase 1 runbook that matches the CLI added in Task 1. Document required environment variables (`ANTHROPIC_API_KEY`, `GEMINI_API_KEY`, optional `OPENROUTER_API_KEY`) and mention that Phase 1 stores dataset artifacts directly in git per D-13. Document the primary full-flow command for a clean checkout: scrape -> generate -> judge -> build. Document the fast path that uses the already retained raw seed artifact `data/raw/seeds-2026-04-24.jsonl` so operators do not reopen the scraper unless they actually need a fresh scrape. Call out the exact output files expected under `data/synthetic`, `data/processed`, `data/splits`, and `data/manifests`, plus the expected 2,000-3,000 target band for `generated.jsonl`. Keep the docs project-specific and concise; remove template-only onboarding language that does not help an operator reproduce Phase 1 outputs.</action>
  <verify>
    <automated>python -m src.data_pipeline.cli --help && rg -n "data/raw/seeds-2026-04-24.jsonl|generated.jsonl|validated.jsonl|python -m src.data_pipeline.cli" readme.md START_HERE.md</automated>
  </verify>
  <done>A clean operator can open the repo and follow explicit Phase 1 commands, including the existing raw-seed fast path and the full scrape-to-build flow, without reverse-engineering Python classes.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/data_pipeline/test_phase1_cli.py -x --tb=short -v`
- `python -m src.data_pipeline.cli --help`
- Repo docs show both the fresh scrape path and the retained-seeds path
</verification>

<success_criteria>

- A repo-level Phase 1 command exists and is test-covered.
- The runbook names the required env vars and exact commands.
- The documented fast path avoids reopening the seed scraper when the retained raw artifact already exists.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation-and-split-governance/01-05-SUMMARY.md`
</output>
