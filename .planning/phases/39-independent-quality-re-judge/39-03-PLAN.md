---
phase: 39-independent-quality-re-judge
plan: 03
type: execute
wave: 3
depends_on: [39-02]
files_modified:
  - src/data_pipeline/judge_merge.py
  - tests/data_pipeline/test_judge_merge.py
  - .planning/codex-final-delta-judge-instructions.md
  - .planning/codex-final-semantic-repair-instructions.md
  - data/processed/phase39-final-judge-carry.jsonl
  - data/processed/phase39-final-judge-delta-targets.jsonl
  - data/processed/phase39-final-judge-batches/iteration-00/manifest.json
  - data/processed/phase39-final-judge-batches/iteration-00/batch-0001-targets.jsonl
  - data/processed/phase39-final-judge-batches/iteration-00/batch-0002-targets.jsonl
  - data/processed/phase39-final-judge-batches/iteration-00/batch-0003-targets.jsonl
  - data/processed/phase39-final-judge-batches/iteration-00/batch-0004-targets.jsonl
  - data/processed/phase39-final-judge-batches/iteration-00/batch-0005-targets.jsonl
  - data/processed/phase39-final-judge-batches/iteration-00/batch-0006-targets.jsonl
  - data/processed/phase39-final-judge-batches/iteration-00/batch-0007-targets.jsonl
  - data/processed/phase39-final-judge-batches/iteration-00/batch-0008-targets.jsonl
  - data/processed/phase39-final-judge-batches/iteration-00/batch-0009-targets.jsonl
autonomous: true
requirements: [JUDGE-01]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "All seven DatasetRecord fields participate in final-snapshot identity; changing any one field forces a fresh judgment."
    - "A historical verdict carries only from one unique exact-record digest, with unchanged scores/pass/reason and rebased final coordinates."
    - "The locked candidate partitions into exactly 1,562 carries and 541 fresh targets before semantic repairs, with no overlap or uncovered final identity."
    - "The 541 targets are materialized in nine deterministic restartable batches of 64 rows except the final 29-row batch, each bound by SHA-256 in an iteration manifest."
    - "A machine-readable semantic-convergence schema and validator bind every candidate, target, judgment, repair, and re-judgment hash and cannot pass with unresolved rows or a repaired digest lacking a fresh verdict."
    - "This plan prepares and validates the work queue only; it does not fabricate or prefill any fresh judgment."
  artifacts:
    - path: "src/data_pipeline/judge_merge.py"
      provides: "Exact-record carry/delta preparation, deterministic batch manifests, convergence schema validation, and final composition contracts"
    - path: "data/processed/phase39-final-judge-delta-targets.jsonl"
      provides: "All unmatched current records with final coordinates, seven fields, and record digest"
    - path: "data/processed/phase39-final-judge-batches/iteration-00/manifest.json"
      provides: "Restart contract binding nine target batches and expected result paths/hashes"
    - path: ".planning/codex-final-delta-judge-instructions.md"
      provides: "Current-session per-row scoring contract for the prepared batches"
    - path: ".planning/codex-final-semantic-repair-instructions.md"
      provides: "Restricted risk/span/XAI repair contract for convergence iterations"
  key_links:
    - from: "data/processed/phase39-mislabel-candidate/splits"
      to: "data/processed/phase39-final-judge-delta-targets.jsonl"
      via: "unique seven-field digest comparison with historical judge-merged records"
      pattern: "dataset_record_digest"
    - from: "data/processed/phase39-final-judge-delta-targets.jsonl"
      to: "data/processed/phase39-final-judge-batches/iteration-00/manifest.json"
      via: "final-coordinate ordering and fixed 64-row batch partition"
      pattern: "batch-0001-targets.jsonl"
    - from: "data/processed/phase39-semantic-convergence.json"
      to: "src/data_pipeline/judge_merge.py"
      via: "recompute-and-validate every declared candidate/target/result/repair hash"
      pattern: "unresolved_count"
---

<objective>
Build and run the deterministic preparation layer for the final Codex delta:
exact carries, fresh targets, restartable batch files, and a fail-closed semantic
convergence contract.

Purpose: separate evidence tooling and target preparation from the large
row-by-row judgment workload, while proving that no stale coordinate or partial
batch can become final evidence.

Output: tested judge tooling, exact carry/delta artifacts, nine initial target
batches, two operator instruction files, and a validator contract consumed by
Plans 39-04 and 39-05.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-independent-quality-re-judge/39-CONTEXT.md
@.planning/phases/39-independent-quality-re-judge/39-RESEARCH.md
@.planning/phases/39-independent-quality-re-judge/39-PATTERNS.md
@.planning/phases/39-independent-quality-re-judge/39-02-SUMMARY.md
@.planning/codex-judge-instructions.md
@src/data_pipeline/judge_merge.py
@src/data_pipeline/apply_mislabel_triage.py
@data/processed/phase39-mislabel-candidate/manifest.json

<interfaces>
Retain the existing closed `CodexJudgeResult` five-score schema. Add
`dataset_record_digest()` over a Pydantic-validated, sorted compact JSON dump of
all seven DatasetRecord fields and `judge_evidence_digest()` over five scores,
pass, and reason. `SemanticConvergence` is a closed versioned model whose
validator reads every referenced file and recomputes rather than trusts hashes.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove exact carry, fresh delta, restartable batching, and semantic-convergence validation end to end</name>
  <files>src/data_pipeline/judge_merge.py, tests/data_pipeline/test_judge_merge.py, .planning/codex-final-delta-judge-instructions.md, .planning/codex-final-semantic-repair-instructions.md</files>
  <read_first>
    src/data_pipeline/judge_merge.py and tests/data_pipeline/test_judge_merge.py (retain the Plan 39-01 same-snapshot lane and error conventions);
    src/data_pipeline/schemas.py (`DatasetRecord` seven-field authority);
    src/data_pipeline/apply_mislabel_triage.py (`CodexSemanticRepairDecision` and staged candidate validator);
    .planning/codex-judge-instructions.md (same five scoring dimensions and pass threshold);
    .planning/phases/39-independent-quality-re-judge/39-RESEARCH.md (unique-digest carry policy and locked counts)
  </read_first>
  <behavior>
    - All-seven-field digest sensitivity and unique-only historical carry are unit-proven; ambiguous historical digests route to fresh targets.
    - Carried evidence retains all five scores, pass, and reason exactly while final split/row_index are rebased and old coordinates remain provenance only.
    - Carry and delta sets are disjoint and exhaustive over final `(split,row_index,seed_id,record_digest)` identities.
    - Batch creation uses final-coordinate order, size 64, explicit target/result paths, record counts, target SHA-256, completion state, and result SHA-256 only after strict validation.
    - An existing valid completed batch is restart-idempotent; a missing, partial, wrong-target, duplicate, or hash-conflicting result remains incomplete and is never overwritten silently.
    - `SemanticConvergence` records initial candidate/target/carry hashes, ordered iterations, per-batch hashes, repair hashes, before/after digests, re-judgment hashes, unresolved identities, and final candidate/result hashes.
    - The convergence validator opens every declared artifact, recomputes each hash/coverage relation, requires `unresolved_count == 0`, and proves each repaired after-digest has a fresh result from a later iteration.
  </behavior>
  <action>
    Extend `judge_merge.py` with final-snapshot preparation and validation subcommands without breaking the existing merge CLI. Implement closed models for batch manifest entries and semantic convergence. Never accept old split/row_index as record identity. Compose carried results into the original judge-result shape only after final coordinates are known; place old coordinates, origin, record/evidence digests, and source hashes in provenance.

    Write `.planning/codex-final-delta-judge-instructions.md` using the same five dimensions and pass rule as the historical instructions. Require actual row-by-row current-session judgment, exact target coordinates/seed, one non-empty reason, and no heuristic/default/copied verdict. State honestly that unchanged records are exact carries and the fresh work covers only the delta.

    Write `.planning/codex-final-semantic-repair-instructions.md` for a separate closed artifact targeting all admitted relabels plus any fresh judgment that exposes risk/span/XAI inconsistency. Repairs may change only risk_tier, suspicious_spans, and xai_explanation, must bind the expected record digest, keep literal spans, and never alter label/text/source/seed or create seed diversity.

    Add tests for one exact carry plus one fresh record, a one-field digest change, ambiguity, coordinate rebasing, fixed-size batches, completed-batch restart, truncated/conflicting results, all convergence hash fields, zero-unresolved enforcement, and mandatory later fresh verdict for every repaired after-digest.
  </action>
  <verify>
    <automated>python -m pytest tests/data_pipeline/test_judge_merge.py -k "digest or carry or delta or batch or convergence" -q</automated>
  </verify>
  <acceptance_criteria>
    The tracer follows one record through carry and one through fresh repair/re-judgment to a valid zero-unresolved convergence object;
    any undeclared mutation, stale coordinate, missing artifact, hash mismatch, repaired-without-rejudge state, or nonzero unresolved set fails;
    instructions retain the five-score comparability contract and prohibit fabricated fresh judgments;
    the existing same-snapshot judge-merge tests remain green.
  </acceptance_criteria>
  <done>The preparation, batch restart, and convergence-validation contracts are production-tested before processing the live delta.</done>
</task>

<task type="auto">
  <name>Task 2: Materialize the locked 1,562-carry and nine-batch 541-row fresh work queue</name>
  <files>data/processed/phase39-final-judge-carry.jsonl, data/processed/phase39-final-judge-delta-targets.jsonl, data/processed/phase39-final-judge-batches/iteration-00/manifest.json, data/processed/phase39-final-judge-batches/iteration-00/batch-0001-targets.jsonl, data/processed/phase39-final-judge-batches/iteration-00/batch-0002-targets.jsonl, data/processed/phase39-final-judge-batches/iteration-00/batch-0003-targets.jsonl, data/processed/phase39-final-judge-batches/iteration-00/batch-0004-targets.jsonl, data/processed/phase39-final-judge-batches/iteration-00/batch-0005-targets.jsonl, data/processed/phase39-final-judge-batches/iteration-00/batch-0006-targets.jsonl, data/processed/phase39-final-judge-batches/iteration-00/batch-0007-targets.jsonl, data/processed/phase39-final-judge-batches/iteration-00/batch-0008-targets.jsonl, data/processed/phase39-final-judge-batches/iteration-00/batch-0009-targets.jsonl</files>
  <precondition>The Plan 39-02 candidate reloads successfully and its run descriptor still binds the locked live input, decision, and historical judge hashes.</precondition>
  <read_first>
    data/processed/phase39-mislabel-candidate/splits/{train,val,test}.jsonl and manifest.json (only target snapshot);
    data/processed/judge-merged.jsonl (historical evidence source, not coordinate authority);
    src/data_pipeline/judge_merge.py (`prepare-final` and `validate-batches`);
    .planning/codex-final-delta-judge-instructions.md (result path and schema contract)
  </read_first>
  <action>
    Run `prepare-final` against the reload-validated candidate and historical merged judge. Require the locked pre-repair hashes before asserting projected constants. Write the 1,562 exact carry rows and 541 fresh targets in final-coordinate order. Fail if any final identity is missing, duplicated, or appears in both sets.

    Split the 541 targets deterministically into iteration-00 batches: `batch-0001` through `batch-0008` each contain 64 rows and `batch-0009` contains 29. For every batch, the manifest declares the exact target path, future result path with the same batch number, target count/hash, first/last final coordinate, `status="pending"`, and null result hash. Write all files as one staged bundle, reload them, and verify their concatenation equals the aggregate target file byte-for-record and in order.

    Do not create result JSONL files or mark any batch complete in this task. Record the candidate manifest hash, historical merged hash, carry hash, aggregate-target hash, and batch-manifest hash for Plan 39-04.
  </action>
  <verify>
    <automated>python -m src.data_pipeline.judge_merge validate-batches --candidate-dir data/processed/phase39-mislabel-candidate --carry data/processed/phase39-final-judge-carry.jsonl --targets data/processed/phase39-final-judge-delta-targets.jsonl --batch-manifest data/processed/phase39-final-judge-batches/iteration-00/manifest.json --require-status pending</automated>
  </verify>
  <acceptance_criteria>
    Locked live projection yields 1,562 unique carries plus 541 unique targets covering all 2,103 candidate records;
    nine target batches exist with counts 64/64/64/64/64/64/64/64/29 and exact aggregate order;
    all target and manifest hashes reproduce on reload;
    no result path exists and no batch is falsely marked complete;
    live canonical splits/manifest and all user-edited sheets retain their input hashes.
  </acceptance_criteria>
  <done>The exact fresh work queue is restartable, hash-bound, and ready for actual current-session Codex judgment with no result fabricated during preparation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Historical evidence -> final candidate | Unchanged records may carry; changed or ambiguous records must not. |
| Aggregate targets -> restartable batches | Partial or conflicting batch state must not look complete. |
| Convergence summary -> promotion | A self-declared JSON status must be recomputed from all underlying artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-39-03-01 | Tampering | historical carry | high | mitigate | Unique all-seven-field digest, unchanged evidence digest, and final-coordinate rebasing. |
| T-39-03-02 | Tampering / Denial of Service | batch state | high | mitigate | Fixed paths/counts, target/result hashes, strict closed-schema coverage, and conflict-safe restart rules. |
| T-39-03-03 | Repudiation | convergence claim | high | mitigate | Validator consumes every declared artifact, recomputes all hashes, requires zero unresolved rows and later fresh verdicts for repaired digests. |
| T-39-03-04 | Information Disclosure | local targets | low | accept | Processing remains local with no web, plugin, provider SDK, or external API. |
</threat_model>

<verification>
- Focused and full `tests/data_pipeline/test_judge_merge.py` pass.
- Live batch validator proves exact 2,103-record partition and nine pending batches.
- Pre/post hashes prove no canonical corpus or user review artifact changed.
</verification>

<success_criteria>
The full fresh-delta workload and all integrity contracts exist before any model judgment begins, and promotion cannot later rely on an unverified convergence assertion.
</success_criteria>

<output>
Create `.planning/phases/39-independent-quality-re-judge/39-03-SUMMARY.md` when done.
</output>
