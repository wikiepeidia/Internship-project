---
phase: 39-independent-quality-re-judge
plan: 05
type: execute
wave: 5
depends_on: [39-04]
files_modified:
  - data/backup/pre-phase39-mislabel-triage/processed/codex-judge-pass.jsonl
  - data/backup/pre-phase39-mislabel-triage/processed/judge-merged.jsonl
  - data/backup/pre-phase39-mislabel-triage/processed/judge-summary.json
  - data/splits/train.jsonl
  - data/splits/val.jsonl
  - data/splits/test.jsonl
  - data/manifests/manifest.json
  - data/processed/phase39-mislabel-decision-manifest.jsonl
  - data/processed/phase39-mislabel-quarantine.jsonl
  - data/processed/phase39-seed-cap-drops.jsonl
  - data/processed/codex-judge-pass.jsonl
  - data/processed/judge-merged.jsonl
  - data/processed/judge-summary.json
  - data/processed/phase39-final-judge-provenance.jsonl
  - .planning/phases/39-independent-quality-re-judge/39-DOWNSTREAM-DATA-CONTRACT.json
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/phases/40-multi-model-training-evidence/40-CONTEXT.md
  - tests/data_pipeline/test_apply_mislabel_triage.py
autonomous: true
requirements: [JUDGE-01]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Promotion cannot begin unless the semantic-convergence validator consumes every referenced artifact and proves zero unresolved identities plus a later fresh verdict for every repaired digest."
    - "Historical 2,421-row judge raw/merged/summary bytes are backed up and hash-verified before canonical processed paths are replaced."
    - "Final splits, manifest, decision/quarantine/cap provenance, complete judge result, merged evidence, stats, and judge provenance promote in one rollback-capable transaction."
    - "The live final judge bundle covers every promoted current record exactly once with disjoint exact-carry/fresh origins and recomputable descriptive statistics."
    - "Phase 40 and Phase 41 live contracts in PROJECT, REQUIREMENTS, ROADMAP, STATE, and 40-CONTEXT derive counts and SHA-256 values from the promoted manifest; no active contract retains 2,403, 1,900/252/251, or the old test hash."
    - "The final human review still blocks Phase 40 through the Phase 39 dependency, but Phase 42 no longer blocks JUDGE-03 or training readiness."
  artifacts:
    - path: "data/manifests/manifest.json"
      provides: "Canonical semantically converged Phase 39 corpus identity and chronology"
    - path: "data/processed/codex-judge-pass.jsonl"
      provides: "Complete current-coordinate final judge result"
    - path: "data/processed/phase39-final-judge-provenance.jsonl"
      provides: "Per-record carry/fresh origin, digest, evidence hash, and iteration"
    - path: ".planning/phases/39-independent-quality-re-judge/39-DOWNSTREAM-DATA-CONTRACT.json"
      provides: "Machine-readable manifest hash, split counts/hashes, label counts, and held-out boundary consumed by later phases"
    - path: ".planning/phases/40-multi-model-training-evidence/40-CONTEXT.md"
      provides: "Manifest-derived Phase 40 prerequisite with refreshed counts and hashes"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Manifest-derived EVAL-08 held-out and EVAL-09 all-data-fit contracts"
  key_links:
    - from: "data/processed/phase39-semantic-convergence.json"
      to: "data/manifests/manifest.json"
      via: "blocking recomputation before final payload construction"
      pattern: "unresolved_count.*0"
    - from: "data/processed/codex-judge-pass.jsonl"
      to: "data/processed/judge-merged.jsonl"
      via: "exact promoted split/row_index/seed_id/record-digest join"
      pattern: "fresh_final_delta|carried_forward_exact_record"
    - from: "data/manifests/manifest.json"
      to: ".planning/phases/40-multi-model-training-evidence/40-CONTEXT.md"
      via: "mechanically rendered records and SHA-256 values"
      pattern: "39-DOWNSTREAM-DATA-CONTRACT.json"
    - from: "data/manifests/manifest.json"
      to: ".planning/ROADMAP.md Phase 41"
      via: "refreshed held-out row count and test SHA-256"
      pattern: "Held-Out Evaluation Discipline"
    - from: ".planning/phases/39-independent-quality-re-judge/39-DOWNSTREAM-DATA-CONTRACT.json"
      to: ".planning/PROJECT.md and .planning/REQUIREMENTS.md"
      via: "refreshed active target-feature and EVAL-08/EVAL-09 counts"
      pattern: "EVAL-08|EVAL-09"
---

<objective>
Atomically freeze the converged corpus and complete judge evidence, then refresh
every live downstream training/evaluation contract from that promoted manifest.

Purpose: prevent a valid new corpus from being paired with stale judge files or
old Phase 40/41 counts and hashes.

Output: one recoverable canonical release, byte-preserved historical judge
backup, complete current-snapshot judge/stat provenance, and manifest-derived
ROADMAP/STATE/Phase 40 contracts.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-independent-quality-re-judge/39-CONTEXT.md
@.planning/phases/39-independent-quality-re-judge/39-04-SUMMARY.md
@.planning/phases/40-multi-model-training-evidence/40-CONTEXT.md
@src/data_pipeline/apply_mislabel_triage.py
@src/data_pipeline/judge_merge.py
@data/processed/phase39-mislabel-candidate/manifest.json
@data/processed/phase39-semantic-convergence.json
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Promote corpus and final judge evidence as one verified rollback-capable release</name>
  <files>data/backup/pre-phase39-mislabel-triage/processed/codex-judge-pass.jsonl, data/backup/pre-phase39-mislabel-triage/processed/judge-merged.jsonl, data/backup/pre-phase39-mislabel-triage/processed/judge-summary.json, data/splits/train.jsonl, data/splits/val.jsonl, data/splits/test.jsonl, data/manifests/manifest.json, data/processed/phase39-mislabel-decision-manifest.jsonl, data/processed/phase39-mislabel-quarantine.jsonl, data/processed/phase39-seed-cap-drops.jsonl, data/processed/codex-judge-pass.jsonl, data/processed/judge-merged.jsonl, data/processed/judge-summary.json, data/processed/phase39-final-judge-provenance.jsonl</files>
  <precondition>`validate-convergence --require-zero-unresolved` succeeds against the current staged candidate and complete fresh-results file.</precondition>
  <read_first>
    data/processed/phase39-semantic-convergence.json (machine-readable gate; do not trust without validator);
    src/data_pipeline/judge_merge.py (`validate-convergence`, final composition, and evidence encoders);
    src/data_pipeline/apply_mislabel_triage.py (locked-input recheck, candidate reload, history-preserving manifest, all-file promotion/rollback);
    src/data_pipeline/reconstruct_zalo_direct_catalog.py (`_replace_payload_bundle` and restoration verification);
    data/manifests/manifest.json and existing processed judge files (original bytes to preserve/replace)
  </read_first>
  <action>
    First run the convergence validator so it opens every candidate, target, batch, result, repair, and re-judgment path named by `phase39-semantic-convergence.json`, recomputes all hashes, proves zero unresolved rows, and proves every repaired after-digest has a later fresh verdict. A summary flag alone is never sufficient.

    Copy the current `codex-judge-pass.jsonl`, `judge-merged.jsonl`, and `judge-summary.json` byte-for-byte to an empty `data/backup/pre-phase39-mislabel-triage/processed/` directory. If a backup path exists, accept it only when its bytes equal the original locked historical hash; otherwise stop. Record backup hashes in new manifest/judge provenance.

    Compose the final current-coordinate judge JSONL by combining unique exact carries with all latest fresh digests, then merge against the converged staged splits and compute descriptive statistics only from that complete file. Build all final payloads in staging: three splits; deep-copied history-preserving manifest with `task_scam_mislabel_triage`, semantic convergence and judge evidence blocks; decision/quarantine/cap sidecars; combined judge; merged judge; summary; and per-record provenance.

    Acquire the exclusive finalization lock, re-hash every locked input and staged artifact immediately before promotion, then replace all canonical destinations through one rollback-capable bundle. Post-write reload must prove DatasetRecord schema, literal spans, normalized/0.95 duplicate absence, seed-disjointness, all-label support, 8% cap, ratios, manifest byte/count/hash entries, exact judge coordinate/seed/digest coverage, disjoint origins, unchanged carried evidence, fresh repaired-digest evidence, and recomputed stats. Any failure restores and hash-checks every original destination, including previously absent sidecars.
  </action>
  <verify>
    <automated>python -m src.data_pipeline.judge_merge validate-final-release --splits-dir data/splits --manifest data/manifests/manifest.json --judge-results data/processed/codex-judge-pass.jsonl --merged data/processed/judge-merged.jsonl --summary data/processed/judge-summary.json --provenance data/processed/phase39-final-judge-provenance.jsonl --convergence data/processed/phase39-semantic-convergence.json</automated>
  </verify>
  <acceptance_criteria>
    Historical backup hashes match the pre-promotion 2,421-row evidence bytes;
    final canonical corpus remains 2,103 rows with 1,665/218/220 split counts unless an earlier explicit recomputation changed the approved projection;
    final judge/merged/provenance cover every promoted record exactly once and stats reproduce;
    convergence is independently revalidated as zero unresolved with fresh evidence for every repaired digest;
    injected write/post-write/rollback/concurrency tests from Plans 39-02/03 remain green;
    user review artifacts retain their pre-task hashes.
  </acceptance_criteria>
  <done>The converged corpus and full current-snapshot judge evidence are one canonical, recoverable, hash-verified release.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refresh every authoritative Phase 40 and 41 data contract from the promoted manifest</name>
  <files>.planning/phases/39-independent-quality-re-judge/39-DOWNSTREAM-DATA-CONTRACT.json, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md, .planning/phases/40-multi-model-training-evidence/40-CONTEXT.md, tests/data_pipeline/test_apply_mislabel_triage.py</files>
  <read_first>
    data/manifests/manifest.json (sole authority for current split counts/hashes/distribution);
    .planning/PROJECT.md (active milestone target features and deployment-fit boundary; preserve dated historical milestone text);
    .planning/ROADMAP.md (Phase 40 dependency/input contract and Phase 41 held-out/all-data contract);
    .planning/REQUIREMENTS.md (active EVAL-08 held-out count and EVAL-09 all-data-fit total);
    .planning/STATE.md (current milestone focus and live Phase 40 planning decision; historical quick-task rows remain historical);
    .planning/phases/40-multi-model-training-evidence/40-CONTEXT.md (`Final-corpus prerequisite` and evaluation-boundary hardcoded values);
    tests/data_pipeline/test_apply_mislabel_triage.py (live invariant-test conventions)
  </read_first>
  <behavior>
    - Downstream contract JSON exactly reproduces promoted manifest SHA-256, each split's SHA-256/records/bytes, per-label distribution, total rows, and held-out test identity.
    - ROADMAP Phase 40 uses final manifest-derived train/validation/test counts and hashes, and Phase 41 uses the final test count/hash and all-data total.
    - PROJECT active target features use the final held-out test count and final all-data deployment-fit total; dated completed-milestone/history statements remain unchanged.
    - REQUIREMENTS EVAL-08 and EVAL-09 use the final held-out test count and final all-data deployment-fit total from the same contract JSON.
    - Phase 40 context replaces every live 2,403, 1,900/252/251, and old split/manifest hash with promoted values and points to the downstream contract JSON.
    - STATE current milestone/Phase 40 planning text uses final manifest-derived wording; append-only historical entries remain unchanged and are explicitly excluded from the active-contract assertion.
    - A test fails if any active Phase 40/41 contract in PROJECT, REQUIREMENTS, ROADMAP, STATE, or 40-CONTEXT disagrees with the current manifest or retains old hardcoded values/hash.
  </behavior>
  <action>
    Generate `39-DOWNSTREAM-DATA-CONTRACT.json` directly from the promoted manifest and split bytes. Include manifest path/hash/version, total rows, each split's records/bytes/hash, label counts, split salt, held-out test identity, generation timestamp, and the rule that Phase 40 cannot read test rows while Phase 41 evaluates this exact test hash once.

    Update only live contracts: PROJECT active milestone target features for the reserved test and post-evaluation all-data fit; REQUIREMENTS EVAL-08/EVAL-09; ROADMAP Phase 40 dependency/input criterion; ROADMAP Phase 41 test/all-data criteria; STATE current milestone focus and current Phase 40 planning decision; and all current prerequisite/evaluation values in `40-CONTEXT.md`. Derive inserted numbers/hashes from the generated JSON rather than transcribing remembered constants. State that Phase 40 waits for Phase 39's completed human/report gates, not Phase 42. Preserve clearly dated completed-milestone and historical snapshot statements byte-for-byte.

    Add a live contract test that loads the manifest/contract JSON and parses PROJECT's active/target-feature region, REQUIREMENTS EVAL-08/EVAL-09, and the named ROADMAP/STATE/40-CONTEXT regions. It must reject the old test hash and old 2,403/1,900/252/251 values in active regions while allowing clearly dated historical Phase 39 quick-task and completed-milestone records elsewhere.
  </action>
  <verify>
    <automated>python -m pytest tests/data_pipeline/test_apply_mislabel_triage.py -k "downstream_contract" -q</automated>
  </verify>
  <acceptance_criteria>
    Machine contract and all active Phase 40/41 prose in PROJECT, REQUIREMENTS, ROADMAP, STATE, and 40-CONTEXT exactly match the promoted manifest and split bytes;
    old canonical counts/hashes are absent from active contract regions;
    ROADMAP says Phase 40 depends on completed Phase 39, with no Phase 42 requirement;
    historical append-only records remain accurate and unedited;
    focused and full data-pipeline tests pass.
  </acceptance_criteria>
  <done>All training/evaluation planning now points to the promoted final corpus, eliminating stale snapshot assumptions before the human/report closure gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Convergence ledger -> canonical promotion | Promotion must recompute evidence, not trust declared completion. |
| Multi-file staging -> live release | Partial replacement would split corpus, manifest, and judge evidence. |
| Final manifest -> downstream plans | Stale counts/hashes could train or evaluate the wrong snapshot. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-39-05-01 | Tampering | convergence gate | high | mitigate | Consume/recompute every referenced artifact, zero unresolved, and repaired-digest fresh-verdict proof. |
| T-39-05-02 | Tampering / Denial of Service | promotion bundle | high | mitigate | Exclusive lock, pre-write hash lock, all-destination verified rollback, and idempotent exact-hash retry. |
| T-39-05-03 | Repudiation | historical judge replacement | medium | mitigate | Byte-identical verified backup with hashes recorded in final provenance. |
| T-39-05-04 | Tampering | downstream contract | high | mitigate | Generate from manifest bytes and test active ROADMAP/STATE/Phase40 regions against the machine contract. |
</threat_model>

<verification>
- Final release validator passes from canonical files plus convergence artifact.
- Full data-pipeline tests pass, including transaction failure injection and downstream contract drift.
- Read-only hashes prove historical backup and user sheets remain intact.
</verification>

<success_criteria>
JUDGE-01 is complete on the promoted final snapshot, and every live Phase 40/41 contract uses its actual manifest-derived counts and hashes before any training begins.
</success_criteria>

<output>
Create `.planning/phases/39-independent-quality-re-judge/39-05-SUMMARY.md` when done.
</output>
