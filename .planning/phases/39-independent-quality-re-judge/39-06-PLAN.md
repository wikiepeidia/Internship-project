---
phase: 39-independent-quality-re-judge
plan: 06
type: execute
wave: 6
depends_on: [39-05]
files_modified:
  - src/data_pipeline/manual_review_sheet.py
  - tests/data_pipeline/test_manual_review_sheet.py
  - .planning/phases/39-independent-quality-re-judge/39-final-manual-review-sheet.md
autonomous: false
requirements: [JUDGE-02]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A new deterministic 100-row sheet samples the promoted final manifest, covering judge pass/fail, all four labels, and exact-carry/fresh judge origins."
    - "A prior human verdict is prefilled only when both the seven-field record digest and complete five-score/pass/reason evidence digest match uniquely and the old checkbox is unambiguous."
    - "Changed, stale, ambiguous, contradictory, or previously blank items remain blank; no model or automation supplies the final human verdict."
    - "A Vietnamese-fluent reviewer personally marks exactly one PASS/FAIL for every remaining row at a blocking checkpoint that cannot auto-approve."
    - "The old manual sheet, old triage sheet, and authoritative compact triage evidence remain byte-identical; the final review uses a new path."
    - "This plan ends only when a read-only validator proves 100 unique, unambiguous final-snapshot decisions; summary/report claims are produced afterward in Plan 39-07."
  artifacts:
    - path: "src/data_pipeline/manual_review_sheet.py"
      provides: "Final-snapshot multi-axis sampling, exact-evidence human carry, and strict read-only completion validation"
    - path: ".planning/phases/39-independent-quality-re-judge/39-final-manual-review-sheet.md"
      provides: "Exactly 100 promoted-final-snapshot examples with genuine human verdicts"
  key_links:
    - from: "data/processed/judge-merged.jsonl"
      to: ".planning/phases/39-independent-quality-re-judge/39-final-manual-review-sheet.md"
      via: "deterministic label/status/origin stratification"
      pattern: "phase39-final-manual-review-v1"
    - from: ".planning/phases/39-independent-quality-re-judge/39-manual-review-sheet.md"
      to: ".planning/phases/39-independent-quality-re-judge/39-final-manual-review-sheet.md"
      via: "unique record plus judge-evidence digest carry only"
      pattern: "carried_forward_exact_evidence"
    - from: "data/manifests/manifest.json"
      to: ".planning/phases/39-independent-quality-re-judge/39-final-manual-review-sheet.md"
      via: "embedded manifest SHA-256 checked on generation and completion"
      pattern: "manifest_sha256"
---

<objective>
Create and genuinely complete a 100-row human review over the promoted final
snapshot, carrying old human work only where record and judge evidence are
provably identical.

Purpose: close the semantic-human boundary honestly and prevent the stale
pre-repair sheet from being presented as final evidence.

Output: tested review tooling and a completed, strictly validated final sheet.
The manifest-bound summary and report edits deliberately follow in Plan 39-07.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-independent-quality-re-judge/39-CONTEXT.md
@.planning/phases/39-independent-quality-re-judge/39-RESEARCH.md
@.planning/phases/39-independent-quality-re-judge/39-05-SUMMARY.md
@.planning/phases/39-independent-quality-re-judge/39-manual-review-sheet.md
@data/manifests/manifest.json
@data/processed/judge-merged.jsonl
@data/processed/phase39-final-judge-provenance.jsonl
@data/backup/pre-phase39-mislabel-triage/processed/judge-merged.jsonl
@src/data_pipeline/judge_merge.py
@src/data_pipeline/manual_review_sheet.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one exact prior-human carry and one mandatory fresh-human decision</name>
  <files>src/data_pipeline/manual_review_sheet.py, tests/data_pipeline/test_manual_review_sheet.py</files>
  <read_first>
    src/data_pipeline/manual_review_sheet.py and its tests (legacy deterministic sampler/renderer and multiline safety);
    src/data_pipeline/judge_merge.py (`dataset_record_digest`, `judge_evidence_digest`, final merged/provenance shape);
    .planning/phases/39-independent-quality-re-judge/39-manual-review-sheet.md (historical decisions, read only);
    data/backup/pre-phase39-mislabel-triage/processed/judge-merged.jsonl (authoritative old record/evidence mapping);
    data/processed/judge-merged.jsonl and final provenance (current sample authority)
  </read_first>
  <behavior>
    - Exactly 100 unique final record digests are selected deterministically when the corpus has at least 100 records.
    - Selection represents judge PASS/FAIL, four labels, and carried/fresh origins; sparse strata fill by greatest underrepresentation then stable hash.
    - Old verdict carry requires one old section, one checked box, exact seven-field record digest, and exact evidence digest over five scores/pass/reason.
    - Any record/evidence field change, blank/dual mark, duplicate, unknown token, stale manifest, or ambiguous mapping prevents carry or completion.
    - Carried rows show `human_verdict_origin=carried_forward_exact_evidence`; all others show `pending_final_human` and blank boxes.
    - Read-only completion mode proves 100 unique sections and exactly one human verdict without writing summary/report output.
  </behavior>
  <action>
    Add a final-snapshot lane while preserving legacy Plan 39-01 behavior. Reuse digest functions from `judge_merge.py`. Parse the historical sheet defensively and reconstruct its authoritative row/evidence from the backed-up historical merged file before carrying a verdict; never trust old coordinates or displayed text alone.

    Implement deterministic three-axis allocation using salt `phase39-final-manual-review-v1`. Render record/evidence digests, current manifest hash, judge origin, human origin, all seven record fields, five scores, pass, reason, and blank or exact-carried verdict. Implement `validate-final --require-complete --check-only` that performs no write and rejects any missing/dual/tampered/duplicated section.

    Add tests for every record/evidence field mismatch, old blanks/dual marks, stale Zalo no-carry, multi-axis coverage, sparse fill, embedded newlines, exactly 100 identities, manifest drift, evidence tampering, and read-only completion.
  </action>
  <verify>
    <automated>python -m pytest tests/data_pipeline/test_manual_review_sheet.py -k "final or carry or evidence or manifest or multi_axis" -q</automated>
  </verify>
  <acceptance_criteria>
    One exact historical decision carries and one changed record remains blank in the tracer;
    changing any of seven record fields or five-score/pass/reason evidence blocks carry;
    sample is deterministic, unique, exactly 100, and covers every available dimension;
    completion validation fails on 99/101 sections, blank/dual mark, duplicate digest, altered evidence, or wrong manifest;
    historical sheet generation remains regression-tested and no old review path is writable.
  </acceptance_criteria>
  <done>The final review generator can reuse only provably identical human evidence and require genuine review everywhere else.</done>
</task>

<task type="auto">
  <name>Task 2: Generate and validate the pending final-snapshot 100-row sheet</name>
  <files>.planning/phases/39-independent-quality-re-judge/39-final-manual-review-sheet.md</files>
  <precondition>The promoted manifest, final merged judge, and final provenance pass Plan 39-05's release validator and bind the same split hashes.</precondition>
  <read_first>
    data/manifests/manifest.json;
    data/processed/judge-merged.jsonl;
    data/processed/phase39-final-judge-provenance.jsonl;
    .planning/phases/39-independent-quality-re-judge/39-manual-review-sheet.md (read only)
  </read_first>
  <action>
    Generate `39-final-manual-review-sheet.md` from the promoted final evidence with sample size 100 and the locked salt. Before and after, hash the old manual sheet, old triage sheet, and compact triage evidence and fail on any byte change.

    Run pending-mode validation: exactly 100 unique current identities; all four labels; judge pass/fail; both judge origins where available; matching manifest/record/evidence digests; and no invalid prefill. Record the number of exact carried decisions and remaining human decisions in the sheet header, but do not generate completion statistics.
  </action>
  <verify>
    <automated>python -m src.data_pipeline.manual_review_sheet validate-final --sheet .planning/phases/39-independent-quality-re-judge/39-final-manual-review-sheet.md --merged data/processed/judge-merged.jsonl --provenance data/processed/phase39-final-judge-provenance.jsonl --manifest data/manifests/manifest.json --allow-pending --check-only</automated>
  </verify>
  <acceptance_criteria>
    Validator reports 100 unique final examples and no invalid carry;
    all required sample dimensions are represented or sparse unavailability is explicitly recorded;
    every non-carried row remains blank for the human;
    no summary/manual pass-rate claim exists yet;
    three historical review/evidence hashes are unchanged.
  </acceptance_criteria>
  <done>The pending final sheet is exact-snapshot, stratified, and ready for the only unavoidable human action.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Complete every remaining final-snapshot verdict as the Vietnamese-fluent reviewer</name>
  <files>.planning/phases/39-independent-quality-re-judge/39-final-manual-review-sheet.md</files>
  <action>The Vietnamese-fluent reviewer personally reads every row marked `pending_final_human` and selects exactly one PASS or FAIL. This checkpoint cannot be auto-approved because automating the semantic verdict would invalidate JUDGE-02.</action>
  <instructions>
    Open `.planning/phases/39-independent-quality-re-judge/39-final-manual-review-sheet.md`.
    For each pending example, read the full message and current label/risk/spans/XAI plus Codex evidence. Mark exactly one PASS or FAIL; add a short note for every FAIL. Do not edit headers, hashes, record/evidence fields, or the old `39-manual-review-sheet.md`.
    Save the same file and reply `final Phase 39 review complete`.
  </instructions>
  <verification>The agent runs the read-only strict validator; it must prove exactly 100 unique final identities, exactly one verdict per row, unchanged embedded evidence, and current-manifest binding.</verification>
  <verify>
    <automated>python -m src.data_pipeline.manual_review_sheet validate-final --sheet .planning/phases/39-independent-quality-re-judge/39-final-manual-review-sheet.md --merged data/processed/judge-merged.jsonl --provenance data/processed/phase39-final-judge-provenance.jsonl --manifest data/manifests/manifest.json --require-complete --check-only</automated>
  </verify>
  <resume-signal>Reply `final Phase 39 review complete` after saving all remaining verdicts.</resume-signal>
  <done>The read-only validator accepts exactly 100 genuine human decisions; Plan 39-07 may now summarize them and change report claims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Historical sheet -> final sheet | Old decisions may refer to stale records/evidence. |
| Human-edited final sheet -> completion gate | Missing, dual, or altered entries must not pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-39-06-01 | Tampering | prior-verdict carry | high | mitigate | Unique record and complete judge-evidence digests plus one old checkbox. |
| T-39-06-02 | Spoofing | human completion | high | mitigate | Blocking human-action checkpoint, no auto-advance, and strict exactly-100 read-only validation. |
| T-39-06-03 | Tampering | historical files | medium | mitigate | New output path only plus pre/post hashes for all three historical user artifacts. |
</threat_model>

<verification>
- Focused and full manual-review tests pass.
- Pending validation passes before checkpoint without claiming completion.
- Complete read-only validation passes only after explicit human action.
</verification>

<success_criteria>
The final sheet contains 100 valid human verdicts on the promoted corpus and can be summarized in the next plan without any stale or automated human claim.
</success_criteria>

<output>
Create `.planning/phases/39-independent-quality-re-judge/39-06-SUMMARY.md` only after the blocking checkpoint succeeds.
</output>
