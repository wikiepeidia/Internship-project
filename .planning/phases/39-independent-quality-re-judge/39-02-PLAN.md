---
phase: 39-independent-quality-re-judge
plan: 02
type: execute
wave: 2
depends_on: [39-01]
files_modified:
  - src/data_pipeline/apply_mislabel_triage.py
  - tests/data_pipeline/test_apply_mislabel_triage.py
  - data/processed/phase39-mislabel-candidate/splits/train.jsonl
  - data/processed/phase39-mislabel-candidate/splits/val.jsonl
  - data/processed/phase39-mislabel-candidate/splits/test.jsonl
  - data/processed/phase39-mislabel-candidate/manifest.json
  - data/processed/phase39-mislabel-candidate/phase39-mislabel-decision-manifest.jsonl
  - data/processed/phase39-mislabel-candidate/phase39-mislabel-quarantine.jsonl
  - data/processed/phase39-mislabel-candidate/phase39-seed-cap-drops.jsonl
  - data/processed/phase39-mislabel-candidate/run.json
  - .planning/phases/39-independent-quality-re-judge/39-MISLABEL-AUDIT.md
  - ".planning/phases/39-independent-quality-re-judge/MISLABEL triage.md"
autonomous: true
requirements: [JUDGE-01, JUDGE-03]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "All 324 human triage decisions are parsed exactly once, with only candidate 103 `Drop` normalized to drop and candidate 320 `Beigin` normalized to benign; raw spellings and reasons remain auditable."
    - "Every decision binds to one current record by seed_id plus a conservative full-text SHA-256 identity, never by a stale split or row_index."
    - "The staged candidate contains 91 human drops, 57 admitted label-only relabels, and 176 lineage quarantines; all six non-label DatasetRecord fields are unchanged for every admitted relabel."
    - "The 176 Zalo decisions from seed_157ce0adb043 remain documented as semantically approved but excluded for shared-lineage dominance, while candidate 47 from seed_c6c8772ac332 remains admitted."
    - "After global 8% capping and deterministic whole-seed reassignment with salt phase39-mislabel-triage-v1, a reload-validated 2,103-row candidate exists with split sizes 1,665/218/220 and all four labels in every split."
    - "The live data/splits files, live manifest, historical judge files, and all three user-edited review artifacts remain byte-identical throughout this plan."
    - "The authoritative compact `MISLABEL triage.md` is force-tracked at SHA-256 c408dcf4161d84056b7c22e1fb3e975352a52cd5fbf2b111f11b5dfece0c089c, while the two modified historical sheets are explicitly excluded from staging."
    - "An interrupted or concurrent run cannot partially promote anything: this plan is stage-only, hash-locks every input, and is deterministic and restart-safe."
  artifacts:
    - path: "src/data_pipeline/apply_mislabel_triage.py"
      provides: "Fail-closed decision parser, identity binder, quarantine/cap/split projection, semantic-repair contract, staging, and rollback-capable promotion API"
    - path: "tests/data_pipeline/test_apply_mislabel_triage.py"
      provides: "Parser, preservation, lineage, deterministic split, integrity, concurrency, and transaction regression coverage"
    - path: "data/processed/phase39-mislabel-candidate/manifest.json"
      provides: "Reload-validated provisional 2,103-row corpus manifest bound to immutable source hashes"
    - path: "data/processed/phase39-mislabel-candidate/phase39-mislabel-decision-manifest.jsonl"
      provides: "One normalized, disposition-bearing audit record for each of 324 human decisions"
    - path: "data/processed/phase39-mislabel-candidate/phase39-mislabel-quarantine.jsonl"
      provides: "Complete original records and reasons for 176 shared-lineage exclusions"
    - path: ".planning/phases/39-independent-quality-re-judge/39-MISLABEL-AUDIT.md"
      provides: "Defense-safe, hash-cited account of the targeted human audit and lineage quarantine"
    - path: ".planning/phases/39-independent-quality-re-judge/MISLABEL triage.md"
      provides: "Byte-identical, force-tracked authoritative 324-decision human evidence input"
  key_links:
    - from: ".planning/phases/39-independent-quality-re-judge/MISLABEL triage.md"
      to: "src/data_pipeline/apply_mislabel_triage.py"
      via: "strict numbered-decision parser preserving raw values"
      pattern: "TriageDecision|normalization_reason"
    - from: "data/processed/judge-merged.jsonl"
      to: "data/processed/phase39-mislabel-candidate/phase39-mislabel-decision-manifest.jsonl"
      via: "historical candidate reconstruction plus seed_id/full-text digest unique join"
      pattern: "record_identity|canonicalize_identity_text"
    - from: "src/data_pipeline/repair_corpus_split_governance.py"
      to: "data/processed/phase39-mislabel-candidate/splits"
      via: "enforce_seed_cap followed by assign_stratified_group_split over whole seed groups"
      pattern: "phase39-mislabel-triage-v1"
    - from: "data/processed/phase39-mislabel-candidate/splits"
      to: "data/processed/phase39-mislabel-candidate/manifest.json"
      via: "reloaded byte counts, record counts, distributions, and SHA-256 entries"
      pattern: "task_scam_mislabel_triage"
---

<objective>
Turn the completed 324-item human mislabel triage into a strict, machine-readable,
lineage-safe candidate migration without changing the live corpus yet.

Purpose: preserve the user's full audit as evidence, admit only independently
usable rows, and prove the destructive corpus projection before any canonical
byte is eligible for promotion. The result is a deterministic staged snapshot
that Plan 39-03 can prepare identity-bound judge work and Plan 39-04 can
semantically re-judge it before Plan 39-05 finalizes the release.

Output: a tested `apply_mislabel_triage.py`, a reload-validated 2,103-row
candidate bundle, exact decision/quarantine/cap provenance, and an honest
`39-MISLABEL-AUDIT.md`. The existing human sheets and raw compact audit are
read-only inputs and remain byte-identical.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-independent-quality-re-judge/39-CONTEXT.md
@.planning/phases/39-independent-quality-re-judge/39-RESEARCH.md
@.planning/phases/39-independent-quality-re-judge/39-PATTERNS.md
@.planning/phases/39-independent-quality-re-judge/39-01-SUMMARY.md
@.planning/phases/39-independent-quality-re-judge/MISLABEL triage.md
@data/manifests/manifest.json
@src/data_pipeline/schemas.py
@src/data_pipeline/generate_mislabel_triage_sheet.py
@src/data_pipeline/apply_task_scam_risk_tier_repair.py
@src/data_pipeline/repair_corpus_split_governance.py
@src/data_pipeline/reconstruct_zalo_direct_catalog.py

<interfaces>
Use the existing `DatasetRecord` seven-field contract from `schemas.py`.
Reuse `select_mislabel_candidates()` for ordered historical candidate recovery,
`enforce_seed_cap(records, cap_pct=0.08)`, and
`assign_stratified_group_split(records, ratios=(0.8, 0.1, 0.1), salt=...)`.
Follow `reconstruct_zalo_direct_catalog.py`'s `_replace_payload_bundle()` and
verified rollback behavior; a sequence of independent atomic replaces is not
a multi-file transaction.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one decision from human text through strict identity binding to an audited label-only disposition</name>
  <files>src/data_pipeline/apply_mislabel_triage.py, tests/data_pipeline/test_apply_mislabel_triage.py</files>
  <read_first>
    .planning/phases/39-independent-quality-re-judge/MISLABEL triage.md (read-only decision syntax, including candidate 103's `Drop` and candidate 320's `Relabel to: Beigin`);
    data/processed/judge-merged.jsonl (historical raw-record authority for reconstructing the 329 flagged / 324 still-live candidates; do not use rendered Markdown as record identity);
    src/data_pipeline/generate_mislabel_triage_sheet.py (`select_mislabel_candidates` and `partition_by_live_presence` ordering semantics);
    src/data_pipeline/apply_task_scam_risk_tier_repair.py (`ConfigDict(extra="forbid")`, line-numbered parse failures, and exact coverage errors);
    src/data_pipeline/schemas.py (`DatasetRecord`; risk tier is contextual and is not derived mechanically from label);
    tests/data_pipeline/test_apply_task_scam_risk_tier_repair.py and tests/data_pipeline/test_generate_mislabel_triage_sheet.py (fixture and error-assertion conventions)
  </read_first>
  <behavior>
    - Parse candidate numbers 1 through 324 exactly once and require a non-empty Note/Notes entry for each; reject duplicates, omissions, extras, arbitrary decision spellings, and labels outside the four DatasetRecord labels before writing output.
    - Preserve `raw_decision`, `normalized_action`, `new_label`, `notes`, and `normalization_reason`; allow exactly the two documented normalization events for candidates 103 and 320 and no others.
    - Canonicalize identity text only with Unicode NFC, CRLF/CR to LF, and removal of one terminal newline; combine seed_id with SHA-256 of the complete canonical text and reject zero or multiple current matches.
    - Demonstrate that changing historical split/row_index metadata does not change the match, while changing any identity-bearing text byte does.
    - Produce exactly one disposition per candidate: 91 `drop`, 57 `admitted_relabel`, or 176 `lineage_quarantine`; candidate 47 is the only admitted Zalo relabel from this targeted set.
    - For every admitted relabel, mutate only `label` and assert equality of text, risk_tier, suspicious_spans, xai_explanation, source, and seed_id before and after.
    - Define a closed `CodexSemanticRepairDecision` input contract for Plan 39-03 that binds the expected seven-field record digest and may explicitly replace only risk_tier, suspicious_spans, and xai_explanation; label, text, source, and seed_id are never accepted as repair fields.
  </behavior>
  <action>
    Implement a production `src/data_pipeline/apply_mislabel_triage.py` tracer path covering parse -> historical candidate reconstruction -> unique live identity -> disposition -> transformed record -> audit row. Define `TriageDecision`, `DecisionDisposition`, and `CodexSemanticRepairDecision` as closed Pydantic models. Preserve raw values verbatim in provenance and produce line-numbered, aggregated errors.

    The compact decision file is untrusted human-edited input, while `data/processed/judge-merged.jsonl` supplies the original candidate records. Use `(seed_id, SHA256(canonicalized complete text))` as durable identity and keep old split/row_index only as historical metadata. Never use case-folding, whitespace collapsing, punctuation cleanup, Markdown-rendered text, or a stale row position to make a join succeed.

    Encode the locked admission policy directly and visibly: all 176 `zalo_social_engineering` relabel decisions from `seed_157ce0adb043` receive `lineage_quarantine`; the independent candidate 47 from `seed_c6c8772ac332` is admitted; the remaining normalized decisions follow the human drop/relabel choice. A quarantined row retains its approved semantic label in the audit but never enters the admitted pool. For admitted relabels, change only `label`; do not infer or rewrite risk tier, spans, explanation, text, source, or seed lineage.

    Add a repair application seam for the next plan, but do not invent repair decisions here. It must accept only a separately authored, hash-bound Codex semantic-repair JSONL, validate literal spans and the resulting DatasetRecord, retain before/after digests, and fail if a non-permitted field would change.
  </action>
  <verify>
    <automated>python -m pytest tests/data_pipeline/test_apply_mislabel_triage.py -k "parser or identity or disposition or label_only or semantic_repair_contract" -q</automated>
  </verify>
  <acceptance_criteria>
    The focused tests start from human-text decisions and end at validated audit/transformed-record output;
    all malformed coverage and ambiguous-identity cases fail before a candidate file is written;
    the only normalization events are candidate 103 `Drop` to drop and candidate 320 `Beigin` to benign;
    the normalized totals are 91 drop, 48 bank relabel, 177 Zalo relabel, and 8 benign relabel before lineage admission;
    disposition totals are 91 drop, 57 admitted relabel, and 176 lineage quarantine;
    a test parameterizes all six preserved fields and proves label-only mutation;
    no network, provider SDK, requests call, or external API path exists in the module.
  </acceptance_criteria>
  <done>The complete human decision surface has a closed, tested, uniquely joined, label-only transformation contract and every candidate has one honest disposition.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stage and reload the full capped, whole-seed candidate with immutable provenance</name>
  <files>src/data_pipeline/apply_mislabel_triage.py, tests/data_pipeline/test_apply_mislabel_triage.py, data/processed/phase39-mislabel-candidate/splits/train.jsonl, data/processed/phase39-mislabel-candidate/splits/val.jsonl, data/processed/phase39-mislabel-candidate/splits/test.jsonl, data/processed/phase39-mislabel-candidate/manifest.json, data/processed/phase39-mislabel-candidate/phase39-mislabel-decision-manifest.jsonl, data/processed/phase39-mislabel-candidate/phase39-mislabel-quarantine.jsonl, data/processed/phase39-mislabel-candidate/phase39-seed-cap-drops.jsonl, data/processed/phase39-mislabel-candidate/run.json, .planning/phases/39-independent-quality-re-judge/39-MISLABEL-AUDIT.md, .planning/phases/39-independent-quality-re-judge/MISLABEL triage.md</files>
  <read_first>
    src/data_pipeline/repair_corpus_split_governance.py (`enforce_seed_cap`, `assign_stratified_group_split`, label-support repair, and 0.95 lexical duplicate policy);
    src/data_pipeline/reconstruct_zalo_direct_catalog.py (`stage_candidate_bundle`, `_replace_payload_bundle`, post-write verification, rollback failure aggregation, and history-preserving `build_updated_manifest` pattern);
    data/manifests/manifest.json (preserve every existing chronology block and append `task_scam_mislabel_triage`);
    tests/data_pipeline/test_reconstruct_zalo_direct_catalog.py (stage/reload and failure-injection patterns);
    tests/data_pipeline/test_repair_corpus_split_governance.py (deterministic cap/split assertions);
    .planning/phases/39-independent-quality-re-judge/39-RESEARCH.md (locked 2,103-row projection and honest audit wording)
  </read_first>
  <behavior>
    - Hash-lock the current inputs before projection: train `6454a271c6133f1ebbd41010390b8ea6ceae0a8ab0a75b2ab545099db3319ee8`, val `7adfe8cd9a124dbb3d87046bb32f9fbd127d3e344c45be77c8bb9efa700aaa75`, test `019aec39979429ca8005dd299d2ddaf7d3ecfdade259eecc4d3129adaed25938`, manifest `4794cedae52cc5531083a569c3e63c419335a0544f365f4a4d6245048efc2b90`, historical merged judge `e8b4d947271717e56556a74136c57d83dd58589c78699d557999140a9fb55750`, and compact decisions `c408dcf4161d84056b7c22e1fb3e975352a52cd5fbf2b111f11b5dfece0c089c`.
    - Build 2,136 admitted rows after 91 human drops and 176 lineage quarantines, then call the existing global iterative 8% cap, recording all 33 cap drops rather than silently omitting them.
    - Assign complete seed groups using ratios `(0.8, 0.1, 0.1)` and salt `phase39-mislabel-triage-v1`; the staged distribution is train 1,665 (bank 597/task 306/benign 517/Zalo 245), val 218 (76/49/72/21), test 220 (70/49/66/35).
    - Validate schema, literal spans, normalized exact duplicates, lexical duplicates at threshold 0.95, seed disjointness, four-label support, global seed cap, split-ratio tolerance, record counts, byte counts, and hashes before and after disk reload.
    - Preserve existing manifest chronology and append input hashes, both normalization events, all 324 decision identities/dispositions, quarantine seed/reason, 33 cap drops, split salt/distribution, source hashes, output hashes, and honest Git dirty state.
    - Stage into an empty deterministic candidate directory; a repeated run with identical locked inputs and identical staged hashes exits successfully without rewriting, while a non-identical/non-empty candidate or concurrent lock fails closed.
    - Snapshot the three user artifacts' hashes at process start and assert them unchanged at exit; never write `39-manual-review-sheet.md`, `39-mislabel-triage-sheet.md`, or `MISLABEL triage.md`.
  </behavior>
  <action>
    Expand `apply_mislabel_triage.py` into a stage-first migration CLI. Pool the three live splits in memory, apply Task 1 dispositions, run `enforce_seed_cap(..., cap_pct=0.08)`, assign whole seeds using the locked split salt, and build all candidate bytes before the first disk write. Record cap removals in a dedicated JSONL rather than merging them into the 91 human drops.

    Stage splits, a deep-copied history-preserving manifest, the 324-row decision manifest, 176-row quarantine, 33-row cap-drop artifact, and a run descriptor into `data/processed/phase39-mislabel-candidate/`. Reload every staged byte and rerun every integrity gate. Add an exclusive run lock and run-id derived from immutable input hashes; deterministic resume may accept an already complete byte-identical candidate, but any mismatch or second writer must stop. Expose the established rollback-capable bundle promotion function for Plan 39-05, but invoke only `--stage-only` here. The live split/manifest/judge destinations are forbidden outputs in this plan.

    Generate `39-MISLABEL-AUDIT.md` mechanically from the staged audit/manifest. State that a Vietnamese-fluent project reviewer targeted all 324 live `task_scam` rows previously flagged below 3/5 for label correctness; this was not full-corpus human annotation. State the 91 removals and 233 semantic relabel decisions, then distinguish the 176 human-approved Zalo semantics from their training exclusion due to shared seed lineage. Label the 2,103-row result as a staged projection awaiting final semantic judgment, not a frozen release.

    Preserve and force-track the authoritative compact decision file at its existing path with exact SHA-256 `c408dcf4161d84056b7c22e1fb3e975352a52cd5fbf2b111f11b5dfece0c089c`; do not rewrite or normalize its bytes. The task commit may include this raw evidence input, the new implementation/tests/audit, and generated artifacts declared by the plan, but must explicitly verify that `39-manual-review-sheet.md` and `39-mislabel-triage-sheet.md` are absent from the staged-name set. If either historical sheet is staged, stop before commit and unstage only those paths without changing their worktree bytes.

    Add full-scale and failure-injection tests: projected constants under the locked hashes, reload integrity, source-hash drift, malformed decisions, cap determinism, split determinism, output chronology, non-empty candidate conflict, simultaneous lock acquisition, idempotent restart, write failure, post-stage failure, post-promotion failure, and rollback verification across every destination. Tests must use temporary copies for all destructive paths.
  </action>
  <verify>
    <automated>python -m pytest tests/data_pipeline/test_apply_mislabel_triage.py -q</automated>
  </verify>
  <acceptance_criteria>
    The full test module passes, including injected write/verification/rollback and concurrent-lock cases;
    the live full-scale stage command produces exactly 2,103 reload-validated candidate rows and no live canonical hash changes;
    the final candidate contains 743 bank, 655 benign, 404 task, and 301 Zalo rows, with Zalo spread across 61 seeds and maximum Zalo seed contribution 5/301;
    no seed crosses splits and no global seed exceeds 8% of 2,103 rows;
    the decision manifest has 324 unique candidate numbers, quarantine has 176 rows, and cap-drop artifact has 33 rows;
    the audit document uses targeted-review wording and explicitly records the lineage quarantine and staged status;
    `MISLABEL triage.md` is tracked at the exact locked hash, and staged-file inspection excludes both modified historical sheets;
    pre/post SHA-256 values for all user-edited artifacts and every live canonical input are identical.
  </acceptance_criteria>
  <done>A deterministic, restart-safe, fully audited 2,103-row candidate is staged and reload-validated, while the current live corpus and every user review artifact remain byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Human-edited Markdown -> decision parser | Free-form local input could contain typos, duplicate decisions, omissions, or injected structure. |
| Historical judge records -> current corpus identity | Positional metadata is stale; only conservative content identity may authorize a mutation. |
| In-memory projection -> staged filesystem bundle | A crash, concurrent process, or partial write could create apparently valid but mismatched evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-39-02-01 | Tampering | `parse_triage_decisions` | high | mitigate | Closed Pydantic schema, exact 1..324 coverage, two explicit normalization exceptions, raw-value retention, and source SHA-256 lock. |
| T-39-02-02 | Tampering | candidate identity join | high | mitigate | Bind seed_id plus conservative full-text SHA-256, reject zero/multiple matches, and ignore stale split/row_index as authority. |
| T-39-02-03 | Repudiation | lineage disposition | medium | mitigate | Persist all 324 normalized decisions and complete original quarantined records with the shared-seed rationale and human-approved semantic label. |
| T-39-02-04 | Tampering / Denial of Service | stage and promotion primitives | high | mitigate | Exclusive lock, immutable input hashes, empty-or-byte-identical candidate rule, reload validation, all-file rollback helper, and injected-failure tests. |
| T-39-02-05 | Information Disclosure | local review artifacts | low | accept | Inputs contain dataset messages but no secrets; keep processing local and never call network/provider APIs. |
</threat_model>

<verification>
- `python -m pytest tests/data_pipeline/test_apply_mislabel_triage.py -q` passes.
- `python -m pytest tests/data_pipeline/ -q` passes with no regressions.
- A read-only validator over `data/processed/phase39-mislabel-candidate/` proves all locked counts, distributions, hashes, span/duplicate/cap/split invariants, and audit coverage.
- A before/after SHA-256 report proves `data/splits/*`, `data/manifests/manifest.json`, historical judge artifacts, and the three user review files were not modified.
</verification>

<success_criteria>
The completed human triage is captured without inventing decisions, the dominant shared lineage is quarantined without misdescribing its semantics, and a candidate that satisfies all structural/integrity gates is ready for a fresh semantic delta judge. Nothing live has been promoted.
</success_criteria>

<output>
Create `.planning/phases/39-independent-quality-re-judge/39-02-SUMMARY.md` when done.
</output>
