---
phase: 44-code-cleanup-defense-prep
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/44-code-cleanup-defense-prep/44-START-AUTHORIZATION.md
  - .planning/phases/44-code-cleanup-defense-prep/phase44_guard.py
  - .planning/phases/44-code-cleanup-defense-prep/test_phase44_guard.py
  - .planning/phases/44-code-cleanup-defense-prep/44-WALKTHROUGH-SCOPE.json
  - .planning/phases/44-code-cleanup-defense-prep/44-WALKTHROUGH-PROTOCOL.md
  - .planning/phases/44-code-cleanup-defense-prep/44-UPSTREAM-GATE.json
  - .planning/phases/44-code-cleanup-defense-prep/44-WALKTHROUGH-INVENTORY.json
autonomous: false
requirements: [CODE-01, CODE-03, CODE-04]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "No Phase 44 execution artifact or walkthrough edit is created until the exact final Phase 42 report receipt and Phase 43 deck receipt independently verify from ancestor commits."
    - "The student explicitly authorizes Phase 44 to start now as final defense preparation after seeing the exact Phase 42/43 closure proof."
    - "A fixed 16-file defense walkthrough scope binds each numbered copy to one final canonical code file, including the local demo server and browser client, without reading any dataset."
    - "The guard can inventory, transcribe exact student text, verify, and seal, but has no path that invents or paraphrases a replacement comment."
  artifacts:
    - path: ".planning/phases/44-code-cleanup-defense-prep/44-START-AUTHORIZATION.md"
      provides: "Verbatim blocking-human authorization that the last phase is starting in the defense-prep window"
    - path: ".planning/phases/44-code-cleanup-defense-prep/phase44_guard.py"
      provides: "Fail-closed upstream, inventory, student-response, body-identity, and final-seal verifier"
    - path: ".planning/phases/44-code-cleanup-defense-prep/44-UPSTREAM-GATE.json"
      provides: "Hash- and commit-bound proof that Phases 42 and 43 are final before Phase 44 starts"
    - path: ".planning/phases/44-code-cleanup-defense-prep/44-WALKTHROUGH-INVENTORY.json"
      provides: "Exact pending file list, canonical hashes, legacy-overlay hashes, and required student fields"
  key_links:
    - from: ".planning/phases/44-code-cleanup-defense-prep/44-UPSTREAM-GATE.json"
      to: ".planning/phases/42-evidence-bound-report-rewrite/42-REPORT-REWRITE-RECEIPT.json"
      via: "Phase 42 verify-final plus receipt hash and ancestor commit"
      pattern: "phase42.*receipt_sha256.*commit"
    - from: ".planning/phases/44-code-cleanup-defense-prep/44-WALKTHROUGH-INVENTORY.json"
      to: "walkthrough/*.py"
      via: "exact canonical-body SHA-256 and explicit pending student response"
      pattern: "canonical_sha256|legacy_overlay_sha256|student_status"
---

<objective>
Build the non-generative guard and freeze the exact final code inventory only after Phases 42 and 43 are genuinely complete.

Purpose: Make early execution, stale walkthrough copies, fabricated review status, and dataset access fail closed before the student walkthrough begins.
Output: Tested guard, fixed scope/protocol, upstream gate receipt, and pending inventory.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-evidence-bound-report-rewrite/42-14-PLAN.md
@.planning/phases/43-evidence-bound-slide-rebuild/43-02-PLAN.md
@walkthrough/README.md
</context>

<execution_rules>
Commit each mutating task after its gate passes and before starting the next task. After every Phase 44 task commit and every administrative SUMMARY commit, run the guard's read-only `verify-handoff` against the exact current tip and require a clean worktree; stop before further work on any failure. Do not squash, amend, or mix unrelated paths into a Phase 44 commit. The opening authorization checkpoint creates no file and no commit; Task 2 records it verbatim in the first Phase 44 commit so that commit can immediately be checked by the newly created guard.
</execution_rules>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Authorize final defense-prep work after exact upstream closure</name>
  <precondition>Phase 42 `42-14-SUMMARY.md` and `42-REPORT-REWRITE-RECEIPT.json` exist, `phase42_guard.py verify-final` exits zero, Phase 43 `43-02-SUMMARY.md` and `43-EVIDENCE-RECEIPTS/compile.json` exist, `compile_and_seal_deck.py verify` exits zero, the four artifact commits are ancestors of current HEAD, and tracked plus untracked worktree state is empty.</precondition>
  <action>Before creating any execution artifact or editing walkthrough code, run the two official read-only verifiers and show the student the four exact artifact paths, SHA-256 values, containing commit IDs, ancestry verdicts, current HEAD, current date, and stated retake date. Ask whether this is genuinely the final defense-preparation window and whether all training, evaluation, report, and slide work is closed. Do not infer authorization from prior messages and do not auto-approve this checkpoint.</action>
  <instructions>The student must answer in their own words and include `I authorize Phase 44 to start now as the final code-walkthrough and defense-preparation phase. No training, evaluation, report, or slide change remains planned before the retake.` If that statement is not true, stop Phase 44 without creating an artifact.</instructions>
  <verification>The executor retains the exact response plus displayed artifact hashes/commits in session for verbatim recording by Task 2; no repository file changes at this checkpoint.</verification>
  <resume-signal>Reply with the exact authorization sentence plus the retake date, or state that Phase 44 must remain blocked.</resume-signal>
  <done>Explicit human authorization exists in-session and no Phase 44 execution file or commit has been created yet.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Prove one synthetic file through the student-only walkthrough state machine</name>
  <precondition>Task 1 received the exact authorization response with no qualification, and the displayed Phase 42/43 hashes, commits, verifier results, current HEAD, and date are still unchanged.</precondition>
  <files>.planning/phases/44-code-cleanup-defense-prep/44-START-AUTHORIZATION.md, .planning/phases/44-code-cleanup-defense-prep/phase44_guard.py, .planning/phases/44-code-cleanup-defense-prep/test_phase44_guard.py, .planning/phases/44-code-cleanup-defense-prep/44-WALKTHROUGH-SCOPE.json, .planning/phases/44-code-cleanup-defense-prep/44-WALKTHROUGH-PROTOCOL.md</files>
  <behavior>
    - A synthetic canonical file becomes a pending inventory row, accepts only an externally supplied student response, materializes that text byte-for-byte above an unchanged canonical body, and verifies the resulting hashes.
    - Missing per-file attestation, blank own-words fields, a changed canonical body, an unlisted path, a forged completed status, duplicate response, or post-response mutation fails closed.
    - The CLI never drafts, suggests, expands, translates, grammar-corrects, or paraphrases comment text and exposes no command that marks a file reviewed without a supplied response.
    - File discovery is limited to the 16 explicit code mappings; the complete `data/` tree, walkthrough data copies, documents, models, predictions, and generated artifacts are outside every open/stat/hash/glob path.
    - Every tracked human-authored code path under the audited architecture roots is classified as an included walkthrough anchor or an exact-path exclusion with category and rationale; an unclassified path fails inventory freeze.
    - `verify-handoff` accepts only a captured 40-hex expected tip, rejects symbolic refs and dirty tracked/untracked state, validates every baseline-to-tip commit against one exact task or administrative path set, and recomputes sealed hashes once a final seal exists.
  </behavior>
  <action>Record Task 1's response verbatim with the displayed upstream identities in `44-START-AUTHORIZATION.md`. Create `phase44_guard.py` with `synthetic-e2e`, `gate-upstream`, `freeze-inventory`, `next`, `record-verbatim`, `verify-group`, `verify-attestation`, `finalize`, `verify-final`, and read-only `verify-handoff` commands. Use atomic JSON writes, duplicate-key rejection, regular-file/reparse checks, SHA-256 over raw bytes, and append-only response identities. `record-verbatim` may add language-appropriate comment delimiters but must preserve submitted UTF-8 text exactly and rebuild the walkthrough body from exact canonical bytes; it must not contain fallback prose or a comment-generation branch. Encode the exact 16-row mapping in `44-WALKTHROUGH-SCOPE.json` exactly as the audit table below, including category and rationale for each row. Add a complete architecture coverage section that classifies every code path under the allowlisted `src/`, `scripts/phase41*`, and runtime demo-asset roots as one included anchor or one exact exclusion; reject missing, duplicate, glob-only, or rationale-free classifications. Document the authorization, checkpoint, task-commit, administrative-summary, and post-commit handoff protocol. The administrative policy is exact: a plan summary commit may touch only its matching `44-01-SUMMARY.md` through `44-05-SUMMARY.md`; a verifier commit may touch only `44-VERIFICATION.md`; a workflow-state closeout commit may touch exactly `.planning/ROADMAP.md`, `.planning/STATE.md`, and `.planning/REQUIREMENTS.md`. Every such commit is followed by another read-only handoff verification, and no administrative file is treated as student-authored or included in the sealed walkthrough tree. Tests use only temporary synthetic repositories and make any access outside the synthetic allowlist fail.</action>
  <verify><automated>pytest .planning/phases/44-code-cleanup-defense-prep/test_phase44_guard.py -q &amp;&amp; python .planning/phases/44-code-cleanup-defense-prep/phase44_guard.py synthetic-e2e</automated></verify>
  <done>The authorization is recorded verbatim, the synthetic path passes, every fabrication/tamper case fails, the first Phase 44 commit contains only Task 2's five files, and `verify-handoff` passes immediately after that commit using its captured full 40-hex tip and a clean worktree.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bind final upstream evidence and freeze the real pending inventory</name>
  <files>.planning/phases/44-code-cleanup-defense-prep/44-UPSTREAM-GATE.json, .planning/phases/44-code-cleanup-defense-prep/44-WALKTHROUGH-INVENTORY.json, .planning/phases/44-code-cleanup-defense-prep/test_phase44_guard.py</files>
  <behavior>
    - Upstream gate records exact Phase 42 `42-14-SUMMARY.md`, final receipt bytes, `verify-final` result, Phase 43 `43-02-SUMMARY.md`, compile receipt/result, all four commit IDs, ancestry, and the baseline HEAD.
    - Inventory contains exactly 16 unique mappings, every row is pending, every canonical file is regular and tracked, local demo server/client coverage is explicit, and no body or comment is changed.
    - A later upstream hash/commit change invalidates every downstream Phase 44 command until the gate and inventory are explicitly refrozen before any student response exists.
  </behavior>
  <action>Run both official verifiers again inside `gate-upstream`. Bind `42-14-SUMMARY.md`, `42-REPORT-REWRITE-RECEIPT.json`, the machine-readable/exit result of `phase42_guard.py verify-final`, `43-02-SUMMARY.md`, `43-EVIDENCE-RECEIPTS/compile.json`, the result of the Phase 43 compile verifier, each artifact's containing commit, exact ancestry to baseline HEAD, and the Task 1 authorization hash. Freeze the exact 16-row scope and the per-path architecture coverage/exclusion audit. Inspect only named code/walkthrough paths. Record canonical/body hashes and the noncanonical legacy-overlay digest without assigning an AI probability. Refuse an unclassified architecture path, missing UI server/client anchor, untracked canonical code, missing Phase 41 source, duplicate mapping, reparse point, or any path outside the scope. Leave every `student_status` pending and provide no prefill option.</action>
  <verify><automated>python .planning/phases/44-code-cleanup-defense-prep/phase44_guard.py gate-upstream --repo-root . --output .planning/phases/44-code-cleanup-defense-prep/44-UPSTREAM-GATE.json &amp;&amp; python .planning/phases/44-code-cleanup-defense-prep/phase44_guard.py freeze-inventory --repo-root . --scope .planning/phases/44-code-cleanup-defense-prep/44-WALKTHROUGH-SCOPE.json --upstream-gate .planning/phases/44-code-cleanup-defense-prep/44-UPSTREAM-GATE.json --output .planning/phases/44-code-cleanup-defense-prep/44-WALKTHROUGH-INVENTORY.json &amp;&amp; pytest .planning/phases/44-code-cleanup-defense-prep/test_phase44_guard.py -q</automated></verify>
  <done>Exact final-state authorities verify, the 16-file inventory and architecture audit are complete/pending/data-blind, Task 3 is committed separately, and the read-only handoff verifier passes against that exact clean tip.</done>
</task>

</tasks>

## Exact Walkthrough Mapping And Architecture Coverage

| Walkthrough path | Canonical repository path | Category | Inclusion rationale |
|---|---|---|---|
| `walkthrough/1_scrape_seeds.py` | `src/data_pipeline/scraper/ncsc_scraper.py` | data acquisition | Entry from public Vietnamese advisory pages into typed seed records. |
| `walkthrough/2_generate_synthetic_data.py` | `src/data_pipeline/generation/generator.py` | data generation | Seed-conditioned generation, checkpointing, and provider boundary. |
| `walkthrough/3_quality_judge.py` | `src/data_pipeline/judge_merge.py` | independent quality control | Joins external judgments to exact corpus rows and exposes quality findings. |
| `walkthrough/4_split_dataset.py` | `src/data_pipeline/repair_corpus_split_governance.py` | repair and split governance | Span repair, seed cap, whole-group splitting, and manifest lineage. |
| `walkthrough/4a_phase40_data_contract.py` | `src/model_adaptation/phase40_contract.py` | training-data boundary | Freezes train/validation authority and blocks unintended split access. |
| `walkthrough/5_train_qlora.py` | `src/model_adaptation/training.py` | Qwen adaptation | Actual QLoRA configuration, response masking, training, and adapter output. |
| `walkthrough/5a_train_phobert.py` | `src/model_adaptation/phobert_training.py` | PhoBERT adaptation | Separate encoder/classification-head training path and four-logit output. |
| `walkthrough/6_convert_to_gguf.py` | `src/model_adaptation/phase40_gguf.py` | local deployment artifact | Verified GGUF export, manifest, and load-smoke boundary. |
| `walkthrough/6a_evidence_and_sha256.py` | `src/model_adaptation/phase40_evidence.py` | evidence integrity | Raw-evidence finalization and SHA-256-linked manifest behavior. |
| `walkthrough/6b_one_shot_evaluation.py` | `src/model_adaptation/phase41_evaluation.py` | final evaluation | One post-freeze two-model evaluation state machine and terminal evidence. |
| `walkthrough/7_analyze_entrypoint.py` | `src/runtime/cli.py` | operator entry | Text analysis command, readiness gate, and result rendering entry point. |
| `walkthrough/8_runtime_service.py` | `src/runtime/service.py` | runtime orchestration | Text/privacy validation, backend dispatch, and fail-closed service boundary. |
| `walkthrough/9_prompt_and_grounding.py` | `src/runtime/analyzers/local_model.py` | explainable decisioning | Structured prompt, parsing, grounded evidence, and safe recommendation logic. |
| `walkthrough/10_gguf_model_backend.py` | `src/runtime/analyzers/gguf.py` | local model inference | Registry resolution, model loading, and offline GGUF invocation. |
| `walkthrough/11_local_demo_server.py` | `src/runtime/demo.py` | local demo UI server | WSGI routes, local API bridge, static allowlist, and browser launch path. |
| `walkthrough/12_local_demo_client.js` | `src/runtime/demo_assets/demo.js` | local demo UI client | User submission, abort/re-entry behavior, result rendering, and accessibility updates. |

The scope contract also contains an exact-path architecture audit. Package markers, binary fonts, generated assets, tests, historical scripts, documentation, and non-entry helper files may be excluded only as named paths with one of those categories and a concrete rationale linking them to an included anchor; no wildcard-only exclusion is accepted. Any tracked human-authored code path under an audited root that is neither one of the 16 anchors nor an exact justified exclusion blocks inventory freeze. `src/runtime/demo.py` and `src/runtime/demo_assets/demo.js` are mandatory anchors, so the local demo UI subsystem cannot be silently excluded.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Phase 42/43 receipts -> Phase 44 | Final report/deck state authorizes the last phase. |
| Canonical source -> walkthrough copy | Executable code bytes must not be changed by comment cleanup. |
| Student response -> tracked comment | Authorship must remain human, while transcription remains mechanical. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-44-01 | Spoofing | student status | high | mitigate | Pending-by-default state plus blocking-human responses in later plans; no auto-complete API. |
| T-44-02 | Tampering | upstream final state | high | mitigate | Re-run official verifiers and bind receipt bytes plus ancestor commits. |
| T-44-03 | Information Disclosure | inventory discovery | high | mitigate | Exact 16-anchor allowlist plus exact-path source exclusions; no data-tree discovery. |
| T-44-04 | Tampering | walkthrough body | high | mitigate | Raw canonical-body hash and byte comparison after transcription. |
| T-44-SC | Tampering | package installation | low | accept | No package installation is authorized. |
</threat_model>

<verification>The guard's synthetic adversarial suite passes; exact Phase 42 Plan 14 and Phase 43 Plan 02 closure verifiers pass; the real inventory has 16 pending rows, complete architecture classification, mandatory UI coverage, and zero source mutation.</verification>

<success_criteria>
- Phase 44 cannot start before exact Phase 42/43 closure.
- The walkthrough scope is complete, explicit, and code-only.
- No generated or prefilled student comment exists.
- The original walkthrough and canonical code are unchanged after inventory freeze.
</success_criteria>

<output>Create `.planning/phases/44-code-cleanup-defense-prep/44-01-SUMMARY.md` when done.</output>
