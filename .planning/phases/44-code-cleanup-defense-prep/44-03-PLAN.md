---
phase: 44-code-cleanup-defense-prep
plan: "03"
type: execute
wave: 3
depends_on: ["44-02"]
files_modified:
  - walkthrough/6a_evidence_and_sha256.py
  - walkthrough/6b_one_shot_evaluation.py
  - walkthrough/7_analyze_entrypoint.py
  - walkthrough/8_runtime_service.py
  - walkthrough/9_prompt_and_grounding.py
  - walkthrough/10_gguf_model_backend.py
  - walkthrough/11_local_demo_server.py
  - walkthrough/12_local_demo_client.js
  - .planning/phases/44-code-cleanup-defense-prep/44-STUDENT-RESPONSES-EVAL-RUNTIME.jsonl
autonomous: false
requirements: [CODE-01, CODE-02, CODE-03]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The student can explain why SHA-256 identifies exact bytes and why a manifest binds a set of evidence, without claiming either proves correctness or authorship."
    - "The student walks the one-shot evaluation and full local inference path file by file in their own words."
    - "The student also walks the local WSGI demo server and browser client, including submit, abort, render, and accessibility behavior."
    - "All eight numbered files retain exact canonical bodies and only verbatim student headers."
  artifacts:
    - path: "walkthrough/6a_evidence_and_sha256.py"
      provides: "Dedicated student-authored SHA-256 and manifest-integrity walkthrough"
    - path: ".planning/phases/44-code-cleanup-defense-prep/44-STUDENT-RESPONSES-EVAL-RUNTIME.jsonl"
      provides: "Eight append-only human response and attestation records"
  key_links:
    - from: "walkthrough/6a_evidence_and_sha256.py"
      to: "src/model_adaptation/phase40_evidence.py"
      via: "student header above exact canonical body"
      pattern: "Canonical source|SHA-256"
---

<objective>Complete the student-authored integrity, evaluation, and local-inference walkthrough.</objective>

<execution_context>@C:/Users/wikiepeidia/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.codex/gsd-core/templates/summary.md</execution_context>

<context>
@.planning/phases/44-code-cleanup-defense-prep/44-02-SUMMARY.md
@.planning/phases/44-code-cleanup-defense-prep/44-WALKTHROUGH-PROTOCOL.md
@.planning/phases/44-code-cleanup-defense-prep/44-WALKTHROUGH-INVENTORY.json
</context>

<execution_rules>Use one task-local commit per checkpoint group. Re-run all earlier group verification before accepting a new response. After each task commit and the later `44-03-SUMMARY.md`-only administrative commit, rerun read-only `verify-handoff` against the exact current tip and a clean worktree. No agent-written comment, suggested sentence, or silent attestation is permitted.</execution_rules>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Student-authored evidence integrity and one-shot evaluation walkthrough</name>
  <precondition>All Plan 44-02 responses and bodies reverify, the upstream gate remains unchanged, and the integrity/evaluation rows are pending.</precondition>
  <files>walkthrough/6a_evidence_and_sha256.py, walkthrough/6b_one_shot_evaluation.py, .planning/phases/44-code-cleanup-defense-prep/44-STUDENT-RESPONSES-EVAL-RUNTIME.jsonl</files>
  <action>Run the per-file student loop first for Phase 40 evidence finalization, then for Phase 41's one-shot two-model evaluator. For the integrity file, require the student to answer four defense questions in their own words: what SHA-256 compares; what the manifest adds; what a mismatch means; and what neither mechanism proves. For the evaluator, require them to explain frozen models, one post-freeze pass, identical conditions, terminal poor results, and prior human content exposure. The agent may point to named functions but must not provide answer text or repair the student's wording. Transcribe exactly and verify canonical bodies.</action>
  <instructions>Submit the six standard fields and authorship sentence for each file. The SHA-256 response must also contain four separately labeled answers written by the student. The executor repeats the checkpoint until both files pass; semantic uncertainty triggers another question, not an auto-completed explanation.</instructions>
  <verification>The executor records exact submitted bytes, verifies both canonical bodies, and runs the evidence-evaluation verifier after both responses are accepted.</verification>
  <resume-signal>Reply with the required fields for the single displayed file, plus the four integrity answers when prompted; the checkpoint repeats until both files verify.</resume-signal>
  <verify><automated>python .planning/phases/44-code-cleanup-defense-prep/phase44_guard.py verify-group --repo-root . --inventory .planning/phases/44-code-cleanup-defense-prep/44-WALKTHROUGH-INVENTORY.json --responses .planning/phases/44-code-cleanup-defense-prep/44-STUDENT-RESPONSES-EVAL-RUNTIME.jsonl --group evidence-evaluation --require-integrity-explanation</automated></verify>
  <done>Both integrity/evaluation files and the student's four-part SHA-256 explanation verify and are committed alone.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Student-authored runtime walkthrough</name>
  <precondition>The evidence/evaluation task commit and every earlier response reverify, and all four runtime rows remain pending.</precondition>
  <files>walkthrough/7_analyze_entrypoint.py, walkthrough/8_runtime_service.py, walkthrough/9_prompt_and_grounding.py, walkthrough/10_gguf_model_backend.py, .planning/phases/44-code-cleanup-defense-prep/44-STUDENT-RESPONSES-EVAL-RUNTIME.jsonl</files>
  <action>Guide the student through CLI entry, service/privacy boundary, prompt parsing and evidence grounding, then GGUF loading/inference. Ask them to trace one text message across the four named files and identify where validation, model invocation, structured parsing, grounding, and safe rendering happen. Do not give a prepared trace. Repeat the checkpoint one file at a time, transcribe exact student text, rebuild exact canonical bodies, and run the group verifier before the task commit.</action>
  <instructions>The student supplies the standard fields, per-file authorship sentence, and a final four-path trace in their own words. The agent can say which function to inspect but cannot supply the explanation placed in the artifacts.</instructions>
  <verification>The executor records exact submitted bytes, rechecks every earlier response/body, and runs the runtime-core verifier after all four responses and the trace are accepted.</verification>
  <resume-signal>Reply with the required fields for the single runtime file currently displayed; after the fourth file, supply the requested four-path trace.</resume-signal>
  <verify><automated>python .planning/phases/44-code-cleanup-defense-prep/phase44_guard.py verify-group --repo-root . --inventory .planning/phases/44-code-cleanup-defense-prep/44-WALKTHROUGH-INVENTORY.json --responses .planning/phases/44-code-cleanup-defense-prep/44-STUDENT-RESPONSES-EVAL-RUNTIME.jsonl --group runtime-core</automated></verify>
  <done>The six evidence/evaluation/runtime files have unique student-authored responses, exact canonical bodies, and two task-local commits.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Student-authored local demo UI walkthrough</name>
  <precondition>All earlier groups reverify, the UI server/client rows remain pending, and the Phase 43 compile receipt plus Phase 42 final receipt still match the upstream gate.</precondition>
  <files>walkthrough/11_local_demo_server.py, walkthrough/12_local_demo_client.js, .planning/phases/44-code-cleanup-defense-prep/44-STUDENT-RESPONSES-EVAL-RUNTIME.jsonl</files>
  <action>Guide the student through `src/runtime/demo.py` and `src/runtime/demo_assets/demo.js` as one local-demo subsystem. Require an own-words trace from browser submit through `/api/analyze` to result/error rendering, plus explanations of local-only serving, static-path allowlisting, request cancellation/re-entry, in-memory history, ARIA/live-region updates, and why the UI is not the model. Do not supply candidate wording. Record each response verbatim with Python `#` or JavaScript `//` delimiters only, preserve exact canonical bodies, and repeat the checkpoint until both anchors verify.</action>
  <instructions>The student supplies the standard six fields and per-file authorship sentence for both files, then a short server-to-client trace in their own words. If they cannot explain a named behavior, point to its function/event handler and ask again without drafting the answer.</instructions>
  <verification>The executor verifies exact response bytes, language-appropriate header delimiters, both canonical bodies, the full prior response chain, and the UI subsystem group.</verification>
  <resume-signal>Reply with the required fields for the single UI file currently displayed; after both files, supply the requested server-to-client trace.</resume-signal>
  <verify><automated>python .planning/phases/44-code-cleanup-defense-prep/phase44_guard.py verify-group --repo-root . --inventory .planning/phases/44-code-cleanup-defense-prep/44-WALKTHROUGH-INVENTORY.json --responses .planning/phases/44-code-cleanup-defense-prep/44-STUDENT-RESPONSES-EVAL-RUNTIME.jsonl --group local-demo-ui</automated></verify>
  <done>All 16 exact architecture anchors, including both local demo UI halves, have unique student-authored responses and task-local commits.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-44-09 | Spoofing | integrity explanation | high | mitigate | Blocking four-part student response; no supplied answer text. |
| T-44-10 | Tampering | evaluation narrative | high | mitigate | Exact Phase 41 canonical body and upstream receipt gate. |
| T-44-11 | Repudiation | runtime walkthrough | high | mitigate | Per-file response hashes plus final cross-file trace. |
| T-44-12 | Information Disclosure | evaluation inputs | high | mitigate | Walkthrough opens source code only; no datasets or prediction rows. |
</threat_model>

<verification>All three group verifiers pass, the integrity explanation is student-authored, local demo server/client coverage is complete, and every one of the 16 canonical-body hashes matches inventory.</verification>
<success_criteria>The student can defend the evidence chain, one-shot evaluation, local runtime, and local demo UI from their own 16-file cheatsheet.</success_criteria>
<output>Create `.planning/phases/44-code-cleanup-defense-prep/44-03-SUMMARY.md` when done.</output>
