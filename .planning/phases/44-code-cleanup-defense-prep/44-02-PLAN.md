---
phase: 44-code-cleanup-defense-prep
plan: "02"
type: execute
wave: 2
depends_on: ["44-01"]
files_modified:
  - walkthrough/1_scrape_seeds.py
  - walkthrough/2_generate_synthetic_data.py
  - walkthrough/3_quality_judge.py
  - walkthrough/4_split_dataset.py
  - walkthrough/4a_phase40_data_contract.py
  - walkthrough/5_train_qlora.py
  - walkthrough/5a_train_phobert.py
  - walkthrough/6_convert_to_gguf.py
  - .planning/phases/44-code-cleanup-defense-prep/44-STUDENT-RESPONSES-DATA-MODEL.jsonl
autonomous: false
requirements: [CODE-01, CODE-02]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The student personally explains every final data and training file before any replacement header is accepted."
    - "Each tracked header contains only the student's verbatim words above an exact final canonical body."
    - "Legacy teaching overlays are removed mechanically and never replaced by agent-written prose."
  artifacts:
    - path: ".planning/phases/44-code-cleanup-defense-prep/44-STUDENT-RESPONSES-DATA-MODEL.jsonl"
      provides: "Eight append-only human response and per-file attestation records"
    - path: "walkthrough/1_scrape_seeds.py"
      provides: "First student-authored data-stage walkthrough over canonical code"
    - path: "walkthrough/5a_train_phobert.py"
      provides: "Student-authored explanation of the second full model"
  key_links:
    - from: ".planning/phases/44-code-cleanup-defense-prep/44-STUDENT-RESPONSES-DATA-MODEL.jsonl"
      to: "walkthrough/*.py"
      via: "exact response digest and verbatim comment bytes"
      pattern: "response_sha256|student_comment_sha256|walkthrough_sha256"
---

<objective>Guide the student through the final data and two-model training path, one file at a time, and transcribe only their own comments.</objective>

<execution_context>@C:/Users/wikiepeidia/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.codex/gsd-core/templates/summary.md</execution_context>

<context>
@.planning/phases/44-code-cleanup-defense-prep/44-01-SUMMARY.md
@.planning/phases/44-code-cleanup-defense-prep/44-WALKTHROUGH-PROTOCOL.md
@.planning/phases/44-code-cleanup-defense-prep/44-WALKTHROUGH-INVENTORY.json
</context>

<execution_rules>Each checkpoint task gets one isolated commit only after every file in that task passes `verify-group`. Never amend the prior task. After each task commit and the later `44-02-SUMMARY.md`-only administrative commit, rerun read-only `verify-handoff` against the exact current tip and a clean worktree before proceeding. The agent may quiz or point to functions, but may not supply candidate wording, complete a blank field, or rewrite the student's response.</execution_rules>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Student-authored data pipeline walkthrough</name>
  <precondition>The Phase 44 upstream gate and inventory reverify unchanged, and all four data-group rows remain pending.</precondition>
  <files>walkthrough/1_scrape_seeds.py, walkthrough/2_generate_synthetic_data.py, walkthrough/3_quality_judge.py, walkthrough/4_split_dataset.py, .planning/phases/44-code-cleanup-defense-prep/44-STUDENT-RESPONSES-DATA-MODEL.jsonl</files>
  <action>Walk the student through scraper, seeded generation, independent judge merge, and repaired group-integrity splitting in numbered order. For each file, show its canonical path/hash, public symbol index, and legacy-overlay diff metadata, then ask blank questions for purpose, inputs/outputs, one design choice, one failure/recovery fact, and the comment they want tracked. Do not explain an answer before the student attempts it and do not offer sample comment text. Re-emit this same blocking checkpoint after each response until all four are complete. Record the response exactly, require the per-file authorship sentence, rebuild the numbered file from the student's header plus exact canonical bytes, and reject any canonical-body drift.</action>
  <instructions>The student replies for one displayed file at a time using their own words. They must include `PURPOSE`, `INPUT_OUTPUT`, `DESIGN_REASON`, `FAILURE_OR_LIMIT`, `COMMENT`, and `I wrote this comment myself and can explain this file: &lt;walkthrough path&gt;`. If an answer is incomplete, ask a narrower question but leave the artifact blank; never answer it on the student's behalf.</instructions>
  <verification>After every response, the executor records only the submitted text and rechecks the canonical-body hash. After the fourth accepted response, the data-core automated verifier below must exit zero.</verification>
  <resume-signal>Reply with the six required fields for the single file currently displayed; the checkpoint repeats for the next file until all four verify.</resume-signal>
  <verify><automated>python .planning/phases/44-code-cleanup-defense-prep/phase44_guard.py verify-group --repo-root . --inventory .planning/phases/44-code-cleanup-defense-prep/44-WALKTHROUGH-INVENTORY.json --responses .planning/phases/44-code-cleanup-defense-prep/44-STUDENT-RESPONSES-DATA-MODEL.jsonl --group data-core</automated></verify>
  <done>Four distinct student responses and attestations verify, four exact canonical bodies carry only their verbatim headers, and the task is committed alone.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Student-authored training and deployment walkthrough</name>
  <precondition>Task 1's commit is present, its four entries still verify, the upstream gate is unchanged, and all four training-group rows remain pending.</precondition>
  <files>walkthrough/4a_phase40_data_contract.py, walkthrough/5_train_qlora.py, walkthrough/5a_train_phobert.py, walkthrough/6_convert_to_gguf.py, .planning/phases/44-code-cleanup-defense-prep/44-STUDENT-RESPONSES-DATA-MODEL.jsonl</files>
  <action>Repeat the student-only loop for the Phase 40 data boundary, genuine Qwen QLoRA training, ordinary PhoBERT encoder/classification-head training, and verified GGUF export. Require the student to distinguish the bounded ordinary-LoRA resource probe from the full Qwen QLoRA run, explain why PhoBERT is not QLoRA, and state what GGUF export changes. These are quiz topics, not prose for the agent to author. Transcribe only exact submitted comment text and keep canonical bodies byte-identical.</action>
  <instructions>The student supplies the same six fields and exact per-file authorship sentence for each displayed file. A request such as "write it for me" does not satisfy this checkpoint; respond with a question about the named function and wait for the student's own explanation.</instructions>
  <verification>After every response, the executor records only the submitted text and rechecks all prior entries. After the fourth accepted response, the model-core automated verifier below must exit zero.</verification>
  <resume-signal>Reply with the six required fields for the single training/deployment file currently displayed; the checkpoint repeats until all four verify.</resume-signal>
  <verify><automated>python .planning/phases/44-code-cleanup-defense-prep/phase44_guard.py verify-group --repo-root . --inventory .planning/phases/44-code-cleanup-defense-prep/44-WALKTHROUGH-INVENTORY.json --responses .planning/phases/44-code-cleanup-defense-prep/44-STUDENT-RESPONSES-DATA-MODEL.jsonl --group model-core</automated></verify>
  <done>All eight data/model entries are human-authored, hash-bound, body-identical to final source, and committed in two task-local commits.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-44-05 | Spoofing | comment authorship | high | mitigate | Non-bypassable per-file checkpoint and exact human response hash. |
| T-44-06 | Repudiation | review completion | high | mitigate | Append-only response plus explicit path-specific attestation. |
| T-44-07 | Tampering | canonical logic | high | mitigate | Materialized body must byte-match the frozen canonical source. |
| T-44-08 | Elevation of Privilege | agent prose | high | mitigate | No suggestion/paraphrase path; blank fields cannot pass verification. |
</threat_model>

<verification>Both group verifiers pass and each task is an isolated commit whose changed walkthrough headers match the corresponding student response bytes.</verification>
<success_criteria>Eight final data/model modules are genuinely explained and commented by the student, with no retained legacy overlay or agent-authored replacement.</success_criteria>
<output>Create `.planning/phases/44-code-cleanup-defense-prep/44-02-SUMMARY.md` when done.</output>
