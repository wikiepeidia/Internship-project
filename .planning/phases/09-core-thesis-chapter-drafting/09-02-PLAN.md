---
phase: "09"
plan: "02"
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - documents/reports/latex/chapters/03_methodology_and_system_design.tex
  - documents/reports/latex/chapters/04_implementation.tex
autonomous: true
requirements:
  - REP-02
  - REP-03
must_haves:
  truths:
    - "Chapter 3 reports concrete data pipeline numbers: 3,000-row corpus, 49/50 quality review pass, four balanced classes"
    - "Chapter 3 names all three pilot candidates (qwen3-4b-instruct-2507, qwen3.5-4b, qwen2.5-7b-instruct) and states the selection basis (recall-first, hardware-fit on 8GB VRAM)"
    - "Chapter 3 addresses the 4B-vs-8B reconciliation: the proposal targeted an 8B path; after a hardware-fit pilot the 4B path was locked as primary"
    - "Chapter 3 reports training artifact numbers: 2,018 training examples, QLoRA, three-epoch run referenced from the final baseline refresh"
    - "Chapter 3 reports GGUF quantization type (q8_0) and warm latency (~13 seconds on CPU)"
    - "Chapter 4 names the three CLI entry points: vnphish demo, python -m src.runtime.cli demo, and the gguf-laptop / accelerated-local runtime profiles"
    - "Chapter 4 names the three package subsystems: src/data_pipeline, src/runtime, src/model_adaptation"
    - "Chapter 4 names the three pyproject.toml dependency groups: dev, train, runtime"
    - "Neither chapter implies OCR, image, audio, or mobile support"
    - "Neither chapter contains GSD jargon (phase numbers, UAT, roadmap, PLAN.md, BLOCK)"
  artifacts:
    - path: "documents/reports/latex/chapters/03_methodology_and_system_design.tex"
      provides: "Expanded methodology chapter with concrete evidence-grounded numbers"
      contains: "3{,}000"
    - path: "documents/reports/latex/chapters/04_implementation.tex"
      provides: "Expanded implementation chapter with module names and CLI surface"
      contains: "vnphish"
  key_links:
    - from: "documents/reports/latex/chapters/03_methodology_and_system_design.tex"
      to: "data/manifests/phase3-large-pilot-2026-05-14.json"
      via: "Pilot candidate numbers sourced from this manifest"
      pattern: "qwen3-4b-instruct-2507"
    - from: "documents/reports/latex/chapters/04_implementation.tex"
      to: "docs/user/USER_GUIDE.md"
      via: "CLI entry point names sourced from user guide"
      pattern: "vnphish"
---

<objective>
Expand Chapters 3 and 4 from their current scaffold prose into full, evidence-grounded chapters. Chapter 3 (Methodology and System Design) needs concrete numbers from the data pipeline manifests, a named pilot comparison narrative, the 4B-vs-8B reconciliation, and training artifact details. Chapter 4 (Implementation) needs module names, CLI entry points, runtime profiles, and dependency group details from the user documentation.

Purpose: These two chapters carry the core technical substance of the thesis — the what-and-why of design decisions. Expanding them with concrete evidence makes the thesis verifiable rather than a narrative retelling.

Output: Expanded 03_methodology_and_system_design.tex and 04_implementation.tex, each with substantive prose replacing the current high-level scaffold sentences.
</objective>

<execution_context>
@C:/Users/wikiepeidia/OneDrive - caugiay.edu.vn/bài tập/usth/GEN14/INTERNSHIP/Internship-project/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wikiepeidia/OneDrive - caugiay.edu.vn/bài tập/usth/GEN14/INTERNSHIP/Internship-project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-core-thesis-chapter-drafting/09-RESEARCH.md
@.planning/phases/08-thesis-structure-and-evidence-map/08-WRITING_GUARDRAILS.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand Chapter 3 — Methodology and System Design with evidence-grounded prose</name>
  <files>
    documents/reports/latex/chapters/03_methodology_and_system_design.tex
  </files>
  <read_first>
    - documents/reports/latex/chapters/03_methodology_and_system_design.tex (current file — read before touching)
    - .planning/phases/08-thesis-structure-and-evidence-map/08-WRITING_GUARDRAILS.md (tone rules)
    - .planning/STATE.md (training artifact numbers: 2018 train, 210 val, QLoRA, checkpoint-505; GGUF q8_0, warm latency ~13s; pilot: qwen3-4b-instruct-2507 winner, qwen3.5-4b runner-up; corpus: 3,000 rows recovered-balanced.jsonl)
  </read_first>
  <action>
    Rewrite 03_methodology_and_system_design.tex. Keep the chapter opening paragraph (describing the five connected layers), the two \input{} calls for tables, and all six section headings. Replace each section body with full substantive prose. Follow the tone rules from WRITING_GUARDRAILS.md throughout. Do not mention "Phase 7a", "Phase 3", "Phase 5", "UAT", "BLOCK", "GSD", "roadmap", "PLAN.md", or any other internal workflow label.

    Section "Development Structure": Keep as-is. It already correctly describes the four-layer plus evaluation structure.

    Section "Data Construction and Split Governance": Expand to 3-4 paragraphs.
    - Paragraph 1: The dataset was assembled from public Vietnamese warning sources and supplemented with synthetic examples generated through a structured prompt pipeline with a quality-judge step. The final corpus contains 3,000 rows across four classes: bank impersonation, social-engineering messages, task scam, and benign. Each build is tied to versioned manifests with SHA-256 content hashes so that later evaluation claims can be traced to a specific snapshot rather than a reconstructed memory.
    - Paragraph 2: A quality-judge pass reviewed a random sample of 50 synthetic examples. Forty-nine passed inspection; the average realism score was 4.68 out of 5 and the average label-correctness score was 4.96 out of 5. This step was included to catch low-quality or mislabeled synthetic rows before they entered the training split.
    - Paragraph 3: The split governance design uses seed-disjoint allocation: each unique seed record can appear in only one split so that training examples cannot contaminate the held-out evaluation. The final validated split after the targeted task-scam data recovery contained 2,018 training examples and 210 validation examples. Held-out support for the final evaluation included 56 bank-impersonation, 75 social-engineering, 62 task-scam, and 61 benign examples.

    Section "Offline Text Runtime Baseline": Expand to 2-3 paragraphs.
    - Paragraph 1: The runtime design centers on a stable text-only analysis contract. Messages are submitted as text, normalized locally, and dispatched to a backend through a stable interface so that swapping backends does not break the operator surface or the output format.
    - Paragraph 2: Privacy is treated as a first-class design requirement. By default, raw message text does not leave the local device. The version~1 boundary is strictly text-only; OCR, image, audio, and mobile input channels are explicitly out of scope.
    - Paragraph 3: A doctor command verifies that the selected runtime profile is loaded correctly before analysis. If the profile is not ready the runtime stops with explicit diagnostic output rather than falling back silently to an unintended path.

    Section "Local Model Selection and Deployment Paths": Expand to 4-5 paragraphs. This is the most important section for the 4B-vs-8B reconciliation.
    - Paragraph 1 — candidate scope: The selection pilot evaluated three open-weight candidates on 33 fixed balanced examples under recall-first scoring: qwen3-4b-instruct-2507, qwen3.5-4b, and qwen2.5-7b-instruct. The pilot was run locally on the target laptop hardware to keep hardware-fit in scope from the start.
    - Paragraph 2 — 4B-vs-8B reconciliation: The project's original proposal planned to adapt an 8B-parameter model. After the pilot evaluation, the 4B-primary path was locked as the baseline because both 4B candidates provided more reliable hardware fit on the project laptop (8~GB VRAM) without sacrificing the recall-first selection criterion. The 7B model was retained as a comparison candidate and as an option for better-equipped hardware, but the 4B path became the primary delivery target. A dated execution update in the proposal record documents this decision.
    - Paragraph 3 — selection result: qwen3-4b-instruct-2507 was selected as the baseline winner and qwen3.5-4b as the runner-up on the basis of recall performance and hardware fit. Both paths were taken through the full adaptation workflow.
    - Paragraph 4 — training: The baseline model was adapted using QLoRA over three epochs on the retained dataset. The final baseline training run used 2,018 training examples and 210 validation examples on a CUDA-enabled device, reaching a training loss of 0.4951 and saving its final checkpoint at step 505. The resulting adapter artifact was registered in the off-device model registry for runtime use.
    - Paragraph 5 — deployment: After training, the adapter-merged model was exported to GGUF format with q8\_0 quantization using the \texttt{convert\_hf\_to\_gguf.py} script. The exported artifact enables local CPU inference without requiring CUDA at runtime. After a prompt-size optimization pass, the warm inference latency on the target laptop CPU was approximately 13~seconds — acceptable for thesis demonstration use.

    Section "Explainable Threat Decisioning": Expand to 2-3 paragraphs.
    - Paragraph 1: The decision layer returns four elements for each message: a risk tier (benign, suspicious, or high-risk), one or more in-scope threat labels for risky inputs, grounded suspicious cues taken from spans of the analyzed text, and safe next-step recommendations.
    - Paragraph 2: The decision design enforces two honesty constraints. Cues must come from the message itself rather than from model confidence scores alone. Recommendations must stay within a safe, generic set and must not instruct users to take actions that could cause harm.
    - Paragraph 3: A fail-closed rule is applied at the runtime boundary: if the configured local profile is not ready, the system stops and reports the reason instead of silently switching to a less safe fallback.

    Section "Design Principles": Keep as-is. It is already well-written and accurate.

    Add \cite{} placeholder markers at two locations (they will be wired in Plan 03):
    - At the end of the explainability sentence in Section "Explainable Threat Decisioning" (first paragraph), add \cite{rjoub2023surveyxai}.
    - At the end of the privacy sentence in Section "Offline Text Runtime Baseline" (second paragraph), add \cite{nist2026privacyframework}.

    Forbidden in this chapter: "Phase 3", "Phase 7a", "GSD", "UAT", "BLOCK", "roadmap", "PLAN.md", "sprint", "wave".
  </action>
  <verify>
    The string "3{,}000" or "3,000" appears in 03_methodology_and_system_design.tex.
    The string "qwen3-4b-instruct-2507" appears in 03_methodology_and_system_design.tex.
    The string "qwen2.5-7b" or "7B" appears in 03_methodology_and_system_design.tex (pilot candidate mentioned).
    The string "2{,}018" or "2,018" appears in 03_methodology_and_system_design.tex.
    The string "q8" appears in 03_methodology_and_system_design.tex (quantization type).
    The string "13" appears in 03_methodology_and_system_design.tex (latency reference).
    The string "rjoub2023surveyxai" appears in 03_methodology_and_system_design.tex.
    The string "nist2026privacyframework" appears in 03_methodology_and_system_design.tex.
    The string "Phase 3" does not appear in 03_methodology_and_system_design.tex.
    The string "BLOCK" does not appear in 03_methodology_and_system_design.tex.
    The string "GSD" does not appear in 03_methodology_and_system_design.tex.
  </verify>
  <done>
    Chapter 3 includes substantive prose for all six sections. The Data Construction section reports 3,000 rows, 49/50 quality pass, 2,018 training examples, and 210 validation examples. The Model Selection section names all three pilot candidates, explains the 4B-vs-8B decision as a hardware-fit choice, and mentions q8_0 quantization and ~13s warm latency. Citation markers \cite{rjoub2023surveyxai} and \cite{nist2026privacyframework} are present. No forbidden terms appear.
  </done>
</task>

<task type="auto">
  <name>Task 2: Expand Chapter 4 — Implementation with module names, CLI entry points, and runtime profiles</name>
  <files>
    documents/reports/latex/chapters/04_implementation.tex
  </files>
  <read_first>
    - documents/reports/latex/chapters/04_implementation.tex (current file — read before touching)
    - docs/user/USER_GUIDE.md (CLI entry points and operator flow — read before writing Ch4)
    - docs/user/LOCAL_MODELS.md (runtime profiles gguf-laptop and accelerated-local — read before writing Ch4)
    - .planning/phases/08-thesis-structure-and-evidence-map/08-WRITING_GUARDRAILS.md (tone rules)
  </read_first>
  <action>
    Rewrite 04_implementation.tex. Keep the chapter opening paragraph and all six section headings. Expand each section body by replacing the current high-level scaffold sentences with concrete implementation detail drawn from USER_GUIDE.md and LOCAL_MODELS.md. Follow WRITING_GUARDRAILS.md throughout. Do not mention "Phase 3", "Phase 7a", "GSD", "UAT", "BLOCK", "roadmap", "PLAN.md", "sprint", or "wave".

    Chapter opening paragraph: Keep as-is. The three subsystem split (src/data_pipeline, src/runtime, src/model_adaptation) and the "one connected system" framing are accurate.

    Section "Codebase Organization": Expand to 2 paragraphs.
    - Paragraph 1: List the three main packages and their roles clearly. The data-pipeline package handles schemas, scraping, normalization, synthetic generation, quality judging, and versioned manifest output. The runtime package exposes the public analysis contract, the service boundary, backend dispatchers, rendering, and the doctor readiness checker. The model-adaptation package manages candidate catalogs, pilot scoring, training orchestration, GGUF conversion, and the off-device registry integration.
    - Paragraph 2: The operator-facing entry point for analysis is \texttt{vnphish} (registered as a console script in \texttt{pyproject.toml}). The primary demo launch paths are \texttt{vnphish demo} and \texttt{python -m src.runtime.cli demo}. The doctor command is \texttt{vnphish doctor} or \texttt{python -m src.runtime.cli doctor}. All entry points are documented in the user guide shipped with the codebase.

    Section "Data Pipeline Implementation": Expand to 2-3 paragraphs. Pull any concrete detail available from USER_GUIDE.md about data commands. If no data command detail is available, use the known structure:
    - Schema validation through Pydantic models keeps each pipeline stage working with a known data shape.
    - The generation and quality-judge steps are explicit CLI commands rather than notebook cells, which keeps provenance visible and reduces accidental contamination between build stages.
    - Versioned manifests record the SHA-256 content hash, build timestamp, and git commit identifier for each produced artifact.

    Section "Runtime Contracts and Service Boundary": Expand to 2-3 paragraphs.
    - \texttt{AnalysisRequest} and \texttt{AnalysisResult} are the primary public types. Both are defined in the runtime contracts module and are the only types that cross the service boundary.
    - The service layer performs message normalization, dispatches to the configured backend, and passes the structured result to the renderer.
    - The renderer converts the structured result into a readable terminal report. This separation means that changing the display format requires no changes to the analysis contract.

    Section "Backend Implementations": Expand to 2 paragraphs.
    - Two local model backends are available in the shipped codebase. The \texttt{gguf-laptop} profile targets CPU inference after GGUF conversion and is the default local profile. The \texttt{accelerated-local} profile targets stronger GPU hardware and reuses the same output contract.
    - The heuristic backend provides the offline baseline when no model artifact is available. All three backends return the same \texttt{AnalysisResult} type so that the rendering layer does not need to branch on backend type.
    - Profile selection is explicit: the operator sets the profile at startup rather than relying on auto-detection. A fail-closed doctor check verifies the selected profile before analysis starts.

    Section "Model Adaptation Workflow": Expand to 2-3 paragraphs.
    - The adaptation package coordinates three steps: candidate selection via a pilot scorecard, training via the QLoRA workflow, and artifact registration in the off-device model registry.
    - Training scripts run over the retained dataset and save periodic checkpoints. After training completes, the convert command merges the adapter weights and exports a GGUF artifact at q8\_0 quantization.
    - The registry-based artifact resolution means that the runtime always loads the newest registered adapter and GGUF file. This prevents stale artifacts from being selected accidentally after a new training run.

    Section "Operator Surface and Packaging": Expand to 2 paragraphs.
    - The \texttt{pyproject.toml} separates dependencies into three groups: \texttt{dev} for development and testing tools, \texttt{train} for GPU-dependent training libraries, and \texttt{runtime} for the inference-only deployment path. A user who only wants to run the demo can install only the \texttt{runtime} group without pulling in training dependencies.
    - The demo server is started with \texttt{vnphish demo} or \texttt{python -m src.runtime.cli demo}. It loads the configured GGUF model once at startup (the warm-up step) so that the first analysis request after the browser opens does not face the full cold-load time. The target warm latency on the laptop CPU is approximately 13~seconds.

    Add one \cite{} placeholder at the end of the privacy sentence in Section "Runtime Contracts and Service Boundary" (second paragraph closing on normalization): add \cite{nist2026privacyframework}.

    Do not imply OCR, image, audio, or mobile support anywhere in this chapter.
  </action>
  <verify>
    The string "vnphish" appears in 04_implementation.tex.
    The string "gguf-laptop" appears in 04_implementation.tex.
    The string "accelerated-local" appears in 04_implementation.tex.
    The string "runtime" appears in 04_implementation.tex alongside "dev" and "train" (dependency groups).
    The string "pyproject.toml" appears in 04_implementation.tex.
    The string "AnalysisRequest" appears in 04_implementation.tex.
    The string "Phase 3" does not appear in 04_implementation.tex.
    The string "GSD" does not appear in 04_implementation.tex.
  </verify>
  <done>
    Chapter 4 has substantive prose for all six sections. The codebase organization section names all three packages and the console script entry points. The backend section names gguf-laptop and accelerated-local profiles. The operator section names the three pyproject.toml dependency groups and the demo launch commands with the ~13s warm latency. A \cite{nist2026privacyframework} marker is present. No forbidden terms or out-of-scope capability claims appear.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| STATE.md training numbers -> Chapter 3 prose | Numbers from session-continuity notes must be transcribed exactly into prose |
| USER_GUIDE.md / LOCAL_MODELS.md -> Chapter 4 prose | CLI surface details must match the actual docs; executor must read these before writing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Information Disclosure | Chapter 3 scope claims | mitigate | Verify no OCR/image/audio/mobile claim is introduced; acceptance criteria checks for absence of those terms |
| T-09-05 | Tampering | Chapter 3 training numbers | mitigate | Source numbers from STATE.md session continuity section only; do not invent or round numbers (2018 not 2000, 210 not 200) |
| T-09-06 | Repudiation | 4B-vs-8B reconciliation | mitigate | Present decision as explicit hardware-fit pilot result, not as a scope reduction; match the proposal execution update framing |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; documentation-only work |
</threat_model>

<verification>
After both tasks complete:
1. Run: grep -c "3.000\|3,000\|3{,}000" documents/reports/latex/chapters/03_methodology_and_system_design.tex -- must return >= 1
2. Run: grep -c "qwen3-4b-instruct-2507" documents/reports/latex/chapters/03_methodology_and_system_design.tex -- must return >= 1
3. Run: grep -c "2.018\|2,018\|2{,}018" documents/reports/latex/chapters/03_methodology_and_system_design.tex -- must return >= 1
4. Run: grep -c "vnphish" documents/reports/latex/chapters/04_implementation.tex -- must return >= 1
5. Run: grep -c "gguf-laptop" documents/reports/latex/chapters/04_implementation.tex -- must return >= 1
6. Run: grep -c "GSD\|Phase 3\|Phase 7\|BLOCK\|roadmap\|PLAN\.md" documents/reports/latex/chapters/03_methodology_and_system_design.tex -- must return 0
7. Run: grep -c "GSD\|Phase 3\|Phase 7\|BLOCK\|roadmap\|PLAN\.md" documents/reports/latex/chapters/04_implementation.tex -- must return 0
8. Human check: Read the Model Selection section of Chapter 3 and confirm the 4B-vs-8B reconciliation is explained as a hardware-fit pilot decision, not as a scope reduction or apology.
</verification>

<success_criteria>
Chapter 3 has substantive prose for all six sections with concrete numbers (3,000-row corpus, 49/50 quality pass, three named pilot candidates, 2,018 training examples, q8_0 GGUF, ~13s latency) and \cite{} markers for rjoub2023surveyxai and nist2026privacyframework. Chapter 4 has substantive prose for all six sections naming all three packages, both runtime profiles, three CLI entry points, and three dependency groups. Neither chapter contains any forbidden GSD term or any claim of OCR/image/audio support.
</success_criteria>

<output>
Create `.planning/phases/09-core-thesis-chapter-drafting/09-02-SUMMARY.md` when done.
</output>
