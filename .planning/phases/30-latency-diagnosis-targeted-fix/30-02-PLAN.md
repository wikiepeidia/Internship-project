---
phase: 30-latency-diagnosis-targeted-fix
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - .planning/phases/30-latency-diagnosis-targeted-fix/artifacts/30-latency-comparison.md
  - .planning/phases/30-latency-diagnosis-targeted-fix/artifacts/30-fix-decision.md
autonomous: false
requirements: [PERF-01, PERF-02, PERF-03]

must_haves:
  truths:
    - "One evidence JSON artifact exists for AC/High Performance (battery/Balanced descoped per D-10 — accepted risk, not measured)"
    - "The evidence artifact is marked post_reboot_confirmed=true and run_purpose=evidence"
    - "The first prompt verdict in the evidence artifact matches the locked scam verdict"
    - "D-05: The comparison record decides whether a specific bottleneck was identified before any code/config change is considered"
    - "D-06: If one bottleneck is identified, the decision record names exactly one targeted fix and requires before/after re-measurement before PERF-02 can close"
    - "D-07: The decision record forbids bundled latency tweaks, llama-cpp upgrades, prompt/schema redesign, backend contract changes, or batches of maybe-faster changes"
    - "D-08: If no specific bottleneck was identified, the phase records no fix applied as the correct PERF-02 outcome"
  artifacts:
    - path: ".planning/phases/30-latency-diagnosis-targeted-fix/artifacts/30-latency-comparison.md"
      provides: "AC cold-latency measurement writeup and bottleneck analysis (battery column marked descoped)"
    - path: ".planning/phases/30-latency-diagnosis-targeted-fix/artifacts/30-fix-decision.md"
      provides: "explicit PERF-02 decision: no fix applied, or one targeted fix required"
---

<objective>
Use the human-produced post-reboot AC evidence artifact to close the measurement requirements and decide whether a targeted fix is justified. Battery/Balanced measurement is descoped (D-10 SUPERSEDED) — the laptop's 1-2h battery life plus a charger-backup plan cover the defense-day risk without a separate measurement. This plan is intentionally blocked on real laptop actions the agent cannot perform: rebooting, selecting AC/High Performance, and running the script as the first post-reboot demo request.
</objective>

<execution_context>
@C:/Users/wikiepeidia/OneDrive - caugiay.edu.vn/bài tập/usth/GEN14/INTERNSHIP/Internship-project/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wikiepeidia/OneDrive - caugiay.edu.vn/bài tập/usth/GEN14/INTERNSHIP/Internship-project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-latency-diagnosis-targeted-fix/30-CONTEXT.md
@.planning/phases/30-latency-diagnosis-targeted-fix/30-RESEARCH.md
@.planning/phases/30-latency-diagnosis-targeted-fix/30-01-SUMMARY.md
@.planning/phases/30-latency-diagnosis-targeted-fix/artifacts/30-cold-latency-runbook.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human performs AC post-reboot evidence run</name>
  <what-built>Plan 30-01 produced the measurement harness and runbook. The coding agent cannot reboot the presentation laptop, select AC/High Performance, or honestly confirm the first post-reboot request; those actions must be done by the human operator. Battery/Balanced measurement is descoped (D-10 SUPERSEDED 2026-07-06) — the laptop's 1-2h battery life plus a charger-backup plan cover the defense-day risk without a separate measurement.</what-built>
  <how-to-verify>
    Follow `.planning/phases/30-latency-diagnosis-targeted-fix/artifacts/30-cold-latency-runbook.md`, AC/High Performance section only.

    Required output:
    - One JSON artifact from an AC/High Performance post-reboot run.

    The command must include `--run-purpose evidence --post-reboot-confirmed`.
  </how-to-verify>
  <resume-signal>Reply with "latency-evidence-ready" and the JSON artifact path. If the run failed, paste the error output.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record the AC evidence and decide the fix gate</name>
  <read_first>
    The JSON path provided by the human checkpoint.
    30-RESEARCH.md "Fix Gate".
    src/runtime/analyzers/gguf.py only if the evidence points to a GGUF runtime parameter bottleneck.
  </read_first>
  <files>
    .planning/phases/30-latency-diagnosis-targeted-fix/artifacts/30-latency-comparison.md
    .planning/phases/30-latency-diagnosis-targeted-fix/artifacts/30-fix-decision.md
  </files>
  <action>
    Validate the JSON artifact: condition must be `ac-high-performance`; `run_purpose` must be `evidence`; `post_reboot_confirmed` must be true; must include a first prompt result matching the locked scam verdict (`high-risk` and `bank_impersonation`).

    Create `30-latency-comparison.md` recording page-ready time, total-to-first-answer time, first browser request latency, and verdict correctness for the AC run. Note battery/Balanced as descoped per D-10 SUPERSEDED (accepted risk, not measured — laptop battery life + charger backup). Write a bottleneck analysis that distinguishes startup/model-warm-up cost from first-request inference cost.

    Create `30-fix-decision.md` with one of exactly two decisions:
    - `NO_FIX_APPLIED`: no specific bottleneck was identified; PERF-02 is satisfied by not changing code and documenting why.
    - `TARGETED_FIX_REQUIRED`: a specific bottleneck was identified; name the bottleneck, name exactly one proposed fix, and stop autonomous execution before applying it unless a follow-up one-fix plan is created.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command '$c=".planning/phases/30-latency-diagnosis-targeted-fix/artifacts/30-latency-comparison.md"; $d=".planning/phases/30-latency-diagnosis-targeted-fix/artifacts/30-fix-decision.md"; if (-not (Test-Path -LiteralPath $c) -or -not (Test-Path -LiteralPath $d)) { exit 1 }; $decision=Get-Content -Raw -LiteralPath $d; if ($decision -notmatch "NO_FIX_APPLIED|TARGETED_FIX_REQUIRED") { exit 1 }'</automated>
  </verify>
  <acceptance_criteria>The comparison artifact validates the required AC evidence run and the decision artifact clearly states whether no fix was applied or exactly one targeted fix is required.</acceptance_criteria>
  <done>PERF-01/PERF-03 are closed if the AC measurement is valid; PERF-02 is closed only if the decision is `NO_FIX_APPLIED` or after a subsequent one-fix implementation and re-measurement if `TARGETED_FIX_REQUIRED`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Human-reported artifact paths -> agent comparison | The agent must validate the JSON content instead of trusting the filename or user wording alone. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-30-04 | Tampering | Mislabelled evidence artifact | mitigate | Validate `run_purpose`, `post_reboot_confirmed`, and condition fields inside both JSON files before accepting them. |
| T-30-05 | Tampering | Unjustified performance fix | mitigate | `30-fix-decision.md` must choose exactly one of `NO_FIX_APPLIED` or `TARGETED_FIX_REQUIRED`; no source edits occur in this plan unless a follow-up targeted fix plan exists. |

</threat_model>

<verification>
The phase can pass with no code fix if the comparison artifact proves the true cold latency and no specific bottleneck is identified. If a specific bottleneck is identified, this plan must stop with `TARGETED_FIX_REQUIRED` and cannot mark PERF-02 complete until one targeted fix is applied and re-measured.
</verification>

<success_criteria>
- AC/High Performance post-reboot first-answer latency recorded.
- Battery/Balanced measurement explicitly recorded as descoped (D-10 SUPERSEDED), not silently omitted.
- PERF-02 decision documented without blind tuning.
</success_criteria>

<output>
Create `.planning/phases/30-latency-diagnosis-targeted-fix/30-02-SUMMARY.md` when done.
</output>
