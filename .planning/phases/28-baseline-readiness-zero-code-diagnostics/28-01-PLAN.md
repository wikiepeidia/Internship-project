---
phase: 28-baseline-readiness-zero-code-diagnostics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify_golden_prompts.py
  - .planning/phases/28-baseline-readiness-zero-code-diagnostics/artifacts/28-RESULTS.md
  - .planning/phases/28-baseline-readiness-zero-code-diagnostics/artifacts/28-golden-prompt-results.json
autonomous: true
requirements: [DIAG-01, DIAG-02, DIAG-03, GOLD-01, GOLD-02]

must_haves:
  truths:
    - "vnphish doctor reports READY (exit 0) on the dev machine"
    - "vnphish analyze produces the correct risk tier and threat label for one message per in-scope threat class (bank impersonation, zalo social engineering, task scam) plus one correctly-classified benign message"
    - "Exactly one scam prompt (bank impersonation) and one benign prompt are locked as the fixed ~1-minute live-demo script"
    - "Each locked golden prompt returns the identical correct verdict across at least 5 repeated runs through the real web demo, not just the CLI"
    - "A first-pass warm-latency reading for a demo request is captured and recorded for later comparison"
  artifacts:
    - path: "scripts/verify_golden_prompts.py"
      provides: "Playwright script that drives the real vnphish demo web UI to lock golden prompts (GOLD-01/02) and capture warm-latency timing (DIAG-03)"
      min_lines: 60
    - path: ".planning/phases/28-baseline-readiness-zero-code-diagnostics/artifacts/28-RESULTS.md"
      provides: "Consolidated human-readable record of DIAG-01, DIAG-02, DIAG-03, GOLD-01, GOLD-02 results"
      min_lines: 30
    - path: ".planning/phases/28-baseline-readiness-zero-code-diagnostics/artifacts/28-golden-prompt-results.json"
      provides: "Machine-readable 5-run stability and latency results for both locked golden prompts"
      contains: "\"stable\""
  key_links:
    - from: "scripts/verify_golden_prompts.py"
      to: "/api/analyze"
      via: "Playwright page.click(#analyze-button) + page.expect_response against a script-owned `vnphish demo` subprocess"
      pattern: "expect_response.*api/analyze"
    - from: ".planning/phases/28-baseline-readiness-zero-code-diagnostics/artifacts/28-RESULTS.md"
      to: ".planning/phases/28-baseline-readiness-zero-code-diagnostics/artifacts/28-golden-prompt-results.json"
      via: "final locked-prompt text and stability tables transcribed from the JSON results file"
      pattern: "STABLE"
---

<objective>
Prove — using only existing zero-code tooling (`vnphish doctor`, `vnphish analyze`, the real `vnphish demo` web UI) — that the demo's core functionality is reproducibly correct on the dev machine, and lock the exact 2 prompts (1 bank-impersonation scam + 1 benign) that will be used for the real ~1-minute live demo during the defense.

This phase makes zero changes to `src/runtime/**`. The only new artifacts are a throwaway Playwright verification script and two written results records.

Purpose: DIAG-01/02/03 establish today's baseline correctness and timing before any Phase 29-31 fixes land. GOLD-01/02 lock the exact live-demo script so the committee sees a proven-stable, non-improvised result in the real ~1-minute window.

Output:
- `scripts/verify_golden_prompts.py` — reusable Playwright verification script (satisfies GOLD-01, GOLD-02, DIAG-03 in one run)
- `.planning/phases/28-baseline-readiness-zero-code-diagnostics/artifacts/28-RESULTS.md` — consolidated results record
- `.planning/phases/28-baseline-readiness-zero-code-diagnostics/artifacts/28-golden-prompt-results.json` — machine-readable stability/latency data
</objective>

<artifacts_produced>
## Artifacts This Phase Produces

| Path | Created By | Purpose |
|------|-----------|---------|
| `scripts/verify_golden_prompts.py` | Task 2 | Playwright script driving the real web demo for GOLD-01/02 + DIAG-03 |
| `.planning/phases/28-baseline-readiness-zero-code-diagnostics/artifacts/28-RESULTS.md` | Task 1 (created), Task 3 (finalized) | Human-readable consolidated results record for all 5 phase requirements |
| `.planning/phases/28-baseline-readiness-zero-code-diagnostics/artifacts/28-golden-prompt-results.json` | Task 3 (produced by running Task 2's script) | Machine-readable 5-run stability + latency data for both golden prompts |
| `.planning/phases/28-baseline-readiness-zero-code-diagnostics/28-01-SUMMARY.md` | execute-plan (standard GSD output step) | Standard plan-execution summary |

No files under `src/runtime/`, `src/data_pipeline/`, or `src/config/` are created or modified — this phase is verification-only.
</artifacts_produced>

<execution_context>
@C:/Users/wikiepeidia/OneDrive - caugiay.edu.vn/bài tập/usth/GEN14/INTERNSHIP/Internship-project/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wikiepeidia/OneDrive - caugiay.edu.vn/bài tập/usth/GEN14/INTERNSHIP/Internship-project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-baseline-readiness-zero-code-diagnostics/28-CONTEXT.md
@.planning/phases/28-baseline-readiness-zero-code-diagnostics/28-RESEARCH.md
@.planning/phases/28-baseline-readiness-zero-code-diagnostics/28-PATTERNS.md

@src/runtime/cli.py
@src/runtime/demo_assets/demo.js
@tests/runtime/conftest.py
@tests/runtime/test_demo.py
@src/data_pipeline/scraper/ncsc_scraper.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: DIAG-01 doctor confirmation + DIAG-02 four-class CLI correctness pass</name>
  <read_first>
    src/runtime/cli.py (handle_doctor, handle_analyze exit-code contract),
    .planning/phases/28-baseline-readiness-zero-code-diagnostics/28-RESEARCH.md (sections "Concrete Mechanics: vnphish doctor", "Concrete Mechanics: vnphish analyze", "Sourcing DIAG-02's Other Threat-Class Candidates", "Common Pitfalls #2, #3"),
    tests/runtime/conftest.py (sample_benign_message fixture, line 18)
  </read_first>
  <files>.planning/phases/28-baseline-readiness-zero-code-diagnostics/artifacts/28-RESULTS.md</files>
  <action>
    Run `vnphish doctor` (or `python -m src.runtime.cli doctor` if the console script is not on PATH) and capture full stdout plus exit code. Confirm exit code 0 and the top-line `READY backend=gguf local_only=True text_only=True`. Per RESEARCH.md Common Pitfall #2, do not treat the `release-gate-summary` line's embedded `verdict=BLOCK` as a doctor failure — it is a stale pre-Phase-7a artifact reference; the doctor check only verifies file readability, not verdict freshness, and the actual final verdict is PASS per STATE.md. Note this cosmetic caveat in the results file so it is not misread later.

    Then run one `vnphish analyze --text "<message>" --channel <channel>` per DIAG-02 class (never embed Vietnamese text in a `curl -d` one-liner per Pitfall #3 — the `--text` CLI argument form is confirmed to work correctly):
    1. bank_impersonation, channel sms: "Thông báo từ TPBank: OTP của bạn là 847291. KHÔNG chia sẻ mã này với bất kỳ ai. Nếu bạn ko thực hiện giao dịch, gọi ngay 0938.xxx.xxx để được hỗ trợ khóa account." — expect exit 0, threat label "Bank impersonation", risk tier non-benign (suspicious or high-risk both count as correct per RESEARCH.md's own note that the model may render this slightly more conservatively than the dataset's gold `high-risk` label).
    2. zalo_social_engineering, channel zalo: "Bro ơi mình đang ở nước ngoài, điện thoại hỏng ko gọi dc. Mày có thể mua giúp tao thẻ Viettel 500k rồi chụp mã gửi qua Zalo k? Tao về trả liền, cần gấp lắm 😭" — expect exit 0, threat label "Zalo social engineering", risk tier non-benign. This candidate was NOT smoke-tested in RESEARCH.md (flagged as an open question) — if it produces the wrong label, exit code 1, or a benign verdict, pull an alternate `zalo_social_engineering` row from `data/splits/recovered-balanced/val.jsonl` (filter rows where `"label": "zalo_social_engineering"`, 75 candidates available) and retry with the next untested row until one classifies correctly. Record any substitution and why in the results file.
    3. task_scam, channel sms: "Bạn ơi, tài khoản của bạn đang có vấn đề. Vui lòng đăng nhập vào app và kiểm tra lại. Nếu cần hỗ trợ, gọi 0123456789. Thanks!" — expect exit 0, threat label "Task scam", risk tier non-benign (this split's task_scam rows are all gold-labeled `suspicious`, not `high-risk` — suspicious is the expected/correct tier here). Also NOT smoke-tested in RESEARCH.md — if it fails, pull an alternate `task_scam` row from the same val.jsonl (62 candidates) and retry until correct, recording the substitution.
    4. benign, channel sms: "Chào bạn, lịch họp nhóm được dời sang 9h sáng mai tại phòng học tầng 3. Nếu bận thì báo lại giúp mình trước tối nay." (the exact `sample_benign_message` fixture text) — expect exit 0, `Risk tier: Benign`, `Threat labels: Benign`. Already confirmed correct in RESEARCH.md; re-run here to reconfirm on this exact invocation.

    Create `.planning/phases/28-baseline-readiness-zero-code-diagnostics/artifacts/28-RESULTS.md` with two sections: `## DIAG-01: vnphish doctor` (full captured output, exit code, the release-gate-summary caveat note) and `## DIAG-02: vnphish analyze correctness pass` (a small table: class | message text used | channel | exit code | risk tier | threat labels | substituted-from-fallback y/n). All 4 rows must show a correct classification before this task is done.
  </action>
  <verify>
    <automated>vnphish doctor; test $? -eq 0 && vnphish analyze --text "Thông báo từ TPBank: OTP của bạn là 847291. KHÔNG chia sẻ mã này với bất kỳ ai. Nếu bạn ko thực hiện giao dịch, gọi ngay 0938.xxx.xxx để được hỗ trợ khóa account." --channel sms | grep -c "Bank impersonation"</automated>
  </verify>
  <done>
    `vnphish doctor` exits 0 and prints the top-line READY status. All 4 DIAG-02 candidates (bank_impersonation, zalo_social_engineering, task_scam, benign) return exit 0 with the correct in-scope threat label (or Benign) and a non-crashing, non-empty result. `28-RESULTS.md` exists and records all captured output plus any candidate substitutions made.
  </done>
  <acceptance_criteria>
    - [ ] `vnphish doctor` output captured with exit code 0 confirmed
    - [ ] Release-gate-summary stale-verdict caveat documented (not mistaken for a doctor failure)
    - [ ] 4/4 DIAG-02 classes produce a correct classification (label matches class; benign message is not flagged as any scam type)
    - [ ] Any zalo_social_engineering or task_scam candidate substitution is recorded with its source row
    - [ ] `28-RESULTS.md` created with DIAG-01 and DIAG-02 sections populated
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Playwright golden-prompt verification script (GOLD-01/02 + DIAG-03 tooling)</name>
  <read_first>
    src/data_pipeline/scraper/ncsc_scraper.py (lines ~45-62, `_fetch_with_playwright` — lazy import + launch/close pattern to imitate, and the exception-swallowing pattern to deliberately NOT copy),
    src/runtime/demo_assets/demo.js (DOM ids and the exact fetch('/api/analyze') call),
    tests/runtime/test_demo.py (lines 39-116, confirms `#message-input`, `#channel-select`, `#analyze-button`, `#result-panel`, and the JSON response shape),
    .planning/phases/28-baseline-readiness-zero-code-diagnostics/28-RESEARCH.md (sections "Concrete Mechanics: vnphish demo + browser-driven verification", "Code Examples", "Don't Hand-Roll", "Common Pitfalls #1, #4")
  </read_first>
  <files>scripts/verify_golden_prompts.py</files>
  <action>
    Create `scripts/verify_golden_prompts.py`, adapting the exact pattern already provided in RESEARCH.md's Code Examples section (built from the `ncsc_scraper.py` `sync_playwright` analog plus the `demo.js`/`test_demo.py` DOM+JSON contract). Requirements for this script:

    - Import `from playwright.sync_api import sync_playwright` lazily inside the function that uses it (matching the project's existing convention in `ncsc_scraper.py`), not at module top-level.
    - Expose an `argparse` CLI with: `--scam-text` (default: the TPBank bank-impersonation candidate validated in Task 1), `--scam-channel` (default `sms`), `--benign-text` (default: the `sample_benign_message` fixture text), `--benign-channel` (default `sms`), `--port` (default `8765`), `--runs` (default `5`). This lets Task 3 retry with a different candidate via CLI args if a candidate fails stability, without editing this file — per D-07, a failing candidate is rejected and swapped, not root-caused.
    - Launch `vnphish demo --no-browser --port <port>` as a script-owned `subprocess.Popen([...])` (never a pattern-based `taskkill`/process-name kill). Wrap the whole run in `try/finally` so the owned process handle is always `.terminate()`-d (with a `.wait(timeout=10)` then `.kill()` fallback if it does not exit cleanly) even if verification fails or raises.
    - Detect server readiness by retrying `page.goto(DEMO_URL, timeout=...)` in a small bounded loop (e.g., up to 30 attempts with a short sleep between attempts), per Pitfall #4 — do NOT parse the subprocess's stdout for a "ready" string, since Python's stdout buffering does not reliably surface prints from a backgrounded process on this Windows/Git-Bash setup. Raise a clear `RuntimeError` if the server never becomes reachable within the retry budget.
    - For each of the two candidates (scam, benign), reuse one Playwright `page` across `--runs` repeated submissions: `page.fill("#message-input", text)`, `page.select_option("#channel-select", channel)`, then `with page.expect_response(lambda r: "/api/analyze" in r.url): page.click("#analyze-button")` — capture `response.json()` and `response.request.timing` per run (this is the exact pattern already given in RESEARCH.md's Code Examples).
    - After all runs for a candidate, compute the set of distinct `(risk_tier, tuple(threat_labels))` verdicts observed. The candidate is "stable" only if that set has exactly one element AND that element is the expected correct verdict (non-benign + correct in-scope label for the scam candidate; `("benign", ("benign",))` for the benign candidate). **Fail loudly** on any instability or wrong verdict — print full per-run diagnostics and either `raise` or `sys.exit(1)`. Do NOT copy `ncsc_scraper.py`'s `except Exception: return None` swallowing pattern here; add a one-line comment in the script explaining why this script deliberately diverges from that analog (verification must surface failures, not hide them).
    - Write a JSON results file to `.planning/phases/28-baseline-readiness-zero-code-diagnostics/artifacts/28-golden-prompt-results.json` with the shape: `{"golden_scam": {"text": ..., "channel": ..., "runs": [{"risk_tier":..., "threat_labels":[...], "latency_ms":...}, ...], "stable": true|false}, "golden_benign": {...same shape...}, "recorded": "<ISO-8601 timestamp>"}`.
    - Script exits 0 only if both candidates are stable+correct; exits 1 otherwise (with the JSON still written, `stable: false` for whichever candidate failed, so Task 3 can inspect what happened).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('scripts/verify_golden_prompts.py', encoding='utf-8').read())" && python scripts/verify_golden_prompts.py --help</automated>
  </verify>
  <done>
    `scripts/verify_golden_prompts.py` exists, parses as valid Python, and its `--help` output confirms an argparse CLI exposing `--scam-text`, `--benign-text`, `--scam-channel`, `--benign-channel`, `--port`, and `--runs`. The script launches and owns its own `vnphish demo` subprocess, detects readiness via `page.goto` retries (not stdout parsing), fails loudly (nonzero exit / raised exception) on any instability instead of swallowing it, and writes the JSON results artifact on every run (pass or fail).
  </done>
  <acceptance_criteria>
    - [ ] Lazy `sync_playwright` import inside a function, matching `ncsc_scraper.py` convention
    - [ ] argparse CLI with scam/benign text+channel overrides, port, and run-count options
    - [ ] Script-owned `subprocess.Popen` for `vnphish demo`, terminated in a `finally` block
    - [ ] Readiness detection via bounded `page.goto` retry loop, not stdout string matching
    - [ ] No swallowed exceptions — instability/wrong verdicts raise or exit nonzero with diagnostics printed
    - [ ] JSON results artifact written on every run, matching the documented shape
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Lock GOLD-01/GOLD-02 golden prompts, capture DIAG-03 latency, finalize results record</name>
  <read_first>
    scripts/verify_golden_prompts.py (from Task 2),
    .planning/phases/28-baseline-readiness-zero-code-diagnostics/artifacts/28-RESULTS.md (from Task 1),
    .planning/phases/28-baseline-readiness-zero-code-diagnostics/28-RESEARCH.md (section "Sourcing the Golden Benign Prompt (D-04)", "Open Questions" #1)
  </read_first>
  <files>
    .planning/phases/28-baseline-readiness-zero-code-diagnostics/artifacts/28-golden-prompt-results.json,
    .planning/phases/28-baseline-readiness-zero-code-diagnostics/artifacts/28-RESULTS.md
  </files>
  <action>
    Run `python scripts/verify_golden_prompts.py` with its defaults (TPBank bank-impersonation scam text validated in Task 1 as the D-02/D-03 candidate; the `sample_benign_message` fixture as the D-04 candidate). Per D-07: if either candidate comes back unstable or incorrect, reject it — do not investigate the root cause — and re-run the script with `--scam-text`/`--scam-channel` or `--benign-text`/`--benign-channel` pointing at an alternate candidate (for the scam side, pull another `bank_impersonation` row from `data/splits/recovered-balanced/val.jsonl`, per the filter recipe in RESEARCH.md; for the benign side, prefer a non-bank-themed `label: benign` row from the same file only if the primary conftest fixture somehow fails). Repeat until both candidates report `stable: true` in the JSON output. Per RESEARCH.md's Open Question #1: a `suspicious` risk tier with the correct `bank_impersonation` label counts as an acceptable "correct verdict" for locking — do not require `high-risk` specifically unless a stable `high-risk` candidate is found with no extra effort.

    Once both candidates are locked, read `.planning/phases/28-baseline-readiness-zero-code-diagnostics/artifacts/28-golden-prompt-results.json` and extract the `latency_ms` value from the first run of either candidate as the DIAG-03 "first-pass warm-latency reading" (Playwright's `request.timing`-derived figure is the same Resource-Timing-API data DevTools' Network tab displays, per RESEARCH.md's explicit recommendation — this satisfies DIAG-03 without a separate manual DevTools pass).

    Append two final sections to `.planning/phases/28-baseline-readiness-zero-code-diagnostics/artifacts/28-RESULTS.md`:
    - `## GOLD-01 / GOLD-02: Locked golden prompts` — the exact final locked scam text and benign text (verbatim), the channel used for each, a per-run table (5 rows each: risk_tier, threat_labels, latency_ms), and `STABLE=true` confirmation for both.
    - `## DIAG-03: Warm-latency reading` — the recorded first-pass latency figure in milliseconds, the method used (Playwright `request.timing`, equivalent to the DevTools Network tab's Resource Timing data), and the date recorded, explicitly noted as the baseline figure for later comparison in Phase 30.
  </action>
  <verify>
    <automated>python -c "import json,sys; d=json.load(open('.planning/phases/28-baseline-readiness-zero-code-diagnostics/artifacts/28-golden-prompt-results.json', encoding='utf-8')); sys.exit(0 if d['golden_scam']['stable'] and d['golden_benign']['stable'] else 1)"</automated>
  </verify>
  <done>
    `28-golden-prompt-results.json` shows `stable: true` for both `golden_scam` and `golden_benign`, each with exactly 5 recorded runs sharing the same correct `(risk_tier, threat_labels)` verdict. `28-RESULTS.md` contains the final locked prompt texts, their 5-run stability tables, and one recorded DIAG-03 warm-latency figure in milliseconds.
  </done>
  <acceptance_criteria>
    - [ ] Golden scam prompt locked: bank_impersonation label, 5/5 identical correct verdicts
    - [ ] Golden benign prompt locked: benign label, 5/5 identical correct verdicts
    - [ ] Any candidate rejection/substitution during locking is documented, with no root-cause investigation performed (per D-07)
    - [ ] One DIAG-03 warm-latency figure (ms) recorded with method and date
    - [ ] `28-RESULTS.md` fully consolidates DIAG-01, DIAG-02, DIAG-03, GOLD-01, GOLD-02 results in one place
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright script -> loopback `vnphish demo` server | Script-driven browser traffic to a `127.0.0.1`-bound WSGI server; same trust level as a human manually using the demo locally — no new network surface |
| Verification script -> owned OS subprocess | The script spawns and must exclusively manage a local `vnphish demo` process for its own lifetime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Information Disclosure | Golden/DIAG-02 candidate message text (bank/OTP/Zalo-style synthetic messages) | accept | All candidates are sourced from already-committed synthetic fixtures/dataset rows (project's DATA-04 100%-synthetic posture); placeholder OTP codes and masked phone numbers only, no real PII — confirmed directly in RESEARCH.md |
| T-28-02 | Elevation of Privilege | `vnphish demo` server bound by the verification script | accept | `demo.py` binds `127.0.0.1` only (confirmed in RESEARCH.md by reading source); the script is simply an additional local client on the same loopback interface — no new exposure |
| T-28-03 | Denial of Service (orphaned local process/port) | Script-owned `subprocess.Popen` running `vnphish demo` | mitigate | Task 2's script must `.terminate()` (with `.wait()`/`.kill()` fallback) its own owned process handle in a `finally` block; never use a pattern-based `taskkill`/process-name kill, which risks matching unrelated processes on the same machine (already observed as a real risk in this project's own research session) |
| T-28-04 | Tampering (silent failure masking) | Verification script's exception handling around candidate stability checks | mitigate | Task 2's script must NOT copy `ncsc_scraper.py`'s `except Exception: return None` swallowing pattern; any instability or wrong verdict must raise/exit nonzero with full diagnostics printed, so a broken golden prompt can never be silently treated as "locked" |
| T-28-SC | Tampering (supply chain) | npm/pip/cargo installs | n/a | Not applicable this phase — zero new packages installed; `playwright` and `llama-cpp-python` are pre-existing, already-audited project dependencies exercised as-is. Package Legitimacy Gate skipped per RESEARCH.md's own scope note |

This phase introduces no new production code, no new endpoints, and no new stored data. Its security posture is identical to the already-verified frozen backend it exercises (`src/runtime/**` is read-only this phase); the only new attack-surface-relevant addition is a throwaway local verification script whose own risks (T-28-03, T-28-04) are fully mitigated by process-ownership and fail-loud discipline.
</threat_model>

<verification>
1. `vnphish doctor` exits 0 with `READY backend=gguf local_only=True text_only=True` — DIAG-01 satisfied.
2. All 4 DIAG-02 CLI candidates (bank_impersonation, zalo_social_engineering, task_scam, benign) classify correctly, recorded in `28-RESULTS.md`.
3. `scripts/verify_golden_prompts.py` exists, is syntactically valid, exposes the documented argparse CLI, and fails loudly (never swallows) on instability.
4. `28-golden-prompt-results.json` shows `stable: true` for both `golden_scam` and `golden_benign` across 5 runs each — GOLD-01 and GOLD-02 satisfied.
5. `28-RESULTS.md` records a DIAG-03 first-pass warm-latency figure in milliseconds with method and date.
6. No file under `src/runtime/`, `src/data_pipeline/`, or `src/config/` was modified — `git status` / `git diff --stat` shows only the 3 new files listed in `files_modified` plus the generated `28-01-SUMMARY.md`.
</verification>

<success_criteria>
- `vnphish doctor` and all 4 `vnphish analyze` DIAG-02 candidates pass on the dev machine (DIAG-01, DIAG-02).
- Exactly one bank-impersonation scam prompt and one benign prompt are locked, each proven correct across 5 identical runs through the real web demo (GOLD-01, GOLD-02).
- One warm-latency figure is captured and recorded for later comparison in Phase 30 (DIAG-03).
- Zero changes to `src/runtime/**` or any other production/backend file.
</success_criteria>

<output>
Create `.planning/phases/28-baseline-readiness-zero-code-diagnostics/28-01-SUMMARY.md` when done
</output>
