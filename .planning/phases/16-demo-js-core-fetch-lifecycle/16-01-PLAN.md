---
phase: 16-demo-js-core-fetch-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/runtime/demo_assets/demo.js
autonomous: true
requirements:
  - CHAT-01
  - CHAT-02
  - CHAT-03
  - CHAT-04
  - INPUT-01
  - INPUT-02
  - INPUT-03
  - INPUT-04

must_haves:
  truths:
    - "Submitting the form appends a right-aligned user bubble and clears the textarea"
    - "Typing indicator (animated dots) appears after user bubble during inference and is removed when response arrives"
    - "Bot bubble renders risk tier badge (with I18N label + data-riskTier), summary as verdict, labels, backend, grounded cues, safe next steps from API response"
    - "Error bubble renders when fetch fails or API returns non-200"
    - "AbortController cancels in-flight request before each new submit"
    - "In-memory history[] accumulates {text, channel} objects; no localStorage"
    - "Plain Enter submits; Shift+Enter inserts newline"
    - "Send button disabled and shows ANALYZE_BTN_BUSY text during in-flight"
    - "rAF scroll anchors thread to bottom after each bubble append"
    - "pytest tests/runtime/test_demo.py passes with zero failures"
  artifacts:
    - path: "src/runtime/demo_assets/demo.js"
      provides: "complete chat fetch lifecycle rewrite"
      contains: "AbortController"
  key_links:
    - from: "src/runtime/demo_assets/demo.js"
      to: "src/runtime/demo_assets/index.html"
      via: "querySelector('[data-slot=...]') + data-i18n injection at clone time"
      pattern: "data-slot"
---

<objective>
Phase 16 rewrites src/runtime/demo_assets/demo.js to deliver the complete end-to-end chat
interaction. The Phase 14 HTML scaffold and Phase 15 i18n.js string table are already in place.
demo.js is currently broken (uses old inner-ID selectors removed in Phase 14). This phase
replaces it with a clean implementation that uses data-slot selectors, window.I18N, and
all the interaction behaviors listed in the success criteria.

Purpose: Make the demo app actually work as a chat — user types, bot replies, errors are handled,
typing state is visible for the full 5–30 s inference window, and history accumulates.
</objective>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite src/runtime/demo_assets/demo.js</name>
  <files>
    src/runtime/demo_assets/demo.js
  </files>

  <read_first>
    - src/runtime/demo_assets/demo.js — current file (understand what to replace)
    - src/runtime/demo_assets/index.html — confirm all data-slot and data-i18n attribute names in templates
    - src/runtime/demo_assets/i18n.js — confirm exact key names for RISK_*, ANALYZE_BTN*, ERR_*, LIST_EMPTY, USER_META, RESULT_META, etc.
    - .planning/phases/16-demo-js-core-fetch-lifecycle/16-CONTEXT.md — all locked decisions
  </read_first>

  <behavior>
    - Submitting form with non-empty textarea: user bubble appears right-aligned, textarea clears
    - Typing bubble appears immediately after user bubble, removed when fetch completes
    - Successful fetch: bot bubble appended with risk tier (data-riskTier attr), verdict, labels, backend, cues, steps
    - Failed fetch or non-200: error bubble appended with ERR_NETWORK message and ERR_NETWORK_STEP step
    - AbortError (from cancelled in-flight request) is swallowed silently
    - Send button disabled + shows ANALYZE_BTN_BUSY during in-flight, re-enabled in finally
    - history[] gains one {text, channel} entry per submit
    - Shift+Enter inserts newline; plain Enter calls form.requestSubmit()
    - Thread scrolls to bottom after each append (rAF)
    - result-panel--empty class removed on first submit
  </behavior>

  <action>
    Write src/runtime/demo_assets/demo.js as a complete replacement. The file must contain:

    SECTION 1 — DOM refs (top of file, identical IDs to Phase 14 contract):
      const form = document.getElementById('analysis-form');
      const messageInput = document.getElementById('message-input');
      const channelSelect = document.getElementById('channel-select');
      const analyzeButton = document.getElementById('analyze-button');
      const sampleButton = document.getElementById('sample-button');
      const resultPanel = document.getElementById('result-panel');
      const userMsgTpl = document.getElementById('user-message-template');
      const resultTpl = document.getElementById('result-template');
      const typingTpl = document.getElementById('typing-template');
      const errorTpl = document.getElementById('error-template');

    SECTION 2 — Module state:
      const history = [];
      let currentController = null;
      const sampleText = "VPBank cảnh báo account Internet Banking của bạn sẽ bị khóa trong 24h. Không chia sẻ mã OTP hoặc Smart OTP và không bấm vào link đăng nhập https://vpbank-secure.example để xác minh ngay.";

    SECTION 3 — Helper: applyI18nToClone(frag)
      A small function that, given a cloned template fragment, applies window.I18N values to all
      `[data-i18n]` elements inside it via textContent. Also handles the special case of
      `.typing-dots[data-i18n-aria]` by setting its aria-label. Use textContent only, never innerHTML.
      Guard with `if (!window.I18N) return;`

    SECTION 4 — Helper: createListItems(listNode, values)
      Same as current, but empty fallback uses `window.I18N?.LIST_EMPTY ?? 'Không có'`.

    SECTION 5 — Helper: scrollToBottom()
      requestAnimationFrame(() => { resultPanel.scrollTop = resultPanel.scrollHeight; });

    SECTION 6 — Helper: setBusyState(isBusy)
      analyzeButton.disabled = isBusy;
      analyzeButton.textContent = window.I18N
        ? (isBusy ? window.I18N.ANALYZE_BTN_BUSY : window.I18N.ANALYZE_BTN)
        : (isBusy ? 'Đang phân tích...' : 'Phân tích tại máy');

    SECTION 7 — appendUserBubble(text)
      - Clone userMsgTpl.content with cloneNode(true).firstElementChild
        (firstElementChild gives the <article> directly, not the fragment wrapper)
      - Set [data-slot="text"].textContent = text
      - applyI18nToClone(article) — applies USER_META
      - resultPanel.classList.remove('result-panel--empty')
      - resultPanel.append(article)
      - scrollToBottom()

    SECTION 8 — appendTypingBubble() → returns article reference
      - Clone typingTpl.content.cloneNode(true).firstElementChild
      - applyI18nToClone(article) — applies TYPING_META + TYPING_ARIA aria-label
      - resultPanel.append(article)
      - scrollToBottom()
      - return article

    SECTION 9 — appendResultBubble(result)
      - Clone resultTpl.content.cloneNode(true).firstElementChild
      - applyI18nToClone(article) — applies RESULT_META, RESULT_DT_LABELS, RESULT_DT_BACKEND, CUES_HEADING, STEPS_HEADING
      - [data-slot="verdict"].textContent = result.summary
      - const riskPill = article.querySelector('[data-slot="risk-tier"]')
        riskPill.textContent = riskLabel map: { benign: I18N.RISK_BENIGN, suspicious: I18N.RISK_SUSPICIOUS, 'high-risk': I18N.RISK_HIGH } ?? result.risk_tier
        riskPill.dataset.riskTier = result.risk_tier
      - [data-slot="labels"].textContent = result.threat_labels.length
          ? result.threat_labels.join(', ')
          : (window.I18N?.LIST_EMPTY ?? 'Không có')
      - [data-slot="backend"].textContent = result.backend_name
      - createListItems([data-slot="grounded-cues"], result.top_cues.map(c => `"${c.span}" — ${c.reason}`))
      - createListItems([data-slot="recommendations"], result.recommendations)
      - resultPanel.append(article)
      - scrollToBottom()

    SECTION 10 — appendErrorBubble(error)
      - Clone errorTpl.content.cloneNode(true).firstElementChild
      - applyI18nToClone(article) — applies ERROR_META, ERROR_STEPS_HEADING
      - [data-slot="message"].textContent = error.message
      - createListItems([data-slot="steps"], error.steps || [])
      - resultPanel.append(article)
      - scrollToBottom()

    SECTION 11 — async function analyzeMessage(event)
      - event.preventDefault()
      - const text = messageInput.value.trim(); if (!text) return;
      - const channel = channelSelect.value;
      - Abort in-flight: if (currentController) currentController.abort(); currentController = new AbortController();
      - history.push({ text, channel });
      - appendUserBubble(text);
      - messageInput.value = '';
      - setBusyState(true);
      - const typingEl = appendTypingBubble();
      - try:
          const response = await fetch('/api/analyze', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ text, channel }),
            signal: currentController.signal
          });
          const payload = await response.json();
          typingEl.remove();
          if (!response.ok) {
            appendErrorBubble(payload.error || { message: window.I18N?.ERR_NETWORK ?? '...', steps: [] });
            return;
          }
          appendResultBubble(payload);
        catch (err):
          typingEl.remove();
          if (err.name !== 'AbortError') {
            appendErrorBubble({
              message: window.I18N?.ERR_NETWORK ?? 'Không thể kết nối với runtime cục bộ.',
              steps: [window.I18N?.ERR_NETWORK_STEP ?? 'Thử lại sau khi runtime cục bộ đã tải xong.']
            });
          }
        finally:
          setBusyState(false);
          currentController = null;

    SECTION 12 — Event listeners
      - form.addEventListener('submit', analyzeMessage);
      - messageInput.addEventListener('keydown', (event) => {
          if (event.key === 'Enter' && !event.shiftKey) {
            event.preventDefault();
            form.requestSubmit();
          }
        });
      - sampleButton.addEventListener('click', () => {
          messageInput.value = sampleText;
          channelSelect.value = 'sms';
          messageInput.focus();
        });

    CRITICAL CONSTRAINTS:
    - NEVER use innerHTML anywhere — XSS risk. Only textContent and setAttribute.
    - NEVER use template inner IDs like #result-summary, #result-risk-tier — these were removed in Phase 14.
    - Always use querySelector('[data-slot="..."]') or querySelector('[data-i18n="..."]') inside cloned elements.
    - The typing bubble must be removable by calling typingEl.remove() directly — so use firstElementChild pattern.
    - No new imports. No ES module syntax (no export/import/type).
    - Script is deferred; all DOM access is safe at script evaluation time.
  </action>

  <verify>
    <automated>cd "C:\Users\wikiepeidia\OneDrive - caugiay.edu.vn\bài tập\usth\GEN14\INTERNSHIP\Internship-project" && python -m pytest tests/runtime/test_demo.py -v 2>&amp;1</automated>
  </verify>

  <done>
    - src/runtime/demo_assets/demo.js is a complete rewrite with no references to old inner IDs
    - All 8 success criteria from ROADMAP Phase 16 are implemented
    - pytest tests/runtime/test_demo.py passes 5/5 (no regressions from the JS changes)
    - No innerHTML usage anywhere in demo.js
    - AbortController present at module level
    - history[] array present at module level
    - window.I18N keys used for all user-facing strings
  </done>
</task>

</tasks>

<verification>
1. pytest tests/runtime/test_demo.py -v must show 5 passed, 0 failed
2. Manual: open browser, submit a short phishing message → user bubble + typing dots → bot reply renders
3. Manual: submit while previous request pending → previous cancelled, new one starts cleanly
4. Manual: Shift+Enter inserts newline; plain Enter submits
5. Manual: send button shows "Đang phân tích..." during request, re-enables on completion
6. Manual: two successive submits accumulate in thread (no clear between them)
</verification>

<success_criteria>
1. User bubble appended to thread, textarea cleared, on submit — CHAT-01
2. Typing indicator visible during inference window — CHAT-02
3. Bot bubble renders all fields (risk tier, verdict, labels, backend, cues, steps) — CHAT-03
4. Error bubble renders on fetch failure or non-200 — CHAT-04
5. Shift+Enter = newline, plain Enter = submit — INPUT-01
6. Channel value included in POST body — INPUT-02
7. Send button disabled + busy text during in-flight — INPUT-03
8. history[] accumulates, no localStorage — INPUT-04
</success_criteria>

<output>
Create `.planning/phases/16-demo-js-core-fetch-lifecycle/16-01-SUMMARY.md` when done.
</output>
