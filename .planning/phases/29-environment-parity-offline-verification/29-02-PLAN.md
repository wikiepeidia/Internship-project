---
phase: 29-environment-parity-offline-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .planning/phases/29-environment-parity-offline-verification/artifacts/29-env01-env05-evidence.md
autonomous: true
requirements: [ENV-01, ENV-05]

must_haves:
  truths:
    - "vnphish doctor reports READY with backend=gguf, local_only=True, text_only=True on this machine, matching Phase 28's DIAG-01 baseline"
    - "llama-cpp-python is exact-pinned in pyproject.toml so a future install cannot silently drift past 0.3.23"
  artifacts:
    - path: "pyproject.toml"
      provides: "exact llama-cpp-python version pin"
      contains: "llama-cpp-python==0.3.23"
    - path: ".planning/phases/29-environment-parity-offline-verification/artifacts/29-env01-env05-evidence.md"
      provides: "ENV-01 doctor re-check output and ENV-05 pin/installed-version confirmation record"
  key_links:
    - from: "pyproject.toml"
      to: "pip dependency resolver"
      via: "exact == version specifier in the runtime optional-dependencies list"
      pattern: "llama-cpp-python==0\\.3\\.23"
---

<objective>
Perform the two lightest-weight ENV checks in this phase: reconfirm `vnphish doctor` still reports READY on this machine (ENV-01, narrowed by CONTEXT.md D-01/D-02 to a sanity re-check, not a fresh install), and tighten `llama-cpp-python`'s version constraint in `pyproject.toml` from an open-ended `>=0.3` to an exact `==0.3.23` pin (ENV-05).

Purpose: ENV-01 confirms Phase 28's DIAG-01 result still holds before other Phase 29 changes land. ENV-05 guarantees any future install (this machine or otherwise) resolves to the exact version the project's validated latency/behavior was measured against, rather than silently drifting to a newer, unvalidated PyPI release (`0.3.32` is currently latest).
Output: an updated `pyproject.toml` pin and a written evidence record of both checks. No source/runtime code changes; no reinstall.
</objective>

<execution_context>
@C:/Users/wikiepeidia/OneDrive - caugiay.edu.vn/bài tập/usth/GEN14/INTERNSHIP/Internship-project/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wikiepeidia/OneDrive - caugiay.edu.vn/bài tập/usth/GEN14/INTERNSHIP/Internship-project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-environment-parity-offline-verification/29-CONTEXT.md
@.planning/phases/29-environment-parity-offline-verification/29-RESEARCH.md
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: ENV-01 sanity re-check — confirm vnphish doctor still reports READY</name>
  <read_first>
    29-RESEARCH.md lines 47-56 (phase_requirements table, ENV-01 row — states the exact expected output already confirmed once this session: "READY backend=gguf local_only=True text_only=True", 11/11 checks PASS) and lines 411-417 (Pitfall 8 — do not over-read ROADMAP's "fresh install" wording as requiring a reinstall; D-01/D-02 already narrowed this to a sanity re-check).
    src/runtime/cli.py lines 77-82 (handle_doctor — prints the report and returns exit code 0 when ready, 1 when not).
    .planning/phases/28-baseline-readiness-zero-code-diagnostics/28-01-SUMMARY.md (Phase 28's DIAG-01 baseline result to compare against).
  </read_first>
  <files>.planning/phases/29-environment-parity-offline-verification/artifacts/29-env01-env05-evidence.md</files>
  <action>
    Run `vnphish doctor` (equivalently `python -m src.runtime.cli doctor`) from the repo root — no fresh install, no new venv, no CWD change (D-02 explicitly scopes ENV-01 to this exact machine's already-proven state). Capture the full printed report. Confirm the header line reads `READY backend=gguf local_only=True text_only=True` and that every listed check shows `PASS`, matching Phase 28's DIAG-01 result. If anything differs (a check now shows FAIL, or the backend/local_only/text_only values changed), stop and flag the discrepancy rather than silently proceeding — do not attempt any remediation as part of this task. Write the captured report verbatim, with a one-line pass/fail summary and today's date, into `.planning/phases/29-environment-parity-offline-verification/artifacts/29-env01-env05-evidence.md` under an "## ENV-01: doctor sanity re-check" heading.
  </action>
  <verify>
    <automated>vnphish doctor; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>`vnphish doctor` exits 0 and prints `READY backend=gguf local_only=True text_only=True` with all checks PASS; the evidence file records this verbatim output.</acceptance_criteria>
  <done>ENV-01 re-check evidence is written and matches Phase 28's DIAG-01 baseline with no regressions.</done>
</task>

<task type="auto">
  <name>Task 2: ENV-05 — exact-pin llama-cpp-python in pyproject.toml</name>
  <read_first>
    29-RESEARCH.md lines 418-428 ("ENV-05 Scope Resolution" — the authoritative two-part interpretation: static pin edit + read-only version confirmation, explicitly no reinstall) and lines 96-109 (Package Legitimacy Audit — llama-cpp-python is a pre-existing dependency, tightening the pin only, no new install surface, no checkpoint gate needed).
    pyproject.toml lines 28-40 (the `[project.optional-dependencies]` block, `runtime` extra).
  </read_first>
  <files>pyproject.toml, .planning/phases/29-environment-parity-offline-verification/artifacts/29-env01-env05-evidence.md</files>
  <action>
    In pyproject.toml's `runtime` optional-dependencies list, change the single entry from `"llama-cpp-python>=0.3"` to `"llama-cpp-python==0.3.23"`. Change nothing else in the file. Do not run `pip install`, `pip uninstall`, `pip install --force-reinstall`, or create/recreate any virtual environment — per D-01/D-02 and 29-RESEARCH.md's explicit "What NOT to do" guidance, a reinstall is not required and would reintroduce exactly the fresh-install risk this milestone declined. After editing the pin, run a read-only confirmation (`pip show llama-cpp-python`) and record its `Version:` line in the evidence file under an "## ENV-05: exact-pin confirmation" heading, alongside the exact diff made to pyproject.toml, to close the letter of ROADMAP's success-criterion wording without an actual reinstall (per 29-RESEARCH.md's "Flag for the planner" note).
  </action>
  <verify>
    <automated>pip show llama-cpp-python | grep -i "^Version: 0.3.23$"</automated>
  </verify>
  <acceptance_criteria>pyproject.toml's runtime extra reads exactly `"llama-cpp-python==0.3.23"`; the currently-installed version (confirmed read-only, no reinstall) is `0.3.23`, matching the new pin; the evidence file records both facts.</acceptance_criteria>
  <done>pyproject.toml exact-pins llama-cpp-python==0.3.23 and the evidence file confirms the installed version already matches, with no reinstall performed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This plan only edits a version constraint string in `pyproject.toml` and re-runs an existing read-only diagnostic (`vnphish doctor`, `pip show`). No request-handling code, network-facing surface, or trust boundary is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03 | Tampering (dependency substitution) | `pyproject.toml` `runtime` extra | accept | `llama-cpp-python` is an existing dependency validated across multiple prior milestones (Phase 3 onward); this plan only tightens `>=0.3` to `==0.3.23` (the already-installed, already-validated version) — no new package is introduced, so the Package Legitimacy Gate does not apply per 29-RESEARCH.md's Package Legitimacy Audit. |
| T-29-04 | Information Disclosure | `29-env01-env05-evidence.md` diagnostic output | accept | `vnphish doctor` output contains only backend/config flags and check names, no secrets; `pip show` output contains only package metadata. Neither command touches `.env/.env` or any API key. |

</threat_model>

<verification>
Run `vnphish doctor` and confirm exit code 0 with `READY backend=gguf local_only=True text_only=True`. Run `pip show llama-cpp-python` and confirm `Version: 0.3.23`. Confirm `pyproject.toml` contains `llama-cpp-python==0.3.23` and no other line changed.
</verification>

<success_criteria>
- `vnphish doctor` reports READY, matching Phase 28's DIAG-01 baseline with no regression.
- `pyproject.toml` exact-pins `llama-cpp-python==0.3.23`.
- No reinstall, venv rebuild, or fresh clone was performed.
</success_criteria>

<output>
Create `.planning/phases/29-environment-parity-offline-verification/29-02-SUMMARY.md` when done
</output>

## Artifacts This Plan Produces

- Updated `pyproject.toml` (`llama-cpp-python==0.3.23` exact pin)
- `.planning/phases/29-environment-parity-offline-verification/artifacts/29-env01-env05-evidence.md` (ENV-01 doctor re-check + ENV-05 pin/version confirmation record)
