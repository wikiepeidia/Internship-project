---
phase: 29-environment-parity-offline-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/runtime/demo.py
  - src/runtime/demo_assets/fonts/be-vietnam-pro-400-vietnamese.woff2
  - src/runtime/demo_assets/fonts/be-vietnam-pro-400-latin-ext.woff2
  - src/runtime/demo_assets/fonts/be-vietnam-pro-400-latin.woff2
  - src/runtime/demo_assets/fonts/be-vietnam-pro-500-vietnamese.woff2
  - src/runtime/demo_assets/fonts/be-vietnam-pro-500-latin-ext.woff2
  - src/runtime/demo_assets/fonts/be-vietnam-pro-500-latin.woff2
  - src/runtime/demo_assets/fonts/be-vietnam-pro-600-vietnamese.woff2
  - src/runtime/demo_assets/fonts/be-vietnam-pro-600-latin-ext.woff2
  - src/runtime/demo_assets/fonts/be-vietnam-pro-600-latin.woff2
  - src/runtime/demo_assets/fonts/be-vietnam-pro-700-vietnamese.woff2
  - src/runtime/demo_assets/fonts/be-vietnam-pro-700-latin-ext.woff2
  - src/runtime/demo_assets/fonts/be-vietnam-pro-700-latin.woff2
  - src/runtime/demo_assets/index.html
  - src/runtime/demo_assets/demo.css
  - tests/runtime/test_demo.py
autonomous: true
requirements: [ENV-03]

must_haves:
  truths:
    - "src/runtime/demo_assets/index.html contains zero references to fonts.googleapis.com or fonts.gstatic.com"
    - "Be Vietnam Pro renders in the demo from self-hosted .woff2 files covering all four weights (400/500/600/700) across vietnamese/latin-ext/latin subsets, matching what the CDN previously served"
    - "Requesting a font filename outside the allowlist (including a path-traversal-style path) returns the same 404 JSON shape as any other unknown route, never file bytes"
  artifacts:
    - path: "src/runtime/demo_assets/fonts/be-vietnam-pro-400-vietnamese.woff2"
      provides: "one of 12 self-hosted Be Vietnam Pro font binaries (400/500/600/700 x vietnamese/latin-ext/latin)"
    - path: "src/runtime/demo.py"
      provides: "allowlisted GET /static/fonts/<filename> route"
      contains: "KNOWN_FONT_FILES"
    - path: "src/runtime/demo_assets/demo.css"
      provides: "self-hosted @font-face declarations for Be Vietnam Pro"
      contains: "@font-face"
    - path: "tests/runtime/test_demo.py"
      provides: "font-route regression coverage (happy path + allowlist rejection)"
      contains: "test_demo_font_assets_are_served"
  key_links:
    - from: "src/runtime/demo_assets/index.html"
      to: "src/runtime/demo_assets/demo.css"
      via: "existing <link rel=\"stylesheet\" href=\"/static/demo.css\"> (unchanged)"
      pattern: "static/demo\\.css"
    - from: "src/runtime/demo.py"
      to: "src/runtime/demo_assets/fonts/*.woff2"
      via: "KNOWN_FONT_FILES allowlist membership check before FONT_DIR / filename read_bytes()"
      pattern: "KNOWN_FONT_FILES"
---

<objective>
Remove the last confirmed offline-claim leak in the local demo: self-host Be Vietnam Pro instead of loading it from the Google Fonts CDN, and add the small allowlisted static-file route the WSGI app needs to actually serve those font bytes (there is currently no generic static-file handler in `src/runtime/demo.py` — every asset path is an exact-match route).

Purpose: ENV-03 requires the demo to work with zero external requests; a leftover `<link>` to `fonts.googleapis.com`/`fonts.gstatic.com` would show up as a failed request the moment network access is cut in Plan 29-04's offline verification, defeating the whole point of that test. This plan must land before 29-04 runs.
Output: 12 vendored `.woff2` files, a new allowlisted `/static/fonts/<filename>` route in `demo.py`, local `@font-face` CSS in `demo.css`, CDN-free `index.html`, and updated/added tests in `tests/runtime/test_demo.py`.
</objective>

<execution_context>
@C:/Users/wikiepeidia/OneDrive - caugiay.edu.vn/bài tập/usth/GEN14/INTERNSHIP/Internship-project/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wikiepeidia/OneDrive - caugiay.edu.vn/bài tập/usth/GEN14/INTERNSHIP/Internship-project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-environment-parity-offline-verification/29-RESEARCH.md
@.planning/phases/29-environment-parity-offline-verification/29-PATTERNS.md
@src/runtime/demo.py
@src/runtime/demo_assets/index.html
@src/runtime/demo_assets/demo.css
@tests/runtime/test_demo.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vendor Be Vietnam Pro font files and add the allowlisted font route</name>
  <read_first>
    29-RESEARCH.md lines 172-247 (Pattern 2: allowlisted route, exact code) and lines 461-479 (Code Examples: fetching the real .woff2 URLs).
    src/runtime/demo.py lines 1-20 (imports, ASSET_DIR) and lines 50-71 (DemoApp.__call__ routing table and the shared 404 fallthrough).
    tests/runtime/test_demo.py lines 18-36 (_call_app harness) and lines 142-156 (test_demo_static_assets_are_served, the closest existing analog).
  </read_first>
  <files>src/runtime/demo.py, src/runtime/demo_assets/fonts/be-vietnam-pro-400-vietnamese.woff2 (+ 11 sibling files), tests/runtime/test_demo.py</files>
  <behavior>
    - Test 1 (happy path): GET /static/fonts/be-vietnam-pro-400-vietnamese.woff2 returns status 200, header Content-Type == "font/woff2", and a non-empty response body.
    - Test 2 (allowlist rejection / path-traversal safety): GET /static/fonts/not-a-real-font.woff2 (an unlisted filename) returns the exact same 404 JSON shape every other unmatched route in this file already returns: {"error": {"message": "Not found", "steps": []}}. No file content is ever read for a filename that is not an exact member of the allowlist.
  </behavior>
  <action>
    Fetch the current Be Vietnam Pro CSS2 response by re-running the curl command in 29-RESEARCH.md's "Fetching the official Be Vietnam Pro .woff2 files" section (modern Chrome User-Agent string, family=Be+Vietnam+Pro:wght@400;500;600;700) — re-fetch at execution time rather than reusing the specific gstatic.com hash URLs already written into 29-RESEARCH.md verbatim, since Google may have rotated the content hash since research was written (29-RESEARCH.md Assumption A2). Parse the response preserving the fixed per-weight ordering documented in 29-RESEARCH.md Pattern 3: each font-weight block (400, then 500, then 600, then 700) lists its three unicode-range subsets in this exact fixed order — vietnamese, latin-ext, latin. Download each of the resulting 12 .woff2 URLs into src/runtime/demo_assets/fonts/, naming each file exactly be-vietnam-pro-{weight}-{subset}.woff2 (weight one of 400/500/600/700, subset one of vietnamese/latin-ext/latin) so the 12 filenames match KNOWN_FONT_FILES byte for byte.
    In src/runtime/demo.py, add three new module-level names alongside the existing ASSET_DIR: FONT_DIR = ASSET_DIR / "fonts", FONT_CONTENT_TYPE = "font/woff2", and KNOWN_FONT_FILES as a frozenset literal containing exactly the 12 filenames above (copy the frozenset verbatim from 29-RESEARCH.md Pattern 2 — do not hand-retype the 12 strings, transcription typos here silently break the route). Inside DemoApp.__call__, insert one new GET branch immediately before the final `return _json_response(start_response, "404 Not Found", {"error": {"message": "Not found", "steps": []}})` fallthrough line: match `method == "GET" and path.startswith("/static/fonts/")`, extract `filename = path.removeprefix("/static/fonts/")`, and when `filename in KNOWN_FONT_FILES` return `_text_response(start_response, "200 OK", FONT_CONTENT_TYPE, (FONT_DIR / filename).read_bytes())`; otherwise fall through to the exact same shared 404 JSON response every other unmatched path in this file already returns (do not write a bespoke error shape for this branch). Do not build a generic path-join/catch-all handler, and do not add any `Path.resolve()` or ".."-stripping logic — the allowlist membership check performed strictly before any `Path` construction is the entire mitigation (29-RESEARCH.md Pitfall 7); a filename that fails the membership check must never reach `(FONT_DIR / filename).read_bytes()`.
    Add the two new tests described in <behavior> to tests/runtime/test_demo.py, following test_demo_static_assets_are_served's structure exactly (build the app with `demo_module.build_demo_app(service=object())`, call `_call_app`, assert on status/headers/body — the font route never touches `self.service`, so the same bare `object()` stand-in is correct here too).
  </action>
  <verify>
    <automated>pytest tests/runtime/test_demo.py -k font -x</automated>
  </verify>
  <acceptance_criteria>All 12 .woff2 files exist under src/runtime/demo_assets/fonts/ with the exact KNOWN_FONT_FILES names; the new route serves each with 200 + font/woff2 + non-empty body; a request for any filename outside the allowlist (including an unlisted or path-traversal-shaped filename) returns the standard 404 JSON error shape, never file bytes; both new tests pass.</acceptance_criteria>
  <done>src/runtime/demo.py exports FONT_DIR/FONT_CONTENT_TYPE/KNOWN_FONT_FILES and routes GET /static/fonts/&lt;filename&gt; through the allowlist; test_demo_font_assets_are_served and its allowlist-rejection counterpart both pass.</done>
</task>

<task type="auto">
  <name>Task 2: Remove CDN font links, add self-hosted @font-face rules, and fix the stale index-page assertion</name>
  <read_first>
    src/runtime/demo_assets/index.html lines 1-15 (head block — which lines to delete vs. preserve).
    src/runtime/demo_assets/demo.css lines 1-40 (insertion point above :root, and the untouched font-family consumer on line 37).
    tests/runtime/test_demo.py lines 39-70 (test_demo_index_serves_text_only_form — line 48 is the assertion that breaks once the CDN link is removed) and lines 142-156 (test_demo_static_assets_are_served, which already checks demo.css for "Be Vietnam Pro" and needs no change).
    29-RESEARCH.md lines 220-326 (Pattern 3: the full 12-block @font-face CSS to insert verbatim) and lines 397-402 (Pitfall 6: the demo.js .example bait URL is a confirmed non-issue, not something to fix).
    29-PATTERNS.md lines 227-233 (explicit flag on the stale test_demo_index_serves_text_only_form assertion and the recommended fix direction).
  </read_first>
  <files>src/runtime/demo_assets/index.html, src/runtime/demo_assets/demo.css, tests/runtime/test_demo.py</files>
  <action>
    In src/runtime/demo_assets/index.html, delete the two `&lt;link rel="preconnect" ...&gt;` lines and the multi-line `&lt;link href="https://fonts.googleapis.com/css2?family=Be+Vietnam+Pro..." rel="stylesheet"&gt;` element (currently lines 8-12) — leave the very next `&lt;link rel="stylesheet" href="/static/demo.css"&gt;` and `&lt;script src="/static/i18n.js"&gt;&lt;/script&gt;` lines completely untouched; no new `&lt;link&gt;`/`&lt;script&gt;` tag needs to be added to this file at all, since the `@font-face` rules live in the already-linked demo.css.
    In src/runtime/demo_assets/demo.css, insert the full 12-block `@font-face` CSS from 29-RESEARCH.md Pattern 3 verbatim, above the existing `:root { ... }` block at the top of the file, with each `src: url(...)` pointing at `/static/fonts/&lt;matching-filename-from-Task-1&gt;` — do not alter the existing `font-family: "Be Vietnam Pro", "Segoe UI Variable Display", system-ui, sans-serif;` declaration on line 37; it resolves the new local `@font-face` sources automatically by name.
    In tests/runtime/test_demo.py, inside test_demo_index_serves_text_only_form, replace the now-invalid line-48 assertion (`assert "Be+Vietnam+Pro" in html or "Be Vietnam Pro" in html`) with two negative assertions: `assert "fonts.googleapis.com" not in html` and `assert "fonts.gstatic.com" not in html`. This both fixes the assertion that would otherwise fail once the CDN link is gone, and converts it into real regression coverage for the CDN removal (the positive "Be Vietnam Pro" text check already exists separately in test_demo_static_assets_are_served against demo.css, so no coverage is lost).
    Run a grep sweep across src/runtime/demo_assets/ for `http://` and `https://` references. Confirm the only remaining hit is `https://vpbank-secure.example` inside demo.js's sample phishing text — an IANA-reserved, never-resolving `.example` domain used as realistic scam-bait content, not a live network dependency (29-RESEARCH.md Pitfall 6). Do not remove or alter that sample text; record in the task's completion note that this hit was reviewed and is a confirmed non-issue, not silently ignored.
  </action>
  <verify>
    <automated>pytest tests/runtime/test_demo.py -x</automated>
  </verify>
  <acceptance_criteria>index.html contains zero fonts.googleapis.com/fonts.gstatic.com references; demo.css contains exactly 12 @font-face rules covering weights 400/500/600/700 across vietnamese/latin-ext/latin subsets; the full tests/runtime/test_demo.py suite passes; the grep sweep for http(s):// across src/runtime/demo_assets/ returns exactly one hit (the documented demo.js bait URL) and zero index.html hits.</acceptance_criteria>
  <done>Google Fonts CDN is fully removed from index.html; Be Vietnam Pro renders from self-hosted @font-face rules in demo.css; the stale test assertion is replaced with CDN-absence checks; grep sweep result is documented, not silently skipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser (client) -> `DemoApp.__call__` (WSGI, loopback-only `127.0.0.1`) | `PATH_INFO` is request-derived input; treated as untrusted even though the server is currently loopback-only, since this phase adds the first route in this file that constructs a filesystem path from a request-derived string. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Tampering / Information Disclosure | New `/static/fonts/<filename>` route in `src/runtime/demo.py` | mitigate | Exact-filename `frozenset` allowlist (`KNOWN_FONT_FILES`) checked via membership test strictly before any `Path` construction; an unlisted or path-traversal-shaped filename (`../../pyproject.toml`, etc.) never reaches `(FONT_DIR / filename).read_bytes()` — it falls through to the same generic 404 JSON response every other unmatched route returns. Verified by Task 1's allowlist-rejection test. |
| T-29-02 | Information Disclosure | Existing exact-match routes (`/`, `/static/demo.css`, `/static/demo.js`, `/static/i18n.js`) | accept | Unchanged by this phase; each route's argument to `_load_asset` is always a hardcoded literal string, never derived from request input — no new risk introduced here. |
| T-29-SC | Tampering (supply chain) | Vendored `.woff2` binaries fetched from `fonts.gstatic.com` | accept | No package-manager install involved (static binary assets, not npm/pip packages); source is Google's own CDN host, fetched directly per D-04, not a third-party mirror. Package Legitimacy Gate is not applicable — no new package dependency is introduced by this plan. |

</threat_model>

<verification>
Run `pytest tests/runtime/test_demo.py -x` and confirm all tests pass, including the two new font-route tests and the corrected index-page assertion. Run the documented grep sweep (`grep -rn "http://\|https://" src/runtime/demo_assets/`) and confirm exactly one hit remains (the `demo.js` `.example` bait URL).
</verification>

<success_criteria>
- `src/runtime/demo_assets/index.html` contains no Google Fonts CDN reference.
- All 12 self-hosted `.woff2` files exist and are served by the new allowlisted route with `Content-Type: font/woff2`.
- An unlisted/path-traversal filename request returns 404 with no file content ever read outside the allowlist.
- `tests/runtime/test_demo.py` passes in full.
</success_criteria>

<output>
Create `.planning/phases/29-environment-parity-offline-verification/29-01-SUMMARY.md` when done
</output>

## Artifacts This Plan Produces

- `src/runtime/demo_assets/fonts/be-vietnam-pro-{400,500,600,700}-{vietnamese,latin-ext,latin}.woff2` (12 new binary files)
- `FONT_DIR`, `FONT_CONTENT_TYPE`, `KNOWN_FONT_FILES` module-level names in `src/runtime/demo.py`
- New allowlisted `GET /static/fonts/<filename>` branch inside `DemoApp.__call__`
- `test_demo_font_assets_are_served` and an allowlist-rejection test in `tests/runtime/test_demo.py`
- Updated `@font-face` block (12 rules) in `src/runtime/demo_assets/demo.css`
- Updated `src/runtime/demo_assets/index.html` (CDN `<link>` tags removed)
- Updated `test_demo_index_serves_text_only_form` assertion in `tests/runtime/test_demo.py`
