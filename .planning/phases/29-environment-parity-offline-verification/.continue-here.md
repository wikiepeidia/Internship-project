# Continue Here: Phase 29 Plan 04 Offline Verification

**Status:** Paused at Task 2 human offline network-disconnect verification.
**Completed before pause:** Task 1 pre-flight and runbook preparation.
**Task 1 commit:** `14cfe37` (`docs(29-04): prepare offline verification runbook`)

## Ready Artifact

Follow this runbook exactly:

`.planning/phases/29-environment-parity-offline-verification/artifacts/29-env02-runbook.md`

It contains the exact locked Phase 28 scam and benign prompts, both copied from:

`.planning/phases/28-baseline-readiness-zero-code-diagnostics/artifacts/28-golden-prompt-results.json`

## Awaiting Human Result

Resume only after the human has physically disabled Wi-Fi, Ethernet, and Ethernet 2 through Windows Settings, confirmed the VPN/VMware virtual adapters have no internet path, run both locked prompts through the browser demo, checked DevTools Network, saved the screenshot, and re-enabled the adapters.

Reply with `offline-verified` plus:

1. Scam prompt exact rendered risk tier and threat labels.
2. Benign prompt exact rendered risk tier and threat labels.
3. DevTools confirmation: no non-`127.0.0.1` requests and no failed external entries for `fonts.googleapis.com` or `fonts.gstatic.com`.
4. Screenshot confirmation: `.planning/phases/29-environment-parity-offline-verification/artifacts/29-env02-devtools-screenshot.png` saved.
5. Any deviation, including wrong verdicts, unexpected external requests, or adapters that could not be disabled.

## Next Automated Step After Resume

If the human result is passing:

1. Run `vnphish doctor` again after network restoration and require `READY`.
2. Write `.planning/phases/29-environment-parity-offline-verification/artifacts/29-env02-offline-results.md`.
3. Commit the evidence artifact.
4. Create `29-04-SUMMARY.md`, update STATE/ROADMAP/REQUIREMENTS, and close Phase 29 only if all ENV-02 criteria pass.

If the human result reports a deviation, record it plainly and do not mark ENV-02 complete until the gap is fixed and rerun.

