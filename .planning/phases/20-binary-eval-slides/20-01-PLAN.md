---
phase: 20
plan: "20-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - documents/reports/latex/slides/sections/08_evaluation.tex
  - documents/reports/latex/slides/sections/09_confusion.tex
autonomous: true
requirements:
  - EVAL-04
  - EVAL-05

must_haves:
  truths:
    - "Slide 8 shows a booktabs per-class metrics table (Prec./Recall/F1/n) instead of the bar chart scalebox"
    - "Slide 8 includes a Binary Classification Result block showing Precision 1.000 · Recall 1.000 · F1 1.000"
    - "Slide 8 retains both original bullet points verbatim (Macro F1 = 0.9553, Task Scam recall caveat)"
    - "Slide 9 uses a two-column layout: 4-class matrix left, binary 2x2 matrix right"
    - "Binary 2x2 table cells: Scam/Scam=193 (CVBLUE!15), Non-scam/Non-scam=61 (CVBLUE!15), both off-diagonal=0"
    - "Slide 9 includes a Key Finding block: all misclassifications are intra-scam, binary F1 = 1.000"
    - "Slide 9 retains both original bullet points verbatim (key finding + cause)"
    - "XeLaTeX compilation produces slides.pdf with zero LaTeX errors"
  artifacts:
    - path: "documents/reports/latex/slides/sections/08_evaluation.tex"
      provides: "Per-class metrics table + binary classification result block"
      contains: "booktabs"
    - path: "documents/reports/latex/slides/sections/09_confusion.tex"
      provides: "Two-column 4-class + binary matrix layout"
      contains: "begin{columns}"
  key_links:
    - from: "08_evaluation.tex"
      to: "slides.tex"
      via: "\\input{} in Evaluation section"
      pattern: "input.*08_evaluation"
    - from: "09_confusion.tex"
      to: "slides.tex"
      via: "\\input{} in Evaluation section"
      pattern: "input.*09_confusion"
---

<objective>
Update two evaluation slides in the defense deck to present binary scam/non-scam results derived from the existing 4-class confusion matrix. No model re-run required — all numbers are analytically derived and locked. Replace the bar-chart figure with a rigorous per-class booktabs table (slide 8), and add a side-by-side binary 2×2 confusion matrix (slide 9). Deck must compile clean with XeLaTeX after both edits.

Purpose: Supervisor requested binary evaluation results (scam vs non-scam). The binary result is perfect (F1=1.000) — all 11 misclassifications are within the scam super-class, never crossing the scam/benign boundary. This is a strong defense talking point.

Output: Two edited .tex files producing slides.pdf with zero compile errors.
</objective>

<execution_context>
@C:/Users/wikiepeidia/OneDrive - caugiay.edu.vn/bài tập/usth/GEN14/INTERNSHIP/Internship-project/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wikiepeidia/OneDrive - caugiay.edu.vn/bài tập/usth/GEN14/INTERNSHIP/Internship-project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-binary-eval-slides/20-CONTEXT.md
</context>

<tasks>

<!-- ═══════════════════════════════════════════════════════════════════
     TASK 1 — Update 08_evaluation.tex (EVAL-05 partial)
     Replaces TikZ bar-chart with booktabs per-class table + binary block
     ═══════════════════════════════════════════════════════════════════ -->
<task type="auto" task_id="20-01-T1" req_ids="EVAL-05">
  <name>Task 1: Replace bar chart with per-class metrics table in 08_evaluation.tex</name>
  <files>documents/reports/latex/slides/sections/08_evaluation.tex</files>
  <action>
Completely replace the contents of 08_evaluation.tex with the following. Do not keep the old \scalebox{\input{...}} line — it is fully replaced by the table below.

The frame gains content, so add [shrink=10] to the \begin{frame} options.

Full replacement file content:

  %% Slide 8: Evaluation Results — per-class metrics (EVAL-05)
  \begin{frame}[shrink=10]{Evaluation Results}
    \framesubtitle{Held-out Set (254 Messages)}

    \begin{center}
      \setlength{\tabcolsep}{5pt}%
      \renewcommand{\arraystretch}{1.15}%
      {\footnotesize
      \begin{tabular}{lcccc}
        \toprule
        \textbf{Class} & \textbf{Prec.} & \textbf{Recall} & \textbf{F1} & \textbf{n} \\
        \midrule
        Bank Impersonation  & 0.836 & 1.000 & 0.911 & 56 \\
        Zalo Social Eng.    & 1.000 & 0.960 & 0.980 & 75 \\
        Task Scam           & 1.000 & 0.871 & 0.931 & 62 \\
        Benign              & 1.000 & 1.000 & 1.000 & 61 \\
        \midrule
        \textbf{Macro avg}  & \textbf{0.959} & \textbf{0.958} & \textbf{0.955} & \textbf{254} \\
        \bottomrule
      \end{tabular}}
    \end{center}

    \vspace{0.1cm}

    \begin{block}{Binary Classification Result}
      Scam (all 3 threat classes) vs Non-scam (benign):
      Precision 1.000 \textperiodcentered\ Recall 1.000 \textperiodcentered\ F1 1.000
      --- zero cross-class boundary errors at the binary level.
    \end{block}

    \vspace{0.05cm}

    \begin{itemize}
      \item \textbf{Macro F1:} \textcolor{CVBLUE}{\textbf{0.9553}} ---
            all 4 threat classes exceeded the 0.80 recall floor.
      \item \textbf{Lowest recall:} Task Scam (0.871) ---
            bank-name + OTP phrasing overlaps with Bank Impersonation class boundary.
    \end{itemize}
  \end{frame}

Implementation notes:
- The \textperiodcentered command produces the centered dot (·) used as the separator in the binary result line. This command is available in standard LaTeX. If the slides preamble does not already load textcomp, use \cdot as a math-mode fallback wrapped in $\cdot$ instead.
- Keep \textcolor{CVBLUE} on the Macro F1 value in the bullet (CVBLUE is already defined in the deck preamble).
- The \midrule before Macro avg row visually separates the per-class rows from the summary row — do not omit it.
- \setlength{\tabcolsep}{5pt} and \renewcommand{\arraystretch}{1.15} are scoped inside \begin{center}...\end{center} so they do not affect surrounding content.
- The two \begin{itemize} bullet points must be reproduced exactly as they appear in the original file — word for word, including the exact numeric values and descriptive text.
  </action>
  <verify>
    <automated>grep -c "begin{tabular}" "documents/reports/latex/slides/sections/08_evaluation.tex"</automated>
  </verify>
  <done>
    - 08_evaluation.tex contains no reference to recall_barchart_bare.tex or \scalebox
    - A \begin{tabular} per-class table with all 5 rows (Bank Impersonation, Zalo Social Eng., Task Scam, Benign, Macro avg) is present
    - A \begin{block}{Binary Classification Result} block is present with Precision 1.000, Recall 1.000, F1 1.000
    - Both original bullet points (Macro F1 = 0.9553, Task Scam recall caveat) are present verbatim
    - Frame options include [shrink=10]
  </done>
</task>

<!-- ═══════════════════════════════════════════════════════════════════
     TASK 2 — Update 09_confusion.tex (EVAL-04 + EVAL-05)
     Two-column layout: 4-class matrix left + binary 2×2 right
     ═══════════════════════════════════════════════════════════════════ -->
<task type="auto" task_id="20-01-T2" req_ids="EVAL-04, EVAL-05">
  <name>Task 2: Add binary 2x2 confusion matrix in two-column layout in 09_confusion.tex</name>
  <files>documents/reports/latex/slides/sections/09_confusion.tex</files>
  <action>
Completely replace the contents of 09_confusion.tex with the following. The new layout uses \begin{columns}[t] to place the existing 4-class matrix on the left and a new binary 2×2 matrix plus Key Finding block on the right. The two existing bullet points at the bottom are preserved verbatim.

The frame is content-heavy; add [shrink=10] to the \begin{frame} options.

Full replacement file content:

  %% Slide 9: Confusion Matrix + Error Analysis (EVAL-04 + EVAL-05)
  \begin{frame}[shrink=10]{Confusion Matrix \& Error Analysis}
    \framesubtitle{Held-out Set (254 Messages)}

    \begin{columns}[t]

      %% ── Left column: 4-class confusion matrix ──────────────────────
      \begin{column}{0.52\textwidth}
        \begin{center}
          {\footnotesize\scalebox{0.72}{%
            \setlength{\tabcolsep}{8pt}%
            \renewcommand{\arraystretch}{1.2}%
            \begin{tabular}{lcccc}
            \toprule
            \textbf{True \textbackslash\ Predicted} & \textbf{Bank Imp.} & \textbf{Zalo SE} & \textbf{Task Scam} & \textbf{Benign} \\
            \midrule
            Bank Impersonation   & \cellcolor{CVBLUE!15}\textbf{56} &  0 &  0 &  0 \\
            Zalo Social Eng.     &  3 & \cellcolor{CVBLUE!15}\textbf{72} &  0 &  0 \\
            Task Scam            &  8 &  0 & \cellcolor{CVBLUE!15}\textbf{54} &  0 \\
            Benign               &  0 &  0 &  0 & \cellcolor{CVBLUE!15}\textbf{61} \\
            \bottomrule
            \end{tabular}%
          }}
        \end{center}
      \end{column}

      %% ── Right column: binary 2×2 matrix + key finding ──────────────
      \begin{column}{0.44\textwidth}
        {\small\textbf{Binary (Scam vs Non-scam):}}
        \vspace{0.15cm}

        \begin{center}
          {\footnotesize
          \setlength{\tabcolsep}{5pt}%
          \renewcommand{\arraystretch}{1.2}%
          \begin{tabular}{lcc}
            \toprule
            \textbf{True \textbackslash\ Pred.} & \textbf{Scam} & \textbf{Non-scam} \\
            \midrule
            Scam     & \cellcolor{CVBLUE!15}\textbf{193} & 0 \\
            Non-scam & 0 & \cellcolor{CVBLUE!15}\textbf{61} \\
            \bottomrule
          \end{tabular}}
        \end{center}

        \vspace{0.1cm}

        \begin{block}{Key Finding}
          All misclassifications are intra-scam (type confusion, not
          scam\(\leftrightarrow\)benign). Binary separation is perfect:
          F1 = 1.000.
        \end{block}
      \end{column}

    \end{columns}

    \vspace{0.1cm}

    \begin{itemize}
      \item \textbf{Key finding:} 8/62 Task Scam + 3/75 Zalo SE misclassified as Bank Impersonation.
      \item \textbf{Cause:} bank name + OTP language triggered bank-naming boundary overlap.
    \end{itemize}
  \end{frame}

Implementation notes:
- The 4-class matrix scalebox changes from 0.82 (original) to 0.72 to fit in the narrower left column — this is expected and intentional.
- \cellcolor{CVBLUE!15} requires the colortbl or xcolor package with table option. Both are already used elsewhere in the deck (original 09_confusion.tex already uses \cellcolor{CVBLUE!15}).
- The \( \leftrightarrow \) produces the double arrow in the Key Finding block. Use inline math mode as shown.
- Binary Scam row: TP = 193 (56 bank_imp + 72 zalo_se + 54 task_scam + 3 zalo_se_to_bank + 8 task_scam_to_bank = 193 total predicted-scam that were true-scam). FP = 0. FN = 0. TN = 61.
- The two \begin{itemize} bullet points must be reproduced exactly as they appear in the original file — word for word.
  </action>
  <verify>
    <automated>grep -c "begin{columns}" "documents/reports/latex/slides/sections/09_confusion.tex"</automated>
  </verify>
  <done>
    - 09_confusion.tex uses \begin{columns}[t] for a two-column layout
    - Left column (0.52\textwidth) contains the 4-class confusion matrix scaled to 0.72
    - Right column (0.44\textwidth) contains the binary 2×2 table with cells: Scam/Scam=193, Non-scam/Non-scam=61, both off-diagonal=0
    - Binary diagonal cells use \cellcolor{CVBLUE!15}
    - A \begin{block}{Key Finding} is present in the right column referencing F1 = 1.000
    - Both original bullet points (key finding + cause) are present verbatim at the bottom
    - Frame options include [shrink=10]
  </done>
</task>

<!-- ═══════════════════════════════════════════════════════════════════
     TASK 3 — XeLaTeX compile check
     Confirms deck compiles cleanly after both edits
     ═══════════════════════════════════════════════════════════════════ -->
<task type="auto" task_id="20-01-T3" req_ids="EVAL-04, EVAL-05">
  <name>Task 3: XeLaTeX compile check — confirm slides.pdf output, zero LaTeX errors</name>
  <files></files>
  <action>
From the directory documents/reports/latex/, run:

  xelatex -interaction=nonstopmode slides.tex

Run it twice if needed (once for cross-references, second for final output). After the run:

1. Confirm the terminal output contains the line: "Output written on slides.pdf"
2. Search the compile log (slides.log) for lines beginning with "! " — there must be zero such lines.
3. If any "! LaTeX Error" line is found, diagnose and fix in 08_evaluation.tex or 09_confusion.tex as appropriate, then re-run.

Common fixes:
- If "\textperiodcentered undefined": replace \textperiodcentered with $\cdot$ in 08_evaluation.tex
- If "Undefined control sequence \leftrightarrow": replace \( \leftrightarrow \) with $\leftrightarrow$ in 09_confusion.tex (the math mode wrapper already uses \( \), which is equivalent — if still failing, try $\leftrightarrow$ directly)
- If "Overfull \hbox" warnings appear (not errors): these are warnings only and do not block compilation — acceptable

The command to check for fatal errors in the log:

  grep "^! " documents/reports/latex/slides.log

This must return no output (exit code 1 from grep = no matches = success).
  </action>
  <verify>
    <automated>grep -c "^! " "documents/reports/latex/slides.log" || true</automated>
  </verify>
  <done>
    - slides.pdf is produced in documents/reports/latex/
    - The compile log contains "Output written on slides.pdf"
    - Zero lines beginning with "! " in slides.log (no fatal LaTeX errors)
    - The PDF page count is unchanged or increases by at most 1 page (layout may reflow slightly)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Editor → LaTeX compiler | Malformed .tex input crosses here; compiler rejects it with fatal errors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | 08_evaluation.tex / 09_confusion.tex | mitigate | Write tool produces deterministic replacement; no user input involved — no injection surface |
| T-20-02 | Denial of Service | xelatex compile loop | accept | Compile runs with -interaction=nonstopmode; infinite loops exit; low risk for static .tex edits |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — LaTeX only, no new package-manager dependencies |
</threat_model>

<verification>
After all three tasks complete:

1. `grep "scalebox.*recall_barchart" documents/reports/latex/slides/sections/08_evaluation.tex` returns no output (bar chart removed).
2. `grep "begin{tabular}" documents/reports/latex/slides/sections/08_evaluation.tex` returns at least 1 match (table present).
3. `grep "Binary Classification Result" documents/reports/latex/slides/sections/08_evaluation.tex` returns 1 match.
4. `grep "begin{columns}" documents/reports/latex/slides/sections/09_confusion.tex` returns 1 match (two-column layout).
5. `grep "193" documents/reports/latex/slides/sections/09_confusion.tex` returns 1 match (binary TP cell).
6. `grep "^! " documents/reports/latex/slides.log` returns no output (zero fatal errors).
7. `documents/reports/latex/slides.pdf` exists and is non-zero bytes.
</verification>

<success_criteria>
- Slide 8 shows a booktabs table with 5 rows (4 classes + macro avg) with Prec./Recall/F1/n columns; bar chart scalebox is gone.
- Slide 8 has a Binary Classification Result block showing Precision 1.000, Recall 1.000, F1 1.000.
- Slide 8 retains both original bullet points unchanged.
- Slide 9 has a two-column layout: left = 4-class matrix (scale 0.72), right = binary 2×2 matrix with CVBLUE!15 diagonal cells (193 and 61) + Key Finding block.
- Slide 9 retains both original bullet points unchanged.
- XeLaTeX produces slides.pdf with zero "! " fatal error lines in the compile log.
</success_criteria>

<output>
Create `.planning/phases/20-binary-eval-slides/20-01-SUMMARY.md` when done.
</output>
