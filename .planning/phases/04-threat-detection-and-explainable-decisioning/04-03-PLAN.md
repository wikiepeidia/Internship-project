---
phase: 04-threat-detection-and-explainable-decisioning
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/runtime/analyzers/gguf.py
  - src/runtime/analyzers/accelerated.py
  - src/runtime/render.py
  - src/runtime/cli.py
  - tests/runtime/test_gguf_backend.py
  - tests/runtime/test_accelerated_backend.py
  - tests/runtime/test_runtime_profiles.py
  - tests/runtime/test_render.py
  - tests/runtime/test_cli.py
autonomous: true
requirements:
  - DET-01
  - DET-02
  - XAI-01
  - XAI-02

must_haves:
  truths:
    - "Both real local profiles return the same Phase 4 field set and semantics through the existing analyze flow"
    - "GGUF and accelerated-local keep their existing loading paths while analyze results stay semantically aligned through the shared Phase 4 decision layer"
    - "The terminal analyze output shows risk tier, in-scope threat labels, grounded cues, and safe next steps without changing the CLI surface"
  artifacts:
    - path: "src/runtime/analyzers/gguf.py"
      provides: "GGUF baseline analyzer wired through the shared Phase 4 decision layer"
      exports: ["GGUFAnalyzer.doctor", "GGUFAnalyzer.analyze"]
    - path: "src/runtime/analyzers/accelerated.py"
      provides: "Accelerated analyzer wired through the shared Phase 4 decision layer"
      exports: ["AcceleratedAnalyzer.doctor", "AcceleratedAnalyzer.analyze"]
    - path: "src/runtime/render.py"
      provides: "Terminal presentation that maps internal labels to user-facing wording and prints safe next steps"
      exports: ["render_analysis_result"]
    - path: "tests/runtime/test_runtime_profiles.py"
      provides: "Cross-profile regression coverage for Phase 4 semantic parity"
      contains: "test_gguf_and_accelerated_profiles_share_phase_four_semantics"
  key_links:
    - from: "src/runtime/analyzers/gguf.py"
      to: "src/runtime/analyzers/local_model.py"
      via: "GGUF raw generation hands off to shared prompt, parsing, grounding, and mapping logic"
      pattern: "build_structured_analysis_prompt|build_analysis_result"
    - from: "src/runtime/analyzers/accelerated.py"
      to: "src/runtime/analyzers/local_model.py"
      via: "accelerated raw generation hands off to the same shared Phase 4 semantics"
      pattern: "build_structured_analysis_prompt|build_analysis_result"
    - from: "src/runtime/cli.py"
      to: "src/runtime/render.py"
      via: "analyze continues to print a terminal-friendly report instead of introducing a new command or output mode"
      pattern: "render_analysis_result"
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD034 MD040 MD041 MD046 MD055 MD056 MD060 -->

<objective>
Wire both real local backends and the existing operator surfaces through the shared Phase 4 decision layer.

Purpose: After Plan 04-02, the shared decision logic exists, but the real GGUF and accelerated analyzers still need to consume it. This plan makes both local profiles emit the same Phase 4 semantics and exposes those richer results through the existing terminal analyze flow.

Output: thin GGUF and accelerated analyzers that delegate Phase 4 semantics to `src/runtime/analyzers/local_model.py`, plus CLI and render integration that surfaces risk tier, labels, grounded cues, and safe recommendations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-threat-detection-and-explainable-decisioning/04-CONTEXT.md
@.planning/phases/04-threat-detection-and-explainable-decisioning/04-AI-SPEC.md
@.planning/phases/04-threat-detection-and-explainable-decisioning/04-RESEARCH.md
@.planning/phases/04-threat-detection-and-explainable-decisioning/04-PATTERNS.md
@.planning/phases/03-local-model-adaptation-and-deployment-paths/03-05-SUMMARY.md
@.planning/phases/03-local-model-adaptation-and-deployment-paths/03-06-SUMMARY.md
@src/runtime/analyzers/gguf.py
@src/runtime/analyzers/accelerated.py
@src/runtime/cli.py
@src/runtime/render.py

<interfaces>
From src/runtime/analyzers/gguf.py:
```python
class GGUFAnalyzer:
    def doctor(self) -> DoctorStatus: ...
    def analyze(self, request: AnalysisRequest) -> AnalysisResult: ...
```

From src/runtime/analyzers/accelerated.py:
```python
class AcceleratedAnalyzer:
    def doctor(self) -> DoctorStatus: ...
    def analyze(self, request: AnalysisRequest) -> AnalysisResult: ...
```

From src/runtime/cli.py:
```python
def build_parser() -> argparse.ArgumentParser: ...
def handle_analyze(args: argparse.Namespace) -> int: ...
def handle_doctor(args: argparse.Namespace) -> int: ...
```
</interfaces>
</context>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GGUF and accelerated raw generation -> shared decision layer | untrusted local model text must produce the same validated Phase 4 semantics across profiles |
| shared decision layer -> terminal rendering | user-facing wording must stay grounded and safe even when internal labels remain dataset-aligned |
| doctor-backed analyze path -> operator expectations | the public commands must remain stable while richer behavior is exposed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Integrity | src/runtime/analyzers/gguf.py | mitigate | Keep GGUF backend thin and route raw generation through the shared Phase 4 parser and mapper instead of duplicating semantics. |
| T-04-03-02 | Integrity | src/runtime/analyzers/accelerated.py | mitigate | Keep accelerated backend thin and reuse the same label, grounding, and recommendation semantics as GGUF. |
| T-04-03-03 | Information Disclosure | src/runtime/render.py | mitigate | Render only the bounded Phase 4 fields and user-facing label wording; do not dump raw model output or internal-only debug payloads. |
| T-04-03-SC | Tampering | cross-profile parity tests | mitigate | Extend runtime-profile tests so neither profile can silently drift in labels, cues, or recommendations. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire GGUF and accelerated analyzers through the shared Phase 4 decision layer</name>
  <files>
    src/runtime/analyzers/gguf.py,
    src/runtime/analyzers/accelerated.py,
    tests/runtime/test_gguf_backend.py,
    tests/runtime/test_accelerated_backend.py,
    tests/runtime/test_runtime_profiles.py
  </files>
  <read_first>
    .planning/phases/04-threat-detection-and-explainable-decisioning/04-RESEARCH.md,
    .planning/phases/04-threat-detection-and-explainable-decisioning/04-PATTERNS.md,
    .planning/phases/03-local-model-adaptation-and-deployment-paths/03-05-SUMMARY.md,
    .planning/phases/03-local-model-adaptation-and-deployment-paths/03-06-SUMMARY.md,
    src/runtime/analyzers/local_model.py,
    src/runtime/analyzers/gguf.py,
    src/runtime/analyzers/accelerated.py,
    tests/runtime/test_gguf_backend.py,
    tests/runtime/test_accelerated_backend.py,
    tests/runtime/test_runtime_profiles.py
  </read_first>
  <behavior>
    - Test: both real local profiles now return `threat_labels` and `recommendations` in the public `AnalysisResult`
    - Test: GGUF and accelerated backends share the same field set and label vocabulary while retaining distinct backend names and explicit profile selection
    - Test: neither backend reimplements label mapping, evidence validation, or recommendation safety locally
  </behavior>
  <action>
    Update `src/runtime/analyzers/gguf.py` and `src/runtime/analyzers/accelerated.py` so raw generation hands off to the shared Phase 4 helper for prompt building, payload extraction, grounding, and additive result mapping. Preserve the current responsibilities exactly: GGUF and accelerated modules own model loading and deterministic generation, while `src/runtime/analyzers/local_model.py` owns decision semantics.

    Preserve the locked Phase 3 continuity while doing this work: `gguf-laptop` remains tied to the baseline-winner GGUF artifact lineage for `qwen3-4b-instruct-2507`, `accelerated-local` remains tied to the trained runner-up `qwen3.5-4b` path, and Phase 4 continues from those retained artifacts instead of reopening candidate selection or pilot logic.

    Extend the backend and runtime-profile suites to prove both profiles now emit the full Phase 4 result surface with aligned semantics. Do not add a new backend, do not reintroduce heuristic-first scoring as the main result path, and do not collapse explicit runtime-profile selection.
  </action>
  <verify>
    <automated>C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m pytest tests/runtime/test_gguf_backend.py tests/runtime/test_accelerated_backend.py tests/runtime/test_runtime_profiles.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/runtime/test_gguf_backend.py` contains `def test_gguf_analyze_returns_phase_four_result_fields`
    - `tests/runtime/test_accelerated_backend.py` contains `def test_accelerated_analyze_returns_phase_four_result_fields`
    - `tests/runtime/test_runtime_profiles.py` contains `def test_gguf_and_accelerated_profiles_share_phase_four_semantics`
    - `src/runtime/analyzers/gguf.py` and `src/runtime/analyzers/accelerated.py` both delegate Phase 4 shaping to `src/runtime/analyzers/local_model.py`
    - explicit runtime-profile selection remains intact for `gguf-laptop`, `gguf-runner-up`, and `accelerated-local`
    - touched backend tests preserve `qwen3-4b-instruct-2507` as the GGUF laptop baseline lineage and `qwen3.5-4b` as the accelerated runner-up lineage without adding new candidate-selection logic
    - `C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m pytest tests/runtime/test_gguf_backend.py tests/runtime/test_accelerated_backend.py tests/runtime/test_runtime_profiles.py -q` exits with code 0
  </acceptance_criteria>
  <done>Both real local profiles emit one shared Phase 4 semantic contract through the existing runtime seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete terminal analyze presentation for model-backed Phase 4 results</name>
  <files>
    src/runtime/render.py,
    src/runtime/cli.py,
    tests/runtime/test_render.py,
    tests/runtime/test_cli.py
  </files>
  <read_first>
    .planning/phases/04-threat-detection-and-explainable-decisioning/04-CONTEXT.md,
    .planning/phases/04-threat-detection-and-explainable-decisioning/04-VALIDATION.md,
    src/runtime/render.py,
    src/runtime/cli.py,
    tests/runtime/test_render.py,
    tests/runtime/test_cli.py
  </read_first>
  <behavior>
    - Test: `python -m src.runtime.cli analyze` prints risk tier, mapped user-facing labels, grounded cues, and safe next steps through the existing terminal flow
    - Test: `analyze` and `doctor` remain the only public commands
    - Test: not-ready states still return doctor guidance rather than a hidden fallback or a new command path
  </behavior>
  <action>
    Finish the renderer and CLI integration around real Phase 4 results. Map internal label literals to the user-facing wording required by D-05 at the render edge, keep output concise and terminal-friendly, and keep the operator surface exactly `analyze` and `doctor`.

    This is presentation work, not a new orchestration surface. Do not default to raw JSON, do not add a second explain command, and do not bypass doctor-gated fail-closed behavior when the selected local backend is unavailable.
  </action>
  <verify>
    <automated>C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m pytest tests/runtime/test_render.py tests/runtime/test_cli.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/runtime/test_render.py` contains `def test_render_analysis_result_maps_internal_labels_to_user_facing_wording`
    - `tests/runtime/test_cli.py` contains `def test_analyze_prints_phase_four_result_through_existing_command_surface`
    - `tests/runtime/test_cli.py` still asserts only `analyze` and `doctor` are exposed
    - `src/runtime/render.py` remains summary-first and terminal-friendly rather than JSON-first
    - `C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m pytest tests/runtime/test_render.py tests/runtime/test_cli.py -q` exits with code 0
  </acceptance_criteria>
  <done>The public analyze flow exposes model-backed Phase 4 results without widening the runtime command surface.</done>
</task>

</tasks>

<verification>
- `C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m pytest tests/runtime/test_gguf_backend.py tests/runtime/test_accelerated_backend.py tests/runtime/test_runtime_profiles.py -q` passes
- `C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m pytest tests/runtime/test_render.py tests/runtime/test_cli.py -q` passes
- Manual GGUF smoke after implementation: `MODEL_ARTIFACT_ROOT="D:/PROJEct/AI MODELS" MODEL_REGISTRY_PATH="D:/PROJEct/AI MODELS/manifests/model-registry.json" RUNTIME_BACKEND=gguf RUNTIME_PROFILE=gguf-laptop C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m src.runtime.cli analyze --text "VPBank canh bao tai khoan se bi khoa neu khong xac minh OTP tai https://vpbank-safe.example" --channel sms` returns risk tier, in-scope labels, grounded cues, and safe recommendations through the existing terminal flow
- Manual accelerated smoke after implementation: `MODEL_ARTIFACT_ROOT="D:/PROJEct/AI MODELS" MODEL_REGISTRY_PATH="D:/PROJEct/AI MODELS/manifests/model-registry.json" RUNTIME_BACKEND=accelerated RUNTIME_PROFILE=accelerated-local C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m src.runtime.cli analyze --text "Em doi so moi, gui ma OTP de xac minh tai khoan ngan hang ngay" --channel messenger` returns the same field set and user-safe guidance through the same command
- After any smoke that exports `RUNTIME_BACKEND`, `RUNTIME_PROFILE`, `MODEL_ARTIFACT_ROOT`, or `MODEL_REGISTRY_PATH`, unset those variables before running repo-wide tests so settings-default assertions keep using repo defaults
</verification>

<success_criteria>
Both the GGUF baseline and the accelerated local profile now produce the same Phase 4 semantics through the existing analyze command, and the terminal renderer exposes those results without changing the public runtime surface.
</success_criteria>

<output>
After completion, create `.planning/phases/04-threat-detection-and-explainable-decisioning/04-03-SUMMARY.md`
</output>

<!-- markdownlint-enable MD022 MD031 MD032 MD033 MD034 MD040 MD041 MD046 MD055 MD056 MD060 -->