---
phase: 04-threat-detection-and-explainable-decisioning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/runtime/contracts.py
  - src/runtime/analyzers/local_model.py
  - src/runtime/render.py
  - tests/runtime/test_contracts.py
  - tests/runtime/test_local_model.py
  - tests/runtime/test_service.py
  - tests/runtime/test_runtime_profiles.py
  - tests/runtime/test_render.py
  - tests/runtime/test_cli.py
autonomous: true
requirements:
  - DET-01
  - DET-02
  - XAI-01
  - XAI-02

must_haves:
  truths:
    - "The public runtime contract can express one risk tier, in-scope labels, grounded cues, and safe next steps without breaking the existing CLI/profile seam"
    - "Operators can run focused runtime tests that fail if labels, grounded evidence presentation, or recommendations disappear from the planned Phase 4 public result surface"
    - "Cross-profile parity tests make semantic drift between GGUF and accelerated-local visible before backend-specific wiring changes land"
  artifacts:
    - path: "src/runtime/contracts.py"
      provides: "Additive Phase 4 compatibility boundary for labels and recommendations"
      exports: ["RiskTier", "ThreatLabel", "AnalysisResult"]
    - path: "src/runtime/analyzers/local_model.py"
      provides: "Shared decision-layer interface and schema scaffolding that later backend plans will implement against"
      exports: ["build_structured_analysis_prompt", "extract_structured_payload", "build_analysis_result"]
    - path: "tests/runtime/test_local_model.py"
      provides: "Wave 0 regression coverage for shared decision-layer semantics"
      contains: "test_build_analysis_result_preserves_phase_four_fields"
    - path: "tests/runtime/test_render.py"
      provides: "Terminal presentation regression coverage for Phase 4 outputs"
      contains: "test_render_analysis_result_includes_risk_labels_cues_and_recommendations"
  key_links:
    - from: "src/runtime/contracts.py"
      to: "src/runtime/render.py"
      via: "renderer consumes additive fields without changing the public analyze command surface"
      pattern: "threat_labels|recommendations"
    - from: "src/runtime/analyzers/local_model.py"
      to: "tests/runtime/test_runtime_profiles.py"
      via: "shared helper expectations keep GGUF and accelerated semantics aligned before backend wiring"
      pattern: "phase four field parity|semantic parity"
    - from: "tests/runtime/test_cli.py"
      to: "src/runtime/cli.py"
      via: "CLI still exposes only analyze and doctor while printing richer mocked results"
      pattern: "choices.keys\(\) == \[\"analyze\", \"doctor\"\]"
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD034 MD040 MD041 MD046 MD055 MD056 MD060 -->

<objective>
Define the additive Phase 4 contract, shared decision-layer interface, and Wave 0 runtime test surface before real backend semantics are changed.

Purpose: Phase 4 cannot safely tune prompts or wire backends until the contract, presentation surface, and cross-profile expectations are explicit. This plan locks those seams first so later work lands against stable, typed interfaces instead of ad hoc backend edits.

Output: additive Phase 4 result fields in `src/runtime/contracts.py`, shared local-model interface scaffolding, and runtime tests covering contract shape, service propagation, cross-profile parity, and terminal rendering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-threat-detection-and-explainable-decisioning/04-CONTEXT.md
@.planning/phases/04-threat-detection-and-explainable-decisioning/04-AI-SPEC.md
@.planning/phases/04-threat-detection-and-explainable-decisioning/04-RESEARCH.md
@.planning/phases/04-threat-detection-and-explainable-decisioning/04-VALIDATION.md
@.planning/phases/04-threat-detection-and-explainable-decisioning/04-PATTERNS.md
@src/runtime/contracts.py
@src/runtime/analyzers/local_model.py
@src/runtime/service.py
@src/runtime/render.py

<interfaces>
From src/runtime/contracts.py:
```python
RiskTier = Literal["benign", "suspicious", "high-risk"]

class SuspiciousCue(BaseModel):
    span: str
    reason: str
    cue_type: str | None = None

class AnalysisResult(BaseModel):
    risk_tier: RiskTier
    summary: str
    top_cues: list[SuspiciousCue] = Field(default_factory=list, max_length=3)
    backend_name: str
    provisional: bool = True
    normalized_text: str | None = None
```

From src/runtime/analyzers/local_model.py:
```python
def build_structured_analysis_prompt(text: str) -> str: ...
def extract_structured_payload(raw_output: str) -> dict[str, Any]: ...
def build_analysis_result(payload: dict[str, Any], request: AnalysisRequest, backend_name: str) -> AnalysisResult: ...
```

From src/runtime/render.py:
```python
def render_analysis_result(result: AnalysisResult) -> str: ...
```
</interfaces>
</context>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared contract -> CLI and tests | additive Phase 4 fields must not break the existing operator surface |
| local-model interface -> profile-specific backends | one semantic contract must be shared before GGUF and accelerated wiring changes |
| richer output fields -> terminal rendering | labels and recommendations must stay concise and privacy-safe in terminal output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | src/runtime/contracts.py | mitigate | Extend `AnalysisResult` additively so Phase 4 fields do not replace or silently rename the existing risk-tier and cue surface. |
| T-04-01-02 | Integrity | src/runtime/analyzers/local_model.py | mitigate | Define one shared decision-layer interface and label vocabulary now so later backend work cannot drift into profile-specific semantics. |
| T-04-01-03 | Information Disclosure | src/runtime/render.py | mitigate | Lock renderer and CLI expectations with mocked outputs so richer fields remain concise and do not introduce raw-text echo or JSON dump defaults. |
| T-04-01-SC | Tampering | pytest runtime scaffolding | mitigate | Keep Wave 0 checks narrow and deterministic; block progress on contract drift before broader Phase 4 backend work. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the public runtime contract additively for Phase 4 labels and recommendations</name>
  <files>
    src/runtime/contracts.py,
    tests/runtime/test_contracts.py
  </files>
  <read_first>
    .planning/phases/04-threat-detection-and-explainable-decisioning/04-CONTEXT.md,
    .planning/phases/04-threat-detection-and-explainable-decisioning/04-AI-SPEC.md,
    .planning/phases/04-threat-detection-and-explainable-decisioning/04-RESEARCH.md,
    src/runtime/contracts.py,
    src/data_pipeline/schemas.py,
    tests/runtime/test_contracts.py
  </read_first>
  <behavior>
    - Test: `AnalysisResult` accepts Phase 4 `threat_labels` and `recommendations` without making them mandatory for legacy callers
    - Test: threat-label literals stay aligned with the dataset vocabulary (`bank_impersonation`, `zalo_social_engineering`, `task_scam`, `benign`)
    - Test: the existing three-cue cap and current `risk_tier`, `summary`, `backend_name`, and `provisional` behavior remain intact
  </behavior>
  <action>
    Extend `src/runtime/contracts.py` additively so the public result can carry in-scope threat labels and safe next-step recommendations while preserving the Phase 2 and Phase 3 compatibility boundary from D-03. Keep the risk-tier contract exactly three-way per D-04, keep internal label literals aligned with the dataset and training schema per D-05, and keep `top_cues` as the short grounded-evidence seam rather than replacing it.

    Use the smallest typed surface that lets later plans express DET-02 and XAI-02 without destabilizing the current operator flow. Do not widen `AnalysisRequest`, do not remove `summary`, and do not introduce a second result object or a user-facing-only label enum in the core contract.
  </action>
  <verify>
    <automated>C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m pytest tests/runtime/test_contracts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/runtime/contracts.py` contains `ThreatLabel`
    - `src/runtime/contracts.py` contains `threat_labels`
    - `src/runtime/contracts.py` contains `recommendations`
    - `tests/runtime/test_contracts.py` contains `def test_analysis_result_accepts_phase_four_labels_and_recommendations`
    - `tests/runtime/test_contracts.py` contains `def test_runtime_threat_labels_match_dataset_schema`
    - the existing `top_cues` max-length contract remains enforced
    - `C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m pytest tests/runtime/test_contracts.py -q` exits with code 0
  </acceptance_criteria>
  <done>The public runtime contract can carry Phase 4 labels and recommendations without breaking the existing result surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define the shared local-model interface and Wave 0 parity tests before backend wiring</name>
  <files>
    src/runtime/analyzers/local_model.py,
    tests/runtime/test_local_model.py,
    tests/runtime/test_service.py,
    tests/runtime/test_runtime_profiles.py
  </files>
  <read_first>
    .planning/phases/04-threat-detection-and-explainable-decisioning/04-AI-SPEC.md,
    .planning/phases/04-threat-detection-and-explainable-decisioning/04-VALIDATION.md,
    .planning/phases/04-threat-detection-and-explainable-decisioning/04-PATTERNS.md,
    src/runtime/analyzers/local_model.py,
    src/runtime/service.py,
    tests/runtime/test_service.py,
    tests/runtime/test_runtime_profiles.py
  </read_first>
  <behavior>
    - Test: one shared helper surface exists for Phase 4 decision semantics instead of separate GGUF and accelerated post-processing contracts
    - Test: the runtime service still normalizes once, preserves the channel hint, and can propagate additive Phase 4 fields from a backend result
    - Test: cross-profile parity now expects the same field set and label vocabulary while still keeping backend names distinct
  </behavior>
  <action>
    Add the interface-level Phase 4 decision scaffolding in `src/runtime/analyzers/local_model.py` first: the shared label vocabulary, the internal decision-shaping seam, and the helper signatures both real local backends will use. Then create `tests/runtime/test_local_model.py` and extend the adjacent service and runtime-profile suites so Phase 4 parity is enforced before real GGUF or accelerated backend wiring changes land.

    Keep this plan interface-first. It should define the shared contract and expectations the later backend plan will consume, not duplicate logic inside `gguf.py` or `accelerated.py`, and it must not change the runtime default backend yet.
  </action>
  <verify>
    <automated>C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m pytest tests/runtime/test_local_model.py tests/runtime/test_service.py tests/runtime/test_runtime_profiles.py -q</automated>
  </verify>
  <acceptance_criteria>
    - new file `tests/runtime/test_local_model.py` exists
    - `tests/runtime/test_local_model.py` contains `def test_build_analysis_result_preserves_phase_four_fields`
    - `tests/runtime/test_local_model.py` contains `def test_local_model_rejects_out_of_scope_labels`
    - `tests/runtime/test_service.py` contains a Phase 4 propagation assertion for `threat_labels` and `recommendations`
    - `tests/runtime/test_runtime_profiles.py` contains `def test_runtime_profiles_expect_phase_four_field_parity`
    - `src/runtime/analyzers/local_model.py` exposes the shared decision-layer surface that later backend plans will call
    - `C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m pytest tests/runtime/test_local_model.py tests/runtime/test_service.py tests/runtime/test_runtime_profiles.py -q` exits with code 0
  </acceptance_criteria>
  <done>The shared decision-layer seam and its parity expectations exist before any backend-specific Phase 4 implementation begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Freeze terminal presentation for richer results without widening the CLI surface</name>
  <files>
    src/runtime/render.py,
    tests/runtime/test_render.py,
    tests/runtime/test_cli.py
  </files>
  <read_first>
    .planning/phases/04-threat-detection-and-explainable-decisioning/04-CONTEXT.md,
    .planning/phases/04-threat-detection-and-explainable-decisioning/04-VALIDATION.md,
    src/runtime/render.py,
    src/runtime/cli.py,
    tests/runtime/test_cli.py
  </read_first>
  <behavior>
    - Test: the renderer prints risk tier, mapped labels, grounded cues, and safe next steps when those fields are present
    - Test: legacy summary-only results still render cleanly
    - Test: the CLI still exposes only `analyze` and `doctor` and can print mocked Phase 4 results without a new command surface
  </behavior>
  <action>
    Extend `src/runtime/render.py` so it can present additive Phase 4 fields in a concise terminal-friendly format while staying backward-compatible with the existing summary-first flow. Add `tests/runtime/test_render.py` and expand `tests/runtime/test_cli.py` with mocked results only so the presentation contract is frozen before real local-model outputs are wired through.

    Preserve the current analyze and doctor entrypoints exactly. Do not add a new orchestration command, do not switch the default output format to raw JSON, and do not leak internal label wording directly to the user if a clearer user-facing mapping belongs in the renderer.
  </action>
  <verify>
    <automated>C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m pytest tests/runtime/test_render.py tests/runtime/test_cli.py -q</automated>
  </verify>
  <acceptance_criteria>
    - new file `tests/runtime/test_render.py` exists
    - `tests/runtime/test_render.py` contains `def test_render_analysis_result_includes_risk_labels_cues_and_recommendations`
    - `tests/runtime/test_render.py` contains `def test_render_analysis_result_preserves_legacy_summary_only_output`
    - `tests/runtime/test_cli.py` still asserts that the only public commands are `analyze` and `doctor`
    - `src/runtime/render.py` can render results that include labels and recommendations without dumping raw JSON
    - `C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m pytest tests/runtime/test_render.py tests/runtime/test_cli.py -q` exits with code 0
  </acceptance_criteria>
  <done>The terminal presentation contract is locked for richer Phase 4 results while the CLI surface remains unchanged.</done>
</task>

</tasks>

<verification>
- `C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m pytest tests/runtime/test_contracts.py -q` passes
- `C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m pytest tests/runtime/test_local_model.py tests/runtime/test_service.py tests/runtime/test_runtime_profiles.py -q` passes
- `C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m pytest tests/runtime/test_render.py tests/runtime/test_cli.py -q` passes
</verification>

<success_criteria>
Phase 4 starts from an additive public contract, a shared decision-layer interface, and executable runtime tests that lock presentation and parity before any backend-specific model behavior changes are attempted.
</success_criteria>

<output>
After completion, create `.planning/phases/04-threat-detection-and-explainable-decisioning/04-01-SUMMARY.md`
</output>

<!-- markdownlint-enable MD022 MD031 MD032 MD033 MD034 MD040 MD041 MD046 MD055 MD056 MD060 -->