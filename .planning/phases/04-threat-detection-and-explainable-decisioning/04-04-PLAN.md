---
phase: 04-threat-detection-and-explainable-decisioning
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - src/config/settings.py
  - src/runtime/doctor.py
  - src/runtime/service.py
  - tests/runtime/test_contracts.py
  - tests/runtime/test_doctor.py
  - tests/runtime/test_runtime_profiles.py
  - tests/runtime/test_cli.py
autonomous: true
requirements:
  - DET-01
  - DET-02
  - XAI-01
  - XAI-02

must_haves:
  truths:
    - "The shipped default runtime path is the real local GGUF baseline rather than the old heuristic default"
    - "Explicit runtime-profile selection still works for runner-up GGUF and accelerated-local instead of being hidden by automatic backend switching"
    - "If the default local model path is unavailable, doctor-backed fail-closed behavior remains intact instead of silently falling back to heuristic output"
  artifacts:
    - path: "src/config/settings.py"
      provides: "Phase 4 default runtime backend and profile configuration"
      exports: ["Settings"]
    - path: "src/runtime/doctor.py"
      provides: "Doctor guidance and readiness checks aligned with the promoted Phase 4 default runtime"
      exports: ["run_runtime_doctor", "format_doctor_report"]
    - path: "tests/runtime/test_doctor.py"
      provides: "Regression coverage for the promoted default backend and explicit profile overrides"
      contains: "test_doctor_uses_gguf_laptop_as_phase_four_default"
  key_links:
    - from: "src/config/settings.py"
      to: "src/runtime/service.py"
      via: "default backend/profile selection determines which analyzer the public analyze flow builds"
      pattern: "runtime_backend|runtime_profile"
    - from: "src/runtime/doctor.py"
      to: "tests/runtime/test_doctor.py"
      via: "doctor output must reflect the promoted Phase 4 default and still give explicit remediation when local resources are absent"
      pattern: "READY backend=gguf|NOT READY backend=gguf"
    - from: "tests/runtime/test_cli.py"
      to: "src/runtime/cli.py"
      via: "plain analyze continues to use the existing command surface while the default backend changes underneath"
      pattern: "analyze"
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD034 MD040 MD041 MD046 MD055 MD056 MD060 -->

<objective>
Promote `gguf-laptop` to the explicit shipped default only after the shared Phase 4 semantics and real-profile integration are green.

Purpose: The current runtime still defaults to `heuristic`, which would leave the public shipped behavior ambiguous after Phase 4. This late-wave plan makes the Phase 4 default explicit while preserving doctor-backed fail-closed behavior and profile selection.

Output: updated runtime defaults in `src/config/settings.py`, doctor and CLI regressions proving the default uses the real GGUF baseline, and explicit-profile tests proving the other local profiles remain selectable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-threat-detection-and-explainable-decisioning/04-CONTEXT.md
@.planning/phases/04-threat-detection-and-explainable-decisioning/04-RESEARCH.md
@.planning/phases/04-threat-detection-and-explainable-decisioning/04-VALIDATION.md
@.planning/phases/03-local-model-adaptation-and-deployment-paths/03-05-SUMMARY.md
@src/config/settings.py
@src/runtime/doctor.py
@src/runtime/service.py
@tests/runtime/test_contracts.py
@tests/runtime/test_doctor.py

<interfaces>
From src/config/settings.py:
```python
class Settings(BaseSettings):
    runtime_backend: str = "heuristic"
    runtime_profile: str = "heuristic"
    runtime_profile_gguf: str = "gguf-laptop"
    runtime_profile_gguf_runner_up: str = "gguf-runner-up"
    runtime_profile_accelerated: str = "accelerated-local"
```

From src/runtime/doctor.py:
```python
def run_runtime_doctor() -> DoctorStatus: ...
def format_doctor_report(status: DoctorStatus) -> str: ...
```
</interfaces>
</context>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| default settings -> runtime service builder | the default backend change affects every plain analyze invocation |
| default runtime -> doctor gating | promoted defaults must still fail closed when local resources are absent |
| explicit profile override -> shipped default | runner-up and accelerated profiles must remain explicit choices rather than being hidden or removed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Denial of Service | src/config/settings.py | mitigate | Promote the default only after GGUF doctor-backed readiness is proven and keep fail-closed behavior intact when artifacts are absent. |
| T-04-04-02 | Integrity | src/runtime/service.py | mitigate | Preserve explicit profile selection and never silently route a failed default back to heuristic output. |
| T-04-04-03 | Repudiation | src/runtime/doctor.py | mitigate | Update doctor tests so readiness and remediation clearly reflect the promoted default runtime path. |
| T-04-04-SC | Tampering | default-profile promotion | mitigate | Treat this as a separate late wave with focused tests instead of mixing it into earlier semantic work. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote `gguf-laptop` to the Phase 4 default and update doctor guidance accordingly</name>
  <files>
    src/config/settings.py,
    src/runtime/doctor.py,
    tests/runtime/test_contracts.py,
    tests/runtime/test_doctor.py,
    tests/runtime/test_runtime_profiles.py
  </files>
  <read_first>
    .planning/phases/04-threat-detection-and-explainable-decisioning/04-CONTEXT.md,
    .planning/phases/04-threat-detection-and-explainable-decisioning/04-RESEARCH.md,
    src/config/settings.py,
    src/runtime/doctor.py,
    src/runtime/service.py,
    tests/runtime/test_contracts.py,
    tests/runtime/test_doctor.py,
    tests/runtime/test_runtime_profiles.py
  </read_first>
  <behavior>
    - Test: the default `Settings()` now select `runtime_backend="gguf"` and `runtime_profile="gguf-laptop"`
    - Test: doctor output and readiness checks reflect the promoted GGUF default while still allowing explicit `gguf-runner-up` and `accelerated-local` selection
    - Test: the promoted default does not reintroduce hidden cloud fallback, raw-text storage, or silent heuristic fallback
  </behavior>
  <action>
    Update `src/config/settings.py` so the shipped Phase 4 default runtime is the real GGUF baseline profile rather than the old heuristic path. Then align `src/runtime/doctor.py` and the adjacent contract/doctor/runtime-profile tests with that promoted default while keeping explicit profile selection intact for `gguf-runner-up` and `accelerated-local` per D-01 and D-02.

    This is an explicit late-wave promotion, not an implicit side effect. Do not remove the heuristic backend from the codebase, but do not silently fall back to it when the promoted default is unavailable.
  </action>
  <verify>
    <automated>C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m pytest tests/runtime/test_contracts.py tests/runtime/test_doctor.py tests/runtime/test_runtime_profiles.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/config/settings.py` defaults `runtime_backend` to `gguf`
    - `src/config/settings.py` defaults `runtime_profile` to `gguf-laptop`
    - `tests/runtime/test_contracts.py` no longer expects `runtime_backend == "heuristic"`
    - `tests/runtime/test_doctor.py` contains `def test_doctor_uses_gguf_laptop_as_phase_four_default`
    - `tests/runtime/test_runtime_profiles.py` still covers explicit selection of `accelerated-local`
    - `C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m pytest tests/runtime/test_contracts.py tests/runtime/test_doctor.py tests/runtime/test_runtime_profiles.py -q` exits with code 0
  </acceptance_criteria>
  <done>The project’s default runtime configuration now points at the Phase 4 GGUF baseline and doctor guidance reflects that choice explicitly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the public analyze flow stays explicit and fail-closed after default promotion</name>
  <files>
    src/runtime/service.py,
    tests/runtime/test_cli.py,
    tests/runtime/test_runtime_profiles.py
  </files>
  <read_first>
    .planning/phases/04-threat-detection-and-explainable-decisioning/04-VALIDATION.md,
    src/runtime/service.py,
    src/runtime/cli.py,
    tests/runtime/test_cli.py,
    tests/runtime/test_runtime_profiles.py
  </read_first>
  <behavior>
    - Test: plain `analyze` now uses the GGUF default when the promoted default profile is ready
    - Test: explicit runtime-profile overrides still work and remain visible in tests
    - Test: when the promoted default is unavailable, analyze still stops at doctor guidance instead of silently routing to heuristic output
  </behavior>
  <action>
    Adjust any runtime-service assumptions that were only valid under the heuristic default so the public analyze path now targets the GGUF baseline by default while still respecting explicit overrides. Keep fail-closed behavior intact, keep profile selection explicit, and make the tests prove there is no hidden fallback path.

    Do not add a new command flag to opt into the default; the existing command surface should remain unchanged. The work here is default selection and safety behavior, not another round of semantic output changes.
  </action>
  <verify>
    <automated>C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m pytest tests/runtime/test_cli.py tests/runtime/test_runtime_profiles.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/runtime/test_cli.py` contains `def test_analyze_uses_phase_four_gguf_default_when_ready`
    - `tests/runtime/test_cli.py` contains a fail-closed assertion that still prints doctor guidance when the promoted default is unavailable
    - `tests/runtime/test_runtime_profiles.py` still proves explicit non-default profile selection remains possible
    - `src/runtime/service.py` does not silently reroute a failed GGUF default back to heuristic output
    - `C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m pytest tests/runtime/test_cli.py tests/runtime/test_runtime_profiles.py -q` exits with code 0
  </acceptance_criteria>
  <done>The public analyze flow now defaults to the real GGUF baseline while preserving explicit profile selection and fail-closed safety.</done>
</task>

</tasks>

<verification>
- `C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m pytest tests/runtime/test_contracts.py tests/runtime/test_doctor.py tests/runtime/test_runtime_profiles.py -q` passes
- `C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m pytest tests/runtime/test_cli.py tests/runtime/test_runtime_profiles.py -q` passes
- Manual doctor smoke after implementation: `MODEL_ARTIFACT_ROOT="D:/PROJEct/AI MODELS" MODEL_REGISTRY_PATH="D:/PROJEct/AI MODELS/manifests/model-registry.json" C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m src.runtime.cli doctor` reports `READY backend=gguf`
- Manual default analyze smoke after implementation: `MODEL_ARTIFACT_ROOT="D:/PROJEct/AI MODELS" MODEL_REGISTRY_PATH="D:/PROJEct/AI MODELS/manifests/model-registry.json" C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m src.runtime.cli analyze --text "Vietcombank canh bao tai khoan se bi khoa trong 24h, vui long nhap OTP tai https://verify-vcb.example" --channel sms` uses the promoted GGUF default and returns the Phase 4 result surface without needing `RUNTIME_BACKEND` or `RUNTIME_PROFILE`
- After any smoke that exports `MODEL_ARTIFACT_ROOT` or `MODEL_REGISTRY_PATH`, unset those variables before running broader repo tests so settings-default assertions continue to validate the repo defaults cleanly
</verification>

<success_criteria>
Phase 4 no longer ships a heuristic default by accident: the default analyze path is the real local GGUF baseline, doctor guidance is aligned with that choice, and explicit profile selection plus fail-closed safety both remain intact.
</success_criteria>

<output>
After completion, create `.planning/phases/04-threat-detection-and-explainable-decisioning/04-04-SUMMARY.md`
</output>

<!-- markdownlint-enable MD022 MD031 MD032 MD033 MD034 MD040 MD041 MD046 MD055 MD056 MD060 -->