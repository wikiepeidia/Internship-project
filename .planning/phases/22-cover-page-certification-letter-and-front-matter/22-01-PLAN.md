---
phase: 22-cover-page-certification-letter-and-front-matter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - documents/reports/latex/main.tex
  - documents/reports/latex/chapters/frontmatter/titlepage.tex
  - documents/reports/latex/chapters/frontmatter/certification.tex
  - documents/reports/latex/chapters/frontmatter/preface.tex
autonomous: true
requirements:
  - COVER-01
  - CERT-01
  - FRONT-01
  - FRONT-02
  - FRONT-03
must_haves:
  truths:
    - "COVER-01: Title page renders BACHELOR THESIS and uses explicit By / Title layout."
    - "CERT-01: Certification letter appears after titlepage and before roman-numbered front matter."
    - "FRONT-01: Front matter order is TOC, Acknowledgements, List of Abbreviations, List of Tables, List of Figures, Abstract."
    - "FRONT-02: List of Abbreviations uses a two-column table and covers required thesis acronyms."
    - "FRONT-03: Abstract includes exactly 6 English keywords and body prose is verified as no more than 250 words."
  artifacts:
    - path: "documents/reports/latex/main.tex"
      provides: "Frontmatter input orchestration"
      contains: "\\input{chapters/frontmatter/certification}"
    - path: "documents/reports/latex/chapters/frontmatter/titlepage.tex"
      provides: "Department-template cover page"
      contains: "BACHELOR THESIS"
    - path: "documents/reports/latex/chapters/frontmatter/certification.tex"
      provides: "Unnumbered supervisor certification letter"
      contains: "To whom it may concern"
    - path: "documents/reports/latex/chapters/frontmatter/preface.tex"
      provides: "Roman-numbered front matter sequence"
      contains: "List of Abbreviations"
  key_links:
    - from: "documents/reports/latex/main.tex"
      to: "documents/reports/latex/chapters/frontmatter/certification.tex"
      via: "\\input immediately after titlepage and before preface"
      pattern: "\\\\input\\{chapters/frontmatter/titlepage\\}\\s*\\\\input\\{chapters/frontmatter/certification\\}\\s*\\\\input\\{chapters/frontmatter/preface\\}"
    - from: "documents/reports/latex/chapters/frontmatter/preface.tex"
      to: "generated TOC/LoT/LoF"
      via: "\\tableofcontents, \\listoftables, and \\listoffigures compile passes"
      pattern: "\\\\tableofcontents.*\\\\listoftables.*\\\\listoffigures"
---

<objective>
Reformat the LaTeX thesis front matter for Phase 22 so the cover page, certification letter, front matter order, abbreviations list, and abstract metadata match the USTH ICT Bachelor Thesis department template.

Purpose: Deliver COVER-01, CERT-01, FRONT-01, FRONT-02, and FRONT-03 without touching Phase 23 or Phase 24 scope.
Output: Updated `titlepage.tex`, new `certification.tex`, updated `main.tex` input order, updated `preface.tex`, and a compile-verified thesis PDF/log.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/22-cover-page-certification-letter-and-front-matter/22-CONTEXT.md
@.planning/phases/22-cover-page-certification-letter-and-front-matter/22-RESEARCH.md
@documents/reports/latex/main.tex
@documents/reports/latex/chapters/frontmatter/titlepage.tex
@documents/reports/latex/chapters/frontmatter/preface.tex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update cover page and insert certification page</name>
  <files>documents/reports/latex/main.tex, documents/reports/latex/chapters/frontmatter/titlepage.tex, documents/reports/latex/chapters/frontmatter/certification.tex</files>
  <action>In `titlepage.tex`, implement COVER-01 by replacing the stale `GRADUATION THESIS` label with `BACHELOR THESIS` and replacing the `Prepared By` block with the department-style `By Phạm Thế Minh` and `Title:` layout. Preserve the existing USTH/ICT identity, border, USTH logo, Times New Roman setup, geometry, student ID, supervisors, and date unless small local spacing changes are required to fit the department fields. Create `certification.tex` for CERT-01 as an unnumbered standalone page immediately after the cover page: begin the formal text with `To whom it may concern`, name Phạm Thế Minh, the thesis title, the Department of Information and Communication Technology, and both supervisors, and leave signature/date lines suitable for final manual signing. Do not call `\pagenumbering` or add a TOC entry in `certification.tex`. In `main.tex`, input `chapters/frontmatter/certification` between `titlepage` and `preface`. Do not modify body chapter inputs, chapter numbering, appendices, evaluation content, or slides.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$title = Get-Content -Raw 'documents/reports/latex/chapters/frontmatter/titlepage.tex'; if ($title -notmatch 'BACHELOR THESIS' -or $title -match 'GRADUATION THESIS|Prepared By' -or $title -notmatch 'By\s+Phạm Thế Minh' -or $title -notmatch 'Title:') { exit 1 }; $main = Get-Content -Raw 'documents/reports/latex/main.tex'; if ($main -notmatch '(?s)\\input\{chapters/frontmatter/titlepage\}\s*\\input\{chapters/frontmatter/certification\}\s*\\input\{chapters/frontmatter/preface\}') { exit 1 }; $cert = Get-Content -Raw 'documents/reports/latex/chapters/frontmatter/certification.tex'; if ($cert -notmatch 'To whom it may concern' -or $cert -match '\\pagenumbering|\\addcontentsline') { exit 1 }"</automated>
  </verify>
  <done>The cover source contains `BACHELOR THESIS`, explicit `By Phạm Thế Minh`, and `Title:`; `certification.tex` exists as an unnumbered letter; `main.tex` inserts certification after titlepage and before preface.</done>
</task>

<task type="auto">
  <name>Task 2: Reorder front matter, add abbreviations, and update abstract keywords</name>
  <files>documents/reports/latex/chapters/frontmatter/preface.tex</files>
  <action>In `preface.tex`, implement FRONT-01 through FRONT-03. Keep `\pagenumbering{roman}` at the start of this file so certification remains outside roman numbering, then order the front matter exactly as: `\tableofcontents`, Acknowledgements, List of Abbreviations, List of Tables, List of Figures, Abstract. Add deliberate TOC entries for the unnumbered chapters/lists where appropriate. Add a two-column List of Abbreviations table using existing `longtable`, `array`, `L{}` column support, and `booktabs`; include at least AI, API, F1, GGUF, GPU, ICT, JSONL, LLM, LoRA, NF4, NCSC, OTP, PEFT, QLoRA, USTH, VRAM, and XAI when present in the thesis. Move the Abstract to the end of the front matter, keep its body concise and truthful to current results, add exactly six English keyword phrases after a clear `Keywords:` marker, and keep the abstract body prose to no more than 250 words excluding the keyword line. Preserve the existing transition to `\pagenumbering{arabic}` immediately before the body starts. Do not restructure body chapters, define Roman numeral thesis sections, add appendices, or change slides.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$p = Get-Content -Raw 'documents/reports/latex/chapters/frontmatter/preface.tex'; $tokens = @('\tableofcontents','\chapter*{Acknowledgements}','\chapter*{List of Abbreviations}','\listoftables','\listoffigures','\chapter*{Abstract}'); $pos = $tokens | ForEach-Object { $p.IndexOf($_) }; if ($pos -contains -1) { exit 1 }; for ($i = 1; $i -lt $pos.Count; $i++) { if ($pos[$i] -le $pos[$i - 1]) { exit 1 } }; $abbrBlock = [regex]::Match($p, '(?s)\\chapter\*\{List of Abbreviations\}(.*?)\\end\{longtable\}'); if (-not $abbrBlock.Success -or $abbrBlock.Groups[1].Value -notmatch '\\begin\{longtable\}\{@\{\}p\{[^}]+\}L\{[^}]+\}@\{\}\}') { exit 1 }; foreach ($abbr in @('AI','API','F1','GGUF','GPU','ICT','JSONL','LLM','LoRA','NF4','NCSC','OTP','PEFT','QLoRA','USTH','VRAM','XAI')) { if ($abbrBlock.Groups[1].Value -notmatch ('\b' + [regex]::Escape($abbr) + '\b')) { exit 1 } }; $kw = [regex]::Match($p, 'Keywords:\}?\s*(.+)'); if (-not $kw.Success -or (($kw.Groups[1].Value -split ';').Count -ne 6)) { exit 1 }; $bodyMatch = [regex]::Match($p, '(?s)\\chapter\*\{Abstract\}.*?\\addcontentsline\{toc\}\{chapter\}\{Abstract\}\s*(.*?)\s*\\noindent\\textbf\{Keywords:'); if (-not $bodyMatch.Success) { exit 1 }; $clean = $bodyMatch.Groups[1].Value -replace '\\[a-zA-Z]+(\[[^\]]*\])?(\{[^}]*\})?', ' ' -replace '[{}]', ' '; if ([regex]::Matches($clean, ""[A-Za-z0-9]+(?:[-''][A-Za-z0-9]+)?"").Count -gt 250) { exit 1 }"</automated>
  </verify>
  <done>`preface.tex` owns the full required front matter sequence; abbreviations are present in a two-column table; Abstract has exactly six English keywords and body prose count is no more than 250 words.</done>
</task>

<task type="auto">
  <name>Task 3: Compile and prove Phase 22 scope stayed clean</name>
  <files>documents/reports/latex/main.tex, documents/reports/latex/chapters/frontmatter/titlepage.tex, documents/reports/latex/chapters/frontmatter/certification.tex, documents/reports/latex/chapters/frontmatter/preface.tex</files>
  <action>From `documents/reports/latex`, run the safe compile sequence when local tools are available: remove stale aux/list/log outputs, run XeLaTeX, run BibTeX, then run XeLaTeX twice more. Check `main.log` has zero fatal compile errors and no undefined-control-sequence or LaTeX-error regressions. If a local compiler command is unavailable, record the exact command failure in the summary and still run the static source verification commands from Tasks 1 and 2. Before finishing, inspect `git diff --name-only` and ensure Phase 22 changed only `main.tex`, `chapters/frontmatter/titlepage.tex`, `chapters/frontmatter/certification.tex`, and `chapters/frontmatter/preface.tex` among thesis sources; keep Phase 23/24 work out of scope: no body restructure, no section renumbering, no appendices, and no slides changes.</action>
  <verify>
    <automated>powershell -NoProfile -Command "Set-Location 'documents/reports/latex'; Remove-Item -ErrorAction SilentlyContinue main.aux,main.bbl,main.blg,main.lof,main.log,main.lot,main.out,main.toc; xelatex -interaction=nonstopmode main.tex; if ($LASTEXITCODE -ne 0) { exit 1 }; bibtex main; if ($LASTEXITCODE -ne 0) { exit 1 }; xelatex -interaction=nonstopmode main.tex; if ($LASTEXITCODE -ne 0) { exit 1 }; xelatex -interaction=nonstopmode main.tex; if ($LASTEXITCODE -ne 0) { exit 1 }; $log = Get-Content -Raw main.log; if ($log -match '(Fatal error|Emergency stop|Undefined control sequence|LaTeX Error)') { exit 1 }"</automated>
    <automated>powershell -NoProfile -Command "$allowed = @('documents/reports/latex/main.tex','documents/reports/latex/chapters/frontmatter/titlepage.tex','documents/reports/latex/chapters/frontmatter/certification.tex','documents/reports/latex/chapters/frontmatter/preface.tex'); $changed = git diff --name-only -- documents/reports/latex; $unexpected = $changed | Where-Object { $allowed -notcontains $_ }; if ($unexpected) { $unexpected; exit 1 }"</automated>
  </verify>
  <done>Safe compile completes or any unavailable local tool is documented; `main.log` contains zero fatal LaTeX errors; changed thesis source files stay within Phase 22 front matter scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LaTeX source -> compiled PDF | Static thesis source becomes a submitted document artifact. |
| Existing aux files -> regenerated TOC/list output | Stale generated files can misrepresent source order until cleaned and rebuilt. |
| Planning artifacts -> thesis prose | Internal workflow notes must not leak into submission-facing front matter. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Information Disclosure | `certification.tex`, `preface.tex` | mitigate | Keep certification and abstract wording formal, submission-safe, and free of GSD/internal planning language; review changed `.tex` diff before summary. |
| T-22-02 | Tampering | `main.toc`, `main.lot`, `main.lof`, `main.log` | mitigate | Delete stale aux/list/log artifacts before the final XeLaTeX/BibTeX/XeLaTeX/XeLaTeX sequence. |
| T-22-03 | Denial of Service | LaTeX compile path | mitigate | Reuse existing packages only; check `main.log` for fatal errors, emergency stops, undefined control sequences, and LaTeX errors. |
| T-22-SC | Tampering | Package installs | accept | No npm, pip, cargo, or LaTeX package install is planned; Phase 22 research found existing `longtable`, `array`, and `booktabs` support sufficient. |
</threat_model>

<verification>
Overall automated checks:
- Task 1 static source check confirms COVER-01 and CERT-01 source wiring.
- Task 2 static source check confirms FRONT-01 ordering, FRONT-02 abbreviations, and FRONT-03 keyword/count rules.
- Task 3 compile check runs from `documents/reports/latex` with XeLaTeX/BibTeX and confirms `main.log` has zero fatal errors.
- Scope guard checks changed thesis source paths are limited to Phase 22 front matter files.

Source coverage audit:

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | Phase 22 | Cover page, certification letter, and front matter match the department template | 22-01 | COVERED | Tasks 1-3 cover all Phase 22 outcomes. |
| REQ | COVER-01 | `BACHELOR THESIS` and By / Title cover layout | 22-01 | COVERED | Task 1. |
| REQ | CERT-01 | Certification letter after titlepage, before roman front matter | 22-01 | COVERED | Task 1. |
| REQ | FRONT-01 | Required front matter order | 22-01 | COVERED | Task 2. |
| REQ | FRONT-02 | Two-column List of Abbreviations | 22-01 | COVERED | Task 2. |
| REQ | FRONT-03 | Abstract 6 keywords and no more than 250 words | 22-01 | COVERED | Task 2. |
| RESEARCH | No new packages | Reuse existing LaTeX packages | 22-01 | COVERED | Tasks 1-3 avoid package churn. |
| RESEARCH | Compile sequence | Clean aux files, XeLaTeX/BibTeX/XeLaTeX/XeLaTeX, log check | 22-01 | COVERED | Task 3. |
| CONTEXT | Department Template Fidelity | Preserve identity and add required cover/certification fields | 22-01 | COVERED | Task 1. |
| CONTEXT | Front Matter Structure | `preface.tex` owns front matter after certification | 22-01 | COVERED | Task 2. |
| CONTEXT | Abbreviations and Abstract | Required abbreviations, six keywords, no more than 250 words | 22-01 | COVERED | Task 2. |
| CONTEXT | Deferred Ideas | No body restructure, Roman section work, appendices, slides sync, or evaluation-table edits | 22-01 | COVERED | Task 3 scope guard. |
</verification>

<success_criteria>
Phase 22 is complete when:
- `documents/reports/latex/chapters/frontmatter/titlepage.tex` uses `BACHELOR THESIS` and the required By / Title layout.
- `documents/reports/latex/chapters/frontmatter/certification.tex` exists and is input from `main.tex` between titlepage and preface.
- `documents/reports/latex/chapters/frontmatter/preface.tex` orders front matter as TOC -> Acknowledgements -> List of Abbreviations -> List of Tables -> List of Figures -> Abstract.
- The abbreviations table includes the required thesis acronyms in two columns.
- The Abstract has exactly six English keyword phrases and body prose count is no more than 250 words.
- The thesis compiles from `documents/reports/latex` with XeLaTeX/BibTeX safe sequence and `main.log` has zero fatal errors.
- No Phase 23/24 source areas are changed.
</success_criteria>

<output>
Create `.planning/phases/22-cover-page-certification-letter-and-front-matter/22-01-SUMMARY.md` when done.
</output>
