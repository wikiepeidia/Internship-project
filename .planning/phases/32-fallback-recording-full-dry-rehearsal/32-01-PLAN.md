---
phase: 32-fallback-recording-full-dry-rehearsal
plan: "01"
type: execute
wave: 1
depends_on:
  - 31-03
files_modified:
  - scripts/verify_phase32_fresh_process.py
  - .planning/phases/32-fallback-recording-full-dry-rehearsal/artifacts/32-fallback-operator-checklist.md
  - .planning/phases/32-fallback-recording-full-dry-rehearsal/artifacts/32-fresh-process-dry-run.json
  - .planning/phases/32-fallback-recording-full-dry-rehearsal/32-01-SUMMARY.md
autonomous: true
requirements:
  - FB-01
  - FB-02
  - FB-03
  - FB-04
requirements_addressed:
  - FB-01
  - FB-02
  - FB-03
  - FB-04
must_haves:
  truths:
    - "FB-01: the operator checklist names the exact recording evidence required and includes the two locked golden prompts verbatim for manual screen recording."
    - "FB-02: the operator checklist names the screenshot-sequence evidence required and makes clear screenshots are manual fallback artifacts, not automated Playwright proof."
    - "FB-03: the operator checklist includes a short live-to-fallback pivot rehearsal script and an evidence field for the user's rehearsal result."
    - "FB-04 substitute: the automated dry-run starts a fresh process via `scripts/START_DEMO_UI.bat`, polls the real local server, submits the locked scam and benign golden prompts through the browser UI, and records correct verdicts in `32-fresh-process-dry-run.json`."
    - "Scope honesty: the plan summary and evidence explicitly state that the fresh-process run is not a literal cold boot and does not cover OS/driver/OneDrive/Defender startup effects."
  artifacts:
    - path: ".planning/phases/32-fallback-recording-full-dry-rehearsal/artifacts/32-fallback-operator-checklist.md"
      provides: "Manual recording, screenshot, and pivot checklist with exact locked prompts."
    - path: ".planning/phases/32-fallback-recording-full-dry-rehearsal/artifacts/32-fresh-process-dry-run.json"
      provides: "Automated fresh-process launcher-backed dry-run evidence for the FB-04 substitute."
  key_links:
    - from: "scripts/verify_golden_prompts.py"
      to: "scripts/verify_phase32_fresh_process.py"
      via: "Reuses the exact DEFAULT_SCAM_TEXT and DEFAULT_BENIGN_TEXT constants."
      pattern: "DEFAULT_SCAM_TEXT"
    - from: "scripts/START_DEMO_UI.bat"
      to: "32-fresh-process-dry-run.json"
      via: "The dry-run launches the real final browser UI launcher, not the direct --no-browser verifier path."
      pattern: "python -m src.runtime.cli demo"
---

<objective>
Create and run the Phase 32 fallback-readiness proof: a manual operator checklist for video/screenshots/pivot rehearsal, plus an automated fresh-process dry-run that launches the real final `.bat` demo launcher and verifies both locked golden prompts through the browser UI.

Purpose: The defense fallback needs honest evidence without reopening the frozen demo app. This plan proves what can be automated and preserves the manual boundary for the recording, screenshot copies, and pivot rehearsal.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-fallback-recording-full-dry-rehearsal/32-CONTEXT.md
@.planning/phases/32-fallback-recording-full-dry-rehearsal/32-RESEARCH.md
@.planning/phases/28-baseline-readiness-zero-code-diagnostics/artifacts/28-golden-prompt-results.json
@.planning/phases/30-latency-diagnosis-targeted-fix/artifacts/30-fix-decision.md
@.planning/phases/31-ui-quirks-edge-cases-regression-re-check/31-03-SUMMARY.md
@scripts/verify_golden_prompts.py
@scripts/verify_ui_quirks.py
@scripts/START_DEMO_UI.bat
@src/runtime/cli.py
@src/runtime/demo.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fallback checklist and fresh-process dry-run harness</name>
  <read_first>
    .planning/phases/32-fallback-recording-full-dry-rehearsal/32-CONTEXT.md
    .planning/phases/28-baseline-readiness-zero-code-diagnostics/artifacts/28-golden-prompt-results.json
    scripts/verify_golden_prompts.py
    scripts/verify_ui_quirks.py
    scripts/START_DEMO_UI.bat
    src/runtime/cli.py
    src/runtime/demo.py
  </read_first>
  <files>scripts/verify_phase32_fresh_process.py, .planning/phases/32-fallback-recording-full-dry-rehearsal/artifacts/32-fallback-operator-checklist.md</files>
  <action>
    Create `scripts/verify_phase32_fresh_process.py`. The script must import or load the exact locked prompt constants from `scripts/verify_golden_prompts.py`, cross-check them against the Phase 28 golden-prompt artifact when that artifact exists, stop any existing listener on the target port, launch `scripts/START_DEMO_UI.bat` through `cmd.exe`, poll `http://127.0.0.1:8765/` until ready, drive a separate Playwright browser against that same local server, submit the scam and benign prompts once each, assert scam is non-benign with `bank_impersonation` and benign is exactly `benign`, write `32-fresh-process-dry-run.json`, and terminate the launcher process tree. Do not modify runtime app code or prompt text.

    Create `32-fallback-operator-checklist.md` with the exact locked prompt texts, a concise manual recording checklist for FB-01, a screenshot-sequence checklist for FB-02, a live-to-fallback pivot checklist for FB-03, and a clear note that the automated run is a fresh-process substitute rather than literal cold-boot proof.
  </action>
  <verify>
    <automated>python scripts\verify_phase32_fresh_process.py --help</automated>
    <automated>powershell -NoProfile -Command "$p='.planning\phases\32-fallback-recording-full-dry-rehearsal\artifacts\32-fallback-operator-checklist.md'; if (-not (Test-Path -LiteralPath $p)) { exit 1 }; $t=Get-Content -Raw -LiteralPath $p; if ($t -notmatch 'VIETCOMBANK' -or $t -notmatch 'VPBank Smart OTP' -or $t -notmatch 'not a literal cold boot') { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>The new script has a working `--help` path and the checklist exists with both locked prompt texts plus the explicit fresh-process-not-cold-boot caveat.</acceptance_criteria>
  <done>Harness and checklist are ready to run.</done>
</task>

<task type="auto">
  <name>Task 2: Run fresh-process dry-run and record honest evidence</name>
  <read_first>
    scripts/verify_phase32_fresh_process.py
    .planning/phases/32-fallback-recording-full-dry-rehearsal/artifacts/32-fallback-operator-checklist.md
    .planning/phases/30-latency-diagnosis-targeted-fix/artifacts/30-fix-decision.md
  </read_first>
  <files>.planning/phases/32-fallback-recording-full-dry-rehearsal/artifacts/32-fresh-process-dry-run.json, .planning/phases/32-fallback-recording-full-dry-rehearsal/32-01-SUMMARY.md</files>
  <action>
    Run `python scripts\verify_phase32_fresh_process.py --port 8765 --output .planning\phases\32-fallback-recording-full-dry-rehearsal\artifacts\32-fresh-process-dry-run.json`. Confirm the JSON reports `overall_pass: true`, `launcher.path` ending in `scripts\START_DEMO_UI.bat`, scam verdict `high-risk` with `bank_impersonation`, benign verdict `benign` with only `benign`, and an explicit `scope_notice` saying the run is not literal cold-boot coverage. Then create `32-01-SUMMARY.md` with coverage metadata that auto-passes only the automated fresh-process proof and routes FB-01/FB-02/FB-03 to human verification.
  </action>
  <verify>
    <automated>python scripts\verify_phase32_fresh_process.py --port 8765 --output .planning\phases\32-fallback-recording-full-dry-rehearsal\artifacts\32-fresh-process-dry-run.json</automated>
    <automated>python -c "import json,pathlib; p=pathlib.Path(r'.planning\phases\32-fallback-recording-full-dry-rehearsal\artifacts\32-fresh-process-dry-run.json'); data=json.loads(p.read_text(encoding='utf-8')); assert data['overall_pass'] is True; assert data['results']['golden_scam']['passed'] is True; assert data['results']['golden_benign']['passed'] is True; assert data['launcher']['path'].endswith('START_DEMO_UI.bat'); assert 'not literal cold-boot' in data['scope_notice']"</automated>
  </verify>
  <acceptance_criteria>The dry-run JSON exists, proves the real `.bat` launcher path was used, records both correct golden-prompt verdicts, and states the cold-boot limitation plainly.</acceptance_criteria>
  <done>Automated FB-04 substitute evidence is complete; FB-01/FB-02/FB-03 remain human-verification items until the user supplies recording/screenshot/pivot evidence.</done>
</task>

</tasks>

<source_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| REQ | FB-01 | Recorded video saved in two local locations | 32-01 | PARTIAL | Checklist provides exact manual evidence path; user must perform and confirm. |
| REQ | FB-02 | Screenshot sequence saved as secondary fallback | 32-01 | PARTIAL | Checklist provides exact manual evidence path; user must perform and confirm. |
| REQ | FB-03 | Live-to-fallback pivot rehearsed | 32-01 | PARTIAL | Checklist provides rehearsal script; user must perform and confirm. |
| REQ | FB-04 | Full cold-boot dry rehearsal | 32-01 | SUBSTITUTE | Automated fresh-process launcher dry-run is substituted per explicit user decision; literal cold boot remains uncovered. |
| CONTEXT | D-32-01 | Do not automate video/screenshots/pivot | 32-01 | COVERED | Checklist only; no automated capture tooling. |
| CONTEXT | D-32-02 | Exercise `START_DEMO_UI.bat` | 32-01 | COVERED | Fresh-process harness launches the real `.bat`. |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Existing local demo process -> fresh process | The dry-run stops any listener on the target port before launching the final `.bat` path. |
| Manual evidence -> phase completion | Video/screenshot/pivot completion relies on user-supplied evidence and must not be inferred from automation. |
| Playwright verifier -> frozen demo app | Automation drives the existing UI only; it must not change backend, UI, prompt, or model behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-01 | Repudiation | Manual fallback evidence | medium | mitigate | Checklist records exact expected evidence; SUMMARY marks these as human_judgment true until confirmed. |
| T-32-02 | Tampering | Locked prompt text | high | mitigate | Script loads prompts from `verify_golden_prompts.py` and cross-checks Phase 28 artifact text. |
| T-32-03 | Denial of Service | Existing demo process on port 8765 | medium | mitigate | Script detects and stops existing port listeners before launching the fresh process, then cleans up its process tree. |
| T-32-04 | Misrepresentation | Cold-boot claim | high | mitigate | Evidence and SUMMARY explicitly state fresh-process substitute is not literal cold-boot coverage. |
</threat_model>

<verification>
Phase 32 automated verification passes when `32-fresh-process-dry-run.json` reports `overall_pass: true`, identifies `scripts/START_DEMO_UI.bat` as the launcher, records correct verdicts for both locked golden prompts, and states the not-literal-cold-boot limitation. Phase-level completion still requires human verification for FB-01, FB-02, and FB-03.
</verification>

<success_criteria>
- A manual fallback checklist exists with exact locked golden prompts and evidence fields for recording, screenshots, and pivot rehearsal.
- The fresh-process dry-run script launches the final `.bat` UI launcher and verifies both golden prompts through the browser UI.
- The dry-run JSON artifact records correct verdicts and the cold-boot limitation.
- The summary routes manual fallback deliverables to human verification instead of auto-passing them.
</success_criteria>

<output>
Create `.planning/phases/32-fallback-recording-full-dry-rehearsal/32-01-SUMMARY.md` when done.
</output>

## Artifacts This Plan Produces

- `scripts/verify_phase32_fresh_process.py`
- `.planning/phases/32-fallback-recording-full-dry-rehearsal/artifacts/32-fallback-operator-checklist.md`
- `.planning/phases/32-fallback-recording-full-dry-rehearsal/artifacts/32-fresh-process-dry-run.json`
- `.planning/phases/32-fallback-recording-full-dry-rehearsal/32-01-SUMMARY.md`

