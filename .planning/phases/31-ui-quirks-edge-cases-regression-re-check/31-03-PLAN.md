---
phase: "31-ui-quirks-edge-cases-regression-re-check"
plan: "03"
type: execute
wave: 2
depends_on:
  - 31-01
  - 31-02
files_modified:
  - src/runtime/demo_assets/demo.js
  - src/runtime/demo_assets/demo.css
  - src/runtime/demo_assets/i18n.js
  - src/runtime/demo_assets/index.html
  - tests/runtime/test_demo.py
  - .planning/phases/31-ui-quirks-edge-cases-regression-re-check/artifacts/31-uiq04-triage.md
  - .planning/phases/31-ui-quirks-edge-cases-regression-re-check/artifacts/31-ui-quirks-after-fix.json
  - .planning/phases/31-ui-quirks-edge-cases-regression-re-check/artifacts/31-golden-regression-results.json
  - .planning/phases/31-ui-quirks-edge-cases-regression-re-check/artifacts/31-regression-results.md
autonomous: true
requirements:
  - UIQ-01
  - UIQ-02
  - UIQ-04
requirements_addressed:
  - UIQ-01
  - UIQ-02
  - UIQ-04
must_haves:
  truths:
    - "UIQ-01: after any UIQ-04 fix, the edge-case matrix passes with no crash, hang, orphaned typing indicator, or stuck disabled button."
    - "UIQ-02/D-04: rapid repeated submit leaves `completed_analyze_response_count == 1`, no completed superseded response, exactly one surviving terminal bubble, no AbortError error bubble, zero typing nodes, and an enabled analyze button."
    - "UIQ-04/D-01: every quirk from the verifier is either fixed as app-origin behavior or documented as non-app/browser noise, with backend and data-slot templates intact."
    - "D-05: Phase 28 golden prompts and Phase 29 touched test suites pass after all Phase 31 changes."
  artifacts:
    - path: ".planning/phases/31-ui-quirks-edge-cases-regression-re-check/artifacts/31-uiq04-triage.md"
      provides: "Quirk classification, SOURCE_LANG_VI finding, app-origin decisions, and fix notes."
    - path: ".planning/phases/31-ui-quirks-edge-cases-regression-re-check/artifacts/31-ui-quirks-after-fix.json"
      provides: "Passing UIQ-01/UIQ-02 verifier run after fixes or documented non-app triage."
    - path: ".planning/phases/31-ui-quirks-edge-cases-regression-re-check/artifacts/31-golden-regression-results.json"
      provides: "Fresh five-run scam/benign golden-prompt regression evidence."
    - path: ".planning/phases/31-ui-quirks-edge-cases-regression-re-check/artifacts/31-regression-results.md"
      provides: "Final command evidence for golden prompts, focused runtime tests, and doctor readiness."
  key_links:
    - from: "31-ui-quirks-results.json"
      to: "src/runtime/demo_assets/*"
      via: "Only app-origin verifier failures justify vanilla asset changes."
      pattern: "overall_pass"
    - from: "src/runtime/demo_assets/index.html"
      to: "tests/runtime/test_demo.py"
      via: "Template and static-contract tests preserve data-slot internals and frozen API behavior."
      pattern: "data-slot"
    - from: "31-golden-regression-results.json"
      to: "Phase 28 locked prompts"
      via: "Existing golden-prompt verifier logic runs five scam and benign browser passes."
      pattern: "golden_scam"
---

<objective>
Close Phase 31 by triaging/fixing confirmed UI quirks, rerunning the verifier to green, and recording final regression evidence.

Purpose: Plans 31-01 and 31-02 create the tools and entrypoint fix. This plan consumes their outputs, handles UIQ-04 without widening scope, and proves D-05 regression checks still pass.
Output: quirk triage notes, after-fix verifier JSON, golden-prompt regression JSON, and final regression results.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-ui-quirks-edge-cases-regression-re-check/31-CONTEXT.md
@.planning/phases/31-ui-quirks-edge-cases-regression-re-check/31-RESEARCH.md
@.planning/phases/31-ui-quirks-edge-cases-regression-re-check/31-PATTERNS.md
@.planning/phases/31-ui-quirks-edge-cases-regression-re-check/31-UI-SPEC.md
@.planning/phases/31-ui-quirks-edge-cases-regression-re-check/31-01-SUMMARY.md
@.planning/phases/31-ui-quirks-edge-cases-regression-re-check/31-02-SUMMARY.md
@.planning/phases/28-baseline-readiness-zero-code-diagnostics/28-01-SUMMARY.md
@.planning/phases/28-baseline-readiness-zero-code-diagnostics/artifacts/28-golden-prompt-results.json
@.planning/phases/29-environment-parity-offline-verification/29-04-SUMMARY.md
@.planning/phases/29-environment-parity-offline-verification/29-VERIFICATION.md
@.planning/phases/30-latency-diagnosis-targeted-fix/30-02-SUMMARY.md
@scripts/verify_ui_quirks.py
@scripts/verify_golden_prompts.py
@src/runtime/demo_assets/demo.js
@src/runtime/demo_assets/demo.css
@src/runtime/demo_assets/i18n.js
@src/runtime/demo_assets/index.html
@tests/runtime/test_demo.py
@tests/runtime/test_local_model.py
@tests/runtime/test_cli.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Triage and fix UIQ-04 app-origin quirks</name>
  <files>src/runtime/demo_assets/demo.js, src/runtime/demo_assets/demo.css, src/runtime/demo_assets/i18n.js, src/runtime/demo_assets/index.html, tests/runtime/test_demo.py, .planning/phases/31-ui-quirks-edge-cases-regression-re-check/artifacts/31-uiq04-triage.md, .planning/phases/31-ui-quirks-edge-cases-regression-re-check/artifacts/31-ui-quirks-after-fix.json</files>
  <behavior>
    - Test 1: verifier failures are classified as app-origin, backend-origin, environment-origin, or non-app browser/profile noise in `31-uiq04-triage.md`.
    - Test 2: SOURCE_LANG_VI is fixed only when the clean Playwright artifact or source search ties it to app code; otherwise the triage note documents the non-app stop condition per D-01.
    - Test 3: confirmed app-origin double-submit failures are fixed in `demo.js` by preserving request-local controller ownership so an aborted request cannot clear the surviving request's busy state.
    - Test 4: any CSS/copy/template fix preserves vanilla assets, approved UI-SPEC tokens, `data-slot` internals, and the frozen `/api/analyze` contract.
  </behavior>
  <action>Read the Plan 31-01 `31-ui-quirks-results.json` artifact and classify every failed case and console/page-error finding into `31-uiq04-triage.md`. For D-01, compare the clean Playwright console capture against local source search for the Phase 29 console token; if no local source or clean-browser app-origin console exists, document non-app/browser-profile noise and stop the hunt. When a finding is app-origin, make the smallest targeted change in existing vanilla assets only. The expected re-entrant race fix, when needed, is in `src/runtime/demo_assets/demo.js`: keep a request-local controller reference, abort the prior controller before the new fetch, swallow AbortError, remove only that request's typing node, and clear `currentController`/busy state only when the finishing request is still the current one. Wire Plan 31-01's strengthened D-04 criteria into the after-fix verifier's `overall_pass`: it must be false unless the `double_submit` case has `completed_analyze_response_count == 1`, no completed superseded response, no AbortError error bubble, zero typing nodes, and an enabled analyze button. When a long-text layout issue is confirmed, reuse existing CSS custom properties and `overflow-wrap` patterns; do not add new hex colors, spacing constants, font sizes, or weights. When copy is needed, add it in `i18n.js` and wire it from JS/HTML; do not hardcode new user-facing copy in templates. Do not modify `src/runtime/demo.py`, `src/runtime/service.py`, `/api/analyze` request/response shape, or cloned template `data-slot` internals.</action>
  <verify>
    <automated>python scripts\verify_ui_quirks.py --port 8766 --output .planning\phases\31-ui-quirks-edge-cases-regression-re-check\artifacts\31-ui-quirks-after-fix.json</automated>
    <automated>python -m pytest tests\runtime\test_demo.py -q</automated>
    <automated>powershell -NoProfile -Command "if (-not (Test-Path '.planning\phases\31-ui-quirks-edge-cases-regression-re-check\artifacts\31-uiq04-triage.md')) { exit 1 }; python -c \"import json,pathlib; p=pathlib.Path(r'.planning\phases\31-ui-quirks-edge-cases-regression-re-check\artifacts\31-ui-quirks-after-fix.json'); data=json.loads(p.read_text(encoding='utf-8')); assert data.get('overall_pass') is True; case=next(c for c in data.get('cases', []) if c.get('name') == 'double_submit'); assert case.get('completed_analyze_response_count') == 1; assert case.get('completed_superseded_response_count', 0) == 0; assert case.get('abort_error_bubble_count', 0) == 0; assert case.get('typing_count') == 0; assert case.get('button_disabled') is False\""</automated>
  </verify>
  <done>`31-uiq04-triage.md` classifies every finding, `31-ui-quirks-after-fix.json` reports `overall_pass: true`, and `tests/runtime/test_demo.py` remains green with backend/templates intact.</done>
</task>

<task type="auto">
  <name>Task 2: Record final Phase 28-30 regression evidence</name>
  <files>.planning/phases/31-ui-quirks-edge-cases-regression-re-check/artifacts/31-golden-regression-results.json, .planning/phases/31-ui-quirks-edge-cases-regression-re-check/artifacts/31-regression-results.md</files>
  <action>Run the final D-05 regression set after Task 1 and Plan 31-02 are complete. Use the existing `scripts/verify_golden_prompts.py` logic for five scam and five benign browser runs, but direct its `RESULTS_PATH` to `.planning/phases/31-ui-quirks-edge-cases-regression-re-check/artifacts/31-golden-regression-results.json` so the Phase 28 source artifact is not overwritten. Then run `python -m pytest tests\runtime\test_local_model.py tests\runtime\test_demo.py tests\runtime\test_cli.py -q` and `python -m src.runtime.cli doctor`. Create `31-regression-results.md` with command lines, exit codes, stable scam/benign verdict summary, pytest pass summary, doctor READY line, and an explicit note that Phase 30 had `NO_FIX_APPLIED` so there is no source diff to regression-test beyond demo startup and serving. Do not alter model thresholds, golden prompt texts, backend service code, or test expectations to force a pass.</action>
  <verify>
    <automated>python -c "import importlib.util,pathlib; spec=importlib.util.spec_from_file_location('verify_golden_prompts', pathlib.Path('scripts/verify_golden_prompts.py')); module=importlib.util.module_from_spec(spec); spec.loader.exec_module(module); module.RESULTS_PATH=pathlib.Path(r'.planning\phases\31-ui-quirks-edge-cases-regression-re-check\artifacts\31-golden-regression-results.json'); raise SystemExit(module.main(['--runs','5','--port','8766']))"</automated>
    <automated>python -m pytest tests\runtime\test_local_model.py tests\runtime\test_demo.py tests\runtime\test_cli.py -q</automated>
    <automated>python -m src.runtime.cli doctor</automated>
    <automated>python -c "import json,pathlib; p=pathlib.Path(r'.planning\phases\31-ui-quirks-edge-cases-regression-re-check\artifacts\31-golden-regression-results.json'); data=json.loads(p.read_text(encoding='utf-8')); assert data['golden_scam']['stable'] is True; assert data['golden_benign']['stable'] is True"</automated>
    <automated>python -c "import pathlib,re; p=pathlib.Path(r'.planning\phases\31-ui-quirks-edge-cases-regression-re-check\artifacts\31-regression-results.md'); text=p.read_text(encoding='utf-8'); assert re.search(r'(?i)command', text); assert re.search(r'(?i)exit code', text); assert re.search(r'(?is)scam.*stable|stable.*scam', text); assert re.search(r'(?is)benign.*stable|stable.*benign', text); assert re.search(r'(?is)pytest.*pass|pass.*pytest', text); assert 'READY' in text; assert 'NO_FIX_APPLIED' in text"</automated>
  </verify>
  <done>Golden prompts are stable 5/5 each, focused runtime/CLI tests pass, doctor reports READY, and `31-regression-results.md` records command lines, exit codes, stable scam/benign summary, pytest pass summary, doctor READY, and the Phase 30 `NO_FIX_APPLIED` note.</done>
</task>

</tasks>

<source_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | - | Demo handles edge-case matrix, CLI confusion is resolved, and Phases 28-30 fixes do not regress | 31-01, 31-02, 31-03 | COVERED | Verifier, CLI clarity, final regression evidence. |
| REQ | UIQ-01 | Full edge-case matrix re-tested with no crash/hang | 31-01, 31-03 | COVERED | Real-demo verifier plus after-fix pass. |
| REQ | UIQ-02 | Rapid double-submit guard re-verified | 31-01, 31-03 | COVERED | Automated Playwright form/Enter re-entry test plus after-fix pass. |
| REQ | UIQ-03 | CLI entrypoint confusion resolved | 31-02 | COVERED | Help text and `.bat` launchers per D-02. |
| REQ | UIQ-04 | UI quirks catalogued/fixed without backend/template breakage | 31-01, 31-03 | COVERED | Console capture, triage note, conditional vanilla-asset fixes, test_demo. |
| RESEARCH | R-01 | Use one new real-demo Playwright verifier, no new deps | 31-01 | COVERED | `scripts/verify_ui_quirks.py`, existing Playwright. |
| RESEARCH | R-02 | Preserve vanilla assets, frozen `/api/analyze`, `data-slot`, UI-SPEC tokens | 31-01, 31-03 | COVERED | Constraints repeated in actions and verified by test_demo. |
| RESEARCH | R-03 | Add argparse help tests and `.bat` launchers | 31-02 | COVERED | `tests/runtime/test_cli.py`, two launchers. |
| RESEARCH | R-04 | Final golden-prompt and focused pytest regressions | 31-03 | COVERED | D-05 commands and artifacts. |
| CONTEXT | D-01 | Reproduce/triage SOURCE_LANG_VI and stop on non-app origin | 31-01, 31-03 | COVERED | Console capture and triage note. |
| CONTEXT | D-02 | Ship launchers and help text without CLI restructure | 31-02 | COVERED | Additive parser wording plus launchers. |
| CONTEXT | D-03 | Edge cases through real web demo using Phase 28 Playwright pattern | 31-01, 31-03 | COVERED | Real browser verifier and after-fix pass. |
| CONTEXT | D-04 | Automated rapid double-submit verification | 31-01, 31-03 | COVERED | Form/Enter repeated submit case and after-fix pass. |
| CONTEXT | D-05 | Re-run golden prompts and Phase 29 touched tests | 31-03 | COVERED | Golden regression JSON, local_model/demo/cli pytest, doctor. |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> demo assets | User input and fetch lifecycle are handled by vanilla JS before crossing to `/api/analyze`. |
| Demo assets -> frozen backend | UI fixes must not change the backend JSON contract or service behavior. |
| Evidence commands -> filesystem | Regression artifacts store verdict/test/doctor evidence for phase closeout. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-03-T | Tampering | `src/runtime/demo_assets/*` | high | mitigate | Restrict UIQ-04 fixes to existing vanilla assets and rerun `tests/runtime/test_demo.py` plus the after-fix verifier. |
| T-31-03-I | Information Disclosure | Phase 31 evidence artifacts | medium | mitigate | Record verdicts, counts, and command outputs; avoid arbitrary raw pasted text beyond locked fixtures and deterministic test payloads. |
| T-31-03-D | Denial of Service | Double-submit and long-input fixes | medium | mitigate | Require verifier `overall_pass: true`, zero orphaned typing nodes, button reset, bounded timeouts, and process cleanup. |
| T-31-03-R | Repudiation | Regression evidence | medium | mitigate | Save command lines, exit codes, stable verdict booleans, pytest summaries, and doctor READY output in phase-local artifacts. |
| T-31-03-SC | Tampering | Package supply chain | low | accept | No package-manager install is planned; all commands use existing project dependencies. |
</threat_model>

<verification>
Overall phase verification is complete when `scripts\verify_ui_quirks.py` reports `overall_pass: true` with the strengthened double-submit assertions, five-run scam/benign golden regression is stable, `python -m pytest tests\runtime\test_local_model.py tests\runtime\test_demo.py tests\runtime\test_cli.py -q` passes, `python -m src.runtime.cli doctor` reports READY, and `31-regression-results.md` contains command lines, exit codes, stable scam/benign summary, pytest pass summary, doctor READY, and the Phase 30 `NO_FIX_APPLIED` note.
</verification>

<success_criteria>
- UIQ-01: empty, very-long, malformed/off-topic, and mixed Vietnamese-English browser cases finish without crash/hang.
- UIQ-02: rapid repeated submit records `completed_analyze_response_count == 1`, no completed superseded response, no duplicate terminal bubbles, no AbortError bubble, zero orphaned typing indicators, and no stuck disabled button.
- UIQ-04: SOURCE_LANG_VI and all verifier findings are either fixed or documented as non-app origin, with backend and templates intact.
- D-05: Phase 28 golden prompt stability and Phase 29 touched test suites remain green after Phase 31 changes.
</success_criteria>

<output>
Create `.planning/phases/31-ui-quirks-edge-cases-regression-re-check/31-03-SUMMARY.md` when done.
</output>
