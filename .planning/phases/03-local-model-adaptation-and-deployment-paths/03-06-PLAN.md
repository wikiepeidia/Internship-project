---
phase: 03-local-model-adaptation-and-deployment-paths
plan: 06
type: execute
wave: 5
depends_on:
  - 03-02
  - 03-04
files_modified:
  - src/runtime/analyzers/accelerated.py
  - tests/runtime/test_accelerated_backend.py
  - tests/runtime/test_runtime_profiles.py
autonomous: true
gap_closure: true
requirements:
  - RUN-03

must_haves:
  truths:
    - "The accelerated local profile uses the locked runner-up `qwen3.5-4b` artifact for inference instead of scaffolded regex and keyword cues or older 4B/7B selection scaffolds"
    - "The accelerated backend keeps the shipped `AnalysisResult` and `DoctorStatus` contracts stable for Phase 4 consumers"
    - "The accelerated doctor only reports READY when the runner-up artifact and its local model-loading prerequisites are actually usable"
  artifacts:
    - path: "src/runtime/analyzers/accelerated.py"
      provides: "Real runner-up-backed accelerated analyzer with load-aware doctor and contract-stable result mapping"
      exports: ["AcceleratedAnalyzer"]
    - path: "tests/runtime/test_accelerated_backend.py"
      provides: "Regression checks proving analyze uses the model path rather than keyword heuristics"
      contains: "test_accelerated_analyze_uses_model_output_not_keyword_scaffold"
    - path: "tests/runtime/test_runtime_profiles.py"
      provides: "Runtime-profile checks proving accelerated selection remains explicit, local-only, and aligned with the locked 4B pair"
      contains: "test_accelerated_profile_stays_explicit_when_model_loader_is_mocked"
  key_links:
    - from: "src/runtime/analyzers/accelerated.py"
      to: "src/model_adaptation/registry.py"
      via: "the analyzer resolves the locked runner-up artifact from the local registry before loading inference resources"
      pattern: "runner_up_id"
    - from: "src/runtime/analyzers/accelerated.py"
      to: "src/runtime/contracts.py"
      via: "model outputs are mapped back into the existing typed `AnalysisResult` contract"
      pattern: "AnalysisResult"
    - from: "tests/runtime/test_accelerated_backend.py"
      to: "src/runtime/analyzers/accelerated.py"
      via: "regression tests fail if keyword scaffolding returns without calling the model loader or inference path"
      pattern: "analyze"
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD034 MD040 MD041 MD046 MD055 MD056 MD060 -->

<objective>
Close the accelerated runtime gap by replacing the scaffolded heuristic analyzer with real local inference against the trained runner-up artifact while preserving the stable Phase 2 runtime contract.

Purpose: `RUN-03` is still only half-shipped. The runtime profile exists and routes correctly, but `AcceleratedAnalyzer.analyze()` still computes risk from regex and keyword cues instead of the trained runner-up adapter. This plan turns the accelerated path into a real local backend without reopening broader Phase 4 DET/XAI work.

Output: a real runner-up-backed `AcceleratedAnalyzer`, plus tests that prove inference behavior comes from the model path rather than the old scaffold logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-local-model-adaptation-and-deployment-paths/03-UAT.md
@.planning/debug/phase3-accelerated-runtime-gap.md
@.planning/phases/03-local-model-adaptation-and-deployment-paths/03-CONTEXT.md
@.planning/phases/03-local-model-adaptation-and-deployment-paths/03-RESEARCH.md
@.planning/phases/03-local-model-adaptation-and-deployment-paths/03-02-SUMMARY.md
@.planning/phases/03-local-model-adaptation-and-deployment-paths/03-04-SUMMARY.md
@src/runtime/analyzers/accelerated.py
@src/runtime/contracts.py
@src/runtime/service.py

<interfaces>
From src/runtime/analyzers/accelerated.py:
```python
class AcceleratedAnalyzer:
    def doctor(self) -> DoctorStatus: ...
    def analyze(self, request: AnalysisRequest) -> AnalysisResult: ...
```

From src/runtime/contracts.py:
```python
class AnalysisResult(BaseModel):
    risk_tier: RiskTier
    summary: str
    top_cues: list[SuspiciousCue] = Field(default_factory=list, max_length=3)
    backend_name: str
    provisional: bool = True
    normalized_text: str | None = None
```

</interfaces>
</context>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| normalized text -> accelerated analyzer | untrusted message text crosses into the local inference backend |
| model registry -> local loader | the backend must resolve only the locked runner-up artifact and local model files |
| model output -> runtime contract | model outputs must be mapped back into the existing typed schema without contract drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | T | src/runtime/analyzers/accelerated.py | mitigate | Remove the scaffolded regex-first decision logic and make inference load the registered runner-up artifact before computing risk. |
| T-03-06-02 | I | src/runtime/analyzers/accelerated.py | mitigate | Keep local-only loading with local artifact paths and no download fallback when model files are missing. |
| T-03-06-03 | D | tests/runtime/test_accelerated_backend.py | mitigate | Add mocked loader tests that fail when the analyzer falls back to keyword heuristics or reports READY without usable local model resources. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the accelerated heuristic scaffold with a real runner-up-backed inference path</name>
  <files>
    src/runtime/analyzers/accelerated.py,
    tests/runtime/test_accelerated_backend.py
  </files>
  <read_first>
    .planning/debug/phase3-accelerated-runtime-gap.md,
    src/runtime/analyzers/accelerated.py,
    src/model_adaptation/registry.py,
    src/runtime/contracts.py,
    tests/runtime/test_accelerated_backend.py
  </read_first>
  <behavior>
    - Test: the analyzer resolves the locked runner-up artifact from the registry and attempts a real local model load instead of applying regex cues
    - Test: a mocked model output can drive a high-risk result on text that does not contain the old heuristic keywords, proving the result comes from model inference
    - Test: a text that does contain the old heuristic keywords can still resolve to a benign or suspicious result when the mocked model output says so, proving the old scaffold no longer controls risk
    - Test: doctor reports NOT READY if the runner-up artifact exists but the local model-loading prerequisites are unusable
    - Test: touched accelerated-backend fixtures encode `qwen3-4b-instruct-2507` as baseline winner and `qwen3.5-4b` as runner-up, matching D-15 and D-16 instead of the older 4B/7B pairing
  </behavior>
  <action>
    Rework `src/runtime/analyzers/accelerated.py` so the accelerated backend loads the trained runner-up artifact for inference, per D-16, D-17, and D-18. Resolve local artifact paths from the existing model registry, keep loading strictly local-only, and map the resulting model output back into the current `AnalysisResult` fields.

    Do not keep the current regex and keyword scaffold as a hidden fallback path. If local inference resources are missing, the analyzer should fail closed through doctor/setup guidance instead of quietly returning heuristic cues.

    While touching the accelerated suite, refresh any selection-sensitive fixtures from the older `qwen3.5-4b` / `qwen2.5-7b-instruct` pairing to the locked 4B pair from D-15 and D-16 so the mocked load path reflects the shipped Phase 3 state.

    Add tests that mock the model loader and inference call so the backend can be verified in-repo without requiring a live GPU run during unit testing.
  </action>
  <verify>
    <automated>C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python312/python.exe -m pytest tests/runtime/test_accelerated_backend.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/runtime/analyzers/accelerated.py` no longer imports or uses `re` for the top-level decision path
    - `tests/runtime/test_accelerated_backend.py` contains `def test_accelerated_analyze_uses_model_output_not_keyword_scaffold`
    - `tests/runtime/test_accelerated_backend.py` contains `def test_accelerated_doctor_fails_when_runner_up_resources_cannot_load`
    - `tests/runtime/test_accelerated_backend.py` stages `qwen3-4b-instruct-2507` as baseline winner and `qwen3.5-4b` as runner-up in its selection fixtures
    - `C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python312/python.exe -m pytest tests/runtime/test_accelerated_backend.py -q` exits with code 0
  </acceptance_criteria>
  <done>The accelerated analyzer uses the trained runner-up artifact for inference and fails closed when local model resources are not actually usable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve explicit accelerated-profile routing and prove the shipped runtime contract did not drift</name>
  <files>
    tests/runtime/test_runtime_profiles.py
  </files>
  <read_first>
    src/runtime/service.py,
    tests/runtime/test_runtime_profiles.py,
    tests/runtime/test_cli.py,
    tests/runtime/test_service.py
  </read_first>
  <behavior>
    - Test: selecting `accelerated-local` still routes explicitly to `AcceleratedAnalyzer`
    - Test: the accelerated profile stays local-only and does not suggest cloud fallback when the model loader is unavailable
    - Test: the result contract seen by service-level callers remains compatible with the existing Phase 2 schema after the accelerated backend becomes real
    - Test: runtime-profile fixtures use the locked 4B baseline/runner-up pair so explicit routing checks no longer depend on the older 7B runner-up scaffold
  </behavior>
  <action>
    Update `tests/runtime/test_runtime_profiles.py` so the runtime-selection path continues to prove explicit accelerated routing, local-only behavior, and contract stability after the analyzer becomes a real inference backend.

    Refresh the selection-sensitive runtime-profile fixtures to the locked Phase 3 pair (`qwen3-4b-instruct-2507` baseline winner, `qwen3.5-4b` runner-up) while keeping the task test-focused unless execution reveals that a tiny service-layer repair is required. The gap diagnosis says the routing path is already correct; the proof needed here is that the real inference backend still respects the shipped contract when chosen through the normal runtime profile flow.
  </action>
  <verify>
    <automated>C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python312/python.exe -m pytest tests/runtime/test_runtime_profiles.py tests/runtime/test_cli.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/runtime/test_runtime_profiles.py` contains `def test_accelerated_profile_stays_explicit_when_model_loader_is_mocked`
    - `tests/runtime/test_runtime_profiles.py` asserts no cloud fallback wording is introduced for accelerated mode
    - `tests/runtime/test_runtime_profiles.py` stages `qwen3-4b-instruct-2507` as baseline winner and `qwen3.5-4b` as runner-up in its selection fixtures
    - `C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python312/python.exe -m pytest tests/runtime/test_runtime_profiles.py tests/runtime/test_cli.py -q` exits with code 0
  </acceptance_criteria>
  <done>The accelerated profile remains explicit in the shipped runtime path, and the contract seen by downstream callers stays stable after real inference replaces the scaffold.</done>
</task>

</tasks>

<verification>
- `C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python312/python.exe -m pytest tests/runtime/test_accelerated_backend.py -q` passes
- `C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python312/python.exe -m pytest tests/runtime/test_runtime_profiles.py tests/runtime/test_cli.py -q` passes
- Doctor smoke after implementation: `MODEL_REGISTRY_PATH="D:/PROJEct/AI MODELS/manifests/model-registry.json" MODEL_ARTIFACT_ROOT="D:/PROJEct/AI MODELS" RUNTIME_BACKEND=accelerated RUNTIME_PROFILE=accelerated-local C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python312/python.exe -m src.runtime.cli doctor` reports READY against the off-repo runner-up artifact set
- Operator smoke check after implementation: `MODEL_REGISTRY_PATH="D:/PROJEct/AI MODELS/manifests/model-registry.json" MODEL_ARTIFACT_ROOT="D:/PROJEct/AI MODELS" RUNTIME_BACKEND=accelerated RUNTIME_PROFILE=accelerated-local C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python312/python.exe -m src.runtime.cli analyze --text "<sample>"` uses the trained runner-up artifact and preserves the current structured output schema
</verification>

<success_criteria>
The accelerated local runtime is no longer a heuristic placeholder: it loads the locked runner-up artifact for real local inference, updates the touched runtime suites to the locked 4B winner/runner-up pair, keeps explicit profile routing, and preserves the stable request/result contracts that Phase 4 depends on.
</success_criteria>

<output>
After completion, create `.planning/phases/03-local-model-adaptation-and-deployment-paths/03-06-SUMMARY.md`
</output>

<!-- markdownlint-enable MD022 MD031 MD032 MD033 MD034 MD040 MD041 MD046 MD055 MD056 MD060 -->