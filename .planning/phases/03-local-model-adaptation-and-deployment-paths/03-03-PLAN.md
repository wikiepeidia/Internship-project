---
phase: 03-local-model-adaptation-and-deployment-paths
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/config/settings.py
  - src/model_adaptation/convert.py
  - src/runtime/analyzers/gguf.py
  - src/runtime/service.py
  - src/runtime/doctor.py
  - tests/model_adaptation/test_convert.py
  - tests/runtime/test_gguf_backend.py
  - tests/runtime/test_runtime_profiles.py
autonomous: true
requirements:
  - RUN-02

must_haves:
  truths:
    - "Phase 3 provides a real GGUF artifact path for both the pilot-selected 4B baseline winner and the runner-up, while pinning the laptop runtime baseline to the 4B winner"
    - "The GGUF runtime integrates through the existing AnalyzerBackend seam and returns the same typed Phase 2 result contract"
    - "Runtime profile selection is explicit and local-first; no cloud or silent remote fallback is introduced"
    - "Doctor checks expose GGUF readiness, artifact availability, and profile selection clearly enough for a laptop-baseline user to diagnose failures"
  artifacts:
    - path: "src/model_adaptation/convert.py"
      provides: "Adapter-to-GGUF conversion requests, metadata staging, and local artifact registration"
      exports: ["build_gguf_request", "convert_to_gguf", "register_gguf_artifact"]
    - path: "src/runtime/analyzers/gguf.py"
      provides: "Laptop-baseline GGUF runtime backend implementing the AnalyzerBackend contract"
      exports: ["GGUFAnalyzer"]
    - path: "tests/model_adaptation/test_convert.py"
      provides: "Executable checks for conversion requests, dry-run conversion, and GGUF artifact metadata"
      contains: "test_build_gguf_request_uses_adapter_artifact"
    - path: "tests/runtime/test_gguf_backend.py"
      provides: "Executable checks for contract-compatible GGUF runtime behavior"
      contains: "test_gguf_backend_returns_analysis_result_contract"
    - path: "tests/runtime/test_runtime_profiles.py"
      provides: "Executable checks for explicit backend/profile selection and fail-closed behavior"
      contains: "test_runtime_service_rejects_unknown_runtime_profile"
  key_links:
    - from: "src/model_adaptation/convert.py"
      to: "src/model_adaptation/training.py"
      via: "GGUF conversion uses registered adapter artifacts from the training plan"
      pattern: "adapter"
    - from: "src/runtime/analyzers/gguf.py"
      to: "src/runtime/analyzers/base.py"
      via: "GGUF backend must implement the existing analyzer protocol"
      pattern: "AnalyzerBackend"
    - from: "src/runtime/service.py"
      to: "src/config/settings.py"
      via: "runtime profile and backend selection remain explicit through settings"
      pattern: "runtime_backend"
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD034 MD040 MD041 MD046 MD055 MD056 MD060 -->

<objective>
Implement the GGUF conversion and laptop-baseline runtime path for the pilot-selected 4B baseline winner while preserving runner-up artifact coverage and the existing Phase 2 runtime contract.

Purpose: `RUN-02` is the concrete delivery point that turns the selected 4B baseline adapter into a usable local model path on consumer laptop hardware. This plan wires Phase 3 artifacts into the shipped runtime without forking the CLI or weakening the local-only fail-closed posture.

Output: GGUF conversion helpers for both selected models, a GGUF analyzer backend pinned to the 4B laptop baseline, explicit runtime profile settings, updated doctor checks, and passing tests for conversion, profile selection, CLI regression, and contract-compatible inference results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-local-model-adaptation-and-deployment-paths/03-CONTEXT.md
@.planning/phases/03-local-model-adaptation-and-deployment-paths/03-RESEARCH.md
@.planning/phases/03-local-model-adaptation-and-deployment-paths/03-VALIDATION.md
@.planning/phases/03-local-model-adaptation-and-deployment-paths/03-01-PLAN.md
@.planning/phases/03-local-model-adaptation-and-deployment-paths/03-02-PLAN.md
@src/config/settings.py
@src/runtime/contracts.py
@src/runtime/analyzers/base.py
@src/runtime/service.py
@src/runtime/doctor.py

<interfaces>
From src/runtime/contracts.py:
```python
class AnalysisRequest(BaseModel):
    text: str
    channel: ChannelName = "unknown"

class AnalysisResult(BaseModel):
    risk_tier: RiskTier
    summary: str
    top_cues: list[SuspiciousCue] = Field(default_factory=list, max_length=3)
    backend_name: str
```

From src/runtime/service.py:
```python
class RuntimeService:
    def analyze_text(self, text: str, channel: ChannelName = "unknown") -> AnalysisResult: ...
```

From src/model_adaptation/training.py (from Plan 02):
```python
def save_adapter_artifacts(...): ...
```

</interfaces>
</context>

<threat_model>

- Asset: the GGUF baseline is the first user-usable local model path promised by Phase 3.
- Threat: conversion logic exists but is not tied to registered training artifacts, so the runtime cannot find or trust the right files.
- Threat: the GGUF backend bypasses the Phase 2 contract and starts returning a second response shape.
- Threat: runtime selection becomes implicit, hidden, or fails open toward a non-local backend.
- Required control in this plan: GGUF artifacts must be registered, backend selection must stay explicit, and the runtime service plus doctor path must keep local-only fail-closed behavior intact.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement GGUF conversion requests and local artifact registration</name>
  <files>
    src/model_adaptation/convert.py,
    tests/model_adaptation/test_convert.py
  </files>
  <read_first>
    .planning/phases/03-local-model-adaptation-and-deployment-paths/03-RESEARCH.md,
    src/model_adaptation/training.py,
    src/model_adaptation/registry.py,
    tests/model_adaptation/test_training.py
  </read_first>
  <behavior>
    - Test: GGUF conversion requests reference registered adapter artifacts rather than arbitrary file paths
    - Test: the laptop baseline conversion path resolves from the selected 4B baseline winner rather than a hardcoded checkpoint id
    - Test: dry-run conversion validates source artifacts, output path, version tag, and quantization profile metadata for both `baseline_winner_id` and `runner_up_id`
    - Test: successful conversion metadata is registered as a GGUF artifact for both selected models without assuming the binaries themselves are git-tracked
  </behavior>
  <action>
    Implement `src/model_adaptation/convert.py` with helpers to build conversion requests, stage GGUF output metadata, and register successful builds through the Phase 3 model registry. Keep the code explicit about source adapter artifacts and output quantization profile names, and make conversion cover both `baseline_winner_id` and `runner_up_id` even though only the baseline winner powers the laptop runtime profile.

    The conversion flow should support dry-run validation because heavy local conversion binaries may not be available in every test environment. Use that dry-run path in unit tests while keeping the request-building and metadata-registration logic real.

    Write `tests/model_adaptation/test_convert.py` so it verifies request assembly, dry-run validation, and GGUF artifact registration.
  </action>
  <verify>
    <automated>cd "C:/Users/wikiepeidia/OneDrive - caugiay.edu.vn/bài tập/usth/GEN14/INTERNSHIP/Internship-project" && python -m pytest tests/model_adaptation/test_convert.py -q</automated>
  </verify>
  <acceptance_criteria>
    - src/model_adaptation/convert.py contains `def build_gguf_request`
    - src/model_adaptation/convert.py contains `def convert_to_gguf`
    - src/model_adaptation/convert.py contains `def register_gguf_artifact`
    - tests/model_adaptation/test_convert.py contains `def test_build_gguf_request_uses_adapter_artifact`
    - tests/model_adaptation/test_convert.py contains `def test_convert_to_gguf_dry_run_registers_metadata_for_baseline_winner_and_runner_up`
    - `python -m pytest tests/model_adaptation/test_convert.py -q` exits with code 0
  </acceptance_criteria>
  <done>GGUF conversion requests and metadata registration exist and are safe to validate locally without a full conversion binary present in every test environment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the GGUF runtime backend, explicit profile selection, and doctor readiness checks</name>
  <files>
    src/config/settings.py,
    src/runtime/analyzers/gguf.py,
    src/runtime/service.py,
    src/runtime/doctor.py,
    tests/runtime/test_gguf_backend.py,
    tests/runtime/test_runtime_profiles.py
  </files>
  <read_first>
    .planning/phases/03-local-model-adaptation-and-deployment-paths/03-CONTEXT.md,
    src/runtime/analyzers/base.py,
    src/runtime/contracts.py,
    src/runtime/service.py,
    src/runtime/doctor.py,
    src/model_adaptation/convert.py
  </read_first>
  <behavior>
    - Test: GGUF analyzer implements the existing `AnalyzerBackend` contract and returns `AnalysisResult`
    - Test: runtime settings expose explicit GGUF profile selection and reject unsupported profiles or missing artifacts
    - Test: `RuntimeService` still enforces text-only and fail-closed boundaries before backend invocation
    - Test: doctor output reports GGUF readiness and artifact-location problems without suggesting cloud fallback
    - Test: the existing runtime CLI regression suite stays green after service and doctor changes
  </behavior>
  <action>
    Add `src/runtime/analyzers/gguf.py` with a `GGUFAnalyzer` implementation that satisfies `AnalyzerBackend`. It must use the existing typed runtime request/result contract, keep `top_cues` within the existing cap, and default the laptop profile to the selected 4B baseline winner rather than a generic "whoever won overall" assumption.

    Extend `src/config/settings.py`, `src/runtime/service.py`, and `src/runtime/doctor.py` to support explicit runtime profile selection for the GGUF path. The runtime must reject unknown profiles and stay local-only when artifacts are missing or the backend is unavailable.

    Update `tests/runtime/test_gguf_backend.py` and `tests/runtime/test_runtime_profiles.py` so they assert contract compatibility, explicit profile selection, and fail-closed behavior. Run `tests/runtime/test_cli.py` in the verification path because this plan changes service and doctor behavior under the shipped CLI surface.
  </action>
  <verify>
    <automated>cd "C:/Users/wikiepeidia/OneDrive - caugiay.edu.vn/bài tập/usth/GEN14/INTERNSHIP/Internship-project" && python -m pytest tests/runtime/test_gguf_backend.py tests/runtime/test_runtime_profiles.py tests/runtime/test_cli.py -q</automated>
  </verify>
  <acceptance_criteria>
    - src/runtime/analyzers/gguf.py contains `class GGUFAnalyzer`
    - src/runtime/service.py contains explicit GGUF profile selection logic
    - src/runtime/doctor.py reports GGUF readiness or artifact remediation guidance
    - tests/runtime/test_gguf_backend.py contains `def test_gguf_backend_returns_analysis_result_contract`
    - tests/runtime/test_runtime_profiles.py contains `def test_runtime_service_rejects_unknown_runtime_profile`
    - `python -m pytest tests/runtime/test_cli.py -q` exits with code 0
    - tests/runtime/test_runtime_profiles.py asserts no cloud fallback wording appears
    - `python -m pytest tests/runtime/test_gguf_backend.py tests/runtime/test_runtime_profiles.py -q` exits with code 0
  </acceptance_criteria>
  <done>The project can expose an explicit GGUF runtime profile behind the existing analyzer seam, and doctor/runtime flows remain local-only and fail-closed.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/model_adaptation/test_convert.py -q` passes
- `python -m pytest tests/runtime/test_gguf_backend.py tests/runtime/test_runtime_profiles.py tests/runtime/test_cli.py -q` passes
- Manual shell check: run `python -m src.runtime.cli doctor` with the GGUF profile selected and confirm readiness or exact local remediation is reported
</verification>

<success_criteria>
Phase 3 can convert both selected adapters into GGUF deployment artifacts and run the 4B baseline winner through an explicit laptop-baseline runtime profile, while preserving the shipped Phase 2 request/result contracts and local-only failure posture.
</success_criteria>

<output>
After completion, create `.planning/phases/03-local-model-adaptation-and-deployment-paths/03-03-SUMMARY.md`
</output>

<!-- markdownlint-enable MD022 MD032 MD033 MD034 MD055 MD056 MD060 -->