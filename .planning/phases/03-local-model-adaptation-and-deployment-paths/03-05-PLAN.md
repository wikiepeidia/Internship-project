---
phase: 03-local-model-adaptation-and-deployment-paths
plan: 05
type: execute
wave: 5
depends_on:
  - 03-02
  - 03-03
files_modified:
  - src/model_adaptation/convert.py
  - src/model_adaptation/cli.py
  - src/runtime/analyzers/gguf.py
  - tests/model_adaptation/test_convert.py
  - tests/model_adaptation/test_cli.py
  - tests/runtime/test_doctor.py
  - tests/runtime/test_gguf_backend.py
autonomous: true
gap_closure: true
requirements:
  - RUN-02

must_haves:
  truths:
    - "Operators can execute a non-dry-run GGUF conversion for the locked baseline winner `qwen3-4b-instruct-2507` and produce a registered local artifact under the off-repo model root"
    - "The same operator path can still build the locked runner-up `qwen3.5-4b` GGUF artifact so the artifact-pair decision from D-08, D-09, D-15, and D-16 remains true"
    - "The gguf-laptop runtime doctor reports READY when the real artifact exists, instead of depending on staged placeholder metadata"
    - "The public `python -m src.runtime.cli analyze` path can open and use the generated `gguf-laptop` artifact, proving the shipped runtime consumes the produced file rather than just seeing registry metadata"
  artifacts:
    - path: "src/model_adaptation/convert.py"
      provides: "Real adapter-to-GGUF conversion orchestration that no longer hard-stops on dry_run=False"
      exports: ["GGUFConversionRequest", "build_gguf_request", "convert_to_gguf", "register_gguf_artifact"]
    - path: "src/model_adaptation/cli.py"
      provides: "Operator-facing convert command for GGUF generation and registration"
      exports: ["build_parser", "handle_convert"]
    - path: "src/runtime/analyzers/gguf.py"
      provides: "GGUF runtime implementation that must open and use the registered baseline-winner artifact instead of relying on registry-only readiness plus heuristic placeholders"
      exports: ["GGUFAnalyzer.doctor", "GGUFAnalyzer.analyze"]
    - path: "tests/model_adaptation/test_convert.py"
      provides: "Regression checks that real conversion dispatches through a converter seam instead of raising immediately"
      contains: "test_convert_to_gguf_invokes_real_converter_without_runtime_error"
    - path: "tests/runtime/test_doctor.py"
      provides: "GGUF readiness coverage against a genuinely registered artifact entry"
      contains: "test_doctor_reports_ready_for_registered_real_gguf_artifact"
    - path: "tests/runtime/test_gguf_backend.py"
      provides: "Public GGUF runtime smoke coverage against the registered baseline-winner artifact and locked 4B selection pair"
      contains: "test_gguf_analyze_uses_registered_baseline_winner_artifact"
  key_links:
    - from: "src/model_adaptation/cli.py"
      to: "src/model_adaptation/convert.py"
      via: "new convert subcommand resolves baseline-winner or runner-up aliases and dispatches the conversion flow"
      pattern: "convert_to_gguf"
    - from: "src/model_adaptation/convert.py"
      to: "src/model_adaptation/registry.py"
      via: "successful conversion writes the GGUF file first, then registers checksum-backed metadata"
      pattern: "save_model_registry"
    - from: "src/runtime/analyzers/gguf.py"
      to: "src/model_adaptation/convert.py"
      via: "public runtime doctor and analyze paths both depend on the artifact record produced by the real conversion flow, so the registered GGUF file must be consumable"
      pattern: "artifact_type == \"gguf\""
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD034 MD040 MD041 MD046 MD055 MD056 MD060 -->

<objective>
Close the Phase 3 GGUF artifact gap by replacing the non-dry-run conversion stub with a real conversion-and-registration flow behind the existing local model tooling.

Purpose: `RUN-02` is still blocked because `convert_to_gguf(..., dry_run=False)` raises before any work happens, no operator-facing convert command exists, and the laptop GGUF doctor cannot become ready against a real artifact. This plan makes the Phase 3 laptop artifact path executable without reopening broader runtime-contract work.

Output: a real conversion path in `src/model_adaptation/convert.py`, an operator-facing `convert` command in `src.model_adaptation.cli`, and regression coverage plus operator smoke proving the GGUF doctor becomes ready and the public runtime analyze path consumes a real registered artifact instead of staged placeholders.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-local-model-adaptation-and-deployment-paths/03-UAT.md
@.planning/debug/phase3-gguf-closeout-gap.md
@.planning/phases/03-local-model-adaptation-and-deployment-paths/03-CONTEXT.md
@.planning/phases/03-local-model-adaptation-and-deployment-paths/03-RESEARCH.md
@.planning/phases/03-local-model-adaptation-and-deployment-paths/03-02-SUMMARY.md
@.planning/phases/03-local-model-adaptation-and-deployment-paths/03-03-SUMMARY.md
@src/model_adaptation/convert.py
@src/model_adaptation/cli.py
@src/runtime/analyzers/gguf.py

<interfaces>
From src/model_adaptation/convert.py:
```python
@dataclass(frozen=True)
class GGUFConversionRequest:
    candidate_id: str
    version_tag: str
    adapter_path: Path
    output_path: Path
    quantization_profile: str
    profile_name: str

def build_gguf_request(..., quantization_profile: str = "q4_k_m") -> GGUFConversionRequest: ...
def register_gguf_artifact(...) -> ModelArtifactRecord: ...
def convert_to_gguf(..., dry_run: bool = False) -> dict[str, object]: ...
```

From src/model_adaptation/cli.py:
```python
def _resolve_candidate_alias(candidate_arg: str, selection: PilotSelection) -> str: ...
def build_parser() -> argparse.ArgumentParser: ...
```

From src/runtime/analyzers/gguf.py:
```python
class GGUFAnalyzer:
    def doctor(self) -> DoctorStatus: ...
```

</interfaces>
</context>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI -> local conversion toolchain | untrusted CLI args decide which registered adapter is converted |
| local conversion toolchain -> registry metadata | only a fully written GGUF file should be registered |
| registry metadata -> runtime doctor | doctor must reflect real artifact availability, not placeholder state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | T | src/model_adaptation/convert.py | mitigate | Verify adapter source exists, write the GGUF output before registration, then compute checksum from the produced file. |
| T-03-05-02 | D | src/model_adaptation/cli.py | mitigate | Keep a dry-run preflight path, but remove the unconditional non-dry-run raise so operators can complete the real closeout flow. |
| T-03-05-03 | R | tests/runtime/test_doctor.py | mitigate | Add regression coverage showing doctor readiness now depends on a genuinely registered GGUF artifact instead of staged placeholder metadata. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the non-dry-run GGUF stub with a real conversion runner and atomic registration path</name>
  <files>
    src/model_adaptation/convert.py,
    tests/model_adaptation/test_convert.py
  </files>
  <read_first>
    .planning/debug/phase3-gguf-closeout-gap.md,
    src/model_adaptation/convert.py,
    src/model_adaptation/registry.py,
    src/model_adaptation/training.py,
    tests/model_adaptation/test_convert.py
  </read_first>
  <behavior>
    - Test: `convert_to_gguf(..., dry_run=False)` dispatches through a real converter seam instead of raising `RuntimeError` before any work starts
    - Test: the locked baseline winner `qwen3-4b-instruct-2507` still resolves to `gguf-laptop`, while the locked runner-up `qwen3.5-4b` still resolves to `gguf-runner-up`, per D-08, D-09, D-15, and D-16
    - Test: the registry is updated only after a produced GGUF file exists and a checksum can be computed from it
    - Test: failed conversions do not leave a registered placeholder artifact behind
  </behavior>
  <action>
    Rework `src/model_adaptation/convert.py` so the non-dry-run path performs a real conversion flow instead of raising unconditionally. Keep the dry-run behavior for narrow tests, but add a real converter seam that the executor can wire to the local GGUF toolchain without forcing heavyweight binaries into unit tests.

    Preserve the current request-building behavior for the locked baseline winner and runner-up, per D-08 and D-09. Register the artifact only after the produced file exists and its checksum is known. Do not keep the current placeholder-registration behavior on failed real conversions.

    As part of this closeout, refresh any touched conversion-side dry-run scaffolds so the locked pair is `qwen3-4b-instruct-2507` as baseline winner and `qwen3.5-4b` as runner-up, not the older `qwen3.5-4b` / `qwen2.5-7b-instruct` test pairing.

    Add unit tests that would fail if the implementation reintroduces the current unconditional raise or registers metadata before a produced file exists.
  </action>
  <verify>
    <automated>C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m pytest tests/model_adaptation/test_convert.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/model_adaptation/convert.py` no longer contains the exact message `Real GGUF conversion is not wired in yet; use dry_run=True`
    - `tests/model_adaptation/test_convert.py` contains `def test_convert_to_gguf_invokes_real_converter_without_runtime_error`
    - `tests/model_adaptation/test_convert.py` contains `def test_convert_to_gguf_registers_after_real_output_exists`
    - touched conversion-side selection fixtures use `qwen3-4b-instruct-2507` as baseline winner and `qwen3.5-4b` as runner-up
    - `C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m pytest tests/model_adaptation/test_convert.py -q` exits with code 0
  </acceptance_criteria>
  <done>The GGUF conversion helper can execute a real conversion path for the locked candidates, and registry writes are tied to a produced artifact instead of a placeholder stub.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the operator-facing convert command and prove doctor readiness against a real registered artifact</name>
  <files>
    src/model_adaptation/cli.py,
    src/runtime/analyzers/gguf.py,
    tests/model_adaptation/test_cli.py,
    tests/runtime/test_doctor.py,
    tests/runtime/test_gguf_backend.py
  </files>
  <read_first>
    .planning/phases/03-local-model-adaptation-and-deployment-paths/03-03-SUMMARY.md,
    src/model_adaptation/cli.py,
    src/runtime/cli.py,
    src/runtime/analyzers/gguf.py,
    tests/model_adaptation/test_cli.py,
    tests/runtime/test_doctor.py,
    tests/runtime/test_gguf_backend.py
  </read_first>
  <behavior>
    - Test: `python -m src.model_adaptation.cli convert` exists and accepts `baseline-winner` plus `runner-up` aliases through the existing selection resolver
    - Test: a successful conversion command returns operator-facing output that identifies the candidate, profile, and artifact path
    - Test: the existing GGUF doctor reports READY when the new command registers a real artifact fixture, without any placeholder-only shortcut
    - Test: the public `python -m src.runtime.cli analyze` path can consume the generated `gguf-laptop` artifact and return the normal structured output shape, proving the shipped runtime opens the file
    - Test: selection-sensitive fixtures in the touched CLI and GGUF runtime suites reflect the locked 4B pair (`qwen3-4b-instruct-2507`, `qwen3.5-4b`) rather than the older 4B/7B scaffolds
    - Test: conversion remains on the model-adaptation CLI and does not fork the public runtime CLI surface, per D-12 and D-14
  </behavior>
  <action>
    Extend `src/model_adaptation/cli.py` with a `convert` subcommand that reuses the current registry-selection and alias-resolution logic. The command should support the locked baseline winner and runner-up, default to the existing local artifact root, honor the configured off-repo `MODEL_ARTIFACT_ROOT`, and keep `--dry-run` available as a safe preflight option.

    Update `src/runtime/analyzers/gguf.py` as part of this task so the public analyze path does not stop at registry metadata; it must open or otherwise consume the registered GGUF artifact and fail closed when that artifact cannot be used. Then update `tests/model_adaptation/test_cli.py` so the operator path is executable from the repo. Update `tests/runtime/test_doctor.py` to assert that the existing GGUF doctor becomes READY when the new conversion path has registered a real GGUF artifact fixture. Extend the adjacent GGUF runtime coverage in `tests/runtime/test_gguf_backend.py` so the public analyze path must resolve and use the newly registered baseline-winner artifact while preserving the normal structured output. While touching these suites, update the remaining selection-sensitive fixtures from the older `qwen3.5-4b` / `qwen2.5-7b-instruct` pairing to the locked 4B pair from D-15 and D-16.

    Do not add a second public runtime entrypoint or any cloud fallback guidance. The only new operator surface in this plan is the model-adaptation `convert` command. Treat the post-implementation `python -m src.runtime.cli analyze` smoke as mandatory for plan success, not as an optional follow-up note.
  </action>
  <verify>
    <automated>C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m pytest tests/model_adaptation/test_cli.py tests/runtime/test_doctor.py tests/runtime/test_gguf_backend.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/model_adaptation/cli.py` contains a `convert` subcommand
    - `src/model_adaptation/cli.py` contains `def handle_convert`
    - `tests/model_adaptation/test_cli.py` contains `def test_convert_command_resolves_baseline_winner_alias`
    - `src/runtime/analyzers/gguf.py` contains the runtime-side artifact consumption path used by the public GGUF analyze flow
    - `tests/runtime/test_doctor.py` contains `def test_doctor_reports_ready_for_registered_real_gguf_artifact`
    - `tests/runtime/test_gguf_backend.py` contains `def test_gguf_analyze_uses_registered_baseline_winner_artifact`
    - touched GGUF closeout fixtures use `qwen3-4b-instruct-2507` as baseline winner and `qwen3.5-4b` as runner-up
    - mandatory non-dry-run operator smoke pins `MODEL_ARTIFACT_ROOT="D:/PROJEct/AI MODELS"` and produces the GGUF artifact under the off-repo model root rather than repo-local defaults
    - mandatory runner-up smoke proves `gguf-runner-up` can also be produced and registered through the same real conversion path
    - `C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m pytest tests/model_adaptation/test_cli.py tests/runtime/test_doctor.py tests/runtime/test_gguf_backend.py -q` exits with code 0
  </acceptance_criteria>
  <done>Operators can drive real GGUF conversion from the repo, and the shipped GGUF runtime proves it can both report READY and analyze text through the genuine artifact.</done>
</task>

</tasks>

<verification>
- `C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m pytest tests/model_adaptation/test_convert.py -q` passes
- `C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m pytest tests/model_adaptation/test_cli.py tests/runtime/test_doctor.py tests/runtime/test_gguf_backend.py -q` passes
- Mandatory operator smoke after implementation: `MODEL_ARTIFACT_ROOT="D:/PROJEct/AI MODELS" MODEL_REGISTRY_PATH="D:/PROJEct/AI MODELS/manifests/model-registry.json" C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m src.model_adaptation.cli convert --candidate baseline-winner --version-tag <tag> --registry-path "D:/PROJEct/AI MODELS/manifests/model-registry.json"` produces a registered GGUF artifact under the off-repo model root
- Mandatory runner-up smoke after implementation: `MODEL_ARTIFACT_ROOT="D:/PROJEct/AI MODELS" MODEL_REGISTRY_PATH="D:/PROJEct/AI MODELS/manifests/model-registry.json" C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m src.model_adaptation.cli convert --candidate runner-up --version-tag <tag> --registry-path "D:/PROJEct/AI MODELS/manifests/model-registry.json"` produces and registers the `gguf-runner-up` artifact under the same off-repo model root
- `MODEL_REGISTRY_PATH="D:/PROJEct/AI MODELS/manifests/model-registry.json" MODEL_ARTIFACT_ROOT="D:/PROJEct/AI MODELS" RUNTIME_BACKEND=gguf RUNTIME_PROFILE=gguf-laptop C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m src.runtime.cli doctor` reports READY
- `MODEL_REGISTRY_PATH="D:/PROJEct/AI MODELS/manifests/model-registry.json" MODEL_ARTIFACT_ROOT="D:/PROJEct/AI MODELS" RUNTIME_BACKEND=gguf RUNTIME_PROFILE=gguf-laptop C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/python.exe -m src.runtime.cli analyze --text "xac minh tai khoan ngay de tranh khoa the"` returns the normal structured analysis payload through the GGUF backend and fails if the generated artifact cannot be opened
</verification>

<success_criteria>
The Phase 3 laptop deployment path is no longer blocked by a hardcoded dry-run stub: the repo can produce and register a real GGUF artifact for the locked baseline winner, preserve runner-up artifact coverage, and prove through the existing doctor plus public analyze surfaces that the shipped runtime actually consumes the produced file.
</success_criteria>

<output>
After completion, create `.planning/phases/03-local-model-adaptation-and-deployment-paths/03-05-SUMMARY.md`
</output>

<!-- markdownlint-enable MD022 MD031 MD032 MD033 MD034 MD040 MD041 MD046 MD055 MD056 MD060 -->