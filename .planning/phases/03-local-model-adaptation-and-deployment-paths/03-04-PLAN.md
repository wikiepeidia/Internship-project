---
phase: 03-local-model-adaptation-and-deployment-paths
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - src/config/settings.py
  - src/runtime/analyzers/accelerated.py
  - src/runtime/service.py
  - src/runtime/doctor.py
  - readme.md
  - docs/user/LOCAL_MODELS.md
  - tests/runtime/test_accelerated_backend.py
  - tests/runtime/test_runtime_profiles.py
  - tests/runtime/test_doctor.py
autonomous: true
requirements:
  - RUN-03

must_haves:
  truths:
    - "Phase 3 adds an optional accelerated local backend without changing the public runtime contract"
    - "The accelerated profile is wired into the production runtime-selection path rather than existing as a dead backend"
    - "Doctor checks make the accelerated profile explicit and diagnosable rather than silently falling back to the GGUF profile"
    - "User-facing docs explain the available local model profiles, their artifact expectations, and the explicit selection path"
    - "Profile parity checks compare GGUF and accelerated outputs at the contract level, not just by backend availability"
  artifacts:
    - path: "src/runtime/analyzers/accelerated.py"
      provides: "Optional accelerated local runtime backend for stronger hardware"
      exports: ["AcceleratedAnalyzer"]
    - path: "docs/user/LOCAL_MODELS.md"
      provides: "Operator/user guidance for local model profiles, artifact expectations, and doctor diagnostics"
      contains: "GGUF"
    - path: "tests/runtime/test_accelerated_backend.py"
      provides: "Executable checks for accelerated-profile contract compatibility and readiness behavior"
      contains: "test_accelerated_backend_returns_phase2_contract_shape"
    - path: "tests/runtime/test_runtime_profiles.py"
      provides: "Executable checks for profile parity and explicit backend switching"
      contains: "test_gguf_and_accelerated_profiles_share_contract_shape"
  key_links:
    - from: "src/runtime/analyzers/accelerated.py"
      to: "src/runtime/analyzers/base.py"
      via: "accelerated backend keeps the same AnalyzerBackend contract as GGUF and heuristic"
      pattern: "AnalyzerBackend"
    - from: "src/runtime/doctor.py"
      to: "src/config/settings.py"
      via: "doctor reports which profile is selected and why a stronger-hardware path is or is not ready"
      pattern: "runtime_backend"
    - from: "src/runtime/service.py"
      to: "src/runtime/analyzers/accelerated.py"
      via: "explicit runtime profile selection must instantiate or route to AcceleratedAnalyzer in the shipped runtime path"
      pattern: "AcceleratedAnalyzer"
    - from: "docs/user/LOCAL_MODELS.md"
      to: "readme.md"
      via: "runtime profile docs and quick-start stay aligned"
      pattern: "GGUF"
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD034 MD040 MD041 MD046 MD055 MD056 MD060 -->

<objective>
Implement the optional accelerated local backend, extend profile-specific doctor checks, and document the final Phase 3 local deployment paths.

Purpose: `RUN-03` is the optional stronger-hardware path that complements the GGUF laptop baseline. It must remain explicit, local-only, and contract-compatible so the project can support a faster profile without fragmenting runtime behavior or confusing users.

Output: accelerated analyzer backend, profile-aware doctor checks, runtime-profile parity tests, and user-facing local-model documentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-local-model-adaptation-and-deployment-paths/03-CONTEXT.md
@.planning/phases/03-local-model-adaptation-and-deployment-paths/03-RESEARCH.md
@.planning/phases/03-local-model-adaptation-and-deployment-paths/03-VALIDATION.md
@.planning/phases/03-local-model-adaptation-and-deployment-paths/03-03-PLAN.md
@src/config/settings.py
@src/runtime/contracts.py
@src/runtime/doctor.py
@src/runtime/analyzers/gguf.py
@readme.md

<interfaces>
From src/runtime/contracts.py:
```python
class DoctorStatus(BaseModel):
    ready: bool
    backend_name: str
    local_only: bool = True
    text_only: bool = True
    checks: list[DoctorCheck]
    setup_steps: list[str] = Field(default_factory=list)
```

From src/runtime/analyzers/gguf.py (from Plan 03):
```python
class GGUFAnalyzer:
    backend_name: str
    def doctor(self) -> DoctorStatus: ...
    def analyze(self, request: AnalysisRequest) -> AnalysisResult: ...
```

</interfaces>
</context>

<threat_model>

- Asset: the accelerated profile is the optional stronger-hardware path promised by `RUN-03`.
- Threat: the accelerated backend drifts from the GGUF contract and returns different response fields or different failure semantics.
- Threat: doctor checks hide profile-specific failures and quietly route users to the wrong backend.
- Threat: docs leave profile selection ambiguous, making the explicit-runtime-selection promise meaningless.
- Required control in this plan: accelerated mode must implement the same contract, doctor must expose profile-specific readiness, and docs must explain profile selection and artifact expectations clearly.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the accelerated local backend with contract-parity tests</name>
  <files>
    src/config/settings.py,
    src/runtime/analyzers/accelerated.py,
    src/runtime/service.py,
    tests/runtime/test_accelerated_backend.py,
    tests/runtime/test_runtime_profiles.py
  </files>
  <read_first>
    .planning/phases/03-local-model-adaptation-and-deployment-paths/03-RESEARCH.md,
    src/runtime/analyzers/base.py,
    src/runtime/analyzers/gguf.py,
    src/runtime/contracts.py,
    tests/runtime/test_gguf_backend.py
  </read_first>
  <behavior>
    - Test: accelerated backend implements `AnalyzerBackend` and returns the Phase 2 `AnalysisResult` contract
    - Test: explicit profile selection can choose accelerated mode without mutating the request schema or CLI command surface
    - Test: the shipped runtime-selection path instantiates or routes to `AcceleratedAnalyzer` when the accelerated profile is selected
    - Test: parity tests compare GGUF and accelerated backends at the contract level
    - Test: accelerated mode remains local-only and does not introduce network fallback
  </behavior>
  <action>
    Add `src/runtime/analyzers/accelerated.py` with an `AcceleratedAnalyzer` implementation for stronger local hardware. Keep its external behavior aligned with `GGUFAnalyzer`: same request shape, same result model, same doctor pattern.

    Extend `src/config/settings.py`, `src/runtime/service.py`, and `tests/runtime/test_runtime_profiles.py` so the project can select the accelerated profile explicitly through the shipped runtime path. The service layer must make the production routing explicit instead of leaving accelerated mode as a backend that exists only in isolation. The tests should compare profile behavior at the contract level instead of asserting backend-specific wording only.

    Add `tests/runtime/test_accelerated_backend.py` to validate readiness behavior, contract shape, and the absence of cloud fallback language.
  </action>
  <verify>
    <automated>cd "C:/Users/wikiepeidia/OneDrive - caugiay.edu.vn/bài tập/usth/GEN14/INTERNSHIP/Internship-project" && python -m pytest tests/runtime/test_accelerated_backend.py tests/runtime/test_runtime_profiles.py -q</automated>
  </verify>
  <acceptance_criteria>
    - src/runtime/analyzers/accelerated.py contains `class AcceleratedAnalyzer`
    - tests/runtime/test_accelerated_backend.py contains `def test_accelerated_backend_returns_phase2_contract_shape`
    - tests/runtime/test_runtime_profiles.py contains `def test_gguf_and_accelerated_profiles_share_contract_shape`
    - tests/runtime/test_runtime_profiles.py asserts explicit selection for the accelerated profile
    - `python -m pytest tests/runtime/test_accelerated_backend.py tests/runtime/test_runtime_profiles.py -q` exits with code 0
  </acceptance_criteria>
  <done>The repo has an optional accelerated backend that remains local-only and contract-compatible with the GGUF profile.</done>
</task>

<task type="auto">
  <name>Task 2: Extend doctor guidance and document the final local model profiles</name>
  <files>
    src/runtime/doctor.py,
    readme.md,
    docs/user/LOCAL_MODELS.md,
    tests/runtime/test_doctor.py,
    tests/runtime/test_runtime_profiles.py
  </files>
  <read_first>
    .planning/phases/03-local-model-adaptation-and-deployment-paths/03-CONTEXT.md,
    src/runtime/doctor.py,
    src/runtime/analyzers/gguf.py,
    src/runtime/analyzers/accelerated.py,
    readme.md
  </read_first>
  <behavior>
    - Test: doctor output names the selected profile and reports profile-specific remediation steps
    - Docs: README points users to the local-model profile guide without suggesting cloud fallback
    - Docs: `docs/user/LOCAL_MODELS.md` explains GGUF baseline vs accelerated profile, explicit selection, and artifact expectations
    - Test: profile-parity checks and the existing CLI regression suite stay green after doctor and docs updates
  </behavior>
  <action>
    Update `src/runtime/doctor.py` so its readiness report can explain both the GGUF baseline and accelerated profile. Keep the tone concrete and local-only: missing artifacts, unsupported hardware, or missing dependencies should map to exact remediation steps without hiding the selected profile.

    Update `readme.md` with a short pointer to the Phase 3 local-model profiles, then create `docs/user/LOCAL_MODELS.md` describing the GGUF baseline, the optional accelerated path, explicit profile selection, doctor expectations, and the fact that model binaries remain local-only.

    Extend `tests/runtime/test_doctor.py` and `tests/runtime/test_runtime_profiles.py` so profile-aware doctor output and documentation-facing behavior remain regression-tested. Include `tests/runtime/test_cli.py` in the verification path because this plan still changes doctor-visible behavior under the shipped CLI.
  </action>
  <verify>
    <automated>cd "C:/Users/wikiepeidia/OneDrive - caugiay.edu.vn/bài tập/usth/GEN14/INTERNSHIP/Internship-project" && python -m pytest tests/runtime/test_doctor.py tests/runtime/test_runtime_profiles.py tests/runtime/test_cli.py -q && grep -nE "GGUF|accelerated|local-only|doctor|profile" readme.md docs/user/LOCAL_MODELS.md</automated>
  </verify>
  <acceptance_criteria>
    - src/runtime/doctor.py reports profile-specific readiness details
    - readme.md contains `GGUF`
    - readme.md contains `LOCAL_MODELS.md`
    - docs/user/LOCAL_MODELS.md contains `accelerated`
    - docs/user/LOCAL_MODELS.md contains `local-only`
    - docs/user/LOCAL_MODELS.md contains `doctor`
    - tests/runtime/test_doctor.py contains profile-aware readiness assertions
    - `python -m pytest tests/runtime/test_doctor.py tests/runtime/test_runtime_profiles.py tests/runtime/test_cli.py -q` exits with code 0
  </acceptance_criteria>
  <done>The Phase 3 deployment profiles are documented, doctor-aware, and explicit enough for users to pick the right local backend without schema or privacy surprises.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/runtime/test_accelerated_backend.py tests/runtime/test_runtime_profiles.py -q` passes
- `python -m pytest tests/runtime/test_doctor.py tests/runtime/test_runtime_profiles.py tests/runtime/test_cli.py -q` passes
- `grep -nE "GGUF|accelerated|local-only|doctor|profile" readme.md docs/user/LOCAL_MODELS.md` returns matches once the docs land
- Manual hardware check: run the accelerated profile on supported local hardware and confirm the doctor report and `AnalysisResult` shape match the GGUF baseline
</verification>

<success_criteria>
Phase 3 finishes with two explicit local runtime profiles: a GGUF laptop baseline and an optional accelerated path for stronger hardware. Both remain local-only, contract-compatible with the shipped Phase 2 runtime surface, and clearly documented.
</success_criteria>

<output>
After completion, create `.planning/phases/03-local-model-adaptation-and-deployment-paths/03-04-SUMMARY.md`
</output>

<!-- markdownlint-enable MD022 MD032 MD033 MD034 MD055 MD056 MD060 -->