---
phase: 03-local-model-adaptation-and-deployment-paths
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/model_adaptation/data.py
  - src/model_adaptation/prompts.py
  - src/model_adaptation/training.py
  - src/model_adaptation/cli.py
  - tests/model_adaptation/test_training.py
  - tests/model_adaptation/test_cli.py
autonomous: true
requirements:
  - MOD-01

must_haves:
  truths:
    - "Only the pilot-selected 4B baseline winner and the runner-up receive full adaptation work in Phase 3"
    - "The training flow is adapter-first, dry-run capable, and keeps large artifacts local-only"
    - "Training consumes the existing Phase 1 split artifacts rather than inventing a separate fine-tuning dataset layout"
    - "Merged checkpoints are not required to complete the Phase 3 baseline"
  artifacts:
    - path: "src/model_adaptation/data.py"
      provides: "Dataset-loading and formatting helpers for Phase 3 fine-tuning"
      exports: ["load_split_records", "build_training_examples"]
    - path: "src/model_adaptation/prompts.py"
      provides: "Prompt or chat-template formatting helpers for the selected Qwen checkpoints"
      exports: ["format_training_prompt"]
    - path: "src/model_adaptation/training.py"
      provides: "QLoRA training orchestration and adapter-save flow for the baseline winner and runner-up checkpoints"
      exports: ["build_training_config", "run_training", "save_adapter_artifacts"]
    - path: "src/model_adaptation/cli.py"
      provides: "Dry-run friendly operator entrypoints for pilot, train, and later conversion flows"
      exports: ["main", "build_parser"]
    - path: "tests/model_adaptation/test_training.py"
      provides: "Executable checks for training-config assembly, split loading, and adapter-save metadata"
      contains: "test_build_training_config_uses_selected_candidate"
    - path: "tests/model_adaptation/test_cli.py"
      provides: "Executable checks for dry-run training commands and explicit candidate selection"
      contains: "test_train_dry_run_uses_baseline_winner_and_runner_up_only"
  key_links:
    - from: "src/model_adaptation/training.py"
      to: "src/model_adaptation/pilot.py"
      via: "training is limited to the baseline winner and runner-up selected in Plan 01"
      pattern: "baseline_winner_id"
    - from: "src/model_adaptation/data.py"
      to: "data/splits/"
      via: "fine-tuning uses the existing split artifacts as source-of-truth"
      pattern: "train.jsonl"
    - from: "src/model_adaptation/training.py"
      to: "src/model_adaptation/registry.py"
      via: "saved adapters must be recorded in the local model registry with checksums and version tags"
      pattern: "save_model_registry"
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD034 MD040 MD041 MD046 MD055 MD056 MD060 -->

<objective>
Implement the Phase 3 QLoRA training slice for the pilot-selected baseline winner and runner-up, including dataset formatting, dry-run capable training orchestration, and adapter artifact registration.

Purpose: The user explicitly wants adapters plus GGUF, not a merged-checkpoint-only workflow. This plan creates the reproducible adapter source artifacts for the two chosen checkpoints while staying compatible with the repo's existing data splits and local-only artifact rules.

Output: training-data helpers, prompt/template helpers, a dry-run friendly training command, adapter-save and registry wiring, and passing tests for training config and CLI behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-local-model-adaptation-and-deployment-paths/03-CONTEXT.md
@.planning/phases/03-local-model-adaptation-and-deployment-paths/03-RESEARCH.md
@.planning/phases/03-local-model-adaptation-and-deployment-paths/03-VALIDATION.md
@.planning/phases/03-local-model-adaptation-and-deployment-paths/03-01-PLAN.md
@src/model_adaptation/schemas.py
@src/model_adaptation/catalog.py
@src/model_adaptation/registry.py
@data/splits/train.jsonl
@data/splits/val.jsonl

<interfaces>
From src/model_adaptation/schemas.py (from Plan 01):
```python
class ModelCandidate(BaseModel):
    candidate_id: str
    hf_source: str
    role: Literal["primary", "fallback"]

class PilotSelection(BaseModel):
  baseline_winner_id: str
    runner_up_id: str
```

From src/model_adaptation/registry.py (from Plan 01):
```python
def save_model_registry(...): ...
def load_model_registry(...): ...
```

From src/data_pipeline/schemas.py:
```python
class DatasetRecord(BaseModel):
    text: str
    risk_tier: Literal["benign", "suspicious", "high-risk"]
    suspicious_spans: list[str]
    xai_explanation: str
```

</interfaces>
</context>

<threat_model>

- Asset: the training flow creates the source artifacts that later runtime plans will depend on.
- Threat: training ignores the pilot outcome and quietly trains extra checkpoints outside the agreed scope.
- Threat: the training implementation is impossible to validate locally because it lacks a dry-run path and only supports full GPU execution.
- Threat: adapter artifacts are written without metadata, making later GGUF conversion and runtime loading irreproducible.
- Required control in this plan: training must be limited to the baseline winner and runner-up, expose a dry-run command path, and register saved adapters with checksum-backed metadata.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build split-loading and training-example formatting helpers for the selected checkpoints</name>
  <files>
    src/model_adaptation/data.py,
    src/model_adaptation/prompts.py,
    tests/model_adaptation/test_training.py
  </files>
  <read_first>
    .planning/phases/03-local-model-adaptation-and-deployment-paths/03-RESEARCH.md,
    src/model_adaptation/schemas.py,
    src/model_adaptation/pilot.py,
    src/data_pipeline/schemas.py,
    data/splits/train.jsonl,
    data/splits/val.jsonl
  </read_first>
  <behavior>
    - Test: split-loading helpers read the existing JSONL split artifacts into typed training rows
    - Test: training-example formatting preserves Vietnamese and mixed-language text without stripping suspicious spans or explanation fields
    - Test: prompt/template helpers can build candidate-specific training examples without forcing a single merged checkpoint format
  </behavior>
  <action>
    Implement `src/model_adaptation/data.py` to load the existing Phase 1 split files and turn them into training-ready records. Keep the helper explicit about which split is being used, and avoid any hidden reshuffling that would undermine dataset traceability.

    Implement `src/model_adaptation/prompts.py` with the minimum formatting needed to prepare examples for Qwen-family fine-tuning. Preserve the dataset's risk-tier and explanation signals instead of reducing every example to a bare class label.

    Update `tests/model_adaptation/test_training.py` so it asserts typed split loading, formatting behavior, and preservation of mixed Vietnamese-English text.
  </action>
  <verify>
    <automated>cd "C:/Users/wikiepeidia/OneDrive - caugiay.edu.vn/bài tập/usth/GEN14/INTERNSHIP/Internship-project" && python -m pytest tests/model_adaptation/test_training.py -q</automated>
  </verify>
  <acceptance_criteria>
    - src/model_adaptation/data.py contains `def load_split_records`
    - src/model_adaptation/data.py contains `def build_training_examples`
    - src/model_adaptation/prompts.py contains `def format_training_prompt`
    - tests/model_adaptation/test_training.py contains `def test_build_training_examples_preserves_mixed_language_text`
    - `python -m pytest tests/model_adaptation/test_training.py -q` exits with code 0
  </acceptance_criteria>
  <done>The Phase 3 training slice can read the existing split artifacts and build candidate-aware QLoRA examples without losing the repo's existing text and explanation signals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement dry-run capable QLoRA orchestration and adapter artifact persistence</name>
  <files>
    src/model_adaptation/training.py,
    tests/model_adaptation/test_training.py
  </files>
  <read_first>
    .planning/phases/03-local-model-adaptation-and-deployment-paths/03-CONTEXT.md,
    src/model_adaptation/data.py,
    src/model_adaptation/registry.py,
    tests/model_adaptation/test_training.py
  </read_first>
  <behavior>
    - Test: training config can be built for the baseline winner and runner-up only
    - Test: dry-run mode validates config, candidate selection, output paths, and artifact registration without requiring a real long-running fine-tune
    - Test: adapter-save helpers register artifact metadata and checksums after successful output staging
    - Test: merged checkpoints are not required in the baseline training flow
  </behavior>
  <action>
    Implement `src/model_adaptation/training.py` with helpers such as `build_training_config()`, `run_training()`, and `save_adapter_artifacts()`. The training layer should accept a selected candidate id, dataset split paths, version tag, dry-run flag, and output root. It must refuse candidates outside the baseline-winner/runner-up selection from Plan 01.

    Keep the training implementation adapter-first. Save adapter metadata through the Plan 01 registry helpers, and capture a version tag or build stamp that later plans can reuse for GGUF conversion.

    Update `tests/model_adaptation/test_training.py` so it checks dry-run behavior, candidate gating, and adapter registration without downloading real checkpoints during unit tests.
  </action>
  <verify>
    <automated>cd "C:/Users/wikiepeidia/OneDrive - caugiay.edu.vn/bài tập/usth/GEN14/INTERNSHIP/Internship-project" && python -m pytest tests/model_adaptation/test_training.py -q</automated>
  </verify>
  <acceptance_criteria>
    - src/model_adaptation/training.py contains `def build_training_config`
    - src/model_adaptation/training.py contains `def run_training`
    - src/model_adaptation/training.py contains `def save_adapter_artifacts`
    - tests/model_adaptation/test_training.py contains `def test_build_training_config_uses_selected_candidate`
    - tests/model_adaptation/test_training.py contains `def test_run_training_rejects_non_selected_candidate`
    - tests/model_adaptation/test_training.py contains `def test_save_adapter_artifacts_registers_metadata`
    - `python -m pytest tests/model_adaptation/test_training.py -q` exits with code 0
  </acceptance_criteria>
  <done>The Phase 3 training layer can build reproducible QLoRA configs, dry-run safely, and register adapter artifacts for the baseline winner and runner-up only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add a dry-run friendly model-adaptation CLI for pilot and training flows</name>
  <files>
    src/model_adaptation/cli.py,
    tests/model_adaptation/test_cli.py
  </files>
  <read_first>
    .planning/phases/03-local-model-adaptation-and-deployment-paths/03-VALIDATION.md,
    src/model_adaptation/pilot.py,
    src/model_adaptation/training.py,
    tests/model_adaptation/test_cli.py
  </read_first>
  <behavior>
    - Test: CLI exposes clear subcommands for pilot and training dry-runs
    - Test: train dry-run rejects candidate ids outside the baseline-winner and runner-up set
    - Test: CLI remains operator-facing and separate from the shipped user runtime CLI
  </behavior>
  <action>
    Implement `src/model_adaptation/cli.py` with explicit operator-oriented commands for the Phase 3 workflow. At minimum, expose dry-run capable `pilot` and `train` commands. Keep this CLI separate from `src.runtime.cli` so Phase 2's user-facing analyzer surface remains narrow and stable.

    Update `tests/model_adaptation/test_cli.py` to validate parser behavior, baseline-winner/runner-up candidate gating, and dry-run output paths using monkeypatched helpers rather than real model downloads. Support symbolic aliases such as `baseline-winner` and `runner-up` so later plans do not hardcode a checkpoint id in verification commands.
  </action>
  <verify>
    <automated>cd "C:/Users/wikiepeidia/OneDrive - caugiay.edu.vn/bài tập/usth/GEN14/INTERNSHIP/Internship-project" && python -m pytest tests/model_adaptation/test_cli.py tests/model_adaptation/test_training.py -q</automated>
  </verify>
  <acceptance_criteria>
    - src/model_adaptation/cli.py contains `add_parser("pilot"`
    - src/model_adaptation/cli.py contains `add_parser("train"`
    - src/model_adaptation/cli.py contains `--dry-run`
    - tests/model_adaptation/test_cli.py contains `def test_train_dry_run_uses_baseline_winner_and_runner_up_only`
    - tests/model_adaptation/test_cli.py contains `def test_cli_exposes_pilot_and_train_commands`
    - `python -m pytest tests/model_adaptation/test_cli.py tests/model_adaptation/test_training.py -q` exits with code 0
  </acceptance_criteria>
  <done>Phase 3 has an operator-facing dry-run CLI for pilot and training workflows without expanding the shipped user runtime surface.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/model_adaptation/test_training.py -q` passes
- `python -m pytest tests/model_adaptation/test_cli.py tests/model_adaptation/test_training.py -q` passes
- `python -m src.model_adaptation.cli train --dry-run --candidate baseline-winner --version-tag phase3-smoke` exits with code 0 once implemented
</verification>

<success_criteria>
Phase 3 can train the pilot-selected 4B baseline winner and runner-up through a reproducible, adapter-first, dry-run capable QLoRA workflow that uses the existing dataset splits and registers saved adapter metadata for later GGUF conversion.
</success_criteria>

<output>
After completion, create `.planning/phases/03-local-model-adaptation-and-deployment-paths/03-02-SUMMARY.md`
</output>

<!-- markdownlint-enable MD022 MD032 MD033 MD034 MD055 MD056 MD060 -->