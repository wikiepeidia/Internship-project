---
phase: 43-evidence-bound-slide-rebuild
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/43-evidence-bound-slide-rebuild/43-EVIDENCE-SLOT-CONTRACT.json
  - scripts/phase43/evidence_slots.py
  - scripts/phase43/archive_locked_deck.py
  - scripts/phase43/lint_slide_deck.py
  - scripts/phase43/compile_and_seal_deck.py
  - tests/phase43/test_evidence_slots.py
  - tests/phase43/test_archive_locked_deck.py
  - tests/phase43/test_lint_slide_deck.py
  - tests/phase43/test_compile_and_seal_deck.py
autonomous: true
requirements: [SLIDE-08, SLIDE-09, SLIDE-10]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A synthetic canonical evidence record can be hash-verified, rendered into deterministic LaTeX/assets, and traced back to its exact source without touching project data or model artifacts."
    - "Bindings cannot carry hand-entered result values; they carry source paths, expected hashes, selectors, and destination slots only."
    - "A structural linter requires an active pause in every active content frame; only a titlepage frame or a sectionpage-only frame can qualify for an explicit structural exemption."
    - "A fresh-output compiler removes exact stale build outputs, runs two XeLaTeX passes, rejects bad logs/tokens, seals source/log/PDF hashes, and independently re-verifies its receipt."
  artifacts:
    - path: .planning/phases/43-evidence-bound-slide-rebuild/43-EVIDENCE-SLOT-CONTRACT.json
      provides: Closed slot schema and slide destinations for every Phase 40/41 handoff item
    - path: scripts/phase43/evidence_slots.py
      provides: Strict binding verification and deterministic evidence rendering
    - path: scripts/phase43/lint_slide_deck.py
      provides: Recursive active-TeX structure, reveal, orphan, and claim linting
    - path: scripts/phase43/archive_locked_deck.py
      provides: Whole-deck closure archiver and restore verifier
    - path: scripts/phase43/compile_and_seal_deck.py
      provides: Fresh two-pass XeLaTeX build, scan, seal, and independent receipt verifier
  key_links:
    - from: scripts/phase43/evidence_slots.py
      to: .planning/phases/43-evidence-bound-slide-rebuild/43-EVIDENCE-SLOT-CONTRACT.json
      via: exact slot-key and destination validation
      pattern: "EVIDENCE-SLOT-CONTRACT"
    - from: scripts/phase43/lint_slide_deck.py
      to: rendered evidence receipt
      via: generated-fragment and asset SHA re-verification
      pattern: "receipt"
---

<objective>
Create the evidence-to-slide trust boundary, whole-deck archiver, and deck-structure linter using synthetic fixtures only, per D-02 and D-03.

Purpose: Phase 43 must make fabricated or stale result insertion mechanically harder than consuming the frozen evidence correctly.
Output: A closed slot contract, deterministic renderer/verifier, whole-deck archiver, structural linter, and synthetic regression tests.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-multi-model-training-evidence/40-REPORT-SLIDE-HANDOFF.md
@.planning/phases/41-one-shot-two-model-evaluation/41-01-PLAN.md
@.planning/phases/41-one-shot-two-model-evaluation/41-02-PLAN.md
@.planning/phases/41-one-shot-two-model-evaluation/41-REVIEW-RESOLUTION.md
@data/models/phase41/phase41-provenance-erratum.json
@.planning/phases/43-evidence-bound-slide-rebuild/43-SOURCE-AUDIT.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Build the complete evidence-slot renderer and anti-fabrication gate</name>
  <files>.planning/phases/43-evidence-bound-slide-rebuild/43-EVIDENCE-SLOT-CONTRACT.json, scripts/phase43/evidence_slots.py, tests/phase43/test_evidence_slots.py</files>
  <behavior>
    - A strict canonical-JSON binding containing a repository-relative source path, SHA-256, typed selector, and destination renders a LaTeX fragment and receipt whose hashes reproduce exactly.
    - Duplicate keys, absolute/path-traversal/reparse paths, hash drift, selector/type mismatch, extra fields, and any literal result-value field fail closed.
    - Phase 41 uses typed canonical inputs under `data/models/phase41/verified-export/9ac54d58c273ab0a8c2f2b4b61e472a51ca94231a94b6847637ecad6ceee49f7/`; the terminal status must be `completed`, the manifest must hash-link results and terminal, the export receipt must bind the full frozen group, and the external erratum SHA-256 `c7be74346f0e217c382e556fbf0a730cb33be50356d4155356a5b024871a1672` must accompany every Phase 41 slot.
    - All H-03 Phase 40/41 slots are destination-unique and raw predictions are represented only by authority hash.
    - Preparation tests inject a deny-all-live-roots policy: fixtures stay under pytest temporary directories and dataset, reserved-test, model-bundle, or drive-letter paths fail before opening; Plan 02 later supplies the closed production allowlist.
  </behavior>
  <action>Start red, then implement the full closed contract plus duplicate-key rejection, canonical JSON, safe path resolution, pre-open allowlist checks, SHA verification, typed extraction, LaTeX escaping, byte-identical asset copying, atomic transaction groups, fresh staging/promotion, and a canonical receipt. Bind every H-03 item. Encode the exact Phase 41 verified-export authority from Plans 41-01/41-02: require `terminal.json.status == completed`; require `evidence-manifest.json` to bind the exact `results.json` and `terminal.json` hashes and both frozen model identities; require the verified-export receipt; then require and hash-bind the external erratum before publishing any Phase 41 fragment. The limitation selector must render one terminal model-evaluation pass plus automated integrity reads, never untouched/global-zero-access wording. Do not add packages or inspect live evidence per D-03.</action>
  <verify><automated>python -m pytest tests/phase43/test_evidence_slots.py -q</automated></verify>
  <done>The complete synthetic binding either produces reproducible fragments/assets/receipt or fails before output; partial Phase 41 and manual-value cases are automated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Archive the whole locked-deck closure and harden restoration</name>
  <files>scripts/phase43/archive_locked_deck.py, tests/phase43/test_archive_locked_deck.py</files>
  <behavior>
    - Capture the entry file, every regular file below the slide-source directory including inactive sections, and every resolved external image dependency.
    - Produce a UTC-dated content-addressed tar, canonical manifest, and receipt with per-file, root, and archive hashes; restore reproduces exact bytes.
    - Path escape/reparse, source mutation, concurrent writers, overwrite, duplicate members, and unmanifested members fail without publishing or damaging prior evidence.
  </behavior>
  <action>Start red, then implement comment-aware dependency discovery plus explicit full `slides/` inclusion, regular-file containment, deterministic tar metadata, CreateNew outputs, exclusive lease, pre/post identities, durable flush, and independent archive reread. Preparation uses synthetic trees only; actual archive creation is reserved for Plan 02 per D-04.</action>
  <verify><automated>python -m pytest tests/phase43/test_archive_locked_deck.py -q</automated></verify>
  <done>Synthetic whole-tree archive, restoration, race, mutation, traversal, and replay scenarios pass fail-closed tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lint every active frame and seal a fresh two-pass build</name>
  <files>scripts/phase43/lint_slide_deck.py, scripts/phase43/compile_and_seal_deck.py, tests/phase43/test_lint_slide_deck.py, tests/phase43/test_compile_and_seal_deck.py</files>
  <behavior>
    - Recursive active TeX resolves comments correctly and requires Get Data -> Train -> GGUF Export -> Evaluation in that order.
    - Every active content frame contains an active pause; only a frame whose body is structurally a titlepage or sectionpage alone is exempt, and every exemption is emitted in the lint receipt. Agenda, references, thank-you, backup, and ordinary narrative frames are not exempt; a commented pause does not count.
    - Unresolved evidence tokens, hand-authored numeric result claims, unreceipted result images, stale fragment hashes, unlisted section files, and undeclared orphan decisions fail.
    - Compile create removes only `slides.aux`, `slides.log`, `slides.nav`, `slides.out`, `slides.snm`, `slides.toc`, `slides.vrb`, `slides.pdf`, and `slides.synctex.gz` under the verified build root, runs exactly two fail-fast XeLaTeX passes, scans the active source closure and fresh log, then seals engine/command/source/log/PDF hashes; verify independently recomputes every binding.
    - A fake XeLaTeX fixture proves stale-PDF deletion, two-pass enforcement, pass failure, bad log/token rejection, fresh receipt creation, and source/log/PDF/receipt tamper detection without compiling the live deck.
  </behavior>
  <action>Implement a dependency-closure linter that strips unescaped comments, follows local inputs without path escape/reparse traversal, identifies frame bodies, and cross-checks the evidence receipt. Enforce SLIDE-09 over every active content frame, with only structural titlepage and sectionpage-only exemptions recorded explicitly; tests must prove agenda, references, thank-you, backup, ordinary no-pause, and comment-only-pause frames fail. Require every section file to be active or named in the contract exclusion list. Implement compile create/verify commands using a fake executable in tests: validate the build root; remove only the nine named stale outputs; invoke two XeLaTeX passes with the handoff flags; reject nonzero exits; scan the fresh log for `Fatal error`, `Emergency stop`, `Undefined control sequence`, `LaTeX Error`, `undefined references`, and `undefined citations`; scan only the active source closure for `PENDING_PHASE`, `P40_`, `P41_`, and `TODO_EVIDENCE`; require PDF/log creation after invocation start; write a canonical CreateNew receipt containing tool/engine/commands/source-closure/log/PDF hashes; durable-flush it; and re-open/recompute everything in verify. Never compile the live deck in Plan 01.</action>
  <verify><automated>python -m pytest tests/phase43/test_lint_slide_deck.py tests/phase43/test_compile_and_seal_deck.py -q</automated></verify>
  <done>Synthetic decks pass only when every active content frame reveals progressively and the fresh two-pass source/log/PDF receipt independently verifies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Frozen evidence -> generated TeX/assets | Untrusted paths and structured values become presentation claims. |
| Slide source -> structural verdict | Comments, orphan files, and TeX indirection can spoof naive grep checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-43-01 | Tampering | Evidence binding | high | mitigate | Strict JSON, exact hashes, typed selectors, closed keys, atomic group validation. |
| T-43-02 | Spoofing | Paths and TeX inputs | high | mitigate | Resolve regular files under allowed roots and reject traversal/reparse aliases before open. |
| T-43-03 | Repudiation | Rendered claims | high | mitigate | Canonical receipt binds every fragment/asset to source SHA and selector. |
| T-43-04 | Information Disclosure | Raw predictions | medium | mitigate | Permit authority hashes only; never open or render raw-prediction rows. |
| T-43-14 | Tampering | Compile outputs | high | mitigate | Exact stale-output cleanup, two fresh passes, log/token gates, and independently recomputed source/log/PDF receipt. |
| T-43-SC | Tampering | Package installation | low | accept | Standard library plus existing pytest only; no install occurs. |
</threat_model>

<verification>
All four focused suites pass from synthetic temporary roots; source review confirms no live artifact discovery or dataset opener exists.
</verification>

<success_criteria>
- Every H-03 evidence item has exactly one typed destination.
- Partial, stale, manually valued, or path-unsafe bindings fail closed.
- The archive tool captures and restores the whole deck source closure without overwrite or TOCTOU ambiguity.
- The linter validates semantic active content, ordered pipeline stages, reveals, and receipt hashes.
- Compile create/verify cannot certify stale or tampered source, log, PDF, command, or receipt bytes.
</success_criteria>

<output>Create `.planning/phases/43-evidence-bound-slide-rebuild/43-01-SUMMARY.md` when done.</output>
