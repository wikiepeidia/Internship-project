---
phase: "14-css-html-scaffolding"
plan: "14-01"
type: "feature"
wave: 1
depends_on: []
files_modified:
  - "src/runtime/demo_assets/index.html"
  - "src/runtime/demo_assets/demo.css"
  - "tests/runtime/test_demo.py"
autonomous: true
requirements:
  - "INFRA-01"
requirements_addressed:
  - "INFRA-01"
must_haves:
  truths:
    - "D-01: The page uses a full-viewport messenger layout with compact header, scrollable thread, and pinned composer."
    - "D-02: The header shows local safety status with compact local-first and text-only chips."
    - "D-03: The desktop layout is projector-readable and can shrink beside code without becoming a fixed phone mockup."
    - "D-04: The thread uses flex: 1 1 0 and min-height: 0; the composer uses safe-area padding."
    - "D-05: The initial state uses a left-aligned welcome bot bubble, not a card or marketing placeholder."
    - "D-06: The welcome copy includes the Vietnamese helper tone: Mình sẽ giúp bạn kiểm tra dấu hiệu lừa đảo tài chính."
    - "D-07: The welcome state includes a small hint row under the bubble without a card layout."
    - "D-08: The welcome bubble is inside the static role=\"log\" chat thread at page load."
    - "D-09: Static HTML includes user, bot, typing, and error chat bubble templates."
    - "D-10: The bot template includes risk badge, verdict, labels/meta, grounded cues, and safe next steps slots."
    - "D-11: No template inner content node uses id; clone targets use data-slot attributes."
    - "D-12: Existing functional IDs analysis-form, message-input, channel-select, sample-button, and analyze-button are preserved."
    - "D-13: CSS uses a trustworthy light clinical palette with navy/teal accents and semantic risk colors."
    - "D-14: Spacing and typography are defense-readable with generous bubble padding and clear labels."
    - "D-15: User bubbles are right-aligned navy/teal filled; bot bubbles are left-aligned white with border."
    - "D-16: Risk tier badges are compact bilingual pills with semantic colors."
    - "Opening GET / serves a chat shell with no missing static CSS or JS links."
    - "Be Vietnam Pro loads from Google Fonts CDN and Vietnamese content uses line-height >= 1.65."
    - "The backend routes and POST /api/analyze contract are unchanged in this phase."
  artifacts:
    - "src/runtime/demo_assets/index.html"
    - "src/runtime/demo_assets/demo.css"
    - "tests/runtime/test_demo.py"
  key_links:
    - "index.html links /static/demo.css and /static/demo.js"
    - "demo.py continues to serve /, /static/demo.css, /static/demo.js, and /api/analyze"
---

# 14-01: Static Chat Shell Scaffold

<objective>
Rewrite the demo HTML and CSS into a static chat-bubble scaffold that satisfies Phase 14: Be Vietnam Pro loading, `100dvh` viewport, compact local-first header, scrollable ARIA live chat thread, pinned composer, and clone-safe chat templates.
</objective>

## Context Decisions Covered

This plan covers all Phase 14 context decisions: D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-14, D-15, and D-16.

## Artifacts this phase produces

- `src/runtime/demo_assets/index.html` chat shell with:
  - static `div` chat thread carrying `id="result-panel"`, `role="log"`, `aria-live="polite"`, and a chat-thread class
  - welcome bot bubble at page load
  - preserved form/control IDs: `analysis-form`, `message-input`, `channel-select`, `sample-button`, `analyze-button`
  - user, bot, typing, and error templates
  - template inner content nodes addressed only by `data-slot`
- `src/runtime/demo_assets/demo.css` chat-shell stylesheet with:
  - Be Vietnam Pro font stack
  - `100dvh` app shell
  - independently scrolling thread area
  - pinned composer with safe-area padding
  - user, bot, typing, error, and risk badge styles
  - responsive width for projector use and code-side demo use
- `tests/runtime/test_demo.py` assertions for the Phase 14 static shell contract.

<tasks>
<task id="T1" type="test">
<title>Codify the static chat shell contract in demo tests</title>
<read_first>
- `.planning/phases/14-css-html-scaffolding/14-CONTEXT.md`
- `.planning/phases/14-css-html-scaffolding/14-RESEARCH.md`
- `tests/runtime/test_demo.py`
- `src/runtime/demo_assets/index.html`
</read_first>
<action>
Update `tests/runtime/test_demo.py` so `test_demo_index_serves_text_only_form` or new focused tests assert the Phase 14 static contract:
- HTML includes a Google Fonts link containing `Be+Vietnam+Pro` or `Be Vietnam Pro`.
- HTML still includes `/static/demo.css` and `/static/demo.js`.
- HTML preserves `id="analysis-form"`, `id="message-input"`, `id="channel-select"`, `id="sample-button"`, and `id="analyze-button"`.
- HTML contains a chat thread element with `role="log"` and `aria-live="polite"`.
- HTML contains the welcome text `Mình sẽ giúp bạn kiểm tra dấu hiệu lừa đảo tài chính.`
- HTML contains templates for user, bot/result, typing, and error bubbles.
- HTML template internals do not contain IDs such as `result-summary`, `result-risk-tier`, `result-labels`, `result-backend`, `result-cues`, `result-recommendations`, `error-message`, or `error-steps`.
- Static CSS assertions check for `100dvh`, `min-height: 0`, `flex: 1 1 0`, `env(safe-area-inset-bottom)`, and `Be Vietnam Pro`.
Keep API behavior tests intact and do not alter fake service contract assertions.
</action>
<acceptance_criteria>
- `tests/runtime/test_demo.py` contains assertions for `role="log"` and `aria-live="polite"`.
- `tests/runtime/test_demo.py` contains assertions for `Be Vietnam Pro`, `100dvh`, `min-height: 0`, and `env(safe-area-inset-bottom)`.
- `tests/runtime/test_demo.py` still asserts API response fields `risk_tier`, `threat_labels`, `top_cues`, `recommendations`, and `backend_name`.
</acceptance_criteria>
</task>

<task id="T2" type="feature">
<title>Rewrite index.html into the static messenger shell</title>
<read_first>
- `.planning/phases/14-css-html-scaffolding/14-CONTEXT.md`
- `.planning/phases/14-css-html-scaffolding/14-RESEARCH.md`
- `src/runtime/demo_assets/index.html`
- `src/runtime/demo_assets/demo.js`
- `src/runtime/demo.py`
- `tests/runtime/test_demo.py`
</read_first>
<action>
Replace the current hero/card HTML with a first-viewport chat app shell:
- Set the document language to Vietnamese-oriented output, such as `lang="vi"`.
- Add Google Fonts preconnect/link elements for Be Vietnam Pro before `/static/demo.css`.
- Use one main app shell with a compact header, local-first/text-only status chips, a chat thread, and pinned composer.
- Keep the chat thread element present at page load with `id="result-panel"`, `role="log"`, `aria-live="polite"`, and a chat thread class.
- Put the welcome bot bubble inside the chat thread and include `Mình sẽ giúp bạn kiểm tra dấu hiệu lừa đảo tài chính.`
- Add a small hint row under the welcome bubble with channel/privacy hints, not a standalone card.
- Preserve `analysis-form`, `message-input`, `channel-select`, `sample-button`, and `analyze-button`.
- Keep supported channel option values `unknown`, `sms`, `zalo`, `messenger`, `telegram`, and `facebook`.
- Include user, bot/result, typing, and error templates. Use `data-slot` for every clone target inside templates.
- Outer template IDs may include compatibility IDs such as `result-template` and `error-template`, but inner template nodes must not have IDs.
- Keep `/static/demo.js` linked with `defer`.
Do not change `src/runtime/demo.py` or add `/static/i18n.js` in this phase.
</action>
<acceptance_criteria>
- `src/runtime/demo_assets/index.html` contains `role="log"` and `aria-live="polite"` in the static chat thread.
- `src/runtime/demo_assets/index.html` contains `Mình sẽ giúp bạn kiểm tra dấu hiệu lừa đảo tài chính.`
- `src/runtime/demo_assets/index.html` contains `data-slot="risk-tier"`, `data-slot="verdict"`, `data-slot="labels"`, `data-slot="grounded-cues"`, and `data-slot="recommendations"`.
- `src/runtime/demo_assets/index.html` contains `id="analysis-form"`, `id="message-input"`, `id="channel-select"`, `id="sample-button"`, and `id="analyze-button"`.
- `src/runtime/demo_assets/index.html` does not contain inner template IDs `result-summary`, `result-risk-tier`, `result-labels`, `result-backend`, `result-cues`, `result-recommendations`, `error-message`, or `error-steps`.
</acceptance_criteria>
</task>

<task id="T3" type="feature">
<title>Rewrite demo.css for the chat shell layout and visual tone</title>
<read_first>
- `.planning/phases/14-css-html-scaffolding/14-CONTEXT.md`
- `.planning/phases/14-css-html-scaffolding/14-RESEARCH.md`
- `src/runtime/demo_assets/demo.css`
- `src/runtime/demo_assets/index.html`
</read_first>
<action>
Replace the warm hero/card CSS with a light clinical chat stylesheet:
- Define a font stack beginning with `"Be Vietnam Pro"` followed by `"Segoe UI Variable Display"` and `system-ui`.
- Use a root/app shell height of `100dvh` with no horizontal overflow.
- Style `.shell` or the equivalent root chat app as a vertical flex container.
- Style the thread area with `flex: 1 1 0`, `min-height: 0`, `overflow-y: auto`, and readable centered max width.
- Style the composer as pinned inside the shell with `padding-bottom: calc(... + env(safe-area-inset-bottom))` or equivalent safe-area handling.
- Use navy/teal user bubbles aligned right and white bordered bot bubbles aligned left.
- Style compact bilingual risk pills with semantic benign/suspicious/high-risk colors.
- Style typing and error bubbles so Phase 16 can use the templates without inventing CSS.
- Include responsive rules for narrow screens without fixed phone-emulator dimensions.
- Include `@media (prefers-reduced-motion: reduce)` with transition/animation suppression for the baseline scaffold.
Keep CSS class names understandable for Phase 16 and Phase 17 follow-up work.
</action>
<acceptance_criteria>
- `src/runtime/demo_assets/demo.css` contains `"Be Vietnam Pro"`.
- `src/runtime/demo_assets/demo.css` contains `100dvh`.
- `src/runtime/demo_assets/demo.css` contains `flex: 1 1 0` and `min-height: 0`.
- `src/runtime/demo_assets/demo.css` contains `env(safe-area-inset-bottom)`.
- `src/runtime/demo_assets/demo.css` defines styles for user, bot, typing, error, and risk badge/pill states.
- `src/runtime/demo_assets/demo.css` contains a `prefers-reduced-motion` media query.
</acceptance_criteria>
</task>

<task id="T4" type="test">
<title>Run focused verification and confirm no backend drift</title>
<read_first>
- `tests/runtime/test_demo.py`
- `src/runtime/demo_assets/index.html`
- `src/runtime/demo_assets/demo.css`
- `src/runtime/demo.py`
</read_first>
<action>
Run focused verification for the static demo contract:
- Execute `python -m pytest tests/runtime/test_demo.py`.
- Confirm `src/runtime/demo.py` remains unchanged unless a prior task explicitly found a required test-only issue, which Phase 14 does not expect.
- If the existing `demo.js` has compatibility issues on page load, fix only HTML/CSS selector availability in this phase; do not rewrite fetch behavior.
- Record any intentional mismatch between old `demo.js` submit rendering and new `data-slot` templates as a deviation for Phase 16, not as a Phase 14 blocker unless page load breaks.
</action>
<acceptance_criteria>
- `python -m pytest tests/runtime/test_demo.py` exits 0.
- `git diff -- src/runtime/demo.py` is empty after execution.
- `git diff -- src/runtime/demo_assets/index.html src/runtime/demo_assets/demo.css tests/runtime/test_demo.py` shows only the planned static scaffold and test changes.
</acceptance_criteria>
</task>
</tasks>

<verification>
- Run `python -m pytest tests/runtime/test_demo.py`.
- Inspect `src/runtime/demo_assets/index.html` for a static `role="log"` chat thread and no inner template IDs.
- Inspect `src/runtime/demo_assets/demo.css` for `100dvh`, `flex: 1 1 0`, `min-height: 0`, `env(safe-area-inset-bottom)`, and `prefers-reduced-motion`.
- Confirm no changes were made to `src/runtime/demo.py`.
</verification>

<success_criteria>
- Opening the demo root serves a chat shell with header, scrollable thread, and bottom composer.
- Be Vietnam Pro is loaded from Google Fonts CDN.
- The static chat thread is present at page load with `role="log"` and `aria-live="polite"`.
- Template clone targets use `data-slot`, not inner IDs.
- Focused demo tests pass.
</success_criteria>
