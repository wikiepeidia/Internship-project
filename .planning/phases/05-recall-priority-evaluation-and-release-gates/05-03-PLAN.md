---
phase: 05-recall-priority-evaluation-and-release-gates
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - src/runtime/analyzers/local_model.py
  - src/model_adaptation/explanation_review.py
  - src/model_adaptation/cli.py
  - tests/runtime/test_local_model.py
  - tests/model_adaptation/test_explanation_review.py
  - tests/model_adaptation/test_cli.py
autonomous: false
requirements:
  - EVAL-03

must_haves:
  truths:
    - "Explanation scoring applies only to risky predictions and keeps benign outputs out of the rubric lane"
    - "Deterministic rubric checks reuse Phase 4 grounding and recommendation-safety semantics instead of inventing a second explanation policy"
    - "A curated manual review sample can be generated from risky predictions and recorded so final release verdicts can merge deterministic and human findings"
  artifacts:
    - path: "src/model_adaptation/explanation_review.py"
      provides: "Risky-only explanation rubric scorer and curated manual review pack builder"
      exports: ["score_explanation_rubric", "build_manual_review_pack"]
    - path: ".planning/phases/05-recall-priority-evaluation-and-release-gates/05-explanation-review-pack.json"
      provides: "Saved phase-local risky-only review pack with reviewable text, deterministic rubric findings, and reviewer decision fields"
      exports: ["phase_local_review_pack"]
    - path: "src/runtime/analyzers/local_model.py"
      provides: "Shared deterministic grounding and recommendation-safety helpers reused by the Phase 5 rubric"
      exports: ["build_analysis_result"]
    - path: "tests/model_adaptation/test_explanation_review.py"
      provides: "Regression coverage for risky-only scope, blocker-vs-flag policy, and review-pack generation"
      contains: "test_explanation_rubric_scopes_to_risky_predictions_only"
  key_links:
    - from: "src/model_adaptation/explanation_review.py"
      to: "src/runtime/analyzers/local_model.py"
      via: "deterministic rubric checks reuse the same exact-span and safe-recommendation semantics Phase 4 already enforces"
      pattern: "grounding|recommendation"
    - from: "src/model_adaptation/explanation_review.py"
      to: "src/model_adaptation/schemas.py"
      via: "only risky typed release-evaluation rows are selected for rubric scoring and manual review packs"
      pattern: "ReleaseEvaluationRow|risk_tier|threat_labels"
    - from: "src/model_adaptation/cli.py"
      to: ".planning/phases/05-recall-priority-evaluation-and-release-gates/05-explanation-review-pack.json"
      via: "a dedicated pre-verdict command writes the saved manual review pack and prints the path for the human checkpoint"
      pattern: "review-pack|path"
    - from: "src/model_adaptation/cli.py"
      to: ".planning/phases/05-recall-priority-evaluation-and-release-gates/05-evaluation-snapshot.json"
      via: "the pre-verdict command consumes the exact saved evaluation snapshot and preserves its run identifier in the review-pack metadata"
      pattern: "run_id|snapshot"
    - from: "tests/model_adaptation/test_explanation_review.py"
      to: "05-RESEARCH.md"
      via: "blocker findings are limited to unsafe recommendations and fabricated evidence while other issues stay advisory"
      pattern: "unsafe recommendations|fabricated evidence"
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD034 MD040 MD041 MD046 MD055 MD056 MD060 -->

<objective>
Implement the explanation-quality lane: deterministic rubric checks over risky predictions plus a curated manual review sample.

Purpose: EVAL-03 is not just a checklist. Phase 5 needs a rubric that scores grounding, label alignment, and safe recommendation quality over risky predictions only, while keeping explanation issues advisory unless they expose unsafe recommendations or fabricated evidence. This plan builds that lane after real evaluation output exists, without diluting the recall-first release policy.

Output: shared deterministic explanation-review helpers, a new `src/model_adaptation/explanation_review.py` scorer and review-pack builder, a dedicated pre-verdict review-pack generation command, focused rubric tests, and one blocking human-review checkpoint over the curated risky sample.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-recall-priority-evaluation-and-release-gates/05-CONTEXT.md
@.planning/phases/05-recall-priority-evaluation-and-release-gates/05-RESEARCH.md
@.planning/phases/05-recall-priority-evaluation-and-release-gates/05-PATTERNS.md
@src/runtime/contracts.py
@src/runtime/analyzers/local_model.py
@tests/runtime/test_local_model.py

<interfaces>
From src/runtime/contracts.py:
```python
class AnalysisResult(BaseModel):
    risk_tier: RiskTier
    summary: str
    top_cues: list[SuspiciousCue] = Field(default_factory=list, max_length=3)
    threat_labels: list[ThreatLabel] = Field(default_factory=list, max_length=2)
    recommendations: list[RecommendationText] = Field(default_factory=list, max_length=3)
    normalized_text: str | None = None
```

From src/runtime/analyzers/local_model.py:
```python
DEFAULT_RISKY_RECOMMENDATIONS = (...)
UNSAFE_RECOMMENDATION_MARKERS = (...)

def build_analysis_result(payload: dict[str, Any], request: AnalysisRequest, backend_name: str) -> AnalysisResult: ...
```
</interfaces>
</context>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime explanation output -> rubric scorer | risky predictions must be judged against the same grounding and recommendation-safety rules the product already ships |
| deterministic rubric -> final release gate | rubric blockers must stay narrowly scoped so advisory quality issues do not override the recall-first policy |
| curated review pack -> human reviewer | the human review sample must be explicit, risky-only, and ready for final verdict synthesis |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Integrity | src/model_adaptation/explanation_review.py | mitigate | Score only risky predictions per D-05 and keep the three locked rubric dimensions from D-04 explicit in code and tests. |
| T-05-03-02 | Tampering | src/runtime/analyzers/local_model.py | mitigate | Reuse exported grounding and unsafe-recommendation semantics so Phase 5 does not invent a second explanation policy that drifts from Phase 4. |
| T-05-03-03 | Repudiation | manual review pack | mitigate | Emit a curated risky-only sample with deterministic pre-checks and reviewer fields so human findings are traceable and can be merged later per D-07. |
| T-05-03-SC | Tampering | blocker policy | mitigate | Restrict explanation blockers to fabricated evidence and unsafe recommendations per D-06; everything else becomes a `FLAG`. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reuse Phase 4 explanation semantics and implement the risky-only deterministic rubric scorer</name>
  <files>
    src/runtime/analyzers/local_model.py,
    src/model_adaptation/explanation_review.py,
    tests/runtime/test_local_model.py,
    tests/model_adaptation/test_explanation_review.py
  </files>
  <read_first>
    .planning/phases/05-recall-priority-evaluation-and-release-gates/05-CONTEXT.md,
    .planning/phases/05-recall-priority-evaluation-and-release-gates/05-RESEARCH.md,
    src/runtime/analyzers/local_model.py,
    tests/runtime/test_local_model.py
  </read_first>
  <behavior>
    - Test: the rubric scores grounding, label alignment, and safe recommendation quality per D-04 and applies only to risky predictions per D-05
    - Test: fabricated evidence and unsafe recommendations become blocker reasons, while weaker grounding or label-alignment issues remain advisory flags per D-06
    - Test: deterministic checks reuse the same cue-grounding and recommendation-safety semantics Phase 4 already enforces
  </behavior>
  <action>
    Extract or expose the narrowest shared helper seam from `src/runtime/analyzers/local_model.py` that lets Phase 5 reuse exact-span grounding and unsafe-recommendation semantics without copying those rules into a third place. Then implement `src/model_adaptation/explanation_review.py` so it scores grounding, label alignment, and safe recommendation quality over risky predictions only, matching D-04 and D-05.

    Keep the blocker policy narrow per D-06. Unsafe recommendations and fabricated evidence block release; weak label alignment, thin cues, or generic safe advice do not. Do not let the rubric become a second release philosophy that competes with the recall-first policy from D-08.
  </action>
  <verify>
    <automated>python -m pytest tests/runtime/test_local_model.py tests/model_adaptation/test_explanation_review.py -q</automated>
  </verify>
  <acceptance_criteria>
    - new file `src/model_adaptation/explanation_review.py` exists
    - `tests/model_adaptation/test_explanation_review.py` contains `def test_explanation_rubric_scopes_to_risky_predictions_only`
    - `tests/model_adaptation/test_explanation_review.py` contains `def test_explanation_rubric_blocks_unsafe_recommendations_or_fabricated_evidence`
    - `tests/model_adaptation/test_explanation_review.py` contains `def test_explanation_rubric_flags_label_alignment_without_blocking`
    - extracted or reused helper logic in `src/runtime/analyzers/local_model.py` keeps Phase 4 grounding and safety semantics intact
    - `python -m pytest tests/runtime/test_local_model.py tests/model_adaptation/test_explanation_review.py -q` exits with code 0
  </acceptance_criteria>
  <done>The deterministic explanation rubric exists, is risky-only, and reuses the same grounding and recommendation-safety semantics the Phase 4 runtime already ships.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate a curated manual review pack for risky predictions with deterministic prefill</name>
  <files>
    src/model_adaptation/explanation_review.py,
    src/model_adaptation/cli.py,
    tests/model_adaptation/test_explanation_review.py,
    tests/model_adaptation/test_cli.py
  </files>
  <read_first>
    .planning/phases/05-recall-priority-evaluation-and-release-gates/05-RESEARCH.md,
    src/model_adaptation/explanation_review.py,
    tests/model_adaptation/test_explanation_review.py
  </read_first>
  <behavior>
    - Test: the manual review pack selects risky predictions only and includes reviewable source or normalized text, gold label, predicted labels, cues, recommendations, and deterministic pre-check results
    - Test: the same evaluated batch and sample-size settings produce the same curated review pack
    - Test: a dedicated pre-verdict CLI command consumes the exact saved 05-02 evaluation snapshot, writes the review pack to a deterministic phase-local path, and prints that path for the human checkpoint before Plan `05-04` runs
  </behavior>
  <action>
    Add a curated manual review-pack builder to `src/model_adaptation/explanation_review.py` that selects a deterministic risky-only subset from typed release-evaluation rows and emits a typed review object the human reviewer can complete. The saved pack must retain the reviewable source or normalized text needed for side-by-side inspection, prefill the deterministic rubric findings, and be written to a deterministic phase-local path such as `.planning/phases/05-recall-priority-evaluation-and-release-gates/05-explanation-review-pack.json`.

    Extend `src/model_adaptation/cli.py` with a dedicated pre-verdict command, such as `prepare-explanation-review`, that consumes the exact saved 05-02 evaluation snapshot, builds the phase-local review pack, stamps the pack with the same run identifier, and prints the concrete pack path for the human checkpoint. This command exists before the final release-eval command from Plan `05-04`; do not force the reviewer to wait until final verdict synthesis just to generate the review artifact.

    Keep this local and structured. Do not introduce cloud judging, hosted review tooling, or a notebook-only workflow. The output needs to feed the final release gate directly in Plan 05-04.
  </action>
  <verify>
    <automated>python -m pytest tests/model_adaptation/test_explanation_review.py tests/model_adaptation/test_cli.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/model_adaptation/test_explanation_review.py` contains `def test_manual_review_pack_selects_risky_predictions_only`
    - `tests/model_adaptation/test_explanation_review.py` contains `def test_manual_review_pack_is_deterministic_for_same_inputs`
    - `tests/model_adaptation/test_cli.py` contains `def test_cli_exposes_prepare_explanation_review_command`
    - `tests/model_adaptation/test_cli.py` contains `def test_prepare_explanation_review_command_prints_saved_pack_path`
    - the review pack includes reviewable source or normalized text, deterministic rubric findings, and fields for reviewer blocker and flag decisions
    - the pre-verdict command consumes the saved Phase 5 evaluation snapshot, writes a saved pack path under `.planning/phases/05-recall-priority-evaluation-and-release-gates/`, and preserves the same run identifier for the human checkpoint and final verdict
    - `python -m pytest tests/model_adaptation/test_explanation_review.py tests/model_adaptation/test_cli.py -q` exits with code 0
  </acceptance_criteria>
  <done>The human-review lane has a structured risky-only sample pack with deterministic prefill that later plans can merge into the final release verdict.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Review the curated risky-only explanation sample and record blocker-vs-flag findings</name>
  <what-built>Generated and saved a deterministic risky-only review pack under the Phase 5 directory from the exact Phase 5 evaluation snapshot, preserving reviewable source or normalized text, gold labels, predicted labels, cues, recommendations, prefilled rubric findings, and the shared run identifier.</what-built>
  <how-to-verify>
    1. Run the dedicated pre-verdict review-pack command and open the generated Phase 5 explanation-review pack path it prints.
    2. For each risky prediction, compare the original text, predicted labels, cues, and recommendations.
    3. Mark only fabricated evidence or unsafe recommendations as `BLOCK` findings per D-06.
    4. Mark weaker grounding, label-alignment, or recommendation-quality issues as `FLAG` findings.
    5. Save the completed review pack so Plan 05-04 can synthesize the final verdict and artifacts.
  </how-to-verify>
  <resume-signal>Type "approved" once the review pack is completed, or list the blocker and flag findings that must feed the final release verdict.</resume-signal>
</task>

</tasks>

<verification>
- `python -m pytest tests/runtime/test_local_model.py tests/model_adaptation/test_explanation_review.py -q` passes
- human review pack is completed and saved for Plan 05-04
</verification>

<success_criteria>
Phase 5 has a risky-only explanation-quality lane that merges deterministic checks with a curated human review sample while keeping blocker findings narrowly scoped to unsafe recommendations and fabricated evidence, and the review pack can be generated before final verdict synthesis.
</success_criteria>

<output>
After completion, create `.planning/phases/05-recall-priority-evaluation-and-release-gates/05-03-SUMMARY.md`
</output>

<!-- markdownlint-enable MD022 MD031 MD032 MD033 MD034 MD040 MD041 MD046 MD055 MD056 MD060 -->