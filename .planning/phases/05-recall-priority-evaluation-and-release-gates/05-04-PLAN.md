---
phase: 05-recall-priority-evaluation-and-release-gates
plan: 04
type: execute
wave: 4
depends_on:
  - 05-02
  - 05-03
files_modified:
  - src/model_adaptation/release_gates.py
  - src/model_adaptation/cli.py
  - src/runtime/doctor.py
  - tests/model_adaptation/test_cli.py
  - tests/model_adaptation/test_release_gates.py
  - tests/runtime/test_doctor.py
autonomous: true
requirements:
  - EVAL-01
  - EVAL-02
  - EVAL-03

must_haves:
  truths:
    - "The final release verdict is `PASS`, `BLOCK`, or `FLAG` with explicit blocker and flag reasons"
    - "One Phase 5 run emits both a phase-local markdown report and a machine-readable JSON artifact containing metrics, reasons, and rubric summary"
    - "Operator entrypoints can run or read the release gate without moving evaluation ownership into the live runtime path or Phase 6 UI work"
  artifacts:
    - path: "src/model_adaptation/release_gates.py"
      provides: "Recall-first verdict synthesis and paired artifact writers for Phase 5 release runs"
      exports: ["synthesize_release_verdict", "write_release_artifacts"]
    - path: "src/model_adaptation/cli.py"
      provides: "Operator command that runs the final Phase 5 release-eval flow and prints verdict plus artifact paths after the review pack is complete"
      exports: ["build_parser"]
    - path: "tests/model_adaptation/test_release_gates.py"
      provides: "Regression coverage for blocker logic, advisory flags, and artifact contents"
      contains: "test_release_gate_blocks_on_zero_support_or_recall_floor_miss"
    - path: "src/runtime/doctor.py"
      provides: "Optional latest-release summary that reads the saved artifact instead of recomputing evaluation"
      exports: ["run_runtime_doctor", "format_doctor_report"]
  key_links:
    - from: "src/model_adaptation/release_gates.py"
      to: "src/model_adaptation/release_evaluation.py"
      via: "overall and per-label metrics feed the recall-first verdict engine"
      pattern: "recall|support|overall_f1"
    - from: "src/model_adaptation/release_gates.py"
      to: "src/model_adaptation/explanation_review.py"
      via: "deterministic and manual rubric summaries merge into blocker and flag reasons"
      pattern: "rubric|manual_review"
    - from: "src/model_adaptation/cli.py"
      to: "data/manifests"
      via: "one operator command writes the JSON artifact and phase-local markdown report from the same typed release result"
      pattern: "manifest|report"
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD034 MD040 MD041 MD046 MD055 MD056 MD060 -->

<objective>
Synthesize the final recall-first release verdict and emit the paired Phase 5 artifacts.

Purpose: After metrics and rubric review exist, Phase 5 still needs one release-engineering layer that enforces the locked recall policy, merges advisory versus blocking explanation findings, and writes the required markdown-plus-JSON evidence of the decision. This plan completes that final gate and makes it runnable from the existing operator tooling.

Output: a recall-first release-gate engine, artifact writers for `.planning/phases/05-recall-priority-evaluation-and-release-gates/` and `data/manifests/`, CLI orchestration, and an optional doctor summary that reads the saved artifact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-recall-priority-evaluation-and-release-gates/05-CONTEXT.md
@.planning/phases/05-recall-priority-evaluation-and-release-gates/05-RESEARCH.md
@.planning/phases/05-recall-priority-evaluation-and-release-gates/05-PATTERNS.md
@src/model_adaptation/cli.py
@src/runtime/doctor.py
@src/runtime/service.py

<interfaces>
From src/model_adaptation/cli.py:
```python
def build_parser() -> argparse.ArgumentParser: ...
```

From src/runtime/doctor.py:
```python
def run_runtime_doctor() -> DoctorStatus: ...
def format_doctor_report(status: DoctorStatus) -> str: ...
```

From src/runtime/service.py:
```python
def build_default_runtime_service() -> RuntimeService: ...
```
</interfaces>
</context>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release metrics and rubric findings -> final verdict engine | the release gate must preserve the locked recall-first policy and explicit explanation blocker policy |
| typed release result -> markdown and JSON artifacts | both artifact forms must come from one canonical result model so their contents cannot drift |
| saved release artifact -> operator doctor summary | runtime doctor may surface latest verdict state, but it must not become a second evaluator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Integrity | src/model_adaptation/release_gates.py | mitigate | Enforce D-01 through D-03 exactly: all risky labels are hard-blocking and each must meet the `0.90` recall floor to avoid a `BLOCK`. |
| T-05-04-02 | Tampering | artifact writer | mitigate | Emit both markdown and JSON artifacts from one typed release result, including overall metrics, per-label metrics, reasons, and rubric summary per D-11 through D-14. |
| T-05-04-03 | Information Disclosure | release artifacts | mitigate | Keep artifact contents limited to evaluation summaries and reasons; do not dump raw held-out message text or raw model payloads into the report. |
| T-05-04-SC | Tampering | runtime doctor summary | mitigate | If operator visibility is added, read the latest saved artifact summary only; do not re-run evaluation or move release logic into the live runtime path. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement recall-first verdict synthesis and paired markdown plus JSON release artifacts</name>
  <files>
    src/model_adaptation/release_gates.py,
    tests/model_adaptation/test_release_gates.py
  </files>
  <read_first>
    .planning/phases/05-recall-priority-evaluation-and-release-gates/05-CONTEXT.md,
    .planning/phases/05-recall-priority-evaluation-and-release-gates/05-RESEARCH.md,
    tests/model_adaptation/test_release_evaluation.py,
    tests/model_adaptation/test_explanation_review.py
  </read_first>
  <behavior>
    - Test: any risky-label `support == 0` or recall below `0.90` produces a `BLOCK` per D-01 through D-03
    - Test: explanation blockers from Plan 05-03 also produce `BLOCK`, while non-blocking rubric issues produce `FLAG` only per D-06
    - Test: both the markdown report and JSON artifact include overall and per-class metrics, blocker or flag reasons, and rubric summary per D-11 through D-14
  </behavior>
  <action>
    Create `src/model_adaptation/release_gates.py` to synthesize the final Phase 5 verdict from the readiness audit, the held-out metrics, and the explanation-review summary. Enforce the hard-blocking risky-label policy from D-01 through D-03 exactly, keep the overall gate philosophy recall-first per D-08, and merge explanation blockers only for fabricated evidence or unsafe recommendations per D-06.

    Write one human-readable markdown report under `.planning/phases/05-recall-priority-evaluation-and-release-gates/` and one machine-readable JSON artifact under `data/manifests/` per D-11 and D-13. Both artifacts must come from the same typed release result, must expose `PASS | BLOCK | FLAG` per D-12, and must include the full contents D-14 locks. Do not introduce a balanced blended score, do not emit binary-only verdicts, and do not add Phase 6 UI output here.
  </action>
  <verify>
    <automated>python -m pytest tests/model_adaptation/test_release_gates.py -q</automated>
  </verify>
  <acceptance_criteria>
    - new file `src/model_adaptation/release_gates.py` exists
    - `tests/model_adaptation/test_release_gates.py` contains `def test_release_gate_blocks_on_zero_support_or_recall_floor_miss`
    - `tests/model_adaptation/test_release_gates.py` contains `def test_release_gate_flags_nonblocking_explanation_issues`
    - `tests/model_adaptation/test_release_gates.py` contains `def test_release_artifact_includes_metrics_reasons_and_rubric_summary`
    - the artifact writer targets `.planning/phases/05-recall-priority-evaluation-and-release-gates/` for markdown and `data/manifests/` for JSON
    - `python -m pytest tests/model_adaptation/test_release_gates.py -q` exits with code 0
  </acceptance_criteria>
  <done>The final Phase 5 gate can synthesize a recall-first `PASS`, `BLOCK`, or `FLAG` verdict and emit both required artifact forms from one canonical result.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add operator orchestration and optional doctor summary without moving evaluation into runtime or UI scope</name>
  <files>
    src/model_adaptation/cli.py,
    src/runtime/doctor.py,
    tests/model_adaptation/test_cli.py,
    tests/runtime/test_doctor.py
  </files>
  <read_first>
    .planning/phases/05-recall-priority-evaluation-and-release-gates/05-PATTERNS.md,
    src/model_adaptation/cli.py,
    src/runtime/doctor.py,
    tests/model_adaptation/test_cli.py,
    tests/runtime/test_doctor.py
  </read_first>
  <behavior>
    - Test: the model-adaptation CLI exposes one final release-eval command that consumes the exact saved evaluation snapshot from Plan `05-02` together with the completed review pack from Plan `05-03`, then runs verdict synthesis and artifact writing
    - Test: the command prints the final verdict plus the markdown and JSON output paths and fails explicitly if the saved evaluation snapshot is missing or if the required review pack from Plan `05-03` is missing, incomplete, or bound to a different run identifier
    - Test: if operator visibility is added to `runtime doctor`, it reads the latest saved release artifact summary rather than recomputing metrics or opening Phase 6 UI work
  </behavior>
  <action>
    Extend `src/model_adaptation/cli.py` with a dedicated final Phase 5 release-eval command that consumes the readiness audit, the exact saved evaluation snapshot from Plan `05-02`, and the completed manual review pack from Plan `05-03`, then performs verdict synthesis and artifact writing. The final command must reject missing or mismatched run identifiers so the reviewed batch cannot drift from the batch that receives the final verdict. Keep this in the existing offline model-adaptation operator surface instead of moving release logic into `src/runtime/service.py`, and make the command print the final verdict and artifact paths clearly.

    If operator-facing release status is useful, update `src/runtime/doctor.py` to read the latest saved JSON artifact summary only. That preserves D-09’s runtime boundary and keeps D-10 intact by avoiding any Phase 6 UI work. Do not add a frontend, do not teach doctor to recompute the release gate, and do not break the existing `pilot`, `train`, `convert`, or `doctor` command surfaces.
  </action>
  <verify>
    <automated>python -m pytest tests/model_adaptation/test_cli.py tests/runtime/test_doctor.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/model_adaptation/test_cli.py` contains `def test_cli_exposes_release_eval_command`
    - `tests/model_adaptation/test_cli.py` contains `def test_release_eval_command_prints_verdict_and_artifact_paths`
    - `tests/runtime/test_doctor.py` contains `def test_doctor_reads_latest_release_gate_summary`
    - the final release-eval command fails explicitly when the saved evaluation snapshot is missing, or when the review pack generated and completed in Plan `05-03` is missing, incomplete, or bound to a different run identifier
    - `python -m pytest tests/model_adaptation/test_cli.py tests/runtime/test_doctor.py -q` exits with code 0
  </acceptance_criteria>
  <done>Operators can run the full Phase 5 release gate from the model-adaptation CLI and optionally read the latest saved verdict through doctor without pushing evaluation logic into the live runtime path.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/model_adaptation/test_release_gates.py tests/model_adaptation/test_cli.py tests/runtime/test_doctor.py -q` passes
</verification>

<success_criteria>
Phase 5 ends with one recall-first final release command that consumes the exact saved evaluation snapshot from Plan `05-02` plus the completed review pack from Plan `05-03`, produces `PASS`, `BLOCK`, or `FLAG` artifacts, preserves explicit blocker and flag reasons, and keeps the demo UI out of scope until Phase 6.
</success_criteria>

<output>
After completion, create `.planning/phases/05-recall-priority-evaluation-and-release-gates/05-04-SUMMARY.md`
</output>

<!-- markdownlint-enable MD022 MD031 MD032 MD033 MD034 MD040 MD041 MD046 MD055 MD056 MD060 -->