---
phase: 12-cambridgeus-presentation-revamp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - documents/reports/latex/slides.tex
  - documents/reports/latex/slides/preamble/colors.tex
autonomous: true
requirements:
  - THME-01
  - THME-02
  - THME-03
  - THME-04
  - THME-05

must_haves:
  truths:
    - "slides.tex declares \\usetheme{CambridgeUS} and \\usecolortheme{beaver}"
    - "slides.tex preamble contains no Metropolis-only color keys (progress bar, title separator)"
    - "slides.tex declares the 3-column custom footline (author | short title | N/Total)"
    - "slides.tex declares \\logo{\\includegraphics[height=0.5cm]{usth.png}}"
    - "slides.tex metadata uses short-form bracket syntax for title, author, and institute (per D-10)"
    - "slides.tex contains 8 \\section{} markers placed between \\input{} calls (per D-04)"
    - "slides.tex declares \\setbeamercolor{block title}{fg=white,bg=CVBLUE} (per D-02)"
    - "colors.tex defines CVBLUE token and removes CVBLUElight/CVBLUEdark Metropolis leftovers"
  artifacts:
    - path: "documents/reports/latex/slides.tex"
      provides: "CambridgeUS-themed Beamer entry point"
      contains: "\\usetheme{CambridgeUS}"
    - path: "documents/reports/latex/slides/preamble/colors.tex"
      provides: "CVBLUE color token, Metropolis colorlets removed"
      contains: "\\definecolor{CVBLUE}{HTML}{1A5276}"
  key_links:
    - from: "slides.tex"
      to: "slides/preamble/colors.tex"
      via: "\\input{slides/preamble/colors.tex} — must appear before \\setbeamercolor calls"
      pattern: "\\\\input\\{slides/preamble/colors\\.tex\\}"
    - from: "slides.tex \\setbeamertemplate{footline}"
      to: "beamerouterthemeinfolines"
      via: "replaces the default infolines footline with 3-column beamercolorbox template"
      pattern: "\\\\insertframenumber.*\\\\inserttotalframenumber"
---

<objective>
Overhaul the Beamer preamble in slides.tex: replace Metropolis with CambridgeUS/beaver, add USTH
branding (logo, footer, color overrides), insert the 8 section markers, and update all metadata
to the D-10 short-form bracket syntax. Clean up colors.tex to remove Metropolis-only colorlets.

Purpose: All downstream content tasks (Plan 02) and compilation (Plan 03) depend on a clean
CambridgeUS preamble. This plan establishes the authoritative theme layer that every slide inherits.

Output:
- documents/reports/latex/slides.tex — full CambridgeUS preamble + section markers
- documents/reports/latex/slides/preamble/colors.tex — CVBLUE only, no Metropolis leftovers
</objective>

<execution_context>
@C:/Users/wikiepeidia/OneDrive - caugiay.edu.vn/bài tập/usth/GEN14/INTERNSHIP/Internship-project/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wikiepeidia/OneDrive - caugiay.edu.vn/bài tập/usth/GEN14/INTERNSHIP/Internship-project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-cambridgeus-presentation-revamp/12-CONTEXT.md
@.planning/phases/12-cambridgeus-presentation-revamp/12-RESEARCH.md

<!-- CANONICAL TEMPLATE — read before touching any preamble line -->
@documents/reports/latex/slides/reference_themes.tex

<!-- CURRENT ENTRY POINT — read to identify every line that must change -->
@documents/reports/latex/slides.tex

<!-- COLOR TOKEN FILE — read to identify Metropolis colorlets to remove -->
@documents/reports/latex/slides/preamble/colors.tex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Metropolis preamble with CambridgeUS/beaver in slides.tex</name>
  <files>documents/reports/latex/slides.tex</files>

  <read_first>
    - documents/reports/latex/slides/reference_themes.tex — CANONICAL TEMPLATE.
      Study lines 1-47 (theme declaration through logo). Copy the footline template verbatim.
    - documents/reports/latex/slides.tex — CURRENT STATE.
      Identify every line to remove and every line to add. Key removal targets (per RESEARCH.md Pitfall 1):
        \usetheme{metropolis}
        \setbeamercolor{frametitle}{bg=CVBLUE, fg=white}
        \setbeamercolor{progress bar}{fg=CVBLUE}
        \setbeamercolor{title separator}{fg=CVBLUE}
        \setbeamercolor{alerted text}{fg=CVBLUE}
      Current metadata lines to replace (no short-form bracket syntax):
        \title{Localized Explainable AI...}
        \author{Phạm Thế Minh} (raw LaTeX escape, not Unicode)
        \institute{...}
  </read_first>

  <action>
    Rewrite documents/reports/latex/slides.tex. Preserve: \documentclass[aspectratio=169,12pt]{beamer}
    (keep 12pt — reference uses 10pt but our deck has established 12pt; do not change font size unless
    compile overflow requires it in Plan 03), \input{slides/preamble/packages.tex},
    \setsansfont and \setmonofont lines, \graphicspath, \setbeamercovered{invisible},
    \begin{document}...\end{document}, and all \input{slides/sections/...} calls.

    REPLACE the theme block and metadata block as follows:

    Theme block (replace \usetheme{metropolis} and the four Metropolis \setbeamercolor lines):
      \usetheme{CambridgeUS}
      \usecolortheme{beaver}

    After \input{slides/preamble/colors.tex} and before \input{slides/preamble/packages.tex},
    insert these CambridgeUS color and font overrides:
      \setbeamercolor{block title}{fg=white, bg=CVBLUE}
      \setbeamercolor{block body}{bg=CVBLUE!10!white}
      \setbeamercolor{framesubtitle}{fg=darkgray}
      \setbeamerfont{framesubtitle}{size=\normalsize, series=\mdseries}
      \setbeamerfont{section in head/foot}{series=\bfseries}
      \setbeamertemplate{navigation symbols}{}

    After the font overrides, insert the 3-column footline template copied verbatim from
    reference_themes.tex lines 29-44:
      \setbeamertemplate{footline}{%
        \leavevmode%
        \hbox{%
          \begin{beamercolorbox}[wd=.333\paperwidth,ht=2.25ex,dp=1ex,center]{author in head/foot}%
            \usebeamerfont{author in head/foot}\insertshortauthor
          \end{beamercolorbox}%
          \begin{beamercolorbox}[wd=.333\paperwidth,ht=2.25ex,dp=1ex,center]{title in head/foot}%
            \usebeamerfont{title in head/foot}\insertshorttitle
          \end{beamercolorbox}%
          \begin{beamercolorbox}[wd=.333\paperwidth,ht=2.25ex,dp=1ex,right]{date in head/foot}%
            \usebeamerfont{date in head/foot}\insertshortdate{}\hspace*{2em}
            \insertframenumber{} / \inserttotalframenumber\hspace*{2ex}
          \end{beamercolorbox}%
        }%
        \vskip0pt%
      }

    After the footline block, insert the logo line copied verbatim from reference_themes.tex line 47:
      \logo{\includegraphics[height=0.5cm]{usth.png}}

    Replace the four metadata lines with D-10 short-form bracket syntax (use UTF-8 Unicode
    characters directly, not LaTeX escape sequences like \d{a}):
      \title[Localized XAI for Vietnamese Phishing]{Localized Explainable AI for Vietnamese\\Financial Phishing Detection}
      \subtitle{Graduation Thesis Defense}
      \author[Phạm Thế Minh]{Phạm Thế Minh \\ {\small Student ID: 23BI14279}}
      \institute[USTH]{University of Science and Technology of Hanoi (USTH) \\ {\small Supervisors: Giang Anh Tuấn \quad|\quad Nguyễn Việt Anh}}
      \date{2026}

    Section markers — insert 8 \section{} declarations between the \input{} calls in the
    document body, exactly as specified in D-04 and RESEARCH.md Pattern 3.
    No \section{} before 01_title.tex or 02_agenda.tex. The first section marker appears
    between \input{sections/02_agenda.tex} and \input{sections/03_problem.tex}:
      \input{slides/sections/01_title.tex}
      \input{slides/sections/02_agenda.tex}

      \section{1. Motivation}
      \input{slides/sections/03_problem.tex}

      \section{2. Architecture}
      \input{slides/sections/04_architecture.tex}

      \section{3. Data Pipeline}
      \input{slides/sections/05_data.tex}

      \section{4. Why Local?}
      \input{slides/sections/06_why_local.tex}

      \section{5. Model}
      \input{slides/sections/07_model.tex}

      \section{6. Evaluation}
      \input{slides/sections/08_evaluation.tex}
      \input{slides/sections/09_confusion.tex}

      \section{7. Demo}
      \input{slides/sections/10_demo.tex}

      \section{8. Conclusion}
      \input{slides/sections/11_contributions.tex}
      \input{slides/sections/12_future.tex}
  </action>

  <verify>
    <automated>
      cd "documents/reports/latex" && grep -c "\\usetheme{CambridgeUS}" slides.tex
      grep -c "\\usecolortheme{beaver}" slides.tex
      grep -c "\\setbeamercolor{block title}" slides.tex
      grep -c "\\logo{" slides.tex
      grep -c "\\insertframenumber" slides.tex
      grep -c "\\\\section{1. Motivation}" slides.tex
      grep -c "metropolis" slides.tex
      grep -c "progress bar" slides.tex
    </automated>
  </verify>

  <acceptance_criteria>
    - slides.tex contains \usetheme{CambridgeUS} (count = 1)
    - slides.tex contains \usecolortheme{beaver} (count = 1)
    - slides.tex contains \setbeamercolor{block title}{fg=white, bg=CVBLUE} (count = 1)
    - slides.tex contains \logo{\includegraphics[height=0.5cm]{usth.png}} (count = 1)
    - slides.tex contains \insertframenumber{} / \inserttotalframenumber (count = 1)
    - slides.tex contains \section{1. Motivation} (count = 1)
    - slides.tex contains \section{8. Conclusion} (count = 1)
    - slides.tex does NOT contain "metropolis" (case-insensitive count = 0)
    - slides.tex does NOT contain "progress bar" (count = 0)
    - slides.tex does NOT contain "title separator" (count = 0)
    - \title[Localized XAI for Vietnamese Phishing]{...} present (short bracket form)
    - \author[Phạm Thế Minh]{...} present (short bracket form)
    - \institute[USTH]{...} present (short bracket form)
  </acceptance_criteria>

  <done>slides.tex is a complete CambridgeUS/beaver Beamer entry point with custom footline, USTH logo,
  CVBLUE block title override, and 8 section markers. All Metropolis theme code is removed.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up colors.tex — remove Metropolis colorlets</name>
  <files>documents/reports/latex/slides/preamble/colors.tex</files>

  <read_first>
    - documents/reports/latex/slides/preamble/colors.tex — CURRENT STATE.
      Current file defines: CVBLUE (keep), CVBLUElight (remove), CVBLUEdark (remove).
      CVBLUElight and CVBLUEdark were only used by Metropolis \setbeamercolor hooks in slides.tex
      which are now removed. Keeping unused colorlets is harmless but confusing.
    - documents/reports/latex/slides/reference_themes.tex — reference does not define CVBLUElight
      or CVBLUEdark, confirming those are Metropolis-only artifacts.
    - RESEARCH.md "State of the Art" section — explicitly lists CVBLUElight/CVBLUEdark as deprecated.
  </read_first>

  <action>
    Replace the full content of colors.tex with only:
      %% Central color token — change one hex value to recolor the whole deck
      \definecolor{CVBLUE}{HTML}{1A5276}

    Remove the two \colorlet lines:
      \colorlet{CVBLUElight}{CVBLUE!15}  ← remove
      \colorlet{CVBLUEdark}{CVBLUE!85}   ← remove

    Remove the comment block "Metropolis theme color wiring (must come AFTER \usetheme{metropolis})"
    since it is no longer accurate.

    The CVBLUE!10!white expression used in \setbeamercolor{block body} in slides.tex is an
    inline xcolor tint expression that does NOT require a named colorlet — xcolor resolves it
    at use time. No new colorlet is needed.
  </action>

  <verify>
    <automated>
      cd "documents/reports/latex/slides/preamble" && grep -c "CVBLUE" colors.tex
      grep -c "CVBLUElight" colors.tex
      grep -c "CVBLUEdark" colors.tex
      grep -c "metropolis" colors.tex
    </automated>
  </verify>

  <acceptance_criteria>
    - colors.tex contains \definecolor{CVBLUE}{HTML}{1A5276} (count = 1)
    - colors.tex does NOT contain CVBLUElight (count = 0)
    - colors.tex does NOT contain CVBLUEdark (count = 0)
    - colors.tex does NOT contain "metropolis" (count = 0)
    - File is 3-4 lines (comment + definecolor + blank line)
  </acceptance_criteria>

  <done>colors.tex contains only the CVBLUE token definition. All Metropolis-only colorlet
  declarations are removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LaTeX source → XeLaTeX compiler | Static document compilation; no user input, no network, no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | slides.tex preamble | accept | File is tracked in git; any accidental overwrite is recoverable. No secret or runtime state. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — all MiKTeX packages pre-installed and verified. No slopcheck needed. |
</threat_model>

<verification>
After both tasks complete:
1. grep -c "\\usetheme{CambridgeUS}" documents/reports/latex/slides.tex should return 1
2. grep -c "metropolis" documents/reports/latex/slides.tex should return 0
3. grep -c "CVBLUElight" documents/reports/latex/slides/preamble/colors.tex should return 0
4. grep -c "\\\\section{" documents/reports/latex/slides.tex should return 8
5. grep -c "\\\\logo{" documents/reports/latex/slides.tex should return 1
</verification>

<success_criteria>
- slides.tex has \usetheme{CambridgeUS} + \usecolortheme{beaver}, no Metropolis remnants
- slides.tex has the 3-column footline (author | title | N/Total) copied from reference_themes.tex
- slides.tex has \logo{\includegraphics[height=0.5cm]{usth.png}}
- slides.tex has \setbeamercolor{block title}{fg=white, bg=CVBLUE} and \setbeamercolor{block body}{bg=CVBLUE!10!white}
- slides.tex has 8 \section{} markers in the document body at the correct positions (D-04)
- slides.tex has D-10 metadata with short bracket forms for title, author, institute
- colors.tex has only \definecolor{CVBLUE}{HTML}{1A5276}
</success_criteria>

<output>
Create .planning/phases/12-cambridgeus-presentation-revamp/12-01-SUMMARY.md when done
</output>
