---
phase: 02-offline-text-ingestion-and-privacy-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/settings.py
  - src/runtime/__init__.py
  - src/runtime/contracts.py
  - src/runtime/analyzers/__init__.py
  - src/runtime/analyzers/base.py
  - tests/runtime/__init__.py
  - tests/runtime/conftest.py
  - tests/runtime/test_contracts.py
  - tests/runtime/test_service.py
  - tests/runtime/test_cli.py
  - tests/runtime/test_privacy.py
  - tests/runtime/test_doctor.py
autonomous: true
requirements:
  - ING-01
  - ING-02
  - RUN-01

must_haves:
  truths:
    - "Phase 2 has explicit runtime contracts for one-message text analysis and doctor readiness reporting"
    - "Wave 0 tests exist for contracts, service, CLI, privacy, and doctor behavior before runtime implementation starts"
    - "Runtime settings carry the local-only, text-only, fail-closed, and cue-cap constraints without introducing cloud fallback"
    - "A swappable analyzer protocol exists so the Phase 2 heuristic backend can be replaced in later phases without changing the CLI contract"
  artifacts:
    - path: "src/runtime/contracts.py"
      provides: "Typed request, result, cue, and doctor status models for the offline runtime"
      exports: ["AnalysisRequest", "AnalysisResult", "SuspiciousCue", "DoctorCheck", "DoctorStatus"]
    - path: "src/runtime/analyzers/base.py"
      provides: "Protocol seam for heuristic and future local-model analyzers"
      exports: ["AnalyzerBackend"]
    - path: "tests/runtime/test_contracts.py"
      provides: "Executable contract checks for runtime models and risk-tier compatibility"
      contains: "test_analysis_result_caps_top_cues"
    - path: "tests/runtime/test_service.py"
      provides: "Wave 0 service expectations for normalize-first orchestration and cue quoting"
      contains: "test_runtime_service_normalizes_once_before_analysis"
    - path: "tests/runtime/test_cli.py"
      provides: "Wave 0 CLI expectations for stdin-first analyze flow"
      contains: "test_analyze_reads_single_message_from_stdin_when_text_flag_absent"
    - path: "tests/runtime/test_privacy.py"
      provides: "Wave 0 privacy expectations for local-only and no-persistence behavior"
      contains: "test_default_runtime_path_never_requests_network_fallback"
    - path: "tests/runtime/test_doctor.py"
      provides: "Wave 0 readiness expectations for doctor output and setup guidance"
      contains: "test_doctor_reports_missing_runtime_dependency_with_exact_command"
  key_links:
    - from: "src/runtime/contracts.py"
      to: "src/data_pipeline/schemas.py"
      via: "shared benign/suspicious/high-risk risk tier vocabulary"
      pattern: "high-risk"
    - from: "tests/runtime/test_contracts.py"
      to: "src/runtime/contracts.py"
      via: "Pydantic model validation and cue-cap assertions"
      pattern: "from src.runtime.contracts import"
    - from: "tests/runtime/test_service.py"
      to: "src/runtime/service.py"
      via: "importlib-based Wave 0 expectations to be satisfied in Plan 02"
      pattern: "importlib.import_module(\"src.runtime.service\")"
    - from: "src/config/settings.py"
      to: "src/runtime/analyzers/base.py"
      via: "runtime backend selection and privacy defaults"
      pattern: "runtime_backend"
---

<objective>
Define the runtime contracts, settings, and Wave 0 tests for the offline text-ingestion slice before the heuristic analyzer and CLI are implemented.

Purpose: Phase 2 needs a narrow, privacy-safe contract surface that locks in the one-message text-only flow, local-only failure posture, and swappable analyzer seam before implementation starts. This plan encodes those rules as executable tests so later plans cannot drift away from D-01 through D-13.

Output: Runtime package skeleton, analyzer protocol, extended settings, and Wave 0 test files for contracts, service, CLI, privacy, and doctor behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-offline-text-ingestion-and-privacy-baseline/02-CONTEXT.md
@.planning/phases/02-offline-text-ingestion-and-privacy-baseline/02-RESEARCH.md
@.planning/phases/02-offline-text-ingestion-and-privacy-baseline/02-VALIDATION.md
@src/config/settings.py
@src/data_pipeline/processing/normalizer.py
@src/data_pipeline/schemas.py

<interfaces>
From src/config/settings.py:
```python
class Settings(BaseSettings):
    data_dir: Path = Path("data")
    split_ratios: tuple[float, float, float] = (0.8, 0.1, 0.1)
    similarity_threshold: float = 0.85
```

From src/data_pipeline/processing/normalizer.py:

```python
def normalize_text(text: str) -> str:
    text = ftfy.fix_text(text)
    text = unicodedata.normalize("NFC", text)
    text = text.strip()
    return re.sub(r"\s+", " ", text)
```

From src/data_pipeline/schemas.py:

```python
class DatasetRecord(BaseModel):
    risk_tier: Literal["benign", "suspicious", "high-risk"]
```

</interfaces>
</context>

<threat_model>

- Asset: pasted financial message text must remain local and memory-only by default per D-07 and D-09.
- Threat: runtime implementation drifts away from the dataset-compatible risk tiers or returns more than three cues, breaking downstream Phase 4/5 expectations.
- Threat: service, CLI, or doctor work lands without Wave 0 tests, letting privacy regressions or cloud fallback sneak in later.
- Required control in this plan: encode text-only, local-only, fail-closed, and cue-cap rules in contracts, settings, and test fixtures before any analyzer logic is written.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create runtime contracts, analyzer protocol, and runtime settings extensions</name>
  <files>
    src/config/settings.py,
    src/runtime/__init__.py,
    src/runtime/contracts.py,
    src/runtime/analyzers/__init__.py,
    src/runtime/analyzers/base.py,
    tests/runtime/__init__.py,
    tests/runtime/test_contracts.py
  </files>
  <read_first>
    .planning/phases/02-offline-text-ingestion-and-privacy-baseline/02-CONTEXT.md,
    .planning/phases/02-offline-text-ingestion-and-privacy-baseline/02-RESEARCH.md,
    .planning/phases/02-offline-text-ingestion-and-privacy-baseline/02-VALIDATION.md,
    src/config/settings.py,
    src/data_pipeline/processing/normalizer.py,
    src/data_pipeline/schemas.py
  </read_first>
  <behavior>
    - Test: AnalysisRequest accepts exactly one text payload and an optional channel in {unknown, sms, zalo, messenger, telegram, facebook} per D-02 and D-03
    - Test: AnalysisRequest rejects empty or whitespace-only text
    - Test: SuspiciousCue requires a non-empty quoted span and non-empty plain-language reason per D-10
    - Test: AnalysisResult accepts only benign, suspicious, or high-risk and defaults provisional=True per D-05
    - Test: AnalysisResult rejects more than 3 top_cues per D-11
    - Test: DoctorStatus exposes ready, local_only, text_only, checks, and setup_steps for doctor/analyze self-check flows per D-12 and D-13
    - Test: runtime RiskTier values match DatasetRecord.risk_tier exactly so later phases can swap backends without changing the contract
    - Test: AnalyzerBackend protocol requires backend_name, doctor(), and analyze() methods and does not include any cloud fallback method per D-04 and D-09
    - Test: Settings exposes runtime_backend="heuristic", runtime_max_cues=3, runtime_min_text_chars=8, runtime_store_raw_text=False, runtime_fail_closed=True, and runtime_text_only_message with the Phase 2 boundary wording
  </behavior>
  <action>
    Create the new `src/runtime/` package and `src/runtime/analyzers/` subpackage so all Phase 2 runtime code lives under `src/runtime/` as recommended by the research.

    Update `src/config/settings.py` to add Phase 2 runtime fields with these exact names and defaults: `runtime_backend: str = "heuristic"`, `runtime_max_cues: int = 3`, `runtime_min_text_chars: int = 8`, `runtime_store_raw_text: bool = False`, `runtime_fail_closed: bool = True`, `runtime_allow_text_flag: bool = True`, and `runtime_text_only_message: str = "Text-only v1: paste extracted text manually. Images/OCR and audio are not accepted in Phase 2."`. Do not add any cloud endpoint, API fallback, or remote transport fields.

    In `src/runtime/contracts.py`, define the typed runtime DTOs that later plans must use exactly: `ChannelName = Literal["unknown", "sms", "zalo", "messenger", "telegram", "facebook"]`, `RiskTier = Literal["benign", "suspicious", "high-risk"]`, `SuspiciousCue`, `AnalysisRequest`, `AnalysisResult`, `DoctorCheck`, and `DoctorStatus`. `AnalysisResult.top_cues` must enforce a maximum of 3 entries. `DoctorStatus` must include `local_only: bool = True` and `text_only: bool = True` so the privacy boundary is part of the contract.

    In `src/runtime/analyzers/base.py`, define `AnalyzerBackend` as a `typing.Protocol` with `backend_name: str`, `doctor() -> DoctorStatus`, and `analyze(request: AnalysisRequest) -> AnalysisResult`. Keep the interface local-only and backend-agnostic so the Phase 2 heuristic implementation can be replaced by a local model backend in Phase 3/4 without changing CLI or renderer code.

    In `tests/runtime/test_contracts.py`, write real pytest coverage for those models using `ValidationError`, `typing.get_args`, and exact string assertions against the new settings fields. The tests must confirm that runtime risk tiers stay aligned with `src.data_pipeline.schemas.DatasetRecord` and that the cue cap is enforced at the model layer.
  </action>
  <verify>
    <automated>cd "C:/Users/wikiepeidia/OneDrive - caugiay.edu.vn/bài tập/usth/GEN14/INTERNSHIP/Internship-project" && python -m pytest tests/runtime/test_contracts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - src/config/settings.py contains `runtime_backend: str = "heuristic"`
    - src/config/settings.py contains `runtime_max_cues: int = 3`
    - src/config/settings.py contains `runtime_min_text_chars: int = 8`
    - src/config/settings.py contains `runtime_store_raw_text: bool = False`
    - src/config/settings.py contains `runtime_fail_closed: bool = True`
    - src/config/settings.py contains `Text-only v1: paste extracted text manually. Images/OCR and audio are not accepted in Phase 2.`
    - src/runtime/contracts.py contains `ChannelName = Literal["unknown", "sms", "zalo", "messenger", "telegram", "facebook"]`
    - src/runtime/contracts.py contains `RiskTier = Literal["benign", "suspicious", "high-risk"]`
    - src/runtime/contracts.py contains `class AnalysisRequest`
    - src/runtime/contracts.py contains `class AnalysisResult`
    - src/runtime/contracts.py contains `class SuspiciousCue`
    - src/runtime/contracts.py contains `class DoctorStatus`
    - src/runtime/analyzers/base.py contains `class AnalyzerBackend(Protocol)`
    - tests/runtime/test_contracts.py contains `def test_analysis_result_caps_top_cues`
    - tests/runtime/test_contracts.py contains `def test_runtime_risk_tier_matches_dataset_schema`
    - `python -m pytest tests/runtime/test_contracts.py -q` exits with code 0
  </acceptance_criteria>
  <done>Runtime settings, contracts, and analyzer protocol exist under `src/runtime/`, and the contract tests pass with the Phase 2 tier, cue-cap, and privacy defaults locked in.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Wave 0 fixtures and scaffold tests for service, CLI, privacy, and doctor behavior</name>
  <files>
    tests/runtime/conftest.py,
    tests/runtime/test_service.py,
    tests/runtime/test_cli.py,
    tests/runtime/test_privacy.py,
    tests/runtime/test_doctor.py
  </files>
  <read_first>
    .planning/phases/02-offline-text-ingestion-and-privacy-baseline/02-VALIDATION.md,
    .planning/phases/02-offline-text-ingestion-and-privacy-baseline/02-RESEARCH.md,
    src/runtime/contracts.py,
    src/data_pipeline/processing/normalizer.py,
    tests/runtime/test_contracts.py
  </read_first>
  <behavior>
    - Test: Wave 0 service expectations cover normalize-once orchestration, Vietnamese and mixed-language text, provisional tiers, and exact top-three cue quoting per ING-02, D-05, D-10, and D-11
    - Test: Wave 0 CLI expectations cover stdin-first analyze, optional `--text` for automation, optional `--channel`, and one-message-per-run with no batch/file mode per D-01, D-02, and D-03
    - Test: Wave 0 privacy expectations cover no raw-text persistence, no network access, no cloud fallback, and no raw message echo in failure output per D-07 and D-09
    - Test: Wave 0 doctor expectations cover explicit readiness checks plus exact remediation commands per D-12 and D-13
    - Test: shared fixtures include suspicious mixed Vietnamese-English text, benign text, short text, and obvious non-text placeholders so later plans exercise the hard text-only boundary per D-08
  </behavior>
  <action>
    Create `tests/runtime/conftest.py` with shared fixtures named exactly `sample_mixed_vn_en_message`, `sample_benign_message`, `sample_short_message`, `sample_non_text_placeholder`, and `expected_runtime_cues`. The mixed-language fixture must contain realistic code-switch markers such as `OTP`, `link`, `account`, or `Smart OTP` because ING-02 depends on preserving those tokens.

    Write the four Wave 0 spec files required by `02-VALIDATION.md`: `tests/runtime/test_service.py`, `tests/runtime/test_cli.py`, `tests/runtime/test_privacy.py`, and `tests/runtime/test_doctor.py`. Use `importlib.import_module()` or helper loader functions inside test bodies rather than top-level imports for `src.runtime.service`, `src.runtime.cli`, and `src.runtime.doctor`, so these files stay syntax-valid before the implementation lands in Plans 02 and 03.

    In `tests/runtime/test_service.py`, define exact test functions for `test_runtime_service_normalizes_once_before_analysis`, `test_runtime_service_returns_top_three_quoted_cues`, and `test_runtime_service_preserves_mixed_language_content`. In `tests/runtime/test_cli.py`, define `test_analyze_reads_single_message_from_stdin_when_text_flag_absent`, `test_analyze_accepts_text_escape_hatch_for_automation`, and `test_cli_only_exposes_analyze_and_doctor_commands`. In `tests/runtime/test_privacy.py`, define `test_default_runtime_path_never_requests_network_fallback`, `test_runtime_does_not_persist_raw_text_by_default`, and `test_failure_output_redacts_user_message`. In `tests/runtime/test_doctor.py`, define `test_doctor_reports_missing_runtime_dependency_with_exact_command` and `test_analyze_self_check_returns_setup_guidance_when_backend_unavailable`.

    Keep these Wave 0 files implementation-facing, not placeholder prose. They should state the exact function names, expected output strings, and monkeypatch points that later plans must satisfy. Do not mark them skipped or xfailed. They should compile cleanly now and become executable as the runtime code appears.
  </action>
  <verify>
    <automated>cd "C:/Users/wikiepeidia/OneDrive - caugiay.edu.vn/bài tập/usth/GEN14/INTERNSHIP/Internship-project" && python -m compileall tests/runtime</automated>
  </verify>
  <acceptance_criteria>
    - tests/runtime/conftest.py contains `def sample_mixed_vn_en_message`
    - tests/runtime/conftest.py contains `def sample_benign_message`
    - tests/runtime/conftest.py contains `def sample_short_message`
    - tests/runtime/conftest.py contains `def sample_non_text_placeholder`
    - tests/runtime/test_service.py contains `def test_runtime_service_normalizes_once_before_analysis`
    - tests/runtime/test_service.py contains `def test_runtime_service_returns_top_three_quoted_cues`
    - tests/runtime/test_cli.py contains `def test_analyze_reads_single_message_from_stdin_when_text_flag_absent`
    - tests/runtime/test_cli.py contains `def test_cli_only_exposes_analyze_and_doctor_commands`
    - tests/runtime/test_privacy.py contains `def test_default_runtime_path_never_requests_network_fallback`
    - tests/runtime/test_privacy.py contains `def test_runtime_does_not_persist_raw_text_by_default`
    - tests/runtime/test_doctor.py contains `def test_doctor_reports_missing_runtime_dependency_with_exact_command`
    - tests/runtime/test_doctor.py contains `importlib.import_module`
    - `python -m compileall tests/runtime` exits with code 0
  </acceptance_criteria>
  <done>Wave 0 runtime fixtures and spec tests exist for service, CLI, privacy, and doctor behavior, and they compile cleanly without requiring the implementation modules to exist yet.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/runtime/test_contracts.py -q` passes
- `python -m compileall tests/runtime` completes without syntax errors
- `python -m pytest tests/runtime/test_contracts.py --collect-only -q` shows only the contract tests for this plan
</verification>

<success_criteria>
The Phase 2 runtime has a stable contract layer and Wave 0 test surface before analyzer or CLI code exists. Settings encode the locked privacy and text-only defaults. Tests now define the exact behavior later plans must satisfy for `ING-01`, `ING-02`, and `RUN-01`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-offline-text-ingestion-and-privacy-baseline/02-01-SUMMARY.md`
</output>
