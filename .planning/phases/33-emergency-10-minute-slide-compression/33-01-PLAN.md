---
phase: 33-emergency-10-minute-slide-compression
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - documents/reports/latex/slides.tex
  - documents/reports/latex/slides/sections/02_agenda.tex
  - documents/reports/latex/slides/sections/03_problem.tex
  - documents/reports/latex/slides/sections/06_why_local.tex
  - documents/reports/latex/slides/sections/08_evaluation.tex
  - documents/reports/latex/slides/sections/09_confusion.tex
  - documents/reports/latex/slides/sections/10_demo.tex
  - documents/reports/latex/slides/sections/11_contributions.tex
  - documents/reports/latex/slides/sections/12_future.tex
  - documents/reports/latex/slides/sections/14_backup.tex
  - .planning/phases/33-emergency-10-minute-slide-compression/33-RUN-PLAN.md
  - .planning/phases/33-emergency-10-minute-slide-compression/33-01-SUMMARY.md
autonomous: true
requirements:
  - TIME-01
  - TIME-02
  - TIME-03
  - TIME-04
  - TIME-05
  - GDEMO-01
  - GDEMO-02
requirements_addressed:
  - TIME-01
  - TIME-02
  - TIME-03
  - TIME-04
  - TIME-05
  - GDEMO-01
  - GDEMO-02
must_haves:
  truths:
    - "The compiled deck (documents/reports/latex/slides.pdf) contains exactly 11 main frames (Title, Agenda, Motivation & Why Local, Architecture, Data, Model, Evaluation Results, Contributions & Future Work, Live Demo, Thank You, References) plus 3 backup appendix frames that do not count toward the footline total."
    - "Architecture (04_architecture.tex), Data (05_data.tex), Model (07_model.tex), References (13_references.tex), Thank You (15_thankyou.tex), and Title (01_title.tex) are byte-identical to their pre-phase content (TIME-03) — zero git diff."
    - "The title slide shows the correct defense date, 15 July 2026, not 14 July 2026 (D-12)."
    - "The Live Demo frame's Sample Output input/output text is byte-identical to the original 10_demo.tex content (D-07); the frame count for that section drops from 2 to 1 (D-05)."
    - "documents/reports/latex/slides.tex compiles clean with XeLaTeX (3 passes, no BibTeX needed) — zero '!'-prefixed lines in slides.log."
    - "A presenter-facing run plan (33-RUN-PLAN.md) documents the pre-compression timing baseline (TIME-01), the post-compression per-slide timing estimate summing to at or under 10 minutes (TIME-05), the locked demo-in-slot decision (GDEMO-02), and a checklist reminding the operator to use the real locked golden scam AND benign prompt text — both required by GDEMO-01 — when running or recording the live demo."
    - "Content cut during the three merges (Motivation+WhyLocal, Evaluation+Confusion, Contributions+Future) is preserved verbatim-or-fuller in a hidden backup appendix (14_backup.tex) placed after Thank You and References via Beamer's native \\appendix, not deleted (D-11)."
  artifacts:
    - path: "documents/reports/latex/slides.tex"
      provides: "Restructured entry point: fixed date, 7 renumbered sections, appendix-wired backup section, frozen main-frame footline total"
    - path: "documents/reports/latex/slides/sections/03_problem.tex"
      provides: "Merged Motivation & Why Local frame (D-02)"
    - path: "documents/reports/latex/slides/sections/08_evaluation.tex"
      provides: "Merged Evaluation Results frame — metrics table + confusion matrix (D-03)"
    - path: "documents/reports/latex/slides/sections/11_contributions.tex"
      provides: "Merged Contributions & Future Work frame (D-04)"
    - path: "documents/reports/latex/slides/sections/10_demo.tex"
      provides: "Single combined Live Demo frame with untouched Sample Output text (D-05, D-07)"
    - path: "documents/reports/latex/slides/sections/14_backup.tex"
      provides: "3 hidden backup/appendix frames preserving content cut during merges (D-11)"
      min_lines: 40
    - path: "documents/reports/latex/slides/sections/02_agenda.tex"
      provides: "Table of Contents column split updated for 7 sections"
    - path: ".planning/phases/33-emergency-10-minute-slide-compression/33-RUN-PLAN.md"
      provides: "Presenter rehearsal doc: baseline + final timing, demo-in-slot decision, GDEMO-01 recording checklist (scam + benign prompts)"
    - path: "documents/reports/latex/slides.pdf"
      provides: "Recompiled 11-main-frame deck, zero XeLaTeX errors (gitignored build output, verified not committed)"
  key_links:
    - from: "documents/reports/latex/slides.tex"
      to: "documents/reports/latex/slides/sections/14_backup.tex"
      via: "\\appendix directive followed by \\input, freezing \\insertmainframenumber at 11"
      pattern: "\\\\appendix"
    - from: "documents/reports/latex/slides.tex footline"
      to: "\\insertmainframenumber"
      via: "footline date-box token swapped from \\inserttotalframenumber so backup frames don't inflate the shown total"
      pattern: "insertmainframenumber"
    - from: ".planning/phases/33-emergency-10-minute-slide-compression/33-RUN-PLAN.md"
      to: "scripts/verify_golden_prompts.py"
      via: "GDEMO-01 checklist quotes DEFAULT_SCAM_TEXT verbatim as the required live-demo scam input"
      pattern: "vcb-secure-alert\\.net"
    - from: ".planning/phases/33-emergency-10-minute-slide-compression/33-RUN-PLAN.md"
      to: "scripts/verify_golden_prompts.py"
      via: "GDEMO-01 checklist quotes DEFAULT_BENIGN_TEXT verbatim as the required live-demo benign input, alongside DEFAULT_SCAM_TEXT"
      pattern: "VPBank Smart OTP"
    - from: "documents/reports/latex/slides/sections/02_agenda.tex"
      to: "documents/reports/latex/slides.tex \\section{} count"
      via: "\\tableofcontents[hideallsubsections,sections={5-7}] matches the new 7-section total"
      pattern: "sections=\\{5-7\\}"
---

<objective>
Compress the defense Beamer deck from 15 frames / 9 sections down to 11 main frames / 7 sections so it reliably fits the 10-minute defense slot on 2026-07-15, without cutting any Architecture/Data/Model content, while preserving the golden-prompt demo sync via an operator checklist (not a slide-text edit) and locking the demo-in-slot decision in a presenter run plan.

Purpose: The defense is 2026-07-13 + 2 days. The current deck runs an estimated ~11:40 against a 10:00 target. This is the single plan for the single-phase v5.2 emergency milestone — it must be executable in one sitting today.

Output: A recompiled `documents/reports/latex/slides.pdf` with 11 timed main frames + a 3-frame hidden backup appendix, and `.planning/phases/33-emergency-10-minute-slide-compression/33-RUN-PLAN.md` giving the presenter a baseline/final timing table, the locked demo-in-slot decision, and the exact golden-prompt texts (scam + benign) to use live.
</objective>

<execution_context>
@C:/Users/wikiepeidia/OneDrive - caugiay.edu.vn/bài tập/usth/GEN14/INTERNSHIP/Internship-project/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wikiepeidia/OneDrive - caugiay.edu.vn/bài tập/usth/GEN14/INTERNSHIP/Internship-project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-emergency-10-minute-slide-compression/33-CONTEXT.md
@.planning/phases/12-cambridgeus-presentation-revamp/12-CONTEXT.md
@scripts/verify_golden_prompts.py
@.planning/phases/28-baseline-readiness-zero-code-diagnostics/artifacts/28-golden-prompt-results.json

<verified_environment>
XeLaTeX (MiKTeX 26.5 / MiKTeX-XeTeX 4.18) and `pdftotext` (poppler, via Git Bash `/mingw64/bin/pdftotext`) are both confirmed installed and working in this environment as of this planning session. `documents/reports/latex/slides.tex` has no `\bibliography{}` call — `13_references.tex` uses a manual `thebibliography`, so no BibTeX pass is needed for this file (unlike `main.tex`, the thesis).

Beamer's native `\appendix` command (defined in `beamerbasesection.sty`, part of the standard `beamer.cls` already installed — no new package) was live-tested in an isolated scratch file during planning: a 3-main-frame + `\appendix` + 2-backup-frame deck compiled clean, and `pdftotext` confirmed the footline showed `1 / 3`, `2 / 3`, `3 / 3` on main frames and `4 / 3`, `5 / 3` on backup frames when the footline template used `\insertmainframenumber` instead of `\inserttotalframenumber`. This confirms the D-11 backup mechanism works exactly as specified below — the numerator keeps counting on backup frames (expected/correct — signals "past the end"), the denominator freezes at the main total.
</verified_environment>

<timing_baseline methodology="word_count / 130wpm * 60s + fixed_overhead(+15s/table, +10s/diagram), then x1.15 realistic-delivery buffer for pauses/gestures/nervousness, rounded to nearest 5s. Title/Agenda/References/Thank-You/Demo frames use fixed visual-pacing values instead of word-rate (mostly non-narrated or highly variable content).">
Baseline (TIME-01) — current 15 frames / 9 sections, computed from the actual current file content:

| # | File | Frame | Est. sec |
|---|------|-------|----------|
| 1 | 01_title.tex | Title | 20 |
| 2 | 02_agenda.tex | Table of Contents | 20 |
| 3 | 03_problem.tex | Problem: Vietnamese Phishing & the Privacy Gap | 60 |
| 4 | 04_architecture.tex | Training Pipeline Overview | 60 |
| 5 | 05_data.tex | Data Pipeline | 75 |
| 6 | 06_why_local.tex | Privacy Risk: Cloud API Data Leakage | 60 |
| 7 | 07_model.tex | Model Adaptation — QLoRA on Qwen3-4B | 60 |
| 8 | 08_evaluation.tex | Evaluation Results | 55 |
| 9 | 09_confusion.tex | Confusion Matrix & Error Analysis | 50 |
| 10a | 10_demo.tex (frame 1) | Sample Output | 35 |
| 10b | 10_demo.tex (frame 2) | Live Demo transition + actual live action | 90 |
| 11 | 11_contributions.tex | Contributions | 45 |
| 12 | 12_future.tex | Limitations & Future Work | 45 |
| 13 | 13_references.tex | References | 15 |
| 15 | 15_thankyou.tex | Thank You | 10 |
| | **TOTAL** | | **700s ≈ 11:40** |

This is ~1:40 over the 10:00 target — the reason this emergency phase exists. Transcribe this table into 33-RUN-PLAN.md as-is for TIME-01; it describes the pre-edit state and does not need to be recomputed.

Target shape for TIME-05 (final, 11 frames, ≤600s / 10:00): Architecture+Data+Model stay unchanged at 60+75+60=195s; Title+Agenda+ThankYou+References stay unchanged at 20+20+10+15=65s; Live Demo (merged) targeted ~90s (unchanged from before — same content, one fewer frame-transition overhead); that leaves ~250s of budget for the 3 merged frames (Motivation & Why Local, Evaluation Results, Contributions & Future Work) — roughly 80-85s each is achievable given the compact 2-bullet-per-column + 1-block structure specified in Tasks 1-2. Task 3 computes the REAL final table using the same methodology against the actual merged content written in Tasks 1-2 (word counts will differ slightly from this projection — that's expected; if the real total exceeds 600s, trim 1-2 bullets from the densest merged frame and re-total).
</timing_baseline>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge Motivation+WhyLocal (D-02) and Evaluation+Confusion (D-03)</name>
  <read_first>
    documents/reports/latex/slides/sections/03_problem.tex
    documents/reports/latex/slides/sections/06_why_local.tex
    documents/reports/latex/slides/sections/08_evaluation.tex
    documents/reports/latex/slides/sections/09_confusion.tex
    .planning/phases/12-cambridgeus-presentation-revamp/12-CONTEXT.md
  </read_first>
  <files>documents/reports/latex/slides/sections/03_problem.tex, documents/reports/latex/slides/sections/06_why_local.tex, documents/reports/latex/slides/sections/08_evaluation.tex, documents/reports/latex/slides/sections/09_confusion.tex</files>
  <action>
    Rewrite `03_problem.tex` into the merged "Motivation & Why Local" frame (D-02). Use `\begin{frame}{Motivation: Vietnamese Phishing \& Why Local Inference}` with `\framesubtitle{The Problem \& the Privacy-First Approach}`. Body: `\begin{columns}[T,onlytextwidth]` two columns. Left column (~0.48\textwidth), heading "The problem", two `\footnotesize` bullets: (1) one bullet merging the Group-IB 2022 cloned-bank-login-page finding and the AIS 2024 biometric-verification bank-impersonation warning (keep the existing bracketed `[Group-IB, 2022]` / `[AIS, 2024]` inline-citation style); (2) a bullet noting suspicious messages carry OTP codes/account details and sending them to a cloud classifier adds transit/retention/provider-exposure risk (reuse the current file's language). Right column (~0.48\textwidth), heading "Why local", two `\footnotesize` bullets pulled from `06_why_local.tex`: (1) one bullet merging the OpenAI ChatGPT March 2023 Redis breach (~1.2% of users) and the Samsung Semiconductor 2023 confidential-code leak; (2) a bullet on the cloud risk path (text leaves device, provider may retain/breach). Close with one `\begin{block}{Problem \& Local-First Solution}` merging both original block statements into one: this project is an offline-first threat detector returning risk tier, threat labels, and safety guidance without sending message text off-device. Try `\footnotesize` first (no `[shrink=]`) — only add `[t,shrink=]` if the Task 3 compile shows overfull content, per the Phase 12 tiered cut-first policy (D-15/D-16/D-17).

    Rewrite `08_evaluation.tex` into the merged "Evaluation Results" frame (D-03), absorbing `09_confusion.tex`. Keep `\begin{frame}{Evaluation Results}` and add `\framesubtitle{Metrics \& Confusion Matrix --- Internal Validation Split (254 Messages)}`. Body: `\begin{columns}[T,onlytextwidth]` two columns. Left column (~0.54\textwidth): the exact per-class metrics table from the current file (Bank Impersonation / Zalo Social Eng. / Task Scam / Benign rows + Macro avg row, Prec./Recall/F1/n columns), reduced to `\scriptsize` to fit two tables side by side. Right column (~0.44\textwidth): the exact 4x4 confusion matrix table from `09_confusion.tex`, including its `\cellcolor{CVBLUE!15}` diagonal highlighting, also `\scriptsize`. Below both columns, exactly 3 combined `\footnotesize` bullets: (1) baseline-to-fine-tuned macro F1 improvement, 0.6702 to `\textcolor{CVBLUE}{0.9625}` (+0.2923); (2) lowest recall is Task Scam (0.871) and all 9 confusion-matrix errors are intra-scam (bank-name/OTP phrasing overlap), zero scam messages misclassified as benign; (3) the caution note that validation reuse and seed overlap prevent an independent-test claim. Both tables are the evidence and must both stay fully intact per D-03 — if the two side-by-side tables plus 3 bullets overflow after the Task 3 compile check, apply `[t,shrink=5]` on the frame (matching the established `06_why_local.tex` pattern) rather than cutting a table.

    Add one LaTeX comment line at the very top of `06_why_local.tex` and of `09_confusion.tex`: `%% SUPERSEDED (Phase 33): content merged into {target file} (main deck) and slides/sections/14_backup.tex (appendix backup). No longer \input from slides.tex.` — substitute the correct target filename in each (`03_problem.tex` for `06_why_local.tex`; `08_evaluation.tex` for `09_confusion.tex`). Leave the rest of each file's content below the comment untouched; it becomes inert once its `\input` line is removed from `slides.tex` in Task 3, so leaving it is zero-risk and gives future readers a paper trail.
  </action>
  <verify>
    <automated>python -c "import pathlib; p=pathlib.Path('documents/reports/latex/slides/sections/03_problem.tex'); t=p.read_text(encoding='utf-8'); assert t.count('begin{frame}')==1; assert 'Group-IB' in t and 'OpenAI' in t and 'Samsung' in t, 'merge missing a source topic'"</automated>
    <automated>python -c "import pathlib; p=pathlib.Path('documents/reports/latex/slides/sections/08_evaluation.tex'); t=p.read_text(encoding='utf-8'); assert t.count('begin{frame}')==1; assert 'cellcolor' in t, 'confusion matrix table not merged in'; assert '0.9625' in t and '0.871' in t"</automated>
    <automated>python -c "import pathlib; a=pathlib.Path('documents/reports/latex/slides/sections/06_why_local.tex').read_text(encoding='utf-8'); b=pathlib.Path('documents/reports/latex/slides/sections/09_confusion.tex').read_text(encoding='utf-8'); assert a.startswith('%% SUPERSEDED'); assert b.startswith('%% SUPERSEDED')"</automated>
  </verify>
  <acceptance_criteria>03_problem.tex is a single frame covering both original topics (Group-IB/AIS + OpenAI/Samsung citations); 08_evaluation.tex is a single frame with both the metrics table and the cellcolor confusion matrix table intact; 06_why_local.tex and 09_confusion.tex both start with the SUPERSEDED comment marker.</acceptance_criteria>
  <done>Both merged frames exist as single, self-contained `\begin{frame}...\end{frame}` blocks with no content lost (only trimmed narrative bullets, never table data or citations), and both orphaned source files are marked superseded.</done>
</task>

<task type="auto">
  <name>Task 2: Merge Contributions+Future (D-04), merge Demo (D-05), create backup appendix (D-11)</name>
  <read_first>
    documents/reports/latex/slides/sections/11_contributions.tex
    documents/reports/latex/slides/sections/12_future.tex
    documents/reports/latex/slides/sections/10_demo.tex
    documents/reports/latex/slides/sections/03_problem.tex
    documents/reports/latex/slides/sections/08_evaluation.tex
  </read_first>
  <files>documents/reports/latex/slides/sections/11_contributions.tex, documents/reports/latex/slides/sections/12_future.tex, documents/reports/latex/slides/sections/10_demo.tex, documents/reports/latex/slides/sections/14_backup.tex</files>
  <action>
    Rewrite `11_contributions.tex` into the merged "Contributions & Future Work" frame (D-04), absorbing `12_future.tex`. Keep `\begin{frame}{Contributions \& Future Work}` with `\framesubtitle{Research Contributions, Limitations \& Next Steps}`. Body: `\begin{columns}[T,onlytextwidth]` two columns. Left column (~0.52\textwidth): `\begin{block}{Contributions}` with the same 4-item `enumerate` from the current file (Vietnamese phishing corpus, privacy-first local runtime, explainable decision layer, honest internal evaluation) — tighten wording only if needed to fit, keep all 4 items. Right column (~0.46\textwidth): heading "Limitations", two `\footnotesize` bullets merging the current 3 limitations from `12_future.tex` (combine the text-only bullet and the Vietnamese-only-training bullet into one bullet, keep the seed-overlap/413-row-unevaluated caveat as the second bullet), then `\begin{block}{Future Direction}` with the current file's one-line future statement (rebuild seed-group-disjoint splits, evaluate once on an untouched human-labelled test set) unchanged.

    Rewrite `10_demo.tex` into a single combined "Live Demo" frame (D-05), merging its two existing frames. Keep `\begin{frame}[fragile]{Live Demo}` — title changed from "Sample Output" to "Live Demo"; `[fragile]` stays because the `lstlisting` remains — with `\framesubtitle{Live Analysis --- Vietnamese Phishing Message}` completely UNCHANGED. Inside, keep the entire `\begin{columns}[T]...\end{columns}` block (the input-text column and the `lstlisting` system-output column) byte-identical to the current file per the user's D-07/D-08 correction — do not touch the Vietnamese input text, the channel label, or any line inside the `lstlisting`. After `\end{columns}` and before `\end{frame}`, add one new short centered line (outside the columns): `\small\textit{}` text reading approximately "Live now: `vnphish analyze` --- ~1 min reserved" to fold in the removed transition frame's live-demo cue and satisfy GDEMO-02's requirement that the demo-in-slot decision be visible in the deck itself. Delete the second `\begin{frame}{Live Demo}...\end{frame}` block (the big centered-text transition frame) entirely — its decorative content needs no backup preservation; D-11 lists only the Motivation+WhyLocal, Evaluation+Confusion, and Contributions+Future merges as needing backup slides, not Demo.

    Add one LaTeX comment line at the top of `12_future.tex`: `%% SUPERSEDED (Phase 33): content merged into slides/sections/11_contributions.tex (main deck) and slides/sections/14_backup.tex (appendix backup). No longer \input from slides.tex.`

    Create new file `slides/sections/14_backup.tex` with exactly 3 backup frames, each titled `Backup: {Topic} --- Full Detail`, using `[allowframebreaks]` on any frame whose content would overflow one slide (matching the existing `13_references.tex` convention), and no `\section{}` markers (they sit after `\appendix` in `slides.tex` with no section wrapper): (1) "Backup: Motivation \& Why Local" reproducing everything present in the original `03_problem.tex` and `06_why_local.tex` (as read, pre-Task-1) that is not verbatim in the new compact `03_problem.tex` you just wrote in Task 1 — both incident bullets in full, the 3-bullet cloud risk path, and both original block statements; (2) "Backup: Evaluation \& Confusion" reproducing the fuller original bullet commentary from the original `08_evaluation.tex` and `09_confusion.tex` (as read, pre-Task-1) that did not make it into the compact 3-bullet merged version you wrote in Task 1 — the 4 original evaluation bullets and the 4 original error-pattern bullets; (3) "Backup: Contributions \& Future" reproducing the full original `11_contributions.tex` and `12_future.tex` content, in case anything was trimmed for space when writing the compact merged frame above. These are Q&A-only slides with no time budget — prefer completeness over brevity; when in doubt, include the detail.
  </action>
  <verify>
    <automated>python -c "import pathlib; t=pathlib.Path('documents/reports/latex/slides/sections/10_demo.tex').read_text(encoding='utf-8'); assert t.count('begin{frame}')==1; assert 'hạn chế' in t and 'Structured decision from sms local runtime' in t, 'Sample Output text was altered'; assert 'fragile' in t"</automated>
    <automated>python -c "import pathlib; t=pathlib.Path('documents/reports/latex/slides/sections/11_contributions.tex').read_text(encoding='utf-8'); assert t.count('begin{frame}')==1; assert 'seed-group-disjoint' in t, 'future-work content not merged in'"</automated>
    <automated>python -c "import pathlib; assert pathlib.Path('documents/reports/latex/slides/sections/12_future.tex').read_text(encoding='utf-8').startswith('%% SUPERSEDED')"</automated>
    <automated>python -c "import pathlib; n=pathlib.Path('documents/reports/latex/slides/sections/14_backup.tex').read_text(encoding='utf-8').count('begin{frame}'); assert n==3, f'expected 3 backup frames, found {n}'"</automated>
  </verify>
  <acceptance_criteria>10_demo.tex has exactly 1 frame with the original input/lstlisting text unchanged; 11_contributions.tex has exactly 1 frame covering both contributions and future-work content; 12_future.tex is marked superseded; 14_backup.tex exists with exactly 3 frames.</acceptance_criteria>
  <done>All 4 merges from D-02/D-03/D-04/D-05 are complete across the 4 source-content tasks (2 in Task 1, 2 here), the backup appendix file exists with the 3 required frames, and both remaining orphaned files are marked superseded.</done>
</task>

<task type="auto">
  <name>Task 3: Restructure slides.tex, wire the appendix, fix the date, compile-verify, and write the run plan</name>
  <read_first>
    documents/reports/latex/slides.tex
    documents/reports/latex/slides/sections/02_agenda.tex
    documents/reports/latex/slides/sections/14_backup.tex
    scripts/verify_golden_prompts.py
  </read_first>
  <files>documents/reports/latex/slides.tex, documents/reports/latex/slides/sections/02_agenda.tex, .planning/phases/33-emergency-10-minute-slide-compression/33-RUN-PLAN.md</files>
  <action>
    Restructure `documents/reports/latex/slides.tex`:
    (1) D-12: change `\date{14 July 2026}` to `\date{15 July 2026}`.
    (2) Renumber the `\section{}` labels from 9 down to 7, matching the new merge structure. This step is applied together with deletions (3)-(5) below, not strictly before them — the two describe one combined end state, not a two-phase edit order. Renumbering only edits the text inside the `\section{...}` lines that survive into the final 7-section list; it does not add, remove, or relocate any `\input{}` line (that is entirely the job of deletions (3)-(5)). Final order of the 7 surviving `\section{}` + `\input{}` pairs: `\section{1. Motivation \& Why Local}` before `\input{slides/sections/03_problem.tex}`; `\section{2. Training Pipeline}` before `\input{slides/sections/04_architecture.tex}`; `\section{3. Data Pipeline}` before `\input{slides/sections/05_data.tex}`; `\section{4. Model Training}` before `\input{slides/sections/07_model.tex}`; `\section{5. Evaluation Results}` before `\input{slides/sections/08_evaluation.tex}`; `\section{6. Conclusion}` before `\input{slides/sections/11_contributions.tex}`; `\section{7. Demo}` before `\input{slides/sections/10_demo.tex}`.
    (3) Remove the `\section{2. Why Local?}` + `\input{slides/sections/06_why_local.tex}` pair entirely (content now lives in `03_problem.tex`).
    (4) Remove the `\section{6. Limitations}` + `\input{slides/sections/12_future.tex}` pair entirely from its current position between Model Training and Evaluation Results (content now lives in `11_contributions.tex`, which stays in its current later position — this single deletion accomplishes D-04's repositioning; `11_contributions.tex`'s own `\input` line does not need to move).
    (5) Remove the `\input{slides/sections/09_confusion.tex}` line (content now lives in `08_evaluation.tex`).
    (6) Keep `\input{slides/sections/15_thankyou.tex}` then `\input{slides/sections/13_references.tex}` in that exact relative order, unchanged, as the last two of the 11 main frames.
    (7) After the References `\input` line and before `\end{document}`, add `\appendix` on its own line followed by `\input{slides/sections/14_backup.tex}` (D-11) — this is Beamer's native, already-installed appendix command; no new package needed.
    (8) In the `\setbeamertemplate{footline}{...}` block, change the token `\inserttotalframenumber` to `\insertmainframenumber` (the one occurrence in the date-box segment currently reading `\insertframenumber{} / \inserttotalframenumber\hspace*{2ex}`) so the footer denominator freezes at the 11 main frames instead of counting the 3 backup frames too.

    Update `documents/reports/latex/slides/sections/02_agenda.tex`: change the second `\tableofcontents[hideallsubsections,sections={5-9}]` to `sections={5-7}` (7 sections total now); leave the first column's `sections={1-4}` unchanged — this is a required consequential fix (the agenda frame itself stays untouched/unmerged per D-01, but its section-range parameters must track the real section count or the Table of Contents would silently under-render).

    Compile: from `documents/reports/latex/`, delete `slides.aux`, `slides.log`, `slides.nav`, `slides.out`, `slides.snm`, `slides.toc`, `slides.vrb`, then run `xelatex -interaction=nonstopmode slides.tex` three times in a row (no BibTeX pass — `13_references.tex` uses a manual `thebibliography`, not `\bibliography{}`, confirmed absent from this file). Confirm zero `!`-prefixed lines in `slides.log`. Then confirm via `pdftotext -layout slides.pdf -` that the footline denominator is `11` on every single frame — main frames read `1 / 11` through `11 / 11`, and the 3 backup frames read `12 / 11` through `14 / 11` (this climbing numerator on backup frames is expected/correct, verified live during planning — it signals "past the end", not a bug).

    This task concentrates the plan's highest-risk work (8 slides.tex restructuring edits, the compile loop, and a new 4-section document) with no other task after it to catch a mistake. As lightweight guidance, not a structural requirement: once the compile above finishes clean (zero `!` lines) and the footline check passes, treat that as a known-good checkpoint — e.g. a local `git add -p`/commit of the slide sources at that point — before starting the 33-RUN-PLAN.md write below, so a verified-good recompiled state exists as a recovery point ahead of the remaining documentation work.

    Create `.planning/phases/33-emergency-10-minute-slide-compression/33-RUN-PLAN.md`, the presenter's rehearsal document, with these sections: a **TIME-01 baseline audit** — transcribe the 15-frame/9-section baseline table and ~700s/11:40 total supplied verbatim in this plan's `<timing_baseline>` context block (it describes the pre-edit state; do not recompute it); a **TIME-05 final timing estimate** — apply the identical word-count/130wpm+overhead+1.15-buffer methodology to the actual 11 main frames you and the prior two tasks just wrote (real per-frame word counts, not the projected ones), producing a table and total that must land at or under 600 seconds (10:00) including 60-75s reserved for the Live Demo frame per D-09; if the real total exceeds 600s, go back and trim 1-2 bullets from the densest merged frame and re-total rather than shipping an over-budget estimate; a **GDEMO-02 demo-in-slot decision** section stating the locked decision verbatim: the demo frame stays in the main deck, ~1 minute is reserved for it, and if a full rehearsal of the compiled deck shows it doesn't fit, cutting the demo live during rehearsal is the presenter's own call (D-09) — not pre-decided by this plan; a **GDEMO-01 recording/live-demo checklist** covering BOTH locked golden prompts verbatim from `scripts/verify_golden_prompts.py` — REQUIREMENTS.md and PROJECT.md require the demo section to reference "the 2 Phase-32 locked golden prompts (scam + benign)", not the scam prompt alone, so the checklist must remind the operator that when actually running or recording the live demo they must type or paste: (a) the `DEFAULT_SCAM_TEXT` constant verbatim (quote the full string including the exact `http://vcb-secure-alert.net/lock?id=9182736` URL) on the `sms` channel, expecting `high-risk` / `bank_impersonation` (cross-referenced against the 5/5-stable `golden_scam` result in `28-golden-prompt-results.json`); and (b) the `DEFAULT_BENIGN_TEXT` constant verbatim — the VPBank Smart OTP text, quote the full string including the `847291` OTP code — on the `sms` channel, expecting `benign` / `benign` (cross-referenced against the 5/5-stable `golden_benign` result in the same `28-golden-prompt-results.json`); plus an explicit note that the static text printed on the Live Demo slide's Sample Output columns is illustrative-only and must NOT be assumed to be the actual live-demo input (neither prompt is edited into the slide itself, per D-07/D-08b — that decision governs slide *text* only, not this presenter checklist); and a closing note recording the D-12 date fix and the 3-frame backup appendix's existence and Q&A-only purpose.
  </action>
  <verify>
    <automated>cd documents/reports/latex && rm -f slides.aux slides.log slides.nav slides.out slides.snm slides.toc slides.vrb && xelatex -interaction=nonstopmode slides.tex && xelatex -interaction=nonstopmode slides.tex && xelatex -interaction=nonstopmode slides.tex</automated>
    <automated>! grep -q "^!" documents/reports/latex/slides.log</automated>
    <automated>python -c "
files = ['01_title','02_agenda','03_problem','04_architecture','05_data','07_model','08_evaluation','11_contributions','10_demo','15_thankyou','13_references']
total = 0
for f in files:
    t = open(f'documents/reports/latex/slides/sections/{f}.tex', encoding='utf-8').read()
    total += t.count('begin{frame}')
assert total == 11, f'expected 11 main frames, got {total}'
print(total)
"</automated>
    <automated>git diff --exit-code --stat -- documents/reports/latex/slides/sections/04_architecture.tex documents/reports/latex/slides/sections/05_data.tex documents/reports/latex/slides/sections/07_model.tex documents/reports/latex/slides/sections/13_references.tex documents/reports/latex/slides/sections/15_thankyou.tex documents/reports/latex/slides/sections/01_title.tex</automated>
    <automated>cd documents/reports/latex && pdftotext -layout slides.pdf - | grep -oE "[0-9]+ / [0-9]+" | awk -F' / ' '{print $2}' | sort -u | awk 'END{exit !(NR==1 && $1=="11")}'</automated>
    <automated>grep -c "date{15 July 2026}" documents/reports/latex/slides.tex</automated>
    <automated>python -c "
import pathlib
t = pathlib.Path('.planning/phases/33-emergency-10-minute-slide-compression/33-RUN-PLAN.md').read_text(encoding='utf-8')
for marker in ['TIME-01', 'TIME-05', 'GDEMO-01', 'GDEMO-02', 'VIETCOMBANK', 'vcb-secure-alert.net', 'VPBank', '847291', '15 July 2026']:
    assert marker in t, f'missing {marker}'
"</automated>
  </verify>
  <acceptance_criteria>XeLaTeX exits with zero '!' lines in slides.log after 3 passes, enforced as a hard pass/fail gate; the 11 main-sequence files sum to exactly 11 `\begin{frame}` occurrences; `git diff --exit-code --stat` on the 6 D-06 untouched files reports no differences (hard-fails on any diff, not just displays one); the compiled PDF's footline shows a single unique denominator of `11` across all frames (main and backup), asserted via a hard pass/fail check rather than merely printed; the date string reads `15 July 2026`; 33-RUN-PLAN.md exists with all required TIME/GDEMO sections and both locked golden-prompt substrings — the scam prompt (`vcb-secure-alert.net`) and the benign prompt (`VPBank`, `847291`).</acceptance_criteria>
  <done>documents/reports/latex/slides.pdf recompiles clean with 11 main frames + 3 non-counted backup frames, the date is corrected, Architecture/Data/Model/References/ThankYou/Title are provably untouched, and 33-RUN-PLAN.md gives the presenter a concrete baseline-vs-final timing comparison, the locked demo-in-slot decision, and the exact golden-prompt text for both the scam and benign prompts to use live.</done>
</task>

</tasks>

<source_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|--------------------|------|--------|-------|
| GOAL | — | Deck reliably fits 10-min slot without cutting Architecture/Data/Model depth; demo synced to golden prompts; demo-in-slot decision locked | 33-01 | COVERED | All 3 tasks jointly deliver this. |
| REQ | TIME-01 | Measured baseline + estimated delivery time | 33-01 | COVERED | Task 3 (33-RUN-PLAN.md), baseline table supplied in plan context. |
| REQ | TIME-02 | Trim/merge non-methodology sections | 33-01 | COVERED | Tasks 1-2 (4 merges). |
| REQ | TIME-03 | Architecture/Data/Model retain full depth, no cuts | 33-01 | COVERED | Not touched by any task; Task 3 verifies via a hard-failing `git diff --exit-code --stat` gate. |
| REQ | TIME-04 | Final deck ~10 slides, still covers problem/methodology/eval/conclusion | 33-01 | COVERED | Tasks 1-3 land at exactly 11 main frames. |
| REQ | TIME-05 | Rough per-slide timing estimate | 33-01 | COVERED | Task 3 (33-RUN-PLAN.md), methodology + worked baseline supplied in plan context. |
| REQ | GDEMO-01 | Demo synced to 2 locked golden prompts, no stale wording | 33-01 | COVERED | Via 33-RUN-PLAN.md recording checklist covering both the scam AND benign prompts (D-08 revision), not a slide-text edit; enforced by an automated marker check on the benign prompt's text, not just the scam prompt's. |
| REQ | GDEMO-02 | Demo-in-slot decision locked in deck + run plan | 33-01 | COVERED | Task 2 (deck cue line) + Task 3 (33-RUN-PLAN.md decision section). |
| RESEARCH | — | (none — RESEARCH.md not produced for this phase; deck-editing task, not a new domain/library integration) | — | N/A | No RESEARCH.md exists for Phase 33; explicitly skipped per orchestrator note. |
| CONTEXT | D-01 | Title/Agenda stay separate, untouched, not merged | 33-01 | COVERED | 01_title.tex never in files_modified; 02_agenda.tex only gets its toc range updated, not merged into anything. |
| CONTEXT | D-02 | Merge Problem+WhyLocal | 33-01 | COVERED | Task 1. |
| CONTEXT | D-03 | Merge Evaluation+Confusion | 33-01 | COVERED | Task 1. |
| CONTEXT | D-04 | Merge Contributions+Future, reposition to Conclusion slot | 33-01 | COVERED | Task 2 (content) + Task 3 (structural deletion accomplishes the reposition). |
| CONTEXT | D-05 | Combine Demo's 2 frames into 1, simple/stable layout | 33-01 | COVERED | Task 2. |
| CONTEXT | D-06 | Architecture/Data/Model/References/ThankYou untouched | 33-01 | COVERED | Excluded from files_modified; Task 3 hard-gates via `git diff --exit-code --stat`. |
| CONTEXT | D-07 (superseded)/D-08 (revised) | Do not edit static Sample Output text; GDEMO-01 satisfied via recording reminder | 33-01 | COVERED | Task 2 preserves text verbatim; Task 3 writes the checklist for both locked prompts. |
| CONTEXT | D-08b | Do not add benign prompt to the slide | 33-01 | COVERED | Task 2's Demo rewrite does not add it to the slide text; D-08b scopes only to the LaTeX slide content — 33-RUN-PLAN.md's GDEMO-01 checklist separately (and correctly) covers both locked prompts, since that requirement lives in the presenter checklist document, not the slide itself. |
| CONTEXT | D-09 | Keep demo frame in main deck; cutting it is the user's own rehearsal-time call | 33-01 | COVERED | Task 2 keeps the frame; Task 3's run plan documents this explicitly as not pre-decided. |
| CONTEXT | D-10 | User manually overlays recorded video via PDF editor post-compile | 33-01 | COVERED | Task 2 keeps the Demo frame layout simple/stable (no busy redesign) per D-05. |
| CONTEXT | D-11 | Cut content -> hidden backup appendix after Thank You/References | 33-01 | COVERED | Task 2 creates 14_backup.tex; Task 3 wires `\appendix` + frozen footline total. |
| CONTEXT | D-12 | Fix `\date{14 July 2026}` to `\date{15 July 2026}` | 33-01 | COVERED | Task 3. |

All 4 source types audited. No MISSING items — proceeding without a phase-split or gap-closure request.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | This phase edits static LaTeX slide source and recompiles a local PDF. No runtime code, network call, user-input path, external service, or dependency install is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|--------------|------------------|
| T-33-01 | Tampering | 04_architecture.tex, 05_data.tex, 07_model.tex, 13_references.tex, 15_thankyou.tex, 01_title.tex | high | mitigate | Task 3 runs `git diff --exit-code --stat` on exactly these 6 files and requires the command to exit 0 (no differences) before the plan is considered done (D-06/TIME-03 hard gate) — `--exit-code` makes this a real pass/fail gate rather than a command that always succeeds while merely printing a diff. |
| T-33-02 | Tampering | documents/reports/latex/slides.tex footline (`\insertmainframenumber` token swap) | medium | mitigate | Verified live via compiled-PDF text extraction (`pdftotext`), not just source inspection — the denominator-check pipeline ends in `awk 'END{exit !(NR==1 && $1=="11")}'` so it hard-fails unless every frame's footline denominator is the single value `11`, not merely printed for manual inspection. |
| T-33-03 | Information Disclosure | slides/sections/14_backup.tex (appendix) | low | accept | Backup slides intentionally expose fuller citation/metric detail for Q&A; content is a subset of what is already public in the thesis report and the main deck itself — no new sensitive data introduced. |
| T-33-04 | Repudiation | Live-demo input text (GDEMO-01) | medium | mitigate | 33-RUN-PLAN.md quotes both exact locked golden-prompt strings — `DEFAULT_SCAM_TEXT` and `DEFAULT_BENIGN_TEXT` — from `scripts/verify_golden_prompts.py` verbatim, so the operator cannot accidentally substitute stale/paraphrased wording, or skip the benign prompt entirely, during the real demo or recording; an automated marker check on both prompts' distinguishing substrings enforces this at verify time. |
| T-33-SC | Tampering | npm/pip/cargo installs | n/a | accept | Phase adds zero new package installs — Beamer's native `\appendix` command is part of the already-installed `beamer.cls`; no CTAN, npm, pip, or cargo package is added. Package legitimacy gate not applicable. |
</threat_model>

<verification>
Phase 33 verification passes when: (1) `cd documents/reports/latex && xelatex -interaction=nonstopmode slides.tex` (run 3x after deleting aux/log/nav/out/snm/toc/vrb files) exits with zero `!`-prefixed lines in `slides.log`, checked via `! grep -q "^!" slides.log` (a real pass/fail gate, not a raw count that inverts on a clean log); (2) the 11 main-sequence section files sum to exactly 11 `\begin{frame}` occurrences; (3) `pdftotext -layout slides.pdf -` shows a single unique footline denominator of `11` across every frame (main frames `1/11`...`11/11`, backup frames `12/11`-`14/11`), asserted via a hard-failing awk check rather than just displayed; (4) `git diff --exit-code --stat` on the 6 D-06/TIME-03 untouched files reports no differences (exits 0); (5) `33-RUN-PLAN.md` exists and contains the TIME-01/TIME-05/GDEMO-01/GDEMO-02 sections plus both exact locked golden-prompt substrings — the scam prompt (`vcb-secure-alert.net`) and the benign prompt (`VPBank`, `847291`) — and the corrected date (`15 July 2026`).
</verification>

<success_criteria>
- Compiled deck lands at 11 main frames (Title, Agenda, Motivation & Why Local, Architecture, Data, Model, Evaluation Results, Contributions & Future Work, Live Demo, Thank You, References) plus a non-counted 3-frame backup appendix reachable only past References.
- Architecture, Data, Model, References, Thank You, and Title sections are unmodified — zero diff (TIME-03), hard-gated by `git diff --exit-code --stat`.
- Title slide shows `15 July 2026` (D-12).
- Live Demo frame's Sample Output input/output text is unmodified; GDEMO-01 is satisfied via the 33-RUN-PLAN.md recording checklist covering both the locked scam and benign golden prompts, not a slide-text edit, per the user's D-07 correction.
- 33-RUN-PLAN.md gives the presenter a baseline timing estimate (~11:40), a final per-slide timing estimate at or under 10:00 (including the reserved ~1-minute demo), and the locked GDEMO-02 demo-in-slot decision.
- XeLaTeX compiles `slides.tex` with zero errors across 3 passes, verified by a gate that actually fails when errors are present.
</success_criteria>

<output>
Create `.planning/phases/33-emergency-10-minute-slide-compression/33-01-SUMMARY.md` when done.
</output>

## Artifacts This Phase Produces

- MODIFIED (structural + content): `documents/reports/latex/slides.tex` — date fix, 7 renumbered sections, appendix wiring, frozen footline total.
- MODIFIED (content rewrite, merged): `documents/reports/latex/slides/sections/03_problem.tex`, `08_evaluation.tex`, `11_contributions.tex`, `10_demo.tex`.
- MODIFIED (toc range only): `documents/reports/latex/slides/sections/02_agenda.tex`.
- MODIFIED (superseded-comment only, content orphaned but preserved): `documents/reports/latex/slides/sections/06_why_local.tex`, `09_confusion.tex`, `12_future.tex`.
- NEW: `documents/reports/latex/slides/sections/14_backup.tex` — 3-frame hidden backup appendix.
- NEW: `.planning/phases/33-emergency-10-minute-slide-compression/33-RUN-PLAN.md` — presenter timing/decision/checklist document.
- NEW: `.planning/phases/33-emergency-10-minute-slide-compression/33-01-SUMMARY.md` — execution summary.
- REGENERATED (gitignored build output, not committed): `documents/reports/latex/slides.pdf` and companion `.aux`/`.log`/`.nav`/`.out`/`.snm`/`.toc`/`.vrb` files.
- UNCHANGED (verified via `git diff --exit-code --stat`, zero diff required): `documents/reports/latex/slides/sections/01_title.tex`, `04_architecture.tex`, `05_data.tex`, `07_model.tex`, `13_references.tex`, `15_thankyou.tex`.
</content>
