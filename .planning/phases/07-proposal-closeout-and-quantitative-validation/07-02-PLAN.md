---
phase: 07-proposal-closeout-and-quantitative-validation
plan: 02
type: execute
wave: 2
autonomous: true
requirements:
  - CLS-03
files_modified:
  - .planning/phases/05-recall-priority-evaluation-and-release-gates/05-evaluation-snapshot.json
  - .planning/phases/05-recall-priority-evaluation-and-release-gates/05-explanation-review-pack.json
  - .planning/phases/05-recall-priority-evaluation-and-release-gates/05-release-eval-*.md
  - data/manifests/phase5-release-eval-*.json
  - D:/PROJEct/AI MODELS/**
---

# Phase 7 Plan 02: Final evaluation package and proposal evidence

Objective: refresh the locked baseline winner from the frozen recovered-balanced split set when needed, then generate one final held-out evaluation package on the repaired closeout holdout that states whether the proposal target F1 >= 0.85 was achieved.

Current status note: the repaired closeout evaluation path now lives under `data/splits/recovered-balanced/`. The earlier `data/splits/val.jsonl` sample run remains a truthful historical artifact, but it is not the closeout evidence path anymore.

Must haves:

- The locked baseline winner remains `qwen3-4b-instruct-2507` unless the user explicitly reopens the model-selection decision.
- Final quality claims must come from `data/splits/recovered-balanced/val.jsonl`, which now has non-zero support for all in-scope labels, not from the older sample split.
- The earlier 20 minute training run was only a smoke test and does not satisfy the plan's evidence requirement.
- If retraining is performed, the refreshed baseline adapter must be converted to GGUF before the default runtime-backed evaluation flow can measure it.
- On this machine, the working closeout conversion path is the Python 3.13 `convert_hf_to_gguf.py` script plus direct `q8_0` output because `llama-quantize` is not installed.
- The evaluation package must include macro and weighted F1, per-class precision/recall/F1, explanation-rubric review, and an explicit `PASS/BLOCK/FLAG` verdict.
- The evaluation package must be regenerated in order: saved snapshot, regenerated review pack, completed manual review, then final release-eval artifacts.
- The repo must retain one school-facing evidence path that answers the proposal question directly: did the final held-out run meet F1 >= 0.85 or not?

Artifacts:

- Refreshed training and GGUF artifacts for the locked baseline winner, if retraining is required.
- A new saved evaluation snapshot under `.planning/phases/05-recall-priority-evaluation-and-release-gates/`.
- A refreshed explanation review pack and final release-eval markdown plus JSON artifacts.
- A concise school-facing closeout note or report update that cites the final held-out result.

Verification:

- Optional retrain if the user wants a fresh baseline before final evaluation:

  `python -m src.model_adaptation.cli train --candidate baseline-winner --version-tag proposal-closeout-full-2026-05-26 --train-split data/splits/recovered-balanced/train.jsonl --val-split data/splits/recovered-balanced/val.jsonl --output-root "D:/PROJEct/AI MODELS" --registry-path "D:/PROJEct/AI MODELS/manifests/model-registry.json" --device cuda --full-precision`

- After any new retrain, convert the refreshed baseline to GGUF:

  `GGUF_CONVERTER_SCRIPT="C:/Users/wikiepeidia/AppData/Local/Programs/Python/Python313/Lib/site-packages/bin/convert_hf_to_gguf.py" python -m src.model_adaptation.cli convert --candidate baseline-winner --version-tag proposal-closeout-gguf-2026-05-26 --output-root "D:/PROJEct/AI MODELS" --registry-path "D:/PROJEct/AI MODELS/manifests/model-registry.json" --quantization-profile q8_0`

- Refresh the repaired-holdout snapshot:

  `python -m src.model_adaptation.cli evaluate-release-split --split-path data/splits/recovered-balanced/val.jsonl --snapshot-path .planning/phases/05-recall-priority-evaluation-and-release-gates/05-evaluation-snapshot.json --run-id phase5-recovered-balanced-val --progress-every 1 --checkpoint-every 1`

- Regenerate the explanation review pack:

  `python -m src.model_adaptation.cli prepare-explanation-review --snapshot-path .planning/phases/05-recall-priority-evaluation-and-release-gates/05-evaluation-snapshot.json --output-path .planning/phases/05-recall-priority-evaluation-and-release-gates/05-explanation-review-pack.json`

- Complete the manual review pack checkpoint, then synthesize the final verdict:

  `python -m src.model_adaptation.cli release-eval --snapshot-path .planning/phases/05-recall-priority-evaluation-and-release-gates/05-evaluation-snapshot.json --review-pack-path .planning/phases/05-recall-priority-evaluation-and-release-gates/05-explanation-review-pack.json --report-dir .planning/phases/05-recall-priority-evaluation-and-release-gates --manifest-dir data/manifests`

Success criteria:

- The final held-out evaluation package is reproducible and tied to `data/splits/recovered-balanced/`.
- The stale historical Phase 5 artifacts are replaced by repaired-holdout artifacts before final reporting begins.
- The repo contains one explicit answer to the proposal metric claim, backed by saved artifacts.
- The project can start final reporting without ambiguity about dataset scale or model quality evidence.
