---
quick_id: 260702-l0q
slug: re-evaluate-phase-28-golden-prompts-with
status: planned
created: 2026-07-02T08:08:05.578Z
---

# Quick Task 260702-l0q: Re-evaluate Phase 28 Golden Prompts

## Objective

Correct the Phase 28 golden prompt boundary so the live demo uses:

- a legitimate benign bank OTP notice that the app returns as `benign`
- a stronger bank-impersonation scam with a suspicious link that the app returns as `bank_impersonation`

## Tasks

1. Confirm the reported benign OTP issue against the current runtime.
2. Add a narrow runtime correction so legitimate bank OTP notices without unsafe action/link cues remain benign.
3. Keep link-based bank impersonation protected by tests.
4. Update `scripts/verify_golden_prompts.py` defaults to the corrected prompt pair.
5. Re-run the real browser verifier for 5 runs each and update Phase 28 evidence.
6. Record the quick-task outcome and update `STATE.md`.

## Verification

- `vnphish analyze` returns `benign` for legitimate OTP notices.
- `vnphish analyze` returns non-benign `bank_impersonation` for the malicious-link Vietcombank prompt.
- Focused runtime tests pass.
- `python scripts/verify_golden_prompts.py` exits 0 and writes stable results for both corrected golden prompts.
