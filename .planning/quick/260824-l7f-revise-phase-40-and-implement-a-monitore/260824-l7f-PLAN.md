---
quick_id: 260824-l7f
phase: quick-260824-l7f
plan: 01
type: quick
wave: 1
depends_on: []
status: planned
autonomous: false
requirements:
  - TRAIN-01
  - TRAIN-02
  - TRAIN-03
  - TRAIN-06
description: Revise Phase 40 and execute a monitored two-hour RTX 5050 LoRA-to-QLoRA decision experiment
files_modified:
  - src/model_adaptation/phase40_local_experiment.py
  - src/model_adaptation/phase40_operator.py
  - src/model_adaptation/training.py
  - tests/model_adaptation/test_phase40_local_experiment.py
  - tests/model_adaptation/test_phase40_operator.py
  - data/models/phase40/setup/
  - data/models/phase40/probes/rtx5050-local-decision/
  - .planning/phases/40-multi-model-training-evidence/40-LOCAL-PROBE-REPORT.md
  - .planning/phases/40-multi-model-training-evidence/40-04-PLAN.md
  - .planning/phases/40-multi-model-training-evidence/40-05-PLAN.md
user_setup:
  - service: Local Python package authority
    why: Genuine QLoRA requires one exact third-party binary package marked SUS by Phase 40 research; dependency-only setup was explicitly requested and completed before the experiment clock or any model load/training.
    status: completed_pre_clock
    dashboard_config:
      - task: Retain sanitized official-wheel provenance for the already normalized dependency-only authorization and exact bitsandbytes 0.50.1 installation.
        location: https://pypi.org/project/bitsandbytes/0.50.1/
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The complete local decision experiment has one immutable two-hour wall-clock deadline; the one audited zero-step compatibility retry, every stage transition, and any RDP interruption remain inside it and cannot reset it."
    - "LoRA runs first with the frozen full-run controls and no smoke-test mutation, then stops honestly on OOM, sufficient measured evidence, the normal 30-minute boundary, or the guarded 60-minute maximum."
    - "Dependency-only bitsandbytes 0.50.1 setup occurs before the experiment clock; LoRA model load/training still runs before QLoRA model load/training, whose start additionally requires verified package provenance, unchanged Torch distribution identity, and complete genuine NF4 proof."
    - "The existing D: Qwen snapshot is used in place without copying, downloading, symlinking, or junctioning, and its repository ID, pinned revision, file inventory, and hashes are sealed before model loading."
    - "Append-only telemetry records system RAM, training-process RSS, Torch allocated/reserved VRAM, device-wide VRAM, GPU temperature/power/utilization, optimizer timing, throughput, and a literal terminal reason even on OOM or timeout."
    - "The output recommends local full QLoRA or Colab fallback from measured ETA/resource evidence, but never turns the partial LoRA attempt into an accuracy result or an engineered failure story."
    - "No code path opens, reads, hashes, copies, uploads, or enumerates data/splits/test.jsonl."
  artifacts:
    - path: data/models/phase40/probes/rtx5050-local-decision/decision-manifest.json
      provides: Immutable two-hour window, stage outcomes, source hashes, telemetry hashes, stop reasons, and final local-versus-Colab recommendation inputs
    - path: data/models/phase40/probes/rtx5050-local-decision/base-model-provenance.json
      provides: Path-free sealed identity for the exact reused Qwen snapshot
    - path: data/models/phase40/probes/rtx5050-local-decision/lora/outcome.json
      provides: Time-bounded LoRA feasibility/resource outcome with no retained adapter
    - path: data/models/phase40/probes/rtx5050-local-decision/lora-retry-1/outcome.json
      provides: Hash-sealed terminal evidence from the single audited compatibility retry, including the literal controller error, retained partial steps, sustained GPU-pressure classification, and verified runtime discard
    - path: data/models/phase40/probes/rtx5050-local-decision/qlora/run-evidence.json
      provides: Exact 5-plus-40 genuine QLoRA measurements or an honest pre-start/runtime failure receipt
    - path: data/models/phase40/probes/rtx5050-local-decision/qlora/package-authority.json
      provides: Sanitized normalized authority linked to the verified pre-clock setup receipt without claiming a verbatim approval phrase
    - path: data/models/phase40/setup/bitsandbytes-0.50.1-install-provenance.json
      provides: Official wheel filename, URL, hash, size, normalized package identity, and a reproducible security-relevant package-selection command that is not misrepresented as the byte-for-byte invocation
    - path: data/models/phase40/setup/bitsandbytes-0.50.1-setup-receipt.json
      provides: Minimal pre/post inventory summaries, exact package delta, distribution identities, pip-check result, and hash link to install provenance
    - path: .planning/phases/40-multi-model-training-evidence/40-LOCAL-PROBE-REPORT.md
      provides: Report-safe measured findings, historical-artifact caveat, QLoRA ETA, and next-run recommendation
  key_links:
    - from: D:/PROJEct/AI MODELS/base/qwen3-4b-instruct-2507
      to: data/models/phase40/probes/rtx5050-local-decision/base-model-provenance.json
      via: exact download-manifest path/repository identity, Hugging Face local-dir revision metadata, reparse-point rejection, and deterministic file hashes
    - from: data/models/phase40/probes/rtx5050-local-decision/lora/telemetry.jsonl
      to: data/models/phase40/probes/rtx5050-local-decision/lora/outcome.json
      via: fail-closed finalization derives peaks, timing, throughput, and stop reason only from append-only raw events
    - from: data/models/phase40/setup/bitsandbytes-0.50.1-setup-receipt.json
      to: data/models/phase40/probes/rtx5050-local-decision/qlora/quantization-proof.json
      via: pre-clock normalized authority and official-wheel provenance are hash-linked into preflight; unchanged Torch distribution identity and the complete model/gradient proof precede QLoRA step zero
    - from: data/models/phase40/probes/rtx5050-local-decision/qlora/run-evidence.json
      to: .planning/phases/40-multi-model-training-evidence/40-LOCAL-PROBE-REPORT.md
      via: the locked ETA formula and resource thresholds are mechanically rendered from verified evidence
---

# Quick Task 260824-l7f: Monitored RTX 5050 LoRA-to-QLoRA decision experiment

<objective>
Replace the stale Phase 40 probe handoff with one executable, monitored, two-hour local decision experiment: reuse the exact existing Qwen bytes, run LoRA first under a bounded feasibility policy, then run a genuine 5-warm-up plus 40-measured-step QLoRA probe and calculate a defensible full-run ETA.

Purpose: Avoid spending Colab credit or laptop hours on an unmeasured assumption while preserving honest evidence. The LoRA attempt answers whether ordinary adapter training is practical on this 8 GB laptop; the QLoRA probe answers whether a full local QLoRA run is likely to fit and finish in useful time. Neither outcome is predetermined.

Output: Tested local-decision tooling, one immutable evidence root, a report-safe local probe report, and revised Phase 40 Plans 04/05 that make Colab a fallback for QLoRA rather than a mandatory destination.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/39-independent-quality-re-judge/39-DOWNSTREAM-DATA-CONTRACT.json
@.planning/phases/40-multi-model-training-evidence/40-CONTEXT.md
@.planning/phases/40-multi-model-training-evidence/40-AI-SPEC.md
@.planning/phases/40-multi-model-training-evidence/40-RESEARCH.md
@.planning/phases/40-multi-model-training-evidence/40-03-SUMMARY.md
@.planning/phases/40-multi-model-training-evidence/40-04-PLAN.md
@.planning/phases/40-multi-model-training-evidence/40-05-PLAN.md
@src/model_adaptation/phase40_operator.py
@src/model_adaptation/training.py
@src/model_adaptation/phase40_callbacks.py
@src/model_adaptation/phase40_evidence.py
@tests/model_adaptation/test_phase40_operator.py
@tests/model_adaptation/test_phase40_training.py
@tests/model_adaptation/test_phase40_quantization.py

Read-only off-repo evidence permitted for this task:

- `D:\PROJEct\AI MODELS\manifests\download-manifest.json`
- `D:\PROJEct\AI MODELS\manifests\model-registry.json`
- `D:\PROJEct\AI MODELS\base\qwen3-4b-instruct-2507\` and its `.cache\huggingface\download\*.metadata` provenance files
- `D:\PROJEct\AI MODELS\finetuned qwen scam\qwen3-4b-instruct-2507\adapter\training-summary.json`

Locked facts verified during planning:

- Canonical training and validation inputs contain 1,658 and 219 rows with SHA-256 `5fa46382db8fb477ef91ec4ba770bf3f8756df9f98b9950fdf5bc1f6ff402e8b` and `746ae6edb5008a8be8e9ef9d65f89fc44e559f99f28cd8d6a77f203ea5986d3c`. The real test file is outside this task and must not be touched even to reconfirm its hash.
- Qwen identity is `Qwen/Qwen3-4B-Instruct-2507` at immutable revision `cdbee75f17c01a7cc42f958dc650907174af0554`.
- The existing snapshot is `D:\PROJEct\AI MODELS\base\qwen3-4b-instruct-2507`; the download manifest binds that path to the exact repository ID and reports 8,060,919,016 bytes. Every inspected Hugging Face local-dir metadata record begins with the pinned revision. This is preliminary identity evidence, not a substitute for the task's complete file seal.
- The snapshot is a real directory, not a link. Runtime validation must additionally reject every Windows reparse point or junction in its complete path and inventory.
- RTX 5050 environment observed on 2026-08-24: 8,151 MiB device memory, Torch `2.12.0+cu132`, CUDA available, BF16 supported, Transformers `5.9.0`, PEFT `0.19.1`, and Accelerate `1.13.0`. Dependency-only setup subsequently installed exactly `bitsandbytes==0.50.1` with `--no-deps` before the experiment clock; no model was loaded and no training was started during setup.
- Qwen controls remain max sequence length 1024, per-device train/eval batch 1, gradient accumulation 4, effective batch 4, three epochs, seed/data seed 42, AdamW Torch, linear schedule, warm-up ratio 0.03, learning rate `2e-4`, and log/eval/save cadence 10/50/50. With 1,658 training rows this is 415 optimizer steps per epoch and 1,245 planned full-run steps.
- The existing `--smoke-test` behavior is forbidden for this experiment because it changes gradient accumulation and would invalidate the ETA.
- The older off-repo training summary identifies an historical `4bit-qlora` run with 2,333 train rows, 254 validation rows, 584 optimizer steps, and 1,151.5112 seconds runtime. It does not retain GPU identity, device/system memory, thermals, or the current corpus, so it is context only and cannot be reported as current RTX 5050 evidence. The remembered 7.7 GB VRAM / approximately 20 GB RAM figures remain unverified unless a retained old artifact proves them.

## Locked decisions

- **QD-01 — One clock:** The local decision window is 7,200 seconds total. Its start/boot identity is immutable. Exactly one retry is permitted only for the sealed zero-step Transformers control-compatibility failure; it deducts the first attempt from both LoRA limits and cannot reset the clock.
- **QD-02 — LoRA first:** Ordinary LoRA uses the full frozen controls. It is disposable feasibility/resource evidence, not a partial accuracy result. Stop on genuine OOM/error, at the normal 1,800-second boundary, or earlier only if the model cannot start. Continue beyond 1,800 seconds only when 1–39 retained post-warm-up steps exist, losses are finite, telemetry is current, and collecting the 40-step evidence target remains possible; then stop on step 40 or at 3,600 seconds, whichever occurs first. Never exceed 3,600 LoRA seconds.
- **QD-03 — QLoRA training second:** The explicit free-form dependency-only instruction is normalized to approval for exactly `bitsandbytes==0.50.1` without pretending the normalized phrase was typed verbatim. Its pre-clock `--no-deps` installation and official-wheel provenance must verify, Torch distribution identity must match on version, CUDA version, module-path string hash, and distribution `RECORD` hash, and genuine four-bit proof must pass before QLoRA model training begins. QLoRA retains 5 warm-up and exactly 40 measured optimizer steps.
- **QD-04 — Reuse, do not duplicate:** Use the exact D: snapshot in place after path, revision, metadata, inventory, and content verification. No model download, copy, symlink, junction, or alternate checkpoint is permitted.
- **QD-05 — Complete telemetry:** Sample every two seconds and at lifecycle boundaries: system total/available/used RAM, worker RSS, Torch allocated/reserved/peak VRAM, device total/used/free VRAM, utilization, temperature, power, performance state, optimizer step duration, examples/s, tokens/s, and literal stop/OOM/timeout reason. Retain raw UTC plus monotonic timestamps.
- **QD-06 — Honest decision:** Derive ETA from the exact successful QLoRA measurements. A local full QLoRA run may be recommended; Colab remains the fallback. LoRA and QLoRA resource results are comparable only under the same machine/configuration window, and no partial LoRA validation/accuracy comparison is allowed.
- **QD-07 — Evaluation boundary:** Use only canonical train and validation. Never inspect the held-out file in this task.
- **QD-08 — Recovery is not another run:** If the one retry reaches terminal telemetry but Windows cleanup prevents outcome sealing, a committed recovery-only command may hash-verify the exact retained state, prove the child and model artifacts absent, discard only the bounded runtime residue, and append the literal error outcome. It cannot load a model, train, relabel the terminal reason, or create a second retry.

## Executed LoRA outcome

- Attempt 1 stopped before `Trainer` construction and optimizer step 1 because Transformers 5.9 changed two control representations. It retained zero steps and did not prove memory pressure.
- Commits `62b2f1e` and `803c3b3` added semantic control verification and one source-hash-bound retry under the original clock. The first attempt's 35.656 seconds were deducted, leaving 1,764.344/3,564.344-second retry limits.
- The retry retained 31 observed optimizer steps: five warm-up and 26 measured. Its median measured step was 53.274 seconds; all logged losses were finite.
- The retry terminal remains `status=error`, `stop_reason=parent_controller_error`, `measured_target_reached=false`. The original controller exception was not retained. Runtime cleanup was incomplete with read-only OneDrive residue; a termination-race diagnosis remains an inference.
- Telemetry proves sustained GPU pressure, not OOM: peak 7,902/8,151 MiB device use, minimum 9 MiB free, and 826 consecutive pressure samples. Minimum available system RAM was 11,464,290,304 bytes and peak temperature was 61C.
- Recovery commits `e8eb645` and `edb3ee8` launched no model process. They verified historical run code/artifact hashes, removed only the bounded runtime, wrote a verified discard receipt, sealed the literal outcome, and appended `lora-retry-1` once.
- No further LoRA retry is authorized. The next training stage is the genuine QLoRA 5+40 probe after its existing package/runtime gates.

<interfaces>
Add one operator surface, `python -m src.model_adaptation.phase40_operator phase40-local-decision`, with explicit stages `preflight`, `record-authority`, `lora`, the single typed `lora-retry-1`, `verify-package`, `qlora`, `finalize`, and `verify`. Every stage consumes the same fixed `--decision-root data/models/phase40/probes/rtx5050-local-decision`; later stages must verify the immutable state and hashes written by earlier stages before doing work. The separate `phase40_lora_recovery seal-lora-retry-failure` surface is recovery-only and is valid solely for the exact retained terminal fingerprint above.

The parent controller launches training in a child Python process with data-loader workers fixed to zero. The parent owns the two-hour deadline, two-second system/device sampler, stdout/stderr capture, and hard termination. The child owns Torch allocated/reserved metrics and optimizer events. No shell-built command is accepted. On a hard deadline, the parent requests an optimizer-boundary stop, gives a short fixed grace period, terminates the one child if required, hashes all partial logs, and removes any adapter/checkpoint under the bounded decision root. A terminated or interrupted path remains evidence, never a successful probe.

The ETA authority is the existing Phase 40 callback formula: median post-warm-up optimizer-step seconds multiplied by 1,245 planned optimizer steps, plus measured evaluation/checkpoint overhead, clearly marked as an estimate. Also retain the measured-step distribution and formula inputs so the number can be recomputed.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Build the fail-closed local decision controller and prove one complete fake lifecycle</name>
  <files>src/model_adaptation/phase40_local_experiment.py, src/model_adaptation/phase40_operator.py, src/model_adaptation/training.py, tests/model_adaptation/test_phase40_local_experiment.py, tests/model_adaptation/test_phase40_operator.py</files>
  <behavior>
    - QD-01: an immutable decision state starts once, records UTC/monotonic/Windows boot identity, enforces 7,200 seconds across every stage, and rejects reset, clock rollback, reboot, duplicate stage, or continuation after expiry.
    - QD-04: preflight accepts only the exact D: path bound by the download manifest and pinned Hugging Face metadata, rejects any symlink/junction/reparse point or identity drift, seals every file through the existing Qwen provenance machinery, sets offline/local-files-only flags, and performs no copy or network call.
    - QD-07: preflight opens only the exact train and validation allowlist paths and checks their frozen counts/hashes/order; a supplied held-out, alternate, or alias path is rejected before open and before the decision artifact directory is populated beyond a sanitized failure receipt.
    - QD-02: LoRA preserves batch 1, gradient accumulation 4, max length 1024, seed 42, three-epoch planned controls and 1,245-step ETA denominator; its soft/hard timebox and narrow 1–39-step extension predicate cannot be overridden past 3,600 seconds.
    - QD-03: QLoRA cannot enter model construction without the hash-linked sanitized pre-clock setup receipt, exact installed version, unchanged Torch distribution identity (version, CUDA version, module-path string hash, and distribution `RECORD` hash), CUDA kernel proof, NF4 plus double-quant config, `is_loaded_in_4bit`, positive `Linear4bit` count, frozen base, adapter-only trainables, and non-null LoRA gradients.
    - QD-05: a two-second append-only sampler joins parent system/device samples to child Torch/optimizer events, preserves missing NVIDIA values as explicit null-plus-raw fields, flushes each record, and always emits a terminal sample and stop reason on success, OOM, timeout, interrupt, or error.
    - QD-03/QD-06: a successful QLoRA outcome requires exactly 5 excluded warm-up plus 40 retained optimizer steps, derives the 1,245-step ETA from raw event references, and cannot be finalized if any event/hash/step is missing.
    - QD-02/QD-03: every partial adapter/checkpoint stays under the decision root, is hashed and removed, and has a verified discard/absence receipt; no probe can resume, publish, register, or seed a later full run.
  </behavior>
  <action>
Implement `phase40_local_experiment.py` as the typed state machine, Windows-safe provenance/monitoring controller, immutable JSON/JSONL writer, outcome finalizer, and report-data verifier for QD-01 through QD-07. Reuse `preflight_phase40_inputs`, `seal_qwen_base_model_snapshot` / `validate_qwen_base_model_snapshot`, `Phase40EvidenceCallback`, `ProbeExecutionContract`, quantization proof, and discard-receipt primitives rather than duplicating their validators. Add Windows reparse-point inspection using file attributes because `Path.is_symlink()` alone is insufficient for junctions. Validate the download-manifest entry and require the load-critical local-dir metadata records for config, tokenizer, index, and every weight shard to carry the exact pinned revision before sealing the complete snapshot into a path-free evidence manifest under the decision root. Do not modify or place a manifest inside the D: snapshot.

Add the `phase40-local-decision` stages to `phase40_operator.py`. Keep the existing Colab/full-run commands unchanged. Every stage uses exact typed arguments and the same decision root; reject unknown fields, absolute paths in retained portable evidence, secrets, stage replay, output-root drift, and any input path outside the two-file train/validation allowlist. `preflight` creates the immutable clock only after all path arguments pass lexical validation, then verifies data/model/environment identity and the hash-linked pre-clock package-setup baseline without model loading. `record-authority` records the normalized decision from that immutable receipt after LoRA, explicitly marking that it was not typed verbatim. `verify-package` compares Torch distribution identity and performs the side-effect-free bitsandbytes/CUDA capability check. `lora` and `qlora` launch the internal child worker with `shell=False`, data-loader workers zero, offline Hugging Face flags, and no smoke flag. `finalize` mechanically builds the manifest/report inputs; `verify` is read-only and rehashes the complete result.

Extend `training.py` with a Phase 40 local-decision config builder and child-worker entry point that materialize the exact matched Qwen controls without a full-run request and without the old smoke-test mutation. LoRA uses a time-bounded disposable lifecycle with its planned full step count retained only for ETA; QLoRA uses the existing exact probe contract. At optimizer boundaries the child consumes the controller stop request, writes failure/resume telemetry on exception, never converts QLoRA to LoRA, and returns no publishable adapter path. Keep model load `local_files_only=True` and `trust_remote_code=False`.

Write tests first with fake model inventories, fake clocks, fake Win32/NVIDIA samplers, and fake training children. Cover each behavior above, including OOM before step zero, OOM after partial events, soft-stop, the narrow healthy extension, absolute hard stops, stale telemetry, malformed `nvidia-smi`, package rejection, Torch drift, incomplete four-bit proof, missing/extra/reordered QLoRA steps, RDP-like parent interruption, and verified adapter deletion. Add an open-spy regression proving a rejected held-out-style input causes zero opens; use only temporary fake files and never access the repository's real held-out split. Preserve the existing full-run operator behavior and all current model-adaptation tests.
  </action>
  <verify>
    <automated>python -m pytest tests/model_adaptation/test_phase40_local_experiment.py tests/model_adaptation/test_phase40_operator.py tests/model_adaptation/test_phase40_training.py tests/model_adaptation/test_phase40_quantization.py -q --basetemp .tmp/pytest-q260824-l7f-controller</automated>
    <automated>python -m src.model_adaptation.phase40_operator phase40-local-decision --help</automated>
  </verify>
  <done>The production operator can execute one fake end-to-end LoRA-then-QLoRA decision lifecycle with an immutable deadline, external snapshot seal, complete telemetry, exact controls/proof, deterministic ETA, and verified artifact disposal while all rejected boundary cases fail before unsafe work.</done>
</task>

<task type="auto">
  <name>Task 2: Seal the already authorized dependency-only setup before the experiment clock</name>
  <read_first>.planning/phases/40-multi-model-training-evidence/40-RESEARCH.md, .planning/phases/40-multi-model-training-evidence/40-AI-SPEC.md</read_first>
  <action>
The operator explicitly instructed dependency-only setup and deferred training. Normalize that free-form instruction to approval for exactly `bitsandbytes==0.50.1`; retain only the neutral source type `explicit_user_dependency_only_instruction` and a SHA-256 of the source, never the raw chat or a fabricated claim that the normalized approval phrase was typed verbatim. Inspect the official PyPI release file, install only the Windows x86-64 wheel with `--no-deps`, and do not load a model or start training. Retain a sanitized install-provenance JSON with the official wheel filename, URL, hash, byte size, normalized package/version, and the reproducible security-relevant package-selection arguments. Record that the actual invocation also produced an ephemeral pip report whose flag/path was intentionally not retained; do not claim byte-for-byte argv or retain that raw report. Retain a separate hash-linked receipt containing only pre/post inventory count and hash, exact package delta, Python/Torch/bitsandbytes distribution identities, NF4 runtime setup check, and `pip check`; do not retain the complete installed-package inventory. Define Torch stability narrowly as unchanged distribution identity across version, CUDA version, module-path string hash, and distribution `RECORD` hash.
  </action>
  <what-built>The tested local controller plus a dependency-only package setup completed before the decision clock; no model load or training occurred.</what-built>
  <how-to-verify>
    1. Verify the receipt and its hash-linked install provenance against the current Python package inventory.
    2. Confirm the exact package delta contains only `bitsandbytes==0.50.1` and `pip check` passes.
    3. Confirm both public setup artifacts contain neither raw user text nor the complete package inventory.
  </how-to-verify>
  <resume-signal>The sanitized setup receipt verifies live.</resume-signal>
  <verify>
    <automated>python -c "from pathlib import Path; from src.model_adaptation.phase40_local_experiment import verify_bitsandbytes_setup_receipt; print(verify_bitsandbytes_setup_receipt(Path('.')))"</automated>
  </verify>
  <done>The exact dependency-only installation is sanitized, provenance-linked, distribution-identity checked, and complete before the experiment clock, while all model loading and training remain pending.</done>
</task>

<task type="auto">
  <name>Task 3: Run LoRA then authorized QLoRA inside two hours and revise Phase 40 from the measured result</name>
  <precondition>Task 1 tests pass, Task 2's sanitized setup receipt verifies live, the RTX 5050 is visible and idle enough for an attended experiment, and AC power is connected.</precondition>
  <files>data/models/phase40/probes/rtx5050-local-decision/, .planning/phases/40-multi-model-training-evidence/40-LOCAL-PROBE-REPORT.md, .planning/phases/40-multi-model-training-evidence/40-04-PLAN.md, .planning/phases/40-multi-model-training-evidence/40-05-PLAN.md</files>
  <action>
Run the focused regression suite first, verify the pre-clock package receipt, then establish one clean decision root and execute `phase40-local-decision --stage preflight` with only `data/splits/train.jsonl`, `data/splits/val.jsonl`, the Phase 39 downstream contract, the exact D: snapshot, the exact off-repo download manifest, model ID/revision, and `--decision-window-seconds 7200`. This stage starts the immutable experiment clock and captures the verified package/Torch distribution baseline, GPU/driver/CUDA, baseline RAM/VRAM/thermal/power facts, data identities, complete model seal, and sanitized command. Do not invoke model acquisition, package installation, a network API, or any held-out path.

Immediately run `--stage lora` with `--lora-soft-limit-seconds 1800`, `--lora-hard-limit-seconds 3600`, `--warmup-steps 5`, `--evidence-target-steps 40`, and the frozen controls. Do not pass `--smoke-test`. Treat OOM, model-load failure, time-bound completion, or a healthy successful partial run as observed evidence. At 1,800 seconds stop normally; extend only under QD-02's predeclared 1–39-step predicate and never beyond the earlier of retained step 40, 3,600 LoRA seconds, or the overall two-hour deadline. Hash and remove every LoRA checkpoint/adapter. Do not run validation for checkpoint selection or call the partial attempt a trained model.

Next persist the normalized Task 2 authority through `--stage record-authority`, linking its source and setup-receipt hashes and marking it as normalized rather than verbatim. Do not install or reinstall anything during the experiment. Run `--stage verify-package`; require exact bitsandbytes identity, successful import/kernel capability, and unchanged Torch distribution identity across version, CUDA version, module-path string hash, and distribution `RECORD` hash. Any drift stops QLoRA and records package/runtime failure without attempting repair.

Only after package verification, run `--stage qlora --warmup-steps 5 --post-warmup-steps 40` with the same frozen controls and the remaining immutable decision time. Require the full model-level NF4/double-quant, `Linear4bit`, frozen-base, adapter-only, forward/backward-gradient proof before step zero. Retain exactly 40 post-warm-up optimizer-step events on success and one isolated evaluation/checkpoint overhead measurement required by the current ETA contract. If the overall clock cannot accommodate completion, stop at 7,200 seconds and record an incomplete timeout rather than resetting the clock or estimating from fewer steps. Hash and discard every QLoRA adapter/checkpoint.

Run `--stage finalize` and `--stage verify`. The immutable manifest must hash the base provenance, data identities, authority/Torch proof, raw child logs, both telemetry streams, optimizer events, outcomes, ETA inputs, and discard receipts. Render `40-LOCAL-PROBE-REPORT.md` mechanically. Report LoRA as one of: pre-start failure, genuine OOM, time-bounded resource-constrained partial run, or time-bounded feasible partial run. Use “too memory-intensive on this configuration” only if a predeclared pressure predicate is actually met; a 30-minute stop alone does not prove LoRA impossible. Report QLoRA ETA as an extrapolation from 5+40 measurements, not completed full runtime. Include the historical 584-step/1,151.5112-second QLoRA summary only as non-comparable context because it lacks current hardware/memory provenance; omit the remembered 7.7/20 GB figures unless a retained old artifact independently verifies them.

Revise `40-04-PLAN.md` to match the executed two-hour LoRA-first policy, complete telemetry, exact external-snapshot reuse, explicit package authority, and QLoRA 5+40 contract. Revise `40-05-PLAN.md` so a fresh step-zero full QLoRA run may execute locally when the verified probe completes, shows no OOM or memory/thermal-pressure failure, and its ETA is acceptable to the operator; otherwise route that run to Colab. Keep Colab checkpoint resume as fallback. Do not silently treat the partial LoRA attempt as the full TRAIN-01 quality run: retain any still-required fresh full LoRA/PhoBERT obligations and explicitly limit the local comparison to feasibility/resources unless the project requirements are separately changed. Preserve validation-only Phase 40 and the single-seed/no-significance limitation.

Finally run all model-adaptation tests and `git diff --check`. Do not start the later full QLoRA run in this quick task; hand back the verified ETA, peak resources, literal stop reasons, and the plan-selected local/Colab route for the operator's next action.
  </action>
  <verify>
    <automated>python -m src.model_adaptation.phase40_operator phase40-local-decision --stage verify --decision-root data/models/phase40/probes/rtx5050-local-decision</automated>
    <automated>python -m pytest tests/model_adaptation -q --basetemp .tmp/pytest-q260824-l7f-regression</automated>
    <automated>git diff --check -- src/model_adaptation/phase40_local_experiment.py src/model_adaptation/phase40_operator.py src/model_adaptation/training.py tests/model_adaptation/test_phase40_local_experiment.py tests/model_adaptation/test_phase40_operator.py .planning/phases/40-multi-model-training-evidence/40-04-PLAN.md .planning/phases/40-multi-model-training-evidence/40-05-PLAN.md .planning/phases/40-multi-model-training-evidence/40-LOCAL-PROBE-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - The single decision manifest proves no more than 7,200 seconds elapsed from immutable preflight start through the terminal outcome, and LoRA consumed no more than 3,600 seconds.
    - Dependency-only bitsandbytes setup and provenance sealing occurred before the experiment clock; LoRA preserves the exact full controls without the old smoke-test mutation and runs before any QLoRA model load/training.
    - LoRA telemetry contains two-second system RAM, process RSS, Torch and device-wide VRAM, temperature, power, utilization, optimizer timing, throughput, and one literal verified stop reason; every temporary model artifact is absent.
    - The pre-clock setup receipt proves only bitsandbytes 0.50.1 was added with `--no-deps`; no raw chat/full inventory is public, and the narrowly defined Torch distribution identity is unchanged.
    - Successful QLoRA evidence proves genuine four-bit mode and exactly 5 warm-up plus 40 retained steps; otherwise the exact pre-start/OOM/timeout reason and no-artifact/discard receipt verify.
    - A successful QLoRA probe contains recomputable 1,245-step ETA inputs and measured overhead; an incomplete probe contains no fabricated ETA.
    - The report distinguishes measurement, extrapolation, historical context, and unverified recollection, and does not claim a partial LoRA accuracy result or engineered OOM.
    - Plans 40-04/05 reflect local-first measured selection with Colab fallback while preserving any unwaived full-model requirements and the Phase 41 held-out boundary.
    - The complete model-adaptation suite and diff hygiene checks pass without accessing the real test split.
  </acceptance_criteria>
  <done>The RTX 5050 has one honest, fully monitored LoRA-to-QLoRA decision record inside the two-hour cap, an exact QLoRA full-run ETA when measurable, no surviving probe adapter, and an evidence-driven local-versus-Colab next step.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Off-repo D: snapshot -> training process | Large existing model bytes are trusted only after exact path/revision/inventory verification and reparse-point rejection. |
| PyPI binary wheel -> local CUDA/Torch runtime | Third-party native code entered during explicit dependency-only pre-clock setup; sanitized official-wheel provenance, exact package delta, and Torch distribution identity constrain that boundary. |
| Parent monitor -> child trainer | Deadline, stop policy, and system/device telemetry cross a process boundary and must remain complete on crash/OOM. |
| Raw events -> report/Phase 40 routing | Measurements become defense claims and a local-versus-Colab recommendation. |
| Canonical train/validation -> model | Approved inputs cross into training while the held-out partition remains unopened. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-L7F-SC | Tampering / Elevation of privilege | bitsandbytes installation | high | mitigate | Phase 40 SUS audit, explicit dependency-only user instruction normalized without fabricated verbatim wording, official PyPI wheel provenance, exact 0.50.1 pin, `--no-deps`, minimal package delta, and unchanged Torch distribution-identity proof. |
| T-L7F-01 | Spoofing / Tampering | external base-model identity | high | mitigate | Exact download-manifest binding, HF metadata revision checks, full deterministic seal, local-files-only load, and rejection of every symlink/junction/reparse point. |
| T-L7F-02 | Denial of service | RTX training controller | high | mitigate | Immutable two-hour parent deadline, cumulative 30/60-minute LoRA boundaries, one child with zero loader workers, graceful stop then bounded termination, and exactly one hash-gated zero-step compatibility retry rather than a retry loop. |
| T-L7F-03 | Repudiation | resource/ETA evidence | high | mitigate | Append-only UTC/monotonic telemetry, terminal samples, raw-event hashes, exact 5+40 QLoRA count, mechanical formula, and read-only verifier. |
| T-L7F-04 | Spoofing | QLoRA identity | high | mitigate | Exact authority/version plus kernel, NF4/double-quant, `Linear4bit`, frozen-base, adapter-only, and gradient proof before step zero; no semantic fallback. |
| T-L7F-05 | Information disclosure | retained commands/environment/logs | medium | mitigate | Allowlisted environment/package/hardware fields, sanitized argv and exception text, path-free portable manifests, and no secrets or user-home inventory. |
| T-L7F-06 | Tampering / Information disclosure | held-out split | critical | mitigate | Exact two-path input allowlist, lexical rejection before open, fake-file open-spy tests, and no held-out path in any real command/evidence artifact. |
| T-L7F-07 | Repudiation | LoRA feasibility wording | high | mitigate | Predeclared stop/resource predicates, literal outcome enum, no accuracy use, no forced OOM, and report generation solely from verified evidence. |
</threat_model>

<verification>
The quick task passes only when the focused controller tests, live sanitized package-receipt verifier, real decision-root verifier, full model-adaptation regression suite, and diff hygiene gate all pass. A real OOM, package compatibility failure, RDP interruption, or two-hour timeout is an acceptable measured outcome only if its partial logs, terminal reason, artifact-absence/discard receipt, and report wording verify. Missing QLoRA steps, Torch distribution-identity drift, mode fallback, deadline reset, unverified external model bytes, retained adapters, or any held-out access blocks completion.
</verification>

<success_criteria>
- The experiment finishes or stops honestly within one immutable two-hour window.
- LoRA is measured first for sustained feasibility/resource behavior under the actual full controls and cannot be misrepresented as a full accuracy result.
- QLoRA either completes the exact genuine 5-plus-40 protocol and produces a recomputable full-run ETA, or preserves a complete failure receipt without fallback.
- Every required memory, GPU, thermal, power, utilization, step, throughput, and stop field is present in hash-linked raw evidence.
- The exact existing Qwen snapshot is reused without download/copy/link indirection and with complete pinned provenance.
- Phase 40 routes a future fresh full QLoRA run to this laptop when supported by evidence and keeps Colab as fallback, while unwaived full LoRA/PhoBERT requirements remain visible.
- No held-out row is accessed and no report sentence overstates what the two-hour experiment proves.
</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Feature / requirement | Task | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Monitored two-hour local LoRA-to-QLoRA decision experiment | 1,3 | COVERED | Controller plus real execution and final verification. |
| REQ | TRAIN-01 | Genuine RTX LoRA feasibility, resource evidence, disposable artifact | 1,3 | COVERED | Latest user decision replaces the old equal 40-step LoRA probe with a time-bounded full-control feasibility attempt; it does not impersonate the still-unwaived full accuracy run. |
| REQ | TRAIN-02 | Genuine RTX QLoRA probe and ETA | 1-3 | COVERED | Exact package authority, four-bit proof, 5 warm-up plus 40 retained steps, and failure branch. |
| REQ | TRAIN-03 | Matched Qwen controls and honest comparison boundary | 1,3 | COVERED | Both modes use the frozen controls; only same-machine resources are compared, not partial LoRA accuracy. |
| REQ | TRAIN-06 | Raw logs, environment/resources, hashes, reproducible ETA/report | 1,3 | COVERED | Append-only telemetry and deterministic finalizer/verifier. |
| RESEARCH | Package Legitimacy Audit | bitsandbytes 0.50.1 requires verified authority and provenance | 2,3 | COVERED | Explicit dependency-only authority was normalized and the exact no-deps install was provenance-sealed before the experiment clock, model load, or training. |
| CONTEXT | RTX probes | Genuine success/OOM/failure, ETA, no engineered outcome, discarded adapters | 1,3 | COVERED | Timebox is predeclared and every terminal state is evidence. |
| CALLER | QD-01 | Total local work capped at two hours | 1,3 | COVERED | Parent process owns immutable global deadline. |
| CALLER | QD-02 | LoRA first, normal 30-minute stop, guarded maximum 60 minutes | 1,3 | COVERED | Narrow extension only gathers the locked evidence target when already progressing. |
| CALLER | QD-03 | QLoRA training second with exact 5+40 and unchanged Torch distribution identity | 1-3 | COVERED | Pre-clock setup is hash-linked; no QLoRA model load/training begins before package and four-bit proof. |
| CALLER | QD-04 | Reuse exact D: snapshot without copy/download/link | 1,3 | COVERED | Manifest, HF metadata, reparse rejection, complete content seal. |
| CALLER | QD-05 | RAM/VRAM/thermal/power/utilization/step telemetry | 1,3 | COVERED | Parent and child streams are hash-linked and terminal-safe. |
| CALLER | QD-06 | Evidence-driven local full QLoRA or Colab fallback | 3 | COVERED | Plan 40-05 routing changes only after verified measurement. |
| CALLER | QD-07 | No real test access | 1,3 | COVERED | Reject-before-open allowlist plus fake-file open-spy and real-command review. |
| HISTORY | old QLoRA summary | Check prior artifact without treating it as current proof | 3 | COVERED | Exact retained fields are context; absent resource/hardware data and recollection are labeled unverified. |
| CONTEXT | Fresh full LoRA/PhoBERT and held-out evaluation | Later Phase 40/41 obligations | — | EXCLUDED | This quick experiment chooses the QLoRA execution route; it does not silently waive full-model or Phase 41 requirements. |

<output>
Execution creates the code/tests, sanitized pre-clock setup evidence, decision evidence/report/plan revisions above, and `.planning/quick/260824-l7f-revise-phase-40-and-implement-a-monitore/260824-l7f-SUMMARY.md`. It does not download/copy a model, start a full QLoRA run, or access the held-out split.
</output>
