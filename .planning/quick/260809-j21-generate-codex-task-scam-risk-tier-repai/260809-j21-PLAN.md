---
quick_id: 260809-j21
phase: quick-260809-j21
plan: 01
type: quick
wave: 1
depends_on: []
status: planned
autonomous: true
requirements: []
description: Generate and fail-closed validate all 187 Codex task_scam risk-tier repair judgments without external providers
files_modified:
  - data/processed/codex-task-scam-risk-tier-repair.jsonl
must_haves:
  truths:
    - "Every one of the 187 prepared targets has exactly one Codex judgment in target-file order."
    - "Each judgment is based on the live row text and matching seed_id, not only the prior judge reason."
    - "Every final suspicious span is an exact case-sensitive substring of the live text, and changed matches the actual field delta."
    - "The repository's fail-closed consumer accepts the complete artifact when exercised against temporary split copies."
    - "No external model, API, web, or plugin is used, and no input or live split file changes."
  artifacts:
    - path: "data/processed/codex-task-scam-risk-tier-repair.jsonl"
      provides: "187 ordered risk_tier and suspicious_spans repair judgments"
      line_count: 187
  key_links:
    - from: "data/processed/task-scam-risk-tier-repair-targets.jsonl"
      to: "data/processed/codex-task-scam-risk-tier-repair.jsonl"
      via: "ordered (split, row_index, seed_id) identity"
    - from: "data/processed/codex-task-scam-risk-tier-repair.jsonl"
      to: "src/data_pipeline/apply_task_scam_risk_tier_repair.py"
      via: "RiskTierRepairResult schema, validate_coverage, and apply_repair on temporary copies"
---

# Quick Task 260809-j21: Generate Codex task_scam risk-tier repair judgments

<objective>
Create the complete 187-line Codex judgment artifact requested by `.planning/codex-task-scam-risk-tier-repair-instructions.md`, preserving target identity and order while correcting each row's `risk_tier` and decisive unsafe-action evidence spans from the live text.

Purpose: Replace a failed keyword repair with row-by-row Codex judgment, then prove the artifact is safe for the separate merge step without touching the live corpus.

Output: `data/processed/codex-task-scam-risk-tier-repair.jsonl` only.
</objective>

<execution_context>
@C:/Users/wikiepeidia/.codex/gsd-core/workflows/execute-plan.md
@.planning/codex-task-scam-risk-tier-repair-instructions.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/codex-task-scam-risk-tier-repair-instructions.md
@data/processed/task-scam-risk-tier-repair-targets.jsonl
@data/splits/train.jsonl
@data/splits/val.jsonl
@data/splits/test.jsonl
@src/data_pipeline/apply_task_scam_risk_tier_repair.py
@tests/data_pipeline/test_apply_task_scam_risk_tier_repair.py

Current deterministic preflight evidence:
- Targets: 187 total (`train=143`, `val=21`, `test=23`).
- Every target currently resolves to an in-range live row with the same `seed_id` (`0` mismatches).
- The requested output file is absent before execution.
- Locked input SHA-256 values are recorded in Task 2.

<interfaces>
The output contract is exactly seven fields per line: `split`, `row_index`, `seed_id`, `new_risk_tier`, `new_suspicious_spans`, `changed`, and `notes`. `RiskTierRepairResult` forbids extra fields and restricts `new_risk_tier` to `benign`, `suspicious`, or `high-risk`. `validate_coverage()` requires one result for every target identity. `apply_repair()` checks live row bounds, `seed_id`, and exact span containment before any writes; this plan invokes it only on temporary copies.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Judge all 187 live task_scam rows in four ordered batches</name>
  <read_first>
    .planning/codex-task-scam-risk-tier-repair-instructions.md
    data/processed/task-scam-risk-tier-repair-targets.jsonl
    data/splits/train.jsonl
    data/splits/val.jsonl
    data/splits/test.jsonl
    src/data_pipeline/apply_task_scam_risk_tier_repair.py
  </read_first>
  <files>data/processed/codex-task-scam-risk-tier-repair.jsonl</files>
  <action>
Work only in this Codex session. Do not use web search, plugins, Claude, provider SDKs, HTTP calls, external APIs, external model CLIs, or heuristic/keyword automation to make the judgments. A read-only local join may be used to display each target beside its live row, but Codex must read and decide every row.

Process target ordinals `1-47`, `48-94`, `95-141`, and `142-187` as four append-only batches of 47, 47, 47, and 46 rows. For each target, parse `data/splits/{split}.jsonl`, select the zero-based `row_index`, and stop immediately if its live `seed_id` differs. Inspect the actual `text`; treat `original_judge_reason` as supporting context rather than the decision source.

Apply the canonical judgment rule exactly: an upfront payment or deposit, OTP disclosure, credential request, or identity-document upload required before a benefit is delivered warrants `high-risk`. Retain `suspicious` when the live text lacks such a decisive unsafe action, even if the prior verdict suggested otherwise, and explain that correction in `notes`. Do not force every row to change.

Write one compact UTF-8 JSON object per target in the target file's exact order and with the exact seven-field schema. Copy `split`, `row_index`, and `seed_id` byte-for-byte. Set `new_risk_tier` to the final judgment. Build `new_suspicious_spans` as the full final list: preserve existing valid spans in their original order and append an exact, case-sensitive substring covering the decisive unsafe action when changing to `high-risk` and that action is not already covered. Never paraphrase a span. Set `changed` true if and only if either final field differs from its current target value. Keep `notes` to one sentence of at most 18 whitespace-delimited words.

After each batch append, load the partial artifact through `load_repair_results()` and require its identities to equal the same-length prefix of the target file before continuing. If an existing partial output is encountered on resume, continue only when it passes that same schema and ordered-prefix gate; otherwise stop instead of overwriting or guessing. Do not edit `text`, `label`, `xai_explanation`, the targets file, or any live split.
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; from src.data_pipeline.apply_task_scam_risk_tier_repair import load_targets,load_repair_results,validate_coverage; tp=Path('data/processed/task-scam-risk-tier-repair-targets.jsonl'); rp=Path('data/processed/codex-task-scam-risk-tier-repair.jsonl'); targets=load_targets(tp); results=load_repair_results(rp); validate_coverage(targets,results); lines=rp.read_text(encoding='utf-8').splitlines(); assert len(lines)==187 and all(line.strip() for line in lines); assert [(r.split,r.row_index,r.seed_id) for r in results]==[(t['split'],t['row_index'],t['seed_id']) for t in targets]; assert all(r.changed==((r.new_risk_tier!=t['current_risk_tier']) or (r.new_suspicious_spans!=t['current_suspicious_spans'])) for t,r in zip(targets,results)); assert all(len(r.notes.split())&lt;=18 for r in results); print('schema/coverage/order/change/notes: PASS (187/187)')"</automated>
  </verify>
  <acceptance_criteria>
    - The output contains exactly 187 nonblank JSONL lines and no extra fields.
    - Output identity triples equal all target identity triples in the same order, once each.
    - Every row reflects a fresh judgment of the live text; no row is decided solely from the prior reason.
    - `changed` exactly reflects differences in `new_risk_tier` or the ordered full span list.
    - Every note is nonempty, one sentence, and at most 18 words.
    - No file other than `data/processed/codex-task-scam-risk-tier-repair.jsonl` is modified.
  </acceptance_criteria>
  <done>The complete ordered judgment artifact exists with 187 independently reviewed, schema-valid decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Validate fail-closed on temporary split copies and report the aggregate</name>
  <read_first>
    data/processed/codex-task-scam-risk-tier-repair.jsonl
    data/processed/task-scam-risk-tier-repair-targets.jsonl
    src/data_pipeline/apply_task_scam_risk_tier_repair.py
    tests/data_pipeline/test_apply_task_scam_risk_tier_repair.py
    data/splits/train.jsonl
    data/splits/val.jsonl
    data/splits/test.jsonl
  </read_first>
  <files>data/processed/codex-task-scam-risk-tier-repair.jsonl</files>
  <action>
Run the focused consumer tests first. Then perform the final acceptance pass with the repository's real `load_targets()`, `load_repair_results()`, `validate_coverage()`, and `apply_repair()` functions. Copy `train.jsonl`, `val.jsonl`, and `test.jsonl` into an OS-managed temporary directory and point `apply_repair()` only at those copies; never invoke the module CLI with its default live `data/splits` destination.

Before and after validation, require these locked SHA-256 values: targets `a3bb71eec149758fc193c97b3f23ddccdc1b8fdba357feb65ed00285a4c4efa3`; train `fff7293b9a5f10789fe3ea2d6a2a3d736613b4bbf32ccf375869640bdac5334c`; val `c3f2d4a6c8d561c6348060a479ad7c29869ae80912a5758d07c3b26bdf3411a3`; test `5c7cc9f86bbc6060dfaa2d0197975d0944a0e476e89727dd789059ebc589b7d6`. A mismatch is a blocking identity-drift failure, not permission to update the baseline.

The temporary apply must validate every live-row identity and every final span without error. Require `rows_changed + rows_kept_as_is == 187`. Print the final changed versus kept counts and the `suspicious_to_high_risk` versus `high_risk_to_other` counts from the validated result, then stop. Do not merge the repairs into the corpus and do not create another data artifact.
  </action>
  <verify>
    <automated>python -m pytest tests/data_pipeline/test_apply_task_scam_risk_tier_repair.py -q</automated>
    <automated>python -c "import hashlib,json,shutil,tempfile; from pathlib import Path; from src.data_pipeline.apply_task_scam_risk_tier_repair import load_targets,load_repair_results,validate_coverage,apply_repair; expected={'data/processed/task-scam-risk-tier-repair-targets.jsonl':'a3bb71eec149758fc193c97b3f23ddccdc1b8fdba357feb65ed00285a4c4efa3','data/splits/train.jsonl':'fff7293b9a5f10789fe3ea2d6a2a3d736613b4bbf32ccf375869640bdac5334c','data/splits/val.jsonl':'c3f2d4a6c8d561c6348060a479ad7c29869ae80912a5758d07c3b26bdf3411a3','data/splits/test.jsonl':'5c7cc9f86bbc6060dfaa2d0197975d0944a0e476e89727dd789059ebc589b7d6'}; hashes=lambda:{p:hashlib.sha256(Path(p).read_bytes()).hexdigest() for p in expected}; assert hashes()==expected,hashes(); targets=load_targets(Path('data/processed/task-scam-risk-tier-repair-targets.jsonl')); results=load_repair_results(Path('data/processed/codex-task-scam-risk-tier-repair.jsonl')); validate_coverage(targets,results); temp=tempfile.TemporaryDirectory(); td=Path(temp.name); [shutil.copy2(Path('data/splits')/f'{name}.jsonl',td/f'{name}.jsonl') for name in ('train','val','test')]; stats=apply_repair(results,td); assert stats['rows_changed']+stats['rows_kept_as_is']==187,stats; assert hashes()==expected,hashes(); print(json.dumps(stats,sort_keys=True)); temp.cleanup()"</automated>
  </verify>
  <acceptance_criteria>
    - The focused fail-closed consumer test module passes.
    - The real consumer accepts all 187 results against temporary split copies with zero coverage, identity, schema, or span errors.
    - All four locked input hashes are unchanged before and after validation.
    - The aggregate accounts for exactly 187 rows and reports changed, kept, suspicious-to-high-risk, and reverse counts.
    - Live `data/splits/*.jsonl` files remain untouched; no corpus merge occurs.
  </acceptance_criteria>
  <done>The sole judgment artifact passes the repository's fail-closed contract non-destructively, and its final aggregate is reported.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Targets to live splits | Target identities select mutable corpus rows; drift could attach a judgment to the wrong text. |
| Codex judgment to repair consumer | Free-form semantic decisions become structured data that a later merge can apply. |
| Local corpus to external services | Vietnamese messages and project data must remain inside this Codex session and workspace. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-J21-01 | Tampering | target/live-row join | high | mitigate | Check zero-based bounds, exact seed identity, target order, and locked SHA-256 values; stop on any drift. |
| T-J21-02 | Tampering | repair JSONL | high | mitigate | Enforce the seven-field Pydantic schema, exact coverage, ordered identities, truthful changed flags, and exact substring spans. |
| T-J21-03 | Information Disclosure | external providers | high | mitigate | Use only local reads, Codex judgment, and local validation; make no web, plugin, provider, HTTP, or external model call. |
| T-J21-04 | Denial of Service | live corpus mutation during validation | medium | mitigate | Exercise `apply_repair()` only against OS-temporary copies and recheck immutable input hashes afterward. |
| T-J21-05 | Repudiation | semantic decision provenance | medium | mitigate | Require a concise per-row note and emit aggregate transition counts from the validated artifact. |
</threat_model>

<verification>
The final gate is intentionally non-mutating: schema and coverage validation operate on the artifact, span and identity validation run through the real applier against temporary split copies, and the four input hashes must remain locked. Any failure stops the task; there is no partial acceptance or live merge fallback.
</verification>

<success_criteria>
- `data/processed/codex-task-scam-risk-tier-repair.jsonl` exists with exactly 187 ordered, nonblank JSON objects.
- All output identities, fields, risk tiers, `changed` values, note lengths, and exact spans satisfy the canonical contract.
- The focused tests and temporary-copy fail-closed apply both exit successfully.
- Changed plus kept equals 187, and transition counts are printed.
- External-provider calls are zero and all input/live split hashes remain unchanged.
</success_criteria>

## Artifacts this phase produces

- `data/processed/codex-task-scam-risk-tier-repair.jsonl` — 187 `RiskTierRepairResult`-shaped judgment records; no code symbols or new interfaces are created.

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Task | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Generate all 187 Codex repair judgments and validate them fail-closed | 1, 2 | COVERED | Exact output and non-mutating validation are both planned. |
| REQ | — | No ROADMAP requirement IDs are assigned to this quick task | — | N/A | Quick-task scope is supplied directly by the caller. |
| RESEARCH | — | No research artifact; research is explicitly prohibited for this task | — | EXCLUDED | Local established contracts are sufficient. |
| CONTEXT | — | Codex-only judgment, exact identity/order/schema, no providers, no input mutation | 1, 2 | COVERED | Caller constraints are implemented verbatim. |
| INSTRUCTIONS | — | Canonical tier rule, exact spans, full final list, truthful changed flag, short notes, aggregate | 1, 2 | COVERED | Every output rule has an action and automated gate. |
| TARGETS | 1-187 | One decision for every prepared target | 1, 2 | COVERED | Four ordered 46-47-row batches plus exact coverage validation. |

<output>
Execution creates only `data/processed/codex-task-scam-risk-tier-repair.jsonl`; corpus application is explicitly outside this quick task.
</output>
