\begin{figure}[H]
\centering
\begin{tikzpicture}[
  event/.style={draw=orange!70!black, fill=orange!8, rounded corners=3pt,
    text width=3.65cm, minimum height=1.15cm, align=center, font=\scriptsize},
  recovery/.style={draw=green!55!black, fill=green!7, rounded corners=3pt,
    text width=3.65cm, minimum height=1.15cm, align=center, font=\scriptsize},
  arr/.style={->, >=stealth, thick, black!45}
]
\node[event] (d1) at (-4.4,2.3) {One group of Zalo messages was written as narration, not as messages};
\node[recovery] (d2) at (0,2.3) {Rewritten offline from the same articles, then re-checked};
\node[event] (l1) at (4.4,2.3) {Ordinary LoRA left 9~MiB of memory free and stopped on a driver fault};

\node[recovery] (l2) at (-4.4,0) {QLoRA measured instead: room to spare and a workable step time};
\node[event] (q1) at (0,0) {First full Qwen run had a bad output folder name and one broken save path};
\node[recovery] (q2) at (4.4,0) {Restarted clean: 1{,}245 steps, checkpoint chosen, model exported};

\node[event] (p1) at (-4.4,-2.3) {PhoBERT attempts exposed logging and finish-up defects};
\node[recovery] (p2) at (0,-2.3) {Training completed; the logs were recovered without touching the results};
\node[event] (a1) at (4.4,-2.3) {Code reorganised, but the review findings are still open};

\draw[arr] (d1)--(d2);
\draw[arr] (d2)--(l1);
\draw[arr] (l1) -- ++(0,-1.15) -| (l2);
\draw[arr] (l2)--(q1);
\draw[arr] (q1)--(q2);
\draw[arr] (q2) -- ++(0,-1.15) -| (p1);
\draw[arr] (p1)--(p2);
\draw[arr] (p2)--(a1);
\end{tikzpicture}
\caption{The order in which things went wrong and what was done about each one. Orange is a failure, green is the fix. The last box is not green because those findings are still open.}
\label{fig:failure-recovery-timeline}
\end{figure}
