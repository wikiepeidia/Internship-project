# Historical walkthrough snapshot

This directory is the complete July 2026 duplicate walkthrough, including the
student's later local edit to `6_convert_to_gguf.py`. It is preserved rather than
discarded, but it is not maintained source.

The locally retained copied dataset describes an obsolete, leakage-affected
snapshot and is intentionally ignored by Git. Do not use it for current counts or
evaluation claims. Use `documents/defense/CODE_WORKFLOW.md` and the actual modules
under `src/`.
